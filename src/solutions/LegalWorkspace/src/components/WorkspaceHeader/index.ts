/**
 * WorkspaceHeader barrel export — layout switcher dropdown with settings action.
 *
 * Primary consumer: workspace page shell.
 * Provides a Fluent v9 Dropdown for switching between system and user workspace
 * layouts, plus a settings gear button and "+ New Workspace" action.
 */

export { WorkspaceHeader } from "./WorkspaceHeader";
export type {
  IWorkspaceHeaderProps,
  WorkspaceLayoutSummary,
} from "./WorkspaceHeader";
