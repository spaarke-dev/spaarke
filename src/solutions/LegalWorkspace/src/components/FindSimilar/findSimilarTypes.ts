/**
 * findSimilarTypes — re-exports from shared @spaarke/ui-components library.
 * Local implementation removed; types now live in the shared FindSimilar module.
 */
export type {
  FindSimilarDomain,
  FindSimilarStatus,
  IDocumentResult,
  IRecordResult,
  IFindSimilarResults,
  IGridRecord,
  IGridColumn,
  IFindSimilarServiceConfig,
  INavigationMessage,
} from '@spaarke/ui-components/components/FindSimilar/findSimilarTypes';
