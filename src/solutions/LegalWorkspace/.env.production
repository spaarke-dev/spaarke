# Production config is now resolved at runtime from Dataverse Environment Variables.
# See src/config/runtimeConfig.ts and resolveRuntimeConfig() from @spaarke/auth.
# No build-time env vars needed for BFF URL or MSAL Client ID.
