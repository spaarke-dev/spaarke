# Production environment variables
# BFF URL and MSAL client ID are resolved at runtime from Dataverse Environment Variables
# via resolveRuntimeConfig() — no build-time values needed for these settings.
