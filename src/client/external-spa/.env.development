# Development environment — Spaarke External Access (CIAM external tenant: spaarkeextid)
# This file is committed. Do NOT put secrets here.
# For local overrides, use .env.local (gitignored).

# BFF API base URL
VITE_BFF_API_URL=https://spaarke-bff-dev.azurewebsites.net

# MSAL / Entra External ID (CIAM) authentication (authorization code + PKCE)
# CIAM external tenant: spaarkeextid.onmicrosoft.com (7052feba-bfc4-43e0-b09e-65014b429131)
VITE_MSAL_AUTHORITY=https://spaarkeextid.ciamlogin.com/7052feba-bfc4-43e0-b09e-65014b429131
VITE_MSAL_TENANT_ID=7052feba-bfc4-43e0-b09e-65014b429131

# PENDING CIAM app registrations — task 028 ops prerequisite (see notes/defer-issues.md):
#   VITE_MSAL_CLIENT_ID  = the SPA public-client app registered IN the CIAM tenant
#   VITE_MSAL_BFF_SCOPE  = api://{ciam-bff-api-app-id}/{scope} (BFF API app exposed in the CIAM
#                          tenant; must match the BFF Ciam:Audience from task 020)
# Replace these TODO markers once the two CIAM app registrations exist. Live CIAM login is the
# deferred Phase-2 E2E; the markers keep the type-check/build green until then.
VITE_MSAL_CLIENT_ID=TODO_CIAM_SPA_CLIENT_ID
VITE_MSAL_BFF_SCOPE=api://TODO_CIAM_BFF_API_APP_ID/SDAP.Access
