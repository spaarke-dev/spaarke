/**
 * `@spaarke/communication-components/components/ReconcileTabs` — the Pillar E
 * reconcile tabs mounted in the task-053 browse-shell right pane AND on the email
 * record form. Fields = task 055 (FR-E4); Tasks = task 056 (FR-E5, later).
 */
export * from './FieldUpdateReconcileTab';
