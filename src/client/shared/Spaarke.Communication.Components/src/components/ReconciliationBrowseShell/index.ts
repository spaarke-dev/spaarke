/**
 * `@spaarke/communication-components` — ReconciliationBrowseShell barrel.
 *
 * The Pillar E reconciliation browse shell (email-communication-intelligence-r2
 * task 053): a `SprkModal` + `nav` "N of M" step-through over the Needs-review
 * queue, hosting the reused reader (left) + a reconcile-tabs slot (right). See
 * ReconciliationBrowseShell.tsx.
 */
export { ReconciliationBrowseShell } from './ReconciliationBrowseShell';
export type { ReconciliationBrowseShellProps, ReconciliationBrowseRecord } from './ReconciliationBrowseShell.types';
