/**
 * `@spaarke/communication-components/components/ReconciliationWorkspace` — the
 * Pillar E reconciliation composition (email-communication-intelligence-r2 task
 * 061): grid → browse shell (renderTabs) → Related-to / Fields / Tasks tabs, with
 * the NFR-10 gate + NFR-11 citation wiring. Mounted by task 062 in the code page
 * + a SpaarkeAi workspace widget.
 */
export { ReconciliationWorkspace } from './ReconciliationWorkspace';
export type { ReconciliationWorkspaceProps } from './ReconciliationWorkspace';
