/**
 * `EmailReadingHeader` component-folder barrel (email-communication-solution-r5
 * task 034) — the reading-pane header (FR-11) + attachments (FR-12) sub-views.
 * NOT yet wired into `../index.ts` — the orchestrator adds the Wave-5 barrel
 * export line for this folder after the parallel wave completes.
 */

export * from './EmailReadingHeader';
export * from './EmailReadingAttachments';
export * from './EmailReadingHeader.types';
