/**
 * @spaarke/daily-briefing-components — utils barrel
 *
 * Shared UI-side utility constants and helpers. R2 task 015 / FR-07 hoist
 * (Toaster id constant moved here so the package no longer reaches back
 * into the standalone DailyBriefing solution).
 */

export { TOASTER_ID } from "./toastUtils";
