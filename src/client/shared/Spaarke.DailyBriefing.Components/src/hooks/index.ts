/**
 * @spaarke/daily-briefing-components — hooks barrel
 *
 * Reusable hooks for Daily Briefing surfaces. Per ADR-012, all hooks are
 * context-agnostic — dependencies are injected via the hook's arguments.
 *
 * Will be populated by R2 task 011 (Group A hoist) with:
 *  - `useBriefingNotifications` (one of three hooks from FR-06 split)
 *  - `useBriefingPreferences`   (one of three hooks from FR-06 split)
 *  - `useBriefingActions`       (one of three hooks from FR-06 split)
 *  - `useBriefingNarration`
 *  - `useInlineTodoCreate`
 *
 * Scaffold placeholder — populated by R2 task 011.
 */

export {};
