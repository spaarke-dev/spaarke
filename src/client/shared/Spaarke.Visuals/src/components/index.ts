/**
 * @spaarke/visuals — components barrel.
 *
 * Presentational visual components land here in VHVU-041 (moved from
 * `src/client/pcf/VisualHost/control/components/`). Empty placeholder until
 * then so the package typechecks.
 */

export {};
