/**
 * @spaarke/visuals — types barrel.
 *
 * Presentational view-model types (VisualType, chart data shapes, card
 * props) land here in VHVU-041 — the single canonical `VisualType` that
 * VHVU-042 reconciles. Empty placeholder until then so the package typechecks.
 */

export {};
