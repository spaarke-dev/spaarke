/**
 * Barrel for @spaarke/smart-todo-components/types.
 *
 * Host-agnostic types — zero LegalWorkspace coupling.
 */

export type {
  ITodoRecord,
  IRegardingContext,
  IFeedSyncBridge,
  IWebApi,
} from './todo';
