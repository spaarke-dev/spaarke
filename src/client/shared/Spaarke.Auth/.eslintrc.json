{
  "root": true,
  "parser": "@typescript-eslint/parser",
  "parserOptions": {
    "ecmaVersion": "latest",
    "sourceType": "module"
  },
  "plugins": ["@typescript-eslint"],
  "ignorePatterns": ["dist/", "node_modules/", "src/**/*.d.ts", "src/**/*.js"],
  "rules": {
    "no-restricted-syntax": [
      "error",
      {
        "selector": "TemplateLiteral[quasis.0.value.raw=/Bearer\\s+$/]",
        "message": "Do not construct Authorization Bearer headers via template literals. Use authenticatedFetch() from @spaarke/auth, which is the only sanctioned site that materializes a Bearer token as a string (AUDIT-FINDINGS-AUTH-SYSTEM §4.1)."
      }
    ]
  },
  "overrides": [
    {
      "files": ["src/authenticatedFetch.ts"],
      "rules": {
        "no-restricted-syntax": "off"
      }
    }
  ]
}
