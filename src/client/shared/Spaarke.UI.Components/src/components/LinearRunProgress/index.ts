/**
 * LinearRunProgress — public barrel export.
 *
 * Presenter for Linear AI Consumer SSE events. Pairs with the
 * `useLinearRunProgress` hook (in ../../hooks). See LinearRunProgressList.tsx
 * for the rendering contract and design rationale.
 */
export { LinearRunProgressList } from './LinearRunProgressList';
export type { ILinearRunProgressListProps } from './LinearRunProgressList';
