// Widget interface types for @spaarke/ai-widgets.
// Populated by task AIPU2-071 (WorkspaceWidget + ContextWidget interface definitions).

// Placeholder export to satisfy the barrel import until task 071 runs.
export type WidgetTypesPlaceholder = never;
