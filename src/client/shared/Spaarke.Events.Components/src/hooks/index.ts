/**
 * @spaarke/events-components — hooks barrel
 *
 * Placeholder for reusable hooks. The original EventsPage hooks folder
 * contained no public hooks (all data + filter state is exposed via
 * `context/EventsPageContext.tsx`'s selector hooks). New hooks (e.g. an
 * `useEventsQuery` extracted from `GridSection`) will land here as Calendar
 * widget needs grow.
 */

export {};
