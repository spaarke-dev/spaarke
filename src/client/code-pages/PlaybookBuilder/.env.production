# Production config is resolved at runtime from Dataverse Environment Variables.
# See resolveRuntimeConfig() in @spaarke/auth.
# This file is intentionally empty — do NOT add BFF URL or MSAL client ID here.
