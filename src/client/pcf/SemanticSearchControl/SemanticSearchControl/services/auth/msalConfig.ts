/**
 * @deprecated This file is fully deprecated and retained only as a placeholder.
 * All MSAL configuration is now handled by @spaarke/auth via authInit.ts.
 * Configuration values (CLIENT_ID, TENANT_ID, BFF_APP_ID) are resolved at
 * runtime from Dataverse environment variables -- no hardcoded values.
 *
 * See authInit.ts for the runtime configuration pattern.
 */

// No exports -- all configuration is resolved at runtime via authInit.ts
// and Dataverse environment variables (environmentVariables.ts).
