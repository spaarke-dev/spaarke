/**
 * @deprecated This barrel is deprecated. Use @spaarke/auth instead.
 * All MSAL configuration is resolved at runtime via authInit.ts
 * and Dataverse environment variables. No hardcoded values remain.
 */

// No exports -- see authInit.ts for the runtime auth initialization pattern.
