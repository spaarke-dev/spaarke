/**
 * cardConfigResolver — re-export shim (VHVU-041).
 *
 * Moved to `@spaarke/visuals`. Preserves the existing
 * `'../utils/cardConfigResolver'` import in ChartRenderer (kept). Full repoint
 * in VHVU-060.
 */

export * from '../../../../shared/Spaarke.Visuals/src/utils/cardConfigResolver';
