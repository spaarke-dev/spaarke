/**
 * trendAnalysis — re-export shim (VHVU-041).
 *
 * Moved to `@spaarke/visuals`. Preserves the existing
 * `'../utils/trendAnalysis'` import in configurations/matterReportCardTrends.ts
 * (kept). Full repoint in VHVU-060.
 */

export * from '../../../../shared/Spaarke.Visuals/src/utils/trendAnalysis';
