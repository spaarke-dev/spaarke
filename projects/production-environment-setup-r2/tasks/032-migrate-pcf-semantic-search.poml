<task id="ENV-032" project="Production Environment Setup R2">
  <metadata>
    <title>Migrate SemanticSearchControl PCF to runtime config resolution</title>
    <status>completed</status>
    <estimated-effort>2 hours</estimated-effort>
    <phase>4</phase>
    <tags>pcf, typescript, auth, config</tags>
    <parallel-group>4</parallel-group>
    <dependencies>ENV-011</dependencies>
  </metadata>

  <prompt>
    Migrate SemanticSearchControl PCF control (src/client/pcf/SemanticSearchControl/) to use @spaarke/auth shared auth pattern and environmentVariables.ts for runtime config resolution. Replace the hardcoded msalConfig.ts (if present) with the shared pattern established in ENV-011. Remove any hardcoded CLIENT_ID or BFF URL from PCF source. This is the PCF control variant — a separate code page with the same name exists and is handled in ENV-027.
  </prompt>

  <role>PCF developer with Power Platform and React experience</role>

  <goal>
    SemanticSearchControl PCF control has no hardcoded CLIENT_ID or BFF URL. Authentication and API config are resolved at runtime from Dataverse environment variables using the shared @spaarke/auth pattern. The control builds successfully.
  </goal>

  <constraints>
    <constraint source="ADR-006">Field-bound PCF controls — must use React 16 APIs (platform-provided)</constraint>
    <constraint source="ADR-022">PCF: React 16 APIs only; ReactControl or StandardControl pattern; no createRoot()</constraint>
    <constraint source="project">Do not bundle React in PCF — use platform-provided React 16/17</constraint>
    <constraint source="project">Use shared environmentVariables.ts pattern from ENV-011</constraint>
    <constraint source="project">This is the PCF control variant; do not modify the code page (ENV-027)</constraint>
  </constraints>

  <knowledge>
    <files>
      <file>src/client/pcf/CLAUDE.md</file>
      <file>.claude/constraints/pcf.md</file>
      <file>src/client/pcf/shared/utils/environmentVariables.ts</file>
      <file>src/client/shared/Spaarke.Auth/src/config.ts</file>
      <file>src/client/pcf/SemanticSearchControl/SemanticSearchControl/</file>
    </files>
  </knowledge>

  <steps>
    <step order="1" name="Read current auth files">Read SemanticSearchControl auth files (msalConfig.ts, authService.ts, or similar). Identify where CLIENT_ID and BFF URL are currently defined.</step>
    <step order="2" name="Replace with shared pattern">Replace hardcoded msalConfig.ts content with imports from @spaarke/auth. Use environmentVariables.ts to read CLIENT_ID and BFF URL from PCF context properties at runtime.</step>
    <step order="3" name="Remove old msalConfig.ts">Delete or clear the old msalConfig.ts that contained hardcoded values. Ensure nothing still imports from it.</step>
    <step order="4" name="Update index.ts if needed">Ensure the PCF index.ts passes the required context properties through to the auth utilities.</step>
    <step order="5" name="Build">Run npm run build from the SemanticSearchControl directory. Confirm no TypeScript errors.</step>
  </steps>

  <outputs>
    <output type="code">src/client/pcf/SemanticSearchControl/SemanticSearchControl/</output>
  </outputs>

  <acceptance-criteria>
    <criterion testable="true">No hardcoded CLIENT_ID in SemanticSearchControl source</criterion>
    <criterion testable="true">No hardcoded BFF URL in SemanticSearchControl source</criterion>
    <criterion testable="true">Control uses @spaarke/auth and environmentVariables.ts for auth config</criterion>
    <criterion testable="true">npm run build succeeds with no TypeScript errors</criterion>
  </acceptance-criteria>
</task>
