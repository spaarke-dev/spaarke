# Smart To Do R5 — Deferrals & Discovered Issues

> Source-of-truth tracker (per project CLAUDE.md "Deferrals & Issues"). Each entry names a concrete failing behavior/contract, not "flexibility" (§11). **Before the wrap-up PR (task 090), each open entry needs a GitHub Issue URL** (`/project-defer-issue-tracking`); `push-to-github` blocks on entries lacking one.

| # | Source task | Concrete gap | Proposed fix | Status |
|---|---|---|---|---|
| D-1 | 012 | `ITodo` (`types/entities.ts`) lacks `sprk_priority`/`sprk_effort` fields, so the priority/effort card UI reads them via **2 `as unknown as` structural casts**. Casts silently break if the field logical names change. | Widen `ITodo` with optional `sprk_priority?: number` / `sprk_effort?: number`; delete the 2 casts in `components/KanbanCard/KanbanCard.tsx` + `components/SmartToDo/KanbanCard.tsx`. (Out of 012's `components/**`-only scope.) | OPEN — no GH issue yet |
| D-2 | 011 | `DataverseService` (Code Page) + `CreateTodoWizard` persist the **derived score** (`sprk_priorityscore`/`sprk_effortscore`) but do **not** persist the raw **choice** (`sprk_priority`/`sprk_effort`). A record created via wizard/quick-add has a score but no selectable label. | Decide product intent: persist the chosen `sprk_priority`/`sprk_effort` alongside the score on create. Likely folds into task 014 (deploy) or a wizard follow-up. | OPEN — no GH issue yet |
| D-3 | 002 | Two live `KanbanCard` implementations in `Spaarke.SmartTodo.Components` (widget flexbox card `components/KanbanCard/` + rich `components/SmartToDo/KanbanCard.tsx` aliased `SmartToDoKanbanCard`). Priority/effort UI (012) had to be added to BOTH. | §11 card unification — one card primitive both surfaces compose. Larger refactor; not in R5's "move 13 files" scope. | OPEN — no GH issue yet |
| D-4 | 002/003 | Hoisted package files carry pre-existing dead code (`dismissingIds`/`handleDismiss` in `components/SmartToDo/SmartToDo.tsx`, unused imports in `TodoAISummaryDialog.tsx`, a browser-`process` ref) — flagged TS6133/TS6196 under stricter consumers, kept verbatim to preserve parity. | Prune dead code + guard/remove the `process` ref. Safe cleanup pass (own small PR). | OPEN — no GH issue yet |
| D-5 | 022 | Query-string-level assertions for `buildTodoItemsQuery(includeCompleted)` not added (no Jest runner in `solutions/SmartTodo` yet). Logic verified via throwaway transpile only. | Add the assertion when task 040 wires Jest to `solutions/SmartTodo`. | ✅ RESOLVED — task 021 landed `services/__tests__/queryHelpers.test.ts`; task 040 added the combination assertions. |
| D-6 | 040 | `src/solutions/SmartTodo/jest.config.cjs` has a pre-existing typo `setupFilesAfterEach` (should be `setupFilesAfterEnv`), so `jest.setup.cjs` silently never loads (the "Unknown option" warning in every jest run). Tests currently pass without it, so fixing it changes runner behavior and must be done deliberately with a full re-run — not bundled into 040. | Rename `setupFilesAfterEach`→`setupFilesAfterEnv`; re-run the full SmartTodo suite to confirm the setup (jest-dom matchers) loading doesn't destabilize any test. Small own PR. | OPEN — no GH issue yet |
| D-7 | UAT 2026-08-17 (021/FR-06) | Operator wants the Filter changed from the structured Priority/Status/Due/Assigned-To pane to a single expanding TEXT SEARCH (name + description + assigned-to). | Replace FilterPane with an expanding search field wired to `searchQuery`; extend card predicate to match assigned-to. **Removes the FR-07 Show-Completed toggle** (operator chose "replace" over keep-Completed 2026-08-17 — FR-07 regression accepted; re-add if missed). | ✅ RESOLVED — inline SearchFilter + blended date-search deployed to `sprk_smarttodo` (commits e4cb780c4/0347dc406). |
| D-8 | UAT 2026-08-17 (030/FR-10) | Creating a To Do via "+ New Task" from within a record (Matter) context does NOT auto-associate that record — RELATED RECORD (RegardingResolver) opens empty. The standalone create path has no host-record inheritance. | Wire `handleNewTask`/`newTaskLauncher` to read the host record (parent Xrm / launch context) and reliably pre-populate the RegardingResolver (createFromEntity or verified data-param lookup pre-fill; RegardingResolver detectPrePopulatedParent then completes the 5 fields). | ✅ RESOLVED — `newTaskLauncher.ts` host-regarding inheritance (commit f8532380f); operator confirmed matter + `sprk_regardingrecordname` set. |
| D-9 | UAT 2026-08-17 (ribbon/FR-19) | Matter command-bar "Create To Do" button does NOT open the create form. Ribbon calls `openCreateTodoWizard`→`openWizardWithBff("sprk_createtodowizard", entityType=sprk_matter&entityId=…)`; the webresource IS deployed (modified 2026-08-03) → runtime failure, not missing config. | Diagnose via operator browser console (F12) on click; fix the ribbon→wizard launch OR (operator design choice) unify the Matter "Create To Do" onto the same OOB form modal as +New Task with auto-associate. | ✅ RESOLVED — root cause = live `MatterRibbons` referenced `sprk_wizard_commands.js` (webresource has no `.js`) → 404. Stripped `.js` (7 refs) + deployed `MatterRibbons` v1.0.0.1 (2026-08-17). |
| D-10 | wrap-up 2026-08-17 (FR-19/tasks 051,052) | "Create To Do" ribbon button exists on Matter only; the 5 other parent entities (Project, Event, Invoice, …) do not have it. | **DEFERRED by operator** — 5 live Dataverse ribbon deploys to dedicated `*Ribbons` solutions (same pattern as MatterRibbons); hold for when operator can eyeball. Graduation Criterion #9 unmet until done. | ⛔ DEFERRED |
| D-11 | wrap-up 2026-08-17 (FR-12/13/task 032) | To Do OOB main form does not hide the command bar / go full-cover header-hidden. | **DEFERRED** — hiding the OOB command bar also removes Save (a genuine constraint). Kept the OOB full-cover form without header-hide. Graduation Criterion #7 partially met. | ⛔ DEFERRED (design constraint) |
| D-12 | wrap-up 2026-08-17 (FR-17/task 041) | Playwright NFR suite authored + typechecks + wired, but NOT run green — no live Power Apps env in the authoring sandbox. 2 selector assumptions (card-selection, theme-toggle) need confirming. | Run `npm run test:e2e -- smart-todo` against the deployed page (`SMART_TODO_URL`); confirm assumptions; assert the 4 NFR criteria green. Graduation Criterion #10 unmet until done. See `notes/task-041-playwright-nfr.md`. | ⛔ DEFERRED (real-env run pending) |
| D-13 | 2026-08-17 (INBOUND / r3 RED-4 DEF-1) | `TodoGenerationService` Rules 1&3 fixed in code (reroute + dry-run gate) but NOT deployed to Azure; behind default-OFF `EnableEventSourcedGeneration`. | Deploy BFF → review dry-run would-create counts → flip flag; then ping the r3 dataverse-access-hardening owner so the stub→throw step proceeds. Epic #427. See `notes/INBOUND-event-sourced-todo-generation-broken.md`. | ⛔ DEFERRED (deploy + operator flag decision) |

## Notes
- D-1/D-2 are the natural fast-follows to the FR-02/03 scoring work (tasks 011/012) and should be resolved before FR-02/03 is called "done" at task 014.
- D-3/D-4 are hoist-hygiene follow-ups from FR-01 (tasks 002/003).
- All entries must be filed as GitHub Issues before the task-090 wrap-up PR.
