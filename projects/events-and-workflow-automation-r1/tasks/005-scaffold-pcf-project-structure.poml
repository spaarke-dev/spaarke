<?xml version="1.0" encoding="UTF-8"?>
<task id="005" project="events-and-workflow-automation-r1">
  <metadata>
    <title>Scaffold PCF project structure</title>
    <phase>1: Foundation &amp; Data Model</phase>
    <status>not-started</status>
    <estimated-hours>4</estimated-hours>
    <dependencies>none</dependencies>
    <blocks>020, 030, 033, 034</blocks>
    <tags>pcf, typescript, react, frontend</tags>
    <rigor-hint>FULL</rigor-hint>
    <rigor-reason>Task tags include 'pcf' (code implementation)</rigor-reason>
    <parallel-group>A</parallel-group>
    <parallel-safe>true</parallel-safe>
  </metadata>

  <prompt>
    Create the PCF project scaffolds for all 5 controls in the Events and Workflow Automation project:
    AssociationResolver, RegardingLink, EventFormController, FieldMappingAdmin, and UpdateRelatedButton.
    Set up proper TypeScript/React configuration following ADR-022 (React 16 APIs).
  </prompt>

  <role>
    SPAARKE platform developer with expertise in PCF control development using TypeScript and React.
    Follow ADR-022 strictly for React 16 APIs. Expert in Fluent UI v9 integration.
  </role>

  <goal>
    5 PCF control projects scaffolded with correct structure, TypeScript configuration, React 16
    setup, Fluent UI v9 dependencies, and build scripts. Each control should compile without errors.
  </goal>

  <context>
    <background>
      This project requires 5 PCF controls that work together to provide the Events and Field Mapping
      functionality. All controls must use React 16 APIs (per ADR-022) and Fluent UI v9 (per ADR-021)
      for consistent UI and dark mode support.
    </background>
    <relevant-files>
      <file>src/client/pcf/ - Existing PCF controls for reference</file>
      <file>src/client/pcf/CLAUDE.md - PCF development guidelines</file>
      <file>docs/guides/PCF-V9-PACKAGING.md - Version bumping instructions</file>
    </relevant-files>
  </context>

  <constraints>
    <constraint source="ADR-022">Must use React 16 APIs: ReactDOM.render(), not createRoot()</constraint>
    <constraint source="ADR-022">Must use platform-library declarations for React/Fluent UI</constraint>
    <constraint source="ADR-021">Must use Fluent UI v9, not v8</constraint>
    <constraint source="ADR-021">Must support dark mode using design tokens</constraint>
    <constraint source="ADR-012">Must import shared components from @spaarke/ui-components</constraint>
  </constraints>

  <knowledge>
    <files>
      <file>.claude/constraints/pcf.md</file>
      <file>.claude/patterns/pcf/control-initialization.md</file>
      <file>.claude/patterns/pcf/theme-management.md</file>
      <file>src/client/pcf/CLAUDE.md</file>
      <file>docs/guides/PCF-V9-PACKAGING.md</file>
    </files>
    <patterns>
      <pattern name="PCF Control Initialization" location=".claude/patterns/pcf/control-initialization.md">
        React 16 pattern with ReactDOM.render() and FluentProvider
      </pattern>
      <pattern name="Theme Management" location=".claude/patterns/pcf/theme-management.md">
        Dark mode support using Fluent UI v9 tokens
      </pattern>
    </patterns>
  </knowledge>

  <steps>
    <step order="1">Review existing PCF controls in src/client/pcf/ for reference patterns</step>
    <step order="2">Review PCF-V9-PACKAGING.md for version management requirements</step>
    <step order="3">Create AssociationResolver control scaffold using pac pcf init</step>
    <step order="4">Create RegardingLink control scaffold using pac pcf init</step>
    <step order="5">Create EventFormController control scaffold using pac pcf init</step>
    <step order="6">Create FieldMappingAdmin control scaffold using pac pcf init</step>
    <step order="7">Create UpdateRelatedButton control scaffold using pac pcf init</step>
    <step order="8">Configure tsconfig.json for each control with strict mode</step>
    <step order="9">Add Fluent UI v9 as platform-library in ControlManifest.Input.xml</step>
    <step order="10">Set up React 16 initialization pattern in each index.ts</step>
    <step order="11">Create base component structure with FluentProvider wrapper</step>
    <step order="12">Configure dark mode theme detection for each control</step>
    <step order="13">Verify npm install and npm run build succeeds for all controls</step>
    <step order="14">Update TASK-INDEX.md: change task 005 status to ✅ completed</step>
    <step order="15">Document scaffold structure in projects/events-and-workflow-automation-r1/notes/</step>
  </steps>

  <tools>
    <tool name="pac">Power Platform CLI for PCF scaffolding</tool>
    <tool name="npm">Node package manager for dependencies</tool>
    <tool name="terminal">Run shell commands</tool>
  </tools>

  <outputs>
    <output type="code">src/client/pcf/AssociationResolver/</output>
    <output type="code">src/client/pcf/RegardingLink/</output>
    <output type="code">src/client/pcf/EventFormController/</output>
    <output type="code">src/client/pcf/FieldMappingAdmin/</output>
    <output type="code">src/client/pcf/UpdateRelatedButton/</output>
  </outputs>

  <acceptance-criteria>
    <criterion testable="true">
      Given each PCF project, when I run npm install &amp;&amp; npm run build, then the build
      completes without errors.
    </criterion>
    <criterion testable="true">
      Given each ControlManifest.Input.xml, when inspected, then Fluent UI v9 is declared as
      platform-library.
    </criterion>
    <criterion testable="true">
      Given each index.ts, when inspected, then React 16 APIs are used (ReactDOM.render, not createRoot).
    </criterion>
    <criterion testable="true">
      Given each control, when inspected, then FluentProvider wraps the root component with theme
      detection.
    </criterion>
  </acceptance-criteria>

  <ui-tests>
    <test name="Scaffold Build Verification">
      <steps>
        <step>Navigate to each PCF project directory</step>
        <step>Run npm install</step>
        <step>Run npm run build</step>
        <step>Verify no TypeScript errors</step>
      </steps>
      <expected>All 5 controls build successfully with no errors</expected>
    </test>
  </ui-tests>

  <notes>
    Follow the existing ThemeEnforcer PCF control as a reference for project structure.

    Version numbers should start at 1.0.0 for initial scaffold. Follow PCF-V9-PACKAGING.md for
    version bumping when deploying.

    This task can run in parallel with tasks 003 and 004 (Parallel Group A).
  </notes>

  <execution>
    <skill>.claude/skills/task-execute/SKILL.md</skill>
    <protocol>
      Before starting this task, load all files listed in knowledge/files section.
      Follow the task-execute skill for mandatory pre-execution checklist.
    </protocol>
  </execution>
</task>
