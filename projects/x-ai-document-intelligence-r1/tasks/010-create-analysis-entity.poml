<?xml version="1.0" encoding="UTF-8"?>
<task id="010" project="ai-document-intelligence-r1">
  <metadata>
    <title>Create sprk_analysis Entity</title>
    <phase>Phase 1B: Entity Creation (Conditional)</phase>
    <status>not-started</status>
    <estimated-hours>4</estimated-hours>
    <dependencies>005</dependencies>
    <blocks>020, 021</blocks>
    <tags>dataverse, entity-creation</tags>
    <condition>Execute only if Task 005 indicates entity is missing</condition>
  </metadata>

  <prompt>
    Create the sprk_analysis entity in Dataverse. This is the core entity for the AI Analysis feature, representing an analysis session tied to a document.

    CONDITIONAL: Only execute if verification (Task 005) shows this entity is missing.

    Entity should include relationships to:
    - sprk_document (N:1 - analysis belongs to document)
    - sprk_analysisaction (N:1 - analysis uses one action)
    - systemuser (N:1 - created by, modified by)
  </prompt>

  <role>
    Power Platform developer with Dataverse entity modeling expertise.
  </role>

  <goal>
    A fully configured sprk_analysis entity with all columns, relationships, and forms ready for use.
  </goal>

  <context>
    <background>
      The Analysis entity is the central entity for the AI feature. It stores the analysis configuration, status, and results. The BFF API already has code expecting this entity.
    </background>
    <dependencies>
      <dependency task="005" status="required">Verification must confirm entity is missing</dependency>
    </dependencies>
  </context>

  <constraints>
    <constraint source="project">Use "sprk_" prefix for schema name</constraint>
    <constraint source="project">Follow existing entity patterns in solution</constraint>
    <constraint source="project">Include audit columns (created, modified)</constraint>
  </constraints>

  <knowledge>
    <files>
      <file>projects/ai-document-intelligence-r1/spec.md</file>
      <file>projects/ai-document-intelligence-r1/CODE-INVENTORY.md</file>
      <file>.claude/skills/dataverse-deploy/SKILL.md</file>
    </files>
  </knowledge>

  <steps>
    <step order="1">Verify Task 005 indicates entity is missing (STOP if it exists)</step>
    <step order="2">Open Power Platform maker portal</step>
    <step order="3">Create entity with columns from spec.md</step>
    <step order="4">Create relationships (document, action, user)</step>
    <step order="5">Create main form and views</step>
    <step order="6">Add to solution</step>
    <step order="7">Update TASK-INDEX.md</step>
  </steps>

  <tools>
    <tool name="power-platform-maker">Create Dataverse entities</tool>
    <tool name="pac">Power Platform CLI</tool>
  </tools>

  <outputs>
    <output type="entity">sprk_analysis entity in Dataverse</output>
    <output type="docs">tasks/TASK-INDEX.md (status update)</output>
  </outputs>

  <acceptance-criteria>
    <criterion testable="true">Entity created with correct schema name</criterion>
    <criterion testable="true">All required columns present</criterion>
    <criterion testable="true">Relationships configured correctly</criterion>
    <criterion testable="true">Main form and views created</criterion>
  </acceptance-criteria>
</task>
