<?xml version="1.0" encoding="UTF-8"?>
<task id="053" project="spaarke-daily-update-service-r2">
  <metadata>
    <title>Migrate `DailyBriefing` solution to auth factory; delete local `authInit`</title>
    <phase>DD: De-Duplication</phase>
    <status>not-started</status>
    <estimated-hours>1</estimated-hours>
    <dependencies>052</dependencies>
    <blocks>054</blocks>
    <tags>auth, frontend, refactoring</tags>
    <rigor-hint>FULL</rigor-hint>
    <rigor-reason>Tag auth triggers FULL; modifies main.tsx auth init order</rigor-reason>
    <parallel-group>none</parallel-group>
    <parallel-safe>false</parallel-safe>
    <parallel-reason>Sequential after 052; modifies main.tsx (file-overlap with 055 risk)</parallel-reason>
  </metadata>

  <prompt>
    Update `src/solutions/DailyBriefing/src/main.tsx` to call `createCodePageAuthInitializer(config)` instead of the local `authInit.ts`. Delete `src/solutions/DailyBriefing/src/authInit.ts`. Build green; auth flow unchanged (verified by dev serve).
  </prompt>

  <role>SPAARKE platform developer. Expert in code page bootstrap + Spaarke Auth v2 consumption.</role>

  <goal>`DailyBriefing` solution uses the shared factory; local `authInit.ts` deleted; auth bootstrap behavior identical.</goal>

  <context>
    <background>
      First of 3 solution migrations. Sequential to keep merge cost low + catch any factory-vs-local divergence early.
    </background>
    <relevant-files>
      <file>src/solutions/DailyBriefing/src/main.tsx</file>
      <file>src/solutions/DailyBriefing/src/authInit.ts</file>
    </relevant-files>
  </context>

  <constraints>
    <constraint source="ADR-028">Use `@spaarke/auth` factory; no per-solution auth init code.</constraint>
    <constraint source="project">FR-20 acceptance: zero `authInit.ts` files under `src/solutions/` after task 054 + 055 complete.</constraint>
  </constraints>

  <knowledge>
    <files>
      <file>docs/adr/ADR-028-spaarke-auth-architecture.md</file>
      <file>.claude/patterns/auth/spaarke-sso-binding.md</file>
      <file>projects/spaarke-daily-update-service-r2/spec.md</file>
      <file>projects/spaarke-daily-update-service-r2/CLAUDE.md</file>
    </files>
  </knowledge>

  <steps>
    <step order="1">Read `DailyBriefing/src/authInit.ts` and `main.tsx`.</step>
    <step order="2">Replace local `authInit` call with `createCodePageAuthInitializer({ ... config ... })`.</step>
    <step order="3">Delete `src/solutions/DailyBriefing/src/authInit.ts`.</step>
    <step order="4">Run `npm run build` in `DailyBriefing/`; verify green.</step>
    <step order="5">Dev serve; smoke test auth flow.</step>
    <step order="6">Update TASK-INDEX.md: change task 053 status from 🔲 to ✅.</step>
  </steps>

  <tools>
    <tool name="npm">Build + dev server.</tool>
  </tools>

  <outputs>
    <output type="code">src/solutions/DailyBriefing/src/main.tsx</output>
  </outputs>

  <acceptance-criteria>
    <criterion testable="true">`src/solutions/DailyBriefing/src/authInit.ts` deleted.</criterion>
    <criterion testable="true">`main.tsx` calls `createCodePageAuthInitializer`.</criterion>
    <criterion testable="true">Build green; auth flow works.</criterion>
  </acceptance-criteria>

  <notes>
    If auth init breaks, the factory's call sequence diverged from the local. Restore from git, surface divergence in `notes/`, and patch the factory (task 052) before retrying.
  </notes>

  <execution>
    <skill>.claude/skills/task-execute/SKILL.md</skill>
    <protocol>Before starting this task, load all files listed in &lt;knowledge&gt;&lt;files&gt;. Follow the task-execute skill for mandatory pre-execution checklist.</protocol>
  </execution>
</task>
