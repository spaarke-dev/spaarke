<?xml version="1.0" encoding="UTF-8"?>
<task id="054" project="spaarke-daily-update-service-r2">
  <metadata>
    <title>Migrate `LegalWorkspace` + `SpaarkeAi` to auth factory; delete local `authInit`</title>
    <phase>DD: De-Duplication</phase>
    <status>not-started</status>
    <estimated-hours>2</estimated-hours>
    <dependencies>053</dependencies>
    <blocks>055</blocks>
    <tags>auth, frontend, refactoring</tags>
    <rigor-hint>FULL</rigor-hint>
    <rigor-reason>Tag auth triggers FULL; modifies 2 main.tsx files; preserves P1 seam wiring</rigor-reason>
    <parallel-group>none</parallel-group>
    <parallel-safe>false</parallel-safe>
    <parallel-reason>Sequential after 053; modifies main.tsx files (file-overlap with 055)</parallel-reason>
  </metadata>

  <prompt>
    Update `src/solutions/LegalWorkspace/src/main.tsx` and `src/solutions/SpaarkeAi/src/main.tsx` to use `createCodePageAuthInitializer`. Delete both solution-local `authInit.ts` files. CRITICAL: Preserve SpaarkeAi's P1 seam wiring (`loadSpaarkeAiNotificationContext` injection from task 002).
  </prompt>

  <role>SPAARKE platform developer. Expert in Spaarke Auth v2 + LegalWorkspace + SpaarkeAi bootstrap.</role>

  <goal>Both solutions use the shared factory; both local `authInit.ts` files deleted; build green; SpaarkeAi P1 seam preserved.</goal>

  <context>
    <background>
      Second of 2 batches in the auth factory migration. Both solutions migrated together because they share LegalWorkspace section-registration plumbing.
    </background>
    <relevant-files>
      <file>src/solutions/LegalWorkspace/src/main.tsx</file>
      <file>src/solutions/LegalWorkspace/src/authInit.ts</file>
      <file>src/solutions/SpaarkeAi/src/main.tsx</file>
      <file>src/solutions/SpaarkeAi/src/authInit.ts</file>
    </relevant-files>
  </context>

  <constraints>
    <constraint source="ADR-028">Spaarke Auth v2 canonical factory consumption.</constraint>
    <constraint source="project">CRITICAL: preserve SpaarkeAi's `loadSpaarkeAiNotificationContext` injection from task 002 (P1 seam).</constraint>
    <constraint source="project">FR-20 acceptance: `find src/solutions -name "authInit.ts"` returns zero after this task.</constraint>
  </constraints>

  <knowledge>
    <files>
      <file>docs/adr/ADR-028-spaarke-auth-architecture.md</file>
      <file>.claude/patterns/auth/spaarke-sso-binding.md</file>
      <file>projects/spaarke-daily-update-service-r2/spec.md</file>
      <file>projects/spaarke-daily-update-service-r2/CLAUDE.md</file>
    </files>
  </knowledge>

  <steps>
    <step order="1">Replace LegalWorkspace `main.tsx` local auth init with `createCodePageAuthInitializer`.</step>
    <step order="2">Delete `LegalWorkspace/src/authInit.ts`. Build LegalWorkspace; verify green.</step>
    <step order="3">Replace SpaarkeAi `main.tsx` local auth init with `createCodePageAuthInitializer`. PRESERVE the `loadSpaarkeAiNotificationContext` injection wired in task 002.</step>
    <step order="4">Delete `SpaarkeAi/src/authInit.ts`. Build SpaarkeAi; verify green.</step>
    <step order="5">Smoke test SpaarkeAi workspace pane — Daily Briefing still renders notifications (P1 seam preserved).</step>
    <step order="6">Verify `find src/solutions -name "authInit.ts"` returns zero results.</step>
    <step order="7">Update TASK-INDEX.md: change task 054 status from 🔲 to ✅.</step>
  </steps>

  <tools>
    <tool name="npm">Build + dev server.</tool>
    <tool name="terminal">Find + grep + file ops.</tool>
  </tools>

  <outputs>
    <output type="code">src/solutions/LegalWorkspace/src/main.tsx</output>
    <output type="code">src/solutions/SpaarkeAi/src/main.tsx</output>
  </outputs>

  <acceptance-criteria>
    <criterion testable="true">`find src/solutions -name "authInit.ts"` returns zero.</criterion>
    <criterion testable="true">Both solutions build green; auth flows work.</criterion>
    <criterion testable="true">SpaarkeAi Daily Briefing P1 seam preserved (notifications still render).</criterion>
  </acceptance-criteria>

  <notes>
    Highest-risk DD task — touches auth init order across 2 solutions including SpaarkeAi which is sensitive to P1 wiring. Take it slow.
  </notes>

  <execution>
    <skill>.claude/skills/task-execute/SKILL.md</skill>
    <protocol>Before starting this task, load all files listed in &lt;knowledge&gt;&lt;files&gt;. Follow the task-execute skill for mandatory pre-execution checklist.</protocol>
  </execution>
</task>
