# Task Index — visual-host-create-button-r1

> Generated by project-pipeline 2026-07-08. Status: 🔲 not-started · 🔄 in-progress · ✅ complete
> **Execution model**: Sonnet 5 @ xhigh default; `opus`-tier tasks (014, 020–024) auto-escalate.

## Tasks

| # | Status | Title | Phase | Rigor | Model | Deps | Parallel |
|---|---|---|---|---|---|---|---|
| 001 | ✅ | Discovery + schema/manifest validation | 0 | STANDARD | sonnet | — | — |
| 002 | ✅ | Schema deltas: `sprk_regardingrecordnumber` no-op + **new** `sprk_document`→`sprk_event` lookup + **new** `sprk_kpiassessment.sprk_regardingrecordurl` (owner-approved 2026-07-08 per 001 findings) | 0 | STANDARD | sonnet | 001 | — |
| 010 | ✅ | Read chart-def columns → `ConfigurationLoader` + `ChartDefinition` | A | FULL | sonnet | 001 | A |
| 011 | ✅ | `WizardRegistry` + `WizardHostProps` + resolveWizard + toast | A | FULL | sonnet | 001 | A |
| 013 | ✅ | `EntityCreationService.createDocumentRecords` multi-bind | A | FULL | sonnet | 001 | A |
| 014 | ✅ | **Migrate `eventService` → `applyResolverFields`** + `lockAssociation` | A | FULL | **opus** | 001 | A |
| 012 | ✅ | "+" button (`CardChrome` + `VisualHostRoot`) + Dialog host + seed | A | FULL | sonnet | 010,011 | — |
| 015 | ✅ | Wire `event` key + PCF version bump + deploy + Event smoke (incl. fixing hardcoded nav-prop string) | A | FULL | sonnet | 002,012,013,014 | — |
| 020 | ✅ | Build shared `WizardFollowOns` module + net-new `AddTodoFollowOnStep` | D | FULL | **opus** | 015 | — |
| 021 | ✅ | Migrate `CreateRecordWizard` → `WizardFollowOns` (delete dup) | D | FULL | **opus** | 020 | D |
| 022 | ✅ | Migrate `CreateMatterWizard` → `WizardFollowOns` (delete dup) | D | FULL | **opus** | 020 | D |
| 023 | ✅ | Migrate `CreateWorkAssignmentWizard` → `WizardFollowOns` (delete dup) | D | FULL | **opus** | 020 | D |
| 024 | ✅ | Migrate `SummarizeFilesWizard` → `WizardFollowOns` (delete dup) | D | FULL | **opus** | 020 | D |
| 030 | ✅ | `CreateInvoiceWizard` + `invoiceService` (end-to-end) | B | FULL | sonnet | 021 | E |
| 040 | ✅ | `CreateReportCardWizard` + `reportCardService` (no files; **retargeted from `sprk_kpiassessment` to `sprk_reportcard`, owner decision 2026-07-08** — see `notes/field-manifests/reportcard.md`) | C | FULL | sonnet | 021 | E |
| 050 | ✅ | Final VisualHost deploy + 3-wizard smoke (Event/Invoice/Report Card) | B/C | FULL | sonnet | 030,040 | — |
| 090 | ✅ | Wrap-up: /test-diet, valid-keys note, README→Complete, lessons-learned, PR | E | STANDARD | sonnet | 050 | — |

## Parallel Execution Groups

| Group | Tasks | Prerequisite | Notes |
|---|---|---|---|
| **A** | 010, 011, 013, 014 | 001 ✅ | Independent files (VisualHost config / registry / EntityCreationService / eventService). 014 dispatched to **opus**, rest **sonnet**. |
| **D** | 021, 022, 023, 024 | 020 ✅ | Per-wizard migration, different folders + delete-own-copy. All **opus**. Migrate+delete per wizard (not big-bang) for regression isolation. |
| **E** | 030, 040 | 021 ✅ | Two new wizards, independent. Both **sonnet**, wrap `CreateRecordWizard`. |

Serial (not grouped): 002 (deps 001, runs alongside Group A) → 012 (deps 010,011) → 015 (deps 002,012,013,014) → 020 → [Group D] + [Group E] → 050 → 090.

## Critical Path

001 → 002 → 015 → 020 → 021 → 030/040 → 050 → 090 (002 now gates 015's Event dual-bind smoke in addition to 040's KPI build; the opus-heavy spine remains 014/020–024).

## High-Risk Items

- **002** (schema deltas): 2 new additive columns (owner-approved 2026-07-08, path A both) — `sprk_document`→`sprk_event` lookup, `sprk_kpiassessment.sprk_regardingrecordurl`. See `notes/field-manifests/event.md` + `kpi.md`.
- **014** (Event resolver migration): regression-test existing Event creation surfaces; ADR-024 5-field + mutual-exclusion. `opus`.
- **015**: also fixes a pre-existing hardcoded-nav-prop bug in `CreateEventWizard.tsx` (document dual-bind previously silently failed).
- **020–024** (WizardFollowOns consolidation): touches 3 existing working wizards; per-wizard regression pass; config-driven grid preserves each card set. `opus`.
- **050**: VisualHost redeploy bundles the lazy-loaded wizard chunks — verify bundle delta < 5 KB (registry) and all 3 wizards load.

## Owner gates
- After **001**: owner signs off validated field manifests before 030/040 build. ✅ **Signed off 2026-07-08** — both schema gaps (Event doc lookup, KPI regardingrecordurl) resolved via Path A; task 002 scope expanded accordingly.
- After **015**: ⚠️ **Human UI smoke test pending.** VisualHost v1.4.28 (with the "+" button + Event flow) is deployed to spaarkedev1, but no browser (`--chrome`) was available to the task 015 subagent, so the actual click-through (hidden Associate-To step, Event resolver fields populated, Matter+Event document dual-bind) was NOT verified live — only statically (types, unit tests, code-review, adr-check, live-schema describe checks). Owner chose (2026-07-08) to proceed to Phase D and defer to task 050's final 3-wizard smoke.
- After **023**: ✅ **Ratified 2026-07-08** (CLAUDE.md §6.5 Path A). Task 023 did NOT delete `AssignWorkStep.tsx`/`CreateFollowOnEventStep.tsx`. Main-session investigation traced the actual call sites and confirmed these are NOT duplicates of the shared `WizardFollowOns` steps despite the shared name: the local "Assign Work" step feeds extra field-groups into the SAME `createWorkAssignment(...)` call that creates the work assignment itself (an in-flight field extension, not a second record), while the shared `AssignWorkFollowOnStep` creates an independent NEW child `sprk_workassignment` for other wizards' follow-ons. Same for "Create Event" (local needs Final-Due-Date + Assigned-To; the generic form doesn't). Owner accepted the exception as documented.
- Post-Group-D cleanup (2026-07-08, main session): confirmed via project-wide grep that `CreateRecordWizard/steps/SendEmailStep.tsx` was truly orphaned after task 023 finished migrating off it (021's own report was based on a stale mid-flight read of a sibling's in-progress file) — deleted it + its 2 barrel exports. Shared-lib build + full test suite re-verified clean (same 7 pre-existing unrelated failures as before Group D — no new regressions).
- **Scope change (2026-07-08, owner decision, before task 040 dispatch)**: task 040 retargeted from `sprk_kpiassessment` directly to `sprk_reportcard` (the parent review artifact KPI Assessments belong to). `sprk_reportcard` is already fully ADR-024 resolver-ready (better than the original KPI target — zero schema delta for the resolver itself). New field manifest: `notes/field-manifests/reportcard.md`. Registry key renamed `kpi-assessment` → `report-card` in `wizardRegistry.ts` (main session; `ENTITY_TO_WIZARD_KEY` maps both `sprk_reportcard` and `sprk_kpiassessment` to it). One new schema addition folded into task 040: `sprk_todo` → `sprk_reportcard` lookup (owner-approved Path A) so the Add To Do follow-on works. Old task file `040-create-kpiassessment-wizard.poml` removed; replaced by `040-create-reportcard-wizard.poml`.
- **Task 050 complete (2026-07-08)**: VisualHost deployed to spaarkedev1 as v1.4.29 (bundle 1.269 MiB, +77.5 KiB vs. the post-task-012 baseline — modest, expected). No browser tool available, so the subagent verified end-to-end by actually creating real Dataverse test records replicating each service's exact payload — Event/Invoice/Report Card all confirmed regarding the host with resolver fields populating correctly, document dual-bind confirmed for Event+Invoice, Add To Do confirmed working against the new `sprk_regardingreportcard` lookup. **Found**: `sprk_recordtype_ref` has no catalog row for `sprk_reportcard`, so a To Do regarding a Report Card only gets 3 of 5 resolver fields (graceful-blank on type + number, confirmed via live read-back — not a crash). Flagged for `/project-defer-issue-tracking` at wrap-up, not a blocker. **6 test records left on spaarkedev1** (`ZZTEST-050-...`) — subagent couldn't delete them (needs interactive consent); owner approved cleanup, all 6 deleted by main session (2026-07-08): sprk_todo, 2× sprk_document, sprk_reportcard, sprk_event, sprk_invoice.
- **Task 090 complete (2026-07-09)**: Live browser UAT (2 rounds) after 050's deploy found 3 lookup bugs + a dialog-sizing bug. Dialog sizing fixed (60vw×70vh, matching the repo-wide ribbon-launch standard, corrected from an initial wrong 95vw/70vh guess). 2 of 3 lookup bugs root-caused + fixed as v1.4.32 (`CreateEventWizard.tsx`'s Matter Type/Practice Area fields fell back to a silent no-op `EMPTY_SEARCH` because the wizard never wired those 2 config keys; a second bug in the same file's local, duplicated org-search helper queried a nonexistent field). The 3rd (Invoice Vendor Organization) has no code-level defect found after two full passes — filed as ISS-01 / GitHub #587, owner-approved to resolve in a follow-on session. `/test-diet`: 163 tests reviewed, 0 scaffolding. See `notes/lessons-learned.md` for the full retrospective.
- **Group E complete (2026-07-08)**: 030 + 040 dispatched in parallel (both dep 021 ✅). Task 040 discovered the real `sprk_todo` regarding-lookup SchemaName convention is `sprk_Regarding{Entity}` (not the field manifest's speculative guess) and created `sprk_RegardingReportCard` accordingly — verified via metadata query before use. Task 040 also made a good ADR-024-compliant call: its Assign Work follow-on does NOT link the created work assignment back to the Report Card, because `sprk_workassignment` has no entity-specific lookup for `sprk_reportcard` — writing partial resolver fields would itself violate ADR-024, so it declined (Path C, documented). Both tasks extended `CreateRecordWizard` additively (030: opt-in `followOnCards` override; 040: opt-in `hideFilesStep`) — concurrent edits verified to coexist cleanly (shared-lib `tsc` + full test suite clean post-merge: 107/114 suites, 1749/1765 tests — same 7 pre-existing unrelated failures as the pre-Phase-D baseline, no new regressions, count grew appropriately with new tests).
