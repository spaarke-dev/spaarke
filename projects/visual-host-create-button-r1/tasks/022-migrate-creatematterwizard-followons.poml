<?xml version="1.0" encoding="UTF-8"?>
<task id="022" project="visual-host-create-button-r1">
  <metadata>
    <title>Migrate CreateMatterWizard onto WizardFollowOns (delete duplicate)</title>
    <phase>D: WizardFollowOns Consolidation</phase>
    <status>not-started</status>
    <estimated-hours>3</estimated-hours>
    <dependencies>020</dependencies>
    <blocks>none</blocks>
    <tags>shared-lib, consolidation, regression</tags>
    <rigor-hint>FULL</rigor-hint>
    <rigor-reason>Migrates an existing working wizard</rigor-reason>
    <model-tier>opus</model-tier>
    <model-tier-reason>Regression-sensitive migration of a live wizard onto the shared module</model-tier-reason>
    <parallel-group>D</parallel-group>
    <parallel-safe>true</parallel-safe>
  </metadata>

  <prompt>Migrate CreateMatterWizard to consume WizardFollowOns and delete its local NextStepsStep.tsx + SendEmailStep.tsx copies.</prompt>
  <role>SPAARKE shared-UI developer. Follow §11.</role>
  <goal>CreateMatterWizard renders Next Steps via WizardFollowOns; its duplicate copies removed; Matter creation + follow-ons unchanged.</goal>

  <context>
    <background>design §5.9. CreateMatterWizard has its own NextStepsStep + SendEmailStep copies. Preserve its card set + behavior.</background>
    <relevant-files>
      src/client/shared/Spaarke.UI.Components/src/components/CreateMatterWizard/ (NextStepsStep.tsx, SendEmailStep.tsx, matterService.ts)
      src/client/shared/Spaarke.UI.Components/src/components/WizardFollowOns/
    </relevant-files>
  </context>

  <knowledge>
    <files>
      projects/visual-host-create-button-r1/design.md
      src/client/shared/Spaarke.UI.Components/src/components/CreateMatterWizard/NextStepsStep.tsx
      src/client/shared/Spaarke.UI.Components/src/components/WizardFollowOns/index.ts
    </files>
  </knowledge>

  <steps>
    <step>Swap CreateMatterWizard's local NextStepsStep for WizardFollowOns FollowOnGrid + its FollowOnCardConfig[].</step>
    <step>Delete NextStepsStep.tsx + SendEmailStep.tsx; fix imports.</step>
    <step>Regression: create a Matter, exercise each Next Step card.</step>
    <step>Build + tests.</step>
  </steps>

  <tools>Read, Grep, Edit, Bash, Skill(code-review)</tools>
  <outputs>CreateMatterWizard on WizardFollowOns; duplicates deleted.</outputs>

  <acceptance-criteria>
    <criterion>No local NextStepsStep/SendEmailStep in CreateMatterWizard (grep clean).</criterion>
    <criterion>Matter creation + follow-ons unchanged (regression pass).</criterion>
    <criterion>Shared lib builds.</criterion>
  </acceptance-criteria>
</task>
