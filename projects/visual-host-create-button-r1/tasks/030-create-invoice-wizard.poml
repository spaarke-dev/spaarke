<?xml version="1.0" encoding="UTF-8"?>
<task id="030" project="visual-host-create-button-r1">
  <metadata>
    <title>CreateInvoiceWizard + invoiceService (end-to-end)</title>
    <phase>B: CreateInvoiceWizard</phase>
    <status>not-started</status>
    <estimated-hours>4</estimated-hours>
    <dependencies>021</dependencies>
    <blocks>050</blocks>
    <tags>shared-lib, wizard, adr-024, frontend</tags>
    <rigor-hint>FULL</rigor-hint>
    <rigor-reason>New wizard + service creating sprk_invoice</rigor-reason>
    <model-tier>sonnet</model-tier>
    <model-tier-reason>Single-component wizard following the canonical workAssignmentService.ts reference; explicit manifest + contract</model-tier-reason>
    <parallel-group>E</parallel-group>
    <parallel-safe>true</parallel-safe>
  </metadata>

  <prompt>Build CreateInvoiceWizard (wrapping CreateRecordWizard) + invoiceService, following the canonical workAssignmentService.ts. Steps: Associate To → Add Files → Enter Info → Next Steps. Associate via applyResolverFields; dual-bind documents; cards Send Email / Add To Do / Assign Work; wire the invoice registry key.</prompt>
  <role>SPAARKE shared-UI developer + ADR-024. Copy the canonical service pattern exactly.</role>
  <goal>"+" on an Invoice-bound visual creates a valid sprk_invoice regarding the host via applyResolverFields; Enter Info collects the owner manifest fields; uploaded docs dual-bind to host + Invoice; useAiPrefill wired but inert (prefillEnabled=false).</goal>

  <context>
    <background>design §5.4/§5.6/§5.7/§5.8; FR-10/12/16. Invoice is resolver-ready (owner-confirmed). Manifest: sprk_invoicenumber, sprk_name, sprk_description, sprk_vendororg→sprk_organization, sprk_invoicedate (default today). Follow workAssignmentService.ts: nav-prop discovery → payload → BU defaults → applyResolverFields → createRecord → file pipeline → warnings.</background>
    <relevant-files>
      src/client/shared/Spaarke.UI.Components/src/components/CreateInvoiceWizard/ (new)
      src/client/shared/Spaarke.UI.Components/src/components/CreateWorkAssignmentWizard/workAssignmentService.ts (CANONICAL)
      src/client/shared/Spaarke.UI.Components/src/components/CreateRecordWizard/CreateRecordWizard.tsx
      src/client/shared/Spaarke.UI.Components/src/components/WizardRegistry/wizardRegistry.ts
    </relevant-files>
  </context>

  <knowledge>
    <files>
      projects/visual-host-create-button-r1/spec.md
      projects/visual-host-create-button-r1/design.md
      projects/visual-host-create-button-r1/notes/field-manifests/invoice.md
      src/client/shared/Spaarke.UI.Components/src/components/CreateWorkAssignmentWizard/workAssignmentService.ts
      src/client/shared/Spaarke.UI.Components/src/services/PolymorphicResolverService.ts
      src/client/shared/Spaarke.UI.Components/src/services/EntityCreationService.ts
      src/client/shared/Spaarke.UI.Components/src/hooks/useAiPrefill.ts
      .claude/adr/ADR-024-polymorphic-resolver-pattern.md
    </files>
  </knowledge>

  <steps>
    <step>Create invoiceService.createInvoice mirroring workAssignmentService: discover sprk_invoice nav-props, build payload from the manifest (sprk_invoicedate default today), BU defaults, applyResolverFields (Matter/Project), createRecord('sprk_invoice'), file pipeline with dual-bind (Invoice + host via createDocumentRecords additionalBinds), warnings.</step>
    <step>Create CreateInvoiceWizard wrapping CreateRecordWizard: associateToStep config, CreateInvoiceStep (Enter Info with useAiPrefill wired but prefillEnabled=false), Next Steps cards (Send Email/Add To Do/Assign Work) via WizardFollowOns.</step>
    <step>Wire wizardRegistry 'invoice' → CreateInvoiceWizard (replace placeholder from task 011).</step>
    <step>Unit tests per ADR-038.</step>
  </steps>

  <tools>Read, Grep, Edit, Write, Skill(adr-check), Skill(code-review)</tools>
  <outputs>components/CreateInvoiceWizard/ + registry entry + tests.</outputs>

  <acceptance-criteria>
    <criterion>Submitting creates a valid sprk_invoice regarding the host with all resolver fields.</criterion>
    <criterion>Enter Info collects exactly the manifest fields; invoicedate defaults to today.</criterion>
    <criterion>Uploaded file dual-binds (host + Invoice subgrids).</criterion>
    <criterion>Prefill seam present but inert; adr-check clean.</criterion>
  </acceptance-criteria>

  <ui-tests>
    <test name="Invoice create from host visual">
      <steps><step>Open an Invoice-bound visual on a Matter</step><step>Click "+"; complete wizard; submit</step><step>Inspect the Invoice regarding + resolver fields</step></steps>
      <expected>Valid sprk_invoice regarding the host; no console errors</expected>
    </test>
  </ui-tests>
</task>
