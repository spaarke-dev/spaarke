# Success-Criteria Verification — spaarke-side-pane-navigation-history-r1

**Verified**: 2026-08-15 · **Env**: spaarkedev1 · **Method**: owner UAT (light+dark) + Jest behavioral tests + deployment evidence.

Maps each spec [Success Criterion 1–12](../spec.md#success-criteria) to concrete evidence.

| # | Criterion (abbrev) | Status | Evidence |
|---|---|---|---|
| 1 | Docked Navigator opens in one gesture from any MDA page; Recent (Viewed) populated by navigation with **no form code** | ✅ | Auto-load via entity-ribbon `EnableRule` (silent) + code-page registrar; capture poll `navigatorCaptureService` writes `history` `sprk_navitem` rows on navigation. Owner UAT: Recent populated by browsing. No form web resource deployed (only the app-startup bootstrap + code page). Tests: `navigatorCaptureService.test.ts`, `RecentTab.test.tsx`. |
| 2 | Recent → Edited from `modifiedon`, **no audit entity** | ➖ (descoped in UAT) | The Viewed/Edited toggle was **removed in the UAT redesign** (owner decision: "keep only captured Viewed"). `editedByMeService` (`modifiedby=me`, no audit entity) remains in the repo + tested; its `CORE_ENTITY_SET` feeds live search. No audit entity used anywhere (constraint honored). Deviation documented in `lessons-learned.md` + `notes/`. |
| 3 | Starring = **per-user** pin, independent of `sprk_monitor` | ✅ | `pinService` writes `sprk_navitem` (UserOwned) only; never writes `sprk_monitor` (project HARD MUST-NOT, asserted in `pinService.test.ts`). **2-user isolation verified** with non-sys-admin users (owner, 2026-08-15) — task 021. |
| 4 | Pin survives sign-out + appears on another device | ✅ | Pins persist as `sprk_navitem` Dataverse rows (server-side, per-user) — inherently cross-device/cross-session. Owner UAT confirmed bookmarks persist across reloads. |
| 5 | "Pin this page" (no typing) + "+ Add bookmark" URL parse (record/view → logical target; non-Dataverse URL raw, opens new tab) | ✅ | `bookmarkService.pinCurrentPage` + `addBookmark` + `urlParse`; weblink opens via `window.open(_blank, noopener)`. Tests: `bookmarkService.test.ts` (21), `urlParse.test.ts` (14). UAT: pin-this-page + pasted-link both worked; names resolve to real record/page names (UAT fix). |
| 6 | Monitored group lists monitor-flagged records, distinct from pins | ✅ | `MonitoredTab` + `monitoredService` (read-only over `sprk_monitor`), **access-based** scope (owner ∪ BU ∪ team ∪ shared via Dataverse security trim — post-UAT fix so it works on BU-owned production records). Distinct tab from Bookmarks. Tests: `monitoredService.test.ts` (12), `MonitoredTab.test.tsx` (10). |
| 7 | History >30 days pruned on next capture write | ✅ | `navigatorCaptureService` prune-on-write (`deleteHistoryItemsOlderThan`, `HISTORY_RETENTION_DAYS=30`); no job, no plugin. Tests: `navigatorCaptureService.test.ts`, `retention.verify.test.ts` (seed old row → capture → confirm deletion). |
| 8 | Search fuzzy-matches Recent/Pinned/Views live + live Dataverse fallback; accelerator focuses; Enter navigates | ✅ | `QuickSwitcher` + `navigatorSearchIndex` (local) + `liveSearchService` (escalation) + Ctrl/Cmd+K. Tests: `QuickSwitcher.test.tsx` (14). UAT: search box functional. |
| 9 | Views tab lists saved views grouped by entity; click opens grid with that view | ✅ | `ViewsTab` reuses `ViewService`; `openView` → `navigateTo({pageType:'entitylist', viewType:'userquery'})` opens the real view **in-app** (UAT fix — was opening default/new-tab). Tests: `ViewsTab.test.tsx` (9). UAT: "views are now working" (owner). |
| 10 | Lost-access record does not display its cached name | ✅ | Read-time trim (`securityTrimService`): `classifyTargets` runs BEFORE `setRows`; `denied` (403/404) rows dropped, `transient` retained. Tests: `securityTrimService.test.ts` (18), plus trim paths in `RecentTab`/`BookmarksTab` tests. |
| 11 | Second stub contributor registers with `{id,icon,title,component}`, renders, no host edits | ✅ | Framework proof — task 085 stub contributor via `sidePaneRegistry`; one new rail icon, no host edits. (Navigator itself intentionally mounts its body directly per the CalendarSidePane single-contributor pattern — see lessons-learned.) |
| 12 | No `Sprk.Bff.Api` changes; no plugin; no per-form web resource | ✅ | Hot-path declaration BFF=N held for the entire project. `git diff` scope: `src/solutions/NavigatorPane/**`, `src/client/shared/Spaarke.UI.Components/**` (client), ribbon solutions, docs — **zero** `src/server/**`, zero `src/dataverse/plugins/**`, zero per-form OnLoad/OnSave web resources. The access-based Monitored fix specifically AVOIDED adding BFF membership code by using the platform's own security trim. |

## Overall

**11 of 12 criteria fully met; 1 (criterion 2, Edited toggle) intentionally descoped** during UAT by owner decision (Viewed-only redesign), with the underlying no-audit-entity derivation preserved in code + tests. All project constraints (no BFF, no plugin, no per-form handler, no audit entity, per-user isolation, security-trim-at-render, never-write-`sprk_monitor`-from-pin) held. Owner UAT passed in both light and dark themes.
