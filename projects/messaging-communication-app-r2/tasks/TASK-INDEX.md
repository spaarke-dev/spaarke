# TASK-INDEX — messaging-communication-app-r2 (Communication Workspace)

> **Generated**: 2026-07-18 via `/project-pipeline`
> **Total tasks**: 21 (20 work + 1 wrap-up)
> **Legend**: 🔲 not-started · 🔄 in-progress/needs-retry · ✅ complete · ⛔ blocked/gated

---

## Task Registry

| # | Title | Wave | Tags | FR | Deps | Blocks | Parallel-safe | Rigor | Model/Effort | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| 001 | Phase-0 audit (spike): live thread/participant schema; confirm category/tags absent (Q2); verify email-r4 `Services/Communication` merged state; plan `sprk_role` choice ints | W0 | dataverse, spike | — | — | 002,003 | true | STANDARD | sonnet/high | ✅ (note: `notes/001-phase0-schema-audit.md`; 11 RegardingFieldMap lookups captured, Q2 absent confirmed, sprk_role=100000000-3; live MCP re-verify deferred before 002/003 apply) |
| 002 | Schema: `sprk_communicationthread` typed-regarding **all-11** lookups (mirror `RegardingFieldMap.All`) + new Lookup discriminator + naming-edited marker + default-thread marker | W0 | dataverse, schema | FR-06 | 001 | 010,070,071 | true | STANDARD | sonnet/high | ✅ (script `scripts/Deploy-ThreadRegardingSchema.ps1` + note `notes/002-thread-regarding-schema.md`; discriminator=`sprk_regardingrecordtype_ref`→`sprk_recordtype_ref`; markers `sprk_nameisautoderived`/`sprk_isdefaultthread`; Text field untouched; **live apply DEFERRED — MCP down**, owner applies+verifies) |
| 003 | Schema: `sprk_communicationparticipant` junction — 6 fields (communication lookup, systemuser, contact, role {From,To,Cc,Bcc}, addresstext, isresolved) | W0 | dataverse, schema | FR-08 | 001 | 050,051 | true | STANDARD | sonnet/high | ✅ (authored: `scripts/Deploy-CommunicationParticipantSchema.ps1` + `notes/003-communicationparticipant-schema.md`; Org-owned + Cascade parent lookup mirror `sprk_communicationattachment`; sprk_role=100000000-3; **LIVE APPLY DEFERRED** — MCP down, owner runs script + records ints before 050/051) |
| 004 | Author participant-junction **schema ADR** (concise + full; ADR-034 path-C tension; INDEX → Accepted) | W0 | adr, docs | FR-08 | — | 050 | **false** (`.claude/`) | STANDARD | opus/high | ✅ (**ADR-048** authored concise `.claude/adr/` + full `docs/adr/`; both INDEXes → Accepted + CHANGELOG; ADR-034 path-C comply-with-intent argued; ADR-047 not claimed; main-session) |
| 010 | `GET /api/communications/by-regarding/{entityType}/{id}` + `ReadByRegardingAsync` on `CommunicationThreadReadService` (reuse impersonation query + access filter) | W1 | bff-api, communication | FR-01 | 002 | 020,080 | true | FULL | sonnet/high | ✅ (`ReadByRegardingAsync` + `RegardingReadResult`; impersonated thread-by-regarding → OR-message-fetch → shared `CommunicationAccessFilter`; entity-set-agnostic via `RegardingFieldMap`; 10 tests incl. ≥3-family theory + private-thread-hidden + internal-only; build/tests green; publish 45.69 MB (−1.3); no new CVE; code-review+adr-check clean. **Live integration DEFERRED** — task-002 thread regarding lookups not yet applied (MCP down); code compiles vs logical-name strings + unit-tested vs mocked Dataverse boundary, per R1 build-and-defer-live) |
| 011 | `GET /api/communications?thread=&regarding=&channel=&from=&to=` filtered query (facets reuse read path; `participant=` stub) | W1 | bff-api, communication | FR-02 | 002 | 051,080 | true | FULL | sonnet/high | ✅ (`QueryCommunicationsAsync` + `CommunicationQueryResult`; thread/regarding/channel/from/to compose onto same impersonation path + shared filter; `participant=` → 501 not-yet-supported (task-051 seam), NO text-LIKE; graceful 400 on malformed/empty; 11 tests incl. per-facet + participant-inert + graceful-degradation + internal-only; build/tests green; same publish/CVE/gates as 010. **Live integration DEFERRED** — same build-and-defer-live as 010) |
| 020 | Regarding-mode extension to `CommunicationTimeline` component (threads-as-groups → per-thread interleaved timelines; calls `by-regarding`) | W2 | frontend, fluent-ui, communication | FR-03 | 010 | 021 | true | FULL | sonnet/high | ✅ (discriminated `mode` prop added — `mode?: 'thread'` unchanged R1 vs `mode: 'regarding'` new; new `ThreadGroup.tsx` + `useRegardingPoll.ts` reuse `buildTimeline`/`MessageRow`/`ChannelBadge` unchanged, no fork; `communicationTimelineApi.readByRegarding` wrapper. **Cross-task gap found+fixed**: task-010's `by-regarding` service queried `sprk_name` but the `ThreadReadResult` DTO discarded it before returning — FR-03's group-header requirement needed it, so added a small additive `Name` field to `ThreadReadResult` (populated on by-regarding, null on R1 per-thread read) + 2 test assertions in `CommunicationByRegardingReadTests.cs`; BFF build+34 tests green. Client: 25 new Jest tests (grouping + reducer), 39/39 total pass; `npm run build` + scoped/full lint clean (2 pre-existing unrelated errors in `DataGrid/HeaderCellContent.tsx` untouched); grep-verified no `@spaarke/auth`/`@azure/communication-*`. Design decisions: regarding mode is READ-ONLY (no compose box — composing stays in thread-id mode/021 PCF); all groups collapsed by default (`defaultExpandedThreadIds` prop to override); groups ordered by most-recent-message-activity descending; polling reuses `pollIntervalMs` (default 5000) via new `useRegardingPoll` hook (full-snapshot refetch each tick, no incremental `since` — endpoint has none). code-review + adr-check clean (self-executed, no Critical/Violation findings). ADR-026 Path-A cited.) |
| 021 | Regarding-mode PCF variant (mirror R1 Timeline PCF + regarding-resolution path; **bound `anchorField`**) + solution pack | W2 | pcf, deploy, e2e-test | FR-04 | 020 | 022 | true | FULL | sonnet/high | ✅ (new sibling PCF `src/client/pcf/CommunicationTimelineRegarding/` — **variant folder** decision, not a mode-switch on the deployed R1 control, to avoid regression risk on the live sprk_communication/sprk_communicationthread placements; documented in current-task.md. Mirrors R1's virtual-ReactControl packaging exactly (index.ts/authInit.ts/Host.tsx/App.tsx/Solution) with one net-new bit: `hostContext.ts`'s `resolveRegardingContext()` reads `(entityType, id)` straight from the Xrm page context — no lookup indirection needed since the host record itself IS the regarding target — validated against an 11-entity allowlist mirroring `RegardingFieldMap.All`. Manifest declares bound `anchorField` (primary Name, required=true — R1 lesson cited) + React 16.14.0/Fluent 9.46.2 platform libraries. `@spaarke/auth` only in `authInit.ts` + the App boundary (grep-verified absent from the shared-component source). Mounts `<CommunicationTimeline mode="regarding" entityType id authenticatedFetch bffBaseUrl pollIntervalMs />` — no `threadId` (discriminated union, task-020 contract). 16/16 new Jest tests green (pure `resolveRegardingContext`, all 11 families + negative cases). `npm run build:prod` clean, 2.35 MiB bundle (matches R1's committed 2.45 MB precedent — @spaarke/auth's bundled msal-browser dominates size, not a regression). Solution ZIP packed + verified (6 well-formed lowercase entries). Grep-verified NO `@azure/communication-*` in the bundle (NFR-04). Self-executed code-review + adr-check clean (0 Critical/Warning; 2 Suggestion-level notes on R1-mirrored boilerplate duplication, explicitly sanctioned by the task's "mirror R1, do not invent a new pattern" instruction). UI-test (Step 9.7) SKIPPED — no `--chrome` session / no live Dataverse env this session; substituted with jest + bundle + manifest inspection evidence. **Live `pac import` DEFERRED to task 022 / owner** (this task stops at build+pack per scope).) |
| 022 | Place the regarding-mode PCF on **all 11** entity forms + form config | W2 | dataverse, deploy, e2e-test | FR-04 | 021 | 080 | true | FULL | sonnet/high | ✅ (spec+script `notes/022-pcf-form-placement.md` + `scripts/Verify-CommunicationTimelineRegardingPlacement.ps1`. **Key finding, code-grounded**: corrected the working assumption "sprk_name for all sprk_ entities" — `IncomingAssociationResolver.GetPrimaryNameField()` (lines 469-483) shows 3 of 9 custom entities use a dedicated primary-name field: `sprk_matter`→`sprk_mattername`, `sprk_project`→`sprk_projectname`, `sprk_event`→`sprk_eventname` (rest + invoice/servicerequest/workassignment/budget/analysis/organization use `sprk_name`; `account`→`name`, `contact`→`fullname` OOB). 11-entity table verified against `RegardingFieldMap.All` verbatim — no invention/omission. **Write-path decision**: maker checklist (Form Designer UI) chosen over a PATCH script for the PCF field-bind step — the `<controlDescriptions>/<customControl>` FormXml schema is not verified/tested end-to-end in this repo (unlike the stable subgrid-classid precedent in `Deploy-TodoSubgridsToElevenParentForms.ps1`), and this task's own rigor-reason flags it "host-affecting, hard-to-reverse" — PCF-DEPLOYMENT-GUIDE.md Step 9 already documents the tested Form Designer procedure. Authored a read-only Web API verification script instead (parse-clean, 0 errors) that checks control registration + per-entity form placement + anchor-field presence — safe to re-run as a progress tracker. Import order + 11-row maker checklist + input-property guidance (copy R1 `CommunicationTimeline` placement's env-constant values) + 11-row smoke matrix template + POML `<ui-tests>` verbatim (matter+contact deep, dark mode, 9-form smoke) all in the note. code-review/adr-check self-executed clean (docs+script only, no code touched; ADR-024/006/026/022/NFR-03 compliance table in note §9). **LIVE APPLY DEFERRED** — no Dataverse/MCP access this session (consistent with 002/003/010/011/020/021/023/040/041/050/070); owner runs `pac solution import` + the 11-form maker checklist + verification script + UI tests, then unblocks 080.) |
| 023 | VisualHost summary count MetricCard config (optional, config-only; drill-through; count-only per T-1) | W2 | dataverse, config | FR-05 | 010 | — | true | STANDARD | sonnet/high | ✅ (spec `notes/023-visualhost-summary-card.md`; exemplar `sprk_matter` config, Count over `sprk_communication`, `_sprk_regardingmatter_value` context field (already live); drill-through → `sprk_communicationthread` entity-list dialog (thread-grain, safe); "unread" scoped out — no schema field; **LIVE APPLY DEFERRED** — MCP down, owner creates record + places control) |
| 030 | New `@spaarke/communication-components` lib + rich Pattern D widget (copy `CalendarWorkspaceWidget`); upgrade `communications-list` **in place** (keep type string); **dual-deploy** | W3 | frontend, fluent-ui, spaarke-ai | FR-12 | 010 | — | true | FULL | sonnet/high | ✅ (new lib `@spaarke/communication-components` — channel + sent-date filter-chip toolbar, per-channel card strip, embedded `<DataGrid hostFilters onRecordsLoaded>`, no custom `onRecordOpen` — reuses framework `defaultRecordOpen` per BUILD-A-NEW-WORKSPACE-WIDGET.md §6.6; reuses configId `e1826c4c-…`; `register-workspace-widgets.ts` + `communications.registration.ts` upgraded in place, type string/section id/configId unchanged, grep-verified single registration; ai-widgets full `tsc --noEmit` clean (0 errors) + `npm run build` clean + 172 pre-existing tests pass + ad-hoc factory-resolution test passed; SpaarkeAi `tsc-surface-gate` reports 0 surface-owned errors. **Vite production bundle blocked on BOTH LegalWorkspace + SpaarkeAi by a PRE-EXISTING, unrelated Compose-feature dependency gap** (`@spaarke/document-operations`/`mammoth`/`@tiptap/extension-unique-id` missing from those apps' own package.json/vite.config.ts for the `compose-components` section) — module-transform phase succeeded on all of this task's files before hitting that unrelated failure; follow-up recommended. Merge-order note recorded below.) |
| 040 | Standalone `sprk_communicationspage` shell (copy `sprk_invoicespage`; reuse config `e1826c4c-…`; **widget/launcher only — no sitemap**); register in `Deploy-AllDataGridConsumers.ps1` | W4 | frontend, deploy | FR-11 | — | — | true | STANDARD | sonnet/high | ✅ built (vite dist/index.html 1.6MB; GUID baked; deploy-script registered; live pac deploy DEFERRED to owner) |
| 041 | Curate grid views/columns on config `e1826c4c-…` so chips auto-derive (channel/person/date/regarding) | W4 | dataverse, config | FR-11 | — | — | true | STANDARD | sonnet/high | ✅ (spec `notes/041-grid-curation.md`; **key finding**: `chipDiscovery.ts` skips Lookup chips in `auto` mode → regarding/person need `filterChips.explicit`+`valueSource`; channel/date auto-derive. Authored explicit block, 2 options A=14-chip/B=lean-5; NFR-05 in place. **LIVE APPLY DEFERRED** — owner pastes JSON into config `e1826c4c-…` `sprk_configjson` + confirms per-entity `nameField`, verifies on 040/030) |
| 050 | Participant-index **write** at capture/send — populate junction rows (message grain, reuse `ParticipantCorrelationRung`; unresolved → isresolved=false) | W5 | bff-api, communication | FR-08 | 003,004 | 051,060 | **false** (shared persist path) | FULL | opus/xhigh | ✅ (new `CommunicationParticipantIndexer` + `CommunicationParticipantSet`; wired into all 5 persist points — 3 outbound in `CommunicationService` + inbound email/chat; reuses `QueryContactByEmailAsync` no 2nd resolver; XOR systemuser/contact; unresolved rows Q-D; best-effort/non-fatal + idempotent; unconditional DI (ADR-032); +10 tests; 554 pass/0 fail; publish ≈0, no CVE; code-review + adr-check 0 violations. **Live junction apply owner-deferred** — compiled vs field logical-name strings + unit-tested vs mocked Dataverse) |
| 051 | `participant=` facet on the filtered `query` endpoint (join the junction) | W5 | bff-api, communication | FR-02 | 011,050 | 080 | true | FULL | sonnet/high | ✅ (replaced the task-011 501 stub in `QueryCommunicationsAsync` with `BuildParticipantClauseAsync` — resolves `participant={systemuser/contact GUID}` against the junction's typed `_sprk_systemuser_value`/`_sprk_contact_value` lookups (any role, FK-backed, not text-LIKE), or `participant={address}` via exact-equality on `sprk_addresstext` (Q-D unresolved external parties); yields candidate message ids that flow through the SAME `QueryVisibleMessagesAsync` impersonated read + `ICommunicationAccessFilter` as every other facet — no second access mechanism, no bypass. No junction match → always-false clause (graceful empty, not an error); malformed (non-GUID, >400 chars) → 400. Composes as AND with thread/regarding/channel/from/to. +7 tests (resolved-person, unresolved-address, AND-composition, no-match graceful-degrade, NEGATIVE access — candidate the impersonated read doesn't return is not leaked, malformed→400, blank→no-op), −1 obsolete 501-stub test; net +6 (560 pass/0 fail/8 pre-existing skip, was 554). Build clean; publish 46.24 MB compressed excl. PDBs (≈0 vs ~46.99 baseline, no new package); no new CVE. code-review + adr-check: 0 violations (FR-02/ADR-028/ADR-034-path-C/NFR-03/§10/§11 all compliant — §11 correctly N/A, pure modification of existing surface). `/conflict-check`: SILENT PASS (master's 5 new commits are unrelated `assistant-r1` UAT work; no concurrent edits to `Services/Communication/`).) |
| 060 | Compose enrichment: Subject/topic + Cc/Bcc + structured recipient picker (reuse `RecipientField`, emit resolved ids feeding 050); meaningful `sprk_name` | W6 | pcf, frontend, communication | FR-10 | 050 | — | true | FULL | sonnet/high | ✅ (`TimelineComposeBox` now carries a required Subject field + Cc/Bcc `RecipientField` rows (reused 3×, not cloned); `ITimelineSendPayload` extended with `subject`/`cc`/`bcc`; `CommunicationTimeline.tsx handleSend` threads them into `sendTimelineMessage` — replaces the old meaningless `Re: conversation {threadId}` fallback; Send is now gated on To+Subject+Body so no message can persist "(No Subject)". **Recipient contract extension**: `ILookupItem.entityType?: 'contact'|'systemuser'` (tagged at source in `userLookup.ts`'s `searchUsersAsLookup`/`searchContactsAsLookup`) → `RecipientField.handleSelectResult` surfaces it onto `IRecipient.entityType` (both additive/optional, backward-compatible — `EmailComposer`'s own To/Cc/Bcc rows unaffected). A resolved pick's exact directory email is what lets the 050 participant indexer's `QueryContactByEmailAsync` land it as a typed junction row instead of an unresolved-address row; free-text stays a fallback (Q-D). Build clean (`tsc`, 0 errors); scoped+full lint clean (same 2 pre-existing unrelated `HeaderCellContent.tsx` errors); consuming PCF `npm run build:prod` clean (2.35 MiB, standard size warning only). +16 tests (7 `TimelineComposeBox.test.tsx` incl. dark mode + resolved-vs-free-text + cc/bcc payload shape; 3 `userLookup.entityType.test.ts`; 2 new + all existing `RecipientField.test.tsx` cases); 13 suites/161 tests pass, 0 regressions. code-review + adr-check: 0 Critical/Violation (ADR-021/028/022, §11 all compliant; 2 low-priority Suggestions noted, non-blocking). No BFF/`.cs` touch (client-only, §10 N/A).) |
| 070 | Auto-threading policy: 3-tier `IThreadResolver` (subject → per-record default → per-user master) + `default-thread` marker; characterization-test existing flows first | W7 | bff-api, communication | FR-09 | 002 | 071 | **false** (shared `ThreadResolver`) | FULL | opus/xhigh | ✅ (Tier-1 SKIP→null now falls through to ladder: Tier 2 per-record default `FindOrCreateDefaultThreadAsync(anchor.type,anchor.id)`, Tier 3 per-user master keyed `("systemuser",ownerId)`; both idempotent via `sprk_isdefaultthread`+denormalized pointer query-before-create, lazy; NFR-02 swallow preserved. 5 characterization tests GREEN before edit + 6 extension tests; 544 Communication pass (533 baseline+11), 8 pre-existing skips. Build clean; publish 45.69 MB (≈0 vs 46.99); no CVE; code-review+adr-check clean (0 violations); `/conflict-check` clean. **Live apply of 002 `sprk_isdefaultthread` DEFERRED** — MCP down; code compiles vs field logical-name strings + unit-tested vs mocked boundary, per R1 build-and-defer-live) |
| 071 | Thread naming re-derive (`BuildTopic` re-derives unless user-edited, marker-gated) + place RegardingResolver PCF on thread form (0 code) | W7 | bff-api, pcf, communication | FR-07 | 002,070 | — | true | FULL | sonnet/high | ✅ (new `IThreadResolver.ReDeriveThreadNameAsync` + `ThreadResolver.BuildTopicFromRegardingAnchor`: marker-gated re-derive — `sprk_nameisautoderived` true/missing=Auto re-derives from current denormalized regarding, `false`=Edited preserves name unconditionally; master-thread guard (`sprk_regardingrecordtype=="systemuser"`) short-circuits before ever resolving it as a regarding record (070 flag honored); CREATE paths (`CreateThreadAsync` + `FindOrCreateDefaultThreadAsync`) now stamp `sprk_nameisautoderived=true` explicitly; NFR-02 try/catch swallow; no-op write-avoidance when derived name unchanged. **PCF placement (0 code)**: instructions authored at `notes/071-regarding-resolver-thread-placement.md` — `entity="sprk_communicationthread"`, bind `regardingRecordType→sprk_regardingrecordtype_ref` (required) + `regardingRecordNameField→sprk_regardingrecordname`, explicit `regardingTargets` (11-entity `RegardingFieldMap.All` list, not the PCF's `sprk_todo`-authored default catalog). **Ambiguity surfaced (root §6, not guessed)**: neither the regarding-change trigger nor the name-edit marker-flip is wired — both are client-side `Xrm.WebApi` writes that bypass the BFF (`ResolverWriteHandler.ts`); recommended follow-on is a Dataverse plugin on Update of the thread, documented in the method's XML doc + the placement note. +14 tests (568 total Communication pass, 554 baseline + 14, 8 pre-existing skips; 070's characterization + extension tests confirmed still green, untouched). Build clean; publish delta ≈0 (stash-isolated pre/post comparison: 46.24 MB excl-PDB both before and after — incl-PDB swung ±3.6 MB across clean rebuilds from build non-determinism, not code size); no new HIGH CVE; code-review + adr-check clean (0 Critical; Suggestion-level notes on create-time subject-derived names being overwritten by anchor-based names on a later genuine re-derive — matches documented design intent, not a defect). `/conflict-check`: silent pass (master's new `assistant-r1` commits touch `CommunicationThreadReadModels.cs`/`CommunicationThreadReadService.cs`, not `ThreadResolver.cs`/`IThreadResolver.cs`).) |
| 080 | Vertical-slice **seam tests**: `by-regarding` + `query` + `participant=` + auto-threading tiers; **11-entity pass**; access-parity (private + internal-only); preserve email/messaging characterization | W8 | bff-api, testing | NFR-03 | 010,011,022,050,051,070 | — | true | TEST-MODIFYING | sonnet/xhigh | ✅ (new `tests/integration/seam/Communication/CommunicationWorkspaceReadSeamTests.cs`, 17 tests, EXTENDS R1 seam harness; closes 5 reuse-first composition gaps — full 11-entity by-regarding matrix, all-5-facets-composed query, filtered-query generic private-thread-hidden, 3-tier ladder w/ REAL `MessagingThreadKeyStrategy`, no-membership-union structural regression guard; coverage map `notes/080-seam-coverage-map.md`. `dotnet test ~Communication`: 584 pass/8 skip/0 fail; full solution: 8654 pass/101 skip/0 fail. code-review+adr-check: 1 Warning found+fixed (tautology in first-draft 11-entity theory), 0 Critical/Violation. Publish-size/CVE N/A (test-only). Live Dataverse verify DEFERRED — MCP down.) |
| 081 | Architecture doc: extend communication architecture with Workspace read endpoints + participant index + regarding-mode + widget | W8 | docs | — | 010,050,070 | — | true | STANDARD | sonnet/high | ✅ (extended `docs/architecture/communication-intelligence-architecture.md` — new §15 "R2 — the Communication Workspace (read/organize layer)" with 5 subsections: 15.1 by-regarding+filtered-query read endpoints (entity-set-agnostic via shared `RegardingFieldMap`, one impersonation+access-filter pipeline, no membership-union), 15.2 `sprk_communicationparticipant` participant index (message-grain, 2 typed lookups, 5 persist points, `participant=` candidate-id join, ADR-034 path-C), 15.3 3-tier `IThreadResolver` auto-threading ladder + naming re-derive + thread regarding schema/RegardingResolver placement, 15.4 Surface 1 regarding-mode Timeline + PCF variant + T-1 VisualHost count-only boundary, 15.5 Surface 3 workspace widget upgrade-in-place + dual-deploy; renumbered old §15/§16 (ADR cross-refs/Related) → §16/§17, added ADR-048 row + refreshed header/canonical-ADR line/footer date. Verified every claim against shipped code (`CommunicationThreadReadService.cs`, `Access/CommunicationAccessFilter.cs`, `ThreadResolver.cs`, `CommunicationParticipantIndexer.cs`, `CommunicationService.cs` persist-point grep, `CommunicationTimeline.types.ts`, `RegardingFieldMap.cs`, task 002/003 schema notes, ADR-048). `communication-service-architecture.md` NOT extended — confirmed out of scope (send/inbound transport mechanics only; already points to the intelligence doc's §9/§15 for messaging-channel narrative). STANDARD rigor, Step 9.5 skipped per POML.) |
| 090 | Project wrap-up (README Complete, lessons-learned, `/test-diet`, archive) | Wrap | wrapup | — | (all) | — | **false** | STANDARD | sonnet/high | ✅ (README → Code-complete + graduation criteria annotated; `notes/lessons-learned.md`; `notes/test-diet-report.md` → **0 scaffolding, all MAINTAIN**; owner deploy gates + 3 open findings surfaced; main-session) |

> **Note**: 20 numbered work tasks (001–004, 010–011, 020–023, 030, 040–041, 050–051, 060, 070–071, 080–081) + 090 wrap-up = **21 tasks**. All 21 POML files authored + validated (`Validate-TaskPoml.ps1` clean).

---

## Parallel Execution Groups

| Group | Tasks | Prerequisite | Notes |
|---|---|---|---|
| **W0-A** | 001, 004 | — | Audit spike + ADR (004 main-session `.claude/`) |
| **W0-B** | 002, 003 | 001 | Two schema deltas (distinct entities, parallel) |
| **W1** | 010, 011 | 002 | Additive read endpoints; parallel |
| **W2** | 020 → 021 → 022; 023 (‖) | 010 | Component → PCF → 11-form placement; 023 config parallel |
| **W3** | 030 | 010 (+ PR #508 / dataset-grid-r2 merge-order) | Widget upgrade (dual-deploy) |
| **W4** | 040, 041 | — | Standalone page + grid curation (parallel) |
| **W5** | **050 (serial)** → 051 | 003 (+004) | 050 shared-path write; 051 facet after |
| **W6** | 060 | 050 | Compose enrichment feeds the index |
| **W7** | **070 (serial)** → 071 | 002 | Auto-threading shared-resolver edit; naming after |
| **W8** | 080, 081 | W1–W7 substantially complete | Seam tests + doc |

**Max concurrency**: 6 agents/wave. `.claude/`-touching (004) + wrap-up (090) run main-session, sequential. **050 + 070 are `parallel-safe: false`** (shared `Services/Communication/` edits — never concurrent; `/conflict-check` before each).

**Model tiers (per CLAUDE.md §8.5)**: default **sonnet @ high**. **opus** on 004 (ADR), 050 (participant write / shared persist path), 070 (auto-threading / shared resolver). **effort xhigh** on 050, 070, 080. All others sonnet @ high.

## Critical Path

```
001 → 002 → 010 → 020 → 021 → 022 → 080 → 090
001 → 003 → 050 → 051 → 080          (participant index → person filter)
002 → 070 → 071                       (auto-threading + naming on shared resolver)
```

## High-Risk / Watch Items

- **050 (participant write)** + **070 (auto-threading)** — shared `Services/Communication/` edits; characterization-test existing email/messaging flows green first; `parallel-safe: false`; `/conflict-check` before PR.
- **010/011 access parity (NFR-03)** — apply R1 impersonation + 2-rule filter; NO membership-union (retired 2026-07-16). Access-parity seam tests in 080.
- **022 (11-form placement)** — larger deploy/test matrix; PCF needs a bound `anchorField`.
- **030 (widget)** — dual-deploy (LegalWorkspace + SpaarkeAi); keep type string; merge-order vs dataset-grid-r2 + PR #508.
- **T-1 (VisualHost)** — 023 count-only; content via BFF Timeline.
- Every BFF-touching task: `/conflict-check` + publish-size + CVE (root §10).

## FR Coverage

FR-01→010 · FR-02→011,051 · FR-03→020 · FR-04→021,022 · FR-05→023 · FR-06→002 · FR-07→071 · FR-08→003,004,050 · FR-09→070 · FR-10→060 · FR-11→040,041 · FR-12→030. NFRs distributed (NFR-01/02 every BFF task; NFR-03→080; NFR-05 §11; NFR-06 reserve-only; NFR-07→030; NFR-08 deploy runbook).
