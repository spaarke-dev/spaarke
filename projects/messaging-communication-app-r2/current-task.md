# Current State — messaging-communication-app-r2 (Communication Workspace)

> **Last Updated**: 2026-07-18 (context-handoff before compaction)
> **Recovery**: Read "Quick Recovery" first. Two projects are in play — **R1 is deployed/merged/essentially done**; **R2 is in DRAFT DESIGN** (this is the active work).

---

## Quick Recovery (READ FIRST)

| Field | Value |
|-------|-------|
| **060 result** | **Task 060 (compose-form enrichment, W6/FR-10) ✅ COMPLETE 2026-07-19** — FULL rigor, sonnet/high. `TimelineComposeBox.tsx`: required Subject `Field`+`Input` (populates `sprk_subject` + server-derived `sprk_name`; gates Send with To/Body — no message can persist "(No Subject)") + Cc/Bcc via 2 more `RecipientField` rows (3 total, all reused not cloned). `ITimelineSendPayload` extended (`subject` required, `cc?`/`bcc?`); `CommunicationTimeline.tsx handleSend` threads them into `sendTimelineMessage`, replacing the old `Re: conversation {threadId}` filler. **Recipient contract**: `ILookupItem.entityType?: 'contact'|'systemuser'` tagged at source in `userLookup.ts` (`searchUsersAsLookup`/`searchContactsAsLookup`/`searchUsersAndContacts`) → `RecipientField` surfaces it onto `IRecipient.entityType` (both additive/optional — `EmailComposer`'s own rows unaffected, dual-consumer verified). BFF DTO only carries email-address arrays (no client resolved-id field; none added — out of scope per "do not change the BFF send path"), so a resolved pick's exact directory email is what lets 050's `QueryContactByEmailAsync` land it as a typed junction row vs. an unresolved-address row; free-text stays a fallback (Q-D). Note: `onSearchRecipients` still NOT wired at the PCF layer (pre-existing task-061-deferred boundary, out of this task's scope) — resolved-path verified via mocked `onSearchRecipients` in tests. Build clean (`tsc`); lint clean (same 2 pre-existing unrelated errors); consuming PCF `npm run build:prod` clean (2.35 MiB; required first-time `npm install --legacy-peer-deps` in this worktree). +16 tests (7 `TimelineComposeBox.test.tsx` + 3 `userLookup.entityType.test.ts` + 2 new/existing `RecipientField.test.tsx`); 13 suites/161 tests pass, 0 regressions. code-review + adr-check: 0 Critical/Violation (ADR-021/028/022/§11 compliant). No BFF/`.cs` touch. No active task. |
| **071 result** | **Task 071 (thread naming re-derive + RegardingResolver PCF placement, W7/FR-07) ✅ COMPLETE 2026-07-19** — FULL rigor, sonnet/high. **(a) BFF re-derive**: new `IThreadResolver.ReDeriveThreadNameAsync` + `ThreadResolver.BuildTopicFromRegardingAnchor` — marker-gated: `sprk_nameisautoderived` true/missing(legacy)=Auto re-derives `sprk_name` from the thread's CURRENT denormalized regarding (`sprk_regardingrecordname` fallback → "Conversation"); `false`=Edited preserves the name unconditionally, no write. **Master-thread guard (070 flag honored)**: `sprk_regardingrecordtype=="systemuser"` short-circuits before ever treating it as a resolvable regarding record — reliable because RegardingResolver's nav-prop discovery can never write to that Text field (confirmed in task-002 note). Both CREATE paths (`CreateThreadAsync` + `FindOrCreateDefaultThreadAsync`) now stamp `sprk_nameisautoderived=true` explicitly (not relying solely on the schema default). No-op write-avoidance when derived name == current name. NFR-02 try/catch swallow. **(b) PCF placement (0 code, deferred live)**: instructions authored at `notes/071-regarding-resolver-thread-placement.md` — `entity="sprk_communicationthread"`, bind `regardingRecordType→sprk_regardingrecordtype_ref` (required) + `regardingRecordNameField→sprk_regardingrecordname`; explicit `regardingTargets` = the 11-entity `RegardingFieldMap.All` list (NOT the PCF manifest's `sprk_todo`-authored default catalog, which differs). **Ambiguity surfaced explicitly (root §6, not guessed)**: neither the regarding-change trigger (invoking `ReDeriveThreadNameAsync`) nor the name-edit marker-flip (Auto→Edited) is wired — both would-be triggers are client-side `Xrm.WebApi.updateRecord` writes (`ResolverWriteHandler.ts` / direct form edit) that bypass the BFF entirely; recommended follow-on is a Dataverse plugin on Update of the thread (documented in the method's XML doc + the placement note, NOT built here — out of this task's bff-api/pcf/communication tag scope). +14 tests in `ThreadResolverTests.cs` (auto-rederive, legacy-null-as-auto, edited-preserves, master-guard, unchanged-name-no-write, retrieve-throws-swallow, ×2 create-path marker-stamp tests) — 568 Communication pass (554 baseline + 14, 8 pre-existing skips); 070's characterization + extension tests confirmed still green, untouched. Build clean; publish delta ≈0 (stash-isolated pre/post comparison against 070's baseline: 46.24 MB excl-PDB identical before and after my edits — incl-PDB swung ±3.6 MB across clean rebuilds from build non-determinism/MVID-timestamp noise, not code size — reported the stable excl-PDB metric); no new HIGH CVE (zero new packages). code-review + adr-check: 0 Critical (ADR-046 no-second-mechanism / ADR-024 regarding-family reuse / ADR-028 read-path-untouched / ADR-032 no new DI / NFR-02 / NFR-04 all compliant; §11 = pure extension of existing `ThreadResolver`/`IThreadResolver`, no new component); Suggestion-level note that a genuine re-derive event overwrites a CREATE-time subject-derived name with the anchor-based name — matches the task's own documented design intent (no message context on a regarding-change event), not a defect. `/conflict-check`: SILENT PASS (master's new commits are unrelated `assistant-r1` work touching `CommunicationThreadReadModels.cs`/`CommunicationThreadReadService.cs`, not `ThreadResolver.cs`/`IThreadResolver.cs`). No active task. |
| **051 result** | **Task 051 (`participant=` facet on the filtered query endpoint, W5/FR-02) ✅ COMPLETE 2026-07-19** — FULL rigor, sonnet/high. Replaced task-011's 501 stub in `CommunicationThreadReadService.QueryCommunicationsAsync` with `BuildParticipantClauseAsync`: resolved-person GUID → matches `_sprk_systemuser_value`/`_sprk_contact_value` on the junction (any role, FK-backed, not text-LIKE); non-GUID → exact-equality match on `sprk_addresstext` (Q-D unresolved external parties), quote-escaped for OData safety. Yields CANDIDATE message ids only — those flow through the SAME `QueryVisibleMessagesAsync` impersonated read + `ICommunicationAccessFilter` as every other facet (no second access mechanism; a candidate the caller cannot see is silently dropped there, proven by a dedicated negative-access test). No junction match → always-false clause (graceful empty, never an error); malformed (non-GUID, >400 chars) → 400; composes as AND with thread/regarding/channel/from/to. **+7 tests** in `CommunicationFilteredQueryTests.cs` (resolved-person, unresolved-address, AND-composition, no-match graceful-degrade, NEGATIVE access, malformed→400, blank→no-op), −1 obsolete 501-stub test → net +6 (560 pass/0 fail/8 pre-existing skip, was 554). Build clean; publish 46.24 MB compressed excl. PDBs (≈0 vs ~46.99 baseline, zero new packages); no new CVE. code-review + adr-check: 0 violations (FR-02/ADR-028/ADR-034-path-C/NFR-03/§10/§11 compliant; §11 correctly N/A — pure modification of existing surface). `/conflict-check`: SILENT PASS (master's new commits are unrelated `assistant-r1` work; no concurrent `Services/Communication/` edits). **⚠️ LIVE JUNCTION APPLY still owner-deferred** (inherits 003/050's MCP-down gate) — the join is compiled vs field logical-name strings + unit-tested vs the mocked Dataverse boundary; NOT live-verified. Owner gate: after 003's schema + 050's write path are live, live-verify `participant=` (resolved/unresolved/negative-access) before relying on it in production. **Unblocks 080** (all its deps — 010,011,022,050,051,070 — are now task-complete, though several carry the same live-apply-deferred caveat). No active task. |
| **050 result** | **Task 050 (participant-index write, W5/FR-08) ✅ COMPLETE 2026-07-19** — FULL rigor, opus/xhigh. NEW `src/server/api/Sprk.Bff.Api/Services/Communication/CommunicationParticipantIndexer.cs` (the writer) + `Models/CommunicationParticipantSet.cs` (input). Wired into **all 5 persist points**: 3 outbound in `CommunicationService` (`SendMessageAsync` — MESSAGE, passes in-hand systemuser sender ref; `SendAsync` — shared-mailbox email; `SendAsUserAsync` — OBO email) via new best-effort helper `WriteParticipantIndexAsync`; inbound email `IncomingCommunicationProcessor` Step 4.7 (normalized envelope From/To/Cc); inbound chat `Channels/MessagingIngestor` (NormalizedMessage). **Contract for downstream**: one `sprk_communicationparticipant` row per (message × person/address × role {From=100000000,To=1,Cc=2,Bcc=3}); resolved → EXACTLY ONE of `sprk_systemuser` XOR `sprk_contact` + `sprk_isresolved=true` + `sprk_addresstext`; unresolved external → both lookups null + `sprk_isresolved=false` + `sprk_addresstext`=raw (Q-D). Reuses `ICommunicationDataverseService.QueryContactByEmailAsync` (no 2nd resolver). Best-effort/non-fatal (indexer never throws) + idempotent (pre-query `(role,addresstext)` keys, skip dups). DI: unconditional singleton in `CommunicationModule` (ADR-032 symmetric). Optional trailing ctor params (mirror `IThreadResolver?`) keep existing unit constructions green. **+10 tests** (`CommunicationParticipantIndexerTests.cs`): resolved-systemuser, resolved-contact, unresolved-row, XOR-never-both, grain-count, best-effort-swallow, idempotent-no-dupe (existing-row + within-call case), capture-path-writes-rows, capture-survives-indexer-failure. Characterization held: **554 pass / 0 fail / 8 pre-existing skip** (baseline was 544 before +10). Build clean; publish delta ≈0 (zero new packages, baseline ~45.69–46.99 MB); no new CVE. code-review + adr-check: **0 violations** (FR-08/ADR-048/ADR-034-path-C/ADR-028/ADR-032/ADR-010/§10/§11 all compliant). `/conflict-check`: SILENT PASS (no master commits on Services/Communication; 070 not concurrent — its work present in-worktree, my edits additive). **⚠️ LIVE JUNCTION APPLY OWNER-DEFERRED** (task-003 `Deploy-CommunicationParticipantSchema.ps1` not yet run — MCP down): code compiles vs field logical-name strings + unit-tested vs mocked Dataverse boundary; the indexer swallows the "entity not applied live" error until the schema lands (owner runs the 003 script + records actual `sprk_role` ints, then live-verifies). **Builds on**: 051 (`participant=` facet reads these rows), 060 (compose feeds resolved ids), 080 (seam tests). No active task. |
| **Active work** | **R2 = "Communication Workspace"** — Wave 0 underway. **Task 001 (Phase-0 audit spike) ✅ COMPLETE 2026-07-19** — findings note at [`notes/001-phase0-schema-audit.md`](notes/001-phase0-schema-audit.md). **Task 040 (standalone All-Communications page, W4/FR-11) ✅ BUILT 2026-07-19** (run early, out of wave order, deps-free): `src/solutions/sprk_communicationspage/` shell + `Deploy-AllDataGridConsumers.ps1` registration; vite `dist/index.html` builds clean (GUID `e1826c4c-…` baked, no `parentContext`, no second config, no sitemap per Q-B). **Live `pac`/web-resource deploy DEFERRED to owner** (deploy gate). **Task 003 (participant junction, W0-B/FR-08) ✅ AUTHORED 2026-07-19**: `scripts/Deploy-CommunicationParticipantSchema.ps1` + `notes/003-communicationparticipant-schema.md` — entity + 6 locked fields + primary name; Org-owned + Cascade parent lookup mirror `sprk_communicationattachment`; person lookups RemoveLink; sprk_role local Choice From/To/Cc/Bcc=100000000-3. **LIVE APPLY DEFERRED** (MCP down — owner runs script `-SolutionUniqueName <messaging solution>` + records actual ints before 050/051). Unblocks 050+051. **Task 002 (thread typed-regarding lookups + markers, W0-B/FR-06) ✅ AUTHORED 2026-07-19**: `scripts/Deploy-ThreadRegardingSchema.ps1` + `notes/002-thread-regarding-schema.md` — 14 additive columns on `sprk_communicationthread` (11 typed `sprk_regarding{...}` lookups mirroring `RegardingFieldMap.All`; NEW Lookup discriminator `sprk_regardingrecordtype_ref`→`sprk_recordtype_ref`; `sprk_nameisautoderived` Boolean default Auto for 071; `sprk_isdefaultthread` Boolean default No for 070). Discriminator named `_ref` (plain name is in-use Text field, MUST-NOT-RETYPE); satisfies RegardingResolver's dynamic discovery so 071 binds it 0-code. Text `sprk_regardingrecordtype` untouched (script guards it); no category/tags/description (Q2). **LIVE APPLY DEFERRED** (MCP down — owner runs script `-SolutionUniqueName <messaging solution>` + MCP-verifies before 010/070/071). Unblocks 010+070+071. No active task. |
| **041 result** | **Task 041 (grid filter-chip curation, W4/FR-11) ✅ AUTHORED 2026-07-19** — spec `notes/041-grid-curation.md`. **Key finding (grounded on `chipDiscovery.ts` code, not the header comment)**: Lookup chips are SILENTLY SKIPPED in `auto`/`allowlist`/`denylist` modes (`buildAutoDescriptor` returns undefined for `kind==='lookup'`; TODO(R2) — metadata lookup targets not projected). Only `explicit` mode + a `valueSource` emits a lookup chip. So **regarding + person chips do NOT auto-derive from layoutXml** — the only lever is a `filterChips.explicit` block in configjson (which must also re-declare channel+date since `explicit` disables auto). channel (`sprk_communicationtype`→optionset) + date (`sprk_sentat`→daterange) DO auto-derive. Authored the explicit block: Option A (14 chips: channel+date+11 RegardingFieldMap.All lookups+sprk_sentby) and Option B (lean 5: channel+date+sentby+matter+person, recommended UX). Regarding family from `RegardingFieldMap.All` (ADR-024 order, 11 lookups). NFR-05 honored — curate config `e1826c4c-…` + view `2bf1c5a5-…` in place, no second default. Zero code/BFF (§10 N/A). **LIVE APPLY DEFERRED** (MCP down) — owner pastes §4 JSON into `e1826c4c-…` `sprk_configjson`, confirms per-entity `nameField` (PrimaryNameAttribute), verifies chips on task 040 page / task 030 widget. No active task. |
| **W1 result** | **Tasks 010 + 011 (BFF reads, W1/FR-01+FR-02) ✅ COMPLETE 2026-07-19** — both implemented together (same service + endpoint group, avoids a same-file race). **010**: `ReadByRegardingAsync` on `CommunicationThreadReadService` + `RegardingReadResult` model + `GET /api/communications/by-regarding/{entityType}/{id}` — impersonated thread-by-regarding query (typed lookup via `RegardingFieldMap`, entity-set-agnostic across all 11) → OR-filter message fetch → shared `CommunicationAccessFilter`; groups messages per thread in the R1 DTO shape. **011**: `QueryCommunicationsAsync` + `CommunicationQueryResult` + `GET /api/communications?thread=&regarding=&channel=&from=&to=&participant=` — thread/regarding(`{entityType}:{guid}`)/channel/from/to facets compose onto the SAME impersonation path + shared filter; **`participant=` → 501 not-yet-supported** (task-051 seam; NO text-LIKE fallback); graceful 400 on malformed/empty. Shared private helper `QueryVisibleMessagesAsync` = the single impersonation+filter pipeline both reuse (no second filter, no membership-union). 21 new tests (10+11) incl. ≥3-family theory, private-thread-hidden (impersonation), internal-only (real filter), participant-inert, graceful-degradation — all green (533 Communication tests pass, 8 pre-existing skips). Build clean; publish **45.69 MB (−1.3 vs 46.99)**; no new CVE; code-review + adr-check clean (0 violations). **⚠️ LIVE INTEGRATION DEFERRED** — task-002 thread regarding lookups not yet applied live (MCP down); code compiles vs field logical-name strings + unit-tested vs mocked Dataverse boundary (R1 build-and-defer-live pattern). Owner gate: apply 002 schema, then live-verify by-regarding across families. |
| **023 result** | **Task 023 (VisualHost summary count MetricCard config, W2/FR-05, optional) ✅ AUTHORED 2026-07-19** — spec `notes/023-visualhost-summary-card.md`. Config-only, no code: one exemplar `sprk_chartdefinition` record for `sprk_matter` — MetricCard, entity `sprk_communication`, `sprk_aggregationtype=Count`, `sprk_contextfieldname=_sprk_regardingmatter_value` (**already live today** — no schema gate, unlike the thread-side lookups). "Unread" scoped out — no read/unread field exists in the schema (a schema gap, not a config gap). **Drill-through target = `sprk_communicationthread`** (entity-list dialog, thread-grain — chosen over `sprk_communication` which is message-grain/content-adjacent, and over the standalone `sprk_communicationspage` which is global/unfiltered per Q-B); documented Path-C limitation: an exact same-form jump to task-022's Timeline section would need `ChartRenderer.tsx` to wire `onViewListClick` for `MetricCard` (currently only `DueDateCardList` consumes it) — a code change, out of scope here, flagged for a future task. Per-entity substitution table provided (§6) if the owner replicates beyond `sprk_matter`. **LIVE APPLY DEFERRED** (MCP down) — owner creates the record + places the VisualHost control on the Matter form + verifies (§4/§5). Not dropped — shipped as low-risk/high-value; Surface 1 (020–022) unaffected either way. No active task. |
| **070 result** | **Task 070 (auto-threading policy, W7/FR-09, opus/xhigh, `parallel-safe:false`) ✅ COMPLETE 2026-07-19** — extended shared `ThreadResolver.cs` (+`IThreadResolver.cs` doc-only). **3-tier ladder**: the former Tier-1 SKIP→null branch (`!resolution.CreateWhenAbsent`, e.g. chat with no ACS thread id) now `return await ResolveViaFallbackLadderAsync(...)` instead of orphaning. **Tier 2 (per-record default)**: `ReadRegardingAnchorAsync` (REUSED, no 2nd mechanism) → `FindOrCreateDefaultThreadAsync(anchor.RecordType, anchor.RecordId, anchor)` — lazy, ONE per record. **Tier 3 (per-user master)**: no regarding → `ReadOwnerIdAsync` (`ownerid` off `sprk_communication`) → `FindOrCreateDefaultThreadAsync("systemuser", ownerId.ToString(), null)` — lazy, ONE per owning user. **Identity decisions**: (a) default+master both keyed by task-002 `sprk_isdefaultthread=true` + the **existing denormalized regarding pointer** (`sprk_regardingrecordtype`/`sprk_regardingrecordid`) as idempotency key — Tier 2 = the ADR-024 record, Tier 3 = `("systemuser", ownerId)`. `"systemuser"` is NOT in `RegardingFieldMap`, so a master never collides with a record-default and never surfaces in task-010/011 by-regarding reads (those query the 11 typed lookups). (b) `FindDefaultThreadAsync` query-before-create → idempotent/lazy; concurrency race documented as benign+non-fatal (point-forward, no dedup sweep). (c) Master owned by app-MI (thread `ownerid` NOT reassigned) — catch-all visibility under impersonated read is a 050/043 concern, out of FR-09 scope. **NFR-02 preserved** — ladder runs inside the existing outer try/catch; failure → null, never throws to send/capture. **Characterization-FIRST**: 5 tests pinning Tier-1 JOIN/CREATE/Direct/no-strategy-guard/NFR-02-swallow written + GREEN against UNEDITED resolver BEFORE editing; +6 extension tests (Tier2 lazy-create, Tier2 idempotent-join, Tier3 master-create, Tier3 idempotent-join, never-null both shapes, ladder-failure non-fatal). **544 Communication pass** (533 baseline + 11), 8 pre-existing skips. Build clean; publish **45.69 MB (≈0 vs 46.99)**; no CVE; code-review + adr-check clean (0 violations, 11 ADRs compliant incl. ADR-024/046/032/028/038); `/conflict-check` clean (no master divergence on `Services/Communication/`; 050 still 🔲, no concurrent BFF writer). No new file/service/DI/package — extends `ThreadResolver`, registered unconditionally via existing `CommunicationModule` (no gate → ADR-032 N/A). **⚠️ LIVE APPLY of 002 `sprk_isdefaultthread` DEFERRED** (MCP down) — code compiles vs field logical-name strings + unit-tested vs mocked boundary, per R1 build-and-defer-live. **Flag for 071**: 071's `BuildTopic` re-derive (gated on `sprk_nameisautoderived==Auto`) must NOT try to resolve `"systemuser"` regarding on a master thread — this task names the master `"Messages"`. Unblocks 071. |
| **021 result** | **Task 021 (regarding-mode PCF variant, W2/FR-04) ✅ COMPLETE 2026-07-19** — FULL rigor, sonnet/high. NEW sibling PCF `src/client/pcf/CommunicationTimelineRegarding/` (control `Spaarke.Controls.CommunicationTimelineRegarding` v1.0.0). **Decision: variant folder, not a mode-switch** on the deployed R1 `CommunicationTimeline` control — avoids regression risk on R1's live `sprk_communication`/`sprk_communicationthread` placements; the 11 regarding-family host entities are entirely disjoint from R1's placements anyway. Mirrors R1's virtual-ReactControl packaging exactly (`index.ts`/`authInit.ts`/`Host.tsx`/`App.tsx`/`Solution/`) with one net-new bit: `hostContext.ts`'s `resolveRegardingContext(hostEntityName, hostRecordId)` reads `(entityType, id)` straight off the Xrm page context — no lookup indirection needed (unlike R1's `sprk_communication`→thread-lookup case) since the host record itself IS the regarding target — validated against an 11-entity allowlist mirroring `RegardingFieldMap.All` (sprk_matter/project/invoice/servicerequest/workassignment/event/budget/analysis/organization/account/contact). Manifest declares bound `anchorField` (primary Name, `usage="bound" required="true"` — R1 lesson cited verbatim in the manifest comment) + React 16.14.0/Fluent 9.46.2 platform libraries; no apostrophes in any `description-key` (known Dataverse XSD gotcha, grep-verified clean). `@spaarke/auth` used only in `authInit.ts` (`initAuth`) + the App boundary (`authenticatedFetch` passed as a prop) — grep-confirmed the shared `CommunicationTimeline.tsx` component source has zero `@spaarke/auth` imports. Mounts `<CommunicationTimeline mode="regarding" entityType id authenticatedFetch bffBaseUrl pollIntervalMs />` — deliberately omits `threadId` (discriminated union, task-020 contract; passing both is a TS error). **Verification**: 16/16 new Jest tests green (pure `resolveRegardingContext`, all 11 families individually + 3 negative cases); `npm run build:prod` clean (2.35 MiB bundle — matches R1's own committed 2.45 MB precedent, both dominated by `@spaarke/auth`'s bundled `msal-browser`, not a regression); `tsc --noEmit` shows zero errors attributable to new files (all pre-existing barrel-import noise from unrelated shared-lib components, same class of finding task 020 already documented); solution ZIP packed + entry-verified (6 well-formed lowercase-named entries: solution.xml/customizations.xml/[Content_Types].xml/ControlManifest.xml/bundle.js/styles.css). Grep-verified **zero** `@azure/communication-*` in the bundle (NFR-04). Self-executed code-review + adr-check (ADR-006/021/022/026/028) — **0 Critical/Warning findings**; 2 Suggestion-level notes on R1-boilerplate duplication (`getXrm`/`getHostEntityName`/`getHostRecordId`, webpack config, jest scaffolding) — explicitly sanctioned by the task's "mirror R1, do not invent a new pattern" instruction, same duplication pattern already present between R1's own `CommunicationTimeline` and `CommunicationMessageActions`. **UI-test (Step 9.7) SKIPPED** — no `--chrome` session and no live Dataverse env available this session; substituted with the jest + bundle + manifest evidence above (mirrors 010/011/070's "build-and-defer-live" precedent). **Live `pac import` DEFERRED to task 022 / owner** (this task's scope stops at build+pack). **Contract for task 022**: place control `sprk_Spaarke.Controls.CommunicationTimelineRegarding` (Solution `CommunicationTimelineRegardingSolution` v1.0.0, ZIP at `src/client/pcf/CommunicationTimelineRegarding/Solution/bin/CommunicationTimelineRegardingSolution_v1.0.0.zip`) on all 11 regarding-family entity forms, binding `anchorField` to each entity's primary Name field; import is owner-run (`pac solution import`) since live Dataverse access is unavailable this session. |
| **Next action** | `continue` → next ready tasks: **W8 080** (seam tests — all deps 010/011/022/050/051/070/071 now task-complete), **W8 081** (architecture doc, deps 010/050/070 done). W5 (050+051), W6 (060), and W7 (070+071) are now fully done. Also open: W0-B live-apply gates (002/003 schema) + 010/011/021/022/023/030/040/041/050/051/060/070/071 live-verify remain owner-deferred (071 additionally has the trigger-wiring gap noted in its result row — a follow-on plugin recommendation, not yet a task; 060's `onSearchRecipients` PCF-wiring gap is similarly owner/follow-on, not a task). All task work via `task-execute`. |
| **022 result** | **Task 022 (11-form placement of the regarding-mode PCF, W2/FR-04) ✅ AUTHORED 2026-07-19** — spec+script `notes/022-pcf-form-placement.md` + `scripts/Verify-CommunicationTimelineRegardingPlacement.ps1`. **Key code-grounded finding**: corrected the working assumption "sprk_name for all sprk_ entities" — `IncomingAssociationResolver.GetPrimaryNameField()` (`Services/Communication/IncomingAssociationResolver.cs:469-483`) shows 3 of 9 custom entities use a dedicated primary-name field: `sprk_matter`→`sprk_mattername`, `sprk_project`→`sprk_projectname`, `sprk_event`→`sprk_eventname` (invoice/servicerequest/workassignment/budget/analysis/organization use `sprk_name`; `account`→`name`, `contact`→`fullname` OOB) — corroborated by grep hits in production code/config using those exact field names. 11-entity table verified verbatim against `RegardingFieldMap.All` — no invention/omission, no second regarding mechanism (ADR-024). **Write-path decision**: chose a maker checklist (Form Designer UI, per `PCF-DEPLOYMENT-GUIDE.md` Step 9) over authoring a PATCH script for the PCF field-bind step, because the `<controlDescriptions>/<customControl>` FormXml schema is not verified/tested end-to-end in this repo (unlike the stable subgrid-classid precedent in `scripts/Deploy-TodoSubgridsToElevenParentForms.ps1`) and this task's own rigor-reason flags the deploy as host-affecting/hard-to-reverse. Authored a **read-only** Web API verification script instead (PowerShell parser-verified, 0 syntax errors) that checks control registration + per-entity form placement + anchor-field presence — safe to re-run repeatedly as a progress tracker; it does not write anything. Note also has the import order (solution ZIP first), the 11-row placement table + maker steps, input-property guidance (copy the exact env-constant values already on R1's `CommunicationTimeline` placement), an 11-row smoke-matrix template, the POML `<ui-tests>` reproduced verbatim (matter+contact deep, ADR-021 dark mode, 9-form smoke), and an ADR/constraint compliance table (ADR-024/006/026/022, NFR-03). code-review + adr-check self-executed clean (docs+script only, no `.cs`/`.ts` touched). UI-test (Step 9.7) SKIPPED — no `--chrome`/live env this session, same as 020/021. **LIVE APPLY DEFERRED** (no Dataverse/MCP access) — owner runs `pac solution import` of the task-021 ZIP, executes the 11-form maker checklist, re-runs the verification script, fills the smoke matrix + UI tests, then task 080 (which depends on 022) can proceed. No active task. |
| **030 result** | **Task 030 (workspace widget upgrade, W3/FR-12) ✅ COMPLETE 2026-07-19** — NEW lib `@spaarke/communication-components` at `src/client/shared/Spaarke.Communication.Components/`: rich Pattern D `CommunicationsWorkspaceWidget` (Channel + Sent-date-range Popover filter-chip toolbar + a per-channel-count "card strip" derived from `onRecordsLoaded`, click-to-toggle-filter, the card-strip analog of Calendar's day-click + embedded `<DataGrid configId="e1826c4c-…" hostFilters onRecordsLoaded>`). **Deliberately NO custom `onRecordOpen`** — `BUILD-A-NEW-WORKSPACE-WIDGET.md` §6.6 cites Communications as the canonical "Layout 1 needs no per-widget wiring" example; the framework's `defaultRecordOpen` handles row-click. **Upgraded in place**: `register-workspace-widgets.ts` (`communications-list` Direct registration) + `communications.registration.ts` (LW section shim) — type string/section id/configId UNCHANGED (grep-verified exactly ONE `'communications-list'` occurrence in the registry — NFR-05). **Build note**: unlike `@spaarke/events-components` (pure `noEmit`), this lib needed a REAL **declaration-only** `tsc` build (`dist/*.d.ts`) because `@spaarke/ai-widgets`'s own build is a real-emit `tsc` with an explicit `rootDir` — a source-only cross-package mapping pulled `@spaarke/ui-components` raw source across that `rootDir` boundary (TS6059); fixed by (a) switching the widget's `ui-components` import from a deep-relative-source path to the bare `@spaarke/ui-components` package specifier (mirrors the OLD `DataverseEntityViewWidget.tsx` this replaces) resolved via a `dist/index.d.ts` paths mapping, and (b) giving the new lib its own declaration-only dist + a matching `dist`-based paths entry in `ai-widgets/tsconfig.json`. **Verification**: `@spaarke/communication-components` `npm run build` clean; `@spaarke/ai-widgets` full `npx tsc --noEmit` **0 errors** + `npm run build` clean + all 172 pre-existing tests pass + an ad-hoc `resolveWorkspaceWidget('communications-list')` factory-resolution test passed end-to-end (removed after verification). **DUAL-DEPLOY (NFR-07)**: added the lib as a `file:` dep + tsconfig `paths` + vite.config.ts alias/transpile-include to BOTH `LegalWorkspace` + `SpaarkeAi` (mirrors the `events-components` wiring, 3 files each). LegalWorkspace Vite build transformed 2869 modules (incl. everything this task touched) before hitting a **pre-existing, unrelated** `@spaarke/compose-components` → `mammoth`/`@spaarke/document-operations` gap (opportunistically added the missing `mammoth` dep; did NOT chase `document-operations` — different feature, flagged for follow-up). SpaarkeAi's `tsc-surface-gate` reports **0 surface-owned errors** (150 pre-existing shared-lib errors separately bucketed by that gate's own design); its Vite build transformed 235 modules before hitting a DIFFERENT pre-existing `@tiptap/extension-unique-id` gap in the same Compose feature. Net: both dual-deploy targets consume + correctly transform the new lib; the full production bundle on both sides is blocked by pre-existing Compose-feature packaging gaps unrelated to this task (recommend a follow-up project/task). `eslint` unavailable in this worktree for `ai-widgets`/`LegalWorkspace`/`SpaarkeAi` (pre-existing tooling gap, not installed) — `tsc`-based typechecks are the load-bearing clean signal. **Merge-order (plan §3, still open)**: coordinate with `spaarke-dataset-grid-framework-r2` (`communications.registration.ts` already carries its FR-08/DEF-005 edits, preserved) + PR #508 (Events.Components boundary — this task's approach mirrors it, does not directly copy from it). No live browser/UI verification (no Chrome/deployed env this session) — `<ui-tests>` deferred per this project's build-and-defer-live pattern. |
| **020 result** | **Task 020 (regarding-mode Timeline component, W2/FR-03) ✅ COMPLETE 2026-07-19** — `CommunicationTimelineProps` is now a discriminated union (`mode?: 'thread'` unchanged R1 vs `mode: 'regarding'` new); new `subcomponents/ThreadGroup.tsx` (collapsible group header: name + count) + `hooks/useRegardingPoll.ts` (mirrors `useThreadPoll` cadence/visibility-pause/overlap-guard) both REUSE `buildTimeline`/`MessageRow`/`ChannelBadge` unchanged (no fork, ADR-012); new `communicationTimelineApi.readByRegarding` wrapper (`GET /api/communications/by-regarding/{entityType}/{id}`). **Found + fixed a cross-task contract gap**: task-010's `ReadByRegardingAsync` queried the thread's `sprk_name` but the `ThreadReadResult` DTO discarded it before returning — FR-03's group-header acceptance criterion needed it. Fix: additive nullable `Name` field on `ThreadReadResult` (populated on by-regarding, `null` on the R1 per-thread read which never needed it), +2 assertions in `CommunicationByRegardingReadTests.cs`. BFF build clean; 34/34 Communication read-service tests green. Client: 25 new Jest tests (pure grouping `mapRegardingReadResultToGroups` + reducer regarding-slice), 39/39 total green; `npm run build` clean; lint clean on all touched files (full-package lint shows only 2 pre-existing unrelated errors in `DataGrid/HeaderCellContent.tsx`, untouched); grep-verified zero `@spaarke/auth`/`@azure/communication-*` imports. Design decisions (full detail in the POML `<notes>`): regarding mode is READ-ONLY (no compose box — sending stays in thread-id mode); all groups collapsed by default (`defaultExpandedThreadIds` prop override, seeded only on first load); groups ordered newest-activity-first (presentational, not access-affecting); polling reuses `pollIntervalMs` (default 5000) via full-snapshot refetch (endpoint has no incremental `since`). code-review + adr-check self-executed against ADR-021/028/012/026 — clean, 0 Critical/Violation findings. **Contract for task 021** (PCF wrapper): mount `<CommunicationTimeline mode="regarding" entityType={...} id={...} authenticatedFetch={...} bffBaseUrl={...} defaultExpandedThreadIds={...} onError={...} className={...} />` — no `threadId` in this mode (discriminated union — passing both is a type error), so 021 must branch its prop-building rather than reuse the thread-id-mode PCF's prop path unchanged. |
| **001 result** | email-r4 `Services/Communication` merged-present (8/8 ref files); `RegardingFieldMap.All` 11 lookups captured verbatim; thread delta pinned (add 11 typed lookups + Lookup discriminator + naming-edited + default-thread markers; keep Text `sprk_regardingrecordtype`; Q2 category/tags/description ABSENT); no `sprk_communicationparticipant` junction exists (003 net-new); `sprk_role` = From/To/Cc/Bcc = 100000000/1/2/3. ⚠️ Live MCP re-verify deferred (MCP down) — gate before 002/003 apply. |
| **Artifacts** | [`spec.md`](spec.md) (12 FR/8 NFR) · [`plan.md`](plan.md) (9-wave WBS) · [`README.md`](README.md) · [`CLAUDE.md`](CLAUDE.md) · [`tasks/TASK-INDEX.md`](tasks/TASK-INDEX.md) (24 tasks). |
| **Base** | Branch synced to latest master 2026-07-18 (merge brought assistant-r1 + email-r4 checkpoint work; email-r4 `Services/Communication` merged — R2 builds additively). |
| **Locked decisions** | Q1=**build participant junction** · Q2=**no category/tags** · Q3=**upgrade grid/widget in place** · Q4=**ship standalone page** · Q5=**all 11 entities** · Q-C=**two typed lookups** · Q-D=**write unresolved-address rows** · Q-E=**stay polling, no spine dependency**. |
| **Coordination** | `/conflict-check` before every BFF wave. Shared-path edits = **050** (participant write) + **070** (auto-threading) — `parallel-safe:false`. Widget **030** merge-order vs dataset-grid-r2 + PR #508. |
| **Status** | R2 = **initialized, ready to execute**; R1 = complete + deployed + archived. |

> **✅ POML task files**: all 21 `tasks/NNN-*.poml` files authored (6 parallel wave-authors) + validated
> (`Validate-TaskPoml.ps1` → 21 clean / 0 errors). Pipeline **paused before Step 5 wave execution** at owner
> request — ready for `work on task 001` (or `continue`).

---

## R1 final state (this session's ops work — all on master)

- **Deployed BFF** to `spaarke-bff-dev` (master `04fb31ecf`). Messaging endpoints live (401, not 404). Timeline PCF renders real messages on the thread form (verified by owner screenshot).
- **Deployed PCFs**: `CommunicationTimeline` v1.0.1 + `CommunicationMessageActions` v1.0.2 — both fixed with a **bound `anchorField` property** (a code component only appears in the form component library if it declares a bound field; the original had none). ZIPs under each PCF's `Solution/bin/`.
- **Merged to master**: PR #655 (messaging), #658 (ACS boot-safety + format), #659 (CI tier2 concurrency fix). Branch `work/messaging-communication-app-r1`.
- **Fixed 3 production issues this session**:
  1. **ACS boot-safety** — the BFF crash-looped at startup (SIGABRT/134) when `Communication__Acs__Endpoint` was unset (ACS client factory threw, resolved eagerly via `MembershipReconcileSweepService`). Fixed: `AcsIdentityService`/`AcsThreadService` now inject `Lazy<client>` (ADR-032). Regression test `AcsBootSafetyTests`. **Set `Communication__Acs__Endpoint=https://spaarke-acs-dev.unitedstates.communication.azure.com` on dev.**
  2. **PCF bound-property** (above).
  3. **CI `CI / Router` spurious red** — tier2 concurrency keyed on `github.ref` cancelled superseded master runs; alls-green treats cancelled tier2 as fail. Fixed: per-SHA concurrency group.
- **⚠️ Deploy prerequisite (binding for staging/prod)**: any env running this BFF MUST set `Communication__Acs__Endpoint` (dev is set; boot-safety fix means missing config no longer crashes, but SEND still needs it).
- **R1 open findings** (for the 090 wrap-up PR): send-into-existing-thread (MED), messaging-archival gap (MED — chat not SPE-archived), DI-cycle-refactor (LOW), config gate (app-user Share privilege on both messaging tables). Owner still to import nothing further (PCFs deployed).

---

## R2 design — key conclusions (from the 5-part investigation)

**Big insight: R2 is smaller than it looked — surfaces 2 & 3 already exist** (built by `ai-spaarke-ai-workspace-UI-r2`).

- **Surface 1 (record threads view) = NEW** — extend `CommunicationTimeline` to a **regarding-mode** (takes a Matter, calls a new `by-regarding` BFF endpoint, renders threads→timelines). Reuses the component + impersonation access filter. Optional VisualHost count card (client-fetch — tension T-1).
- **Surface 2 (grid) = ALREADY BUILT** — config record `sprk_gridconfiguration` GUID `e1826c4c-9575-f111-ab0e-7ced8ddc4a05`; framework auto-derives channel/person/date/regarding chips. R2 = optional ~50-line standalone page.
- **Surface 3 (SpaarkeAi widget) = EXISTS THIN** — `communications-list` widget is grid-only; R2 upgrades in place to rich Calendar-style (copy `CalendarWorkspaceWidget`), new lib `@spaarke/communication-components`.
- **CC-1 thread regarding** — add typed `sprk_regarding*` lookups + a **new Lookup discriminator** (the thread's `sprk_regardingrecordtype` is Text, not a Lookup — can't bind RegardingResolver to it); place RegardingResolver PCF on thread form (0 code). Naming re-derive + category/tags = new. Don't use CommunicationConnections/Field-Mapping.
- **CC-2 person filter** — sender/recipients are `;`-joined TEXT, not queryable. Needs a new **`sprk_communicationparticipant` junction** (reuse `ParticipantCorrelationRung` email→contact resolution; align ADR-034 tuple). Interim: lookup-only (`sprk_sentby`/`sprk_regardingperson`).
- **CC-3 read endpoints** — `by-regarding` + filtered `query`; extend `CommunicationThreadReadService` + `IImpersonatedCommunicationQuery` + `ICommunicationAccessFilter`. **Do NOT** reintroduce membership-union on reads (retired 2026-07-16).
- **CC-4 auto-threading** — subject → record-default → master.
- **CC-5 compose enrichment (just added)** — the R1 compose form has NO Subject/Cc/Bcc → every msg is `sprk_subject="(No Subject)"`, `sprk_name="Message: (No Subject)"`, and `To`→`sprk_to` TEXT. R2 adds Subject/topic (feeds naming), structured recipient picker (feeds the participant index), Cc/Bcc. Reuse existing `RecipientField`.

**OWNER DECISIONS (locked 2026-07-18, design §10):**
1. Participant index → **BUILD the `sprk_communicationparticipant` junction in R2** (no lookup-only interim). W5 mandatory.
2. Category/tags → **NOT in R2.** Threads = regarding + name only. Removes taxonomy check from W0.
3. Coordination with `ai-spaarke-ai-workspace-UI-r2` → **STILL OPEN** (one-line confirm needed): sanction upgrading their shipped `sprk_gridconfiguration` (`e1826c4c-…`) + `communications-list` widget **in place**. Only gate left before `/design-to-spec`.
4. Standalone "All Communications" page → **SHIP IT** (~50-line shell, copy `sprk_invoicespage`; register in `Deploy-AllDataGridConsumers.ps1`).
5. Scope → **ALL 11 regarding-family entities** from day one (Surface 1 on all 11 forms; W1/W4 test matrix expands; `by-regarding` endpoint already entity-set-agnostic).

**Net: R2 is the full-breadth build** — bigger on entities/junction/page, smaller by dropping category/tags.

**Message compose field mapping (as-built, for reference)**: To→`sprk_to` (text, `;`-joined) · Body→`sprk_body` · Rich/Plain→`sprk_bodyformat` (HTML=100000001/Plain=100000000) · Attach→`sprk_communicationattachment` child rows · (auto) type=Message(100000004), direction=Outgoing(100000001), from=`sprk_from`, sentat, name="Message: {subject}", thread lookup, inReplyTo, acsmessageid/acsthreadid. Persist: `CommunicationService.cs:695-727`.

---

## Files created/modified this session (R2)
- `projects/messaging-communication-app-r2/design.md` (NEW — draft, with CC-1..CC-5, 3 surfaces, hot-path decl, §11 reuse ledger, 8 waves, 5 open Qs).
- `projects/messaging-communication-app-r2/notes/r2-resource-investigation.md` (NEW — 5-audit findings archive with exact file paths).
- `projects/messaging-communication-app-r2/current-task.md` (this file).

## Recovery Instructions
1. Read Quick Recovery + "R2 design key conclusions" above.
2. If owner has answered the 5 questions → run `/design-to-spec` on `design.md`, then `/project-pipeline`. Otherwise, present the 5 questions.
3. R1: if asked to finish, task `090` wrap-up (README→Complete, `/test-diet`, lessons-learned, surface the open findings). R1 branch `work/messaging-communication-app-r1` still exists (merged, not deleted).
4. Do NOT re-run the R2 investigation — findings are in `notes/r2-resource-investigation.md`.
