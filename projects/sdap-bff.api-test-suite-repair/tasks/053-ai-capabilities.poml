<task id="053" project="sdap-bff.api-test-suite-repair">
  <metadata>
    <title>P23.M4 — Ai/Capabilities tests</title>
    <status>completed</status>
    <estimated-effort>2-3 hours</estimated-effort>
    <assigned>AI agent</assigned>
    <tags>phase-2-3, medium-tier, p23-m, bff-api, testing, ai, capabilities</tags>
    <parallel-group>23-M</parallel-group>
    <parallel-safe>true</parallel-safe>
    <parallel-reason>Services/Ai/Capabilities/*.cs (excluding Streaming*) is disjoint from other M-track batches and from H-track batches</parallel-reason>
    <rigor>FULL</rigor>
    <repair-not-rewrite>true</repair-not-rewrite>
  </metadata>

  <prompt>
    Repair the MEDIUM-tier Ai/Capabilities tests under
    `tests/unit/Sprk.Bff.Api.Tests/Services/Ai/Capabilities/*.cs`. EXCLUDE `Streaming*` files
    (handled by task 031). Per design.md §3.3 these are mocked-orchestration tests for capability
    router and tool dispatch.

    For each failing test:
    1. Identify failure (router rule change, capability registration change, tool dispatch change)
    2. Determine: `test-stale` vs `production-bug`
    3. Apply §6.2 trait
    4. Verify Pass

    Per project CLAUDE.md NFR-02 + design.md §4.1: this is repair, NOT rewrite. If ANY file's diff
    would exceed 50% line replacement, HALT and file
    `projects/sdap-bff.api-test-suite-repair/escalations/rewrite-request-T-053-{FileName}.md`
    per §4.8 escalation procedure. Do NOT proceed without owner approval.

    NO production code changes (NFR-01). Per §6.2, every touched test ends in `repaired`,
    `real-bug-pending-fix`, `flaky-quarantined`, or archived. `Failed` is FORBIDDEN at close.
  </prompt>

  <role>Senior .NET test repair specialist with capability-router/tool-dispatch expertise.</role>

  <goal>
    All Ai/Capabilities test files (excluding Streaming*) end in §6.2 final end-state. Zero Failed.
  </goal>

  <inputs>
    <file purpose="spec">projects/sdap-bff.api-test-suite-repair/spec.md</file>
    <file purpose="design">projects/sdap-bff.api-test-suite-repair/design.md</file>
    <file purpose="project-claude">projects/sdap-bff.api-test-suite-repair/CLAUDE.md</file>
    <file purpose="baseline">projects/sdap-bff.api-test-suite-repair/baseline/test-baseline-2026-MM-DD.trx</file>
    <file purpose="real-bug-ledger">projects/sdap-bff.api-test-suite-repair/ledgers/real-bug-ledger.md</file>
  </inputs>

  <constraints>
    <constraint source="NFR-01">MUST NOT modify production code</constraint>
    <constraint source="NFR-02">MUST NOT replace &gt;50% of any file without §4.8 escalation</constraint>
    <constraint source="§6.2">Every touched test gets a §6.2 final end-state trait</constraint>
    <constraint source="§4.5">MUST NOT touch `CustomWebAppFactory.cs`</constraint>
    <constraint source="FR-17">All MEDIUM-tier tests must end in §6.2 final end-state</constraint>
  </constraints>

  <knowledge>
    <files>
      <file>projects/sdap-bff.api-test-suite-repair/CLAUDE.md</file>
      <file>projects/sdap-bff.api-test-suite-repair/design.md</file>
      <file>.claude/constraints/bff-extensions.md</file>
      <file>.claude/constraints/ai.md</file>
      <file>.claude/adr/ADR-013-ai-architecture.md</file>
    </files>
  </knowledge>

  <context>
    <background>
      Per design.md §3.3 Ai/Capabilities is MEDIUM-tier. Streaming-related Capabilities tests are
      handled by task 031 (P23.A2); this task covers the non-streaming Capabilities tests.
    </background>
    <dependencies>
      <dependency task="014" status="not-started">Compile health verified</dependency>
    </dependencies>
  </context>

  <relevant-files>
    <file>tests/unit/Sprk.Bff.Api.Tests/Services/Ai/Capabilities/*.cs (excluding Streaming*)</file>
  </relevant-files>

  <steps>
    <step order="1">List `tests/unit/Sprk.Bff.Api.Tests/Services/Ai/Capabilities/*.cs` excluding `Streaming*`. From Phase 0 baseline TRX, identify failing tests.</step>
    <step order="2">For each file: compute pre-edit line count; read failing tests + production capability code.</step>
    <step order="3">If any planned file edit would exceed 50% of file lines → HALT, file `escalations/rewrite-request-T-053-{FileName}.md`, await owner approval.</step>
    <step order="4">Classify each failure: `test-stale` vs `production-bug`.</step>
    <step order="5">Apply traits per §6.2; file ledger entries as needed.</step>
    <step order="6">After each file: `dotnet test --filter "FullyQualifiedName~Ai.Capabilities"` (modulo Streaming exclusion) verify outcome.</step>
    <step order="7">Update `current-task.md` with disposition.</step>
    <step order="8">Verify `git status` shows NO production changes.</step>
  </steps>

  <tools>
    <tool name="dotnet">test</tool>
    <tool name="git">status</tool>
  </tools>

  <outputs>
    <output type="code">tests/unit/Sprk.Bff.Api.Tests/Services/Ai/Capabilities/*.cs (non-Streaming, repaired)</output>
    <output type="docs">projects/sdap-bff.api-test-suite-repair/ledgers/real-bug-ledger.md (entries if bugs found)</output>
  </outputs>

  <acceptance-criteria>
    <criterion testable="true">All Ai/Capabilities tests (non-Streaming) Pass or Skip-with-reason; zero Failed</criterion>
    <criterion testable="true">Per-file diff &lt;50% line replacement (NFR-02)</criterion>
    <criterion testable="true">Every touched test has a §6.2 final end-state trait</criterion>
    <criterion testable="true">No overlap with task 031 (Streaming Capabilities files)</criterion>
    <criterion testable="true">`git status` shows zero changes under `src/`, `power-platform/`, `infra/`, `scripts/`</criterion>
    <criterion testable="true">`CustomWebAppFactory.cs` is NOT modified</criterion>
  </acceptance-criteria>

  <notes>
    <scope-extension date="2026-05-31">
      Wave 1 baseline reconciliation (task 008) confirmed Services/Ai/Capabilities/* (non-Streaming)
      failure cluster: 2 failures in CapabilityRouterBenchmarkTests. Falls within existing relevant-files
      glob — no scope change, annotation only. See `notes/handoffs/phase23-scope-delta-2026-05-31.md`.
    </scope-extension>
  </notes>
</task>
