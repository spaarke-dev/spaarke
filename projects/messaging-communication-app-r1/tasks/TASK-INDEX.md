# TASK-INDEX — messaging-communication-app-r1

> **Generated**: 2026-07-16 via `/project-pipeline`
> **Total tasks**: 28 (27 work + 1 wrap-up)
> **Legend**: 🔲 not-started · 🔄 in-progress/needs-retry · ✅ complete · ⛔ blocked/gated

---

## Task Registry

| # | Title | Wave | Tags | FR | Deps | Blocks | Parallel-safe | Rigor | Model/Effort | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| 001 | Phase-0: audit live `sprk_communication` schema + confirm `Message=100000004` choice integer (MCP read) | W0 | dataverse, spike | — | — | 004,005,006 | true | STANDARD | sonnet/high | ✅ (live Dataverse MCP; Message=100000004 confirmed; 6 cols + communicationUserId absent→clean adds; grouping stays (A)) |
| 002 | Phase-0: decide private-thread grant mechanism (`GrantAccess` vs `sprk_externalrecordaccess`) | W0 | spike, security | — | — | 042 | true | STANDARD | sonnet/high | ✅ (**option B APPROVED by owner 2026-07-16** — sprk_externalrecordaccess overlay ∪ ADR-034; binds 042/041/050) |
| 003 | Phase-0: ACS spike (identity + server-minted chat token + thread + Event Grid round-trip; latency, echo-dedup, publish-size) | W0 | spike, acs | — | — | 010,011,012,020,030 | true | STANDARD | sonnet/high | ✅ (ACS harness compile-verified vs real SDKs; publish delta **+0.22 MB MEASURED**→~45.52; echo-dedup key=ACS msg id; latency needs live infra; researcher-memory gap flagged) |
| 004 | Schema: `sprk_communicationthread` entity + `sprk_thread` lookup + thread↔channel child table | W0 | dataverse, schema | FR-05 | 001 | 040,050 | true | STANDARD | sonnet/high | ✅ (owner-created + **verified live** 2026-07-16: sprk_communicationthread entity + sprk_communicationchannelref child + sprk_thread lookup; anchor REUSES sprk_regardingrecord* — no sprk_anchor*) |
| 005 | Schema: `sprk_communication` privacy/internal-only/privilege/ACS-key columns + `communicationUserId` on user/contact | W0 | dataverse, schema | FR-08 | 001 | 042,010 | true | STANDARD | sonnet/high | ✅ (owner-created + **verified live**: lookup as-built **`sprk_communicationthread`** [not sprk_thread]; ACS field as-built **`sprk_acsthreadid`** [not sprk_acschatthreadid]; isprivate/isinternalonly/privilegeclassification/acsmessageid + communicationuserid on user+contact — see AS-BUILT table in messaging-schema-spec.md) |
| 006 | `CommunicationType.Message = 100000004` C# enum extension | W0 | bff-api | FR-16 | 001 | 020 | true | FULL | sonnet/high | ✅ (enum +Message=100000004; build clean, 352 Comm tests pass; gates PASS; publish ~0 delta; pre-existing Kiota HIGH CVE noted, not a regression) |
| 007 | Author **ADR-046** (ACS messaging channel) — concise + full; INDEX placeholder → Accepted | W0 | adr, docs | FR-17 | — | 020,021 | **false** (`.claude/`) | STANDARD | opus/high | ✅ (main-session; ADR-046 concise→Accepted + full docs/adr authored + INDEX updated; ADR-047 reserved for notification-spine) |
| 010 | ACS identity + server-side chat-token minting (`communicationUserId ↔ Dataverse`; uniform minting) | W1 | bff-api, acs, auth | FR-03 | 003,005 | 011,020,051 | true | FULL | sonnet/high | ✅ (Services/Communication/Acs/ IAcsIdentityService+impl; central TokenCredential ADR-028; sprk_communicationuserid persist; 7 tests; build clean; publish +~0.1MB; no new CVE; gates clean. Live ACS deferred) |
| 011 | ACS thread + membership ops (create thread w/ 30-day retention; Add/RemoveParticipants) | W1 | bff-api, acs | FR-15 | 010 | 020,041,051 | true | FULL | sonnet/high | ✅ (Services/Communication/Acs/ IAcsThreadService+impl; Azure.Communication.Chat 1.4.0; 30-day retention at create [clamped 30–90]; batch Add/Remove rate-aware [.Chunk(10)], 429→retryable, idempotent, MRI guard; 12 tests; +~0.1MB; no new CVE; gates clean. Live thread-create deferred) |
| 012 | ACS provisioning: per-customer resource + Event Grid system topic/subscriptions (ADR-027) | W1 | bff-api, acs, provisioning | FR-18 | 003 | 030 | true | FULL | sonnet/high | ✅ (Bicep-driven ADR-027: acs-communication.bicep + customer.bicep; ADR-032 Null-Object DeferredAcsBoundaryProvisioner in RegistrationModule [Path-A vs CommunicationModule to avoid 010 conflict]; 8 tests; publish ≈0; runbook notes/012-acs-provisioning-runbook.md. Live provisioning deferred) |
| 020 | `MessagingChannelSender` + `MessagingArchiver` (ADR-045 seam impls; dispatch by `Message`; transcript → SPE) | W2 | bff-api, communication, acs | FR-01 | 006,007,010,011 | 051,070 | true | FULL | sonnet/high | ✅ (Channels/MessagingChannelSender+MessagingArchiver, keyed to Message; consumes 010/011 Acs seams; ProviderMessageId=ACS msg id [echo-dedup contract]; transcript→SPE ADR-007; CommunicationService stays ACS-free; additive seam props; 10 tests; 382 Comm pass; pkg delta 0; gates clean. Live send→051/deferred) |
| 021 | `ICommunicationChannelIngestor` seam (net-new) + `MessagingIngestor` | W2 | bff-api, communication, architecture | FR-02 | 007 | 030,031 | true | FULL | sonnet/high | ✅ (NET-NEW ICommunicationChannelIngestor seam + MessagingIngestor [persists one Message via IGenericEntityService, sprk_acsmessageid dedupe key, best-effort enrichment NFR-02]; dispatcher ResolveIngestor added [optional ctor, back-compat]; email inbound byte-unchanged; 6 tests; pkg delta 0; gates clean. Normalizer/idempotency/DLQ = 031) |
| 030 | Event Grid webhook ingress (subscription-validation handshake) → Service Bus job | W3 | bff-api, communication, jobs | FR-02 | 012,021 | 031 | true | FULL | sonnet/high | ✅ (Api/AcsEventGridEndpoints POST /api/communications/acs/eventgrid [AllowAnonymous+rate-limit] + AcsEventGridIngressService; security boundary: handshake echo + fail-closed topic allow-list + optional const-time shared-secret, nothing enqueued on any negative path; enqueues IncomingMessagingJobPayload on existing comm queue; 7 tests; pkg delta 0; gates clean. Live EventGrid sub deferred [needs reachable BFF]) |
| 031 | ACS-event normalizer → `NormalizedMessage` → idempotent persist (dedupe on ACS message id) + DLQ | W3 | bff-api, communication, jobs | FR-02,04 | 030,021 | 040,051 | false (serial after 030) | FULL | sonnet/xhigh | ✅ (Engine/AcsEventNormalizer + Jobs/Handlers/IncomingMessagingJobHandler [JobType=IncomingMessaging]; idempotent via IIdempotencyService key `acs-msg:{acsMessageId}`, persist-before-mark, lock-race clean; **fixed latent bug: CommunicationJobProcessor was email-only, would DLQ all messaging jobs**; DLQ on Poisoned; enrichment non-fatal NFR-02; 21 msg tests, 376 Comm pass; +0.28MB cumulative; gates clean. Live EventGrid deferred) |
| 040 | `IThreadResolver` (direction-symmetric) — extend `ThreadContinuityRung` + `CommunicationService`; email characterization first | W4 | bff-api, communication, refactoring | FR-06 | 004,031 | 041,042,043,050,051 | **false** (shared-path serial) | FULL | opus/xhigh | ✅ (IThreadResolver + per-channel IThreadKeyStrategy [Email ancestry / Messaging ACS ChatThreadId]; invoked from orchestrators [IncomingCommunicationProcessor/MessagingIngestor/CommunicationService], NOT the pure ThreadContinuityRung — **directional deviation, more ADR-compliant, documented**; optional null ctor param keeps frozen tests untouched; reuses regarding family [no sprk_anchor*]; best-effort NFR-02; **characterization 414→421, 0 email regressions**; gates 0 violations. Outbound-chat assign + backfill deferred by design) |
| 041 | Membership derivation + reconcile job (open via ADR-034; reconcile ACS from Dataverse; event + sweep) | W4 | bff-api, communication, jobs | FR-07 | 040,011 | — | true | FULL | sonnet/high | ✅ (Services/Communication/Membership/ derivation + ACS reconcile job [existing sdap-jobs queue, event-driven + periodic sweep, audit/change]; reverse-ADR-034 open derivation [reuses discovery, not new engine]; **projection-never-exceeds proven [resulting⊆desired, fail-closed guard]**; derivation errors propagate→retry [no mass-removal thrash]; IThreadExplicitParticipantReader Null-Object seam [042/043 fill]; 19 tests; cherry-picked [aaa50f917], 671 tests green; gates 0 violations. Live reconcile deferred) |
| 042 | Privacy / internal-only / privilege — BFF query-filter enforcement, point-forward | W4 | bff-api, communication, security | FR-08 | 040,005,002 | 050 | true | FULL | opus/xhigh | ✅ **REWORKED to impersonation** (2026-07-16, research-driven): record access = Dataverse impersonation [`MSCRMCallerID`=systemuserid, fail-closed on empty, no app-only fallback] via new `DataverseImpersonation` + `RetrieveMultipleImpersonatedAsync` [additive to DataverseWebApiService]; CommunicationAccessFilter now pure sync app-flags [internal-only fail-closed + privilege metadata-only/no-AI ADR-015]; hand-computed union retired for reads [design §5 leverage-not-rebuild]; RetrievePrincipalAccess documented for discrete gates; 423 Comm tests, security gate 0 Critical. **Live impersonation deferred → owner config: app-user Delegate role + tables Read=User-level** |
| 043 | 1:1 direct threads — explicit two-participant membership | W4 | bff-api, communication | FR-09 | 040 | — | true | FULL | sonnet/high | ✅ (POST /threads/direct find-or-creates a `sprk_threadtype=Direct` thread owned by caller + POA `GrantAccess` share to the other participant [OOB Manage-access = the decided access model; NO new table, NO regarding anchor]; real `IThreadExplicitParticipantReader` [`DirectThreadExplicitParticipantReader`] replaces the 041 Null-Object → owner ∪ POA-grantees, topology-gated [empty for Open]; find-or-create dedup checks BOTH ownership directions [ordered-pair reuse]; **CRITICAL message-access fix**: `GrantMessageAccessAsync` grants each Direct-thread message ReadAccess to both participants on BOTH send [051] + ingest [021/031] paths [best-effort/non-fatal, no-op for non-Direct] — needed because msgs are app-owned + Read=User-level; 3 POA primitives added to `DataverseWebApiService` [OTC cache, GrantAccess, POA read]; `IDataverseAccessGrantService` ADR-010 test-seam; reuses `ICallerSystemUserResolver`; 11 tests [incl. third-user-excluded, both-directions reuse, no-anchor] + 478 Comm pass; build clean; publish 46.99 MB [Δ+1.35, <5 justify]; CVE pre-existing Kiota only; gates CLEAN [3 ArchTest fails pre-existing]. **NEW owner config gate: app-user Share privilege on both tables. OPEN FINDING: Open-thread messages need analogous grant — see access-model-decision.md**) |
| 050 | BFF thread-read + unread-count endpoints (access-filtered per FR-08) | W5 | bff-api, communication | FR-11 | 040,042,004 | 060 | true | FULL | sonnet/high | ✅ (2 GET endpoints `/threads/{id}/messages` + `/unread-count` on existing group; new SCOPED `CommunicationThreadReadService` reads IMPERSONATED [`RetrieveMultipleImpersonatedAsync` via new ADR-010 test-seam `IImpersonatedCommunicationQuery`] → applies SHARED task-042 `ICommunicationAccessFilter` [no 2nd filter]; reuses `ICallerSystemUserResolver` oid→systemuserid, fail-closed 403 on unresolved [no app-only fallback]; attachments = 1 bulk impersonated query [no fan-out, NFR-07]; last-seen = caller `?since` [no new table]; 7 tests [real filter, mock only Dataverse boundary] + 467 Comm pass; build clean; publish 45.64 MB [Δ≈+0.01]; CVE pre-existing Kiota only; 9.5 code-review+adr-check CLEAN [3 ArchTest fails proven PRE-EXISTING via stash]; conflict-check soft-pass) |
| 051 | Outbound send path — persist-on-send + echo-dedup wiring (messaging dispatch) | W5 | bff-api, communication, acs | FR-04 | 020,031,040,010,011 | 062 | true | FULL | sonnet/xhigh | ✅ (CommunicationService Message branch SendMessageAsync: persist-on-send [1 record, sprk_acsmessageid/acsthreadid], dispatch via existing ResolveSender(Message)→020, best-effort 040 thread-resolve + enrichment [NFR-02], no ACS type/token leak; **echo-dedup marks `acs-msg:{ProviderMessageId}` = 031's key → real send→echo e2e = exactly one record**; IServiceScopeFactory singleton→scoped bridge; 8 tests, 464 Comm pass; +0.33MB; gates 0 violations. Live BFF e2e deferred; blocks 062) |
| 052 | Open-thread message access grant — impersonated-read completion (gap closure from 043) | W5 | bff-api, communication, security | FR-11,NFR-06 | 040,041,043,050 | 060 | false | FULL | sonnet/high | ✅ (generalized 043's `GrantMessageAccessAsync` hook [same signature, same 2 call sites]: Direct → owner∪POA [UNCHANGED, early return]; Open/record-anchored → grant msg ReadAccess to the task-041 `DeriveAuthorizedSetAsync` **systemuser** participants [contacts skipped R2], same `GrantAccessAsync` [no 2nd mechanism]; best-effort/non-fatal. Closes the read-model gap so task-050 impersonated reads return matter-thread msgs. **Broke a real 3-node DI cycle** [AccessSvc→Derivation→Reader→AccessSvc] with `Lazy<IThreadMembershipDerivationService>` [documented; verified: build clean + 481 Comm unit + 12 WebApplicationFactory&lt;Program&gt; boot tests pass]; +3 tests [Open grants exactly derived set, non-member excluded, Direct no-regression, contact-skip, failure-swallow]; publish ≈46.99 MB [Δ≈0]; CVE pre-existing Kiota only; gates CLEAN. Suggestion logged: future refactor could extract participant-reader to remove the cycle) |
| 060 | Polling conversation/timeline component (`@spaarke/ui-components`, Fluent v9): interleaved email+chat, reply nesting, compose box, unread indicator, ~5s poll | W6 | frontend, fluent-ui | FR-10 | 050 | 061,063 | true | FULL | sonnet/high | ✅ (`CommunicationTimeline/` in @spaarke/ui-components: chronological interleave + cycle-safe/hop-capped `inReplyTo` nesting [pure `buildTimeline`, 19 tests]; `useThreadPoll` 5s [prop-overridable] + Page-Visibility pause + in-flight guard + unmount cleanup; consumes 050's `/threads/{id}/messages` + `/unread-count` via injected `authenticatedFetch` [NO `@spaarke/auth` import, grep-verified]; NO ACS SDK [grep-verified, NFR-04]; Fluent v9 + `makeStyles`/`tokens` + DOMPurify-sanitized HTML bodies; REUSES EmailComposer `BodyEditor`/`AttachmentList`/`RecipientField` [import not clone]; `npm run build` [tsc] + lint clean [2 pre-existing DataGrid errors untouched]. **Send-contract gap flagged** [compose sends Email-typed, no communicationType/inReplyTo — same family as finding #2; belongs to 062/send-path follow-up]) |
| 061 | Package timeline as PCF + deploy to OOB form + UI tests | W6 | pcf, deploy, e2e-test, dataverse | FR-10 | 060 | — | true | FULL | sonnet/high | ✅ (NEW virtual PCF `src/client/pcf/CommunicationTimeline/` mirrors the 062 `CommunicationMessageActions` template: namespace `Spaarke.Controls`, control-type `virtual`, v1.0.0; mounts shared `<CommunicationTimeline/>` [React-16 type-seam cast] with `threadId`/`authenticatedFetch`/`bffBaseUrl`/`pollIntervalMs=5000`. `threadId` via pure `resolveThreadId(entity,recordId,threadLookup)` [6 unit tests ✅]: on `sprk_communicationthread`→record id, on `sprk_communication`→`_sprk_communicationthread_value` lookup, else info-notice. **ADR-022 platform libs React 16.14.0 + Fluent 9.46.2** [externalized `Reactv16`/`FluentUIReactv940`, React 18 NOT bundled]; `@spaarke/auth` at PCF boundary only [same authInit + env-var pattern, no new env vars]; `npm run build:prod` clean [asset-size perf warns only]; **NFR-04 no `@azure/communication` in bundle** [grep-verified 0]. Optional `onQuoteIntoEmail`/`onSearchRecipients`/`associations` NOT wired [host-owned dialog, deferred out of R1 viewer scope]. Solution ZIP `Solution/bin/CommunicationTimelineSolution_v1.0.0.zip` [639 KB] packed. **`pac solution import` DEFERRED to owner** [owner env]. 9.5 gates clean [ADR-021/022/026/028]) |
| 062 | PCF send/respond accessories on OOB `sprk_communication`/thread form (mirror `CommunicationActions`) | W6 | pcf, frontend, communication | FR-12 | 051,060 | — | true | FULL | sonnet/high | ✅ (§11: NEW `CommunicationMessageActions` PCF [CommunicationActions hard-gates non-Email → justified new control]; platform React 16/17 + Fluent v9; reuses TimelineComposeBox; `@spaarke/auth` at boundary only; NO ACS in bundle [grep 0]; `build:prod` clean + solution ZIP packaged. **Closed the send-contract gap [finding #2]**: client `sendCommunication` + BFF `SendCommunicationRequest` gain `communicationType`/`threadId`/`inReplyTo` [additive, email path unchanged]; `CommunicationService.AssignExplicitThreadAsync` stamps `sprk_communicationthread` lookup to the target thread [R1 Dataverse-association; ACS-thread reuse = R2] + fires the 052 grant [verified]; 484 Comm tests pass. **DEPLOY DEFERRED to owner** — `CommunicationMessageActionsSolution_v1.0.0.zip` built + validated; handoff steps in commit/notes. Minor: reused compose box requires a To that ACS ignores [UX polish]) |
| 063 | Bidirectional inline content quoting (email↔message) via `sprk_body` | W6 | frontend, communication | FR-13 | 060 | — | true | FULL | sonnet/high | ✅ (pure `quoteBody(src, srcFmt, tgtFmt, attribution?)` helper [HTML `<blockquote>` vs `>`-prefix per target fmt using bodyformat ints 100000000/1; source-HTML→plaintext via block-boundary→\n + DOMPurify strip; XSS-safe incl. round-trip; 20 tests]; quote-into-message reuses the 060 `TimelineComposeBox` prefill prop [§11 one mechanism]; quote-into-email via `onQuoteIntoEmail` callback whose payload matches EmailComposer's `initial*` props exactly [host spreads with zero translation]; Fluent v9 `ChatRegular`/`MailRegular` affordances on MessageRow; NO ACS import, NO `.eml` parse [grep]; tsc + lint clean [2 pre-existing DataGrid errors untouched]; 20/20 + 19/19 + 141/141 jest pass) |
| 070 | Message attachment materialization (ACS/file → SPE → `sprk_document` → intersection); enforce `CHAT-ATTACHMENT-POLICY.md` | W7 | bff-api, communication, spe | FR-14 | 020,005 | — | true | FULL | sonnet/high | ✅ (MessageAttachmentMaterializer: ACS/file→SPE [SpeFileStore ADR-007]→governed sprk_document→sprk_communicationattachment intersection; reference-not-binary; CHAT-ATTACHMENT-POLICY enforced pre-upload [25MB cap+MIME allow-list; 413/415 ProblemDetails]; schema unchanged; 8 tests; ran in isolated worktree→cherry-picked [242ab5e72], 590 tests green; gates clean. Caller-wiring in 031/060; live round-trip deferred) |
| 080 | Vertical-slice seam tests: messaging send/archive/ingest, `IThreadResolver`, privacy; preserve email-inbound characterization | W8 | bff-api, testing | NFR-08 | 031,040,042,051 | — | true | TEST-MODIFYING | sonnet/high | ✅ (**reuse-first per ADR-038 no-clone**: ACs ~95% already satisfied by existing MAINTAIN-class tests — echo-dedup `CommunicationServiceMessageSendTests`, idempotency+DLQ `IncomingMessagingJobHandlerTests`, resolver `ThreadResolverSeamTests`, privacy `CommunicationAccessFilterTests`+`CommunicationThreadReadServiceTests`, email-characterization `InboundPipelineTests`+`IncomingAssociationResolverTests`. Additive only: **NEW `tests/integration/seam/Communication/MessagingSpineSeamTests.cs`** [2 tests composing raw ACS event→real `AcsEventNormalizer`→dispatcher→`MessagingIngestor`→real `ThreadResolver`→persist, fresh+join branches — the composition gap no single existing test covers] + **point-forward-open** `ReadThreadAsync_PrivateThreadOpenedPointForward_ReturnsOnlyPostBoundaryMessages` in `CommunicationThreadReadServiceTests` [genuine NFR-06 gap]. Coverage map `notes/080-seam-coverage-map.md` feeds `/test-diet`. Verified 487 pass/8 skip/0 fail; banned-pattern grep clean; no product code change. 9.5 gates clean. **Finding 1 (OPEN)**: Message send path never invokes archiver → no SPE transcript for chat [AC1 half not composable without fabricating behavior — surfaced, not faked]) |
| 081 | Architecture doc: thread model + ACS transport + ingestor seam (wire ADR-046) | W8 | docs | FR-17 | 040,007 | — | true | STANDARD | sonnet/high | 🔲 |
| 090 | Project wrap-up (README Complete, lessons-learned, `/test-diet`, archive) | Wrap | wrapup | — | (all) | — | **false** | STANDARD | sonnet/high | 🔲 |

---

## Parallel Execution Groups

| Group | Tasks | Prerequisite | Notes |
|---|---|---|---|
| **W0-A** | 001, 002, 003, 007 | — | Spikes + ADR (007 main-session `.claude/`); 002 security-sensitive; 003 gates all ACS work |
| **W0-B** | 004, 005, 006 | 001 | Schema + enum (distinct surfaces, parallel) |
| **W1** | 010 → 011; 012 (‖) | 003 (+005 for 010) | ACS server integration; identity before thread ops; 012 parallel |
| **W2** | 020, 021 | 006 + 007 + W1 | Channel provider seams; 021 adds net-new ingestor seam |
| **W3** | 030 → 031 | 012 + 021 | Inbound capture; 031 serial after 030 (idempotency/echo-dedup) |
| **W4** | **040 (serial)** → {041, 042, 043} | 004 + 031 | 040 serializes shared-path edit; 041–043 parallel after |
| **W5** | 050, 051 | 040 + 042 (050); 020/031/040 (051) | Endpoints — read + write |
| **W6** | 060 → {061, 062, 063} | 050 (+051 for 062) | Timeline component then PCF packaging + accessories + quoting |
| **W7** | 070 | 020 + 005 | Attachment materialization (parallel track after W2) |
| **W8** | 080, 081 | W1–W7 substantially complete | Seam tests + doc |

**Max concurrency**: 6 agents/wave. `.claude/`-touching tasks (007) + wrap-up (090) run main-session, sequential.

**Model tiers (per CLAUDE.md §8.5)**: default **sonnet @ high**. **opus** on: **007** (ADR authoring), **040** (shared-path engine extension over the frozen email path), **042** (privacy — security-sensitive). **effort: xhigh** on: **031** (idempotent capture / echo-dedup), **040**, **042**, **051** (echo-dedup wiring). All others sonnet @ high.

## Critical Path

```
001 → 004 → 040 → 050 → 060 → 061 → 080 → 090
003 → 010/011 → 020 → 021 → 030 → 031 → 040  (ACS + inbound spine feeds 040)
```

## High-Risk / Watch Items

- **042 (privacy/privilege)** — R1's highest-risk area (NFR-06, security-sensitive). Explicit `code-review` + `adr-check` at Step 9.5; ACS membership never exceeds Dataverse-derived access; point-forward switch.
- **040 (`IThreadResolver`)** — edits shared `Services/Communication/` code (email-r4). Characterization tests for existing email MUST stay green before extending. `parallel-safe: false`. `/conflict-check` before PR.
- **031 (idempotent capture)** — Event Grid at-least-once + duplicates + own-echo; dedupe on ACS message id via `IIdempotencyService`; DLQ from day one.
- **003 (ACS spike)** — gates W1/W2; measure send→persist latency, echo-dedup, publish-size before build.
- **notification-spine coordination** — align `threadId` contract + `kind` taxonomy at joint intake; not an R1 blocker (R1 polls).
- Every BFF-touching task: `/conflict-check` before PR + publish-size + CVE report (root §10).

## FR Coverage

FR-01→020 · FR-02→021,030,031 · FR-03→010 · FR-04→031,051 · FR-05→004 · FR-06→040 · FR-07→041 · FR-08→005,042 · FR-09→043 · FR-10→060,061 · FR-11→050 · FR-12→062 · FR-13→063 · FR-14→070 · FR-15→011 · FR-16→006 · FR-17→007,081 · FR-18→012. NFRs distributed (031,042,080; publish-size on every BFF task).
