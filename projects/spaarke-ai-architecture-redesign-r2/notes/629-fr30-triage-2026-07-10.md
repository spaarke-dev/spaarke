# #629 FR-30 memory-governance — triage disposition

> **Date**: 2026-07-10 · **From**: compose-r2 (FR-30, task 063 §6.5 escalation) → core (redesign-r2)
> **Verdict**: **DEFER to the memory hard-governance project** (the same separate governance project the spec already names). NOT r2 core scope. Maps to an existing operator ruling — not a new decision.

## What #629 asks (two coupled asks)

- **Ask 1 — dispatched-action gated capture path.** A governed `memory.write` reachable from a **non-chat dispatched-action completion**, consumable by `Services/Compose` through a `PublicContracts` facade. The shipped `memory.write` (task 057) is chat-invocation-context-only (`SupportedInvocationContexts => Chat`; non-chat `Validate()` = Failure by design). Compose's insights originate from dispatched catalog Actions, so no path exists and compose cannot fork one (ADR-013 facade + no-local-variant charter).
- **Ask 2 — untrusted-origin governance gate.** AI-derived (untrusted-origin) memory writes MUST be a governed, Policy-v2-visible **deliberate promotion**, not a silent auto-write — FR-30's memory-poisoning-prevention requirement.

## Why this is the deferred governance project, not r2

1. **Ask 2 IS the deferred work, verbatim.** The r2 spec Deferrals section defers "Memory hard-governance rules → separate governance project (owner ruling 2026-07-08): full **untrusted-origin ban + `trustLevel` enforcement**, semantic-retrieval↔memory trust boundary, litigation-hold, memory-poisoning eval families." Ask 2 = the untrusted-origin gate + poisoning prevention = exactly that deferral. Operator re-affirmed the accepted residual risk (cross-session document-injection poisoning) at reconciliation.

2. **Ask 1 without Ask 2 is forbidden by r2's own constraints.** compose states plainly: "Ask 1 without Ask 2 is a poisoning surface." r2's project CLAUDE.md Key Technical Constraints: **"Untrusted content can NEVER originate a memory write."** Building the dispatched-action (untrusted-origin) capture path WITHOUT the gate would ship the exact poisoning surface the deferred project exists to prevent. The two asks are coupled and must land together — in the governance project.

3. **r2's `memory.write` posture was deliberately chat-only + silent-Tier-1.** Extending it to a governed non-chat promotion is a different governance model (deliberate-promotion, Policy-v2-visible) than r2 shipped (silent auto-capture) — that model belongs to the governance project's design, not a bolt-on.

## Disposition

- **#629 → assigned to the memory hard-governance project** (same project carrying #616's row-level memory-read piece and the group-e deferrals). Both asks ship there, together.
- **compose-r2 FR-30 user value** waits on that project. Their task 063 (insight-distillation + facade invocation + workspace→Record scope + capture→recall eval) is "ready on delivery" — it plugs into Ask 1's facade once the governance project defines it.
- **No r2 core work.** Recorded in the reconciliation as part of the group-e deferral (already operator-signed).

## Response to compose
See `notes/HANDOFF-to-compose-r2-fr30-triage-2026-07-10.md`.
