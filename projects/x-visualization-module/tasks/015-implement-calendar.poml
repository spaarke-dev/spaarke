<?xml version="1.0" encoding="UTF-8"?>
<task id="015" project="visualization-module">
  <metadata>
    <title>Implement CalendarVisual component</title>
    <phase>2: Core Chart Components</phase>
    <status>not-started</status>
    <estimated-hours>4</estimated-hours>
    <dependencies>004</dependencies>
    <blocks>017, 020</blocks>
    <tags>pcf, react, fluent-ui, frontend</tags>
  </metadata>

  <prompt>
    Build the CalendarVisual component using Fluent v9 primitives. This is a custom
    calendar grid showing events/tasks by date with day click for drill-through.
    Note: Calendar is NOT part of @fluentui/react-charting.
  </prompt>

  <role>
    React developer with Fluent UI v9 expertise. Custom component design.
  </role>

  <goal>
    A reusable CalendarVisual component that displays a monthly calendar grid
    with event indicators and day click for drill-through.
  </goal>

  <context>
    <background>
      Calendar visual shows tasks/events/deadlines by date. Users click a day
      to see all records for that date. Not a full calendar app, just visualization.
    </background>
  </context>

  <constraints>
    <constraint source="spec">MUST use @fluentui/react-components v9 primitives (not charting)</constraint>
    <constraint source="ADR-021">MUST use Fluent design tokens for all styling</constraint>
    <constraint source="ADR-021">MUST support light, dark, and high-contrast modes</constraint>
    <constraint source="project">MUST emit DrillInteraction on day click with date filter</constraint>
  </constraints>

  <knowledge>
    <files>
      <file>.claude/adr/ADR-021-fluent-design-system.md</file>
      <file>.claude/patterns/pcf/theme-management.md</file>
      <file>src/client/pcf/CLAUDE.md</file>
    </files>
  </knowledge>

  <steps>
    <step order="1">Create CalendarVisual.tsx component file</step>
    <step order="2">Define props interface (events, currentMonth, onDayClick)</step>
    <step order="3">Build month grid layout using Fluent v9 layout primitives</step>
    <step order="4">Add day cells with event count indicators</step>
    <step order="5">Implement month navigation (prev/next)</step>
    <step order="6">Implement onDayClick that emits DrillInteraction with "between" operator for date</step>
    <step order="7">Style with makeStyles and Fluent tokens</step>
    <step order="8">Create Storybook stories (empty, with events, navigation)</step>
    <step order="9">Verify dark mode appearance</step>
    <step order="10">Update TASK-INDEX.md: change this task's status to completed</step>
  </steps>

  <outputs>
    <output type="code">src/client/pcf/VisualHost/control/components/CalendarVisual.tsx</output>
    <output type="code">src/client/pcf/VisualHost/stories/CalendarVisual.stories.tsx</output>
  </outputs>

  <acceptance-criteria>
    <criterion testable="true">Component renders monthly calendar grid</criterion>
    <criterion testable="true">Days with events show indicator/count</criterion>
    <criterion testable="true">Day click emits DrillInteraction with date range</criterion>
    <criterion testable="true">Month navigation works (prev/next buttons)</criterion>
    <criterion testable="true">Component looks correct in dark mode</criterion>
  </acceptance-criteria>

  <notes>
    This is a CUSTOM component, not from @fluentui/react-charting.
    May require separate PCF control if integration proves complex.
  </notes>

  <execution>
    <skill>.claude/skills/task-execute/SKILL.md</skill>
  </execution>
</task>
