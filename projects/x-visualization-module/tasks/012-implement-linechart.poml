<?xml version="1.0" encoding="UTF-8"?>
<task id="012" project="visualization-module">
  <metadata>
    <title>Implement LineChart component</title>
    <phase>2: Core Chart Components</phase>
    <status>not-started</status>
    <estimated-hours>3</estimated-hours>
    <dependencies>004</dependencies>
    <blocks>017, 020</blocks>
    <tags>pcf, react, fluent-ui, frontend</tags>
  </metadata>

  <prompt>
    Build the LineChart component using @fluentui/react-charting. Support line and area
    variants with data point click for drill-through.
  </prompt>

  <role>
    React developer with @fluentui/react-charting expertise.
  </role>

  <goal>
    A reusable LineChart component that renders time-series or trend data
    using Fluent v9 charting library, with click handlers for drill-through.
  </goal>

  <context>
    <background>
      Line charts show trends over time like "Matters Opened per Month" or
      "Invoice Amounts by Quarter". Area variant fills below the line.
    </background>
    <relevant-files>
      <file>src/client/pcf/VisualHost/control/components/</file>
    </relevant-files>
  </context>

  <constraints>
    <constraint source="spec">MUST use @fluentui/react-charting (LineChart, AreaChart)</constraint>
    <constraint source="ADR-021">MUST NOT use Chart.js, Recharts, VisX, or D3</constraint>
    <constraint source="ADR-021">MUST use Fluent design tokens for colors</constraint>
    <constraint source="project">MUST emit DrillInteraction on point click</constraint>
  </constraints>

  <knowledge>
    <files>
      <file>.claude/adr/ADR-021-fluent-design-system.md</file>
      <file>.claude/patterns/pcf/theme-management.md</file>
      <file>src/client/pcf/CLAUDE.md</file>
    </files>
  </knowledge>

  <steps>
    <step order="1">Create LineChart.tsx component file</step>
    <step order="2">Define props interface (data, showArea, onPointClick)</step>
    <step order="3">Implement using LineChart or AreaChart from @fluentui/react-charting</step>
    <step order="4">Configure color palette from Fluent tokens</step>
    <step order="5">Implement onPointClick that emits DrillInteraction</step>
    <step order="6">Add responsive sizing</step>
    <step order="7">Create Storybook stories (line, area, multi-series)</step>
    <step order="8">Verify dark mode appearance</step>
    <step order="9">Update TASK-INDEX.md: change this task's status to completed</step>
  </steps>

  <tools>
    <tool name="npm">Build and test</tool>
  </tools>

  <outputs>
    <output type="code">src/client/pcf/VisualHost/control/components/LineChart.tsx</output>
    <output type="code">src/client/pcf/VisualHost/stories/LineChart.stories.tsx</output>
  </outputs>

  <acceptance-criteria>
    <criterion testable="true">Component renders line chart correctly</criterion>
    <criterion testable="true">Component renders area chart variant correctly</criterion>
    <criterion testable="true">Point click emits DrillInteraction with date/category value</criterion>
    <criterion testable="true">Uses @fluentui/react-charting</criterion>
    <criterion testable="true">Component looks correct in dark mode</criterion>
  </acceptance-criteria>

  <execution>
    <skill>.claude/skills/task-execute/SKILL.md</skill>
  </execution>
</task>
