<?xml version="1.0" encoding="UTF-8"?>
<task id="020" project="visualization-module">
  <metadata>
    <title>Build Visual Host PCF core</title>
    <phase>3: Visual Host PCF Control</phase>
    <status>not-started</status>
    <estimated-hours>4</estimated-hours>
    <dependencies>017</dependencies>
    <blocks>021, 023</blocks>
    <tags>pcf, react, fluent-ui, frontend</tags>
  </metadata>

  <prompt>
    Build the core Visual Host PCF logic that selects and renders the appropriate
    chart component based on the visual type from configuration.
  </prompt>

  <role>
    PCF control developer with expertise in Fluent UI v9 and React.
  </role>

  <goal>
    A working Visual Host PCF that dynamically renders any of the 7 chart components
    based on the sprk_chartdefinition configuration passed to it.
  </goal>

  <context>
    <background>
      The Visual Host is the unified entry point. It receives a chartDefinitionId,
      loads configuration, and renders the matching chart component.
    </background>
    <relevant-files>
      <file>src/client/pcf/VisualHost/control/</file>
      <file>src/client/pcf/VisualHost/control/components/</file>
    </relevant-files>
  </context>

  <constraints>
    <constraint source="ADR-006">MUST follow PCF lifecycle pattern</constraint>
    <constraint source="ADR-021">MUST wrap in FluentProvider with resolved theme</constraint>
    <constraint source="project">MUST render all 7 visual types correctly</constraint>
    <constraint source="project">MUST include version footer in UI</constraint>
  </constraints>

  <knowledge>
    <files>
      <file>.claude/adr/ADR-006-pcf-over-webresources.md</file>
      <file>.claude/patterns/pcf/control-initialization.md</file>
      <file>.claude/patterns/pcf/theme-management.md</file>
      <file>src/client/pcf/CLAUDE.md</file>
    </files>
  </knowledge>

  <steps>
    <step order="1">Create ChartRenderer component that switches on visual type</step>
    <step order="2">Map VisualType enum to chart components</step>
    <step order="3">Pass configuration props to selected component</step>
    <step order="4">Wire up DrillInteraction callback to parent</step>
    <step order="5">Add loading and error states</step>
    <step order="6">Add version footer following PCF pattern</step>
    <step order="7">Add expand/view details toolbar button</step>
    <step order="8">Test with mock configuration in Storybook</step>
    <step order="9">Update TASK-INDEX.md: change this task's status to completed</step>
  </steps>

  <outputs>
    <output type="code">src/client/pcf/VisualHost/control/components/ChartRenderer.tsx</output>
    <output type="code">src/client/pcf/VisualHost/control/App.tsx (updated)</output>
  </outputs>

  <acceptance-criteria>
    <criterion testable="true">ChartRenderer switches correctly on all 7 visual types</criterion>
    <criterion testable="true">Version footer displays in control</criterion>
    <criterion testable="true">Expand button appears in toolbar</criterion>
    <criterion testable="true">Loading state shows spinner</criterion>
    <criterion testable="true">Error state shows user-friendly message</criterion>
  </acceptance-criteria>

  <execution>
    <skill>.claude/skills/task-execute/SKILL.md</skill>
  </execution>
</task>
