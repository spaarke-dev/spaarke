<?xml version="1.0" encoding="UTF-8"?>
<task id="031" project="visualization-module">
  <metadata>
    <title>Build two-panel layout component</title>
    <phase>4: Drill-Through Workspace</phase>
    <status>not-started</status>
    <estimated-hours>3</estimated-hours>
    <dependencies>030</dependencies>
    <blocks>032</blocks>
    <tags>pcf, react, fluent-ui, frontend</tags>
  </metadata>

  <prompt>
    Create the two-panel layout for the drill-through workspace:
    left panel (1/3) for chart, right panel (2/3) for dataset grid.
  </prompt>

  <role>
    React developer with responsive layout expertise.
  </role>

  <goal>
    A responsive two-panel layout component that arranges chart and dataset
    side-by-side with proper proportions and responsive behavior.
  </goal>

  <context>
    <background>
      The drill-through workspace has a fixed layout: chart on left (1/3 width),
      dataset grid on right (2/3 width). May stack vertically on small screens.
    </background>
  </context>

  <constraints>
    <constraint source="ADR-021">MUST use Fluent v9 layout tokens</constraint>
    <constraint source="project">Layout: 1/3 chart, 2/3 dataset</constraint>
    <constraint source="project">MUST be responsive (stack on mobile)</constraint>
  </constraints>

  <knowledge>
    <files>
      <file>.claude/adr/ADR-021-fluent-design-system.md</file>
      <file>src/client/pcf/AnalysisWorkspace/</file>
    </files>
  </knowledge>

  <steps>
    <step order="1">Create TwoPanelLayout.tsx component</step>
    <step order="2">Define left panel (chart) and right panel (grid) slots</step>
    <step order="3">Style with Fluent tokens for spacing and responsive breakpoints</step>
    <step order="4">Add resize handle (optional) for panel adjustment</step>
    <step order="5">Create Storybook story showing both panels</step>
    <step order="6">Test responsive behavior</step>
    <step order="7">Update TASK-INDEX.md: change this task's status to completed</step>
  </steps>

  <outputs>
    <output type="code">src/client/pcf/DrillThroughWorkspace/control/components/TwoPanelLayout.tsx</output>
  </outputs>

  <acceptance-criteria>
    <criterion testable="true">Layout renders with correct proportions (1/3 + 2/3)</criterion>
    <criterion testable="true">Panels stack on small screens</criterion>
    <criterion testable="true">Uses Fluent spacing tokens</criterion>
  </acceptance-criteria>

  <execution>
    <skill>.claude/skills/task-execute/SKILL.md</skill>
  </execution>
</task>
