# Deferred issues — spaarkeai-compose-fidelity-r4.5

Tracked follow-ups surfaced during execution. (Two-write rule: file as GitHub Issues at PR/wrap-up time.)

| ID | Title | Surfaced | Disposition | GitHub Issue |
|----|-------|----------|-------------|--------------|
| DEF-01 | `ComposeEditor.advisoryComments.test.tsx` fails ("placed 1 vs 2") — advisory-comment **target-resolution**. Confirmed **pre-existing on master** (failing at task 012 before task 013 touched the file; no advisory-placement source changed on this branch). | WS-1 gate (013) | **Assign to WS-3 task 031** — advisory anchoring depends on the paragraph/numbering model 031 builds; the test's own sessionId is `session-nda-review-031`. Re-run after 031; if still red, fix there. | {URL} |
| DEF-02 | `/api/compose/project` + `/upload` run **synchronous** projection (CPU) on `byte[] Content` with only Kestrel's implicit ~28.6 MB body cap — no Compose-specific size guard. | WS-1 gate (code-review) | **Hardening follow-up** — add an explicit request-size limit aligned with the 25 MB chat-attachment policy (`docs/standards/CHAT-ATTACHMENT-POLICY.md`). Same profile as existing Load path (not a regression); low severity. Consider at WS-1 deploy PR or a small hardening task. | {URL} |
| DEF-03 | **NFR-02 numbering divergence (write ↔ read).** `NumberingComputationEngine.Compute` (031, `ComposeDocxProjectionBuilder.cs`) keys its running counter by `(abstractNumId, level)` ONLY — never by `numId`. `ComposeDocumentRenderer.RenderBlocks` (026/027) intentionally allocates a FRESH `w:num` instance + `w:startOverride=1` every time an ordered run is broken by a non-ordered-list block (a routine "two numbered lists separated by prose" document shape), correctly authoring per-OOXML-spec restart-at-1 semantics. Because the read-side counter ignores `numId`, once `(abstractNumId, level)` has been used by an EARLIER `numId` sharing the same `abstractNumId`, the engine just keeps incrementing instead of re-seeding from the new numId's `startOverride` — so a SECOND ordered list the renderer intends to display "1., 2." instead reads back as "3., 4." Confirmed empirically by `tests/integration/seam/Compose/ComposeNumberingRoundTripSeamTests.cs::RoundTrip_TwoOrderedListsSeparatedByAParagraph_ReadSideLabelsMatchWriteSideRestartIntent` (round-trips a doc authored by the REAL `ComposeDocumentRenderer` through the REAL WS-3 pipeline). The 3-corpus-doc golden Theory (24 cases, 031) never exercised this because every corpus exemplar uses a single `numId` per `abstractNumId` (031's own notes state this constraint explicitly). **Which side is right**: the WRITE side (per ECMA-376 — numbering counters are instance-`numId`-scoped; two independent `w:num` referencing the same `w:abstractNum` have independent counters by construction, which is exactly why a `w:startOverride` on a fresh instance is the standard Word "Restart at 1" idiom). The READ side (031's engine) needs to key its counter (or at minimum its reset trigger) by `numId`-awareness, not `abstractNumId` alone. | WS-3 (task 033 round-trip test) | **Escalation surfaced, NOT patched** (033 is a test-authoring task; fixing 031's counter model is a design change requiring its own task/PR + regression coverage against the existing 24-case golden Theory). Recommend: a focused follow-up task extends `NumberingComputationEngine`'s counter key/reset logic to treat a `numId` carrying an UNCONSUMED `w:startOverride` as a hard reset trigger for that `(abstractNumId, level)` counter, re-verify against golden Theories + this round-trip test, then flip `RoundTrip_TwoOrderedListsSeparatedByAParagraph_...` from red to green (do NOT weaken the assertion — the fix is in `ComposeDocxProjectionBuilder.cs`, not the test). Left as a genuinely FAILING test in this PR (intentional — "fails loudly", not skipped) so CI surfaces it until reconciled. Blast radius: any Compose-authored (born-in-editor) document containing 2+ separate ordered lists mis-numbers the 2nd+ list on subsequent reads/opens. | {URL} |
