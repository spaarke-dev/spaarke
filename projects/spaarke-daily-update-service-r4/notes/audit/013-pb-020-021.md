# Audit: PB-020 + PB-021 — Pre-W1-Migration Baseline

> **Task**: 013 — Audit PB-020 (notification-tasks-due-soon) + PB-021 (notification-tasks-overdue)
> **Date**: 2026-06-25
> **Purpose**: Capture deployed Dataverse state of these 2 playbooks BEFORE W1 (PR 3) tasks 022/023 migrate them to membership-scope.
> **Scope**: Read-only audit. No deployed rows modified.
> **Source-of-truth (post-W1)**: `projects/spaarke-daily-update-service/notes/playbooks/notification-tasks-{due-soon,overdue}.json` — these files are pre-W1 today and will be **edited by W1 tasks 022/023** to add a `LookupUserMembership` (ActionType 52) parallel branch + FetchXml union.
> **Reconciliation note (binding for task 015)**: Task 015 MUST SKIP PB-020 and PB-021. W1 tasks 022/023 own the migration; W1.4 (or equivalent W1 wrap step) redeploys these two playbooks. A separate R4 verification step confirms post-W1 convergence.

---

## Overall Verdict

| Playbook | Code | Deployed | LookupUserMembership (ActionType 52) | Verdict |
|---|---|---|---|---|
| Tasks Due Soon | PB-020 | YES (Active) | **NO** (absent) | PRE-MIGRATION (expected) |
| Tasks Overdue | PB-021 | YES (Active) | **NO** (absent) | PRE-MIGRATION (expected) |

**Both playbooks** are in the expected PRE-MIGRATION state. The repo source-of-truth JSON files at `projects/spaarke-daily-update-service/notes/playbooks/` ALSO lack ActionType 52 nodes today; they will gain them under W1 PR 3 tasks 022/023.

**No escalation required.** No "ALREADY-MIGRATED" surprise (which would have made W1 redundant). No missing-from-deployed-state defect.

**However**, a substantive **deployed-vs-repo divergence** exists in both playbooks: the deployed nodes target the **`sprk_event` custom Task entity** (with `__actionType: 51` query mode), while the repo JSON files target the **OOB `task` activity entity** (with `__actionType: 22` updateRecord). This is documented per-playbook below for awareness, but task 015 still SKIPS these playbooks per the binding rule above — W1 will rewrite the repo JSON files (the canonical source-of-truth) and redeploy them in one operation. Task 015 owners should not attempt to reconcile this divergence ad-hoc.

---

## PB-020 — Tasks Due Soon

### Deployed state (spaarkedev1, as of 2026-06-25)

| Field | Value |
|---|---|
| `sprk_analysisplaybookid` | `77f77aa5-5f2d-f111-88b5-7ced8d1dc988` |
| `sprk_name` | Tasks Due Soon |
| `sprk_playbookcode` | `PB-020` |
| `sprk_playbooktype` | 2 (Notification) |
| `statecode` | 0 (Active) |
| `sprk_ispublic` | Yes |
| `modifiedon` | 2026-06-25T21:00:15Z |

### Deployed `sprk_configjson` (playbook-level)

```json
{
  "schedule": { "frequency": "hourly" },
  "category": "tasks-due",
  "channelLabel": "Upcoming Tasks",
  "channelIcon": "clock",
  "parameters": {
    "dueWithinDays": {
      "type": "integer",
      "default": 3,
      "options": [1, 2, 3, 5, 7],
      "label": "Due within",
      "unit": "days"
    }
  }
}
```

### Deployed nodes (4 total)

| Node | nodetype | __actionType | Notes |
|---|---|---|---|
| Start | 100000002 (Control) | 33 | scope: `user-matters` |
| Query Tasks Due Soon | (null) | 51 | queryMode over `sprk_event` (NOT `task`) |
| Check Results | 100000002 (Control) | 30 | condition `dueSoonQuery.output.count > 0` → Create Notification |
| Create Notification | (null) | 50 | regardingType: `sprk_event`; iterateItems; title body templates |

### LookupUserMembership (ActionType 52) presence check

**ABSENT.** No node in the deployed PB-020 has `__actionType: 52`. This is the EXPECTED pre-W1 state per FR-7. W1 task 023 will add a parallel `LookupUserMembership` branch in the repo JSON and redeploy.

### Divergence vs repo JSON (`notification-tasks-due-soon.json`)

| Aspect | Repo (canonical pre-W1) | Deployed | Divergent? |
|---|---|---|---|
| Playbook-level `id` | `NP-002` | `PB-020` (only `sprk_playbookcode`) | Naming convention difference (informational) |
| Playbook `schedule.frequency` | `daily` (06:00) | `hourly` | YES — substantive |
| Playbook `parameters.dueWithinDays` | Present (default 3, options 1/2/3/5/7) | Present (identical) | No |
| Query node `entityLogicalName` | `task` (OOB activity) | `sprk_event` (Spaarke custom Task) | **YES — substantive entity divergence** |
| Query node `__actionType` | 22 (updateRecord/queryMode) | 51 (query) | YES — substantive |
| Query node FetchXml attributes | `subject`, `scheduledend`, `regardingobjectid`, `activityid` | `sprk_eventname`, `sprk_duedate`, `sprk_finalduedate`, `sprk_eventid`, `sprk_eventtype_ref`, `statuscode`, `sprk_regardingrecordname`, `sprk_regardingrecordurl` | YES — substantive |
| Query node FetchXml filter | scheduledend ≥ today AND scheduledend ≤ today+dueWithinDays | sprk_duedate OR sprk_finalduedate within today..dueSoonWindowUtc; statuscode=659490001; sprk_eventtype_ref = a specific Task type GUID | YES — substantive |
| Check Results `conditionJson` | string `"{{...}} > 0"` | structured `{ operator: "gt", left, right: 0 }` | YES — schema shape |
| Create Notification `regardingType` | `task` | `sprk_event` | YES — substantive (follows query change) |
| Create Notification `actionUrl` (item) | `etn=task&id={{item.activityid}}` | `etn=sprk_event&id={{item.sprk_eventid}}` | YES — substantive (follows entity change) |
| LookupUserMembership branch (ActionType 52) | ABSENT | ABSENT | No (both pre-W1) |

**Verdict for PB-020**: PRE-MIGRATION (expected). Deployed state has substantively diverged from the repo JSON — deployed targets `sprk_event` (Spaarke's custom Task entity), repo targets `task` (OOB activity). **This divergence is OUT OF SCOPE for task 015.** W1 task 023 will rewrite the repo JSON to (a) reconcile entity choice (presumably to `sprk_event` since that's the live production behavior), (b) add the membership-scope union FetchXml, and (c) add the LookupUserMembership (ActionType 52) parallel branch. W1's redeploy step replaces the deployed config in one operation. Task 015 SKIPS PB-020.

---

## PB-021 — Tasks Overdue

### Deployed state (spaarkedev1, as of 2026-06-25)

| Field | Value |
|---|---|
| `sprk_analysisplaybookid` | `4369cab2-5f2d-f111-88b5-7ced8d1dc988` |
| `sprk_name` | Tasks Overdue |
| `sprk_playbookcode` | `PB-021` |
| `sprk_playbooktype` | 2 (Notification) |
| `statecode` | 0 (Active) |
| `sprk_ispublic` | Yes |
| `modifiedon` | 2026-06-25T21:00:22Z |

### Deployed `sprk_configjson` (playbook-level)

```json
{
  "schedule": { "frequency": "hourly" },
  "category": "tasks-overdue",
  "channelLabel": "Overdue Tasks",
  "channelIcon": "warning",
  "parameters": {}
}
```

### Deployed nodes (4 total)

| Node | nodetype | __actionType | Notes |
|---|---|---|---|
| Start | 100000002 (Control) | 33 | scope: `user-matters` |
| Query Overdue Tasks | (null) | 51 | queryMode over `sprk_event` (NOT `task`) |
| Check Results | 100000002 (Control) | 30 | condition `overdueQuery.output.count > 0` → Create Notification |
| Create Notification | (null) | 50 | regardingType: `sprk_event`; iterateItems |

### LookupUserMembership (ActionType 52) presence check

**ABSENT.** No node in the deployed PB-021 has `__actionType: 52`. This is the EXPECTED pre-W1 state per FR-7. W1 task 022 will add a parallel `LookupUserMembership` branch in the repo JSON and redeploy.

### Divergence vs repo JSON (`notification-tasks-overdue.json`)

| Aspect | Repo (canonical pre-W1) | Deployed | Divergent? |
|---|---|---|---|
| Playbook-level `id` | `NP-001` | `PB-021` (only `sprk_playbookcode`) | Naming convention difference (informational) |
| Playbook `schedule.frequency` | `daily` (06:00) | `hourly` | YES — substantive |
| Playbook `parameters` | empty `{}` | empty `{}` | No |
| Query node `entityLogicalName` | `task` (OOB activity) | `sprk_event` (Spaarke custom Task) | **YES — substantive entity divergence** |
| Query node `__actionType` | 22 (updateRecord/queryMode) | 51 (query) | YES — substantive |
| Query node FetchXml attributes | `subject`, `scheduledend`, `regardingobjectid`, `activityid` | `sprk_eventname`, `sprk_duedate`, `sprk_finalduedate`, `sprk_eventid`, `sprk_eventtype_ref`, `statuscode`, `sprk_regardingrecordname`, `sprk_regardingrecordurl` | YES — substantive |
| Query node FetchXml filter | scheduledend < today | sprk_duedate OR sprk_finalduedate < today; statuscode=659490001; sprk_eventtype_ref = a specific Task type GUID | YES — substantive |
| Check Results `conditionJson` | string `"{{...}} > 0"` | structured `{ operator: "gt", left, right: 0 }` | YES — schema shape |
| Create Notification `regardingType` | `task` | `sprk_event` | YES — substantive (follows query change) |
| Create Notification `actionUrl` (item) | `etn=task&id={{item.activityid}}` | `etn=sprk_event&id={{item.sprk_eventid}}` | YES — substantive (follows entity change) |
| LookupUserMembership branch (ActionType 52) | ABSENT | ABSENT | No (both pre-W1) |

**Verdict for PB-021**: PRE-MIGRATION (expected). Same substantive entity divergence pattern as PB-020 (deployed `sprk_event` vs repo `task`). **OUT OF SCOPE for task 015.** W1 task 022 rewrites the repo JSON + redeploys.

---

## Carry-Forward Notes for Task 015 (Reconcile + Redeploy)

1. **SKIP PB-020 and PB-021.** W1 PR 3 tasks 022 + 023 own the migration of these two playbooks. Task 015's reconcile pass MUST exclude these `sprk_playbookcode` values explicitly.
2. **Substantive divergence is expected.** Both playbooks show repo `task`/ActionType 22 vs deployed `sprk_event`/ActionType 51. Do NOT attempt to "fix" this divergence by reverting deployed-to-repo — W1 will rewrite the repo JSON as part of its own work, presumably aligning to the live `sprk_event` shape.
3. **Membership branch absent in both** — this is the entire reason W1 tasks 022/023 exist (per spec FR-7, line 142-143).
4. **R4 verification step (post-W1 redeploy)** — after W1 PR 3 merges and the W1 redeploy step runs, a separate verification (not task 015) should re-query PB-020 + PB-021 and confirm:
   - LookupUserMembership (ActionType 52) node present
   - FetchXml union covers both ownerid `eq-userid` AND membership-derived matter scope
   - Repo JSON ↔ deployed convergence achieved
5. **Schedule frequency** — both playbooks deployed with `hourly` frequency vs repo's `daily 06:00`. This is observation only; W1 task owners decide whether to align repo to `hourly` or align deployed to `daily` during the W1 rewrite.

---

## Baseline snapshot (for forensic comparison)

| Playbook | Deployed GUID | Deployed code | Deployed node count | ActionType 52 present | Repo JSON path |
|---|---|---|---|---|---|
| PB-020 (Tasks Due Soon) | `77f77aa5-5f2d-f111-88b5-7ced8d1dc988` | PB-020 | 4 | NO | `projects/spaarke-daily-update-service/notes/playbooks/notification-tasks-due-soon.json` |
| PB-021 (Tasks Overdue) | `4369cab2-5f2d-f111-88b5-7ced8d1dc988` | PB-021 | 4 | NO | `projects/spaarke-daily-update-service/notes/playbooks/notification-tasks-overdue.json` |

This snapshot is preserved for post-W1 reconciliation auditing.
