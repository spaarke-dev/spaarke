# Audit 014 — PB-017 (notification-matter-activity) + PB-022 (notification-work-assignments) — stub baseline

> **Task**: 014
> **Date**: 2026-06-25
> **Environment**: spaarkedev1
> **Auditor**: task-execute / jps-playbook-audit (read-only)
> **Phase**: PR 2 W0 — pre-W1 baseline capture
> **Scope**: PB-017 + PB-022 ONLY (parallel-safe with tasks 012, 013)

---

## Purpose

Capture the **pre-W1 stub baseline** of the 2 notification playbooks that the spec flags as "currently stub" (FR-8 line 145-146 + FR-9 line 148-149). PR 3 W1 tasks 024 (matter-activity) and 025 (work-assignments) will rewrite the repo JSON to full implementation AND redeploy. This audit documents the **starting state** so task 015 reconciliation knows to **SKIP these two playbooks**.

---

## Sources of truth consulted

| Layer | Path | State |
|---|---|---|
| Repo JSON — PB-017 | `projects/spaarke-daily-update-service/notes/playbooks/notification-matter-activity.json` | Stub (1 createNotification node, no query node, no edges) |
| Repo JSON — PB-022 | `projects/spaarke-daily-update-service/notes/playbooks/notification-work-assignments.json` | Stub (1 createNotification node, no query node, no edges) |
| Deployed — PB-017 | `sprk_analysisplaybook` GUID `24051c80-5f2d-f111-88b5-7ced8d1dc988` | Active (statecode 0), 1 node, no edges |
| Deployed — PB-022 | `sprk_analysisplaybook` GUID `be7874be-5f2d-f111-88b5-7ced8d1dc988` | Active (statecode 0), 1 node, no edges |

---

## PB-017 — Matter/Project Activity Summary

### Repo JSON (current)

- **Playbook-level** — name "Matter/Project Activity Summary", type 2 (Notification), public, `capabilities=["notify"]`, scopes all empty, `sprk_configjson` has cron `0 8 * * 1-5` (weekdays 8am Central), `timeWindow` parameter, channel `matter-activity`.
- **Nodes** — 1 node only: "Query Matter Activity", `nodeType=Workflow`, `actionType=50` (CreateNotification — despite the name "Query"), `canvasType=createNotification`, `dependsOn=[]`. Body uses mustache `{{matter.name}}` / `{{activitySummary}}` — but no query node materializes those variables.
- **Edges** — empty.

### Deployed `sprk_configjson` (playbook header)

```json
{
  "schedule": {
    "cronExpression": "0 8 * * 1-5",
    "timezone": "America/Chicago",
    "description": "Weekdays at 8:00 AM Central"
  },
  "parameters": {
    "timeWindow": {
      "type": "string",
      "default": "24h",
      "options": ["12h", "24h", "48h", "7d"],
      "description": "How far back to look for matter activity"
    }
  },
  "channel": "matter-activity",
  "channelLabel": "Matter/Project Activity"
}
```

Header matches repo exactly.

### Deployed nodes (1)

| Node | NodeType | OutputVariable | configjson __actionType | Notes |
|---|---|---|---|---|
| "Query Matter Activity" | 100000000 (AI Analysis) | `output_e14c0d7f-5f2d-f111-88b5-7c1e520aa4df` | 0 (placeholder) | configjson is `{"__canvasNodeId":"e14c0d7f-…","__actionType":0,"isConfigured":true,"validationErrors":[]}` — **bare placeholder; no real action wired** |

### Divergence vs repo

- Repo defines 1 `createNotification` node (actionType 50) with explicit title/body mustache; deployed is 1 **placeholder** node (actionType 0, nodeType "AI Analysis") with no title/body. The deployed row has **drifted further** from the repo stub — it's even more empty than the repo's stub.
- Edges: both 0.

### Stub-ness verdict — PB-017: **STUB-AS-EXPECTED**

The deployed row is in stub state. Note the deployed has a node-type/action mismatch (nodeType=AI Analysis but actionType=0) — this is functionally inert (no executor would dispatch it). The repo defines a CreateNotification node, but produces zero output without a preceding query. Both states confirm: **no real query, no LookupUserMembership, no membership scope, no emit-ready notification stream**. Spec FR-8 will rewrite this entirely.

---

## PB-022 — New Work Assignments

### Repo JSON (current)

- **Playbook-level** — name "New Work Assignments", type 2 (Notification), public, `capabilities=["notify"]`, scopes all empty, `sprk_configjson` has cron `0 */2 * * 1-5` (every 2 hours weekdays), `timeWindow` parameter, channel `work-assignments`.
- **Nodes** — 1 node only: "Notify New Assignment", `nodeType=Workflow`, `actionType=50` (CreateNotification), `canvasType=createNotification`, `dependsOn=[]`. Body uses mustache `{{task.subject}}` / `{{task.matterName}}` / `{{task.dueDate}}` — no query node materializes those variables.
- **Edges** — empty.

### Deployed `sprk_configjson` (playbook header)

```json
{
  "schedule": {
    "cronExpression": "0 */2 * * 1-5",
    "timezone": "America/Chicago",
    "description": "Every 2 hours on weekdays"
  },
  "parameters": {
    "timeWindow": {
      "type": "string",
      "default": "24h",
      "options": ["12h", "24h", "48h", "7d"],
      "description": "How far back to look for new assignments"
    }
  },
  "channel": "work-assignments",
  "channelLabel": "New Work Assignments"
}
```

Header matches repo exactly.

### Deployed nodes (1)

| Node | NodeType | OutputVariable | configjson __actionType | Notes |
|---|---|---|---|---|
| "Notify New Assignment" | null | `workAssignmentNotification` | 50 (CreateNotification) | configjson matches repo JSON: title/body/category/priority/actionUrl/regardingId/regardingType/recipientId with `{{task.*}}` mustache. **Inert without prior query node.** |

### Divergence vs repo

- Repo node and deployed node match field-for-field on the createNotification config. Deployed nodeType is `null` (no choice value) vs repo `"Workflow"` — minor metadata difference but functionally same.
- Edges: both 0.

### Stub-ness verdict — PB-022: **STUB-AS-EXPECTED**

Deployed row is in stub state and matches repo JSON. The single createNotification node references `{{task.*}}` variables that no upstream node provides — so even if dispatched, **zero notifications would be produced**. Spec FR-9 will replace with a full membership-scoped query + emit graph.

---

## Cross-cutting observations

1. **Both playbooks are deployed** with `statecode=0` (Active) — the spec's "7 deployed notification playbooks" claim holds (no missing rows).
2. **Both produce zero notifications today** — neither has a query node feeding the createNotification template. They are inert in production scheduling.
3. **Neither has a `LookupUserMembership` (ActionType 52) node** — both will need W1 task 024/025 to add membership-scope per ADR-034.
4. **PB-017 deployed has drifted further than repo** (the deployed node is `nodeType=AI Analysis` + `actionType=0` placeholder, while repo defines a createNotification stub). PB-022 deployed matches repo. This drift in PB-017 is consistent with someone opening it in PlaybookBuilder and dropping an empty AI-Analysis node — but it remains stub and W1 will fully replace it.

---

## Carry-forward note for task 015 (Reconcile + redeploy)

🚨 **Task 015 MUST SKIP PB-017 and PB-022.**

Reason: W1 PR 3 tasks 024 (notification-matter-activity) and 025 (notification-work-assignments) will:
1. Author full repo JSON from scratch (replacing the current stub JSON)
2. Redeploy to spaarkedev1

Redeploying current stub repo JSON via task 015 would be wasted work and would temporarily revert PB-017's deployed placeholder-AI-node back to the repo's createNotification-stub-node — meaningless churn. Skip these in 015's reconciliation; let 024/025 own end-to-end authorship + deployment.

Final state (post-W1) reconciliation will be verified by AC-5a (spec line 128) and the PR 3 wrap (task 029).

---

## Carry-forward note for tasks 024 / 025 (W1 implementation)

W1 task 024 + 025 should be aware that:

1. **PB-017 deployed node is currently a bare AI-Analysis placeholder** (not the createNotification stub in repo JSON) — when rewriting, the existing node(s) will need to be deleted/replaced wholesale, not patched.
2. **PB-022 deployed node matches the repo stub** — can be replaced cleanly via standard playbook redeploy.
3. **Both playbooks' header `sprk_configjson` (schedule, parameters, channel)** appears reasonable and may be preserved; W1 only needs to rewrite the node graph + edges, not the playbook-level schedule/parameters.
4. **Use PB-016 (`notification-new-documents.json`) as the reference** for membership-aware query → emit graph (per project CLAUDE.md — "PB-016 reference example — already membership-aware").

---

## Sources

| Item | Path / GUID |
|---|---|
| MCP query | `mcp__dataverse__read_query` against `sprk_analysisplaybook` (header) + `sprk_playbooknode` (nodes) |
| PB-017 playbook GUID | `24051c80-5f2d-f111-88b5-7ced8d1dc988` |
| PB-022 playbook GUID | `be7874be-5f2d-f111-88b5-7ced8d1dc988` |
| PB-017 node GUID | `7cfc8c62-902f-f111-88b5-7ced8d1dc988` ("Query Matter Activity") |
| PB-022 node GUID | `c10a54bf-5f2d-f111-88b5-7c1e520aa4df` ("Notify New Assignment") |
| Spec references | FR-8 (line 145-146), FR-9 (line 148-149), FR-5 (line 127), AC-5a (line 128) |

---

## Conclusion

| Playbook | Verdict | Action for task 015 | Action for W1 (PR 3) |
|---|---|---|---|
| PB-017 (notification-matter-activity) | **STUB-AS-EXPECTED** | SKIP | Task 024 rewrites repo JSON + redeploys (delete current AI-Analysis placeholder node + create membership-aware query → emit graph) |
| PB-022 (notification-work-assignments) | **STUB-AS-EXPECTED** | SKIP | Task 025 rewrites repo JSON + redeploys (replace current createNotification stub with membership-aware query → emit graph) |

Baseline captured. Audit closed.
