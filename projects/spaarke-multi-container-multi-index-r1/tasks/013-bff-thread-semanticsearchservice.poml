<?xml version="1.0" encoding="UTF-8"?>
<task id="013" project="spaarke-multi-container-multi-index-r1">
  <metadata>
    <title>Thread `SearchIndexName` through `SemanticSearchService.cs`</title>
    <phase>B: BFF resolver extension</phase>
    <status>not-started</status>
    <estimated-hours>1-2</estimated-hours>
    <dependencies>010, 011</dependencies>
    <blocks>016</blocks>
    <tags>bff-api, api, services, ai</tags>
    <rigor-hint>FULL</rigor-hint>
    <rigor-reason>Tags include `bff-api`; modifies .cs service code that directly invokes the resolver. Backward-compat invariant.</rigor-reason>
    <parallel-group>B2</parallel-group>
    <parallel-safe>true</parallel-safe>
    <parallel-reason>File-disjoint with tasks 014 (RagService) and 015 (RecordSearchService) — different service files, independent edits.</parallel-reason>
  </metadata>

  <prompt>
    Thread `searchIndexName` (from the request DTO `SemanticSearchRequest.SearchIndexName`) through `SemanticSearchService.cs` to its call into `IKnowledgeDeploymentService.GetSearchClientAsync(searchIndexName)`. Existing callers passing no value (or null/empty) continue to fall through to the existing 2-tier chain (NFR-02 — backward-compat).
  </prompt>

  <role>
    BFF developer familiar with `Sprk.Bff.Api/Services/Ai/SemanticSearch/`. Read `.claude/constraints/bff-extensions.md` for the binding pre-merge checklist.
  </role>

  <goal>
    `SemanticSearchService` receives `searchIndexName` (typically from the request DTO via the endpoint layer) and passes it to `_knowledgeDeployment.GetSearchClientAsync(searchIndexName)`. Existing tests pass unmodified.
  </goal>

  <context>
    <background>
      Task 010 extended the resolver. Task 011 added the DTO field. This task is the first of three "thread-through" tasks — `SemanticSearchService` is the primary consumer; `RagService` (014) and `RecordSearchService` (015) follow the same pattern in parallel.
    </background>
    <relevant-files>
      <file>src/server/api/Sprk.Bff.Api/Services/Ai/SemanticSearch/SemanticSearchService.cs</file>
      <file>src/server/api/Sprk.Bff.Api/Services/Ai/IKnowledgeDeploymentService.cs</file>
      <file>src/server/api/Sprk.Bff.Api/Models/Ai/SemanticSearch/SemanticSearchRequest.cs</file>
      <file>src/server/api/Sprk.Bff.Api/CLAUDE.md</file>
      <file>projects/spaarke-multi-container-multi-index-r1/spec.md</file>
    </relevant-files>
  </context>

  <constraints>
    <constraint source="spec.md FR-BFF-07">Search endpoint MUST thread `SearchIndexName` from request DTO into resolver call. This task delivers the service-side thread-through (endpoint-side wiring in task 016).</constraint>
    <constraint source="spec.md NFR-02">Backward-compat — existing tests pass UNMODIFIED. Callers without `searchIndexName` continue to work.</constraint>
    <constraint source="ADR-001">Minimal-API service conventions; pass parameters explicitly through call chain (no service locator).</constraint>
    <constraint source="ADR-013">AI architecture — service stays within `Services/Ai/SemanticSearch/`.</constraint>
    <constraint source=".claude/constraints/bff-extensions.md">Binding pre-merge checklist for any BFF addition.</constraint>
  </constraints>

  <knowledge>
    <files>
      <file>.claude/adr/ADR-001-bff-minimal-api.md</file>
      <file>.claude/adr/ADR-013-ai-architecture.md</file>
      <file>.claude/constraints/bff-extensions.md</file>
      <file>src/server/api/Sprk.Bff.Api/CLAUDE.md</file>
      <file>projects/spaarke-multi-container-multi-index-r1/spec.md</file>
    </files>
    <patterns>
      <pattern name="Thread-through" location="ADR-001">
        Accept the new optional parameter at the public service method; pass it explicitly to the resolver call (`_knowledgeDeployment.GetSearchClientAsync(searchIndexName)`). Do not stash in field state.
      </pattern>
    </patterns>
  </knowledge>

  <steps>
    <step order="1">Read `SemanticSearchService.cs` and identify the public method(s) called by the endpoint layer (likely `SearchAsync(SemanticSearchRequest request, ...)`).</step>
    <step order="2">In that method, read `request.SearchIndexName` and pass it to `_knowledgeDeployment.GetSearchClientAsync(request.SearchIndexName)`. If the resolver is called in multiple places, thread it to all of them (or refactor to single call site if obvious).</step>
    <step order="3">Verify existing callers (without `searchIndexName` set) continue to work — `request.SearchIndexName` will be null on legacy callers, and the resolver falls through to the existing chain (FR-BFF-04, delivered by task 010).</step>
    <step order="4">Add a unit test (or extend existing) confirming the thread-through: mock `IKnowledgeDeploymentService`, call `SemanticSearchService.SearchAsync` with `SearchIndexName = "spaarke-file-index"`, verify the mock received that exact value.</step>
    <step order="5">Run `dotnet build` + `dotnet test`; verify existing tests pass unmodified.</step>
    <step order="6">Update `TASK-INDEX.md` status: 🔲 → ✅. Update `current-task.md`.</step>
  </steps>

  <tools>
    <tool name="Read">Read existing service code</tool>
    <tool name="Edit">Thread parameter through</tool>
    <tool name="Bash">Build + test verification</tool>
  </tools>

  <outputs>
    <output type="code">src/server/api/Sprk.Bff.Api/Services/Ai/SemanticSearch/SemanticSearchService.cs</output>
  </outputs>

  <acceptance-criteria>
    <criterion testable="true">FR-BFF-07 (part 1 of 3): `SemanticSearchService` passes `searchIndexName` through to the resolver call. Verified by unit test with mocked `IKnowledgeDeploymentService`.</criterion>
    <criterion testable="true">NFR-02: Existing test suite passes UNMODIFIED.</criterion>
  </acceptance-criteria>

  <notes>
    Parallel with tasks 014 + 015 (same pattern, different services). All three converge into task 016 (endpoint wiring).
  </notes>

  <execution>
    <skill>.claude/skills/task-execute/SKILL.md</skill>
    <protocol>
      Before starting, load all files in &lt;knowledge&gt;&lt;files&gt;. Follow task-execute Step 0 (rigor declaration: FULL) before any code change.
    </protocol>
  </execution>
</task>
