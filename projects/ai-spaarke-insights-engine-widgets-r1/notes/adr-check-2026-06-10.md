# adr-check Report — Insights Engine Widgets r1 spec.md

> **Task**: 004 — adr-check spec.md sweep (verify NFR-09 compliance)
> **Date**: 2026-06-10
> **Auditor**: task-execute / adr-check (Rigor: STANDARD)
> **Scope**: `projects/ai-spaarke-insights-engine-widgets-r1/spec.md` (and `CLAUDE.md` cross-refs)
> **Authority**: `.claude/adr/INDEX.md` + per-ADR concise files; `.claude/skills/adr-check/SKILL.md` workflow

---

## Overall Verdict

| Dimension | Result |
|---|---|
| **NFR-09 (no new ADR introduced by r1)** | ✅ **PASS** |
| **ADR application correctness (cited in spec)** | ⚠ **PASS WITH ONE DOCUMENTATION DEFECT** — one citation misattributes a rule to ADR-014 |
| **ADR application correctness (cited in CLAUDE.md only)** | ✅ PASS (+1 minor warning on ADR-012 wording) |
| **Phase 1 gating recommendation** | **PROCEED** with the one spec.md edit recommended below; the defect is a citation-label error, not a constraint violation. |

---

## NFR-09 Verification — No New ADR Introduced

**Method**: `git diff master...HEAD --name-only` filtered for ADR directories.

**Result**:
- `docs/adr/`: 0 files added, 0 files modified on branch `work/ai-spaarke-insights-engine-widgets-r1`.
- `.claude/adr/`: 0 files added, 0 files modified.
- All branch changes are confined to `projects/ai-spaarke-insights-engine-widgets-r1/`.

**Spec.md self-declaration cross-check** (lines 59, 226, 257): spec.md explicitly bars new ADR introduction in Out of Scope, NFR-09, and MUST NOT rules.

**Verdict**: ✅ **NFR-09 PASS** — no `docs/adr/` or `.claude/adr/` additions; spec language is internally consistent with the constraint.

---

## Per-ADR Findings (cited in spec.md §"Applicable ADRs", lines 234-241)

| ADR | Canonical title | Spec citation | Verdict | Notes |
|---|---|---|---|---|
| **ADR-001** | Minimal API + BackgroundService (single BFF runtime) | "Single BFF runtime, no microservices." | ✅ **PASS** | r1 ships zero new services; all AI invocation routes through existing `IInsightsAi`. |
| **ADR-009** | Redis-First Caching | "use canonical `DistributedCacheExtensions.GetOrCreateAsync<T>`; in-process MemoryCache requires explicit ADR-009 exception XML doc." | ✅ **PASS** | FR-21 extends per-topic TTL configuration on existing `IInsightsPlaybookExecutionCache` (which uses `IDistributedCache`); no new cache abstraction. |
| **ADR-010** | DI Minimalism | "no new interface seams in r1" | ✅ **PASS** | r1 introduces zero new interfaces. Telemetry meter is standalone. |
| **ADR-013** | AI Architecture (extend BFF; CRUD→AI via `Services/Ai/PublicContracts/` facade) | "AI features extend BFF; CRUD-side consumers route through `Services/Ai/PublicContracts/` facades. r1 consumes existing `IInsightsAi.AnswerQuestionAsync` directly — no new facade." | ✅ **PASS** | Per audit DR-003, widget invocation is the canonical facade consumer pattern. r1 is downstream consumer, not facade author. |
| **ADR-014** | **AI Caching and Reuse Policy** (per `.claude/adr/ADR-014-ai-caching.md` + `docs/adr/ADR-014-ai-caching-and-reuse-policy.md`) | "Playbook prompts in Dataverse `sprk_analysisaction.sprk_systemprompt`, NOT .txt files." | ❌ **FAIL (misattribution)** | The cited rule is NOT in ADR-014. ADR-014 is **AI Caching and Reuse Policy** (cacheable artifacts, key versioning, tenant scoping). The "prompts in Dataverse" rule is codified in `projects/bff-ai-architecture-audit-r1/notes/canonical-architecture-decisions.md` §2.7 + DR-007. **Recommend spec.md edit below.** |
| **ADR-018** | Feature Flags and Kill Switches (→ 503 ProblemDetails) | "feature gates must surface 503 ProblemDetails to clients (NOT 500)." | ✅ **PASS** | FR-25 acceptance verifies 503; ADR-018 MUST rule satisfied. |
| **ADR-019** | ProblemDetails & Error Handling | "error response shape." | ✅ **PASS** | FR-25 + JSON envelope FR-14 do not leak content; consistent with ADR-019 MUST NOT (no content leakage). |
| **ADR-021** | Fluent UI v9 Design System | (Cited transitively via CLAUDE.md + NFR-05). | ✅ **PASS** | NFR-05 "honors Spaarke Fluent v9 design system theming" + dark mode (Resolution Decisions §FR-03 → Popover + Dialog from `@fluentui/react-components`). Q-U2 (sprk_icon as Fluent component name) is consistent. |
| **ADR-032** | BFF Null-Object Kill-Switch Pattern | "Null-Object Fail-Fast: facade Null peers throw `FeatureDisabledException` with stable `errorCode`. r1 relies on existing PR #351 Null peers (no new facades to add Null peers for)." | ✅ **PASS** | r1 adds no new conditional services; consumes the kill-switch contract surfaced by existing `IInsightsAi` Null peer per ADR-032 P3 pattern. |

---

## Per-ADR Findings (cited only in project CLAUDE.md)

| ADR | Verdict | Notes |
|---|---|---|
| **ADR-006** (UI Surface Architecture) | ✅ PASS | Matter form OnLoad handler is thin JS (Q-U6: `Xrm.WebApi`); permissible "command/script" surface per ADR-006. |
| **ADR-012** (Shared Component Library — `@spaarke/ui-components` as SoT) | ⚠ **WARNING (informational, not a violation)** | Spec ships `InsightSummaryCard` in `@spaarke/ai-widgets`, not `@spaarke/ui-components`. The FR-03 Resolution Decision (line 402) selected `@spaarke/ai-widgets` after pre-flight investigation found no inline-expand-to-modal pattern in `@spaarke/ui-components`. ADR-012 cites `@spaarke/ui-components` as "single source of truth for all reusable UI" — strict reading would require justification. The justification IS in Resolution Decisions but does not name ADR-012 explicitly. **Optional spec edit suggested**: add a sentence to Resolution Decisions row "FR-03" clarifying that `@spaarke/ai-widgets` is the canonical home for AI-widget composition siblings (FeedbackButtons, CitationBadge, etc., per CLAUDE.md "Reference code"), consistent with the multi-package shared-library framing rather than violating ADR-012. |
| **ADR-028** (Spaarke Auth v2) | ✅ PASS | NFR-08 + Q-U5 inherit existing record authz; no new auth surface added. |
| **ADR-030** (PaneEventBus) | ✅ N/A | Explicitly marked "Not used in r1 (per-record widget)" in CLAUDE.md. |
| **ADR-031** (Stage Lifecycle) | ✅ N/A | Explicitly marked "Not used in r1" in CLAUDE.md. |

---

## Recommended spec.md Edit (mandatory — for the ❌ FAIL above)

**Location**: `spec.md` line 239, in §"Technical Constraints → Applicable ADRs".

**Current text**:
```markdown
- **ADR-014** — Playbook prompts in Dataverse `sprk_analysisaction.sprk_systemprompt`, NOT .txt files.
```

**Recommended replacement** (preserves the rule, corrects the citation):
```markdown
- **ADR-014** — AI Caching and Reuse Policy: derived AI artifacts (text, embeddings, completions) are cacheable with versioned, tenant-scoped keys. r1 inherits this via existing `IInsightsPlaybookExecutionCache`; per-topic TTL (FR-21) extends the policy.
- **Audit DR-007 / `canonical-architecture-decisions.md` §2.7** — Playbook prompts live in Dataverse `sprk_analysisaction.sprk_systemprompt`, NOT in `/Prompts/` directories or `.txt` files. (This is an audit-codified pattern, not an ADR — consistent with NFR-09.)
```

**Rationale**: The prompt-in-Dataverse rule is binding and correctly applied throughout the spec (FR-10 + Q-U4 Resolution Decision cite it accurately). The defect is purely a citation-label error that would mislead a downstream reviewer searching `docs/adr/ADR-014-*` for the rule. Fixing the label preserves NFR-09 (no new ADR), keeps the binding rule in force, and points to the actual source of truth.

**Same edit also applies** to `projects/ai-spaarke-insights-engine-widgets-r1/CLAUDE.md` "Key constraints" table (the row that reads "Audit canonical prompt pattern (§2.7) … per ADR-014" — verify the ADR-014 attribution; that file already correctly cites the audit §2.7 as the primary authority, so no edit may be needed in CLAUDE.md itself — review at task-005 or pre-Phase-1).

---

## Optional spec.md Edit (informational — for the ⚠ WARNING above)

**Location**: `spec.md` Resolution Decisions table, row "FR-03" (line 402).

**Suggested addition** (one sentence at the end of the "Decision" column):
> "Placement in `@spaarke/ai-widgets` (sibling package to `@spaarke/ui-components` per `Spaarke.AI.Widgets/` v0.1.0) preserves ADR-012's shared-library intent while keeping AI-specific Fluent v9 composition siblings (CitationBadge, ConfidenceIndicator, GroundednessHighlight) co-located."

This is a defensive clarification only — it strengthens the audit trail without changing behavior. Skip if the project owner considers the Resolution Decisions narrative sufficient on its own.

---

## Phase 1 Gating Decision

- **Mandatory edit** (ADR-014 citation correction): SHOULD be applied to spec.md before Phase 1 begins. The defect is a citation-label error, not a constraint violation, so Phase 1 implementation tasks are NOT blocked by ADR misapplication — but a downstream auditor reading spec.md as authoritative would be misled. Recommend applying the edit at task-005 (Phase 0 wrap-up) or as a one-line spec amendment.
- **Optional edit** (ADR-012 wording strengthening): defer to PR description / design-review judgment.
- **NFR-09**: VERIFIED PASS. No `docs/adr/` changes on branch.

---

## Auditor Signature

This report is the sole deliverable of task 004. The auditor did NOT modify `spec.md` (per task rubric "Do NOT modify spec.md yourself in this task — recommend the edit"). The recommended edits are concrete and ready for a separate edit task or a quick maintainer commit.

— task-execute (RIGOR: STANDARD) / adr-check skill workflow, 2026-06-10
