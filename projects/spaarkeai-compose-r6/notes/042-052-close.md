# Tasks 042 + 052 — close-out record

> 2026-08-07 · 042 commit `d06f3b34e` · both TEST-MODIFYING → Step 9.5 gates unconditional

## Task 042 — PDF intake tests + lossiness UX (FR-06, Success Criterion 2)

**Delivered** (`d06f3b34e`):
- **Server seam** `tests/integration/seam/Compose/ComposePdfIntakeRoundTripSeamTests.cs` (4 facts,
  through-the-wire on `ComposeFidelitySeamFixture`, which gained an `IComposePdfIntakeSource`
  boundary double at the SAME PublicContracts seam as the 032 template source):
  1. The full Success-Criterion-2 round trip — PDF NDA opens (sourceFormat:"pdf", synthesized PK
     carrier, reflow + page-chrome warnings, versionId null per MEDIUM-3) → model edit →
     create-on-save mints a NEW docx (valid OOXML, edit present, NDA title + table cells
     round-tripped) → inherits the source record's `sprk_matter` (B-MED-3) →
     `ReplaceFileContentAsUserAsync` NEVER called on the `.pdf` item.
  2. Intake unavailable → **503** ProblemDetails with the real detail (HIGH-1 lock-in).
  3. Nothing projectable → **422**, never a blank mount.
  4. Replace-save onto a `.pdf` target with a VALID docx baseline → **422 "NEW Word document"**
     BEFORE any write (A-HIGH-1 version-skew lock-in).
- **Client** (+16 tests; the three suites 64/64): sourceFormat reducer lifecycle (set + key mint,
  older-BFF null, clear/re-target/fileName-swap incl. `.PDF` + undefined-name, B-LOW-3 versionId
  re-baseline vs the non-PDF adopt-only-when-null contract, saveFailed retention, all-mounts clear);
  banner honest-copy (fixed-layout / new-Word-document / version-history, **no "identical to
  source"**, dark theme, dismiss/re-warn/retire, friendly copy for every `pdf-intake-*` code); save
  routing (dirty→Shape-2 create, clean→Shape-3 passthrough, **negative: never
  `/documents/{pdfId}/save`**, second save→replace on the NEW item).
- **A-LOW-2 fix** (`ComposeWorkspace.tsx`): `pdf-intake-*` facts fold into the first save's
  degradation banner even on the op-log/byte-passthrough path.
- **I-7 audit repair**: the A-HIGH-1 `.pdf` target guard's `.EndsWith` tripped the LEXICAL
  write-path text-search audit (`ComposeWritePathTextSearchAuditTests` — latently red on master
  since #747; the audit suite wasn't in the earlier filtered runs). Rephrased to
  `Path.GetExtension` + `string.Equals` — identical semantics, no banned token.

**Directional deviation (recorded per the POML's directional mode)**: the POML says "over the REAL
intake path", but the real intake calls Azure Document Intelligence — not runnable in CI (no
credentials). The seam drives from the `DocumentLayout` PublicContracts contract (the intake seam by
name) through the REAL projector → renderer → canonical hub → endpoints; the Azure-DI-shape →
DocumentLayout mapping is separately locked by `TextExtractorLayoutMappingTests`
(DocumentIntelligenceModelFactory). The lossiness-UX assertion splits the same way: the seam asserts
the SERVER data contract (marker + warnings), the jest suites assert the rendered MESSAGE + honesty.

**Evidence**: server Compose+seam+contract **897/897** · client tsc clean, **64/64** · publish
**47.06 MB** compressed incl. PDBs (Δ +0.06 vs 47.00 — includes master's FR-C3 merge-ins; ceiling
60) · `/conflict-check` soft-pass.

## Task 052 — version-history tests (FR-07, Success Criterion 4)

**Deviation: delivered-early-in-050; verified, not re-authored.** Task 050's
`tests/integration/seam/Compose/SpeVersionHistoryOboSeamTests.cs` (authored + Step-9.5-reviewed in
the 050 wave) already covers 052's ENTIRE closed acceptance set:

| 052 criterion | Covering fact |
|---|---|
| List returns v3 + v4 for the authorized user | `ListVersions_ReturnsVersionMetadata_…` (asserts "3.0" AND "4.0", newest-first, + never-app-only) |
| Open v3 while v4 latest = EXACT bytes, read-only | `OpenPriorVersion_AfterLaterVersionExists_ReturnsThatVersionsExactBytes_AndMutatesNothing` (byte-equality, explicitly ≠ v4's bytes, unknown-id 404, write-shaped facade calls `Times.Never`) + `NoRestoreOrBranchRouteExists_…` (restore 404, POST 405) |
| Negative authz — denied, bytes never leak | `CallerNotAuthorizedForDocument_…Return403_AndNeverTheBytes` (SPE-boundary denial per ADR-028) + `UnauthenticatedCaller_…401_AndNeverReachTheSpeFacade` |
| Seam shape, no banned tests | WebApplicationFactory slice; mock ONLY at ISpeFileOperations |

Re-authoring duplicate slices would violate root §11 + the ADR-038/test-diet build-vs-maintain
posture. 052 closes as **verification**: suite re-run green (7/7) on the current HEAD
(post-#747-merge + post-042). No new files; no Step 9.5 re-review needed (the covering suite passed
050's own gate).

## 042 Step 9.5 — verdict PASS-WITH-FINDINGS; ALL findings fixed same-session (`19cfb8d0b`)

The reviewer independently re-ran the suites (6/6) and cleared: the intake-facade boundary judgment
(legitimate PublicContracts double, POML intent satisfied with the documented deviation), the I-7
audit repair semantics (all Path.GetExtension edge cases equivalent), fixture safety (loose null
default only ever yields a loud 503; per-class fixture instances), and the second-save/sourceFormat
proof. Findings → triage:

| # | Finding | Resolution |
|---|---|---|
| MED-1 | `NotContain("CONFIDENTIAL\n")` was vacuous (run text never carries newlines) — a chrome-inlining regression would stay green | **FIXED** — raw-token asserts (`CONFIDENTIAL`, `Page 1 of 2`; ordinal, no body-prose collision) |
| MED-2 | Binding-plan "apply-template 422" fact missing — the A-MED-1 guard had zero coverage | **FIXED** — new seam fact (template resolves, item bytes %PDF-, typed 422 + honest detail, write-never) |
| MED-3 | forkNew + retry-same-key FLOW tests missing (reducer retention was necessary-not-sufficient — a fresh-key-per-attempt regression would break G7 dedup silently) | **FIXED** — retry test (one-shot 500 injection; `transientKey` identical across attempts) + forkNew test (editor-hosted split-button mode arg; fresh key ≠ load-minted) |
| LOW-1 | The workspace→banner wire untested | **FIXED** — banner asserted rendered pre-save + retired post-save in the dirty flow test |
| LOW-2 | pdf-intake facts re-surface on repeated op-log saves | **ACCEPTED-HONEST** — acknowledged in the fold comment (each persisted artifact embodies the loss; banner dismissible; model-path save clears) |
| LOW-3 | no-extension fileName-swap case untested | **FIXED** — reducer case added |

Final: seam 5/5 · client suites 66/66 · tsc clean.
