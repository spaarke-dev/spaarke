# Spaarke AI Platform Unification R6 — AI Context

> **Purpose**: This file provides context for Claude Code when working on the R6 architecture convergence project.
> **Always load this file first** when working on any task in this project.

---

## Project Status

- **Phase**: Ready for Implementation (Phase A pending start)
- **Last Updated**: 2026-06-07
- **Current Task**: Not started (project-pipeline artifacts just generated)
- **Next Action**: Run task-create (Step 3 of project-pipeline) to decompose plan.md into ~80 POML task files

---

## Quick Reference

### Key Files
- [`spec.md`](spec.md) — AI-optimized implementation specification (59 FRs, 16 NFRs across 9 pillars)
- [`design.md`](design.md) — Comprehensive R6 design document (1457 lines)
- [`README.md`](README.md) — Project overview + graduation criteria
- [`plan.md`](plan.md) — Implementation WBS + Discovered Resources + Q1–Q11 decisions + parallel execution groups
- [`current-task.md`](current-task.md) — **Active task state** (context recovery)
- [`tasks/TASK-INDEX.md`](tasks/TASK-INDEX.md) — Task tracker (generated by task-create)

### Project Metadata
- **Project Name**: spaarke-ai-platform-unification-r6
- **Type**: Architecture convergence (BFF + Frontend + Dataverse)
- **Complexity**: High (9 pillars, ~80 tasks, multi-pillar interaction)
- **Predecessor**: spaarke-ai-platform-unification-r5 (closed with known limitations 2026-06-06)

---

## Context Loading Rules

When working on this project, Claude Code MUST:

1. **Always load this file first** when starting work on any task
2. **Check current-task.md** for active work state (especially after compaction/new session)
3. **Reference spec.md + plan.md** for FR/NFR specifics + binding decisions
4. **Reference README.md** for graduation criteria
5. **Load the relevant task file** from `tasks/` based on current work
6. **Apply ADRs** loaded automatically via adr-aware (relevant set in plan.md §2)
7. **Read R5 lessons-learned**: `projects/spaarke-ai-platform-unification-r5/notes/lessons-learned.md` for 4 gap families + 7 carry-forward patterns

**Context Recovery**: If resuming work, see [Context Recovery Protocol](../../docs/procedures/context-recovery.md)

---

## 🚨 MANDATORY: Task Execution Protocol

**ABSOLUTE RULE**: All task work MUST use the `task-execute` skill. DO NOT read POML files directly and implement manually.

### Auto-Detection Rules (Trigger Phrases)

| User Says | Required Action |
|-----------|-----------------|
| "work on task X" | Execute task X via task-execute |
| "continue" / "keep going" / "next task" | Read TASK-INDEX.md, find first 🔲, invoke task-execute |
| "continue with task X" / "resume task X" | Execute task X via task-execute |
| "pick up where we left off" | Load current-task.md, invoke task-execute |

**Implementation**: When user triggers task work, invoke Skill tool with `skill="task-execute"` and task file path.

### Why This Matters

The task-execute skill ensures:
- ✅ Knowledge files loaded (ADRs, constraints, patterns)
- ✅ Context tracked in current-task.md
- ✅ Proactive checkpointing every 3 steps
- ✅ Quality gates run (code-review + adr-check) at Step 9.5
- ✅ Progress recoverable after compaction

**Bypassing this skill leads to**: missing ADR constraints, lost progress after compaction, skipped quality gates.

### Parallel Task Execution

When tasks can run in parallel (no inter-task dependencies), each task STILL uses task-execute. Pattern: ONE message with MULTIPLE Skill tool invocations (one per task). Max 6 parallel agents per wave per project-pipeline cap.

**Build verification between waves**: After each parallel wave, main session MUST run `dotnet build src/server/api/Sprk.Bff.Api/` if any `.cs` modified; relevant `npm run build` if any `.ts`/`.tsx` modified. STOP if build fails.

See [task-execute SKILL.md](../../.claude/skills/task-execute/SKILL.md) for complete protocol.

### 🚨 MUST: Multi-File Work Decomposition

For tasks modifying 4+ files, Claude Code MUST:
1. Decompose into dependency graph; identify parallel-safe vs sequential work
2. Delegate to subagents in parallel where safe (ONE message, MULTIPLE Task tool calls)
3. Parallelize when files are in different modules / no shared interfaces / no cross-imports
4. Serialize when files have tight coupling / one must exist before another / sequential logic required

`.claude/` path tasks are sequential (sub-agent write boundary per root CLAUDE.md §3). task-create auto-marks `parallel-safe: false` for these.

---

## 🚨 ADRs Are Defaults — Challenge When Warranted (binding operating principle)

**ADRs are guardrails, not immutable laws.** The default is to follow them. But agents MUST surface trade-offs honestly when an ADR or rule produces a suboptimal technical outcome — they MUST NOT silently build workarounds around an ADR to avoid touching it.

### When to challenge an ADR (mandatory stop-and-surface)

Sub-agents executing R6 tasks MUST stop and report to the main session (and through it, to the user) when ANY of these conditions hold:

1. **The optimal technical solution is blocked by a discipline rule, not a binding architectural constraint** (e.g., the constraint is a project scope rule like NFR-03 "no new ADRs," not a hard architectural invariant)
2. **The "compliant" path requires a known-fragile workaround** — losing streaming UX, building a parallel implementation, accepting runtime fragility to avoid compile-time investment, etc.
3. **The ADR is being enforced on a case the original author didn't anticipate** — the spec/ADR was written without the specific scenario in mind
4. **An existing ADR can be cleanly extended (additively) but the project rule forbids it** — the change doesn't violate the ADR's intent; it just requires updating the document

### What the agent MUST do (not what to silently work around)

When any condition above holds:

1. **STOP execution** before implementing the suboptimal workaround
2. **NAME THE SPECIFIC ADR** — don't say "an ADR blocks this." Say "ADR-029 publish-size constraint, NFR-03 scope rule, etc." Cite the rule's text.
3. **VERIFY the rule actually blocks the change** — walk through each binding ADR and check whether the change violates its content (not just whether the project rule says "no changes")
4. **ENUMERATE the options** in plain English:
   - Follow the ADR as currently written (what it costs technically)
   - Modify or extend the ADR (what it costs in process + what it gains technically)
   - Document an exception with rationale (what it costs in clarity + future-cost)
5. **WAIT for explicit user direction** before proceeding

### NFR-03 is a scope-discipline default, NOT a hard architectural rule

NFR-03 says "no new ADRs in R6." This was a scope-discipline decision at spec authoring time to prevent ADR proliferation. Like any project rule, it can be revised per-case when the cost of NOT doing so is materially worse than the cost of doing so.

**Concrete example**: R6 Wave 7 Q9 migration surfaced that `WorkingDocumentTools` cannot fit the `IToolHandler` contract without a streaming overload. The optimal answer (add a streaming method to the interface + small focused ADR) was blocked by NFR-03. After analysis, the user decided to revise NFR-03 for this specific case and accept a new ADR (ADR-032 "Streaming chat-tool contract"). The R7-deferral was avoided because the cost (3 days + 1 ADR doc) was bounded and the gain (10/10 chat tools migrated, no closeout known-limit) was clear.

### Anti-pattern (do NOT do this)

❌ "I noticed adding the JSON Schema validator NuGet would cost ~1 MB which exceeds the R6 ≤+5 MB budget per NFR-02, so I implemented runtime validation in the handler's ValidateChat instead." — This is silently working around the ADR. The right pattern is: "I noticed JSON Schema validator NuGet adds ~1 MB. Per NFR-02 this exceeds the R6 budget. The trade-off is: (a) accept the ~1 MB and have catalog-write-time validation, (b) defer NuGet and run validation at handler-validate-time but accept that malformed rows fail at LLM invocation. **Surfacing for your decision.**"

### Pattern (do this)

✅ Name the rule. Verify what it actually says (not what discipline says). Walk through the optimal answer + the cost of the workaround. Surface for explicit decision.

This applies to ALL R6 tasks, especially sub-agent dispatch. Sub-agent prompts should explicitly enable this pattern; main session should validate the agent surfaced trade-offs vs working around them.

---

## R6 Binding Decisions (Q1–Q11 resolved 2026-06-07)

These are decided. Tasks reference them without re-prompting. Full table in [plan.md](plan.md) §2 "Key Technical Decisions".

**Highlights**:
- **Q1 + Q2**: Persona = standalone `sprk_aipersona` entity; most-specific-wins inheritance (SYS- < CUST- < playbook)
- **Q5 (re-shaped)**: `outputSchema` on **action** (intrinsic shape); destination + widgetType on **node config** (per-playbook routing); `StructuredOutputStreamWidget` schema-aware; duplicate-fire fix at **CapabilityRouter** (not action metadata). Migrate 4 existing actions: summarize-chat, summarize-workspace, matter-prefill, project-prefill. **Pre-fill hook signatures + 45s timeout + `useAiPrefill` UNCHANGED per NFR-07.**
- **Q7 (scope expansion)**: Build full memory management UI in R6 (was R7-deferred). Pinned Memory CRUD + visualization in Context pane. Phase C +1–2 weeks.
- **Q9 (revised 2026-06-07 after deep analysis)**: 10 chat tools migrated in THREE sub-waves grouped by migration shape — NOT a single big-bang. Wave 7 = 4 trivial tools (AnalysisQuery, KnowledgeRetrieval, TextRefinement, VerifyCitations). Wave 8 = 4 citation/SSE-state tools (DocumentSearch, WebSearch, CodeInterpreter, LegalResearch). Wave 9 = WorkingDocumentTools (with new **ADR-032 Streaming chat-tool contract** — NFR-03 revised per "ADRs Are Defaults" principle since the optimal technical answer doesn't fit current contract). AnalysisExecutionTools deleted (redundant with Pillar 3 `invoke_playbook`). 2 bridges (InvokeSummarize, InvokeInsightsQuery) deleted via task 023. Reasoning: deep analysis showed 10 tools are heterogeneous; sub-wave grouping reduces file-overlap risk + validates citations-in-metadata pattern at small scale before applying at scale.
- **Q11**: New `IInvokePlaybookAi` facade per ADR-013 (not extension of `IWorkspacePrefillAi`).
- **Pillar 6 split**: 6a (state model + persist + endpoint + prompt snapshot) → 6b (chat tools + affordances + conflict) + 6c (trace widget + events) + 7 + 9 parallel after 6a
- **Handler order**: Wave 1 deterministic (4 parallel) → Wave 2 LLM-assisted (4 parallel)

---

## Key Technical Constraints

### Binding NFRs (from spec)
- **NFR-01 Conversational primacy** — LLM responds conversationally for most turns. CapabilityRouter restricts tool list but never prevents conversational ability. Refinement / follow-up / comparison / context-injection all work via full conversation history + memory.
- **NFR-02 BFF publish size** — ≤+5 MB total across R6 budget. Per-task verification required. Current baseline ~45.65 MB; ceiling 60 MB hard.
- **NFR-03 ADR compliance** — R6 honors without modification: ADR-013, ADR-030, ADR-031, ADR-014, ADR-016, ADR-028, ADR-010, ADR-008, ADR-015, ADR-029. **NO new ADRs in R6.**
- **NFR-04 Zero Microsoft Agent Framework references** in code or design. The ONLY acceptable mentions are explicit "NO Agent Framework" statements in design.md §1.2 and §4.0.
- **NFR-05 4-channel PaneEventBus preserved** (workspace / context / conversation / safety per ADR-030). Additive event types only; no 5th channel.
- **NFR-06 4-stage shell lifecycle preserved** (welcome / loading / active-chat / review per ADR-031). No new stages.
- **NFR-07 Pre-fill flow preserved entirely** — `MatterPreFillService`, `ProjectPreFillService`, `useAiPrefill` hook, 45s timeout, `$choices`-constrained output contract, `Workspace:PreFillPlaybookId` / `Workspace:ProjectPreFillPlaybookId` config — ALL UNCHANGED. R6 may extend `IWorkspacePrefillAi` with new methods but existing signatures stay.
- **NFR-08 11 production node executors preserved** (AiAnalysis, AiCompletion, Condition, DeliverOutput, DeliverToIndex, UpdateRecord, CreateTask, SendEmail, CreateNotification, QueryDataverse, Start). R6 does NOT modify, deprecate, or extend any.
- **NFR-09 M365 Copilot thinness preserved** — Agent Gateway endpoints (`/api/agent/*`) stay thin adapters.
- **NFR-10 Token budget management** — 8K system prompt budget per `PlaybookChatContextProvider` preserved.
- **NFR-13 Safety pipeline preserved** — PromptShield + Groundedness + Citations + Privilege + Cross-matter via `SafetyPipelineMiddleware`.

### MUST Rules (from spec §Technical Constraints)
- ✅ **MUST** NOT introduce Microsoft Agent Framework dependencies in code or design
- ✅ **MUST** preserve pre-fill flow signatures + 45s timeout + `useAiPrefill` per NFR-07
- ✅ **MUST** preserve all 11 node executors as-is per NFR-08
- ✅ **MUST** keep PaneEventBus at exactly 4 channels per ADR-030
- ✅ **MUST** keep shell stages at exactly 4 per ADR-031
- ✅ **MUST** route CRUD-side AI consumers through `Services/Ai/PublicContracts/` facades per ADR-013
- ✅ **MUST** include `tenantId` in all Redis cache keys per ADR-014
- ✅ **MUST** verify BFF publish size per task; report delta vs prior baseline; ≤60 MB ceiling per ADR-029
- ✅ **MUST** register new R6 services within existing DI modules per ADR-010
- ✅ **MUST** apply endpoint filters for authorization on new endpoints per ADR-008
- ✅ **MUST** preserve LLM conversational primacy — scopes augment, never replace, per NFR-01
- ✅ **MUST** route chat `/summarize` through `PlaybookExecutionEngine` (no alternate-key bypass after Pillar 4 lands)
- ✅ **MUST** preserve existing 10 chat tools' functionality during Q9 batch migration (regression test required)
- ✅ **MUST** build all 8 typed handlers (per owner clarification — Generic was stopgap, specialized handlers were intent)

### MUST NOT Rules
- ❌ **MUST NOT** add 5th PaneEventBus channel
- ❌ **MUST NOT** add new shell lifecycle stage
- ❌ **MUST NOT** inject `IOpenAiClient`, `IPlaybookService`, `IPlaybookOrchestrationService`, or other AI-internal types directly into CRUD-side code (ADR-013 facade boundary)
- ❌ **MUST NOT** introduce new ADRs (R6 operates within existing constraints)
- ❌ **MUST NOT** add new feature flags per ADR-018
- ❌ **MUST NOT** modify wizard pre-fill flow's existing signatures or contracts
- ❌ **MUST NOT** replace `PlaybookExecutionEngine` or any of the 11 node executors
- ❌ **MUST NOT** add Microsoft Agent Framework references anywhere
- ❌ **MUST NOT** log user message content in execution-trace events per ADR-015
- ❌ **MUST NOT** hardcode persona text in C# after Pillar 1 lands (data-driven only)
- ❌ **MUST NOT** load action by alternate key in chat `/summarize` path after Pillar 4 lands

---

## Per-Pillar Binding Rules

### Pillar 1 (Persona)
- **Entity**: `sprk_aipersona` (standalone; mirrors existing 4-scope schema with SYS-/CUST- prefix enforcement, `parentScopeId` for inheritance, `description`, `systemPrompt`, `scopeType`)
- **Resolution**: Most-specific-wins (global SYS- < tenant CUST- < playbook-attached) via existing `IScopeResolverService`
- **Migration**: Seed default global SYS- row with current hardcoded `BuildDefaultSystemPrompt()` text VERBATIM; replace `BuildDefaultSystemPrompt()` call site in `SprkChatAgentFactory.CreateAgentAsync`
- **Authoring UX**: Power Apps Dataverse forms (Q3 — no custom admin UI in R6)

### Pillar 2 (Tool Registry + 8 Typed Handlers)
- **Rename**: `IAnalysisToolHandler` → `IToolHandler` with type alias preserved
- **DTO extensions**: `AvailableInContexts` enum (Playbook/Chat/Both) + `JsonSchema` field on `AnalysisTool`
- **Context split**: `ToolExecutionContext` (playbook) + `ChatInvocationContext` (chat) inherit shared base
- **Migration batch (Q9)**: All 10 pre-R5 chat tools (DocumentSearch, AnalysisQuery, KnowledgeRetrieval, TextRefinement, WorkingDocument, AnalysisExecution, WebSearch, CodeInterpreter, LegalResearch, VerifyCitations) → single Phase A task with comprehensive regression test
- **8 typed handlers**: Wave 1 deterministic (DateExtractor, FinancialCalculator, ClauseComparison, FinancialCalculation) → Wave 2 LLM-assisted (EntityExtractor, ClauseAnalyzer, RiskDetector, InvoiceExtraction)

### Pillar 3 (Generic `invoke_playbook`)
- **Facade**: NEW `IInvokePlaybookAi` in `Services/Ai/PublicContracts/` per ADR-013 (Q11)
- **Tool**: `invoke_playbook(playbookId, parameters)` with dynamic playbook list in description
- **Removed**: `InvokeSummarizePlaybookTool` + `InvokeInsightsQueryTool` C# files; callers route through generic tool

### Pillar 4 (Playbook FK fix)
- **Data fix**: `summarize-document-for-chat@v1` node FK → `SUM-CHAT@v1` action; FK chain validates at startup
- **Refactor**: `SessionSummarizeOrchestrator` invokes `PlaybookExecutionEngine.ExecuteAsync(playbookId)`; existing session-files Azure Search filter + Structured Outputs mode + streaming JSON delta preserved; NO alternate-key lookup remains in chat path

### Pillar 5 (Output schema — RE-SHAPED PER Q5)
- **Action**: `sprk_analysisaction.outputSchema` JSON field (intrinsic data shape). Action-fixed; describes what the action produces.
- **Node config**: `destination` enum (chat / workspace / form-prefill / side-effect) + `widgetType` (for workspace destinations). Per-playbook routing. Existing `DeliverOutput` node executor reads these.
- **Widget**: `StructuredOutputStreamWidget` reads action's outputSchema and renders schema-aware (array → bullets; object → labeled key-value blocks). Fixes R5 TL;DR + Entities bugs.
- **Dedup**: At **CapabilityRouter** level. One user intent → one route → one playbook → one DeliverOutput → one render. NOT solved by action metadata.
- **Migration**: 4 existing actions populate `outputSchema`: summarize-document-for-chat, summarize-document-for-workspace, matter-prefill, project-prefill. **Pre-fill hook signatures + 45s timeout + `useAiPrefill` UNCHANGED per NFR-07.**

### Pillar 6 (split 6a/6b/6c)
- **6a (gates 6b/6c/7/9)**: `WorkspaceTab` canonical TypeScript interface; Redis hot tier (24h TTL) + Cosmos durable persistence on pin (Q4 hybrid); `GET /api/workspace/state` endpoint; per-turn snapshot in `SprkChatAgentFactory.CreateAgentAsync` system prompt
- **6b**: Chat tools for tab mutation (`send_workspace_artifact`, `update_workspace_tab`, `close_workspace_tab` — registered as `sprk_analysistool` rows); user affordances ("Send to Workspace", "Add to Assistant" toggle, "Pin to Matter"); conflict resolution per Q8 (user wins; agent reads `lastUserEditAt` and refuses on stale write with re-read prompt)
- **6c**: Execution-trace widget in Context pane (Claude-Code-like ordered timeline); additive `context.*` events: `tool_call_started`, `tool_call_completed`, `knowledge_retrieved`, `playbook_node_executing`, `playbook_node_completed`, `decision_made`. Additive `workspace.*` events: `user_selection`, `tab_edited`, `tab_focused`, `tab_provenance_clicked`. **ADR-015 BINDING**: log tool name + decision + timestamp ONLY. Never user message text. Deterministic IDs (matterId, scopeId) acceptable.

### Pillar 7 (Memory + Q7 expansion)
- **Infrastructure**: Summarization compression (replace oldest M turns with LLM summary when sliding window exceeds budget); pinned-context entity in Cosmos `memory` container (pinType: user-preference / system-rule / matter-fact); selective recall via embedding similarity (existing `IEmbeddingCache`); hierarchical composition (recent verbatim + compressed mid + retrieved old)
- **Activation**: `MatterMemoryService` already exists; wire into chat-agent system prompt assembly
- **Shared budget**: Token budget tracker — factory + document context + knowledge retrieval + memory share existing 8K budget per NFR-10
- **User affordances**: agent recognition of "remember X" / "forget X" / "always X" via existing CapabilityRouter
- **Q7 SCOPE EXPANSION (THIS PROJECT)**: Build full Pinned Memory CRUD + visualization UI in Context pane. Pillar 7 ships UI in R6 instead of deferring R7.

### Pillar 8 (Command Router)
- **Parser**: `CommandRouter` builds `Intent { command, references[], rawText }` before agent invocation
- **Hard slashes** (deterministic, bypass LLM; <100ms latency target): `/clear`, `/new-session`, `/help`, `/export`, `/save-to-matter [matterId]`, `/pin`
- **Soft slashes** (intent shortcuts via agent): `/summarize`, `/draft`, `/extract-entities`, `/analyze`
- **References** (resolved at parse time): `#scope`, `@<entity>`, `#<filename>`
- **Composition**: `/summarize #engagement-letter.docx`; `/draft response to @opposing-counsel about #motion-to-dismiss`
- **Natural language**: still works alongside slashes via existing `CapabilityRouter`

### Pillar 9 (Widget Visibility Contract)
- **Interface**: `getAgentVisibleState(): SerializedWidgetState` (nullable; widget opts in)
- **Registry**: `WorkspaceWidgetRegistry` extended with optional `getVisibleState?: () => unknown`
- **Per-widget implementations**: Summary returns `{ widgetType, summary, tldr, hasUserEdits }`; DocumentViewer returns `{ widgetType, filename, mimeType, sizeBytes, hasSelection, selectionText? }`; Dashboard returns `{ widgetType, dashboardName, lastViewedSection }` (NOT chart data); Table returns `{ widgetType, rowCount, sortColumn, filteredColumns, selectedRows[] }`
- **Prompt builder**: gathers visible state from each Assistant-visible tab; respects `visibleToAssistant: true` filter per Pillar 6a
- **Privacy default**: widgets that should NOT expose to LLM don't appear in agent prompts; user override via "Add to Assistant" toggle (Pillar 6b)

---

## Cross-Pillar Orchestration Rules

- **6a gates 6b, 6c, 7, 9**: state model interfaces (WorkspaceTab + getVisibleState) must land first
- **Pillar 7's memory consumers depend on 6a**: workspace state is one source of context the memory layer composes from
- **Pillar 9's prompt builder depends on 6a**: per-turn snapshot uses visibleToAssistant filter
- **Pillar 2 + 6b chat tools share tool registry**: 6b's `send_workspace_artifact` / `update_workspace_tab` / `close_workspace_tab` register as `sprk_analysistool` rows after Pillar 2 infra lands
- **Pillar 3 + 4 + 5 share `PlaybookExecutionEngine`**: Pillar 4's FK fix + orchestrator refactor unblocks Pillar 3's generic tool + Pillar 5's CapabilityRouter dedup
- **Handler workstream spans Phase A–C**: Wave 1 starts after Pillar 2 infra (Phase A); Wave 2 follows Wave 1
- **CapabilityRouter dedup (Pillar 5 FR-30) requires**: Pillar 4 PlaybookExecutionEngine refactor + Pillar 3 generic invoke_playbook tool (both unify the routing surface)

---

## Confirmation Triggers (project-specific for R6)

Per session execution-style preference, parallel-agent waves proceed autonomously, but the main session MUST pause and confirm with the user when any of these triggers fire:

- Any task touches an ADR file (`.claude/adr/*.md` or `docs/adr/*.md`)
- Any task proposes a new top-level DI registration (vs adding to existing modules per ADR-010)
- Any task modifies a public contract surface in `src/server/api/Sprk.Bff.Api/Services/Ai/PublicContracts/`
- Any task proposes a schema change to a production Dataverse entity (e.g., adding fields to `sprk_analysisaction`)
- Any task finds the existing pre-fill flow needs modification beyond data migration (NFR-07 binding)
- Any task touches the 11 production node executors (NFR-08 binding — should be NONE in R6)
- Any wave fails build verification
- Any task surfaces a need for a new ADR (R6 does not allow new ADRs — this is an escalation gate)
- Any phase exit gate requires user sign-off (Phase A exit → B; Phase B → C; Phase C → D; Phase D → wrap-up)

---

## R5 Carry-Forward (Mandatory Reading)

**File**: `projects/spaarke-ai-platform-unification-r5/notes/lessons-learned.md`

**4 architectural gap families** (each R6 pillar maps to one):
- **Gap A**: Two upload paths (FR-07 inline + server-side) → one mental model gap → R6 Pillar 5/8 (path as intent property)
- **Gap B**: Chat persona hardcoded, tool registry ignored, playbook FK bypassed → R6 Pillars 1, 2, 4 (data-driven contracts)
- **Gap C**: Schema-aware rendering implicit (string-only widget) → R6 Pillar 5 (output-type metadata)
- **Gap D**: Workspace ↔ Assistant one-way (agent can't read tab state) → R6 Pillar 6 (bidirectional state model)

**7 carry-forward patterns**:
- Cross-package File-ref forwarding (optional fields for future consumers)
- FileList is live (snapshot before clearing)
- Wire-stream contract assertions (parser ↔ widget boundary tests)
- Diagnostic logs as one-shot probes (commit in bug PR, remove in closeout PR)
- `SprkChat.onBeforeSendMessage` is informational-only (can't suppress; accept duplicate-fire until architecture fixes it — Pillar 5 closes this)
- Two-wrapper widget architecture (PaneEventBus event → tab install; widget renders independently)
- Path A vs Path B parallelism is a smell (one intent → one path — Pillar 5 enforces structurally)

**3 process learnings** (what NOT to do):
- Diagnostic logs left in shipped code
- Two-upload-path ambiguity not surfaced at design
- Renderer fidelity not test-covered (Pillar 5 fixes via integration tests)

---

## Decisions Made

<!-- Log key architectural/implementation decisions here as project progresses -->
<!-- Format: Date, Decision, Rationale, Who -->

- **2026-06-07**: Q1–Q11 resolved (see plan.md §2 for full table) — Session walkthrough during /project-pipeline kickoff
- **2026-06-07**: Q5 re-shaped from spec.md original — `outputSchema` on action, destination on node config, widget schema-aware, dedup at CapabilityRouter (cleaner separation; same handlers everywhere)
- **2026-06-07**: Q7 scope expansion approved — Full Memory Management UI in R6 (was R7-deferred); Phase C +1–2 weeks
- **2026-06-07**: Q9 batch migration chosen over incremental — All 10 chat tools migrate in single Phase A task with comprehensive regression test
- **2026-06-07**: Pillar 6 split into 6a/6b/6c — Internal sequencing optimization

---

## Implementation Notes

<!-- Add notes about gotchas, workarounds, or important learnings during implementation -->

- **Worktree branch already exists**: `work/spaarke-ai-platform-unification-r6`. No new branch creation needed at Step 4. Commits go directly to this branch.
- **Master baseline as of 2026-06-07**: BFF builds with 0 errors + 16 warnings (manageable); publish size ~45.65 MB; ceiling 60 MB.
- **Existing PR overlap**: PR #360 touches `.claude/constraints/bff-extensions.md` + `.claude/patterns/ai/`. Non-code overlap; no merge-conflict risk for R6 code work.

---

## Resources

### Applicable ADRs (verified)
- ADR-008 (endpoint filters) · ADR-010 (DI minimalism) · ADR-012 (shared components / Fluent v9) · ADR-013 (AI architecture + facade boundary) · ADR-014 (AI caching) · ADR-015 (AI data governance) · ADR-016 (rate limits) · ADR-018 (feature flags) · ADR-027 (Dataverse solution management) · ADR-028 (Spaarke Auth v2) · ADR-029 (BFF publish hygiene) · ADR-030 (PaneEventBus 4-channel) · ADR-031 (4-stage shell lifecycle)

### Related Projects
- **Predecessor**: `projects/spaarke-ai-platform-unification-r5/` (closed 2026-06-06; lessons-learned.md is mandatory reading)
- **Coordination**: Insights Engine R2 (Wave F may impact Pillar 3 `invoke_playbook` for `insights.query` capability)

### External Documentation
- `docs/architecture/playbook-architecture.md` · `scope-architecture.md` · `chat-architecture.md` · `AI-ARCHITECTURE.md` · `SPAARKEAI-WORKSPACE-ARCHITECTURE.md` · `SPAARKEAI-COMPONENT-MODEL.md` · `SPAARKEAI-DASHBOARD-AND-WIDGET-MODEL.md`
- `docs/guides/JPS-AUTHORING-GUIDE.md` · `SCOPE-CONFIGURATION-GUIDE.md` · `WORKSPACE-AI-PREFILL-GUIDE.md` · `INSIGHTS-PLAYBOOK-VS-RAG-DECISION-TREE.md`
- Root [`CLAUDE.md`](../../CLAUDE.md) — repo-wide instructions; §10 BFF Hygiene binding for R6

---

*This file should be kept updated throughout project lifecycle. R6-specific decisions land in "Decisions Made"; cross-pillar contract drift gets noted in "Implementation Notes".*
