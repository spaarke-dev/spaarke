<task id="103" project="spaarke-ai-platform-unification-r6">
  <metadata>
    <title>D-H-03 `ClauseComparisonHandler` (FR-16, Wave 1 — Pure Deterministic)</title>
    <phase>Parallel — 8 Typed Tool Handlers</phase>
    <status>not-started</status>
    <estimated-effort>6 hours</estimated-effort>
    <dependencies>100</dependencies>
    <blocks>105, 106, 107, 108, 109</blocks>
    <tags>bff-api, ai, handler</tags>
    <rigor-hint>FULL</rigor-hint>
    <rigor-reason>New BFF handler; structural-diff correctness has high blast radius for contract review; FULL quality gates required.</rigor-reason>
    <parallel-group>H-G1</parallel-group>
    <parallel-safe>true</parallel-safe>
    <parallel-reason>Wave 1 pure-deterministic handler; independent of 101/102/104.</parallel-reason>
  </metadata>

  <prompt>
    Build `ClauseComparisonHandler` — a pure-deterministic `IToolHandler` implementation
    per FR-16 that performs text-diff with structural awareness on contract clauses.
    Input: two clause texts (e.g., `clauseA` + `clauseB`) plus optional structural hints
    (sentence-level vs paragraph-level vs token-level granularity). Output: structured
    diff report — segments added, segments removed, segments unchanged, segments modified;
    plus a normalized similarity score (0.0–1.0). NO LLM call. NO Azure OpenAI dependency.
    Uses well-known diff algorithm (Myers or patience diff) on tokenized + normalized
    text.

    "Structural awareness" means: ignores cosmetic whitespace differences; preserves
    sentence boundaries in the segment output; can be configured to group by paragraph
    vs sentence vs word via `sprk_configuration`. Casing-sensitivity, punctuation-sensitivity,
    stop-word handling all configurable.

    Handler registers via auto-discovery per task 100's contract — implements `IToolHandler`;
    `HandlerId = "ClauseComparisonHandler"`; lives in
    `Services/Ai/Handlers/ClauseComparisonHandler.cs`. Requires corresponding
    `sprk_analysistool` row with `sprk_handlerclass = "ClauseComparisonHandler"`.

    Tests: contract test + positive cases (identical clauses → similarity 1.0 + all
    unchanged; single-sentence change → only that sentence in modified set; reordered
    paragraphs detected as moves not delete+add) + error cases (empty clause, exceeds
    max length) + config-driven test (granularity setting changes diff output
    deterministically).
  </prompt>

  <role>
    Senior BFF engineer with .NET 8 + text-diff algorithm experience. Familiar with
    Myers diff, longest-common-subsequence, tokenization strategies, and the legal-domain
    constraints (cosmetic vs substantive change distinction).
  </role>

  <goal>
    `ClauseComparisonHandler.cs` implemented in `Services/Ai/Handlers/`; auto-discovered;
    `sprk_analysistool` row deployed; unit tests cover positive + error + config-driven
    cases including identical/single-change/reorder scenarios; ZERO LLM/Azure-OpenAI
    dependencies; BFF publish-size delta ≤+0.5 MB (or justified higher if a diff NuGet
    is added — DiffPlex or similar); `code-review` + `adr-check` green.
  </goal>

  <inputs>
    <file purpose="project-spec">projects/spaarke-ai-platform-unification-r6/spec.md</file>
    <file purpose="project-plan">projects/spaarke-ai-platform-unification-r6/plan.md</file>
    <file purpose="project-claude">projects/spaarke-ai-platform-unification-r6/CLAUDE.md</file>
    <file purpose="root-claude">CLAUDE.md</file>
    <file purpose="handler-interface">src/server/api/Sprk.Bff.Api/Services/Ai/IAnalysisToolHandler.cs</file>
    <file purpose="existing-handler-example">src/server/api/Sprk.Bff.Api/Services/Ai/Handlers/GenericAnalysisHandler.cs</file>
    <file purpose="handler-conventions">src/server/api/Sprk.Bff.Api/Services/Ai/Handlers/HandlerRegistrationConventions.md</file>
    <file purpose="bff-claude">src/server/api/Sprk.Bff.Api/CLAUDE.md</file>
  </inputs>

  <constraints>
    <constraint source="ADR-010">DI minimalism — auto-discovery; ZERO manual DI line outside `AnalysisServicesModule`.</constraint>
    <constraint source="ADR-013">Handler lives in `Services/Ai/Handlers/`; no direct CRUD-side injection.</constraint>
    <constraint source="ADR-015">Telemetry logs handler name + execution outcome + timestamp ONLY; never clause text content (clauses are user input).</constraint>
    <constraint source="ADR-029">BFF publish-size delta measured + reported; per-task ≤+0.5 MB (justify if a diff NuGet is added).</constraint>
    <constraint source="spec FR-16">Handler MUST be pure deterministic — NO LLM call, NO Azure OpenAI dependency; structural-awareness configurable.</constraint>
    <constraint source="spec NFR-02">R6 BFF publish-size budget ≤+5 MB total. NuGet additions count against budget.</constraint>
    <constraint source="project CLAUDE.md Pillar 2">Implements `IToolHandler`; auto-discovered; `sprk_analysistool` row required.</constraint>
    <constraint source=".claude/constraints/bff-extensions.md">Verify no new HIGH-severity CVE if a diff NuGet is added — run `dotnet list package --vulnerable --include-transitive` and report results.</constraint>
  </constraints>

  <knowledge>
    <topic>bff/typed-handler-deterministic</topic>
    <files>
      <file>.claude/patterns/api/service-registration.md</file>
      <file>.claude/patterns/api/endpoint-definition.md</file>
      <file>.claude/patterns/testing/unit-test-structure.md</file>
      <file>.claude/patterns/testing/mocking-patterns.md</file>
      <file>.claude/adr/ADR-010-di-minimalism.md</file>
      <file>.claude/adr/ADR-013-ai-architecture.md</file>
      <file>.claude/adr/ADR-015-ai-data-governance.md</file>
      <file>.claude/adr/ADR-029-bff-publish-hygiene.md</file>
      <file>.claude/constraints/bff-extensions.md</file>
      <file>docs/architecture/AI-ARCHITECTURE.md</file>
      <file>docs/guides/SCOPE-CONFIGURATION-GUIDE.md</file>
      <file>docs/guides/JPS-AUTHORING-GUIDE.md</file>
      <file>src/server/api/Sprk.Bff.Api/Services/Ai/IAnalysisToolHandler.cs</file>
      <file>src/server/api/Sprk.Bff.Api/Services/Ai/Handlers/GenericAnalysisHandler.cs</file>
    </files>
  </knowledge>

  <context>
    <background>
      `ClauseComparisonHandler` supports the legal-review workflow where a reviewer
      compares a proposed clause vs the original (or a standard template). Because the
      math is deterministic, reviewers + auditors can replay the diff and get identical
      output — important for litigation defensibility.

      Distinct from LLM-assisted `ClauseAnalyzerHandler` (task 106) which STRUCTURES a
      single clause into a typed schema. This handler simply compares two clause texts.
    </background>
    <relevant-files>
      <file>src/server/api/Sprk.Bff.Api/Services/Ai/Handlers/ClauseComparisonHandler.cs (NEW)</file>
      <file>tests/unit/Sprk.Bff.Api.Tests/Services/Ai/Handlers/ClauseComparisonHandlerTests.cs (NEW)</file>
      <file>infra/dataverse/sprk_analysistool-clause-comparison-row.json (NEW)</file>
      <file>projects/spaarke-ai-platform-unification-r6/notes/task-103-deployment-evidence.md (NEW)</file>
    </relevant-files>
    <related-work>
      <item>Task 100 — handler infrastructure. Hard dependency.</item>
      <item>Sibling Wave 1 tasks 101, 102, 104 — parallel-safe.</item>
      <item>Sibling Wave 2 task 106 — LLM-assisted `ClauseAnalyzerHandler` (different intent).</item>
      <item>Task 109 — dispatch test exercises this handler.</item>
    </related-work>
  </context>

  <steps>
    <step order="0" name="Context Check + RIGOR LEVEL">
      Check context. If &gt;70%, run `context-handoff` then `/compact`. Declare:

      ```
      🔒 RIGOR LEVEL: FULL
      📋 REASON: New BFF handler; structural-diff correctness; Pillar 2 binding; FR-16
      📖 PROTOCOL STEPS TO EXECUTE: all 11 steps below including Step 9.5 quality gates
      Proceeding with Step 0...
      ```
    </step>
    <step order="1" name="Review Progress">
      Read `tasks/TASK-INDEX.md`; confirm 103 is 🔲; dependency 100 complete.
    </step>
    <step order="2" name="Gather Resources">
      Read every &lt;inputs&gt; + &lt;knowledge&gt; file. Specifically extract the 4-point
      contract from `HandlerRegistrationConventions.md`.
    </step>
    <step order="3" name="Design Handler Surface">
      Define `HandlerId = "ClauseComparisonHandler"`; `ConfigSchema` with `granularity`
      (sentence/paragraph/word), `caseSensitive`, `punctuationSensitive`, `stopWordList`.
      Decide: implement Myers diff by hand (zero NuGet) OR add a small diff library
      (DiffPlex). Document choice + size-impact in notes.
    </step>
    <step order="4" name="Implement Handler">
      Create `src/server/api/Sprk.Bff.Api/Services/Ai/Handlers/ClauseComparisonHandler.cs`.
      Tokenize input per granularity setting; normalize per config flags; compute diff;
      emit segments (added/removed/unchanged/modified) + similarity score. ZERO
      `IOpenAiClient` dependency.
    </step>
    <step order="5" name="Author Unit Tests">
      Create `ClauseComparisonHandlerTests.cs` using `TypedToolHandlerTestFixture`. Cover:
      contract test; positive (identical → similarity 1.0; one-sentence change; reorder
      detection; cosmetic whitespace-only ignored when normalization on); error (empty,
      max-length); config-driven (granularity sentence vs paragraph produces different
      segment sets deterministically); telemetry (no clause text leaked).
    </step>
    <step order="6" name="Seed sprk_analysistool Row">
      Create `infra/dataverse/sprk_analysistool-clause-comparison-row.json` with
      `sprk_handlerclass = "ClauseComparisonHandler"`, `sprk_toolcode = "CLAUSE-CMP@v1"`,
      sample `sprk_configuration`, `AvailableInContexts = "Playbook"`. Deploy to Spaarke
      Dev. Capture evidence.
    </step>
    <step order="7" name="Verify Build + Tests + Vuln Check">
      `dotnet build` + `dotnet test --filter "ClauseComparisonHandler"`. Run
      `dotnet list package --vulnerable --include-transitive`; report no new HIGH-severity
      CVE per `.claude/constraints/bff-extensions.md`.
    </step>
    <step order="8" name="Verify BFF Publish-Size Delta">
      `dotnet publish -c Release ...`. Report absolute + diff vs baseline. Target ≤+0.5 MB
      (or higher if NuGet added, with justification).
    </step>
    <step order="9" name="Verify Acceptance Criteria">
      Walk through criteria; record evidence.
    </step>
    <step order="9.5" name="Quality Gates (FULL rigor)">
      Run `code-review` + `adr-check`. Both MUST pass.
    </step>
    <step order="10" name="Document + Complete">
      Update status `complete`; TASK-INDEX 103 🔲 → ✅; reset `current-task.md`.
    </step>
  </steps>

  <tools>
    <tool name="dotnet">Build BFF + run tests + publish + vuln check</tool>
    <tool name="pac">Deploy seed row</tool>
    <tool name="git">Commit</tool>
    <tool name="dataverse-deploy">Deploy seed row</tool>
    <tool name="code-review">FULL-rigor quality gate</tool>
    <tool name="adr-check">FULL-rigor quality gate</tool>
  </tools>

  <outputs>
    <output type="code">src/server/api/Sprk.Bff.Api/Services/Ai/Handlers/ClauseComparisonHandler.cs (NEW)</output>
    <output type="code">tests/unit/Sprk.Bff.Api.Tests/Services/Ai/Handlers/ClauseComparisonHandlerTests.cs (NEW)</output>
    <output type="infrastructure">infra/dataverse/sprk_analysistool-clause-comparison-row.json (NEW)</output>
    <output type="notes" ephemeral="true">projects/spaarke-ai-platform-unification-r6/notes/task-103-deployment-evidence.md</output>
    <output type="docs">projects/spaarke-ai-platform-unification-r6/tasks/TASK-INDEX.md (103 🔲 → ✅)</output>
    <output type="docs">projects/spaarke-ai-platform-unification-r6/current-task.md (reset)</output>
  </outputs>

  <acceptance-criteria>
    <criterion testable="true">`ClauseComparisonHandler.cs` exists, implements `IToolHandler`, contains ZERO references to `IOpenAiClient` or Azure OpenAI.</criterion>
    <criterion testable="true">Handler auto-discovered per `AutoDiscoveryVerificationTests`.</criterion>
    <criterion testable="true">`sprk_analysistool` row deployed with `sprk_handlerclass = "ClauseComparisonHandler"`.</criterion>
    <criterion testable="true">Unit tests pass: contract, positive (identical/single-change/reorder), error (empty/max-length), config-driven (granularity swap), telemetry (no clause text in logs).</criterion>
    <criterion testable="true">Deterministic property verified — identical inputs + same config → identical diff output.</criterion>
    <criterion testable="true">BFF build succeeds.</criterion>
    <criterion testable="true">BFF publish-size delta ≤+0.5 MB (or justified if NuGet added); recorded.</criterion>
    <criterion testable="true">`dotnet list package --vulnerable` shows no new HIGH-severity CVE.</criterion>
    <criterion testable="true">`code-review` + `adr-check` both pass.</criterion>
  </acceptance-criteria>
</task>
