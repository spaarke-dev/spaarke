# R1.5 Scope Moved — Proactive Push Now Owned by `spaarke-notification-spine-r1`

> **Date**: 2026-07-20
> **Decision**: Owner directive — notification-spine §6 resolved as **path (iii): combined project**.
> **Authoritative record**: `projects/spaarke-notification-spine-r1/design.md` §4A (absorbed scope) + §6 (resolution).
> **Note placement**: written to the main-repo copy of this project; mirror to the worktree copy (`spaarke-wt-spaarkeai-assistant-enhancements-r1`) at next sync.

## What changed

The **R1.5 proactive-push release** — designed in this project's `design.md` §14.1a (five-layer architecture), §12.5 (SignalR resolution), §14.1b (general `kind`-typed spine), §7, but never spec'd or tasked — is **absorbed into `spaarke-notification-spine-r1`** as that project's second proving consumer (`kind=suggestion`). No separate r1.5 project will be created.

**This supersedes §14.1a/§14.1b's assignment of the SignalR channel + outbox + Daily-Briefing producer + client subscriber (build items 20–24) to R1.5.** Those docs should be read with this note; do not decompose R1.5 from this project.

## What this means for this project

- **assistant-r1 closes at its reactive milestone** — the create-flow vertical (002/010/012/013/014/020/031/052) is shipped, merged to master, and through UAT rounds R3/R4. Remaining tail is owner-facing UAT items (R4-6/11/12) only.
- The reactive-first ordering decision (2026-07-15) is **satisfied**: the spine project's suggestion waves build on the shipped create flows.
- All §14.1a design content carries over intact (mapped 1:1 in spine design §4A): the durable `sprk_` outbox (payload authority, `appnotification` as optional mirror), signal-only/at-most-once SignalR semantics, chip-source renderer reusing the shipped dispatch + ack-gate (no new pipeline, no second gate), and the polling fallback endpoint. One supersession: the **Default-mode (hub-in-BFF) assumption is subject to the spine's §8A gate-zero spike** (Serverless recommended; spike decides).

## Asks of this project (carried into the spine's plan)

1. Produce the **"what lights up when `Notification` becomes routable" audit** from the dispatch side (spine §10 #7) — flipping the `DispositionRoutability` `Notification` leg is a behavior-surface change to the shipped catalog.
2. Confirm at spine spec review that the Layer-A seam preserves the **`SurfaceLaunch`/dispatch entry points + P5 ack-contract** (spine §10 #8 / §5B.3) — acting on a proactive suggestion must be behaviorally identical to a reactive dispatch.
3. Coordinate `Services/Ai` merge order while the UAT tail is active (`/conflict-check`).
