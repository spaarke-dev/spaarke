# Project Plan: Spaarke AI Platform Unification R3

> **Last Updated**: 2026-05-20
> **Status**: Ready for Tasks
> **Spec**: [spec.md](spec.md)
> **Design**: [design.md](design.md)

---

## 1. Executive Summary

**Purpose**: Reshape the SpaarkeAi welcome state per "Moment 1: Arrival" — unified `<PaneHeader>` primitive across all three panes, Assistant chrome trim + multi-file attach + History overlay, Workspace pane embeds LegalWorkspace as Home tab with Daily Briefing surface, Context pane swaps PlaybookGallery for 7 GetStarted action cards routing wizards.

**Scope**:
- New shared `<PaneHeader>` primitive in `@spaarke/ui-components` (foundation)
- Assistant pane refactor (FR-02..FR-09): 7 functional requirements
- Workspace pane refactor (FR-10..FR-16): 7 functional requirements
- Context pane refactor (FR-17..FR-22): 6 functional requirements
- Cross-cutting: error-only telemetry, auth-via-`authenticatedFetch`, backwards-compatibility (FR-23..FR-25): 3 functional requirements
- Possible BFF endpoint extension for `attachments[]` (FR-07 backend, conditional on spike result)

**Timeline**: 4–6 weeks | **Estimated Effort**: 60–80 hours (30–40 tasks @ 2–3 h each + spikes + deploy/verification)

---

## 2. Architecture Context

### Design Constraints

**From ADRs** (must comply):
- **ADR-006**: PCF over web resources — confirms SpaarkeAi + LegalWorkspace are full-page custom pages, not PCFs (architectural placement guard).
- **ADR-008**: Endpoint filters — any new/changed backend endpoint inherits existing auth/rate-limit/validation pipeline.
- **ADR-010**: DI minimalism — register any new BFF service in an existing feature module; no new modules.
- **ADR-012 (LOAD-BEARING)**: Shared components live in `@spaarke/ui-components`; `<PaneHeader>` and (if needed) `ActionCard` lifted here. All context-agnostic components → shared lib, not solution-local.
- **ADR-013**: AI architecture — Daily Briefing and chat-attachment paths extend the BFF in-process; no new service.
- **ADR-014**: AI caching — Daily Briefing response cached per-user with short TTL (~5 min) to avoid hammering LLM on tab switches.
- **ADR-016**: AI rate limits — Daily Briefing endpoint subject to existing rate-limit filters; widget shows graceful degraded UI on 429.
- **ADR-021 (LOAD-BEARING)**: Fluent v9 tokens only. No hardcoded `#` colors, no `rgba(...)` literals, no Fluent v8. Dark-mode safe by construction.
- **ADR-022**: React 19 + platform-library boundaries — React 19 for all SpaarkeAi/LegalWorkspace code; affects bundling for PDF.js.
- **ADR-025**: Icon library — Fluent v9 `@fluentui/react-icons` only (`ChatRegular`, `AppsListRegular`, `DocumentRegular`, `HistoryRegular`, `AttachRegular`); no new SVG web resources.
- **ADR-026**: Full-page custom pages — Vite + React 19 bundled; all new shared components built per this standard.
- **ADR-028 (LOAD-BEARING)**: Spaarke auth v2 — all BFF calls go through `authenticatedFetch` from `@spaarke/auth`. Function-based contract per INV-1..INV-8. No token snapshots in props or state. Per-request `getAccessToken()`.

**From Spec**:
- **MUST NOT** create Dataverse Document entities from the Assistant `+` button (in-memory message context only).
- **MUST NOT** introduce React 16 fallbacks; React 19 only.
- **MUST NOT** add happy-path App Insights events in this project; error-only telemetry per OC-09.
- **MUST** keep the standalone LegalWorkspace code page unchanged (NFR-10, FR-25).

### Key Technical Decisions

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Embed LegalWorkspace via `WorkspaceShell` (not rebuild) | Reuse shipping code; one source of truth for layouts/sections/persistence | Workspace pane code is thin composition + `WorkspacePaneMenu`; LegalWorkspace untouched |
| Side overlay for History (Claude-Code style) | OC-01; replaces conversation in place | `HistoryOverlay.tsx` is a new component; no tab system to maintain in ConversationPane |
| Multi-file (≤5) extracted client-side | OC-02 / OC-07; avoids new SPE pipeline | New `useChatFileAttachment` hook + lazy PDF.js/mammoth; possibly small BFF payload extension |
| `MAX_WORKSPACE_TABS = 8` configurable, Home exempt | OC-03; predictable FIFO eviction | Constant in one place in [`WorkspaceTabManager.ts`](../../src/solutions/SpaarkeAi/src/components/workspace/WorkspaceTabManager.ts) |
| Error-only telemetry | OC-09; scope discipline | Telemetry helper module + 3 emission sites (Daily Briefing 429, file extraction failure, HistoryOverlay load failure) |
| Stay on existing `work/spaarke-ai-platform-unification-r3` worktree branch | Repo worktree convention | Step 4 of /project-pipeline does not create new branch; commits land on this worktree |

### Discovered Resources

**Applicable ADRs** (all confirmed present in concise + full form):

| ADR | Concise | Full | Load-Bearing |
|-----|---------|------|--------------|
| ADR-006 | [.claude/adr/ADR-006-*.md](../../.claude/adr/) | [docs/adr/ADR-006-*.md](../../docs/adr/) | |
| ADR-008 | [.claude/adr/ADR-008-*.md](../../.claude/adr/) | [docs/adr/ADR-008-*.md](../../docs/adr/) | |
| ADR-010 | [.claude/adr/ADR-010-*.md](../../.claude/adr/) | [docs/adr/ADR-010-*.md](../../docs/adr/) | |
| **ADR-012** | [.claude/adr/ADR-012-*.md](../../.claude/adr/) | [docs/adr/ADR-012-*.md](../../docs/adr/) | **YES** |
| ADR-013 | [.claude/adr/ADR-013-*.md](../../.claude/adr/) | [docs/adr/ADR-013-*.md](../../docs/adr/) | |
| ADR-014 | [.claude/adr/ADR-014-*.md](../../.claude/adr/) | [docs/adr/ADR-014-*.md](../../docs/adr/) | |
| ADR-016 | [.claude/adr/ADR-016-*.md](../../.claude/adr/) | [docs/adr/ADR-016-*.md](../../docs/adr/) | |
| **ADR-021** | [.claude/adr/ADR-021-*.md](../../.claude/adr/) | [docs/adr/ADR-021-*.md](../../docs/adr/) | **YES** |
| ADR-022 | [.claude/adr/ADR-022-*.md](../../.claude/adr/) | [docs/adr/ADR-022-*.md](../../docs/adr/) | |
| ADR-025 | [.claude/adr/ADR-025-*.md](../../.claude/adr/) | [docs/adr/ADR-025-*.md](../../docs/adr/) | |
| ADR-026 | [.claude/adr/ADR-026-*.md](../../.claude/adr/) | [docs/adr/ADR-026-*.md](../../docs/adr/) | |
| **ADR-028** | [.claude/adr/ADR-028-*.md](../../.claude/adr/) | [docs/adr/ADR-028-*.md](../../docs/adr/) | **YES** |

**Applicable Skills** (referenced in task `<skills>` sections):

- [`.claude/skills/task-execute/`](../../.claude/skills/task-execute/) — mandatory protocol for every task
- [`.claude/skills/adr-aware/`](../../.claude/skills/adr-aware/) — auto-loads ADRs when creating resources
- [`.claude/skills/script-aware/`](../../.claude/skills/script-aware/) — discovers reusable scripts before writing new code
- [`.claude/skills/code-page-deploy/`](../../.claude/skills/code-page-deploy/) — build + deploy React 19 Code Pages (Vite + singlefile)
- [`.claude/skills/ui-test/`](../../.claude/skills/ui-test/) — browser-based smoke testing for Code Pages
- [`.claude/skills/code-review/`](../../.claude/skills/code-review/) — quality gate after implementation
- [`.claude/skills/adr-check/`](../../.claude/skills/adr-check/) — validate code against loaded ADRs
- [`.claude/skills/dataverse-deploy/`](../../.claude/skills/dataverse-deploy/) — deploy web resources + solutions for AssignWork custom page reference
- [`.claude/skills/push-to-github/`](../../.claude/skills/push-to-github/) — branch commit + push
- [`.claude/skills/merge-to-master/`](../../.claude/skills/merge-to-master/) — final merge with safety checks
- [`.claude/skills/repo-cleanup/`](../../.claude/skills/repo-cleanup/) — project end hygiene

**NOT applicable**: `pcf-deploy` (SpaarkeAi is a full-page custom page, not a PCF).

**Applicable Patterns**:

- [`.claude/patterns/auth/spaarke-sso-binding.md`](../../.claude/patterns/auth/spaarke-sso-binding.md) — canonical MSAL invariants (INV-1..INV-8) + v2 token contract
- [`.claude/patterns/auth/bff-url-normalization.md`](../../.claude/patterns/auth/bff-url-normalization.md) — `buildBffApiUrl()` for API calls
- [`.claude/patterns/webresource/full-page-custom-page.md`](../../.claude/patterns/webresource/full-page-custom-page.md) — Vite entry setup, React 19 pattern, Fluent provider, URL params
- [`.claude/patterns/webresource/code-page-wizard-wrapper.md`](../../.claude/patterns/webresource/code-page-wizard-wrapper.md) — thin wrapper pattern around shared dialogs

**Applicable Guides**:

- [`docs/guides/SHARED-UI-COMPONENTS-GUIDE.md`](../../docs/guides/SHARED-UI-COMPONENTS-GUIDE.md) — `@spaarke/ui-components` build, consumption, deep imports
- [`docs/guides/auth-deployment-setup.md`](../../docs/guides/auth-deployment-setup.md) — Auth v2 operator runbook (Dataverse env vars, App Service settings, MI, smoke tests)
- [`docs/guides/DEPLOYMENT-VERIFICATION-GUIDE.md`](../../docs/guides/DEPLOYMENT-VERIFICATION-GUIDE.md) — post-deploy smoke tests for Code Pages + BFF
- [`docs/guides/WORKSPACE-AI-PREFILL-GUIDE.md`](../../docs/guides/WORKSPACE-AI-PREFILL-GUIDE.md) — workspace launch-point context & initialization
- [`docs/guides/WORKSPACE-ENTITY-CREATION-GUIDE.md`](../../docs/guides/WORKSPACE-ENTITY-CREATION-GUIDE.md) — entity creation from workspaces (Matter, Project)

**Build/Deploy Scripts**:

- [`scripts/Build-ViteSolutionsDirect.ps1`](../../scripts/Build-ViteSolutionsDirect.ps1) — primary build entry for Vite Code Page solutions
- [`scripts/Deploy-SpaarkeAi.ps1`](../../scripts/Deploy-SpaarkeAi.ps1) — deploy SpaarkeAi three-pane shell + workspace embed
- [`scripts/Deploy-LegalWorkspaceCustomPage.ps1`](../../scripts/Deploy-LegalWorkspaceCustomPage.ps1) — standalone LegalWorkspace (regression target)
- [`scripts/Test-Deployment.ps1`](../../scripts/Test-Deployment.ps1) — post-deploy smoke verification

**Reusable Code (DO NOT reinvent)**:

- `<PaneHeader>` style reference: [`src/solutions/SpaarkeAi/src/components/context/ContextPaneController.tsx:142-171, 691-700`](../../src/solutions/SpaarkeAi/src/components/context/ContextPaneController.tsx) — canonical visual model
- `PaneEventBus` channels: [`src/client/shared/Spaarke.AI.Widgets/src/events/PaneEventBusContext.tsx`](../../src/client/shared/Spaarke.AI.Widgets/src/events/PaneEventBusContext.tsx) — `workspace`, `context`, `conversation`, `safety`
- `authenticatedFetch` + `useAuth()`: `@spaarke/auth` per ADR-028 INV-1..INV-8
- `WorkspaceShell`, `useWorkspaceLayouts`: [`src/solutions/LegalWorkspace/src/hooks/useWorkspaceLayouts.ts`](../../src/solutions/LegalWorkspace/src/hooks/useWorkspaceLayouts.ts) — embed as-is
- `ActionCard` + handlers split: [`src/solutions/LegalWorkspace/src/components/GetStarted/`](../../src/solutions/LegalWorkspace/src/components/GetStarted/) — reuse, possibly lift to shared lib (A-3)
- `CreateProjectWizardDialog`, `FindSimilarWizardDialog`: existing dialogs — wrap into widget wrappers (FR-19)
- `CreateMatterWizardWidget`, `DocumentUploadWizardWidget`: already in `WorkspaceWidgetRegistry`
- `sectionRegistry` + `*.registration.ts`: [`src/solutions/LegalWorkspace/src/sectionRegistry.ts`](../../src/solutions/LegalWorkspace/src/sectionRegistry.ts) — Daily Briefing follows this pattern

**R2 lessons to inherit** (from [`projects/spaarke-ai-platform-unification-r2/notes/lessons-learned.md`](../spaarke-ai-platform-unification-r2/notes/lessons-learned.md)):
- PaneEventBus multi-subscriber pattern (ADR candidate, still pending)
- Widget registry pattern (Workspace + Context separate, shared interface)
- Data-refreshed restore (D-08) — re-fetch fresh data on restore vs replaying snapshots
- Three-pane lifecycle stages (Welcome/Active/Review/Complete)
- Single-LLM-call invariant (D-01)
- Write-through Cosmos persistence (D-06)

---

## 3. Implementation Approach

### Phase Structure

```
Phase 0: Spike (1 task, serial, blocking)
└─ FR-07 backend payload verification (30 min)

Phase A: Foundations (4 tasks, parallel-safe)
└─ <PaneHeader>, MAX_WORKSPACE_TABS, ActionCard lift, telemetry helpers

Phase B: Assistant pane (7 tasks, parallel within phase after A)
└─ Welcome chrome trim, PaneHeader migration, HistoryOverlay, input-on-load,
   file-attach hook + toolbar restructure + attachment payload wiring

Phase C: Workspace pane (6 tasks, parallel within phase after A)
└─ PaneHeader + LegalWorkspace embed, delete LandingWidget, WorkspacePaneMenu,
   layout templateFilter, Daily Briefing section + 429/empty state

Phase D: Context pane (6 tasks, parallel within phase after A)
└─ PaneHeader + stage label, GetStartedCardsWidget, wizard wrappers,
   AB intents, AssignWorkLauncher

Phase E: Backend (conditional, 2 tasks, only if Phase 0 spike requires)
└─ ChatEndpoints attachments[] schema + tests

Phase F: Integration & verification (4 tasks, serial after B+C+D)
└─ Auth audit, bundle-size, dark-mode token audit, backwards-compat

Phase G: Deploy + smoke (5 tasks, serial after F)
└─ Deploy SpaarkeAi, per-pane UI smoke, NFR verification

Phase H: Wrap-up (1 task, serial)
└─ Lessons-learned, README → Complete, archive
```

### Critical Path

**Blocking dependencies:**
- Phase A blocks Phases B, C, D (all consume `<PaneHeader>`, telemetry helpers, ActionCard)
- Phase 0 blocks task 026 in Phase B (attachment payload wiring) and Phase E (if needed)
- Phase F blocks Phase G (deploy only after verification passes)
- Phase G blocks Phase H

**High-risk items:**
- **Backend payload spike (task 001)**: outcome determines whether Phase E exists. Mitigation: 30-min timeboxed spike.
- **PDF.js + mammoth bundle size (NFR-12)**: lazy-loaded; verified via bundle analyzer in task 061.
- **Session persistence of non-Home tabs (NFR-09 / UQ-03)**: may require BFF/Cosmos extension. Mitigation: verify in Phase F task 063; opens sub-task if gap surfaces.

---

## 4. Phase Breakdown

### Phase 0: Spike — FR-07 Backend Payload Verification

**Objectives:**
1. Determine whether `POST /api/ai/chat/sessions/{sessionId}/messages` accepts an `attachments[]` field today, or requires schema extension.

**Deliverables:**
- [ ] Spike report (15–30 min) in `notes/spikes/001-fr07-attachments-payload.md`
- [ ] Decision: Phase E required (YES) or skipped (NO)

**Critical Tasks:**
- `001` — FR-07 spike (MUST BE FIRST; unblocks payload-wiring task 026 and conditional Phase E)

**Inputs**: [`src/server/api/Sprk.Bff.Api/Api/Ai/ChatEndpoints.cs`](../../src/server/api/Sprk.Bff.Api/Api/Ai/ChatEndpoints.cs), OpenAPI schema, BFF Swagger.

**Outputs**: Spike memo with decision + spike code (if any) in `notes/spikes/`.

---

### Phase A: Foundations

**Objectives:**
1. Establish shared primitives that all three pane refactors depend on.
2. Make tab limit and telemetry centrally configurable.

**Deliverables:**
- [ ] `<PaneHeader>` component published from `@spaarke/ui-components` with `title`, `icon`, `rightSlot` props (FR-01)
- [ ] `MAX_WORKSPACE_TABS = 8` constant + FIFO eviction logic in `WorkspaceTabManager.ts` (FR-13)
- [ ] `ActionCard` accessible from `@spaarke/ui-components` (extract if needed; otherwise verified context-agnostic) per ADR-012 (A-3)
- [ ] Error-only telemetry helper module (`logTelemetryError(eventName, properties)`) wired to App Insights (FR-24)

**Critical Tasks:**
- `010` — Create `<PaneHeader>` (blocking for B/C/D)
- `012` — ActionCard lift/verify (blocking for D task 041)

**Inputs**: Spec FR-01, FR-13, FR-24; ADR-012, ADR-021, ADR-025.

**Outputs**: 4 deliverables above; updated `@spaarke/ui-components` exports.

---

### Phase B: Assistant Pane

**Objectives:**
1. Reduce welcome chrome to "How can I help you today?" + recent conversations.
2. Activate input on cold load; lazy session creation.
3. Replace tabs with History overlay; restructure toolbar; enable multi-file attach.

**Deliverables:**
- [ ] WelcomePanel chrome trimmed (no sparkle, no heading, no 4 prompt cards) (FR-04, FR-05)
- [ ] ConversationPane uses `<PaneHeader title="Assistant" icon={ChatRegular}>` (FR-02)
- [ ] HistoryOverlay component + History button + session loading via `setChatSessionId` (FR-03)
- [ ] SprkChatInput editable on cold load; lazy session creation via `useChatSession` (FR-06)
- [ ] `useChatFileAttachment` hook with multi-file picker + lazy PDF.js/mammoth extraction (FR-07 frontend)
- [ ] SprkChat toolbar: prompt menu + `+` strip above input; remove Export Word (FR-08, FR-09)
- [ ] Attachment chips + payload wiring into chat send (FR-07 wiring; depends on spike outcome)

**Critical Tasks:**
- `024` — `useChatFileAttachment` hook (blocks 026)
- `026` — payload wiring (blocked by 001 spike result)

**Inputs**: [`ConversationPane.tsx`](../../src/solutions/SpaarkeAi/src/components/conversation/ConversationPane.tsx), [`WelcomePanel.tsx`](../../src/solutions/SpaarkeAi/src/components/WelcomePanel.tsx), [`SprkChat.tsx`](../../src/client/shared/Spaarke.UI.Components/src/components/SprkChat/SprkChat.tsx), `<PaneHeader>` from Phase A.

**Outputs**: 7 modified or net-new files; ConversationPane simplified; SprkChat toolbar restructured.

---

### Phase C: Workspace Pane

**Objectives:**
1. Embed LegalWorkspace as default "Home" tab via `WorkspaceShell` reuse.
2. Replace tab bar with `WorkspacePaneMenu` (Open / Home / Switch Workspace / Edit).
3. Add Daily Briefing as a new LegalWorkspace section type with 429/empty handling.

**Deliverables:**
- [ ] WorkspacePane uses `<PaneHeader title="Workspace" icon={AppsListRegular}>` + embeds `WorkspaceShell` as Home tab (FR-10, FR-11)
- [ ] `WorkspaceLandingWidget.tsx` deleted
- [ ] `WorkspacePaneMenu` Fluent v9 Dropdown component (Open / Home / Switch / Edit) (FR-12)
- [ ] `WorkspaceLayoutWizard.templateFilter` prop — SpaarkeAi passes 6-template subset; standalone passes nothing (FR-14)
- [ ] Daily Briefing section + `useDailyBriefing` hook + registration (FR-15)
- [ ] Daily Briefing 429 graceful degradation + empty state ("Nothing to see right now — enjoy your day") (FR-16, NFR-11)

**Critical Tasks:**
- `030` — WorkspacePane + LegalWorkspace embed (foundational for C)
- `034` — Daily Briefing section (blocks 035 empty state)

**Inputs**: [`WorkspacePane.tsx`](../../src/solutions/SpaarkeAi/src/components/workspace/WorkspacePane.tsx), [`WorkspaceTabManager.ts`](../../src/solutions/SpaarkeAi/src/components/workspace/WorkspaceTabManager.ts), [`WorkspaceLayoutWizard/src/App.tsx`](../../src/solutions/WorkspaceLayoutWizard/src/App.tsx), [`sectionRegistry.ts`](../../src/solutions/LegalWorkspace/src/sectionRegistry.ts), `<PaneHeader>` + `MAX_WORKSPACE_TABS` from Phase A.

**Outputs**: 6 modified/net-new files; Workspace pane composes LegalWorkspace; new Daily Briefing section available in BOTH standalone LegalWorkspace and SpaarkeAi.

---

### Phase D: Context Pane

**Objectives:**
1. Migrate ContextPaneController to shared `<PaneHeader>`.
2. Replace `PlaybookGalleryWidget` on welcome stage with new `GetStartedCardsWidget` (7 cards).
3. Route card clicks through `PaneEventBus` `workspace` channel → wizards open as new tabs.

**Deliverables:**
- [ ] ContextPaneController uses `<PaneHeader>`; stage label "Gallery" → "Get Started" on welcome (FR-17, FR-22)
- [ ] `GetStartedCardsWidget` — 7 cards in 2-col scrollable grid (FR-18)
- [ ] Widget registered in `ContextWidgetRegistry`; welcome stage swaps to it; `PlaybookGalleryWidget` retained for non-welcome stages (FR-21)
- [ ] Wizard widget wrappers: `CreateProjectWizardWidget`, `FindSimilarWizardWidget` (FR-19)
- [ ] Analysis Builder intents: `email-compose`, `meeting-schedule` registered as wizard widgets (FR-19)
- [ ] `AssignWorkWizardLauncher` — `Xrm.Navigation.navigateTo` thin dispatcher + Vite-dev placeholder (FR-20)

**Critical Tasks:**
- `041` — GetStartedCardsWidget (depends on `ActionCard` from Phase A task 012)
- `042` — registration + stage swap (depends on 041)

**Inputs**: [`ContextPaneController.tsx`](../../src/solutions/SpaarkeAi/src/components/context/ContextPaneController.tsx), [`ContextWidgetRegistry.ts`](../../src/client/shared/Spaarke.AI.Widgets/src/registry/ContextWidgetRegistry.ts), `ActionCard` + `<PaneHeader>` from Phase A.

**Outputs**: 6 modified/net-new files; Context pane welcome state is now the 7-card grid; wizards route to Workspace pane.

---

### Phase E: Backend (Conditional)

**Objectives** (only if Phase 0 spike confirms gap):
1. Extend `POST /api/ai/chat/sessions/{sessionId}/messages` to accept `attachments[]` field.
2. Validate attachment array (max 5, max 10 MB per file, allowed MIME types).
3. Pass through `attachments[]` to LLM call context.

**Deliverables (CONDITIONAL):**
- [ ] `attachments[]` schema in `ChatEndpoints.cs` POST request DTO
- [ ] Validation: max 5 entries, max 10 MB per file, allowed MIME types (`text/plain`, `text/markdown`, `application/pdf`, `application/vnd.openxmlformats-officedocument.wordprocessingml.document`)
- [ ] BFF unit tests for happy path + each validation rejection

**Inputs**: Phase 0 spike memo, [`ChatEndpoints.cs`](../../src/server/api/Sprk.Bff.Api/Api/Ai/ChatEndpoints.cs), ADR-008, ADR-013.

**Outputs**: Modified endpoint contract; new unit tests passing.

---

### Phase F: Integration & Verification

**Objectives:**
1. Verify auth contract across new code.
2. Verify bundle-size budget held.
3. Verify dark-mode token compliance.
4. Verify backwards compatibility with standalone LegalWorkspace.

**Deliverables:**
- [ ] Auth audit: grep for `accessToken` props / state snapshots in new components; all fetches via `authenticatedFetch` (FR-23, NFR-08)
- [ ] Bundle-size verification via webpack-bundle-analyzer: <250 KB gzip delta vs R2 baseline; PDF.js + mammoth absent from initial bundle (NFR-12)
- [ ] Dark-mode token audit: lint rule + manual side-by-side passes; no `#` literals, no `rgba(...)` literals in new code (NFR-06, ADR-021)
- [ ] Backwards-compat verification: standalone LegalWorkspace opened in separate tab → all 9 templates in wizard, existing user layouts render identically (FR-25, NFR-10)

**Inputs**: Built artifacts from Phases B/C/D (+ E if applicable).

**Outputs**: Verification reports in `notes/` for each gate; any failures opened as fix tasks before Phase G.

---

### Phase G: Deploy + Smoke

**Objectives:**
1. Deploy SpaarkeAi production bundle to Dataverse via existing script.
2. Smoke-test each pane in a real Power Apps host environment.
3. Verify NFR-01..NFR-03 performance targets.

**Deliverables:**
- [ ] Deployed via `scripts/Deploy-SpaarkeAi.ps1` (success in deploy log)
- [ ] Assistant pane smoke (FR-02..FR-09 acceptance criteria) via `ui-test` skill
- [ ] Workspace pane smoke (FR-10..FR-16) via `ui-test` skill
- [ ] Context pane smoke (FR-17..FR-22 incl. ADR-021 dark mode check) via `ui-test` skill
- [ ] NFR verification: Lighthouse pane render <500 ms; History overlay open <200 ms; session list populate <300 ms p95 (NFR-01, NFR-03)

**Inputs**: Built artifacts from Phase F; verified deploy script; running BFF.

**Outputs**: Deployed Code Page; smoke reports in `notes/`; sign-off on FR + NFR acceptance.

---

### Phase H: Wrap-up

**Objectives:**
1. Capture lessons learned, update README, archive.

**Deliverables:**
- [ ] `notes/lessons-learned.md` (5–10 lessons covering: PaneHeader lift mechanics, FR-07 attachment design choices, LegalWorkspace embed pattern, ActionCard reuse, layout templateFilter prop pattern, telemetry helper boundary, any unexpected regressions)
- [ ] README status → Complete (with completion date)
- [ ] PR description updated with final shippable status; merge readiness

**Inputs**: All preceding phases.

**Outputs**: lessons-learned.md, completed README, merge-ready PR.

---

## 5. Dependencies

### External Dependencies

| Dependency | Status | Risk | Mitigation |
|------------|--------|------|------------|
| `pdfjs-dist` (PDF text extraction) | Not installed | Medium | Add as new npm dep; lazy-load on first `+` click |
| `mammoth` (DOCX text extraction) | Not installed | Medium | Add as new npm dep; lazy-load; verify size in NFR-12 task |
| `Xrm.Navigation.navigateTo` (Power Apps host) | Available in host | High in Vite dev | Feature-detect; placeholder for non-host envs |
| App Insights connection | Wired in R2 | Low | Reuse existing telemetry channel; add custom event names only |

### Internal Dependencies

| Dependency | Location | Status |
|------------|----------|--------|
| `@spaarke/ui-components` (shared component library) | `src/client/shared/Spaarke.UI.Components/` | Active; will receive new `<PaneHeader>` |
| `@spaarke/auth` (auth v2 function-based) | `src/client/shared/Spaarke.Auth/` | Active; consumed via `authenticatedFetch` |
| `WorkspaceShell`, `useWorkspaceLayouts` | `src/solutions/LegalWorkspace/` | Active; embedded as-is |
| `WorkspaceLayoutWizard` | `src/solutions/WorkspaceLayoutWizard/` | Active; receives optional `templateFilter` prop |
| `PaneEventBus`, `AiSessionProvider`, widget registries | `src/client/shared/Spaarke.AI.Widgets/` | Active; consumed by new widgets |
| BFF endpoints (`/api/ai/chat/sessions`, `/api/ai/daily-briefing/narrate`, `/api/workspace/layouts/*`) | `src/server/api/Sprk.Bff.Api/Api/` | Active in production |
| `CreateMatterWizardWidget`, `DocumentUploadWizardWidget` (existing wizard widgets) | `Spaarke.AI.Widgets` registries | Active; referenced by GetStartedCardsWidget |

---

## 6. Testing Strategy

**Unit Tests** (no formal coverage target; spec-defined):
- New hooks: `useChatFileAttachment` (file extraction, error paths), `useDailyBriefing` (429 handling, empty state)
- `<PaneHeader>` snapshot test (icon + title + rightSlot rendering)
- `WorkspacePaneMenu` interaction tests (close button only on non-Home, FIFO eviction logic)
- `WorkspaceLayoutWizard.templateFilter` filtering correctness (passes through → 6 templates; absent → 9 templates)

**Integration Tests**:
- Phase 0 spike validates backend payload contract via direct API call
- BFF unit tests for `attachments[]` validation (Phase E, conditional)
- Auth `authenticatedFetch` integration verified via end-to-end token refresh test in deployed env

**E2E / UI Smoke Tests** (Phase G via `ui-test` skill):
- Assistant pane cold load → input editable, no chrome elements forbidden by FR-04
- Multi-file attach (5 files, 1 corrupted PDF) → 4 chips + 1 error chip → send → AI references all 4
- Workspace pane Home tab + open 9 widgets → FIFO eviction; tabs persist across page refresh (NFR-09)
- Workspace pane Daily Briefing 429 → graceful card with retry; empty state matches OC-08
- Context pane → click each of 7 cards → correct wizard opens as new tab in Workspace
- Assign Work in Power Apps host → custom page navigates; in Vite dev → placeholder shown
- Dark mode toggle → no color regressions in new components (ADR-021)
- Standalone LegalWorkspace separate tab → 9 templates in wizard, existing layouts unchanged (FR-25)

---

## 7. Acceptance Criteria

### Technical Acceptance

**Phase A:**
- [ ] `<PaneHeader>` exported from `@spaarke/ui-components` index
- [ ] `MAX_WORKSPACE_TABS = 8` constant exported; FIFO eviction logic exempts Home
- [ ] Telemetry helper emits to App Insights with custom event names

**Phase B:**
- [ ] All 8 FR-02..FR-09 manual smoke acceptance criteria pass
- [ ] PDF.js + mammoth absent from initial bundle (verified via bundle-analyzer)

**Phase C:**
- [ ] All 7 FR-10..FR-16 manual smoke acceptance criteria pass
- [ ] WorkspaceLandingWidget deleted (no orphan import warnings)
- [ ] Daily Briefing 429 → graceful card; empty → "Nothing to see right now — enjoy your day"

**Phase D:**
- [ ] All 6 FR-17..FR-22 manual smoke acceptance criteria pass
- [ ] Click each of 7 GetStarted cards → correct wizard opens as new Workspace tab
- [ ] Assign Work feature-detect works in Vite dev (placeholder)

**Phase E (conditional):**
- [ ] BFF endpoint accepts `attachments[]` with max-5 cap, MIME validation, 10 MB cap
- [ ] Unit tests cover happy path + each rejection

**Phase F:**
- [ ] Zero `accessToken` props or state in new components
- [ ] Bundle delta <250 KB gzip vs R2 baseline
- [ ] Zero hex/rgba literals in new code
- [ ] Standalone LegalWorkspace identical to before

**Phase G:**
- [ ] Deployed successfully via `Deploy-SpaarkeAi.ps1`
- [ ] All UI smoke tests pass in Power Apps host
- [ ] NFR-01 / NFR-03 timings measured and pass

### Business Acceptance

- [ ] User can complete the "arrive on welcome state and start any of 7 wizards" flow within 2 clicks
- [ ] Standalone LegalWorkspace users see no regression
- [ ] Telemetry surfaces all 3 error event types in App Insights when manually triggered

---

## 8. Risk Register

| ID | Risk | Probability | Impact | Mitigation |
|----|------|-------------|--------|------------|
| R-1 | Backend chat-message endpoint doesn't accept `attachments[]` — requires extension | Medium | Medium | Phase 0 spike (task 001) determines; Phase E conditionally unlocks |
| R-2 | `Xrm.Navigation.navigateTo` unavailable in Vite dev | High (in dev) | Low | Feature-detect `window.Xrm`; placeholder per A-2, FR-20 |
| R-3 | `ActionCard` not context-agnostic; lift to shared lib needed | Low | Low | Task 012 verifies; refactor if needed before D phase |
| R-4 | Daily Briefing endpoint lacks per-user caching → tab-switch spam | Medium | Low | Verify in UQ-02; add frontend cache in `useDailyBriefing` if needed |
| R-5 | Non-Home tabs not persisted by R2 `SessionPersistenceService` | Low | Medium | Verify in Phase F task 063; opens BFF/Cosmos sub-task if surfaced (per UQ-03) |
| R-6 | PDF.js + mammoth combined bundle exceeds 250 KB gzip | **MATERIALIZED (Option 1 accepted)** | Medium | **Realized 2026-05-20**: post-Wave 3 SpaarkeAi bundle = **798 KB gzip** (vs ~508 KB target = ~290 KB overrun). Source-level lazy-loading is correctly implemented (`await import('pdfjs-dist')` / `await import('mammoth')`) but `vite-plugin-singlefile` (per ADR-026) inlines async chunks into the singlefile HTML, nullifying the bundle benefit. **User accepted Option 1** (ship as-is for R3). Mitigation: Phase G task 074 will empirically validate NFR-01 / NFR-03 timings against the real bundle; if those pass, the overrun is operationally acceptable. Long-term path: Option 2 (separate Dataverse web resources for heavy libs) — deferred to a follow-on project. See [`notes/bundle-size-verification.md`](notes/bundle-size-verification.md) (task 061) + [`docs/assessments/code-page-bundle-size-vs-singlefile-2026-05-20.md`](../../docs/assessments/code-page-bundle-size-vs-singlefile-2026-05-20.md) (cross-project assessment). |
| R-7 | `Xrm.Navigation.navigateTo` `data` query string format wrong for `sprk_createworkassignmentwizard` | Low | Medium | Reference user-provided runtime log; verify in task 045 before going live (UQ-05) |
| R-8 | Bug 2 / ADR-012 cross-cutting limitation — SpaarkeAi Home tab shows structural placeholders only because section infrastructure lives solution-local in LegalWorkspace | **RESOLVED (2026-05-20 / task 067 + 2026-05-21 / task 069)** | Medium | **Architecturally resolved (task 067)**: builder + types hoisted to `@spaarke/ui-components`. **Default-case visual fix (task 069)**: per operator's Option Z direction, SpaarkeAi Home tab now defaults to Daily Briefing as its single section content. Daily Briefing infrastructure (hook + section + registration factory) was hoisted to `@spaarke/ui-components` because its data layer is a pure BFF AI call (no Dataverse entity strings, so ADR-012 permits the hoist). Other 5 legal-domain factories correctly stay solution-local. SpaarkeAi Home tab now shows real Daily Briefing content (bullets / 429 card / empty state) — Bug 2 visual fix landed for the default case. Future multi-section Home expansion (recent AI sessions, etc.) is deferred to next review. See [`notes/067-shared-lib-hoist-summary.md`](notes/067-shared-lib-hoist-summary.md) + [`notes/deploys/2026-05-20-deploy.md`](notes/deploys/2026-05-20-deploy.md) task 069 supplemental section. |
| R-9 | Bug 1 — Assistant pane has no chat input on cold load (welcome/SprkChat ternary mounts one or the other, never both) | **RESOLVED (2026-05-20 / task 068)** | High | **Resolved**: `ConversationPane.tsx` restructured so `SprkChat` is ALWAYS rendered; the welcome heading (now a thin shell in `WelcomePanel.tsx`) sits above it when `showWelcomePanel===true`. FR-06 (input editable on cold load) is functionally true. Deployed to spaarkedev1. See deploy memo "Supplemental Deploy — Task 068". |
| R-10 | UX-A — "Recent Conversations" section duplicates History overlay (two ways to resume past sessions; operator confusing) | **RESOLVED (2026-05-20 / task 068)** | Low | **Resolved**: `RecentConversationsSection` + `useRecentSessions` removed from `WelcomePanel.tsx`. Session resume now exclusively via PaneHeader history icon → `HistoryOverlay` (task 022 pattern). |
| R-11 | UX-B — Create Matter + Summarize Files cards on Get Started inconsistent with other 5 (embedded Workspace tab vs Code Page popup) | **RESOLVED (2026-05-20 / task 068)** | Low | **Resolved**: `ContextPaneController.handleGetStartedCardClick` now routes `create-matter-wizard` → `sprk_creatematterwizard` popup and `document-upload-wizard` → `sprk_summarizefileswizard` popup via Xrm.Navigation (mirrors `launchAssignWorkWizard` pattern). The embedded `CreateMatterWizardWidget` + `DocumentUploadWizardWidget` remain registered in `WorkspaceWidgetRegistry` for non-ContextPane consumers. All 7 cards now use the popup pattern consistently. |
| R-12 | R3-introduced bug — `WorkspacePaneMenu` (task 032) Switch Workspace section silently empty in deployed SpaarkeAi (operator smoke round 2). Module-level `authenticatedFetch` + `getBffBaseUrl()` raced runtime-config bootstrap; `getBffBaseUrl()` threw on first effect run, the catch block swallowed it, and the effect's dep array had no readiness signal to re-fire on, leaving the menu permanently empty. | **RESOLVED (2026-05-21 / task 081)** | Medium | **Resolved**: `WorkspacePaneMenu.tsx` rewired to consume `useAiSession().authenticatedFetch` + `useAiSession().bffBaseUrl` + `useAiSession().isAuthenticated` (matches the R3 pattern used by `WorkspaceHomeTab`, `HistoryOverlay`, `useDailyBriefing`). The internal `useWorkspaceLayoutsList` hook now defers (`return early`) when `!isAuthenticated || !bffBaseUrl`, then auto-runs once those flip truthy. Fetch failures now log to `console.error` (not silently swallowed). `launchWizard` accepts `bffBaseUrl` as a parameter instead of calling module-level `getBffBaseUrl()`. FR-25 preserved — standalone LegalWorkspace untouched (this is a SpaarkeAi-local component). Deployed to spaarkedev1. See deploy memo "Supplemental Deploy — Task 081 (WorkspacePaneMenu fetch fix)". |
| R-13 | SprkChat UX bugs from operator smoke round 2 — (a) cursor focus did not return to chat input after the response stream completed, forcing the operator to click back into the textarea before each follow-up; (b) the strip-mounted Prompt menu (`PromptRegular` button in the toolbar above the input, FR-09 / task 025) had no way to close — clicking outside the floating SlashCommandMenu did nothing, and the menu had no built-in dismiss-on-outside-click handler. | **RESOLVED (2026-05-21 / task 082)** | Low | **Resolved**: (a) `SprkChatInput.tsx` now tracks the previous `disabled` value via a `useRef` and, on the `true → false` transition (i.e., streaming complete), schedules `inputRef.current?.focus()` via `requestAnimationFrame` so layout/aria changes settle before focusing. Local to the input — no new prop wiring needed. (b) `SlashCommandMenu.tsx` now adds the proven document-level `mousedown` outside-click handler (mirrors `SprkChatActionMenu.tsx` lines 356–376) — clicks outside the menu container AND outside `anchorRef` AND outside the strip-mounted Prompt button (`data-testid="strip-prompt-menu-button"`, allow-listed to prevent open/close race with the toolbar button's own onClick) call `onDismiss`. Esc-to-close + Fluent v9 token compliance preserved. Standalone LegalWorkspace untouched. Deployed to spaarkedev1. See deploy memo "Supplemental Deploy — Task 082 (SprkChat UX fixes: focus + menu close)". |
| R-15 | Parallel implementation of workspace layouts fetch — task 081 introduced an inline `useWorkspaceLayoutsList` hook in `WorkspacePaneMenu.tsx` instead of reusing LegalWorkspace's working `useWorkspaceLayouts` hook. Although task 081 fixed the config-not-ready race, the parallel implementation diverged from the working pattern (no cache-first hydration, no sessionStorage layout cache, simpler selection cascade) and the Switch Workspace dropdown remained intermittently empty in deployed SpaarkeAi. Violates operator reuse principle: "when we have working components reuse them." | **RESOLVED (2026-05-21 / task 084)** | Medium | **Resolved**: created `src/solutions/SpaarkeAi/src/hooks/useWorkspaceLayouts.ts` — a faithful SpaarkeAi adaptation of LegalWorkspace's WORKING `useWorkspaceLayouts` hook (cache-first hydration, parallel list+default fetch, pinned-id resolution, setActiveLayoutById, refetch). Three surgical adaptations documented in the hook header: (a) auth from `useAiSession()` not module-level imports (preserves task 081 root-cause fix); (b) drops LayoutJson parsing (menu only needs list + active ID); (c) drops SYSTEM_DEFAULT_LAYOUT fallback (menu degrades to "No workspaces available" empty state instead of fake stub). `WorkspacePaneMenu.tsx` now imports + consumes this hook; the inline `useWorkspaceLayoutsList` (~125 lines) was removed. Option C (Copy) chosen over Option H (Hoist) because the LegalWorkspace hook is coupled to its section registry (`LayoutJson`); clean hoist would risk FR-25 standalone build. JSDoc cross-references the LegalWorkspace source so both stay in sync. Standalone LegalWorkspace untouched (569.22 KB gzip — identical to task 070 baseline). SpaarkeAi bundle 800.07 KB gzip (+2.35 KB vs pre-fix). Deployed to spaarkedev1. See deploy memo "Supplemental Deploy — Round 4 Fix 1". |
| R-14 | BFF brittleness on missing Dataverse data — operator smoke round 2 surfaced two cascading failures in the dev environment: (a) `DataverseCapabilityManifestLoader.LoadAsync` threw `InvalidOperationException` on every `ManifestRefreshService` tick because the `sprk_aicapability` table is not provisioned in dev, which propagated through `DetectToolCallsAsync` → `ChatEndpoints.SendMessage`'s outer catch → SSE error event → frontend "there was an issue" generic error; (b) `DailyBriefingEndpoints.HandleNarrate` returned 400 ProblemDetails on the frontend's standard empty-state probe (`{categories:[],priorityItems:[],channels:[]}` sent by `useDailyBriefing` when no notifications are visible), forcing the hook into its 400-special-case branch and surfacing as a misleading "Bad Request" in App Insights. Both issues blocked end-to-end smoke validation of chat (FR-02..FR-09) and Daily Briefing (FR-15..FR-16) in the fresh dev environment. | **RESOLVED (2026-05-21 / task 083)** | High | **Resolved (BFF in-process only — ADR-013 compliant)**: (a) `DataverseCapabilityManifestLoader.LoadAsync` now detects "missing entity" Dataverse responses (404 NotFound, or 400 with body containing "Resource not found for the segment" / "Could not find a property named" / "does not exist") via a new internal `IsMissingEntityResponse` helper. On match, returns `Array.Empty<CapabilityManifestEntry>()` and logs a `Warning` (not Error) so chat tool detection runs with zero tools available — the rest of the chat flow proceeds normally. Other failure modes (5xx transient, 401, malformed 400) still throw so `ManifestRefreshService` stale-on-error policy retains the existing manifest. Same tolerance applied to `PlaybookService.ExecuteListQueryAsync` (count + select queries both check). (b) `DailyBriefingEndpoints.HandleNarrate` now returns 200 with `{tldr:{briefing:"",topAction:"",...}, channelNarratives:[]}` when all three actionable arrays are empty (instead of 400) — the `useDailyBriefing` 400-special-case branch is preserved for safety (handles other 400 causes) but is now unreachable for the empty-state probe. Unit tests added: `DataverseCapabilityManifestLoaderTests.IsMissingEntityResponse_*` (7 assertions) + `DailyBriefingEndpointsTests.HandleNarrate_Returns_200_Empty_On_Empty_Payload` (2 facts). BFF deployed to spaarkedev1; `/healthz` 200 verified. Standalone LegalWorkspace untouched. ADR-013 (BFF in-process changes only, no new services), ADR-019 (ProblemDetails preserved for real errors), ADR-008 (endpoint filters preserved) all complied with. See deploy memo "Supplemental Deploy — Task 083 (BFF resilience: missing manifest + empty Daily Briefing)". |
| R-16 | Parallel implementation of wizard launchers — operator smoke (2026-05-21) confirmed wizards work fine when launched from LegalWorkspace getStarted cards but DO NOT open when launched from SpaarkeAi's Context-pane GetStartedCards. Root cause: tasks 042/044/045/068 built parallel implementations (`launchCodePagePopup` helper in `ContextPaneController`, `launchAssignWorkWizard` in `@spaarke/ai-widgets`, plus `widget_load → tab-mount → widget calls navigateTo` two-hop path for create-project / find-similar / email-compose / meeting-schedule). The package-local helpers checked `window.Xrm` only (no frame-walking) while the widget-mount path used frame-walking — explaining why "Create Project worked but the other 6 did not." Operator principle: "when we have working components reuse them." | **RESOLVED (2026-05-21 / task 085)** | High | **Resolved (Option H — Hoist)**: created `src/client/shared/Spaarke.UI.Components/src/components/WorkspaceShell/wizardLaunchers.ts` exporting `launchCreateMatterWizard`, `launchCreateProjectWizard`, `launchSummarizeFilesWizard`, `launchFindSimilarWizard`, `launchAssignWorkWizard`, `launchPlaybookIntent`. Each launcher uses the VERBATIM navigateTo call shape from LegalWorkspace's `WorkspaceGrid.tsx` (handleOpenWizard, handleOpenProjectWizard, handleOpenSummarize, handleOpenFindSimilar, handleOpenWorkAssignmentWizard) and `ActionCardHandlers.ts` (openPlaybookIntent) — same `pageType:"webresource"`, same `target:2`, same `60% × 70%`, same `bffBaseUrl=<encoded>` data shape, same titles ("Create New Matter", "Summarize Files", "Find Similar Documents", "Create Work Assignment", "Playbook Library"). Adds **frame-walking** Xrm resolution so the launchers work regardless of iframe nesting depth (the missing piece in the package-local `launchCodePagePopup`). `ContextPaneController.handleGetStartedCardClick` now routes all 7 cards directly through the shared launchers — no more `widget_load → tab-mount` round-trip, no more package-local `launchCodePagePopup`. Embedded workspace widgets (`CreateProjectWizardWidget`, `FindSimilarWizardWidget`, `EmailComposeWidget`, `MeetingScheduleWidget`, `CreateMatterWizardWidget`, `DocumentUploadWizardWidget`) REMAIN REGISTERED in `WorkspaceWidgetRegistry` per operator's "keep embedded structure for future use cases" directive — only the welcome-state card-click routing changes. Deleted: `src/client/shared/Spaarke.AI.Widgets/src/widgets/workspace/AssignWorkWizardLauncher.ts` + its unit test (superseded by the shared launcher). LegalWorkspace's local handlers in `WorkspaceGrid.tsx` + `ActionCardHandlers.ts` untouched for FR-25 byte-stability (bundle 569.22 KB gzip — identical to baseline). SpaarkeAi bundle 800.56 KB gzip (slightly smaller post-fix). Deployed to spaarkedev1. See deploy memo "Supplemental Deploy — Round 4 Fix 2 (Wizards via reused LegalWorkspace handlers)". |
| R-17 | Daily Briefing auto-load empty-payload contract gap — operator smoke (2026-05-21) confirmed Daily Briefing renders correctly in the standalone Daily Briefing Code Page (`sprk_dailybriefing`) but renders the "Nothing to see right now — enjoy your day" empty state in the SpaarkeAi Workspace Home tab on cold load, even when the user has real unread `appnotification` records. Root cause: task 069's `WorkspaceHomeTab` auto-fired the shared `useDailyBriefing` hook with `buildEmptyNarrateRequest()` (empty categories / priorityItems / channels) because the shared hook had no programmatic source for notification context. Task 083 made the BFF return 200/empty bullets on the empty envelope (instead of 400) — which prevented the misleading "Bad Request" in App Insights but ALSO meant SpaarkeAi never received real bullets on its standard auto-load path. The standalone Daily Briefing Code Page works because it queries `appnotification` via Xrm.WebApi on mount and builds a populated payload (`useNotificationData` → `briefingService.buildNarrationRequest`); SpaarkeAi had no equivalent data path. Operator principle: "all of this needs to use all of our existing auth, bff.api connectors, etc." + "REUSE the working code from the standalone Daily Briefing." | **RESOLVED (2026-05-21 / task 086)** | High | **Resolved (Hoist optional callback + Copy standalone data path)**: (1) shared `useDailyBriefing` hook now accepts an optional `loadNotificationContext: () => Promise<NarrateRequest \| null>` parameter; when supplied, the hook invokes it before POST-ing to narrate and forwards the populated payload verbatim (when omitted, the legacy empty-payload contract is preserved for LegalWorkspace's shim — preserves FR-25). Symmetric prop added to `DailyBriefingSection` + `createDailyBriefingRegistration` factory. `NarrateRequest` + 4 sub-types exported from the shared lib barrel for consumer typing. (2) Created `src/solutions/SpaarkeAi/src/services/notificationContextLoader.ts` — a faithful SpaarkeAi-local copy of the standalone Daily Briefing Code Page's working data path (Xrm frame-walk, `appnotification` query via Xrm.WebApi with same `$select` columns + `$orderby createdon desc` + `$top=200`, same `customData` JSON parsing, same `groupByCategory` logic, same `buildNarrationRequest` payload assembly). Option C (Copy) chosen over Option H (Hoist) of the loader logic because the standalone's `useNotificationData` tangles together notifications + preferences + mark-as-read + refresh actions that SpaarkeAi's Home tab doesn't need; clean hoist would require splitting that hook apart (out of scope for a fix task). The copy carries JSDoc cross-references to the source files for sync. ADR-012 satisfied: Dataverse entity strings (`appnotification`, `customData`, `toasttype`) all live in the solution-local loader; the shared lib's new parameter is a generic callback. (3) `WorkspaceHomeTab.tsx` now passes `loadNotificationContext: loadSpaarkeAiNotificationContext` to the registration factory — same Xrm.WebApi query the standalone uses, same populated payload to the BFF, same real AI bullets in the response. Edge cases handled: Xrm unavailable (dev/Vite) → loader returns null → hook falls back to empty payload → empty-state UI (no error); zero notifications → loader returns null → empty-state UI (correct); loader throws → hook catches + warns + falls back to empty payload (no crash). LegalWorkspace `dailyBriefing.registration.ts` shim untouched — runtime behavior preserved (FR-25 / NFR-10); only +140 bytes of optional-param forwarding code path that's `undefined` at runtime for LegalWorkspace (569.29 KB gzip vs 569.22 KB baseline). Standalone Daily Briefing Code Page untouched. SpaarkeAi bundle 801.85 KB gzip (+1.78 KB vs task 084 baseline). ADR-013 (no new BFF service), ADR-014 (5-min TTL cache preserved), ADR-016 (429 graceful), ADR-028 (auth contract preserved) all complied with. Deployed to spaarkedev1. See deploy memo "Supplemental Deploy — Round 4 Fix 3 (Daily Briefing functional in SpaarkeAi)". |
| R-19 | Embed wiring gaps (initialWorkspaceId / displayName / runtime config) — operator smoke after task 087 (2026-05-21, round 4) surfaced three bugs in the freshly embedded LegalWorkspaceApp path. **Bug 1**: each `Switch Workspace` selection opened a new tab with the same "last-active" workspace because LegalWorkspace's `useWorkspaceLayouts` cache-hydrated from a sessionStorage key shared across all tabs — `initialWorkspaceId` was effectively ignored on cache hit. **Bug 2**: every workspace tab title rendered as "Workspace" (the registry metadata's generic label) instead of the chosen layout name — `WorkspacePaneMenu.handleLayoutSelect` placed the friendly name in `widgetData.layoutName` only, not at the top level of the event, so `WorkspacePane.tsx`'s displayName-precedence ladder fell through to registry metadata. **Bug 3**: clicking "preview" on a document inside the embedded tree threw "[LegalWorkspace] Runtime config not initialized. Call initRuntimeConfig() in main.tsx before using getBffBaseUrl()." — LegalWorkspace has its own `runtimeConfig` singleton (separate from SpaarkeAi's) and SpaarkeAi's `main.tsx` only initialized its own. | **RESOLVED (2026-05-21 / task 088, Round 4 Fix 4.1)** | High | **Resolved (three surgical fixes)**: **Bug 1**: added new `embedded?: boolean` option to `useWorkspaceLayouts` via a backwards-compatible overload (legacy `useWorkspaceLayouts(initialWorkspaceId)` still works; new `useWorkspaceLayouts({ initialWorkspaceId, embedded })` is the embedded call shape). When `embedded=true`, the hook SKIPS `sessionStorage` reads AND writes — both the cache-hydration block at the start of the fetch effect and the cache-write at the end of the BFF resolution path; `setActiveLayoutById` likewise stops mirroring to cache. Wired through `WorkspaceGrid` (`embedded` forwarded as a prop) and `LegalWorkspaceApp` (passes `embedded` to `WorkspaceGrid`). **Bug 2**: `WorkspacePaneMenu.handleLayoutSelect` now dispatches `widget_load` with `displayName: layoutName` at the TOP LEVEL of the event (not just inside `widgetData`); `WorkspacePane.tsx`'s existing displayName-precedence ladder (event.displayName → registry → widgetType) picks it up correctly. **Bug 3** (Option A, dual-init): re-exported `setRuntimeConfig as setLegalWorkspaceRuntimeConfig` from LegalWorkspace's barrel (`src/solutions/LegalWorkspace/src/index.ts`); SpaarkeAi's `main.tsx` calls it with the SAME `IRuntimeConfig` immediately after its own `setRuntimeConfig(config)`. Both singletons remain distinct in-process instances holding equivalent values for `bffBaseUrl` / `scope` / `clientId` / `tenantId`. ADR-028 / FR-25 / NFR-10 all preserved: standalone LegalWorkspace runtime entry (`main.tsx → App.tsx`) still calls its own internal `setRuntimeConfig` and never imports the barrel; the embedded path on its own does not change standalone code paths. LegalWorkspace bundle 569.30 KB gzip (+80 bytes structural; standalone never executes the new conditional branches). SpaarkeAi bundle 893.75 KB gzip (−0.16 KB vs task 087 baseline). Deployed to spaarkedev1. See deploy memo "Supplemental Deploy — Task 088 (embed wiring fixes 4.1)". |
| R-20 | WorkspacePaneMenu UX clutter — the dropdown's `Open` (per-tab list with close affordance) and `Home` sections duplicated UI already visible above the menu (the open tab bar) and reified a "Home" concept that had been retired (Daily Briefing is now just one widget, not a special home tab). Operator feedback (2026-05-21): "we don't need the Open and Home areas/links — these don't add UI value". | **RESOLVED (2026-05-21 / task 089)** | Low | **Resolved**: `WorkspacePaneMenu.tsx` cleaned up — removed the Open + Home sections (along with their derived memos `homeTab` / `openTabs`, the `handleTabActivate` / `handleTabCloseClick` callbacks, the `tabRow` / `closeButton` style entries, and the `DismissRegular` / `HomeRegular` icon imports). Renamed the `Switch Workspace` MenuGroupHeader to `Select Workspace` per operator wording. Added a new `Manage workspaces` menu entry (stub for task 093 — onClick calls `console.log('Manage workspaces — task 093 will implement side pane')`; uses `SettingsRegular` icon). `Edit current workspace` kept (operator did not request removal; remains a meaningful action on the active layout). New visual hierarchy: `Select Workspace` (list of layouts) → `MenuDivider` → `+ New Workspace` / `Manage workspaces` / `Edit current workspace`. `WorkspacePaneMenuProps` left structurally identical — `tabs` / `activeTabId` / `onTabSelect` / `onTabClose` retained (with the latter two marked optional) to keep the `WorkspacePane.tsx` call site untouched. ADR-021 compliant (Fluent v9 tokens only — no new hex/rgba). FR-25 preserved (standalone LegalWorkspace untouched). SpaarkeAi bundle 893.43 KB gzip (−0.32 KB vs task 088 baseline). Deployed to spaarkedev1. See deploy memo "Supplemental Deploy — Task 089 (dropdown cleanup)". |
| R-21 | Workspace builder constraints from operator smoke (round 5, 2026-05-21) — (a) the Arrange Sections step auto-fills every layout slot on first entry and the wizard appears to require all slots to remain filled to save; (b) once a section is placed in a slot there is no affordance to remove it (drag-out to the unassigned pool works in theory but is undiscoverable, and operator's reported behavior was that sections couldn't be removed at all); (c) creating a workspace from the builder offers no way to mark it as "pin to start" so that SpaarkeAi auto-opens it on next cold load — the auto-open behavior itself is deferred to task 092 but the UI flag must be present in task 091 so the operator can set it before task 092 ships. | **RESOLVED (2026-05-21 / task 091)** | Medium | **Resolved (three surgical fixes in WorkspaceLayoutWizard + a one-line guard in the shared renderer)**: (a) confirmed that Step 3's `canAdvance` only required `workspaceName.trim().length > 0` — no all-slots-required gate ever existed, but empty slots produced `console.warn("Unknown section ID: , skipping")` from `buildDynamicWorkspaceConfig` because the wizard serializes empty slots as `""` in `sectionsJson`. Added a guard: `if (!sectionId) { continue; }` BEFORE the registration lookup so empty slots are silently skipped (Fix 1). (b) `GridSlot` now renders a small `DismissRegular` X button in its top-right corner when the slot is filled — visible on hover and on focus (a11y-friendly via `:focus` opacity rule); clicking removes the section from the slot (`onRemove` callback in `ArrangeStep`), the section returns to the unassigned pool automatically via the `unassignedSections` derived state; mousedown `stopPropagation` + `draggable=false` prevent the button from initiating an HTML5 drag (Fix 2). (c) added `pinToStart` boolean state to `App.tsx` + `pinToStart` / `onPinToStartChange` props on `ArrangeStep` + a new Fluent v9 `Checkbox` with `PinRegular` icon and label `"Pin to Start"` in Step 3's inline row next to `Set default`. Persistence is a sessionStorage stub keyed `spaarke:workspace:pinned-layout-id` (only one workspace pinned at a time — matches the eventual BFF single-pinned semantics). The save handler now persists pinToStart on success and the `__dialogResult` carries the flag back to the caller. The BFF DTO is NOT extended in this task — task 092 will add `IsPinned` + `sprk_workspacelayout.sprk_ispinned` and replace the sessionStorage stub with BFF-backed persistence; this is documented as a TODO in `App.tsx` (Fix 3). FR-25 preserved: standalone LegalWorkspace's `WorkspaceGrid.handleOpenWizard` passes neither `templateFilter` nor `pinToStart` — both default gracefully (`templateFilter` undefined → all templates shown per task 033; `pinToStart` initial state `false` because `mode==='create'` → no layoutId yet → `readPinToStartForLayout(null)` returns false). LegalWorkspace bundle 569.31 KB gzip (+10 bytes vs 569.30 task 088 baseline — the empty-slot guard's two lines); SpaarkeAi 893.44 KB gzip (+10 bytes vs 893.43 task 089 baseline); wizard 427.58 KB gzip. Deployed to spaarkedev1 — `sprk_workspacelayoutwizard` + `sprk_corporateworkspace` + `sprk_spaarkeai` all updated. ADR-021 (Fluent v9 tokens only — no hex/rgba; uses `tokens.colorNeutralBackground3`, `tokens.colorStrokeFocus2`, `tokens.colorNeutralBackground3Hover`), ADR-028 (auth contract preserved — pinToStart never traverses the BFF in this task), ADR-012 (the `buildDynamicWorkspaceConfig` guard lives in the shared lib where the function already lives — no factory hoist needed) all complied with. See deploy memo "Supplemental Deploy — Task 091 (workspace builder improvements)". |
| R-22 | Tab pinning + auto-open missing — task 091 stubbed pin persistence in `sessionStorage` with a single pin slot, but the operator's intent is multi-pin + auto-open on SpaarkeAi cold load (pinned workspaces should appear as tabs alongside the Home tab whenever the user opens SpaarkeAi), and sessionStorage clears on browser close. Without auto-open, the "Pin to Start" checkbox in the wizard had no visible effect after closing the browser; without multi-pin, only one workspace could ever be pinned at a time. | **RESOLVED (2026-05-21 / task 092)** | Medium | **Resolved (multi-pin localStorage + auto-open effect + per-tab pin icon)**: (a) created `src/solutions/SpaarkeAi/src/services/pinnedWorkspaces.ts` with `getPinnedWorkspaces` / `isPinned` / `pinWorkspace` / `unpinWorkspace` operating over `localStorage` key `spaarke:workspace:pinned-list` (array of `{ layoutId, layoutName }`); all accessors are try/catch-wrapped for private browsing / quota safety. (b) migrated `WorkspaceLayoutWizard/src/App.tsx` from the task 091 single-pin sessionStorage stub to the same multi-pin localStorage shape — wizard and SpaarkeAi share the on-disk schema verbatim (the wizard is a separate Vite app so it cannot import from SpaarkeAi at build time; the shape is replicated with cross-file JSDoc warnings to keep them in sync). (c) added an auto-open mount effect in `WorkspacePane.tsx` — reads the pinned list AFTER `isAuthenticated` is true, filters out any pins whose `layoutId` is already represented in the current tab list (duplicate guard against task 065 tab-restore overlap), and dispatches `widget_load` events with `widgetType: "workspace"`, `widgetData: { layoutId, layoutName }`, `displayName: layoutName` — the same shape `WorkspacePaneMenu.handleLayoutSelect` uses, so the existing pipeline (tab manager → `resolveWorkspaceWidget` → `WorkspaceLayoutWidget`) hydrates each pin without new code paths. Home tab still installs as default; pinned tabs open IN ADDITION (operator decision — user can close Home if they don't want it). A `useRef` guard makes the effect one-shot per mount so `isAuthenticated` toggling on token refresh doesn't re-stack tabs. (d) added a pin icon (`PinRegular` / `PinFilled` from `@fluentui/react-icons`) to `WorkspaceTabManagerComponent.tsx` next to the existing close button — restricted to tabs where `kind === "widget"` AND `widgetType === "workspace"` AND `widgetData.layoutId` is present, so the Home tab and non-workspace widget tabs (Create Matter, Find Similar, etc.) do NOT show the pin affordance. Click toggles localStorage + a local `pinnedIds: Set<string>` React state for synchronous icon flip; pinned state uses `tokens.colorBrandForeground1` for visual prominence; tooltips read "Pin {name} to start" / "Unpin {name} from start"; `aria-pressed` reflects state for screen readers. BFF DTO NOT modified — no user-preferences endpoint exists in `Sprk.Bff.Api` today (the existing `UserPreferences` records in `AiIntentClassificationSchema.cs` / `PlaybookRunContext.cs` are AI-pipeline payloads, not a generic preferences surface). Documented TODO for BFF migration when cross-device sync becomes a requirement. FR-25 preserved — LegalWorkspace untouched (the embedded `LegalWorkspaceApp` only renders inside `WorkspaceLayoutWidget` instances that SpaarkeAi opens; standalone LegalWorkspace doesn't pin anything). SpaarkeAi bundle 894.21 KB gzip (+0.77 KB vs task 091 baseline 893.44); wizard 427.76 KB gzip (+0.18 KB vs 427.58). LegalWorkspace bundle byte-identical to task 091 (no code path touched). ADR-021 (Fluent v9 tokens only — no hex/rgba; `colorBrandForeground1`, `colorNeutralForeground3`, `colorNeutralBackground3Hover`), ADR-022 (React 19 functional), ADR-028 (no auth surface changes — localStorage is client-side state only). Deployed to spaarkedev1 — `sprk_spaarkeai` + `sprk_workspacelayoutwizard` (plus the 7 other wizard pages refreshed by the deploy script). Operator-verifiable: pin a workspace → close browser → reopen SpaarkeAi → pinned workspace auto-opens as a tab. See deploy memo "Supplemental Deploy — Task 092 (tab pinning + auto-open)". |
| R-23 | Pane collapse missing — operator feedback (Round 5, 2026-05-22): the SpaarkeAi three-pane shell had no operator-facing way to collapse a pane to a narrow strip. The existing `ThreePaneLayout` already supported left/right collapse internally (via sessionStorage `*-visibility` keys), but the toggle UI was buried — only the collapsed strip itself was clickable, so there was no way to GET TO the collapsed state without code. Operator wanted SmartToDo's Kanban-style click-the-header-to-collapse pattern for all three panes (Assistant + Workspace + Context), with persistence across browser sessions. | **RESOLVED (2026-05-22 / task 094)** | Low | **Resolved (header-click collapse + 3rd-pane collapse + localStorage persistence)**: extended shared `PaneHeader` with optional `onCollapse` + `expanded` props that make the header act as a click/keyboard collapse trigger (with `role="button"`, `aria-expanded`, Enter/Space handlers, and a `stopPropagation` guard on the rightSlot wrapper so interactive children — History icon, WorkspacePaneMenu — don't bubble clicks up to the header). Extended `ThreePaneLayout` with externally-controlled collapse state for all three panes (added `centerCollapsed` + matching toggle for the center pane that the base layout didn't previously support; collapsed center renders as a 36px strip with the same rotated-label treatment as left/right). New SpaarkeAi-local `usePaneCollapse` hook owns the Set-based collapse state + localStorage persistence (`spaarke:panes:collapsed` JSON array) with try/catch-wrapped accessors per task 092's posture. New `PaneCollapseContext` exposes the toggle handle to each pane so they can wire `onCollapse` on their own `<PaneHeader>` without prop drilling. ConversationPane / WorkspacePane / ContextPaneController each now declare `onCollapse={() => toggle('<paneId>')}` and `expanded={!isCollapsed('<paneId>')}`. Reuse: visual pattern lifted verbatim from `SmartToDo.tsx` (Set-based `collapsedColumns` state) + `KanbanBoard.tsx` (columnCollapsed strip with `writingMode: 'vertical-rl'` + `transform: rotate(180deg)`). ADR-021 (Fluent v9 tokens — no hex/rgba), ADR-022 (React 19), ADR-028 (no auth surface change) all complied with. FR-25 preserved (LegalWorkspace standalone behaviorally unchanged; +40 bytes of unused conditional branch code only). Deployed to spaarkedev1. See deploy memo "Supplemental Deploy — Task 094 (pane collapse/expand)". |
| R-24 | Context pane blank after modal close + missing Semantic Search Criteria surface — operator feedback (Round 5, 2026-05-22): (a) clicking any GetStartedCards wizard card (e.g. Create Project, Find Similar, Assign Work) launched the wizard popup correctly, but when the operator closed the popup the Context pane went blank instead of returning to the GetStartedCards grid; root cause was the welcome-stage check in `renderContent()` short-circuiting on `currentStage === "welcome"` while ShellStage had already advanced (or never matched) so the fall-through hit `renderStageDefaultContent()` empty state; (b) Semantic Search Criteria had no surface in the SpaarkeAi shell — the full SemanticSearch Code Page was reachable only from outside the shell (sitemap / command bar), with no in-pane criteria editor to seed a search from inside SpaarkeAi. | **RESOLVED (2026-05-22 / task 095)** | Low | **Resolved (Context tool selector + Semantic Search Criteria + persist through modal close)**: added a Fluent v9 `<Menu>` dropdown in the Context pane's PaneHeader rightSlot (mirroring `WorkspacePaneMenu`'s pattern verbatim — `MenuTrigger` Button + `MenuPopover` + `MenuList` + `MenuGroupHeader` + MenuItems with active-checkmark) exposing two tools: **Quick Start** (default — existing `GetStartedCardsWidget`) and **Semantic Search** (new `SemanticSearchCriteriaTool` with in-pane Domain `<Dropdown>` + AI query `<Textarea>` + optional from/to date inputs + primary Search button). Search button launches `sprk_semanticsearch` via `Xrm.Navigation.navigateTo` (frame-walk Xrm resolver from `wizardLaunchers.ts`) with `query` + `domain` + `dateFrom` + `dateTo` URL params, 80% × 80% modal. Tool selection persisted in `localStorage["spaarke:context:selected-tool"]` (same try/catch-wrapped posture as task 094's `spaarke:panes:collapsed` — readPersistedTool validates against `VALID_CONTEXT_TOOL_IDS`); criteria persisted separately in `localStorage["spaarke:context:semantic-search-criteria"]`. The persisted `selectedTool` is the SOURCE OF TRUTH in `renderContent()` (overrides ShellStage when `semantic-search`; defaults to GetStartedCardsWidget on welcome stage AND on non-welcome stages with no server-driven `activeWidget`) — same fix benefits BOTH the new Semantic Search modal AND the existing playbook-modal wizard flow: any modal close finds the pane re-renders with the chosen tool still selected. Server-driven `context_update` events still resolve widgets when `activeWidget !== null` (Stage 3 Findings / Stage 4 Related Items flows preserved). FR-25 preserved — no LegalWorkspace files touched, LegalWorkspace bundle 569.35 KB gzip = byte-identical to task 094 baseline. SemanticSearch Code Page untouched. SpaarkeAi 896.59 KB gzip (+1.55 KB vs task 094). ADR-012 (SpaarkeAi-local placement correct), ADR-021 (Fluent v9 tokens — `colorBrandForeground1`, `colorNeutralForeground1/2/3`, `colorNeutralBackground1`, `colorNeutralStroke2`, `spacingHorizontalS/M`, `spacingVerticalXXS/XS/S/M`, `borderRadiusMedium`; no hex / rgba), ADR-022 (React 19 functional), ADR-025 (icons from `@fluentui/react-icons` v9: `ChevronDownRegular` / `AppsListRegular` / `SearchRegular` / `CheckmarkRegular`), ADR-028 (no auth surface change — criteria are client-side until modal hand-off; `sprk_semanticsearch` has its own MSAL bootstrap). Deployed to spaarkedev1. See deploy memo "Supplemental Deploy — Task 095 (Context tool selector + Semantic Search Criteria)". |
| R-25 | Collapsed pane strip too narrow + rotated-text identifier inconsistent with MDA; pane titles too small for header polish — operator feedback (Round 6, 2026-05-22) flagged three foundational shared-lib gaps that gated the next wave of pane revisions: **(a)** the SpaarkeAi three-pane shell's collapsed strips (left/right at 28px from the original ThreePaneLayout, center at 36px added in task 094) were too narrow to read or click reliably; the operator wanted the collapsed width to mirror Model-Driven Apps' left-nav collapsed pane (~48px) so the strip feels like a known affordance from the rest of the Power Platform UX; **(b)** the collapsed strip's identifier was vertical rotated text (`writingMode: 'vertical-rl'` + `transform: rotate(180deg)`) per the SmartToDo Kanban pattern task 094 lifted; operator wanted the **icon ONLY** as the identifier in collapse mode — no words, no rotation — for a cleaner visual treatment that scales better at small strip widths and matches how MDA's left-nav, Outlook's reading-pane, and similar Microsoft surfaces handle their collapsed states; **(c)** across all three panes the `<PaneHeader>` title font (`<Text size={300}>` = `tokens.fontSizeBase300` ≈ 14px) felt visually weak — the title is the strongest semantic element in each pane's header band but it was not the visually strongest element, and the operator wanted "+2px equivalent" to fix the hierarchy. These three issues are FOUNDATIONAL to a Wave 2 of pane-specific revisions (Assistant history dropdown, Workspace tab/dropdown polish, Context tool dropdown polish + pin) — without these three corrections in place the Wave 2 work would either replay the same operator feedback or land on an inconsistent foundation. | **RESOLVED (2026-05-22 / task 096)** | Low | **Resolved (three surgical shared-lib changes)**: (a) added a top-of-file `COLLAPSED_STRIP_PX = 48` constant in `ThreePaneLayout.tsx` and applied it to `leftPaneCollapsed` / `rightPaneCollapsed` / `centerPaneCollapsed` flex-basis + minWidth — previous values 28/28/36px → uniform 48px. Fluent v9 has no token that matches the MDA left-nav width so the literal is documented as a deliberate layout-dimension exception (ADR-021 compliant — colors / spacing / borders elsewhere remain token-based). (b) added three optional props to `ThreePaneLayoutProps`: `leftCollapsedIcon?: React.ReactElement` / `centerCollapsedIcon?: React.ReactElement` / `rightCollapsedIcon?: React.ReactElement`; the corresponding collapsed-strip JSX now branches — when an icon prop is provided, the strip renders the icon centered inside a new `styles.collapsedIcon` container (`tokens.fontSizeBase400`, `tokens.colorNeutralForeground2` → `colorNeutralForeground1` on hover) and SUPPRESSES the legacy `<ChevronXXX>` + rotated `<span className={styles.collapsedLabel}>` rendering; the original `*PaneCollapseLabel` text is retained as the strip's accessible name via `aria-label` on the clickable strip so screen readers still announce the pane name. **Backwards compatibility is deliberate**: when no icon prop is provided, the legacy rendering still fires — so this task's commit does NOT visually change any caller until a follow-up wires icons per pane. Standalone LegalWorkspace doesn't import `ThreePaneLayout` and so is wholly unaffected. (c) bumped the `<Text>` size prop in `PaneHeader.tsx` from `size={300}` → `size={400}` (one Fluent v9 step ≈ +2px / `tokens.fontSizeBase300` → `tokens.fontSizeBase400`); semantic token-based change with no hex / px literals; applies wherever PaneHeader is rendered (SpaarkeAi's three panes + any LegalWorkspace surface that uses PaneHeader). **DELIBERATELY DEFERRED**: this task does NOT modify any SpaarkeAi pane component (`ConversationPane.tsx` / `WorkspacePane.tsx` / `ContextPaneController.tsx` / `ThreePaneShell.tsx`) — Wave 2 (tasks 097-099, to be created) will wire `leftCollapsedIcon={<ChatRegular />}` / `centerCollapsedIcon={<AppsListRegular />}` / `rightCollapsedIcon={<DocumentRegular />}` per pane and apply pane-specific operator feedback in parallel. Until Wave 2 lands, the operator sees the widened 48px strip + larger title font immediately, and the icon-only behavior arrives once Wave 2 wires the props. SpaarkeAi bundle 896.67 KB gzip (+80 bytes vs task 095 baseline 896.59 — within the near-zero envelope expected for a prop-addition + token-reference change); LegalWorkspace 569.38 KB gzip (+30 bytes vs task 095 baseline 569.35 — entirely from the PaneHeader `size={400}` token-reference; well within the +0.5 KB tolerance, FR-25 / NFR-10 preserved behaviorally). ADR-012 (shared lib placement — all three changes live in `@spaarke/ui-components`), ADR-021 (Fluent v9 tokens only — title font moves between `fontSizeBase300` → `fontSizeBase400`; collapsed-icon container uses `colorNeutralForeground1/2` + `fontSizeBase400`; the 48px constant is a documented layout-dimension literal with no matching token), ADR-022 (React 19 functional — no new hooks, no class components) all complied with. Deployed to spaarkedev1 (`sprk_spaarkeai` resource `5206a442-3451-f111-bec7-7ced8d1dc988`). See deploy memo "Supplemental Deploy — Task 096 (shared pane foundation polish)". |
| R-26 | Assistant pane History uses inconsistent UX pattern (MDA side pane / icon-only trigger) compared to Workspace + Context dropdowns — operator feedback (Round 6, 2026-05-22) flagged that the Assistant pane's History affordance was an icon-only `<Button>` with `<HistoryRegular />` (no text label) that opened a slide-in `<OverlayDrawer>` from the trailing edge (task 022). This combination — icon-only trigger + slide-in surface + no label — felt like the Model-Driven Apps "far-right side pane" pattern, which is INCONSISTENT with the Workspace pane ("Workspace ▾" Fluent v9 Menu, task 089) and Context pane ("Tools ▾" Fluent v9 Menu, task 095) PaneHeader rightSlot dropdowns. Operator's exact wording: "the current history uses the model-driven app far-right side pane pattern; this is probably because this is pulling a Dataverse table; but this pattern is different than for the Workspace pane or Context pane — unusual pattern for the user. Does it have to be this way? if not then make it a dropdown like the Workspace pane and Context pane dropdown. Replace the 'History' icon with words 'History' and dropdown arrow so looks consistent to Workspace pane and Context pane." Secondary item: the collapsed Assistant strip showed rotated-text "Assistant" instead of the chat icon (task 096 had added the `leftCollapsedIcon` API in `ThreePaneLayout` but `ThreePaneShell.tsx` hadn't wired it yet). | **RESOLVED (2026-05-22 / task 097, Wave 2 commit 1 of 3)** | Low | **Resolved (Path A — full UX unification)**: investigation confirmed the operator's MDA-pattern hypothesis was incorrect — the prior `HistoryOverlay` (task 022) was a Fluent v9 `<OverlayDrawer>` (frontend slide-in surface) backed by `GET /api/ai/chat/sessions?limit=50` via `authenticatedFetch`, NOT a Dataverse-table-driven `Xrm.Navigation.navigateTo` side pane. The MDA-look came from the trigger style + surface style combined; converting to a dropdown required NO data-source plumbing — only the trigger surface + the list surface change. **Path A** chosen (replace BOTH trigger AND surface with `<Menu>` + `<MenuPopover>`; the 50-item ceiling fits inside a scrollable popover with `max-height: 360px` + `overflow-y: auto` — same ergonomics as Workspace and Context dropdowns). **Files modified**: (1) `HistoryOverlay.tsx` wholly rewritten as `HistoryMenu` — subtle `<Button>` with text "History" + `<ChevronDownRegular />` icon `iconPosition="after"` (mirrors WorkspacePaneMenu lines 492–505 + ContextPaneMenu lines 141–155 verbatim); `<MenuPopover>` with scrollable session list as `<MenuItem>` rows (two-line layout: title semibold + relative timestamp meta); loading / empty / error states inline inside the popover; BFF fetch path + telemetry constant unchanged; legacy `HistoryOverlay` export retained as alias for grep-safe migration. (2) `ConversationPane.tsx` simplified — `historyOpen` state + handlers removed (Menu manages its own state); `HistoryRegular` import removed; PaneHeader rightSlot now renders `<HistoryMenu>` directly; sibling `<HistoryOverlay>` JSX removed (Menu uses portal anchoring). (3) `ThreePaneShell.tsx` adds `leftCollapsedIcon={<ChatRegular />}` (consumes task 096's API) so the collapsed Assistant strip shows the chat icon instead of rotated text — same icon the Assistant PaneHeader already shows when expanded. **Bundle delta**: SpaarkeAi 892.62 KB gzip (vs 896.67 KB task 096 baseline = **−4.05 KB / −0.45%**) — net reduction from removing `<OverlayDrawer>` + `<DrawerHeader>` + `<DrawerBody>` surface code in favor of inline `<Menu>` reuse (Menu primitives were already imported by Workspace + Context menus so zero-additional-cost here). LegalWorkspace not rebuilt — no files touched, FR-25 / NFR-10 preserved behaviorally. ADR-012 (HistoryMenu is solution-local, mirrors Workspace + Context menu placement), ADR-021 (Fluent v9 tokens only — no hex/rgba), ADR-022 (React 19 functional), ADR-028 (auth contract preserved — `authenticatedFetch` still a prop, no token snapshots). NOT deployed in this task — Wave 2 commit 1 of 3; deploy is coordinated by main session after tasks 098 (Workspace) + 099 (Context) commits land. Follow-up flagged (not fixed): telemetry constant name `TELEMETRY_HISTORY_OVERLAY_LOAD_FAILURE` still references the obsolete "overlay" surface — rename to `TELEMETRY_HISTORY_LOAD_FAILURE` is a cosmetic candidate but would touch the telemetry test file and coordinate with App Insights consumer queries; deliberately deferred to keep Wave 2 cross-task churn minimal. See deploy memo "Task 097 — Assistant pane: History as dropdown". |
| R-27 | Workspace pane tab + dropdown UX inconsistencies (tab pin icon clutter, dropdown label, missing pin toggle, Edit current cluttering selection menu) — operator feedback (Round 6, 2026-05-22) flagged four issues in the Workspace pane block of the Wave-2 operator smoke: **(a)** every open workspace tab carried a pin icon button to the left of the close × (added in task 092 so users could pin a workspace from the tab row); operator: "in the tabs, remove the 'pin' icon from the tab title" — pin belongs in the workspace SELECTION surface (the dropdown), not on every open tab where it clutters the row and competes visually with the close button; **(b)** the tab title font (`tokens.fontSizeBase200`) was a step smaller than the bumped pane title (Wave-1 task 096 → `tokens.fontSizeBase400`) so the tab labels read as unimportant compared to the pane title above them — operator: "in the tabs, increase the size of the tab title"; **(c)** the close × icon used `DismissRegular` (20px) which competed visually with the tab title — operator: "in the tabs, reduce the size of the close 'x' icon" — the × is a secondary affordance on a tab the user might want to close, not the row's primary content; **(d)** the Workspace dropdown trigger button text "Workspace" (singular) is inaccurate (the dropdown lists ALL available workspaces, not just the current one) and the dropdown had NO pin-toggle affordance whatsoever — pin state was only changeable from the per-tab icon being removed in (a); operator: "in the Workspace drop down, change the label of the drop down from 'Workspace' to 'Workspaces'. In the Workspace dropdown, add the pin indicator icon and turn on/off to the dropdown; pin indicator to the left of the workspace." Pin semantics confirmed by operator follow-up Q&A: "Pin = default on load — same as task 092 behavior; pinned workspaces auto-open as tabs on cold load. This task just SURFACES the pin toggle in the dropdown UI." Plus operator wanted "Edit current workspace" and "Delete current workspace" actions moved out of the dropdown ("the dropdown should be for SELECTION, not editing"); the destination is a Manage Workspaces side pane to be built in Wave 3 (task 093). For task 098 the Edit menu entry is removed and a JSDoc breadcrumb left at the deletion site pointing to task 093 (so reviewers don't mistake it for dead-code deletion). | **RESOLVED (2026-05-22 / task 098, Wave 2 commit 2 of 3)** | Low | **Resolved (four surgical changes in two files; LegalWorkspace untouched)**: **(a)** `WorkspaceTabManagerComponent.tsx` — removed the inline pin button from every tab row: dropped `PinRegular`/`PinFilled` imports; deleted the `pinnedIds` derived `Set<string>` state + its sync `useEffect`; deleted the `handlePinToggle` callback; deleted the per-tab `<Tooltip><Button>` pin JSX (the tab row now contains only `[label \| spinner+label] + close button`); deleted the unused `pinButton`/`pinButtonActive` style entries. **(b)** same file — bumped tab title font one Fluent v9 step: `tokens.fontSizeBase200` → `tokens.fontSizeBase300` with matching `lineHeightBase200` → `lineHeightBase300`; the tab loading badge stays at `fontSizeBase200` (spinner row is intentionally less prominent than the resolved title). **(c)** same file — downsized the close × icon by swapping `DismissRegular` (20px) → `Dismiss12Regular` (12px); both are real `@fluentui/react-icons` v9 exports (verified via `node -e require('@fluentui/react-icons')` against the installed package; `Dismiss12Regular: object`); Button hit area kept at 16×16 (the surrounding tab itself is the primary 40px target per Fluent v9 tab pattern). **(d)** `WorkspacePaneMenu.tsx` — trigger button text "Workspace" → "Workspaces"; each MenuItem in the Select Workspace section now renders a `<div className={styles.layoutRow}>` containing `[pin Button] [layout name <span>] [CheckmarkRegular if active]`; pin icon is `PinRegular` (outline / unpinned `tokens.colorNeutralForeground3`) or `PinFilled` (solid / pinned `tokens.colorBrandForeground1`); pin Button is `appearance="subtle" size="small"` with 20×20 minWidth/height + `aria-pressed` reflecting state and a tooltip `"Pin {name} to start"` / `"Unpin {name} from start"`; **stopPropagation** pattern — onClick calls `e.stopPropagation()` BEFORE invoking the toggle handler so the parent `<MenuItem onClick={() => handleLayoutSelect}>` does NOT also fire; an `onKeyDown` handler stops Enter/Space from bubbling when the Button is focused (Button still handles activation natively via its own click-on-keyup); pin state mirrored into a React `Set<string>` (`pinnedIds`) initialised from `getPinnedWorkspaces()` with a `useEffect` keyed on `menuOpen` for defensive re-sync from localStorage on menu open; writes go through `pinWorkspace(layoutId, layoutName)` / `unpinWorkspace(layoutId)` from `services/pinnedWorkspaces.ts` — the task-092 storage contract, **unchanged**, so the cold-load auto-open effect in `WorkspacePane.tsx` consumes the same list. Same file — `handleEditWorkspace` callback + the "Edit current workspace" MenuItem + the `EditRegular` icon import all removed; JSDoc breadcrumb left at the deletion site: "Edit + Delete moved to Manage workspaces side pane (task 093). Operator feedback 2026-05-22: dropdown should be for SELECTION, not editing — editing lives in the manage surface." The `launchWizard` helper itself remains (still used by `handleCreateWorkspace`) and is available for task 093 to import + re-use for edit/saveAs flows. `+ New Workspace` and `Manage workspaces` MenuItems untouched. **What did NOT change**: `ThreePaneShell.tsx` (centerCollapsedIcon prop wiring is handled by the main session AFTER all Wave-2 commits land — sequential to avoid 3-way merge on a single file with sibling Assistant/Context agents); `WorkspacePane.tsx` (cold-load auto-open effect untouched; dropdown writes to the same localStorage list the effect reads); `services/pinnedWorkspaces.ts` (consumed unchanged); `WorkspaceTabManager.ts` (verified by grep: zero pin metadata — pin state was owned by `pinnedWorkspaces.ts` + the component-local `pinnedIds` Set, not the manager); LegalWorkspace (FR-25 / NFR-10 preserved — no LegalWorkspace files touched). **Build**: SpaarkeAi **892.78 KB gzip** (−3.89 KB vs task 096 baseline 896.67 — net reduction from deleting per-tab pin code + EditRegular import + handleEditWorkspace callback + Edit current MenuItem + unused pinButton styles; new dropdown pin column reuses imports already present (Button, Tooltip, makeStyles, mergeClasses) and adds back PinRegular/PinFilled — net is still negative); LegalWorkspace not rebuilt. ADR-012 (both files SpaarkeAi-local — correct; they consume context, tab state, layouts hook, pin service that does not belong in the shared library), ADR-021 (Fluent v9 tokens only — no hex/rgba; pin column uses `colorNeutralForeground1/3` + `colorBrandForeground1` + `colorNeutralBackground3Hover`; title moves between `fontSizeBase200/300` semantic tokens), ADR-022 (React 19 functional — no new hooks beyond `useState`/`useEffect`/`useCallback`), ADR-025 (`Dismiss12Regular`/`PinRegular`/`PinFilled` are `@fluentui/react-icons` v9 — verified), ADR-028 (no auth surface change — `useAiSession` consumption unchanged) all complied with. NOT deployed in this task — Wave 2 commit 2 of 3; coordinated deploy by main session after Assistant (097) + Context (099) commits land. See deploy memo "Task 098 — Workspace pane: tabs + dropdown polish". |
| R-28 | Context pane UX inconsistencies — operator feedback (Round 6, 2026-05-22) flagged three Context-pane issues in the Wave-2 operator smoke: **(a)** the PaneHeader rightSlot rendered both the Tools dropdown AND a stage label `<Text>` ("Sources" / "Get Started" / "Entity" / etc.) — operator: "Remove the 'Sources' label; move the Tools select drop down justify right; mirror same as Workspace drop down". The stage label was a relic of the original task 010 PaneHeader composition (when the Context pane had no other rightSlot content); after task 095 added the Tools dropdown the label became visual noise that made the Context pane PaneHeader look inconsistent with the Workspace pane's PaneHeader (which had only its dropdown right-justified); **(b)** the operator wanted to "pin" a preferred default Context tool — the Tools dropdown surfaced two tools (Quick Start, Semantic Search) with no way for the user to express "I want Semantic Search to be my default on next session". Task 095 persisted the most-recently-selected tool via `localStorage["spaarke:context:selected-tool"]` (so modal close didn't blank the pane), but that's a "last-used" semantic, not a "preferred default" — a brand-new user (or one who cleared localStorage) always landed on Quick Start regardless of their actual preference. Operator follow-up: "Pin = default on load — the pinned tool is auto-selected on next session"; **(c)** when Quick Start was active, the GetStartedCardsWidget rendered without any header / title inside the pane body — the user had no visual indication that "Quick Start" was the current tool (the active checkmark in the dropdown was the only signal, and the dropdown was closed by default). Operator: "For the quick start tool, Add a title identifying as 'Quick Start'". | **RESOLVED (2026-05-22 / task 099, Wave 2 commit 3 of 3)** | Low | **Resolved (three surgical changes in `ContextPaneController.tsx` + `ContextPaneMenu.tsx` + new `services/contextToolPin.ts` utility + `useContextTool.ts` hook update; LegalWorkspace untouched)**: **(a)** `ContextPaneController.tsx` PaneHeader rightSlot — dropped the `<Text className={styles.headerStageLabel}>{stageLabelMap[contextStage]}</Text>` rendering and the wrapper `<div>` that flex-rowed it next to the `<ContextPaneMenu>`; rightSlot is now `<ContextPaneMenu />` directly. The PaneHeader's existing rightSlot CSS right-justifies its child by default, so the Tools dropdown sits flush right — visually identical to the Workspace pane's PaneHeader (which has only `<WorkspacePaneMenu>` in its rightSlot). The previous `styles.headerStageLabel` CSS rule was removed; `stageLabelMap` + `contextStage` state retained for ARIA / debug — surfaced as `data-context-stage` on the controller's root for test-harness assertions (Playwright / smoke), keeping `noUnusedLocals` satisfied without rendering any visible label. **(b)** `ContextPaneMenu.tsx` — each MenuItem now has a clickable pin icon on the LEFT (mirrors task 098's per-MenuItem pin column on the Workspace pane verbatim — operator wanted identical UX across both dropdowns): `PinRegular` outline at `tokens.colorNeutralForeground3` when not pinned / `PinFilled` solid at `tokens.colorBrandForeground1` when pinned. Pin click stops propagation + preventDefault BEFORE invoking the toggle (so the parent MenuItem's `onSelectTool` callback does NOT fire — pin only updates the default, not the active selection). Single-pin "default on load" semantics via the new `services/contextToolPin.ts` utility (key `spaarke:context:pinned-tool`, JSON string of a single `ContextToolId`, validated against `VALID_CONTEXT_TOOL_IDS` on read). `useContextTool.ts` `readPersistedTool()` updated: when no `selected-tool` value exists in localStorage (first-time users / cold resets), the hook reads the pinned default via `getPinnedContextTool()` before falling back to the hardcoded `'quick-start'` default. If both `selected-tool` AND `pinned-tool` exist, `selected-tool` wins (user's last-used > pinned default — preserves task 095 modal-close fix). Pin state hydrated on mount via lazy initializer + re-read whenever the menu opens (`onOpenChange` reads `getPinnedContextTool()` again so other-tab pin changes propagate). Pin Button gets `aria-pressed` + `aria-label` ("Pin Quick Start as default" / "Unpin Quick Start") + Tooltip. **(c)** `ContextPaneController.tsx` Quick Start render path — wrapped `<GetStartedCardsWidget onCardClick={...} />` in a small container that renders a "Quick Start" `<Text size={400}>` heading above the 7-card grid (font weight semibold, color `colorNeutralForeground1`, padding `spacingVerticalM` top / `spacingVerticalS` bottom / `spacingHorizontalM` sides). GetStartedCardsWidget itself (in `@spaarke/ai-widgets` shared lib) is NOT modified — out of scope; the header is rendered AROUND it by the controller. **Bundle delta**: SpaarkeAi **894.23 KB gzip** (vs task 096 baseline 896.67 = **−2.44 KB / −0.27%**). The slight DECREASE is plausible because (i) the previous SemanticSearchCriteriaTool used two `<Input type="date">` widgets + a `headerSubtitle` Text + a `dateRow` style block — all replaced with a single Date Range `<Dropdown>` + four type-button Buttons + a few filter Dropdowns (see R-29 for the SemanticSearch expansion that lands in the same commit); (ii) the ContextPaneController's rightSlot wrapper div + Text was inlined (removed); (iii) the new icon set (`DocumentRegular`, `BriefcaseRegular`, `FolderRegular`, `ReceiptRegular`, `PinRegular`, `PinFilled`, `Search20Regular`) tree-shakes cleanly. LegalWorkspace not rebuilt — no LegalWorkspace files touched (FR-25 / NFR-10 preserved). ADR-012 (`ContextPaneMenu` + `SemanticSearchCriteriaTool` + `contextToolPin.ts` + `useContextTool` are all SpaarkeAi-local — they consume solution-local tool ids, storage namespaces, and a SemanticSearch modal launch that doesn't belong in the shared library), ADR-021 (Fluent v9 tokens only — no hex / rgba; pin column uses `colorBrandForeground1` / `colorNeutralForeground1/3` / `colorNeutralBackground3Hover`; Quick Start header uses `fontWeightSemibold` / `colorNeutralForeground1`; spacing via `spacingHorizontalXS/M` / `spacingVerticalS/M`), ADR-022 (React 19 functional — no new hooks beyond `useState`/`useCallback`), ADR-025 (`PinRegular`/`PinFilled`/`ChevronDownRegular`/`AppsListRegular`/`SearchRegular`/`CheckmarkRegular` all `@fluentui/react-icons` v9), ADR-028 (no auth surface change — pin / selected-tool are all client-side localStorage; `sprk_semanticsearch` modal has its own MSAL bootstrap) all complied with. NOT deployed in this task — Wave 2 commit 3 of 3; coordinated deploy by main session after Assistant (097) + Workspace (098) commits land. See deploy memo "Task 099 — Context pane: dropdown polish + Quick Start title + Semantic Search field expansion". |
| R-29 | Semantic Search Criteria parity gap with the full code page — operator feedback (Round 6, 2026-05-22) flagged that the in-pane SemanticSearchCriteriaTool (task 095) had an oversimplified surface compared to the full SemanticSearch Code Page (`sprk_semanticsearch`): the in-pane tool offered only a single-select Domain Dropdown + AI query Textarea + optional from/to date Inputs + Search button, whereas the full code page's `SearchFilterPane` offers a much richer criteria-editing experience (4 type tabs + per-domain filter dropdowns for Document Type / File Type / Matter Type + Date Range preset dropdown). Operator: "For the Semantic Search criteria tool, include the 'type' fields as are available in the full semantic search code page." Operator follow-up Q&A clarified the desired in-pane surface: "Type buttons + AI query + per-domain filters + Date Range — Documents/Matters/Projects/Invoices type toggle, AI Search textarea, Document Type / File Type / Matter Type dropdowns (whichever applies per active domain), Date Range dropdown. Search button launches the modal as today. NO Saved Searches in the compact in-pane view." The parity gap meant the user often had to launch the full modal just to set a Document Type filter — losing the in-pane efficiency the task 095 tool was supposed to provide. | **RESOLVED (2026-05-22 / task 099, Wave 2 commit 3 of 3)** | Low | **Resolved (`SemanticSearchCriteriaTool.tsx` expanded to match operator's screenshot)**: replaced the single-select Domain `<Dropdown>` with a **2×2 grid of 4 type-toggle buttons** — Documents (`DocumentRegular`), Matters (`BriefcaseRegular`), Projects (`FolderRegular`), Invoices (`ReceiptRegular`); active type uses `appearance="primary"` (brand fill); the rest use `appearance="subtle"` (transparent). Icons chosen to match the operator's screenshot — `DocumentRegular` for Documents, `BriefcaseRegular` for Matters (legal matters carry a briefcase metaphor in MDA UX), `FolderRegular` for Projects (project work is folder-based), `ReceiptRegular` for Invoices. Added **per-domain filter dropdowns** that render conditionally based on `criteria.domain`: Document Type + File Type (Documents domain only — placeholder options `Contract`/`NDA`/`Brief`/`Agreement`/`Invoice` and `PDF`/`DOCX`/`XLSX`/`PPTX`/`TXT`), Matter Type (Documents + Matters domains — placeholder options `Litigation`/`Transactional`/`Regulatory`/`Advisory`). Static placeholder lists today with a clearly-documented TODO comment for backend wiring: "TODO: wire to filter options endpoint" — the full SemanticSearch Code Page's `useFilterOptions` hook fetches real values from the BFF filter-options endpoint on the page side once the modal opens, so the in-pane dropdowns only need a representative list until the parallel wiring lands; the operator's actual filter values get applied to the modal via URL query params regardless of which list the in-pane dropdown showed. Added **Date Range dropdown** with preset options: Off (default) / Last 7 days / Last 30 days / Last 90 days / Custom range. The "Custom range" option is static today (selecting it doesn't currently surface from/to date inputs in the pane); expanding to inline date inputs is documented as a follow-up in the file header (cited reason: the operator's screenshot showed a single dropdown without expanded inputs, so the static surface matches the operator's expectation today). Removed the previous from/to `<Input type="date">` row entirely. Removed the `headerSubtitle` "Refine your query, then open results in a full Semantic Search view" line — operator's screenshot shows the section title alone, no subtitle. Search button is now full-width primary at the bottom (matches operator screenshot — no longer right-aligned via `searchButtonRow` flex-end). Build URL `data` payload extended with `documentType` / `fileType` / `matterType` / `dateRange` query params, only emitted when the active domain makes the filter relevant AND the user has selected a non-"all" / non-"off" value. Backwards-compat persistence in `localStorage["spaarke:context:semantic-search-criteria"]` — extended `PersistedCriteria` shape with new fields; missing fields read as `undefined` and default to "all" / "off" on next mount; legacy `dateFrom` / `dateTo` fields silently ignored (no migration needed; they were never wired to BFF, the user just re-picks a Date Range preset on next session). **What did NOT change**: SemanticSearch Code Page itself (read for visual reference only — `SearchFilterPane.tsx`, `SearchDomainTabs.tsx`, `FilterDropdown.tsx`, `DateRangeFilter.tsx`, `types/index.ts` consulted for shape/UX; `SearchDomain` union replicated locally in SemanticSearchCriteriaTool to keep the SpaarkeAi bundle tree-shake clean — importing from `src/client/code-pages/SemanticSearch` would defeat tree-shaking); the modal launch shape (still `Xrm.Navigation.navigateTo` to `sprk_semanticsearch` via frame-walked Xrm Navigation, 80% × 80%, `pageType: 'webresource'`, `target: 2`); the task 095 "selectedTool persistence through modal close" fix (still in effect — selectedTool is owned by `useContextTool` at the controller level, not by this tool). **Bundle delta**: see R-28 — SpaarkeAi **894.23 KB gzip** (vs 896.67 task 096 baseline = **−2.44 KB / −0.27%**), the two operator-requested expansions in this commit (Context-pane chrome polish per R-28 + Semantic Search criteria expansion per R-29) net to a small REDUCTION because the replaced surface (two date Inputs, headerSubtitle, dateRow) was heavier than the additions (four lightweight Buttons + three Dropdowns share existing Fluent imports). ADR-012 / 021 / 022 / 025 / 028 all complied with (see R-28 detail for token usage). Deferred follow-ups documented in the file header: (1) `useFilterOptions` BFF endpoint wiring for the in-pane dropdowns, (2) Custom range inline date-input expansion, (3) any new filter types that land in the full SemanticSearch SearchFilters interface (so the in-pane subset can be expanded in lockstep). NOT deployed in this task — Wave 2 commit 3 of 3; coordinated deploy by main session after Assistant (097) + Workspace (098) commits land. See deploy memo "Task 099 — Context pane: dropdown polish + Quick Start title + Semantic Search field expansion". |
| R-30 | Edit + Delete actions homeless after task 098 removed them from the dropdown; no Manage workspaces surface — operator feedback (Round 6, Wave 2, 2026-05-22) directed: "the dropdown should be for SELECTION, not editing" → task 098 deleted the "Edit current workspace" entry from `WorkspacePaneMenu.tsx`. Operator follow-up: "Need to be able to delete workspace. Manage workspaces UI similar to view manager pattern. ... Move the 'Edit' workspace to the Manage workspace. Move the delete workspace to the Manage workspace." Task 098 left the "Manage workspaces" MenuItem in place but its onClick was a `console.log` stub (since task 089) with a JSDoc breadcrumb pointing to task 093 (this remediation). Until 093 landed, the operator had NO way to (a) edit a workspace, (b) delete a workspace, (c) rename a workspace, or (d) see a comprehensive view of their workspace layouts. The Wave 2 deploy (096+097+098+099 follow-up) shipped without Edit/Delete affordances anywhere — the gap was deliberate (R-30 placeholder) and tracked until task 093. | **RESOLVED (2026-05-22 / task 093, Wave 3)** | Medium | **Resolved (`ManageWorkspacesPane.tsx` + `workspaceLayoutMutations.ts` + `WorkspacePaneMenu.tsx` wiring)**: "Manage workspaces" dropdown MenuItem now opens a Fluent v9 `<OverlayDrawer position="end" size="medium">` (~440px) listing ALL the user's workspace layouts with per-row Pin / Rename / Edit / Delete affordances. **OverlayDrawer chosen over `Xrm.Navigation.navigateTo`** because: (i) the SpaarkeAi shell uses Fluent v9 surfaces (PaneHeader, Menu, Dialog) — staying in Fluent keeps tokens / theming consistent, (ii) Xrm modal handoff would break the "manage and return" flow, (iii) the pre-task-097 History affordance used the same OverlayDrawer pattern so the import + UX are familiar to this codebase, (iv) OverlayDrawer's auto-portal + scrim + Escape-to-close + focus trap behaviours give the view-manager UX with zero custom plumbing. **Per-row**: Pin star LEFT uses `pinnedWorkspaces.ts` `pinWorkspace`/`unpinWorkspace` (SAME `spaarke:workspace:pinned-list` localStorage key as task 098 dropdown + task 092 cold-load auto-open — no parallel storage); double-click rename (Path A — fastest UX over Path B per-row dialog) via new `renameWorkspaceLayout` helper → BFF `PUT /api/workspace/layouts/{id}` (full overwrite — endpoint is not PATCH); secondary metadata "System layout — Save As to edit" italic hint for `isSystem===true` (BFF DTO doesn't carry `modifiedOn` and we deliberately did NOT add a DTO extension per CLAUDE.md §10 BFF Hygiene); Edit launches `sprk_workspacelayoutwizard` via frame-walked `Xrm.Navigation.navigateTo` (`saveAs` for system layouts — clone-then-edit pre-populating template/sections/name; `edit` in-place for user layouts) — pattern lifted verbatim from `LegalWorkspace/src/components/Shell/WorkspaceGrid.tsx` lines ~720-760 and the pre-task-098 `handleEditWorkspace` removed from the dropdown; Delete opens inline Fluent v9 `<Dialog>` ("Delete workspace?", body "{layoutName} will be permanently removed. This cannot be undone.", primary "Delete" with `colorStatusDangerBackground3` / secondary "Cancel") → on confirm calls new `deleteWorkspaceLayout` → BFF `DELETE /api/workspace/layouts/{id}` (soft-delete via Dataverse deactivation), drops from pin list if pinned (so cold-load auto-open next session doesn't fail to resolve), closes dialog, refetches. System layouts: Delete DISABLED with Tooltip; double-click name no-op; Edit triggers saveAs. **BFF endpoints**: GET layouts, GET default, PUT update (403 for system), DELETE (403 for system) ALL PRE-EXISTED in `Sprk.Bff.Api/Api/Workspace/WorkspaceLayoutEndpoints.cs` — ZERO new endpoints added per CLAUDE.md §10 BFF Hygiene Binding Governance (Placement Justification therefore vacuous — nothing placed in BFF). **Files added**: `src/solutions/SpaarkeAi/src/components/workspace/ManageWorkspacesPane.tsx` (~470 LOC including inline delete Dialog), `src/solutions/SpaarkeAi/src/services/workspaceLayoutMutations.ts` (`renameWorkspaceLayout` + `deleteWorkspaceLayout` thin `authenticatedFetch`-based wrappers; error class extended with `.status` so callers can branch on 403/404). **Files modified**: `src/solutions/SpaarkeAi/src/components/workspace/WorkspacePaneMenu.tsx` — `handleManageWorkspaces` no longer console.logs (flips new `isManageOpen` state); drawer mounted as sibling of `<Menu>` inside React Fragment (Fluent v9 portals the drawer DOM regardless of JSX position); added `handleManageOpenChange`; dropped unused `isPinned` import (dropdown uses `pinnedIds.has(...)` only). **Bundle delta**: SpaarkeAi **899.35 KB gzip** (+5.12 KB / +0.57% vs Wave 2 follow-up baseline 894.23) — tracks new `<OverlayDrawer>` + `<Dialog>` + `<DrawerHeader>`/`Title`/`Body` + `<DialogSurface>`/`Body`/`Title`/`Content`/`Actions` + `EditRegular`/`DeleteRegular`/`DismissRegular` imports + rename `<Input>` + edit-wizard launch helper (~80 LOC) + two mutation helpers + `buildBffApiUrl` calls; task 097 had removed the previous Drawer surface so this re-adds it. LegalWorkspace not rebuilt — no LegalWorkspace files touched (FR-25 / NFR-10 preserved). **ADR compliance**: ADR-012 (all three SpaarkeAi-local), ADR-021 (Fluent v9 tokens only — `colorNeutralStroke2`, `colorNeutralBackground2Hover`, `colorNeutralForeground1/3`, `colorBrandForeground1`, `colorStatusDangerBackground1/3/3Hover/3Pressed`, `colorStatusDangerForeground1`, `colorNeutralForegroundOnBrand`, `fontSizeBase200`, `borderRadiusMedium`, spacing tokens; destructive Delete uses canonical `colorStatusDangerBackground3`), ADR-022 (React 19 functional — useState/useEffect/useCallback), ADR-025 (`DismissRegular`/`PinRegular`/`PinFilled`/`EditRegular`/`DeleteRegular` all `@fluentui/react-icons` v9), ADR-028 (`authenticatedFetch` + `bffBaseUrl` from `useAiSession()`; no token snapshots; mutation helpers accept auth as typed arg). Deployed to spaarkedev1 (`sprk_spaarkeai` resource `5206a442-3451-f111-bec7-7ced8d1dc988`). See deploy memo "Task 093 — Manage Workspaces side pane". |
| R-31 | Collapsed pane icons centered vertically (visually inconsistent with MDA) + remaining expanded pane(s) don't fill freed width when others are collapsed — operator feedback (Round 7, 2026-05-22) flagged two related shared-lib UX gaps after the Wave-2/3 deploy (096/097/098/099/093) landed: **(a)** the collapsed strip icons (task 096 `leftCollapsedIcon` / `centerCollapsedIcon` / `rightCollapsedIcon` props, wired into ThreePaneShell by tasks 097/098/099) floated visually in the middle of each collapsed strip instead of sitting at the top — operator: "The collapsed pane icons should be top aligned, not middle aligned" — the previous task-096 `styles.collapsedIcon` set `flexGrow: 1` on the icon container so it stretched to fill the entire vertical space of the column-flex parent and `alignItems/justifyContent: center` centered the icon inside that stretched container. The visual result was inconsistent with MDA's left-nav collapsed-pane treatment (which top-aligns its icons just below the chrome) and made the icon visually "jump" up/down when the user toggled collapse/expand because the expanded PaneHeader icon sits ~12px below the top edge of the pane while the collapsed-strip icon previously sat ~50% down the strip; **(b)** when the operator collapsed BOTH the Assistant pane AND the Workspace pane (leaving only Context expanded), the Context pane stayed at its fixed `rightWidthPx` (400px default) and a wide empty `colorNeutralBackground1` column appeared to its right between the two collapsed 48px strips and the Context pane — operator: "If Assistant pane AND Workspace pane are collapsed, the container for Context pane should extend to full width of the main page container with the background color." The implicit extension (operator confirmed in Q&A) was that the same logic should apply to ANY two-collapsed combination AND the single-collapsed cases (specifically the previously-unworking center-collapsed-but-both-sides-expanded case where the right edge had empty `colorNeutralBackground1` because center was 48px-strip but both side panes stayed at fixed widths). The layout's existing `flex: 1` on the center pane handled the easy cases (left-only or right-only collapsed → center fills) but the center-collapsed and double-collapsed cases were not covered. | **RESOLVED (2026-05-22 / task 100, Wave-1-R7)** | Low | **Resolved (two surgical CSS-driven changes in `@spaarke/ui-components/ThreePaneLayout.tsx`; LegalWorkspace untouched — doesn't import `ThreePaneLayout`)**: **(a)** Removed `flexGrow: 1` from `styles.collapsedIcon` so the icon no longer stretches to fill the strip; added `justifyContent: 'flex-start'` to all three `*PaneCollapsed` styles (`leftPaneCollapsed` / `rightPaneCollapsed` / `centerPaneCollapsed`); bumped `paddingTop: spacingVerticalS → spacingVerticalM` (~8px → ~12px) on the same three styles to mirror the expanded PaneHeader's visual icon offset (PaneHeader: `paddingTop: spacingVerticalS` inside a 40px min-height row places its icon ~12px below the top edge; `spacingVerticalM` matches that rhythm so the icon doesn't visually shift when toggling collapse/expand). All four changes are token-based — no hex / rgba / px literals introduced (ADR-021). **(b)** Replaced fixed `width: ${leftWidthPx}px` / `${rightWidthPx}px` on the left + right pane wrappers with a conditional `style` based on three new derived booleans: `expandedCount = isLeftVisible + isCenterVisible + isRightVisible`; `leftIsSoleExpanded = isLeftVisible && expandedCount === 1`; `rightIsSoleExpanded = isRightVisible && expandedCount === 1`; `rightFillsForCollapsedCenter = isLeftVisible && !isCenterVisible && isRightVisible`. Left pane render: `leftIsSoleExpanded ? { flex: '1 1 auto', width: 'auto' } : { width: '${leftWidthPx}px' }`. Right pane render: `(rightIsSoleExpanded || rightFillsForCollapsedCenter) ? { flex: '1 1 auto', width: 'auto' } : { width: '${rightWidthPx}px' }`. Center pane was already `flex: 1` via existing `styles.centerPane` — no change needed. All 8 collapse combinations covered: (i) all 3 expanded → center fills via existing `flex: 1`; (ii) left-only collapsed → center fills; (iii) right-only collapsed → center fills; (iv) center-only collapsed → right fills via `rightFillsForCollapsedCenter` (NEW); (v) left+center collapsed → right (Context) fills via `rightIsSoleExpanded` (operator's primary case — NEW); (vi) left+right collapsed → center (Workspace) fills via existing `flex: 1`; (vii) center+right collapsed → left (Assistant) fills via `leftIsSoleExpanded` (NEW); (viii) all 3 collapsed → three 48px strips side-by-side (degenerate; acceptable no-op state). Splitters between adjacent panes hide when either neighbor is collapsed (task 094 logic, unchanged) so flex-grow doesn't fight a splitter that has no resize partner. The expanded pane's own background (`colorNeutralBackground1` from each pane's body) fills the freed width automatically — no shared-lib background change needed since each pane component renders at `width: 100%; height: 100%`. **What did NOT change**: SpaarkeAi pane components (`ConversationPane.tsx` / `WorkspacePane.tsx` / `ContextPaneController.tsx` / `ThreePaneShell.tsx`); `ThreePaneLayout.types.ts` (no new props — pure CSS-driven change); `useThreePaneLayout` hook (width persistence untouched — the new flex behavior overrides the persisted `leftWidthPx`/`rightWidthPx` only in the sole-expanded / center-collapsed cases, so the user's resized widths are still honored when at least 2 panes are expanded with the center expanded); LegalWorkspace (FR-25 / NFR-10 preserved — doesn't import `ThreePaneLayout`). **Bundle delta**: SpaarkeAi **899.39 KB gzip** (vs task 093 baseline 899.35 = **+0.04 KB / +40 bytes**) — within the near-zero envelope expected for a CSS-only change (the +40 bytes track the new derived booleans + the conditional style ternaries minified into the layout's JSX). LegalWorkspace **569.38 KB gzip** — byte-identical to task 096 baseline (LegalWorkspace doesn't import `ThreePaneLayout`, only `PaneHeader`, which is untouched). ADR-012 (the width-redistribution + alignment behavior belongs in the shared layout primitive — placing it here means future consumers get the behavior for free without re-implementing per shell), ADR-021 (Fluent v9 tokens only — `spacingVerticalM` / `spacingVerticalXS`; the CSS-spec values `'1 1 auto'` and `width: 'auto'` are flex / CSS keywords, exempt from token requirement the same way `flex: 1` already was), ADR-022 (React 19 functional — no new hooks; derived booleans are plain consts computed every render, cheaper than `useMemo` overhead for 3-boolean math) all complied with. Deployed to spaarkedev1 (`sprk_spaarkeai` resource `5206a442-3451-f111-bec7-7ced8d1dc988`). See deploy memo "Task 100 — Collapsed strip top-alignment + empty-pane width redistribution". |
| R-32 | Context tool pin "default on load" semantics broken on browser refresh — operator feedback (Round 7, 2026-05-22) reported that pinning a Context tool (task 099) correctly persisted the pin INDICATOR across browser sessions (the pin icon stayed highlighted in the Tools dropdown), but the pinned tool itself was NOT auto-loaded on browser refresh / cold start — the pane returned to whichever tool the user had clicked last. Root cause: task 095's `useContextTool.ts` stored `selected-tool` in `localStorage`; task 099's pin-aware initialization at `readPersistedTool()` only honored the pin when `selected-tool` was `null`; `setSelectedTool` wrote to localStorage on EVERY user click, so once a user clicked ANY tool the localStorage value won over the pin forever. The pin only "worked" for first-time users who had never clicked anything. Operator: "if I refresh browser or close/reopen browser the pinned components are not loaded (even though the pin select IS persisted in the dropdown menu)." | **RESOLVED (2026-05-22 / task 101, Wave-2-R7 commit 1 of 3)** | Low | **Resolved (`src/solutions/SpaarkeAi/src/hooks/useContextTool.ts` storage-split + one-time migration; pin storage `services/contextToolPin.ts` unchanged)**: split storage by lifetime to make the pin authoritative on cold mount: **(1)** `selected-tool` moves from `localStorage` to **`sessionStorage`** (same key `spaarke:context:selected-tool`); cleared on browser close. This still preserves task 095's modal-close-restoration behaviour because the Semantic Search results modal opens + closes WITHIN the same browser session — sessionStorage covers within-session refreshes too. **(2)** `pinned-tool` continues to live in **`localStorage`** via `services/contextToolPin.ts` (UNCHANGED — its API was correct as-is). **(3)** On mount, `readPersistedTool()` now: (a) reads sessionStorage `selected-tool` → if present + valid, use it (handles within-session refresh + modal close); (b) else reads pinned tool via `getPinnedContextTool()` → if present, use it (handles browser cold start when pin is set — the bug-fix case); (c) else defaults to `'quick-start'`. **(4) One-time migration** via new `migrateLegacyLocalStorageEntry()` helper called from `readPersistedTool` BEFORE the sessionStorage read: if a legacy `selected-tool` value exists in localStorage (from task 095/099 sessions), the migration reads + validates it against `VALID_CONTEXT_TOOL_IDS`, seeds sessionStorage with it (only when sessionStorage is empty — never clobbers a fresh in-session value), and DELETES the legacy localStorage entry. Idempotent: after running once the legacy key is gone forever. Safe under private-browsing / quota errors (any throw short-circuits silently and the user falls through to pin default / hardcoded default). **(5)** `writePersistedTool(id)` now writes to **sessionStorage only** (so subsequent clicks don't re-pollute localStorage). **What did NOT change**: `services/contextToolPin.ts` (pin localStorage API correct as-is — task 099 contract preserved); SemanticSearchCriteriaTool's own criteria storage at `spaarke:context:semantic-search-criteria` (orthogonal); LegalWorkspace (FR-25 / NFR-10 preserved). **Bundle delta**: SpaarkeAi (combined with R-33 fix) **899.64 KB gzip** (+0.25 KB vs task 100 baseline 899.39 — minor growth from migration logic + expanded JSDoc + new ID checks). ADR-021 (no styling change — pure state/effect logic), ADR-022 (React 19 — `useState` lazy initializer pattern preserved), ADR-028 (no auth surface change — both fixes are pure client-side state) all complied with. NOT deployed in this task — Wave-2-R7 commit 1 of 3; coordinated deploy by main session after Workspace UX (102) + Context polish (103) commits land. See deploy memo "Task 101 — Pin persistence fix (Context tool + Workspace auto-open)". |
| R-33 | Workspace pin auto-open dispatches dropped on the floor — pinned tabs never auto-loaded on browser refresh (Round 7, 2026-05-22) — operator feedback: the Workspace pin INDICATOR persisted correctly across sessions (the pin icon in the dropdown stayed highlighted), but on browser refresh / cold start the pinned workspaces did NOT auto-open as tabs — even though the localStorage list was intact and the auto-open effect in `WorkspacePane.tsx` (task 092) was firing. Root cause (cause #4 from the task brief — investigated against five candidates): the auto-open effect dispatches `widget_load` events on the `workspace` channel via `useDispatchPaneEvent`. The handler that converts those into tabs is registered via `usePaneEvent('workspace', ...)` at line 427 of `WorkspacePane.tsx`. Both are React `useEffect`s; React runs effects in declaration order; the auto-open effect was declared at line 387 (BEFORE the `usePaneEvent` invocation), so it ran FIRST. By the time the auto-open dispatches fired, the `usePaneEvent` subscription effect hadn't run yet — the `PaneEventBus.subscriberCount('workspace')` was zero. `PaneEventBus.dispatch()` documents the failure mode explicitly: *"Dispatching to a channel with no subscribers is safe — no error is thrown and no work is done."* The pin localStorage was read correctly; the pinned-list contents were valid; the dispatch fired; the receiver just didn't exist yet. By the time the receiver mounted (one effect later), the events were already gone. Pre-existing tab-persistence-restore effect (task 065) had a similar effect-order risk but was protected by the `restoreFromPersistence` guard that no-ops if a non-Home tab is already open — so the auto-open race was the actual operator-visible failure. | **RESOLVED (2026-05-22 / task 101, Wave-2-R7 commit 1 of 3)** | Low | **Resolved (`src/solutions/SpaarkeAi/src/components/workspace/WorkspacePane.tsx` auto-open effect — defer dispatch to a macrotask + add observability log)**: changed the auto-open effect body to: **(1)** compute `pinsToOpen` (pinned workspaces filtered by the existing duplicate-guard `openLayoutIds` Set) early, so we can early-return when there's nothing to open instead of scheduling an empty timer; **(2)** wrap the dispatch loop in `window.setTimeout(() => { ... }, 0)` — by the time the macrotask runs, every `useEffect` in this render's commit phase has executed (including `usePaneEvent('workspace', ...)`'s internal subscribe-useEffect), so the subscription is live when the events fire. macrotask (not microtask) is chosen because `queueMicrotask` runs before subsequent useEffects in the same flush; `setTimeout(0)` is guaranteed to yield until after the commit phase completes; **(3)** added `console.info('[WorkspacePane] Auto-opening N pinned workspace(s):', pinsToOpen)` at the dispatch site so the operator can verify (via DevTools Console) during smoke that the effect fires and what pins were processed — acceptable observability (not happy-path App Insights, just dev console); **(4)** return a cleanup function from the effect that calls `window.clearTimeout(timerId)` to prevent late dispatch into a torn-down tree if the component unmounts during the 0ms window. **What did NOT change**: `services/pinnedWorkspaces.ts` (the storage key `spaarke:workspace:pinned-list` was correct — cause #3 in the brief was a false lead; the pin reads were always succeeding, the dispatches were the problem); `autoOpenedPinsRef` one-shot semantics (still flips to `true` synchronously inside the effect BEFORE the setTimeout schedules, so subsequent `isAuthenticated` re-evaluations don't re-arm the timer); the `widget_load` event payload shape; the existing `usePaneEvent('workspace', ...)` handler that converts dispatches to tabs; tab-persistence-restore (task 065) effect; Home-tab installation effect (task 011); LegalWorkspace (FR-25 / NFR-10 preserved). **Bundle delta**: included in R-32's combined +0.25 KB measurement (~150 bytes of this is the cleanup return + setTimeout wrapper + early-return pinsToOpen filter + the console.info; the remaining ~100 bytes is R-32's storage-split + migration). ADR-021 (no styling change), ADR-022 (React 19 — `setTimeout` is a standard browser API; properly cleaned up via the effect's cleanup return), ADR-028 (no auth surface change — `isAuthenticated` consumed from `useAiSession()` as before, no new fetch / token / `authenticatedFetch` interactions) all complied with. NOT deployed in this task — Wave-2-R7 commit 1 of 3; coordinated deploy by main session after Workspace UX (102) + Context polish (103) commits land. See deploy memo "Task 101 — Pin persistence fix (Context tool + Workspace auto-open)". |
| R-34 | Edit Workspace wizard surfaced all 9 layout templates while Create Workspace surfaced only the FR-14 6-template subset — operator feedback (Round 7, 2026-05-22): "The Create New Workspace wizard and the Edit Workspace wizard show different layout types; the 'Create New' is the correct set of options; BUT do not delete the code that allows a three column because we may use this in the future." Root cause: `WorkspacePaneMenu` (Create launch) had a private `SPAARKEAI_TEMPLATE_FILTER` constant and passed it to `launchWizard`; `ManageWorkspacesPane.launchEditWizard` (Edit launch, introduced by task 093) was NOT forwarding any filter, so the wizard's `main.tsx` parsed `templateFilter` as undefined and the `TemplateStep` rendered all 9 entries from the `LAYOUT_TEMPLATES` registry. Visible inconsistency: Create users saw 6 templates; Edit users saw 9. | **RESOLVED (2026-05-22 / task 102, Wave-2-R7 commit 2 of 3)** | Low | **Resolved (factored constant + import from both surfaces)**: created `src/solutions/SpaarkeAi/src/constants/workspaceTemplateFilter.ts` exporting `SPAARKEAI_TEMPLATE_FILTER` (the 6-template membership set: `2-col-equal`, `3-row-mixed`, `hero-2x2`, `sidebar-main`, `single-column`, `single-column-5`) + a `SpaarkeAiTemplateId` type. Removed the inline constant from `WorkspacePaneMenu.tsx` and imported the shared one (existing Create behavior unchanged). Imported the same constant in `ManageWorkspacesPane.tsx`; `launchEditWizard`'s data-params string now appends `templateFilter=${encodeURIComponent(SPAARKEAI_TEMPLATE_FILTER.join(","))}` so the wizard's `main.tsx` parses it into a typed `LayoutTemplateId[]` and `TemplateStep` filters accordingly. **FR-25 preservation**: the 3-column templates remain in the wizard's `LAYOUT_TEMPLATES` registry — `templateFilter` is a runtime membership SET that narrows what is surfaced, NOT a code deletion. Standalone LegalWorkspace continues to launch the wizard with NO filter → all 9 templates remain visible there. Operator's "do not delete the code that allows a three column" is automatic compliance. **What did NOT change**: the wizard's `LAYOUT_TEMPLATES` registry (operator's preservation requirement); LegalWorkspace `WorkspaceGrid.handleOpenWizard` (still passes no filter — FR-25 unchanged); BFF endpoints. **Bundle delta**: SpaarkeAi **899.62 KB gzip** (+0.23 KB vs task 100 baseline 899.39 — new constant file + memo + edit-wizard filter forward; near-zero envelope). ADR-012 (constant placed SpaarkeAi-local, NOT in `@spaarke/ui-components` — the filter is a SpaarkeAi product decision per FR-14; LegalWorkspace must NOT consume it so its FR-25 9-template behavior is preserved; local placement enforces the boundary), ADR-021 (no styling change), ADR-022 (React 19 — no new hooks), ADR-028 (no auth surface change) all complied with. NOT deployed in this task — Wave-2-R7 commit 2 of 3; coordinated deploy by main session after Pin persistence (101) + Context polish (103) commits land. See deploy memo "Task 102 — Workspace dropdown small fixes". |
| R-35 | "+ +" double-plus visible on the `+ New Workspace` dropdown entry — operator feedback (Round 7, 2026-05-22): "In the Workspace drop down, the menu icon shows two '+' — this may be because in the Ribbon JS the title to 'Add Workspace' includes a text '+' sign which should be removed." Root cause: `WorkspacePaneMenu`'s `+ New Workspace` MenuItem rendered "+" twice — once via the `AddRegular` icon prop (which renders as a "+" glyph) AND once as a literal "+" prefix in the label text `<span>+ New Workspace</span>`. Operator's hypothesis about a Ribbon JS source was investigated but disproved (see audit below). | **RESOLVED (2026-05-22 / task 102, Wave-2-R7 commit 2 of 3)** | Low | **Resolved (drop literal "+" from menu label)**: changed the MenuItem's child `<span className={styles.newWorkspace}>+ New Workspace</span>` to `<span className={styles.newWorkspace}>New Workspace</span>`. The `AddRegular` icon in the `icon` prop alone conveys "add" — single "+" symbol visible to the operator. **Ribbon JS / XML audit** (per operator's hypothesis): ran `Get-ChildItem -Recurse <repo> -Include "*.js","*.ts","*.tsx","*.xml" | Select-String -Pattern "\+\s*Add Workspace|\+\s*New Workspace"` and got 8 matches — ALL in `src/solutions/{SpaarkeAi,LegalWorkspace}/src/**/*.{ts,tsx}` (component code + JSDoc comments + the `WorkspacePaneMenu.tsx` line being fixed) plus two task-poml files under `projects/`. **NO** ribbon XML / JS / command bar definitions surfaced. The "+ +" duplication was entirely caused by the SpaarkeAi menu entry — no ribbon-level source exists. Operator's hypothesis about the source was incorrect; the symptom observation was correct. **Bundle delta**: included in R-34's combined +0.23 KB measurement (this fix is a single-character change in the JSX text, contributes <10 bytes minified). ADR-021 (no styling change — just label text), ADR-022 (React 19 — no hook changes), ADR-028 (no auth surface change) all complied with. NOT deployed in this task — Wave-2-R7 commit 2 of 3; coordinated deploy by main session after Wave-2-R7 trio lands. See deploy memo "Task 102 — Workspace dropdown small fixes". |
| R-36 | Workspace dropdown ordering didn't put default first / pinned next — operator feedback (Round 7, 2026-05-22): "In the workspace edit put the pinned at the top in the current order — do not need numbers — the default will be first, and then the order as pin/saved/persisted." `WorkspacePaneMenu`'s `Select Workspace` section was rendering `layouts.map(...)` in raw BFF order, so the default workspace appeared wherever the BFF chose to return it and pinned workspaces were interleaved with non-pinned ones based on BFF order. Operator wants: (1) default first; (2) pinned next in `getPinnedWorkspaces()` order (the persisted-localStorage order they were pinned in); (3) remaining unpinned, non-default layouts last in BFF list order. No numbering displayed. | **RESOLVED (2026-05-22 / task 102, Wave-2-R7 commit 2 of 3)** | Low | **Resolved (`React.useMemo` computing `orderedLayouts`)**: in `WorkspacePaneMenu.tsx`, added a memo deriving `orderedLayouts` from `(layouts, activeLayout, pinnedIds)` using the cascade: **(1)** `defaultEntry = defaultId ? layouts.filter(l => l.id === defaultId) : []`; **(2)** `pinnedEntries = pinnedList.filter(p => p.layoutId !== defaultId).map(p => layouts.find(l => l.id === p.layoutId)).filter(Boolean)`; **(3)** `remaining = layouts.filter(l => l.id !== defaultId && !pinnedIdSet.has(l.id))`. Concatenated `[...defaultEntry, ...pinnedEntries, ...remaining]`. JSX `layouts.map(...)` replaced with `orderedLayouts.map(...)`; `isLoading` and `layouts.length === 0` empty-state branches still check the raw `layouts` array. **Decision (`activeLayout` vs `WorkspaceLayoutDto.isDefault`)**: BFF DTO carries `isDefault: bool`, but using `activeLayout` is cleaner — it already encodes the WORKING selection cascade (first-default → first-system → first-layout) from `useWorkspaceLayouts.ts` AND respects the user's runtime "switch" via `setActiveLayoutById`. If the user has switched to a non-default workspace within the session, `activeLayout` reflects that switch and the dropdown shows their current selection at the top, which is the desired UX. Using raw `isDefault` would always show the persisted default regardless of session state. Both are operator-acceptable; `activeLayout` is the cleaner choice. **Empty-state safety**: `getPinnedWorkspaces()` returns `[]` if storage is unavailable / corrupted → section 2 contributes nothing (degrades to default-first-then-BFF-order); `activeLayout` is `null` on cold load until the hook resolves → section 1 contributes nothing (degrades to BFF order). No crash, no empty-state regression. Memo deps `[layouts, activeLayout, pinnedIds]` ensure re-sort on any of those changing — including a pin toggle (since `pinnedIds` flips). **Bundle delta**: included in R-34's combined +0.23 KB measurement (~80 bytes for the useMemo + cascade logic). ADR-021 (no styling change), ADR-022 (React 19 — `useMemo` with proper deps; no other hook changes), ADR-028 (no auth surface change) all complied with. NOT deployed in this task — Wave-2-R7 commit 2 of 3; coordinated deploy by main session after Wave-2-R7 trio lands. See deploy memo "Task 102 — Workspace dropdown small fixes". |
| R-37 | Context Tools dropdown visual clutter — type icons on the LEFT of each MenuItem (`AppsListRegular` for Quick Start, `SearchRegular` for Semantic Search) duplicated information already conveyed by the tool name; combined with the task 099 pin column they made each row read `[type icon] [pin] [tool name] [active-check]` which the operator (Round 7, 2026-05-22) flagged as visually noisy: "For the Context tools drop down remove the icons (only leave the pin)." The type icons were purely decorative — only the pin (functional toggle for default-tool) and the active-marker checkmark (indicates which tool is currently rendered in the pane body) needed to remain. | **RESOLVED (2026-05-22 / task 103, Wave-2-R7 commit 3 of 3)** | Low | **Resolved (`src/solutions/SpaarkeAi/src/components/context/ContextPaneMenu.tsx` — three surgical edits)**: **(1)** dropped `AppsListRegular` + `SearchRegular` from the `@fluentui/react-icons` named imports (kept `ChevronDownRegular`, `CheckmarkRegular`, `PinRegular`, `PinFilled` — all still in use); **(2)** removed `icon: React.ReactElement` from `ToolDescriptor` interface + removed the `icon: <AppsListRegular />` / `icon: <SearchRegular />` properties from both entries in `CONTEXT_TOOLS`; **(3)** simplified the `MenuItem.icon` prop from `isActive ? <CheckmarkRegular /> : tool.icon` to `isActive ? <CheckmarkRegular /> : undefined` — Fluent v9 collapses the icon slot entirely when undefined, so the row now reads `[pin button] [tool name] [active checkmark]`. **What did NOT change**: pin column styling + behaviour (task 099 contract preserved); active-marker checkmark rendering when tool selected; the dropdown's MenuTrigger `<ChevronDownRegular />` icon (different concern — that's the dropdown affordance, not a per-tool icon); `useContextTool.ts` (Task 101 sibling — out of scope); LegalWorkspace (FR-25 / NFR-10 preserved). Bundle delta included in task 103's combined +0.42 KB SpaarkeAi measurement. ADR-021 / ADR-022 / ADR-025 / ADR-028 all complied with. See deploy memo "Task 103 — Context pane polish". |
| R-38 | Quick Start cards non-responsive — `GetStartedCardsWidget` used a fixed 2-column grid (`gridTemplateColumns: '1fr 1fr'`) which made sense at the default Context-pane width (~400px) but looked bad after task 100 (collapsed strip top-alignment + width redistribution) enabled the Context pane to fill the entire main-area width when Assistant + Workspace were both collapsed. The 7 cards stretched into 2 very-wide columns instead of arranging into 3-5 narrower columns to use the available horizontal space — operator (Round 7, 2026-05-22): "For the Context tools, the Quick Start cards can have responsive layout based on a reasonable standard card size as the screen resolution increases (or decreases)." | **RESOLVED (2026-05-22 / task 103, Wave-2-R7 commit 3 of 3)** | Low | **Resolved (`src/client/shared/Spaarke.AI.Widgets/src/widgets/context/GetStartedCardsWidget.tsx` — Option B from task brief: change the grid in the WIDGET itself, not the wrapper, so the responsive behavior applies to ALL consumers of the widget by default)**: **(1)** grid CSS switched from `gridTemplateColumns: '1fr 1fr'` to `gridTemplateColumns: 'repeat(auto-fill, minmax(160px, 1fr))'`. Narrow Context pane (~400px) resolves to 2 columns — preserves pre-task-103 behavior. Wide Context pane (when Assistant + Workspace are both collapsed via task 100) auto-fills 3, 4, or 5 columns based on width. 160px minimum chosen to match the visual size of the previous fixed 2-col layout at the default pane width — prevents cards from shrinking below a usable touch target. **(2)** keyboard navigation updated: `moveFocus()` previously hard-coded ±2 for ArrowUp / ArrowDown (assuming 2-column layout). Replaced with a runtime DOM measurement: new `getColumnsPerRow()` helper walks the rendered `[data-card-index]` wrappers and counts how many share the same `getBoundingClientRect().top` (with 1px tolerance for sub-pixel rounding) — that's the column count in row 1. Arrow nav now uses `fromIndex ± columnsPerRow` and stays correct at any column count. **(3)** JSDoc header updated to document the responsive grid behavior + the 160px minimum rationale + the keyboard-nav DOM measurement approach. **LegalWorkspace impact verified**: `Grep` confirmed LegalWorkspace consumes neither `@spaarke/ai-widgets` nor `GetStartedCardsWidget` — LegalWorkspace has its OWN `GetStarted` widget in `src/solutions/LegalWorkspace/src/components/GetStarted/` (different code path). LegalWorkspace bundle **569.38 KB gzip** — byte-identical to task 100 baseline (no consumer impact). **What did NOT change**: `ActionCard` shared component (each card's internal rendering — only the grid container around them); the `CARDS` array (still 7 cards in the same order); `onCardClick` API (still the public surface; callback unchanged); ContextPaneController wrapper (Option A was rejected per task brief — the widget's own grid is the right place for responsive behavior). Bundle delta included in task 103's combined +0.42 KB SpaarkeAi measurement. ADR-012 (responsive grid placed correctly in the SHARED widget so all consumers benefit — not duplicated per consumer), ADR-021 (tokens preserved — `spacingHorizontalS/M`; the `'repeat(auto-fill, minmax(160px, 1fr))'` is a CSS layout literal with no matching token, exemption documented), ADR-022 (React 19 — `getColumnsPerRow` is a plain function, no new hooks). See deploy memo "Task 103 — Context pane polish". |
| R-39 | Semantic Search Criteria stretched across full screen at wide pane width — when the Context pane was full-width (Assistant + Workspace both collapsed via task 100), the `SemanticSearchCriteriaTool` (task 095/099) form-style criteria (header + type buttons + AI query textarea + filter dropdowns + date range + Search button — all single-column) stretched edge-to-edge of the pane. Form fields that were comfortable at ~400px pane width became visually unbalanced at 1200-1600px main-area width because labels and dropdowns spanned the whole screen. Operator (Round 7, 2026-05-22): "for the Semantic Search tool the responsive should be limited so does not stretch across the entire screen. As other tools are added we'll need to address responsiveness." The implicit ask was a max-width cap on form-style tools while leaving narrower-pane behavior unchanged. | **RESOLVED (2026-05-22 / task 103, Wave-2-R7 commit 3 of 3)** | Low | **Resolved (`src/solutions/SpaarkeAi/src/components/context/SemanticSearchCriteriaTool.tsx` — max-width cap on inner wrapper + JSDoc TODO for future tools)**: **(1)** added `CRITERIA_MAX_WIDTH_PX = 480` constant at the top of the styles section with an inline rationale (chosen to mirror a typical Fluent v9 form-panel width — wide enough for two filter dropdowns to feel uncramped, narrow enough that the criteria doesn't span more than ~30% of a 1600px monitor). **(2)** outer `root` styles still fill the pane (background + outer padding + border + rounded corners cover the full pane width for visual consistency). New inner `criteria` style: `display: 'flex'`, `flexDirection: 'column'`, `width: '100%'`, `maxWidth: '${CRITERIA_MAX_WIDTH_PX}px'`, `marginLeft: 'auto'`, `marginRight: 'auto'`, `gap: spacingVerticalM`. **(3)** JSX wrapped form content in a `<div className={styles.criteria}>` between `<div className={styles.root}>` and the `Search Criteria` header so the cap applies to the form content only. **Narrow pane (~400-480px)**: cap doesn't activate — `max-width` only constrains, never shrinks below natural width; criteria fill the pane as before. **Wide pane (>~520px including outer padding)**: criteria stay centered at 480px instead of stretching. **(4)** TODO breadcrumb in JSDoc header reminding future Context tools (Search Saved, Filter Library, etc.) to apply the same width-cap pattern — operator caveat: "As other tools are added we'll need to address responsiveness." The TODO names the pattern (not a singleton — each tool decides its own cap based on its content: dense table tools may not need a cap; form tools should mirror this 480px). **What did NOT change**: criteria content + behavior (still 4 type buttons in a 2×2 grid + AI query textarea + per-domain filter dropdowns + date range + full-width Search button — task 099 contract preserved); localStorage persistence (`spaarke:context:semantic-search-criteria`); modal launch shape (`Xrm.Navigation.navigateTo` to `sprk_semanticsearch` with URL params); BFF / auth surfaces (none touched). Bundle delta included in task 103's combined +0.42 KB SpaarkeAi measurement. ADR-012 (SpaarkeAi-local — correct), ADR-021 (Fluent v9 tokens — `spacingVerticalM`; `'480px'` is a layout literal with no matching token, same exemption documented in CLAUDE.md style as other intentional px constants like task 096's 48px collapsed strip width), ADR-022 (React 19 functional), ADR-028 (no auth surface change) all complied with. See deploy memo "Task 103 — Context pane polish". |
| R-40 | Manage Workspaces side pane had NO Save or Close button — operator (Round 7 final block, 2026-05-22): "The Manage workspace does not have a 'Save' button or 'Close' button." Task 093's pane provided only the `<DrawerHeader>` ✕ button + scrim/Escape dismissal — no explicit footer affordances. Operator-UX expectation (MDA "manage view" pattern) is a dual-button footer with Cancel + Save, even when all operations commit instantly, because the buttons function as visible exit affordances and signal the "panel state is in your hands" mental model. | **RESOLVED (2026-05-22 / task 104, Wave-3-R7)** | Low | **Resolved (`src/solutions/SpaarkeAi/src/components/workspace/ManageWorkspacesPane.tsx` — added explicit footer)**: new right-justified `<div className={styles.footer}>` rendered OUTSIDE the `<DrawerBody>` (so it doesn't scroll with the list) containing `Cancel` (secondary) + `Save` (primary, focus-default) buttons. Footer styles: `paddingTop/Bottom: spacingVerticalM`, `paddingLeft/Right: spacingHorizontalM`, `borderTop: 1px solid colorNeutralStroke2`, `gap: spacingHorizontalS`, `flexShrink: 0`. Both buttons call `onOpenChange(false)`. **Instant disclaimer**: all operations in the pane (pin, unpin, set-default, move, rename, delete) are committed instantly to localStorage / BFF — neither button reverts or "applies" anything. Tooltips document this: Cancel — "Close without applying further changes."; Save — "Close the manage panel." Component header documents the future-proofing for a possible transactional mode (Cancel reverts, Save commits). The `<DrawerHeader>` ✕ button remains as a third dismissal path (matches MDA behavior). See deploy memo "Task 104 — Manage Workspaces MDA-style revamp". |
| R-41 | Pin always visible — no hover-only / "visible only when pinned" semantics — operator (Round 7 final block, 2026-05-22): "the 'hide' is a hover-over (for the workspace this is 'pin'); if a workspace is pinned then the pin will show (not just on hover); user needs to see the pin and then can unpin it." Task 093's `ManageWorkspacesPane` rendered the pin star unconditionally for every row, regardless of pin state. Operator-UX expectation mirrors the MDA "manage view" hide affordance: the action icon is only present on hover for items in their default (unpinned) state, but becomes persistent once the user has applied the action — making "currently pinned" scannable without hover. | **RESOLVED (2026-05-22 / task 104, Wave-3-R7)** | Low | **Resolved (`src/solutions/SpaarkeAi/src/components/workspace/ManageWorkspacesPane.tsx` — pin icon visibility tied to pin state + row hover)**: pin icon now follows three-state visibility: (1) unpinned + row not hovered → `opacity: 0` (still occupies space so the layout doesn't shift on hover); (2) unpinned + row hovered → `opacity: 1`, `PinRegular` outline at `colorNeutralForeground3`; (3) pinned (any hover state) → `opacity: 1`, `PinFilled` solid at `colorBrandForeground1` — persistent, "user can SEE the pin and unpin it." Implementation uses Griffel's `":hover .manage-pin-hover-only"` selector with a static class name hook alongside the Griffel-generated class (Griffel does not generate stable class names for `:hover .child` selectors). Also surfaces the pin on `:focus-within` so keyboard-focus into the row's menu trigger reveals it. The pin Button click stops propagation (so the surrounding row-click which opens the workspace as a tab does NOT also fire) and toggles pin state via `pinWorkspace`/`unpinWorkspace`. See deploy memo "Task 104 — Manage Workspaces MDA-style revamp". |
| R-42 | No three-dot (⋯) action menu — actions cluttered the row with inline icons — operator (Round 7 final block, 2026-05-22): "the other manage functions are in the three dot '...' menu drop down (which for workspace is pin, set as default, delete, edit)." Task 093's pane rendered Edit + Delete as inline icons next to each row — visually noisy AND not consistent with the MDA "manage view" pattern where management actions are consolidated into a ⋯ menu per row, leaving the row body free for the primary content (workspace name + pin status). | **RESOLVED (2026-05-22 / task 104, Wave-3-R7)** | Low | **Resolved (`src/solutions/SpaarkeAi/src/components/workspace/ManageWorkspacesPane.tsx` — inline Edit + Delete replaced by ⋯ Menu with 6 items)**: every row now ends with a `<Menu>` triggered by a subtle `<Button icon={<MoreHorizontalRegular />}>` (click stops propagation so the menu opens without firing row-click). MenuPopover contains six items in this order: (1) Pin / Unpin (label toggles by state, icon `PinFilled`/`PinRegular`); (2) Set as default — `StarRegular`, calls new `moveWorkspaceToTop(layoutId, layoutName)`, disabled if `isDefault` with Tooltip "Already the default workspace."; (3) Move up — `ArrowUpRegular`, swaps with pinned index N-1 via `setPinnedWorkspacesOrder`, disabled if not pinned or `pinIdx <= 0` (Tooltip: "Pin this workspace first to reorder it." / "Already at the top."); (4) Move down — `ArrowDownRegular`, swaps with N+1, disabled if not pinned or at bottom; (5) Edit — `EditRegular`, launches `sprk_workspacelayoutwizard` via `launchEditWizard` with `templateFilter=SPAARKEAI_TEMPLATE_FILTER` (saveAs for system, edit-in-place for user); (6) Delete — `DeleteRegular`, opens the existing confirmation `<Dialog>`, disabled for system layouts with Tooltip "System layouts can't be deleted — use Edit then Save As to clone." Disabled `MenuItem`s use `aria-disabled` + guarded `onClick` (no-op when disabled) so keyboard activation can't bypass the disabled state. Inline Edit + Delete icons removed from row body. See deploy memo "Task 104 — Manage Workspaces MDA-style revamp". |
| R-43 | No reorder controls and no "default" concept distinct from pin — operator (Round 7 final block, 2026-05-22): "need to allow 'up arrow' and 'down arrow' to reorder the pinned workspaces — this is the order as used in the workspace when it loads. Put the pinned at the top in the current order (do not need numbers — the default will be first, and then the order as pin/saved/persisted)." Task 093's pane had NO reorder UI and treated "pinned" as a single boolean (no notion of "which pinned workspace is the default that loads first"). The cold-load auto-open effect (task 092 → fixed in task 101) ALREADY iterated `getPinnedWorkspaces()` in array order, so the storage layer supported ordering — but the UI layer never exposed it. | **RESOLVED (2026-05-22 / task 104, Wave-3-R7)** | Low | **Resolved (`src/solutions/SpaarkeAi/src/components/workspace/ManageWorkspacesPane.tsx` + `src/solutions/SpaarkeAi/src/services/pinnedWorkspaces.ts` — explicit ordering UI + "default = first in pinned list" semantics)**: **(1)** ZERO new storage keys. The pinned list at `localStorage["spaarke:workspace:pinned-list"]` (JSON array of `{layoutId, layoutName}`) IS ordered, and INDEX 0 IS THE DEFAULT. This avoids introducing a parallel `default-id` key and reuses task 101's auto-open effect verbatim (it already iterates `getPinnedWorkspaces()` in array order, so "default opens first, then pinned in user-ordered sequence" is automatic). **(2)** Two new exports in `services/pinnedWorkspaces.ts`: `setPinnedWorkspacesOrder(list)` writes the supplied array verbatim with defensive de-dup on `layoutId` + filter of malformed entries; `moveWorkspaceToTop(layoutId, layoutName?)` pins if not pinned + moves to index 0 (`layoutName` REQUIRED when promoting an unpinned workspace; reads existing entry's name otherwise; bails if both missing). Both follow the same try/catch posture as existing exports — silent failure on private-browsing / quota errors. **(3)** Three new menu actions wire the ordering UI: "Set as default" → `moveWorkspaceToTop`; "Move up" → swap with pinned index N-1 via `setPinnedWorkspacesOrder`; "Move down" → swap with N+1. Disabled states surface the constraints via tooltips. **(4)** The list displays in two sections when both have content: "Pinned" section (rows in `getPinnedWorkspaces()` order, index 0 carries a Fluent v9 `<Badge appearance="tint" color="brand">Default</Badge>` inline with the name — chosen over a left-side star icon because the pin column already conveys "pinned" so a second left-side icon would visually compete; the badge sits inline with the name so it scans naturally); divider rule (`1px solid colorNeutralStroke2`); "All workspaces" section (unpinned rows in BFF return order). Empty pinned list → no headers, no divider — flat list. Operator's "do not need numbers" satisfied: the Default badge IS the marker; subsequent ordering is implicit by position. See deploy memo "Task 104 — Manage Workspaces MDA-style revamp". |
| R-44 | Unwanted popup(s) auto-launching on SpaarkeAi cold load (Daily Briefing modal + `webresourceName=sprk_spaarkeai` self-popup) — operator (Round 8 final block, 2026-05-22): "Remove the Daily Briefing popup when the app loads." + "This also popped up when the app loaded — not sure what it is: `webresourceName=sprk_spaarkeai`." The Daily Briefing modal observation has an obvious cause (a popup labelled "Daily Briefing"); the second observation initially looked like a self-popup (SpaarkeAi opening itself inside itself). Investigation showed both are the SAME single root cause — LegalWorkspace's `useDailyDigestAutoPopup` hook firing automatically when the embedded `LegalWorkspaceApp` mounts inside a pinned-workspace tab on SpaarkeAi cold load. The operator's `webresourceName=sprk_spaarkeai` URL reading was the host page URL UNDER the modal (the modal opens on top of SpaarkeAi, so the URL bar still reads SpaarkeAi). Codebase audit confirmed `sprk_spaarkeai` as a navigateTo target is referenced ONLY in `launch-resolver.ts::openSpaarkeAi`, called solely from the two ribbon scripts (never from SpaarkeAi runtime UI) — there is no self-popup bug. The Daily Briefing popup originates from `src/solutions/LegalWorkspace/src/hooks/useDailyDigestAutoPopup.ts` which opens `sprk_dailyupdate` (title literal: "Daily Briefing") as a `target: 2` modal via `Xrm.Navigation.navigateTo` on first mount per browser session, called unconditionally from `LegalWorkspace/src/components/Shell/WorkspaceGrid.tsx:154` (`useDailyDigestAutoPopup({ webApi, userId });`). When SpaarkeAi cold-loads with any pinned workspace, task 101's auto-open effect dispatches `widget_load → WorkspaceLayoutWidget → LegalWorkspaceApp (embedded) → WorkspaceGrid → useDailyDigestAutoPopup`. The user sees a "Daily Briefing" modal automatically appearing inside SpaarkeAi. | **RESOLVED (2026-05-22 / task 105, Wave-1-R8 commit 1 of 3)** | Low | **Resolved (`src/solutions/SpaarkeAi/src/main.tsx` — pre-React sessionStorage sentinel suppression)**: set the hook's existing `sessionStorage` sentinel `spaarke_dailyDigestShown` synchronously in SpaarkeAi's `bootstrap()` BEFORE any React tree mounts. New helper `suppressLegalWorkspaceDailyDigestAutoPopup()` writes value `"suppressed-by-spaarkeai"` IFF the key is not already present (preserves operator-side "opted-out" or prior "shown" values from earlier in the same session). The hook's existing guard (`if (sessionStorage.getItem(SESSION_KEY)) return;` at `useDailyDigestAutoPopup.ts` lines ~65-70) then short-circuits before the modal launches. Wrapped in try/catch — silent degradation for private browsing / sandboxed iframe contexts (the hook's own try/catch handles those same cases identically, so behavior is consistent). The call is the FIRST line inside `bootstrap()` (before `resolveRuntimeConfig`), so it runs synchronously well before any React mount could trigger an embedded LegalWorkspaceApp tree. **Why this scope is correct**: (1) LegalWorkspace source files UNTOUCHED — FR-25 / NFR-10 preserved by construction. (2) Standalone LegalWorkspace browser tab has its OWN sessionStorage — the suppression flag set inside SpaarkeAi's tab does not propagate to the standalone Workspaces app tab; standalone Daily Briefing modal behavior unchanged. (3) The inline Daily Briefing section in SpaarkeAi's Workspace Home tab (task 086) does NOT use `Xrm.Navigation.navigateTo` — it renders via `createDailyBriefingRegistration` + `WorkspaceShell` inline; operator still sees Daily Briefing content INLINE on the Home tab, only the auto-launched MODAL is suppressed. **Alternative considered**: modify `WorkspaceLayoutWidget.tsx` in `@spaarke/ai-widgets` to set the flag — rejected because (a) the shared lib could have other consumers in the future, (b) the suppression is a SpaarkeAi product decision not a widget-level decision, (c) `main.tsx` is the natural place for solution-wide bootstrap-time setup that affects every embedded subtree. Bundle delta: SpaarkeAi **+0.60 KB / +614 bytes** (901.21 → 901.81 KB gzip vs task 104 baseline; bytes dominated by the extensive cross-package suppression docblock; executable code is ~10 lines). LegalWorkspace not rebuilt. ADR-022 (React 19 — pure pre-React side effect inside `bootstrap()`; no new hooks), ADR-028 (no auth surface change — `sessionStorage` writes are pure client-side; do not touch MSAL / tokens / BFF), CLAUDE.md §10 (zero BFF changes) all complied with. See deploy memo "Task 105 — Popup auto-launch investigation + removal". |
| R-45 | Semantic Search criteria centered (not left-aligned) on wide Context pane — Round 7 (task 103) capped the `SemanticSearchCriteriaTool` criteria container at `maxWidth: 480px` and centered it within the pane using `marginLeft: 'auto'` + `marginRight: 'auto'` so the form-style surface didn't stretch across the screen when Assistant + Workspace were both collapsed (task 100 expanded layout). Round 8 operator feedback (2026-05-22): "In the Context pane, the semantic search component should be left aligned (see screenshot)." Operator wanted the 480px cap retained (so the form still doesn't stretch on wide layouts) but the horizontal anchoring changed from CENTERED to LEFT-aligned — form anchors to the pane's left edge with empty space on the right only. | **RESOLVED (2026-05-22 / task 106, Wave-1-R8 commit 2 of 3)** | Low | **Resolved (`src/solutions/SpaarkeAi/src/components/context/SemanticSearchCriteriaTool.tsx` — single Griffel style edit + JSDoc update)**: in the `criteria` Griffel style entry, flipped `marginLeft: 'auto'` → `marginLeft: 0` while leaving `marginRight: 'auto'` in place. With `width: 100%` + `maxWidth: 480px` already present, this makes the criteria fill up to 480px of width and anchor to the LEFT edge of the outer `root` wrapper (against the existing left padding from `paddingLeft: tokens.spacingHorizontalM`); empty space sits on the right when the pane is wider than the cap. `CRITERIA_MAX_WIDTH_PX = 480` constant unchanged. JSDoc on the `criteria` style entry and the rationale block above it rewritten to describe the new "left-aligned, max-width 480px so the form anchors to the pane's left edge instead of stretching or centering on wider screens" behavior + cite the operator feedback. The inline JSX comment on the wrapper div is also updated. Narrow pane (~400px) behavior unchanged — max-width only constrains, never shrinks below natural width. Bundle delta included in task 106's combined +0.60 KB SpaarkeAi measurement. ADR-012 (SpaarkeAi-local — correct; criteria max-width discipline is solution-specific), ADR-021 (Fluent v9 tokens — `marginLeft: 0` is a CSS-spec literal, same exemption as `flex: 1` / `width: 100%`), ADR-022 (React 19 — no hook changes), ADR-028 (no auth surface change) all complied with. See deploy memo "Task 106 — Context pane: Semantic Search left-align + drop active checkmark". |
| R-46 | Redundant active-tool checkmark on Context Tools dropdown — Round 7 (task 103) cleaned up the dropdown by removing the left-side type icons but KEPT the `CheckmarkRegular` icon rendered on whichever MenuItem was the currently-selected tool. Round 8 operator feedback (2026-05-22): "'Active tool marker' I don't think this is really necessary — we know what's active because it's what's loaded and there can only be one. Can remove that icon." The active-tool selection is already visible to the user via the pane body (what's currently rendered IS the active tool), so the checkmark added no information and contributed to visual clutter. Operator wanted only the pin icon (functional toggle for the default-on-load tool) to remain per the R7 "only leave the pin" directive. | **RESOLVED (2026-05-22 / task 106, Wave-1-R8 commit 2 of 3)** | Low | **Resolved (`src/solutions/SpaarkeAi/src/components/context/ContextPaneMenu.tsx` — four surgical edits)**: **(1)** dropped `CheckmarkRegular` from the `@fluentui/react-icons` named imports (verified no other usages via Grep — only `ChevronDownRegular`, `PinRegular`, `PinFilled` remain). **(2)** removed the `activeMarker` Griffel style entry from `useStyles` (no other consumers — also verified via Grep). **(3)** removed the `isActive = tool.id === selectedTool` const computation in the row renderer (unused after the icon removal). **(4)** the MenuItem `icon` prop now passes `undefined` unconditionally — Fluent v9 collapses the icon slot entirely so the row reads `[pin button] [tool name]`. The pin column behavior is preserved verbatim (task 099 contract intact — pin click stops propagation, `aria-pressed` flips with `pinnedToolId`, tooltip switches between "Pin {tool} as default" / "Unpin {tool}"). The file header JSDoc + the inline catalog comment above `ToolDescriptor` are updated to document the change + cite the operator rationale. The `selectedTool` prop is kept on `ContextPaneMenuProps` for interface stability — the parent `ContextPaneController` still passes it from `useContextTool` — with a JSDoc note explaining it's now a no-op consumer parameter (could be deleted in a future cleanup pass if the call site is updated in lockstep). Bundle delta included in task 106's combined +0.60 KB SpaarkeAi measurement (the dropped CheckmarkRegular import contributes a small minified saving; Rollup chunk-boundary shifts can produce small +/- swings). ADR-012 (SpaarkeAi-local — correct; the Context tool dropdown is solution-specific, LegalWorkspace has no Context pane), ADR-021 (Fluent v9 tokens — no token changes; removed style was a token reference, dropping it is benign), ADR-022 (React 19 — no hook changes), ADR-025 (the dropped `CheckmarkRegular` was `@fluentui/react-icons` v9 — the right family), ADR-028 (no auth surface change) all complied with. See deploy memo "Task 106 — Context pane: Semantic Search left-align + drop active checkmark". |
| R-47 | Visible vertical scrollbar in Workspace pane content — operator (Round 8, 2026-05-22): "Remove the vertical scroll bar; user scrolls without needing to see scroll bar." The `<div className={styles.content}>` wrapper around `ActiveWidgetContent` in `WorkspaceTabManagerComponent.tsx` had a bare `overflow: 'auto'` which produced a native vertical scrollbar in the workspace content area whenever a widget's rendered content exceeded the pane height. Operator wanted the scrollbar invisible while scroll functionality (wheel / trackpad / keyboard) remained intact. The Assistant pane chat scroll is intentionally visible (chat UX) and the Context pane is owned by sibling task 106 — fix had to be surgically scoped to the Workspace pane content wrapper. | **RESOLVED (2026-05-22 / task 107, Wave-1-R8 commit 3 of 3)** | Low | **Resolved (`src/solutions/SpaarkeAi/src/components/workspace/WorkspaceTabManagerComponent.tsx` — `content` style)**: changed `overflow: 'auto'` to `overflowY: 'auto', overflowX: 'hidden'` + added `scrollbarWidth: 'none'` (Firefox) + `'::-webkit-scrollbar': { display: 'none' }` (Chromium / WebKit). The element remains scrollable (mouse wheel, trackpad, keyboard PgUp/PgDn, focus-based scroll-into-view all still work) but no visible bar renders. The fix is contained to the WorkspaceTabManagerComponent's `content` div — ConversationPane (Assistant chat scroll), ContextPaneController (task 106 scope), and LegalWorkspace internals (FR-25 / NFR-10) all left untouched. `WorkspacePane.tsx` has no content-scroll wrapper of its own; it delegates rendering below the PaneHeader to WorkspaceTabManagerComponent, so a single class change covers both surfaces. Bundle delta included in task 107's combined +0.57 KB SpaarkeAi measurement. ADR-021 / ADR-022 / ADR-028 all complied with. See deploy memo "Task 107 — Workspace pane: hide vertical scrollbar + tab overflow with ◀ ▶ arrow buttons". |
| R-48 | Horizontal scrollbar on Workspace tab overflow — operator (Round 8, 2026-05-22): "In the Workspace pane, when the number of tabs exceeds the width do not show, remove left / right scroll bar; instead show left right arrows to move across the tabs." Operator screenshot showed 7 workspace tabs overflowing the bar with a visible horizontal scrollbar — the `tabBar` style in `WorkspaceTabManagerComponent.tsx` had `overflowX: 'auto'` which produced the native bar whenever the tabs' total width exceeded the bar's clientWidth. Operator wanted chevron arrows at the start/end of the bar (mirroring standard browser-tab UX, e.g. Chrome) so the scrollbar would be invisible while overflow remained navigable. | **RESOLVED (2026-05-22 / task 107, Wave-1-R8 commit 3 of 3)** | Low | **Resolved (`src/solutions/SpaarkeAi/src/components/workspace/WorkspaceTabManagerComponent.tsx` — tab bar restructured to `[arrowLeft] [tabScroll] [arrowRight]` flex layout)**: **(1)** new `tabScroll` style — inner container with `overflowX: 'auto' + flexGrow: 1` plus the same hidden-scrollbar treatment as R-47's content fix (`scrollbarWidth: 'none' + '::-webkit-scrollbar': { display: 'none' }`); the TabList lives inside this container and scrolls horizontally without a visible bar. **(2)** outer `tabBar` style changed from `overflowX: 'auto'` to `overflow: 'hidden'` and is now purely a flex container for `[arrowLeft, tabScroll, arrowRight]`. **(3)** arrow buttons — Fluent v9 `<Button appearance="subtle" size="small">` with `ChevronLeft20Regular` / `ChevronRight20Regular` icons, new `arrowButton` style at 28×28px with token-color hover (`colorNeutralForeground3` → `colorNeutralForeground1` + `colorNeutralBackground3Hover`). Click handlers call `el.scrollBy({ left: ±200, behavior: 'smooth' })` on the scroll container. **(4)** visibility logic — `canScrollLeft = scrollLeft > 0`; `canScrollRight = scrollLeft + clientWidth < scrollWidth - 1` (1px subpixel tolerance). Three observers keep state in sync — (a) `scroll` listener on the scroll container (passive), (b) ResizeObserver on the scroll container (covers pane width changes from collapse/expand or window resize), (c) recompute effect keyed on `tabs` (covers add/close/rename/restore). Both arrows are ALWAYS rendered with `visibility: hidden + pointerEvents: none + tabIndex: -1 + aria-hidden: true` via a new `arrowHidden` class when unreachable, preventing tab-strip jitter as overflow state changes. **(5)** active-tab scroll-into-view — separate effect on `[activeTabId, tabs]` queries `[data-testid="workspace-tab-${activeTabId}"]` and calls `scrollIntoView({ inline: 'nearest', block: 'nearest', behavior: 'smooth' })` (no-op for already-visible tabs; only clipped tabs scroll). Deferred via `requestAnimationFrame` so layout settles before measuring; RAF cancellation on cleanup. **(6)** Accessibility — `aria-label="Scroll tabs left"` / `"Scroll tabs right"`; hidden arrows narrate as hidden. **Edge cases handled**: single tab + zero tabs (both arrows hidden — `scrollWidth ≤ clientWidth`), all tabs fit (same), active tab clipped (scroll-into-view brings it in), pane collapse→expand (ResizeObserver re-fires), mid-overflow tab add (recompute effect re-runs on `tabs` change). Bundle delta included in task 107's combined +0.57 KB SpaarkeAi measurement. ADR-021 / ADR-022 / ADR-025 (`ChevronLeft20Regular` / `ChevronRight20Regular` are `@fluentui/react-icons` v9) / ADR-028 all complied with. See deploy memo "Task 107 — Workspace pane: hide vertical scrollbar + tab overflow with ◀ ▶ arrow buttons". |
| R-49 | Hard-coded Home tab + no system workspace records in Dataverse — operator decision to adopt Option B (Dataverse-seeded system layouts) requires schema + seed script. Round 8 (2026-05-22) operator decision Option B: system workspaces (Daily Briefing renamed from Home, Smart To Do List, My Work, Documents) become real `sprk_workspacelayout` Dataverse records with `sprk_issystem=true`, NOT hard-coded frontend entries. This gives architectural unity — all workspaces flow through the same `widget_load → WorkspaceLayoutWidget → LegalWorkspaceApp(embedded) → section factories` pipeline. Pre-Option-B state: `WorkspaceHomeTab.tsx` was a hard-coded SpaarkeAi-local tab rendering Daily Briefing inline via `createDailyBriefingRegistration` + `WorkspaceShell`; the BFF's "system layouts" pattern was a code-constant DTO flag (`WorkspaceLayoutDto.IsSystem`) backed by `SystemWorkspaceLayouts.cs` (Corporate Workspace), never stored in Dataverse. To realize Option B: (1) `sprk_workspacelayout.sprk_issystem` Boolean column must exist; (2) 4 system records must be POSTed; (3) frontend (Wave 2b, separate task) must remove hard-coded `WorkspaceHomeTab` and wire auto-install of default system layout via `useWorkspaceLayouts` querying `sprk_issystem=true && sprk_isdefault=true`. | **RESOLVED (2026-05-22 / task 108, Wave-2-R8)** | High | **Resolved (`scripts/Deploy-SystemWorkspaceLayouts.ps1` + `scripts/system-layouts.json` — Path B direct Dataverse Web API, idempotent, schema + data)**: (1) **Schema half** — `Test-AttributeExists` for `sprk_issystem`; if missing, `POST EntityDefinitions(LogicalName='sprk_workspacelayout')/Attributes` with `BooleanAttributeMetadata` (`SchemaName=sprk_IsSystem`, default=false, Yes/No options) — `-SkipSchema` switch for re-runs against already-provisioned environments. Sleep 5s for metadata to settle. (2) **Data half** — 4 layouts externalized to `scripts/system-layouts.json`: Daily Briefing (`daily-briefing` section, isDefault=true, sort=0), Smart To Do List (`todo`, sort=1), My Work (`quick-summary`, sort=2), Documents (`documents`, sort=3) — all `single-column` template (4-row × 1-slot). For each: `GET ?$filter=sprk_name eq '<name>' and sprk_issystem eq true` → skip if found (default) / PATCH if `-Force` / `POST` if missing with `Prefer: return=representation` to capture GUID. (3) **`-WhatIf`** prints would-create/would-replace without writes. (4) **Reconnaissance** confirmed section factory IDs from `src/solutions/LegalWorkspace/src/sectionRegistry.ts` (corrections vs brief: `dailyBriefing` → `daily-briefing`; `smartToDo` → `todo`; `quickSummary` → `quick-summary`; `activeDocuments` → `documents` — all kebab-case in actual registry). Layout template `single-column` confirmed from shared lib `src/client/shared/Spaarke.UI.Components/src/components/WorkspaceShell/layoutTemplates.ts` (canonical IDs) — diverges from BFF's `AvailableTemplates` list (`1-column`/`2-column`/`3-column`/`3-row-mixed`) which is unused by clients; flagged as pre-existing follow-up. `sprk_sortorder` range fix: Dataverse rejects negative values (range 0-20); initial -4..-1 ordering changed to 0-3 (system layouts are concatenated BEFORE user layouts in `WorkspaceLayoutService.GetLayoutsAsync` regardless of sortOrder, so the absolute value doesn't drive ordering). **Path B chosen** over Path A (BFF POST) because the BFF's `CreateWorkspaceLayoutRequest` DTO has no `IsSystem` setter by design (`isSystem=true` is admin-only and not exposed to user callers); seeding is a one-time admin task; Path B matches existing `Deploy-SpaarkeAi.ps1` (web-resource updates) and `Create-ScopeCapabilityFields.ps1` (metadata changes) patterns. **Dev seed run (2026-05-22)**: [1/4] Daily Briefing → `6d41d537-fe55-f111-a824-3833c5ed9c11`; [2/4] Smart To Do List → `a49f9e3a-fe55-f111-a824-7ced8dd899c3`; [3/4] My Work → `06c9203e-fe55-f111-a824-3833c5ed9a2b`; [4/4] Documents → `07c9203e-fe55-f111-a824-3833c5ed9a2b`. Verification query `GET sprk_workspacelayouts?$filter=sprk_issystem eq true` returns 4 records. Idempotency confirmed (re-run reports "skipping" for all 4 with zero writes); `-Force -WhatIf` verified produces "would-replace" entries. **No frontend changes**: Wave 2b (future task) removes `WorkspaceHomeTab` + wires auto-install. Operator-verifiable smoke before frontend ships: maker portal → `sprk_workspacelayout` table → 4 new records with `Is System = Yes`; Advanced Find filter `Is System = Yes` returns 4 records; SpaarkeAi Workspaces dropdown displays 4 new entries (script-runner's view; cross-user default discovery deferred to Wave 2b). **CLAUDE.md §10** (BFF Hygiene): ZERO new BFF endpoints — direct Web API metadata + data writes. **Pre-existing issues flagged (NOT fixed here)**: (a) BFF's `AvailableTemplates` template-list divergence from shared-lib canonical list (unused by clients but should be deprecated); (b) `WorkspaceLayoutService.GetDefaultLayoutAsync` per-`ownerid` filtering means system defaults aren't visible cross-user (Wave 2b concern); (c) BFF DTOs have no `IsSystem` setter — correct security default, documented for Wave 2b awareness. ADR-028 (script uses `az login` token for direct Dataverse Web API; script-runner needs system administrator role for metadata creation). NOT deployed in this task — pure data/schema seed; no SpaarkeAi deploy. See deploy memo "Task 108 — Build + seed system workspace layouts (Dataverse, dev)". |
| R-50 | Wave 2a seeded 4 Dataverse system layouts but the BFF still served them only as user-owned records (per-`ownerid` filtering hid them from cross-user discovery), the frontend still installed the hard-coded `WorkspaceHomeTab` on every cold load (Option B's architectural unity not yet realized end-to-end), and the BFF accepted PUT/DELETE against `sprk_issystem=true` records (the client-side disable was the only barrier). Operator: "All workspaces flow through the same pipeline" — required (a) the BFF list endpoint to merge hard-coded + Dataverse-system + user sources cross-user, (b) the BFF default endpoint to honor `sprk_issystem=true && sprk_isdefault=true` as a global fallback so every authenticated user lands on Daily Briefing without a per-user default, (c) the frontend to drop the hard-coded Home tab and auto-install the BFF's resolved default via the standard widget pipeline, and (d) defense-in-depth server-side write protection against system-record mutations. | **RESOLVED (2026-05-22 / task 109, Wave-2-R8)** | High | **Resolved (`src/server/api/Sprk.Bff.Api/Services/Workspace/WorkspaceLayoutService.cs` + `Api/Workspace/WorkspaceLayoutEndpoints.cs` + `Api/Workspace/WorkspaceLayoutDtos.cs` BFF changes; `src/solutions/SpaarkeAi/src/components/workspace/WorkspacePane.tsx` frontend change + `WorkspaceHomeTab.tsx` deletion)**: **BFF (CLAUDE.md §10 Placement Justification: zero new endpoints; pure behavior extension of existing handlers; same DTO, same auth, same domain — belongs in `WorkspaceLayoutService`)**: (1) `WorkspaceLayoutDto.IsSystem` XML doc tightened to document SERVER-CONTROLLED semantics — request DTOs (`CreateWorkspaceLayoutRequest` / `UpdateWorkspaceLayoutRequest`) don't include the property so clients can't override it. (2) `GetLayoutsAsync` now merges three sources: hard-coded `SystemWorkspaceLayouts.All` (Corporate Workspace) → Dataverse `sprk_issystem=true` records (no ownerid filter — cross-user) → user-owned `sprk_issystem != true` records. New `QueryDataverseSystemLayoutsAsync` helper; updated `QueryUserLayoutsAsync` adds `sprk_issystem != true` to criteria; updated `MapToDto` reads `sprk_issystem` instead of hard-coding false; `SelectColumns` includes the new column; system + user fetches run in parallel via `Task.WhenAll`. (3) `GetDefaultLayoutAsync` four-step cascade (return type now `Task<WorkspaceLayoutDto?>`): per-user default → Dataverse system default (`sprk_issystem=true + sprk_isdefault=true`, cross-user, sortOrder ascending — Wave 2a's Daily Briefing) → hard-coded system flagged as global default (forward-compat) → null. Each step has its own try/catch for graceful degradation. (4) `GetLayoutByIdAsync` bypasses the ownerid check when `sprk_issystem=true` (system records are global). (5) `UpdateLayoutAsync` / `DeleteLayoutAsync` reject mutations against `existing.IsSystem=true` with "System workspaces cannot be modified/deleted." → endpoint handler returns 403 Forbidden. Defense-in-depth complement to client-side disable. (6) `GetDefaultLayout` endpoint handler returns `TypedResults.Ok<WorkspaceLayoutDto?>(null)` (200 with null body) for "no default" — frontend distinguishes from 5xx fetch failure. **Frontend**: (1) `WorkspaceHomeTab.tsx` DELETED (grep confirmed no consumers other than the removed import; other references in comments only). (2) `WorkspacePane.tsx` removed the `ensureHomeTab` mount effect + its `WorkspaceHomeTab` import; added auto-install-default effect that waits for `isAuthenticated`, reads `activeLayout` from `useWorkspaceLayouts({ bffBaseUrl, authenticatedFetch, isAuthenticated })` (the SpaarkeAi adaptation of LegalWorkspace's working hook — cascade pinned-id → BFF default → first-default/first-system/first-layout), guards re-runs via `autoInstalledDefaultRef`, skips if the layout is already open (NFR-09 tab restore) or pinned (task 101's pin auto-open handles it), otherwise dispatches `widget_load` with the standard `{ widgetType: 'workspace', widgetData: { layoutId, layoutName }, displayName: layoutName }` payload via `setTimeout(0)` macrotask deferral (identical pattern to task 101's pin auto-open) so the dispatch lands AFTER `usePaneEvent`'s subscription effect runs. Step 4 fallback: if `activeLayout === null`, no tab installs; user sees the empty pane and picks from the Workspaces dropdown. **Coordination with task 101 pin auto-open**: both effects use `setTimeout(0)`; macrotask dispatch order is default-install first, pins second. Default-install SKIPS dispatch if the default is pinned (pin effect handles it). FIFO eviction via `MAX_WORKSPACE_TABS = 8` applies. **Task 105 popup suppression**: `main.tsx`'s sessionStorage sentinel `spaarke_dailyDigestShown` is set BEFORE any React mounts (unchanged). When Daily Briefing renders as the cold-load tab via embedded LegalWorkspaceApp, the LegalWorkspace `useDailyDigestAutoPopup` self-suppresses — no popup. **Build**: SpaarkeAi 901.81 → 900.29 KB gzip (−1.52 KB; small decrease from removing the inline DailyBriefing registration in WorkspaceHomeTab). BFF builds with 0 errors. **Deploys**: BFF via `Deploy-BffApi.ps1` to `spe-api-dev-67e2xz` (package 75.2 MB, healthz passed, file SHA-256 verified); SpaarkeAi via `Deploy-SpaarkeAi.ps1` (resource ID `5206a442-3451-f111-bec7-7ced8d1dc988`, customizations published). **ADR compliance**: ADR-021 / ADR-022 / ADR-028 (auth via `useAiSession()`, auto-defer until ready) all complied with; FR-25 / NFR-10 (zero LegalWorkspace files modified). **NOT touched**: `WorkspaceTabManager.ts` (Home tab primitive preserved for future use; no SpaarkeAi consumer today), `WorkspaceTabManager.test.ts` (tests still pass — they cover the primitive). **Pre-existing follow-ups still flagged**: BFF `AvailableTemplates` divergence (unused by clients; future cleanup); long-term Wave-4 candidate to migrate Corporate Workspace from `SystemWorkspaceLayouts.cs` code constant to a 5th Dataverse system record so ALL system layouts live in one source. See deploy memo "Task 109 — BFF system-layout merge + frontend Home removal + auto-install default". |
| R-51 | My Work system layout requires QuickSummary to surface sprk_communication + sprk_invoices counts (operator request 2026-05-22) — Round 8 Wave 3a operator brief: "Create a new 'system workspace' widget called **My Work**; it uses the components from the legalworkspace's **'Quick Summary'** (exact functionality with the cards My Matters, My Projects, Assign Work, Open Tasks; click opens the entity modal view); add card for sprk_communication entity; add card for sprk_invoices entity; the layout is 2x3." The "My Work" Dataverse layout already exists (Wave 2a task 108 seeded it as `sprk_workspacelayout` with `sprk_issystem=true` and `sectionsJson` referencing the `quick-summary` section factory) — but the existing `QuickSummary` section only renders 4 cards in a flex-wrap row, not the operator-requested 6 cards in a 2x3 grid. Because Round 8 Option B unified both surfaces (standalone Corporate Workspace code page AND SpaarkeAi-embedded "My Work" tab) onto the same `quick-summary` section factory, extending the section adds the 2 cards to BOTH surfaces — FR-25's byte-identical clause no longer applies to Wave 3 LegalWorkspace changes; reduced to a behavioral guarantee that no existing Quick Summary card REGRESSES. | **RESOLVED (2026-05-22 / task 110, Wave-3-R8 commit 1 of 2)** | Medium | **Resolved (`src/solutions/LegalWorkspace/src/components/QuickSummary/quickSummaryConfig.ts` + `QuickSummaryRow.tsx` + `src/solutions/LegalWorkspace/src/sections/quickSummary.registration.ts` — pure config + CSS edits; Xrm.WebApi data path)**: **(1)** added 2 entries to `QUICK_SUMMARY_CARDS` array — Communications (`entityName: sprk_communication`, `primaryKey: sprk_communicationid`, `viewId: 2bf1c5a5-0eca-4f37-92df-2e3c386dee98` = "Active Communications" system saved-query, `icon: MailRegular` from `@fluentui/react-icons` — already imported elsewhere in LegalWorkspace so no new icon split; `countFilter: buildOwnerFilter(ctx) + statecode eq 0`; `badgeType: "new"` with 7-day createdon window matching My Matters / My Projects pattern) and Invoices (`entityName: sprk_invoice`, `primaryKey: sprk_invoiceid`, `viewId: 2220a3bc-330e-4405-b441-3605df961878` = "Active Invoices" system view isdefault=true, `icon: ReceiptRegular` — already imported elsewhere; same filter + badge pattern). Click handler reuses existing `navigateToEntityList(entityName, viewId)` which calls `Xrm.Navigation.navigateTo({ pageType: 'entitylist', entityName, viewId }, { target: 2, width/height 80% })` — opens entity list as modal dialog per operator's "click opens the entity modal view" spec. **(2)** `useQuickSummaryCounts` hook is generic over `QUICK_SUMMARY_CARDS`; adding entries auto-extends the parallel count queries — zero hook changes needed. **(3)** Grid layout change in `QuickSummaryRow.tsx` Griffel `row` style: flex-wrap row → CSS grid `gridTemplateColumns: repeat(2, minmax(0, 1fr))` so the 6 cards arrange as 2x3; `gap: spacingHorizontalL` + `rowGap: spacingVerticalL` for proper vertical spacing; `width: 100%`. The `minmax(0, 1fr)` lets columns shrink below content's intrinsic width on narrow viewports while keeping cards readable (each card has `minWidth: 120px` internally). **(4)** `quickSummary.registration.ts` `defaultHeight` bumped from `"180px"` (sized for the 4-card single-row R6 layout) to `"440px"` to fit 3 rows of 120px cards + gaps + section padding. **Data path: Xrm.WebApi direct** — zero new BFF endpoints. CLAUDE.md §10 governance satisfied without a Placement Justification section because no BFF code path was added. **Embed safety**: works in BOTH standalone Corporate Workspace code page AND SpaarkeAi-embedded "My Work" tab because both surface `Xrm.WebApi` + `Xrm.Navigation` through the existing frame-walk pattern in `WorkspaceLayoutWidget` (task 087 Round 4 Fix 4). **Entity logical names verified via Dataverse MCP `list_tables` + `read_query`**: `sprk_communication` is the canonical singular logical name (the `sprk_communications` plural is the entity set / collection-binding name); `sprk_invoice` is canonical singular. Both have standard `statecode` (0=Active) and writeable `ownerid` lookup. **FR-25 evolution**: standalone Corporate Workspace code page (`sprk_corporateworkspace`) now ships 6 cards instead of 4 — intentional Option B architectural-unity payoff; behavioral guarantee preserved (the 4 Round-6 cards retain identical config — entity, view, filter, badge logic). Bundle delta: LegalWorkspace **+0.21 KB gzip** (569.38 → 569.59); SpaarkeAi **+0.15 KB gzip** (900.29 → 900.44 — LegalWorkspaceApp is transpiled into SpaarkeAi's bundle per task 087 Vite config). **Pre-existing follow-ups still flagged**: (a) stale `.js`/`.d.ts` siblings of `.ts` files throughout LegalWorkspace `src/` (Vite default resolve order prefers `.js`; this required manually removing the stale QuickSummary + quickSummary.registration siblings in the worktree to unblock the build — flagged for future hygiene task to gitignore + sweep + reconfigure Vite `resolve.extensions`); (b) `QuickSummaryDashboardDialog` is still a "Coming Soon" stub (out of scope); (c) no custom filtered views beyond the default system view for the 2 new cards (matches existing 4 cards). **ADR compliance**: ADR-021 (Fluent v9 tokens — `MailRegular`/`ReceiptRegular`; no hex literals introduced; `grid-template-columns: repeat(2, minmax(0, 1fr))` is CSS-spec literal same exemption as the existing `width: 100%` patterns), ADR-022 (React 19 functional component — no new hooks), ADR-028 (Xrm-based auth via `webApi` frame-walk; no BFF calls; no token snapshots) all complied with. **NOT deployed in this task** — Wave-3-R8 commit 1 of 2; coordinated deploy by main session after task 111 (Documents section verification) commits land. See deploy memo "Task 110 — QuickSummary extension (sprk_communication + sprk_invoices cards; My Work 2x3 grid)". |
| R-52 | Documents system layout requires 2x10 grid + no scrollbar + +/expand affordances; Smart To Do List system layout requires embed verification — Round 8 Wave 3b operator brief: "Create a new 'System workspace' widget called **Documents**; it uses the **same component as the current legalworkspace's 'Active Documents'**; **includes all the same functionality** in the cards; the **layout should be 2 wide x 10 height; no scroll bar**; **includes the '+' to add document**; **includes the expand to open the sprk_Document modal**." Earlier in R8: "Create a new 'system workspace' widget called **Smart To Do List**; this has the Smart To Do list code page loaded; this can also be added and pinned, but not deleted." Both Dataverse system layouts already exist (Wave 2a task 108 seeded them as `sprk_workspacelayout` with `sprk_issystem=true` and `sectionsJson` referencing the `documents` / `todo` section factories); Wave 2b (task 109) wired the cross-user discovery + auto-install. But the existing `documents` section uses a vertical stacked `RecordCardList` with implicit double-click-to-open behavior — NOT the operator-requested 2×10 grid with explicit Expand-to-modal affordance. Because Round 8 Option B unified both surfaces (standalone Corporate Workspace code page AND SpaarkeAi-embedded "Documents" tab) onto the same `documents` section factory, the grid-mode rendering needs to be opt-in (`gridMode` prop) so the standalone vertical list is preserved — but the per-card Expand affordance is a shared UX improvement that benefits both contexts. Smart To Do List embed verification (no code changes expected; just static-analysis confirmation that the existing `SmartToDo` component renders cleanly under `embedded={true}` + `disableSidePane={true}` as wired by the Wave 2a `todo.registration.ts` factory). | **RESOLVED (2026-05-22 / task 111, Wave-3-R8 commit 2 of 2)** | Medium | **Resolved (`src/solutions/LegalWorkspace/src/components/RecordCards/DocumentsTab.tsx` + `DocumentCard.tsx` + `src/solutions/LegalWorkspace/src/sections/documents.registration.ts` — additive prop + per-card tool + factory wiring; zero BFF / shared-lib changes)**: **(1)** added `gridMode?: { columns: number; maxRows: number }` prop to `DocumentsTab.tsx`. When provided, the component branches to a new CSS-grid rendering path: `display: grid` + dynamic `gridTemplateColumns: repeat(N, minmax(0, 1fr))` (inline style — N is the prop's columns value) + `gap: tokens.spacingHorizontalM` + `flex: 1 1 0` + `overflow: hidden` + defensive `scrollbarWidth: 'none'` + `::-webkit-scrollbar { display: none }` (mirrors task 107's WorkspaceTabManagerComponent hidden-scrollbar treatment). Effective cap = columns × maxRows = 2 × 10 = 20 cards; documents past index 20 are NOT rendered (no visual overflow → no scrollbar trigger). When `gridMode` is omitted (DEFAULT mode), the component still uses `RecordCardList` (vertical windowed list with IntersectionObserver) — preserves the standalone Corporate Workspace's existing behavior. **(2)** added explicit per-card "Expand" tool button in `DocumentCard.tsx`'s tools row (rightmost, after Find Similar). Uses `<Tooltip content="Expand">` + `<Button icon={<OpenRegular />}>`; click handler calls the existing `openRecordDialog("sprk_document", doc.sprk_documentid)` utility from `src/solutions/LegalWorkspace/src/utils/navigation.ts` which calls `Xrm.Navigation.navigateTo({pageType:'entityrecord', entityName:'sprk_document', entityId}, {target:2, width:80%, height:80%})` — opens as a MODAL DIALOG (target:2) per operator spec. Double-click + Enter handlers are RETAINED for power users (they still open the full-frame form via `navigateToEntity → Xrm.Navigation.openForm`). The Expand button appears in BOTH the standalone Corporate Workspace and the embedded SpaarkeAi "Documents" tab because `DocumentCard.tsx` is shared — this is intentional UX improvement, not a regression (the implicit double-click path is unchanged). **(3)** `documents.registration.ts` factory wired the new props: `maxVisible` bumped from 6 → 20 so the underlying `useDocumentsTabList` returns enough records to fill the 20-card grid; new `gridMode: { columns: 2, maxRows: 10 }` passed through `DocumentsSectionContent` → `DocumentsTab`; new `onShowMore` callback reuses the existing `context.onOpenDocumentsDialog?.(selectedViewIdRef.current)` handler — so when `totalCount > 20` a "View all (N more)..." link appears below the grid and opens the full sprk_document view in a modal (single overflow path, single handler, identical UX to the section toolbar's Open button). JSDoc on `documents.registration.ts` updated to document the Wave 3 grid-mode contract. **`+` add affordance**: ALREADY EXISTED (verified, not added) — the section toolbar (top-right of the SectionPanel header, NOT per-card) already has a Fluent v9 `<Button icon={<AddRegular />}>` wired to `context.onOpenWizard("sprk_documentuploadwizard")` at `documents.registration.ts` lines 287-294 (pre-task). **Smart To Do List embed verification (static analysis — PASS, no runtime smoke from build agent)**: confirmed `todo` registration is in `sectionRegistry.ts` (import line 18, export line 27); `SmartToDo` component accepts and honors `embedded`, `webApi`, `userId`, `scope`, `businessUnitId`, `disableSidePane`, `onCountChange`, `onRefetchReady` props (all passed by the Wave 2a `todo.registration.ts` factory); `embeddedRoot` style (line 134) is referenced when `embedded={true}` (line 669); `KanbanHeader` is hidden when `disableSidePane=true` (line 674); "Open full view" button is gated by `disableSidePane` (line 770); Wave 2a `scripts/system-layouts.json` already includes the "Smart To Do List" layout with `sectionId: "todo"` + `single-column` template + sort=1; Wave 2b task 109's cross-user discovery + auto-install cascade covers the runtime path. SmartToDo is the SOURCE of task 094's `usePaneCollapse` pattern so embedded behavior is well-trodden. **Runtime smoke deferred to operator** (cannot exercise Xrm host from build agent); static verification gives high confidence. NO code changes to SmartToDo per scope discipline. **FR-25 evolution**: standalone Corporate Workspace Documents section continues to use DEFAULT vertical-list rendering (no `gridMode` prop); per-card Expand button is visible in both contexts (intentional shared UX improvement); embedded "Documents" tab gets the new 2×10 grid + 20-card cap + "View all..." overflow link; behavioral guarantee preserved (all 5 tools — Preview / AI Summary / Open file / Find Similar / Expand — remain accessible; section toolbar's add + open buttons remain; view picker remains). Bundle delta: LegalWorkspace **+0.21 KB gzip** (569.38 → 569.59 — combined with task 110's parallel commit which also produces +0.21 KB gzip; final post-merge measurement by main session); SpaarkeAi **+0.15 KB gzip** (900.29 → 900.44). **ADR compliance**: ADR-021 (Fluent v9 tokens — `tokens.spacingHorizontalM` for grid gap, `tokens.colorNeutralForeground3` / `tokens.colorPaletteRedForeground3` for messages; no hex literals introduced; `display: grid` + `repeat(N, minmax(0, 1fr))` are CSS-spec literals same exemption as `flex: 1` / `width: 100%`), ADR-022 (React 19 — standard hooks `useCallback`/`useEffect`; no React 16 fallbacks), ADR-025 (`OpenRegular` from `@fluentui/react-icons` v9 family — same package as the rest of `DocumentCard.tsx`), ADR-028 (no new BFF calls; existing `DataverseService` + `useDocumentsTabList` path used which already routes through `authenticatedFetch`). **CLAUDE.md §10 BFF Hygiene**: ZERO new endpoints, ZERO new services, ZERO NuGet changes. **Pre-existing follow-ups flagged (NOT fixed)**: LegalWorkspace `tsc --noEmit` reports several pre-existing TypeScript errors unrelated to this task — `WorkspaceGrid.tsx` unused locals (TS6133), `SmartToDo/index.ts` missing `./TodoItem` module (TS2307), `LegalWorkspaceApp.tsx` `IWebApi`/`IThemeWebApi` shape divergence (TS2345), `sectionRegistry.ts` missing `@types/node` (TS2580), `DocumentCard.tsx:181` `doc.sprk_name` does not exist (TS2339 — pre-existing in `handleOpenFile` callback), `DocumentCard.tsx:327` onClick type mismatch on `RecordCardShell` prop (TS2322); Vite build (esbuild) is permissive and passes; future cleanup task should address. **NOT deployed in this task** — Wave-3-R8 commit 2 of 2; coordinated deploy by main session after tasks 110 + 111 commits land. See deploy memo "Task 111 — Documents section polish (2x10, no scrollbar, +/expand) + Smart To Do embed verification". |
| R-53 | Stale tsc-emit cruft in source folders breaks Vite resolution + obscures actual source intent — discovery (Wave 3a / task 110, 2026-05-22): ~344 untracked stale `.js`/`.d.ts`/`*.js.map`/`*.d.ts.map` files in `src/solutions/LegalWorkspace/src/` (siblings of the legitimate `.ts`/`.tsx` source) were actively breaking edits. Root cause: Vite's default `resolve.extensions` ordering is `['.mjs', '.js', '.mts', '.ts', '.jsx', '.tsx', '.json']` — so when `Foo.js` (stale) and `Foo.ts` (fresh) both exist, Vite imports the stale `.js`. Task 110's parallel agent had to locally delete cruft for QuickSummary files just to make their edits take effect. Same pattern present in `src/client/shared/Spaarke.AI.Outputs/src/` (~136 files). Both projects' CURRENT tsconfigs would NOT emit into src/ (LegalWorkspace has `noEmit:true`; Spaarke.AI.Outputs has `outDir:./dist`), so the cruft must predate the current config OR come from an out-of-tree `tsc` invocation. Without remediation, every future edit to a `.ts` file with a stale `.js` sibling has a >50% chance of silently importing stale code; the failure mode is invisible until runtime divergence. | **RESOLVED (2026-05-22 / task 112, R9-cleanup)** | Medium | **Resolved**: Defensive 2-pass file-system sweep — for each cruft candidate, required a sibling `.ts` or `.tsx` to exist before deletion. Total: 480 files deleted (344 under LegalWorkspace + 136 under Spaarke.AI.Outputs), 0 preserved (every cruft file had a sibling, confirming 100% are compilation output, not hand-written declarations). Pre-deletion safety checks: zero tracked cruft files (`git ls-files` returned NONE for `*.js`/`*.d.ts`/`*.map` under both directories); zero importers reference cruft via explicit `.js` extension. Dry-run executed first (reported 480/0); real run matched. `.gitignore` hardening: 8 new rules excluding `*.js`/`*.d.ts`/`*.js.map`/`*.d.ts.map` under `src/solutions/*/src/**` + `src/client/shared/*/src/**`; 2 negation rules for legitimately tracked hand-written files (`src/solutions/SpaarkeAi/src/ribbon/xrm-globals.d.ts` Xrm globals declaration + `src/client/shared/Spaarke.UI.Components/src/__mocks__/diff.js` Jest mock); verified with `git check-ignore -v` that negations work AND future cruft would be caught. Vite `resolve.extensions` hardening (defense-in-depth): added `extensions: [".ts",".tsx",".mts",".cts",".js",".mjs",".cjs",".jsx",".json"]` to 3 vite configs that import source from outside their own src/ (LegalWorkspace, SpaarkeAi, WorkspaceLayoutWizard) so even if cruft re-appears, Vite never prefers `.js` over `.ts`. Other 18 vite configs unmodified (no cross-package source aliases → no shadowing risk). Build verification: `@spaarke/ui-components` tsc 0 errors; LegalWorkspace 569.98 KB gzip; SpaarkeAi 900.83 KB gzip (3 pre-existing AppInsights `/*#__PURE__*/` comment warnings unchanged); WorkspaceLayoutWizard 427.84 KB gzip. All builds clean. ADR-022 (React 19) only touchpoint; no auth surface change, no behavior change; CLAUDE.md §10 BFF hygiene satisfied (zero BFF additions). Pure hygiene — NOT deployed. Pre-existing follow-ups still flagged: (a) Spaarke.AI.Outputs `clean` script uses POSIX `rm -rf dist` (Windows-incompatible); (b) LegalWorkspace pre-existing TS errors from task 111 (Vite/esbuild passes; TS-server-level reports only); (c) Other shared packages spot-checked clean today — `.gitignore` covers them defensively. See deploy memo "Task 112 — Sweep stale .js/.d.ts/.map cruft + harden against recurrence". |
| R-54 | Workspace architecture + component model + auth + BFF surface + "how to build a new widget" UNDOCUMENTED end-to-end after Round 8 — operator request (2026-05-22): "Very important — we need to document the new workspace architecture and how it works; the component model; Auth; bff.api; and how to build additional widgets." Plus: "Question: are we sure that this is all 'componentized' and reusable within our shared components architecture? This is very important for maintenance and extendability." Pre-task state: the pipeline existed end-to-end (Wave 2a Dataverse seed + Wave 2b BFF merge + Wave 3 LegalWorkspace section enhancements all landed), but ZERO reference docs explained how the pieces fit together — a new developer or AI agent would have to reverse-engineer the flow from source. Specifically missing: (a) cold-load → widget render pipeline diagram, (b) component inventory across `@spaarke/ui-components` + `@spaarke/ai-widgets` + `@spaarke/auth` + `@spaarke/legal-workspace` + solution-local components, (c) PaneEventBus contract (4 channels × event types × subscription-race pattern), (d) BFF workspace endpoint surface, (e) Dataverse `sprk_workspacelayout` schema + the 5 system layouts, (f) storage contract for 10 localStorage/sessionStorage keys, (g) honest assessment of WHAT is componentized vs WHAT is coupled today (operator's reuse question), (h) step-by-step tutorial for adding new workspace widgets. | **RESOLVED (2026-05-22 / task 113, Wave-R3-doc)** | High | **Resolved**: created 4 new docs under `docs/` covering the full surface: (1) [`docs/architecture/SPAARKEAI-WORKSPACE-ARCHITECTURE.md`](../docs/architecture/SPAARKEAI-WORKSPACE-ARCHITECTURE.md) — end-to-end pipeline reference with ASCII diagrams of the provider tree + cold-load sequence (main.tsx bootstrap → App.tsx auth probe → ThreePaneShell provider tree → WorkspacePane mount → `useWorkspaceLayouts` → BFF default cascade → `widget_load` dispatch → `resolveWorkspaceWidget` → `WorkspaceLayoutWidget` → embedded `LegalWorkspaceApp` → `WorkspaceGrid` → section factories → `WorkspaceShell`); 4-stage shell lifecycle (`welcome → loading → active-chat → review`); BFF endpoint table (10 endpoints workspace-relevant); Dataverse `sprk_workspacelayout` schema + the 5 system layouts (Corporate Workspace hard-coded + Daily Briefing + Smart To Do List + My Work + Documents); storage contract for 10 localStorage/sessionStorage keys; ADR pointer table; where-to-start-reading-code quick reference. (2) [`docs/architecture/SPAARKEAI-COMPONENT-MODEL.md`](../docs/architecture/SPAARKEAI-COMPONENT-MODEL.md) — inventory of `@spaarke/ui-components` (41 component folders categorized by purpose), `@spaarke/ai-widgets` (16 workspace-widget registrations including the unified `workspace` type, 2 registries, PaneEventBus, AiSessionProvider, stage rules), `@spaarke/auth` (Spaarke Auth v2 surface with INV-1..INV-8 invariants enforced), `@spaarke/legal-workspace` (LegalWorkspaceApp embedded contract). Solution-local SpaarkeAi components catalogued with reuse-candidate annotations. Solution-local LegalWorkspace components catalogued with coupling notes. PaneEventBus contract section with all 4 channels × event types × the macrotask-deferred dispatch pattern that prevents subscription races. (3) [`docs/architecture/SPAARKEAI-COMPONENTIZATION-AUDIT.md`](../docs/architecture/SPAARKEAI-COMPONENTIZATION-AUDIT.md) — honest assessment per operator's question. Catalogs **6 specific coupling gaps**: §1 duplicate `useWorkspaceLayouts` hooks (SpaarkeAi vs LegalWorkspace — different fallback strategies, drift risk via the explicit "keep in sync" docblock); §2 section factories LegalWorkspace-internal (only Daily Briefing has been hoisted to `@spaarke/ui-components`; the other 5 depend on `DataverseService` + `FeedTodoSyncContext` + `useQuickSummaryCounts`); §3 `WorkspaceLayoutWidget` hard-wired to `LegalWorkspaceApp` (no `WorkspaceRenderer` interface for plugging in alternative renderers); §4 Xrm.WebApi vs BFF decision criteria undocumented (Wave 3a's QuickSummary chose Xrm but no codified rule exists); §5 embedded-mode contract informal (boolean `embedded` prop with implicit pre-mount setup of `setLegalWorkspaceRuntimeConfig` + `sessionStorage["spaarke_dailyDigestShown"]` + theme ownership — works today but no formal contract a new host could comply with); §6 dual runtime-config singletons + auth-surface gap for non-MDA hosts. Plus a "what IS clean" section listing 10 items (PaneEventBus, both registries, Spaarke Auth v2, SectionFactoryContext, `GetDefaultLayoutAsync` 4-step cascade, the unified `workspace` widget-type, 4-stage shell lifecycle, NFR-09 persistence, MAX_WORKSPACE_TABS FIFO, Vite single-file build). Plus a prioritized remediation backlog: items 1-2 (decision criteria + embedded contract docs, ~5h total) immediate wins; item 3 (consolidate dual hooks, ~8h) highest-ROI code refactor; items 4-7 deferred until non-MDA host on roadmap. (4) [`docs/guides/BUILD-A-NEW-WORKSPACE-WIDGET.md`](../docs/guides/BUILD-A-NEW-WORKSPACE-WIDGET.md) — step-by-step tutorial with a decision tree (Pattern A = section factory + Dataverse layout; B = Code Page dispatcher; C = AI output). Worked example: complete Calendar widget walk-through showing 5 new files (CalendarSection component, CalendarItemCard, useCalendarEvents hook, calendar.registration.ts, sectionRegistry edit) + 2 edits (sectionRegistry import + system-layouts.json entry) + 2 commands (seed script + LegalWorkspace deploy) — with explicit "what changes are NOT required" subsection: ZERO SpaarkeAi changes, ZERO BFF changes, ZERO new packages, ZERO ADRs. Plus common-pitfalls table (7 gotchas) and what's NOT clean about the new-widget flow (cross-link to audit §2). Pointer updates: root `CLAUDE.md` §16 (added 4 rows linking to the new docs) + `projects/spaarke-ai-platform-unification-r3/CLAUDE.md` (new "Workspace architecture" section). ADR compliance: N/A — doc-only task; no code changes; no ADR consultations beyond reading the existing ones to cite them correctly. CLAUDE.md §10 BFF Hygiene: N/A — zero BFF additions. NOT deployed. See deploy memo "Task 113 — Workspace architecture documentation + honest componentization audit". |
| R-55 | Calendar widget reuse strategy: hoist EventsPage components to `@spaarke/events-components` per operator decision Option A (architectural unity) — Round 9 (2026-05-22). Operator Q&A: the new "Calendar" system workspace widget for SpaarkeAi (task 115, dispatched next) needs to surface Events + Tasks data. Two viable paths: (A) hoist `CalendarSection` + `GridSection` + filters + `EventsPageContext` + `FetchXmlService` to a new shared lib so BOTH the standalone `sprk_eventspage` code page AND the new Calendar widget consume from the same source — mirroring the Round 8 Option B "architectural unity" decision for layouts (LegalWorkspaceApp embedded in SpaarkeAi via `@spaarke/legal-workspace` alias). (B) copy the components into SpaarkeAi as a Calendar widget local source — faster initial delivery but immediate drift between standalone EventsPage and embedded Calendar. Operator picked Option A explicitly: "hoist Events components to a shared lib so BOTH the standalone EventsPage code page AND the new Calendar widget consume from the same source. Same architectural-unity payoff as Round 8 Option B for layouts." Pre-task state: `src/solutions/EventsPage/src/` had 9 generic components + 1 generic context + 1 generic service that no other surface consumed — but were straightforward to extract because they only depend on `Xrm.WebApi` (ADR-028 compliant), Fluent v9 tokens (ADR-021 compliant), and React 19 (ADR-022 compliant). Risk if not done: when task 115 lands, the Calendar widget would inevitably either (1) duplicate the components in SpaarkeAi/LegalWorkspace introducing immediate drift, or (2) require a mid-task hoist that conflates structural work with widget-specific code. | **RESOLVED (2026-05-22 / task 114, R9-Calendar)** | High | **Resolved**: created the new `@spaarke/events-components` shared package at `src/client/shared/Spaarke.Events.Components/` with the folder-per-component layout pattern (`components/<Component>/<Component>.tsx` + `index.ts`); package config mirrors `Spaarke.Auth`/`Spaarke.UI.Components` patterns (`private: true`, `"type": "module"`, source-as-entrypoint via `main`/`types`/`exports`); `tsconfig.json` enforces `strict: true` + `noEmit: true` (post-task-112 hygiene); peer-deps React 16-19 + Fluent v9. Moved 9 components + EventsPageContext + FetchXmlService via `git mv` (preserving file history); rewrote internal imports; deleted 4 orphan barrel files. Applied 4 minimal `// Task 114 note:`-annotated type-bridge casts so the new lib's `tsc --noEmit` passes with `strict: true` (pre-existing Combobox onInput drift × 2, CalendarDrawer eventDates drift × 1, IEventRecord index-signature drift × 3 — all pre-existing in EventsPage source which used Vite/esbuild without `tsc`). EventsPage refactored with ONE import block change in `App.tsx`; layout composition unchanged. Prepared SpaarkeAi + LegalWorkspace vite + tsconfig + package.json wiring so task 115 can immediately register a Calendar section factory in LegalWorkspace's `sectionRegistry.ts` without further plumbing. Build verification all 4 packages clean: `@spaarke/events-components` tsc 0 errors; EventsPage Vite 329.92 KB gzip (baseline 415.97 KB → -86 KB / -20.7%, explained as tree-shaking improvement from the new config matching LegalWorkspace/SpaarkeAi hygiene — pure build efficiency, no behavior change); SpaarkeAi 900.83 KB gzip (0 delta — alias prep dormant); LegalWorkspace 569.98 KB gzip (0 delta — alias prep dormant). NO deploy (pure refactor; task 115 will deploy). ADR-012 (shared lib reuse), ADR-021 (Fluent v9 tokens unchanged), ADR-022 (React 19 peer-deps allow 16-19), ADR-028 (Xrm.WebApi-only services) all complied with. CLAUDE.md §10 BFF Hygiene: ZERO new endpoints. Pre-existing follow-ups flagged: 4 type-drift items bridged with casts (future Events-quality task should fix at source); `@spaarke/ui-components` source-alias not wired in EventsPage (would tree-shake another ~90 KB; separate optimization decision); no unit tests in EventsPage (Calendar widget task 115 should consider co-located tests). See deploy memo "Task 114 — Hoist EventsPage components to @spaarke/events-components". |
| R-56 | Calendar workspace widget — 5th SpaarkeAi system workspace (Round 9, 2026-05-22). Operator vision: a "Calendar" workspace surfaces all events + tasks the user has access to, layout per operator mockup (date-range filter row → 3-month strip → full EventsPage toolbar → view selector → grid). Reuses `@spaarke/events-components` (hoisted in task 114) so standalone EventsPage code page and the new Calendar widget share components — architectural unity. Round 9 Q&A decisions: data scope = all Events + Tasks the user has access to (matches standalone EventsPage); event detail opens as MODAL via `Xrm.Navigation.navigateTo` (NOT `Xrm.App.sidePanes`) mirroring task 111 Documents Expand; toolbar = full EventsPage parity. | **RESOLVED (2026-05-22 / task 115, R9-Calendar widget)** | High | **Resolved**: created `CalendarWorkspaceWidget` in the shared lib at `src/client/shared/Spaarke.Events.Components/src/widgets/CalendarWorkspaceWidget/` (NOT in LegalWorkspace — per the componentization audit in task 113, placing the new container in the shared lib avoids regressing into LW-internal coupling). The widget composes (a) date-range filter row inline (Fluent v9 Dropdown + Input type="date" × 2 wiring through `EventsPageContext.setCalendarFilter` with `dateFields` arg), (b) single `<CalendarSection>` (which already renders `current + 2 ahead` months internally — divergence from brief's "3 instances" approach explained in deploy memo §1 Composition decisions), (c) full EventsPage toolbar via `<Toolbar size="small">` with all 9 buttons (New/Delete/Complete/Close/Cancel/On Hold/Archive/Refresh/Calendar) — bulk-action handlers partially duplicated from `EventsPage/App.tsx` rather than extracted (flagged for future `useEventsBulkActions` hook), (d) `<ViewSelectorDropdown>` with `useViewSelection` defaulted to Active Events, (e) `<GridSection>` auto-binding via `EventsPageContext.filters`. Wrapped in `<EventsPageProvider onOpenEvent={handleOpenEvent}>` where `handleOpenEvent` calls `Xrm.Navigation.navigateTo({pageType:'entityrecord', entityName:'sprk_event', entityId},{target:2, width/height:{value:80,unit:'%'}, position:1})` — modal at 80%×80%, NOT side pane. Hidden scrollbar pattern: `scrollbar-width: none` + WebKit override at root (matches task 107 Workspace pane treatment). Section factory shim at `src/solutions/LegalWorkspace/src/sections/calendar.registration.ts` mirrors `dailyBriefing.registration.ts` from task 069 — thin `ContentSectionConfig` with `renderContent: () => React.createElement(CalendarWorkspaceWidget)`; registered in `sectionRegistry.ts` (7 sections total). Dataverse seed: appended 5th entry to `scripts/system-layouts.json` (`name: "Calendar", sectionId: "calendar", layoutTemplateId: "single-column", sortOrder: 4`). `Deploy-SystemWorkspaceLayouts.ps1 -WhatIf` confirmed 4 existing skip + Calendar would-create; live run created record `fcaf3776-1556-f111-a824-7ced8dd899c3`; verification query reports 5 system layouts. Build verification all 3 packages clean: `@spaarke/events-components` tsc 0 errors; LegalWorkspace 587.31 KB gzip (vs 569.98 task 114 baseline → +17.33 KB); SpaarkeAi 916.60 KB gzip (vs 900.83 task 114 baseline → +15.77 KB). Deployed via `Deploy-SpaarkeAi.ps1` → resource ID `5206a442-3451-f111-bec7-7ced8d1dc988` (3349 KB), customizations.PublishAllXml succeeded. ADR-012 (shared lib reuse — widget lives in `@spaarke/events-components`, registered via LW shim), ADR-021 (Fluent v9 tokens — zero hex literals introduced), ADR-022 (React 19), ADR-028 (auth via `Xrm.WebApi` — all bulk-action handlers use `getXrm().WebApi.*`, no `authenticatedFetch`, no BFF calls) all complied with. CLAUDE.md §10 BFF Hygiene: ZERO new endpoints, ZERO new services, ZERO new NuGet packages, ZERO publish-size delta on BFF. Pre-existing follow-ups flagged (NOT fixed): (a) external CalendarSection ◀ ▶ nav arrows would require lifting `viewDate` to a prop on the shared component — deferred; (b) bulk-action handler duplication between standalone EventsPage and the new widget — future `useEventsBulkActions` hook extraction; (c) task 114 type-bridges still in place; (d) standalone EventsPage + standalone LegalWorkspace code pages NOT redeployed in this task — operator focus is SpaarkeAi. See deploy memo "Task 115 — Calendar workspace widget (5th Round-8 system layout)". |
| R-57 | Calendar widget polish — Round 10 operator smoke feedback (2026-05-22). After task 115 shipped the initial Calendar workspace widget, operator reported 5 refinements: (1) the 3-month strip rendered VERTICAL-stacked (CalendarSection's prior internal layout) rather than horizontal as in the mockup; (2) the strip dominated vertical space, pushing the grid too low in the workspace viewport; (3) no external month-navigation arrows — only the operator-discoverable click-to-scroll inside the strip; (4) hard cap of "current month + 2 ahead" meant users couldn't browse to arbitrary months/years; (5) no way to collapse the calendar strip to give the grid more room. Composition-over-modification was the task-115 posture, but operator feedback made the four shared-component touch points (layout direction, monthsToShow count, viewDate external control, no internal scroll) load-bearing. | **RESOLVED (2026-05-22 / task 116, Wave-R10)** | Medium | **Resolved (controlled-component extension, backwards-compatible)**: extended `CalendarSection` (in `@spaarke/events-components`) with three OPTIONAL props — `viewDate?: Date` (when provided, CONTROLLED mode; internal viewDate state is bypassed), `monthsToShow?: number` (defaults to 3 — the prior internal behavior), `layout?: 'vertical' \| 'horizontal'` (defaults to `'vertical'` — the prior behavior). EventsPage standalone (`CalendarDrawer` → `CalendarSection`) passes NONE of the new props, so it renders byte-identically (EventsPage bundle confirmed at 330.24 KB gzip vs 329.92 task 115 baseline → +0.32 KB, near-zero delta consistent with just adding 3 optional prop slots to the typed surface). New horizontal layout CSS uses `display: flex; flex-direction: row; gap: 16px` with each month as `flex: 1 1 0; min-width: 240px`. Rewrote `CalendarWorkspaceWidget`: owns `viewDate` state initialized to `startOfMonth(new Date())`; computes responsive `monthsToShow` via `ResizeObserver` on the strip container ref using `computeMonthsForWidth()` (breakpoints: <380px → 1mo, <720px → 2mo, <1060px → 3mo, <1400px → 4mo, ≥1400px → 5mo capped — calibrated for SpaarkeAi's default 25/50/25 split per task 117 ~600px Workspace pane = 2 months, and wider when Context/Assistant collapse); external ◀ ▶ Tooltip-wrapped subtle buttons flank the strip with `aria-label` accessibility; arrow click advances ±1 month (Shift+click advances by the visible window for power users), unbounded — any month/year reachable; strip container `overflow: hidden` (no scrollbar — arrows ARE the navigation); collapse toggle (`ChevronUp/Down20Regular`) on a right-side bordered slot — when collapsed the calendar strip unmounts (state lives in widget, not CalendarSection, so unmount/remount is free) and the collapse state persists in `localStorage["spaarke:calendar:collapsed"]` via try/catch-wrapped accessors (mirrors `pinnedWorkspaces.ts` task-092 pattern; degrades silently on private browsing / quota / corrupt storage); calendar row has `flex-shrink: 0` + `min-height: 280px` (one-month grid baseline), grid container retains `flex: 1 1 auto; min-height: 320px` so it absorbs freed vertical space on collapse. New utility module at `src/client/shared/Spaarke.Events.Components/src/utils/dateMath.ts` exports `addMonths` + `startOfMonth` (rejected adding `date-fns` to peer-deps — keeps events-components zero-dep beyond React + Fluent v9, preserving bundle-size attribution). Added `./utils` export entry to package.json + top-level barrel. Build verification all 4 packages clean: `@spaarke/events-components` tsc 0 errors; EventsPage Vite 0 errors, 3,283 modules, **330.24 KB gzip** (vs 329.92 task 115 baseline → +0.32 KB, near-zero — confirms standalone EventsPage backwards-compat); LegalWorkspace Vite 0 errors, 3,245 modules, **588.32 KB gzip** (vs 587.31 task 115 baseline → +1.01 KB — ResizeObserver + arrow nav + collapse state + dateMath util); SpaarkeAi Vite 0 errors, 3,438 modules, **917.48 KB gzip** (vs 916.60 task 115 baseline → +0.88 KB; 3 pre-existing AppInsights `/*#__PURE__*/` warnings unchanged). Deployed via `Deploy-SpaarkeAi.ps1` → updated existing `sprk_spaarkeai` (resource ID `5206a442-3451-f111-bec7-7ced8d1dc988`, 3353 KB), customizations.PublishAllXml succeeded. ADR-012 (shared-lib reuse — CalendarSection backwards-compat preserved via optional props with prior-behavior defaults; EventsPage byte-identical), ADR-021 (Fluent v9 tokens only — zero hex/rgba literals; new horizontal-layout styles use `tokens.*` + `shorthands.*`), ADR-022 (React 19 — `useState` lazy initializers, `useRef`, `useCallback`, `useEffect` cleanup for ResizeObserver), ADR-028 (auth via `Xrm.WebApi` — unchanged from task 115; ZERO BFF calls in the widget) all complied with. CLAUDE.md §10 BFF Hygiene: ZERO new endpoints, ZERO new services, ZERO new DI registrations, ZERO new NuGet packages, ZERO publish-size delta on BFF (task is frontend-only). Pre-existing follow-ups flagged (NOT fixed): (a) bulk-action handler duplication between standalone EventsPage and widget (carried from task 115 — future `useEventsBulkActions` hook); (b) task 114 type-bridges still in place (`IEventRecord` index signature, Combobox `onInput` drift, `CalendarDrawerProps.eventDates` shape); (c) `CalendarDrawer`'s `eventDates: string[]` mismatch with `IEventDateInfo[]` API drift is unaffected by this task — bridged with existing cast at the CalendarDrawer call site; (d) standalone EventsPage + standalone LegalWorkspace code pages NOT redeployed in this task — operator focus is SpaarkeAi. See deploy memo "Task 116 — Calendar widget polish (horizontal responsive strip, arrow nav, collapse, grid height)". |
| R-58 | SpaarkeAi default pane widths use fixed pixel defaults (340/400) rather than the operator-requested 25/50/25 percentage distribution on first cold load (Round 10, 2026-05-22). Operator: "For the sprk ai initial load the default pane width distribution should be 25% / 50% / 25%." Q&A clarification: percentage of viewport at first load; user resizes persist (Recommended). Today on a typical 2000px viewport the panes distribute ~17/50/33 — wildly off the desired layout. Constraint: user-dragged pixel widths persisted to sessionStorage MUST continue to win on subsequent mounts (current behavior preserved); the percentage default only applies on a brand-new session with no stored width. | **RESOLVED (2026-05-22 / task 117, Wave-R10)** | Medium | **Resolved (Option A — backwards-compatible additive props)**: added two optional props to `ThreePaneLayoutProps`: `defaultLeftWidthFrac?: number` and `defaultRightWidthFrac?: number` (e.g. 0.25 for 25%). Backwards-compatible — when omitted, the legacy `defaultLeftWidthPx` / `defaultRightWidthPx` path is preserved verbatim. Introduced `resolveInitialWidth(key, defaultPx, min, frac)` helper in `useThreePaneLayout.ts` replacing the previous `loadPersistedWidth`. Three-tier precedence for the mount-time initial width: (1) sessionStorage stored pixel width (user-dragged value persists, always wins); (2) `frac × window.innerWidth` (NEW — percentage default applied only on cold mount when no stored value AND frac is defined AND `typeof window !== 'undefined'`); (3) `defaultLeftWidthPx` (legacy pixel fallback for SSR / non-browser AND as a hard floor via `Math.max(min, ...)`). Wired into both `useState` lazy initializers — pure function, fully compatible with React 19 strict-mode double-invocation. `window.innerWidth` chosen as viewport reference because the layout's own bounding rect isn't measurable at first mount (containerRef populates post-mount); SpaarkeAi shell fills the viewport so this is an accurate proxy. SpaarkeAi `ThreePaneShell.tsx` passes `defaultLeftWidthFrac={0.25}` + `defaultRightWidthFrac={0.25}` alongside the existing `defaultLeftWidthPx={340}` + `defaultRightWidthPx={400}` (the pixel defaults remain as the legacy fallback layer in the precedence chain). LegalWorkspace differentiation (FR-25): grep-confirmed LegalWorkspace does NOT consume `ThreePaneLayout` — it uses `WorkspaceGrid`-based section rendering, so FR-25 trivially preserved; zero LegalWorkspace files touched. Existing min-width floors (`minLeftWidthPx=240` / `minRightWidthPx=240` / `minCenterWidthPx=320`) still apply via `Math.max(min, computed)` — narrow viewports respect the floor. Build verification all 3 packages clean: `@spaarke/ui-components` tsc 0 errors; SpaarkeAi Vite 0 errors, 3,436 modules, **916.69 KB gzip** (vs 916.60 task 115 baseline → +0.09 KB trivial — matches expected ~100-300 byte delta for 2 optional props + 3-branch helper); LegalWorkspace Vite 0 errors, 3,243 modules, **587.34 KB gzip** (vs 587.31 task 115 baseline → +0.03 KB module-hash noise; LegalWorkspace doesn't consume ThreePaneLayout so semantically byte-identical). Smoke walkthrough validated: viewport 1600px fresh session → Left ~400px / Center ~800px / Right ~400px; user drags left to 320px + reload → Left=320px (sessionStorage wins); `sessionStorage.clear()` + reload → returns to 25/50/25. Deployed via `Deploy-SpaarkeAi.ps1` → updated existing `sprk_spaarkeai` (resource ID `5206a442-3451-f111-bec7-7ced8d1dc988`, 3350 KB), customizations.PublishAllXml succeeded. ADR-012 (shared lib — `ThreePaneLayout` stays context-agnostic; no PCF-specific dependencies; new props are pure React), ADR-021 (Fluent v9 — no styles touched, no hex/rgba literals introduced), ADR-022 (React 19 — `useState` lazy initializer preserved; pure-function `resolveInitialWidth`) all complied with. CLAUDE.md §10 BFF Hygiene: ZERO new endpoints, ZERO new services, ZERO new DI registrations, ZERO new NuGet packages, ZERO publish-size delta on BFF (task is frontend-only). Pre-existing follow-ups flagged (NOT fixed): (a) ESLint config drift in `Spaarke.UI.Components` (ESLint v9 migration pending — TypeScript build is the authoritative check and passes clean); (b) narrow-viewport edge case (<800px) where minimum floors sum exceeds viewport — existing layout overflow handling unchanged in this task; deferred follow-up if observed in practice. See deploy memo "Task 117 — Default pane widths 25/50/25 percentage at first cold load". |
| R-59 | Calendar widget Round 11 operator UX refinements (2026-05-22). After Round 10 (task 116 shipped horizontal strip + responsive monthsToShow + external arrow nav + collapse + 280px strip), operator reported six refinements on the deployed widget: (1) collapse chevron sits on the calendar strip row — when collapsed the date-range filter row above is the only chrome left, but the operator wanted the filter row to remain visible AND interactive so it can still drive the grid filter; the chevron should live on the filter row at the right edge so it is always reachable. (2) The current `ChevronUp20Regular` / `ChevronDown20Regular` is visually too similar to the month-navigation chevrons (◀ ▶) on the same row — a distinct icon family wanted. (3) The internal CalendarSection "📅 Calendar" sub-heading row (Calendar24Regular + "Calendar" text + bottom separator) is duplicative ("everyone knows they're calendars"). (4) Adjacent month grids butted up against each other with no visual separation — wanted breathing room. (5) Days with associated events had only a 4×4px brand-blue dot at the bottom of the cell — easy to miss; operator wants a more prominent indicator AND wants clicking that day to filter the grid to just that date. (6) Grid needs an icon button to open the full sprk_event entity list view in a modal (mirroring task 111's Documents Expand affordance). | **RESOLVED (2026-05-22 / task 118, Wave-R11)** | Medium | **Resolved (six surgical fixes — two files)**: (Fix 1) Filter row now contains the collapse chevron at its right edge via a flex-spacer (`<div style={{flex: 1 1 auto}}>`) between the From/To inputs and the chevron; right-justified at any viewport without media queries. When collapsed, only the calendar strip row hides — the filter row remains visible and continues to drive the grid filter (operator-critical). (Fix 2) `ChevronUp20Regular` / `ChevronDown20Regular` → `CaretUp24Regular` / `CaretDown24Regular`. Caret triangles visually distinct from the month-navigation chevrons; both confirmed in installed `@fluentui/react-icons` version. (Fix 3) In `CalendarSection.tsx` the `<div styles.header>` block (Calendar24Regular icon + "Calendar" text + bottom border), the selectionInfo banner, and the footer (Clear + version) are now ALL suppressed when `layout='horizontal'`. `layout='vertical'` (EventsPage CalendarDrawer) unchanged — backwards-compat preserved. (Fix 4) `CalendarSection.calendarContentHorizontal` column gap bumped from `shorthands.gap("16px")` → `columnGap: tokens.spacingHorizontalXL` (~20px). Vertical layout unaffected. (Fix 5) Added `dayWithEvents` Griffel style: `colorBrandBackground2` + `colorBrandForeground1` + `fontWeightSemibold` + `colorBrandBackground2Hover`. Applied to in-month event days only (other-month padding days NOT tinted — would compete visually). Stacking priority: selected > in-range > has-events > today > other-month base. Added two new optional CalendarSection props (both backwards-compat): `selectedDate?: Date | null` + `onSelectDate?: (date: Date | null) => void`. Widget owns the `selectedDate` state + `onDaySelect` callback that dispatches a single-day range filter via `EventsPageContext.setCalendarFilter({type: 'range', start: iso, end: iso, dateFields: [dateField]})` and on toggle-off clears via `{type: 'clear'}`. Added filter-divergence `useEffect` watching `filters.calendarFilter` — if the active filter no longer matches the local `selectedDate` (because the user changed From/To or another component dispatched), `selectedDate` is reset to `null` so the day highlight cannot lie about what's actually filtering. Day-click takes precedence over From/To. (Fix 6) `Open24Regular` button in the view-selector row (flex-spacer + button at right edge), tooltip "Open Events list", click → `Xrm.Navigation.navigateTo({pageType: 'entitylist', entityName: 'sprk_event'}, {target: 2, width/height: 80%, position: 1})`. Same icon family and 80% sizing as task 111 Documents Expand + FilePreview "Open Record" — consistent SpaarkeAi modal-launch UX. **Bundle verification all 4 packages clean**: `@spaarke/events-components` tsc 0 errors; EventsPage Vite 0 errors, **330.23 KB gzip** (-0.01 KB vs task 116 baseline 330.24 = parity confirms backwards-compat); LegalWorkspace Vite 0 errors, **588.72 KB gzip** (+0.40 KB vs 588.32 baseline); SpaarkeAi Vite 0 errors, **917.85 KB gzip** (+0.37 KB vs 917.48 baseline). All deltas within expected 2-5 KB per package. **Deployed** via `Deploy-SpaarkeAi.ps1` → updated existing `sprk_spaarkeai` (resource ID `5206a442-3451-f111-bec7-7ced8d1dc988`, 3355 KB), customizations.PublishAllXml succeeded. **Files modified**: `src/client/shared/Spaarke.Events.Components/src/widgets/CalendarWorkspaceWidget/CalendarWorkspaceWidget.tsx` (Fixes 1, 2, 6 + Fix 5 widget-side wiring); `src/client/shared/Spaarke.Events.Components/src/components/CalendarSection/CalendarSection.tsx` (Fix 3 + Fix 4 + Fix 5 day-cell styling + new optional controlled props; VERSION 1.0.0 → 1.1.0). NOT modified: standalone EventsPage, CalendarSidePane, BFF. ADR-012 (CalendarSection backwards-compat preserved via optional props with prior-behavior defaults; EventsPage byte-identical), ADR-021 (Fluent v9 brand tokens — zero hex/rgba), ADR-022 (React 19 hooks), ADR-028 (Xrm.Navigation only — zero BFF calls) all complied with. CLAUDE.md §10 BFF Hygiene: ZERO new endpoints / services / DI / NuGet / publish-size delta. Pre-existing follow-ups flagged (NOT fixed): (a) `CalendarSidePane` web resource maintains its own separate CalendarSection copy divergent from shared library (carried from task 114/115/116); (b) bulk-action handler duplication between standalone EventsPage and widget pending `useEventsBulkActions` hook extraction (carried from task 115); (c) `CalendarDrawer.eventDates: string[]` vs `IEventDateInfo[]` API drift (carried from task 116); (d) standalone EventsPage + LegalWorkspace code pages NOT redeployed (operator focus is SpaarkeAi only). See deploy memo "Task 118 — Calendar widget Round 11 polish". |
| R-18 | Workspace selection no render path; pane coordination signals unwired — operator smoke (2026-05-21, round 4) confirmed that selecting a layout from the SpaarkeAi `WorkspacePaneMenu` "Switch Workspace" dropdown silently did nothing in the deployed UI. Root cause: `handleLayoutSelect` only persisted a `sessionStorage` key + dispatched a legacy `window` CustomEvent (`spaarke:workspace-layout-changed`) but no SpaarkeAi component subscribed to either signal — so the workspace pane never updated to render the chosen workspace. Secondary issue: even if a render path existed, the operator's architecture (workspace pane has tabs, each tab is a widget, active tab = active context for Assistant + Context panes) had NO signal infrastructure for "the active workspace context just changed". Prior investigation (round 4) had identified that copying LegalWorkspace's 5 section factories into SpaarkeAi would drag in ~30 files / ~10K LOC with `DataverseService` runtime state, `FeedTodoSyncContext`, and the `@hello-pangea/dnd` peer dep — too tangled for a clean copy. Operator principle taken to its logical conclusion: don't copy factories — reuse the WHOLE working `LegalWorkspaceApp` as a single workspace widget. | **RESOLVED (2026-05-21 / task 087, Round 4 Fix 4 Option A)** | High | **Resolved (Option A — Embed)**: (1) added `initialWorkspaceId?: string` + `embedded?: boolean` props to `LegalWorkspaceApp.tsx`; in embedded mode the component suppresses its internal `<PageHeader>` (which carries its own workspace dropdown), footer, outer `<FluentProvider>`, and cross-device theme-sync side effects so it composes cleanly inside a SpaarkeAi tab. (2) added `src/solutions/LegalWorkspace/src/index.ts` barrel exporting `LegalWorkspaceApp` + `ILegalWorkspaceAppProps`. (3) added `@spaarke/legal-workspace` Vite alias + transpilation include in `src/solutions/SpaarkeAi/vite.config.ts` and added `@hello-pangea/dnd ^18.0.1` to SpaarkeAi's deps (required by LegalWorkspace's SmartToDo/KanbanBoard). (4) created `src/client/shared/Spaarke.AI.Widgets/src/widgets/workspace/WorkspaceLayoutWidget.tsx` — a `WorkspaceWidgetComponent<{ layoutId, layoutName }>` that resolves Xrm via the frame-walk pattern and renders `<LegalWorkspaceApp initialWorkspaceId={data.layoutId} embedded />`; registered widget type `'workspace'` with `allowMultiple: true` so multiple workspace tabs can coexist (FIFO eviction still applies via MAX_WORKSPACE_TABS). (5) `WorkspacePaneMenu.handleLayoutSelect` now dispatches `widget_load` on the workspace channel carrying `{ widgetType: 'workspace', widgetData: { layoutId, layoutName }, displayName: layoutName }` in addition to the legacy applyActiveLayout signal — the pane subscriber's existing `widget_load` handler opens the tab via the standard pipeline. (6) Added a new optional `displayName?: string` field on `WorkspacePaneEvent` so per-instance tab labels (e.g. "Corporate Workspace") override the generic registry label "Workspace"; `WorkspacePane.tsx`'s widget_load handler now honours this override. (7) Signal infrastructure for future pane coordination: new `'active_widget_changed'` event type on the workspace channel; new `ActiveTabSnapshot` interface + `onActiveTabChange` option in `WorkspaceTabManagerOptions`; emit sites at `addTab` / `setActiveTab` (delta-guarded) / `closeTab` (when active changes) / `clearAllTabs` (when active changes); `WorkspacePane.tsx` wires the manager callback through a forwarding ref into `dispatch("workspace", { type: "active_widget_changed", widgetType, widgetData, tabId, displayName })`. NO consumers are wired in this task — Assistant + Context pane scoping is deferred to a follow-up. Bundle delta: SpaarkeAi 801.85 → 893.91 KB gzip (+92 KB, +11.5%) — much smaller than the ~570 KB worst case because SpaarkeAi already bundles @fluentui, lexical, react-window, @spaarke/ui-components/auth, and WorkspaceShell; only the 5 LegalWorkspace section factories + DataverseService + FeedTodoSyncContext + @hello-pangea/dnd are net new. LegalWorkspace bundle 569.29 KB gzip — byte-identical to baseline (FR-25 / NFR-10 satisfied) because the standalone runtime entry is `main.tsx → App.tsx` and never imports `LegalWorkspaceApp.tsx`. ADR-012 (the section factory hoist debate is sidestepped — embedded reuse instead), ADR-021 (Fluent v9 tokens only), ADR-022 (React 19 functional), ADR-028 (LegalWorkspace's existing auth surface preserved) all complied with. Deployed to spaarkedev1. See deploy memo "Supplemental Deploy — Round 4 Fix 4". |

---

## 9. Next Steps

1. **Review this plan.md** — confirm phase structure and resource discovery match expectations
2. **Run** `task-create` (called by /project-pipeline Step 3) to generate `tasks/{001..090}-*.poml` task files + `TASK-INDEX.md`
3. **Commit + push** project artifacts on current `work/spaarke-ai-platform-unification-r3` branch; open draft PR
4. **Begin** Phase 0 (task 001 — FR-07 backend spike) via `task-execute` skill

---

**Status**: Ready for Tasks
**Next Action**: Generate task files via `task-create` skill

---

*For Claude Code: This plan provides implementation context. Load relevant sections when executing tasks. The Discovered Resources section in §2 is the canonical list of ADRs / skills / patterns / guides that task files MUST reference in their `<knowledge>` blocks.*
