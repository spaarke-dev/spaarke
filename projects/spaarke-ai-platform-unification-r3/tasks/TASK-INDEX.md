# Task Index — Spaarke AI Platform Unification R3

> **Last Updated**: 2026-05-20
> **Total Tasks**: 36 (1 spike + 4 foundations + 7 Assistant + 6 Workspace + 6 Context + 2 backend conditional + 4 verify + 5 deploy/smoke + 1 wrap-up)
> **Project Status**: Not started
> **Status Legend**: 🔲 not-started · 🔄 in-progress · 🚫 blocked · ✅ completed · ⏭️ skipped

---

## Quick Status

| Phase | Tasks | Status |
|---|---|---|
| Phase 0 (Spike) | 1 (001) | ✅ |
| Phase A (Foundations) | 4 (010-013) | ✅ |
| Phase B (Assistant) | 7 (020-026) | 🔲 |
| Phase C (Workspace) | 6 (030-035) | 🔲 |
| Phase D (Context) | 6 (040-045) | 🔲 |
| Phase E (Backend, conditional) | 2 (050, 051) | ✅ — Phase E COMPLETE (per spike 001 decision; memo: notes/spikes/001-fr07-attachments-payload.md) |
| Phase F (Verification) | 5 (060-063, 065) | ✅ all 5 complete — NFR-09 fix landed in task 065 |
| Phase G (Deploy + Smoke) | 5 (070-074) | 🔄 (070 ✅ deployed; 071-074 in-progress — operator-driven smoke) |
| Phase H (Wrap-up) | 1 (090) | 🔲 |

---

## Full Task Listing

| ID | Title | Phase | Status | Dependencies | Blocks | Parallel | Rigor | Est. h |
|----|-------|-------|--------|--------------|--------|----------|-------|--------|
| 001 | Spike: FR-07 attachments payload verification | 0 (Spike) | ✅ | none | 010-013, 026, 050, 051 | — (serial) | STANDARD | 1 |
| 010 | Create `<PaneHeader>` shared component | A (Foundations) | ✅ | 001 | 021, 022, 030, 032, 040 | A | FULL | 2 |
| 011 | Configure `MAX_WORKSPACE_TABS = 8` + FIFO | A (Foundations) | ✅ | 001 | 032 | A | FULL | 2 |
| 012 | Lift `ActionCard` to `@spaarke/ui-components` (or verify shared) | A (Foundations) | ✅ | 001 | 041 | A | FULL | 3 |
| 013 | Error-only telemetry helpers | A (Foundations) | ✅ | 001 | 022, 035 | A | STANDARD | 2 |
| 020 | WelcomePanel chrome trim | B (Assistant) | ✅ | 001, 010-013 | — | B | FULL | 2 |
| 021 | ConversationPane → PaneHeader | B (Assistant) | ✅ | 010 | 022 | B (serial w/ 022 on ConversationPane.tsx) | FULL | 2 |
| 022 | HistoryOverlay component + wiring | B (Assistant) | ✅ | 010, 021 | — | B (serial w/ 021) | FULL | 3 |
| 023 | SprkChatInput editable on cold load | B (Assistant) | ✅ | 010 | 025, 026 | B (serial w/ 025 on SprkChat.tsx) | FULL | 2 |
| 024 | `useChatFileAttachment` hook + lazy extraction | B (Assistant) | ✅ | 010 | 025, 026 | B | FULL | 4 |
| 025 | SprkChat toolbar restructure (+, remove Word) | B (Assistant) | ✅ | 023, 024 | 026 | B (serial w/ 023) | FULL | 3 |
| 026 | Wire attachments into chat send payload | B (Assistant) | ✅ | 001, 024, 025 | — | — (serial — gated by spike) | FULL | 3 |
| 030 | WorkspacePane → PaneHeader + embed LegalWorkspace | C (Workspace) | ✅ | 010 | 031, 032, 034 | C (serial on WorkspacePane.tsx) | FULL | 4 |
| 031 | Delete `WorkspaceLandingWidget.tsx` | C (Workspace) | ✅ | 030 | — | C (serial w/ 030) | STANDARD | 1 |
| 032 | `WorkspacePaneMenu` Dropdown component | C (Workspace) | ✅ | 010, 011, 030 | — | C (serial on WorkspacePane.tsx) | FULL | 4 |
| 033 | `WorkspaceLayoutWizard.templateFilter` prop | C (Workspace) | ✅ | 001 | — | C | STANDARD | 2 |
| 034 | Daily Briefing section + `useDailyBriefing` hook | C (Workspace) | ✅ | 030 | 035 | C | FULL | 4 |
| 035 | Daily Briefing 429 + empty state | C (Workspace) | ✅ | 013, 034 | — | C (depends on 034) | FULL | 3 |
| 040 | ContextPaneController → PaneHeader + "Get Started" label | D (Context) | ✅ | 010 | 042 | D (serial on ContextPaneController.tsx) | FULL | 3 |
| 041 | `GetStartedCardsWidget` (7 cards, 2-col grid) | D (Context) | ✅ | 012 | 042 | D | FULL | 3 |
| 042 | Register widget + welcome-stage swap | D (Context) | ✅ | 040, 041 | — | D (serial on ContextPaneController.tsx) | FULL | 3 |
| 043 | Wizard widget wrappers (CreateProject, FindSimilar) | D (Context) | ✅ | 001 | — | D | FULL | 2 |
| 044 | Analysis Builder intents (email-compose, meeting-schedule) | D (Context) | ✅ | 001 | — | D | STANDARD | 2 |
| 045 | `AssignWorkWizardLauncher` (Xrm.Navigation) | D (Context) | ✅ | 001 | — | D | FULL | 2 |
| 050 | Extend ChatEndpoints with attachments[] (CONDITIONAL) | E (Backend) | ✅ | 001 | 051, 026 | E | FULL | 3 |
| 051 | BFF unit tests for attachments[] payload (CONDITIONAL) | E (Backend) | ✅ | 050 | — | E (serial w/ 050) | STANDARD | 2 |
| 060 | Auth audit (no token snapshots, all via `authenticatedFetch`) | F (Verification) | ✅ | All B/C/D | 070 | F | STANDARD | 2 |
| 061 | Bundle-size verification (<250 KB gzip delta vs R2) | F (Verification) | ✅ | All B/C/D | 070 | F | STANDARD | 2 |
| 062 | Dark-mode token audit (no hex/rgba in new code) | F (Verification) | ✅ | All B/C/D | 070 | F | STANDARD | 2 |
| 063 | Backwards-compat verification (standalone LW + persistence) | F (Verification) | ✅ | C tasks | 070 | F | STANDARD | 2 |
| 065 | Extend SessionPersistence with workspace tabs[] (NFR-09 fix) | F (Verification — remediation) | ✅ | 011, 030, 050, 063 | 070 | — (serial) | FULL | 5 |
| 066 | Redeploy WorkspaceLayoutWizard + update Deploy-WizardCodePages.ps1 (Bug 3 fix from smoke) | F (Verification — remediation) | ✅ | 032, 033, 070 | 072 | — (serial) | STANDARD | 0.5 |
| 067 | Hoist workspace section registry to @spaarke/ui-components (Bug 2 fix + ADR-012 compliance) | F (Verification — remediation) | ✅ | 030, 034, 070 | 072 | — (serial) | FULL | 12 |
| 068 | Smoke remediation: Bug 1 (chat box) + UX-A (Recent Conversations) + UX-B (all wizards as popups) | F (Verification — remediation) | ✅ | 020, 021, 023, 025, 042, 067, 070 | 071, 072, 073 | — (serial) | FULL | 2.5 |
| 069 | Daily Briefing as SpaarkeAi Home tab default content (Option Z minimum scope; Bug 2 visual fix) | F (Verification — remediation) | ✅ | 034, 067, 068, 070 | 072 | — (serial) | FULL | 5 |
| 081 | WorkspacePaneMenu config-ready fetch fix (smoke remediation round 2) | F (Verification — remediation) | ✅ | 032, 070 | 072 | — (serial) | STANDARD | 1 |
| 082 | SprkChat UX fixes — input focus restore + slash menu outside-click (smoke remediation round 2) | F (Verification — remediation) | ✅ | 023, 025, 070 | 071 | — (serial) | STANDARD | 1 |
| 083 | BFF resilience: missing Dataverse manifest + empty Daily Briefing payload (smoke remediation round 2 — unblocks chat + Daily Briefing in dev) | F (Verification — remediation) | ✅ | 050, 070 | 071, 072 | — (serial) | STANDARD | 1.5 |
| 084 | WorkspacePaneMenu: replace task-081 parallel implementation with reused LegalWorkspace `useWorkspaceLayouts` hook (Round 4 Fix 1 — workspaces dropdown still empty after 081) | F (Verification — remediation) | ✅ | 032, 081 | 072 | — (serial) | STANDARD | 2 |
| 085 | Get Started wizard launchers: hoist LegalWorkspace's exact navigateTo handlers to `@spaarke/ui-components` and reuse from SpaarkeAi ContextPaneController (Round 4 Fix 2 — wizards opened from Context-pane cards now route through the proven LegalWorkspace call shape; removes parallel `launchCodePagePopup` + `launchAssignWorkWizard` helpers) | F (Verification — remediation) | ✅ | 042, 044, 045, 068 | 073 | — (serial) | STANDARD | 3 |
| 086 | Daily Briefing functional in SpaarkeAi Home tab (Round 4 Fix 3 — Hoist optional `loadNotificationContext` callback into shared `useDailyBriefing` hook + `createDailyBriefingRegistration` factory; Copy standalone Daily Briefing Code Page's Xrm.WebApi `appnotification` query + payload assembly into SpaarkeAi-local `notificationContextLoader.ts`; wire into `WorkspaceHomeTab.tsx` so the narrate endpoint receives a populated payload — same data path as the standalone, so the embedded section returns real AI bullets on cold load instead of the empty-state UI; LegalWorkspace shim untouched preserves FR-25) | F (Verification — remediation) | ✅ | 034, 035, 069, 083 | 072 | — (serial) | FULL | 4 |
| 087 | Workspace dropdown opens chosen workspace via embedded `LegalWorkspaceApp` (Round 4 Fix 4, Option A — Add `initialWorkspaceId` + `embedded` props to `LegalWorkspaceApp.tsx`; create `WorkspaceLayoutWidget` that embeds the full LegalWorkspace experience as a single workspace pane tab; wire `WorkspacePaneMenu.handleLayoutSelect` to dispatch `widget_load` so the existing tab pipeline opens it; add `active_widget_changed` event + `onActiveTabChange` manager callback as foundation signal infrastructure for future Assistant/Context pane coordination — no consumers wired in this task; @hello-pangea/dnd added to SpaarkeAi as a transitive dep of LegalWorkspace's SmartToDo; SpaarkeAi gzip +92 KB; standalone LegalWorkspace bundle byte-identical so FR-25/NFR-10 preserved) | F (Verification — remediation) | ✅ | 067, 084, 086 | 072 | — (serial) | FULL | 5 |
| 089 | WorkspacePaneMenu UX cleanup — remove `Open` + `Home` sections (operator feedback 2026-05-21: redundant with the visible tab bar + Home concept retired); rename `Switch Workspace` MenuGroupHeader to `Select Workspace`; add `Manage workspaces` stub entry (onClick logs placeholder — full side pane in task 093); keep `Edit current workspace` (operator did not request removal). Trims styles + icon imports unused by removed sections. `WorkspacePaneMenuProps` callbacks marked optional (back-compat with `WorkspacePane.tsx` call site, no `WorkspacePane.tsx` edit needed). SpaarkeAi 893.43 KB gzip (−0.32 KB vs task 088). LegalWorkspace untouched (FR-25). Deployed to spaarkedev1. | F (Verification — remediation) | ✅ | 088 | — | — (serial) | STANDARD | 1 |
| 091 | Workspace builder UX iteration (Round 5 — operator smoke). **Fix 1**: empty slots in the layout canvas are now save-tolerant — the wizard already only required a workspace name to advance; `buildDynamicWorkspaceConfig` now silently skips empty section IDs (was logging a warning) so user-removed slots render as empty space instead of console noise. **Fix 2**: each filled slot in the Arrange Sections step now renders a small `DismissRegular` X button in its top-right corner — clicking removes the section from the slot and returns it to the unassigned pool; visible on hover and on focus (a11y); does NOT interfere with HTML5 drag (stopPropagation + draggable=false). **Fix 3**: added `Pin to Start` checkbox to Step 3's inline row (next to `Set default`) with `PinRegular` icon; default unchecked; persistence stubbed via `sessionStorage` key `spaarke:workspace:pinned-layout-id` (only one pinned at a time). BFF DTO (`CreateWorkspaceLayoutRequest` / `UpdateWorkspaceLayoutRequest`) NOT modified — task 092 will add `IsPinned` field + `sprk_workspacelayout.sprk_ispinned` column + wire SpaarkeAi auto-open. Wizard's `__dialogResult` now carries the `pinToStart` flag for callers that want to react immediately. FR-25 preserved — standalone LegalWorkspace passes neither `templateFilter` nor `pinToStart`; both default gracefully (LegalWorkspace bundle 569.31 KB gzip vs 569.30 baseline = +10 bytes from the empty-slot guard's two new lines; SpaarkeAi 893.44 KB gzip vs 893.43 baseline = +10 bytes). Wizard bundle 427.58 KB gzip. Deployed to spaarkedev1 (sprk_workspacelayoutwizard + sprk_corporateworkspace + sprk_spaarkeai all updated). | F (Verification — remediation) | ✅ | 033, 088 | 092 | — (serial) | FULL | 3 |
| 088 | Embed wiring fixes (Round 4 Fix 4.1 — three bugs from task 087 operator smoke). **Bug 1**: each embedded LegalWorkspaceApp tab showed the same "last-active" workspace because `useWorkspaceLayouts` cache-hydrated from a sessionStorage key shared across all tabs. Added new `embedded?: boolean` option to `useWorkspaceLayouts` (backwards-compat overload) — when true, hook SKIPS sessionStorage reads + writes so each tab honours its own `initialWorkspaceId` deep-link only. Wired through `WorkspaceGrid` and `LegalWorkspaceApp`. **Bug 2**: all workspace tabs showed title "Workspace" (registry metadata's generic label) instead of the chosen layout name. `WorkspacePaneMenu.handleLayoutSelect` now sets `displayName: layoutName` at the TOP LEVEL of the `widget_load` event (not just `widgetData.layoutName`) so `WorkspacePane.tsx`'s displayName-precedence ladder picks the per-instance label. **Bug 3**: document preview + other actions inside the embedded tree threw "[LegalWorkspace] Runtime config not initialized" because LegalWorkspace has its OWN `runtimeConfig` singleton (distinct from SpaarkeAi's) and SpaarkeAi's `main.tsx` only initialized SpaarkeAi's. Option A (dual-init): re-exported `setRuntimeConfig as setLegalWorkspaceRuntimeConfig` from LegalWorkspace's barrel; SpaarkeAi's bootstrap calls it with the SAME `IRuntimeConfig` after its own `setRuntimeConfig(config)`. Both singletons remain distinct in-process instances holding equivalent values. Standalone LegalWorkspace untouched at the runtime entry point (its own `main.tsx → App.tsx` still calls its own internal `setRuntimeConfig`); FR-25 / NFR-10 preserved (gzip 569.30 KB vs 569.22 KB baseline = +80 bytes of structural code for new option object normalization in `useWorkspaceLayouts`; standalone code paths never traverse the new branches). SpaarkeAi bundle 893.75 KB gzip (−0.16 KB vs task 087 baseline). Deployed to spaarkedev1. | F (Verification — remediation) | ✅ | 087 | 072 | — (serial) | FULL | 2 |
| 092 | Tab pinning + auto-open pinned workspaces on SpaarkeAi startup (Round 5 — operator UX iteration extending task 091). **Storage**: replaced task 091's single-pin `sessionStorage` stub with a multi-pin `localStorage` list keyed `spaarke:workspace:pinned-list` — array of `{ layoutId, layoutName }` records. New utility `src/solutions/SpaarkeAi/src/services/pinnedWorkspaces.ts` exports `getPinnedWorkspaces()` / `isPinned()` / `pinWorkspace()` / `unpinWorkspace()` with try/catch around every localStorage call (private browsing / quota safe). Wizard's `App.tsx` updated to the same shape (BFF DTO still NOT modified; no user-preferences endpoint exists in the codebase). **Auto-open**: new mount effect in `WorkspacePane.tsx` reads the pinned list AFTER auth ready (`isAuthenticated` guard), filters out workspaces that are already open (duplicate guard by `widgetData.layoutId`), and dispatches `widget_load` for each remaining pin — same pipeline `WorkspacePaneMenu.handleLayoutSelect` uses, so pinned tabs hydrate via the existing tab manager + `WorkspaceLayoutWidget` chain. Home tab still installs as default; pinned tabs open IN ADDITION (user can close Home if they don't want it). One-shot per mount via `autoOpenedPinsRef` so token refresh doesn't re-stack tabs. **Pin icon on tabs**: `WorkspaceTabManagerComponent.tsx` now renders a `PinRegular` / `PinFilled` button next to the close button on every workspace tab (`widgetType === "workspace"` + `widgetData.layoutId` present, `kind === "widget"`). Home tab + non-workspace widget tabs (Create Matter wizard etc.) do NOT show the pin icon. Click toggles the localStorage list + updates local React `pinnedIds` Set so the icon flips synchronously. Pinned state uses `tokens.colorBrandForeground1` for visual prominence; unpinned matches close-button subtle treatment. Tooltips read "Pin {name} to start" / "Unpin {name} from start"; `aria-pressed` reflects state. **Build**: SpaarkeAi 894.21 KB gzip (+0.77 KB vs task 091 baseline 893.44); wizard 427.76 KB gzip (+0.18 KB vs 427.58). LegalWorkspace untouched (FR-25 / NFR-10). **TODO documented**: BFF user-preferences endpoint (cross-device sync) deferred to next review — no such endpoint exists in `Sprk.Bff.Api` today (the `UserPreferences` records in `AiIntentClassificationSchema.cs` / `PlaybookRunContext.cs` are AI-pipeline payloads, not a generic preferences surface). Deployed to spaarkedev1 (sprk_spaarkeai + sprk_workspacelayoutwizard + the 7 other wizard pages refreshed by the all-wizards deploy script). | F (Verification — remediation) | ✅ | 088, 091 | — | — (serial) | FULL | 3 |
| 096 | Shared pane foundation polish (Round 6 — operator smoke feedback 2026-05-22). Three surgical changes in `@spaarke/ui-components` to ground the next wave of pane revisions: **(1) PaneHeader title font** — `<Text>` size prop bumped `300 → 400` (one Fluent v9 step ≈ +2px / `fontSizeBase300` → `fontSizeBase400`) for stronger visual hierarchy across all three SpaarkeAi panes; **(2) Collapsed strip width** — `leftPaneCollapsed` / `centerPaneCollapsed` / `rightPaneCollapsed` widened from 28/28/36px to a uniform 48px via a new top-of-file `COLLAPSED_STRIP_PX = 48` constant; mirrors the Model-Driven Apps left-nav collapsed width per operator request (the previous 28px strip was too narrow to read or click reliably); **(3) Icon-only collapsed identifier API** — added three new optional props to `ThreePaneLayoutProps`: `leftCollapsedIcon?: React.ReactElement` / `centerCollapsedIcon?: React.ReactElement` / `rightCollapsedIcon?: React.ReactElement`. When provided, the collapsed strip renders the icon centered (in a new `collapsedIcon` style: `tokens.fontSizeBase400`, `tokens.colorNeutralForeground2` → `colorNeutralForeground1` on hover) and SUPPRESSES the rotated-text rendering; the existing `*PaneCollapseLabel` text is retained as the strip's accessible name via `aria-label` on the clickable strip so screen readers still announce the pane name. **Backwards-compat**: when `*CollapsedIcon` is NOT provided, the legacy `<ChevronXXX>` + rotated-text rendering still works unchanged — standalone LegalWorkspace + any caller that hasn't been updated continues to render as before. **DELIBERATELY DEFERRED**: this task does NOT modify any SpaarkeAi pane component (`ConversationPane.tsx` / `WorkspacePane.tsx` / `ContextPaneController.tsx` / `ThreePaneShell.tsx`) — Wave 2 (tasks 097-099 to be created) will wire `leftCollapsedIcon` / `centerCollapsedIcon` / `rightCollapsedIcon` per pane and apply pane-specific operator feedback (Assistant history dropdown, Workspace tab/dropdown changes, Context tool dropdown polish + pin). Until Wave 2 lands, all three panes continue rendering the rotated-text identifier — operator sees the widened 48px strip + larger title font immediately, icon-only behavior arrives with Wave 2. **Build**: shared lib TypeScript clean; SpaarkeAi 896.67 KB gzip (+0.08 KB / +80 bytes vs task 095 baseline 896.59 — within near-zero envelope); LegalWorkspace 569.38 KB gzip (+0.03 KB / +30 bytes vs task 095 baseline 569.35 — within the +0.5 KB tolerance, entirely from the PaneHeader `size={400}` token-reference change; LegalWorkspace doesn't import ThreePaneLayout). FR-25 / NFR-10 preserved (LegalWorkspace standalone behavior unchanged; the +30 byte delta is a token-reference cost only). ADR-012 (shared lib placement), ADR-021 (Fluent v9 tokens only — `colorNeutralForeground1/2`, `fontSizeBase400`; the 48px constant is a documented layout-dimension literal with no matching token), ADR-022 (React 19 functional). Deployed to spaarkedev1 (`sprk_spaarkeai` resource `5206a442-3451-f111-bec7-7ced8d1dc988`). See deploy memo "Supplemental Deploy — Task 096 (shared pane foundation polish)". | F (Verification — remediation) | ✅ | 010 | 097, 098, 099 | — (serial; foundation for Wave 2) | FULL | 2 |
| 098 | Workspace pane revisions — tabs + dropdown polish (Round 6 operator smoke 2026-05-22, Wave 2 commit 2 of 3). **WorkspaceTabManagerComponent.tsx**: removed the inline pin button from every tab row (operator: "pin belongs in the workspace selection surface, not on every open tab") — dropped `PinRegular`/`PinFilled` imports, `pinnedIds` derived `Set<string>` state + sync effect, `handlePinToggle` callback, and the per-tab Tooltip+Button rendering; bumped tab title font one Fluent v9 step (`tokens.fontSizeBase200` → `tokens.fontSizeBase300` with matching `lineHeightBase200` → `lineHeightBase300`); downsized close × icon by swapping `DismissRegular` (20px) → `Dismiss12Regular` (12px) — both are real `@fluentui/react-icons` v9 exports; deleted `pinButton`/`pinButtonActive` style entries. **WorkspacePaneMenu.tsx**: trigger button label "Workspace" → "Workspaces" (operator: plural — the dropdown lists ALL workspaces, not just the current one); each MenuItem in the Select Workspace section now renders a `<div className={styles.layoutRow}>` containing `[pin Button] [layout name <span>] [CheckmarkRegular if active]` — pin icon is `PinRegular`/`PinFilled` with `tokens.colorBrandForeground1` when pinned; pin Button click stops propagation so the parent MenuItem.onClick (handleLayoutSelect) does NOT also fire; onKeyDown stops Enter/Space from bubbling when the Button is focused (Button still handles activation natively); pin state mirrored into a React `Set<string>` (`pinnedIds`) initialised from `getPinnedWorkspaces()` with a `useEffect` keyed on `menuOpen` for defensive re-sync; writes go through `pinWorkspace`/`unpinWorkspace` from `services/pinnedWorkspaces.ts` (task-092 storage contract unchanged). "Edit current workspace" MenuItem + `handleEditWorkspace` callback + `EditRegular` icon import all removed (operator: "the dropdown should be for SELECTION, not editing — editing lives in the manage surface"); JSDoc breadcrumb left at the deletion site pointing to task 093 (Manage workspaces side pane). `+ New Workspace` and `Manage workspaces` entries untouched. **Not touched**: `ThreePaneShell.tsx` (centerCollapsedIcon wiring deferred to main session after Wave 2 lands — avoids 3-way merge); `WorkspacePane.tsx` (cold-load auto-open effect untouched — dropdown writes to the same localStorage list the effect reads); `WorkspaceTabManager.ts` (zero pin metadata in this file, verified by grep); `services/pinnedWorkspaces.ts` (consumed unchanged); LegalWorkspace (FR-25 preserved). **Build**: SpaarkeAi **892.78 KB gzip** (−3.89 KB vs task 096 baseline 896.67 — reduction from deleted per-tab pin code + dropped EditRegular/handleEditWorkspace + unused pinButton styles); LegalWorkspace not rebuilt. ADR-012 (both files SpaarkeAi-local — correct), ADR-021 (Fluent v9 tokens only — `colorNeutralForeground1/3`, `colorBrandForeground1`, `colorNeutralBackground3Hover`, `fontSizeBase200/300` semantic tokens), ADR-022 (React 19 functional), ADR-025 (`Dismiss12Regular`/`PinRegular`/`PinFilled` are `@fluentui/react-icons` v9 — verified via `require('@fluentui/react-icons')`), ADR-028 (no auth surface change). NOT deployed in this task — Wave 2 commit 2 of 3; coordinated deploy after Assistant (097) + Context (099) commits land. See deploy memo "Task 098 — Workspace pane: tabs + dropdown polish". | F (Verification — remediation) | ✅ | 096 | — | Wave-2 (parallel-safe with 097, 099) | FULL | 2 |
| 099 | Context pane revisions — dropdown polish + Quick Start title + Semantic Search field expansion (Round 6 operator smoke 2026-05-22, Wave 2 commit 3 of 3). **ContextPaneController.tsx**: dropped the stage label rendering ("Sources" / "Get Started" / "Entity" / etc.) from the PaneHeader rightSlot per operator request so the Tools dropdown alone sits right-justified, mirroring the Workspace pane's PaneHeader layout. `stageLabelMap` + `contextStage` state retained for ARIA / debug — exposed as `data-context-stage` on the root for test harnesses (Playwright / smoke) without rendering visible text. Wrapped the welcome-stage `<GetStartedCardsWidget>` in a new "Quick Start" section header (`<Text size={400} weight="semibold">`, color `colorNeutralForeground1`, padding `spacingVerticalM` top / `spacingVerticalS` bottom / `spacingHorizontalM` sides) so the user can SEE which Context tool is currently active inside the pane body. `GetStartedCardsWidget` itself (in `@spaarke/ai-widgets` shared lib) is NOT modified — header is rendered AROUND it. **ContextPaneMenu.tsx**: added clickable pin icon on the LEFT of each tool's MenuItem (`PinRegular` outline at `colorNeutralForeground3` when not pinned / `PinFilled` solid at `colorBrandForeground1` when pinned). Single-pin "default on load" semantics — pinning a new tool implicitly unpins the previous one via the new storage utility. Pin Button click uses `stopPropagation()` + `preventDefault()` so it does NOT trigger the MenuItem's `onSelectTool` (which would change the active tool — but pin should only update the default, not the active selection). Tooltip + `aria-pressed` + `aria-label` ("Pin Quick Start as default" / "Unpin Quick Start") provided. Pin state hydrated on mount via `getPinnedContextTool()` + re-read whenever the menu opens (covers other-tab pin changes). Pin styling (`toolRow` / `pinButton` / `pinButtonActive`) mirrors task 098's posture on the Workspace pane verbatim so the operator sees a coherent cross-pane pin UX. `<CheckmarkRegular>` active marker remains on the row's icon prop to indicate the currently SELECTED tool (which may differ from the pinned default at runtime). **SemanticSearchCriteriaTool.tsx**: replaced single-select Domain `<Dropdown>` with a 2×2 grid of **4 type-toggle buttons** — Documents (`DocumentRegular`), Matters (`BriefcaseRegular`), Projects (`FolderRegular`), Invoices (`ReceiptRegular`); active type uses `appearance="primary"` (brand fill); the rest use `appearance="subtle"`. Added **per-domain filter dropdowns**: Document Type + File Type (Documents domain only), Matter Type (Documents + Matters domains). Static placeholder option lists today (`Contract`/`NDA`/`Brief`/`Agreement`/`Invoice`; `PDF`/`DOCX`/`XLSX`/`PPTX`/`TXT`; `Litigation`/`Transactional`/`Regulatory`/`Advisory`) with a TODO comment for filter-options endpoint wiring — the full SemanticSearch Code Page's `useFilterOptions` hook fetches real values on the page side once the modal opens. Added **Date Range dropdown** with preset options: Off / Last 7 days / Last 30 days / Last 90 days / Custom range (the "Custom range" option is static today; expanding to inline date inputs is documented as a follow-up in the file header). Removed the previous from/to `<Input type="date">` row and the `headerSubtitle` "Refine your query..." line (layout matches the operator's screenshot without them). Search button is now full-width primary at the bottom. Build URL `data` payload extended with `documentType` / `fileType` / `matterType` / `dateRange` params (only emitted when relevant for the active domain AND non-default). Backwards-compat persistence in `localStorage["spaarke:context:semantic-search-criteria"]` — missing fields read as undefined and default to "All" / "Off" on next mount; legacy `dateFrom` / `dateTo` fields silently ignored. **useContextTool.ts**: `readPersistedTool()` now consults the new `getPinnedContextTool()` utility as the fallback when no `selected-tool` value exists in localStorage. If both `selected-tool` AND `pinned-tool` exist, `selected-tool` wins (user's last-used > pinned default). Makes the pin's "default on load" semantics take effect on first session + cold reset without disturbing existing tool-selection persistence. **contextToolPin.ts (NEW)**: exports `getPinnedContextTool()` / `pinContextTool(id)` / `unpinContextTool()`. Single-pin storage at `localStorage["spaarke:context:pinned-tool"]` (JSON string). Validated against `VALID_CONTEXT_TOOL_IDS` on every read; corrupt / unknown values fall back to `null`. Same try/catch-wrapped posture as `useContextTool.ts` (task 095) and `usePaneCollapse.ts` (task 094) — silent failure on private-browsing / quota-exceeded. **Not touched**: `ThreePaneShell.tsx` (rightCollapsedIcon wiring deferred to main session after Wave 2 lands — avoids 3-way merge); `@spaarke/ai-widgets/GetStartedCardsWidget` (out of scope — header rendered AROUND it by the controller); `@spaarke/ui-components/PaneHeader` (Wave 1 sealed); SemanticSearch Code Page (read for visual reference only — `SearchDomain` union replicated locally to keep bundle tree-shake clean); LegalWorkspace (FR-25 / NFR-10 preserved). **Build**: SpaarkeAi **894.23 KB gzip** (−2.44 KB / −0.27% vs task 096 baseline 896.67 — net reduction from removing two `<Input type="date">` widgets + `headerSubtitle` Text + `dateRow` style block + ContextPaneController rightSlot wrapper div + Text, replaced by lighter type-button grid + 3 Dropdowns + Date Range Dropdown); LegalWorkspace not rebuilt (no files touched). ADR-012 (all five files SpaarkeAi-local — correct), ADR-021 (Fluent v9 tokens only — `colorBrandForeground1`, `colorNeutralForeground1/2/3`, `colorNeutralBackground1/3Hover`, `colorNeutralStroke2`, `spacingHorizontal*`/`spacingVertical*`, `fontSizeBase200`, `fontWeightSemibold`, `borderRadiusMedium`), ADR-022 (React 19 functional), ADR-025 (`PinRegular`/`PinFilled`/`Search20Regular`/`DocumentRegular`/`BriefcaseRegular`/`FolderRegular`/`ReceiptRegular`/`ChevronDownRegular`/`AppsListRegular`/`SearchRegular`/`CheckmarkRegular` are all `@fluentui/react-icons` v9), ADR-028 (no auth surface change — criteria stay client-side; `sprk_semanticsearch` has its own MSAL bootstrap; pin / selected-tool localStorage only). NOT deployed in this task — Wave 2 commit 3 of 3; coordinated deploy after Assistant (097) + Workspace (098) commits land. See deploy memo "Task 099 — Context pane: dropdown polish + Quick Start title + Semantic Search field expansion". | F (Verification — remediation) | ✅ | 095, 096 | — | Wave-2 (parallel-safe with 097, 098) | FULL | 2 |
| 101 | Pin persistence fix — Context tool + Workspace auto-open across browser sessions (Round 7 operator smoke 2026-05-22, Wave-2-R7 commit 1 of 3). **Bug A (Context tool)**: task 095 stored `selected-tool` in `localStorage`; task 099's "pin = default on load" only fires when that key is null, but `setSelectedTool` writes it on every click so once a user clicked anything the pin was ignored forever. Fix: split storage by lifetime — `selected-tool` moves to **sessionStorage** (preserves task 095 modal-close-restoration within a session); pin stays in **localStorage** (`services/contextToolPin.ts` unchanged). One-time migration in `readPersistedTool()` reads any leftover localStorage value, seeds sessionStorage, deletes the legacy key (idempotent, safe under private-browsing / quota errors). Pin is now authoritative on cold mount whenever sessionStorage is empty. **Bug B (Workspace auto-open)**: the auto-open effect in `WorkspacePane.tsx` (task 092) dispatched `widget_load` events BEFORE the `usePaneEvent('workspace', ...)` subscription was registered on the PaneEventBus. Both are useEffects; React runs effects in declaration order, so the auto-open effect (declared at line 387) ran before usePaneEvent's internal subscribe-useEffect (called via the hook at line 427). PaneEventBus.dispatch silently drops events with zero subscribers (`PaneEventBus.ts` lines 128-129). The pin localStorage was read correctly; the events just fell on the floor. Fix: defer dispatches to a macrotask via `setTimeout(..., 0)` so by the time the timer fires every useEffect from this render's commit phase has executed (including usePaneEvent's), making the subscription live. Added `console.info('[WorkspacePane] Auto-opening N pinned workspace(s):', pins)` at the dispatch site for operator-visible DevTools observability during smoke. Effect cleanup clears the timer on unmount to avoid late dispatch into a torn-down tree. **Files changed**: `src/solutions/SpaarkeAi/src/hooks/useContextTool.ts` (storage split + one-time migration + header docblock rewrite); `src/solutions/SpaarkeAi/src/components/workspace/WorkspacePane.tsx` (auto-open effect: filter `pinsToOpen` early, wrap dispatch in `setTimeout`, log via `console.info`, return cleanup). **NOT touched**: `services/contextToolPin.ts` (pin localStorage API correct as-is); `services/pinnedWorkspaces.ts` (storage key was correct — cause #3 in the brief was a false lead); LegalWorkspace (FR-25 / NFR-10 preserved). **Build**: SpaarkeAi **899.64 KB gzip** (+0.25 KB vs task 100 baseline 899.39 — minor growth from storage-split logic + console.info + JSDoc); LegalWorkspace not rebuilt. ADR-021 (no styling changes — pure state/effect logic), ADR-022 (React 19 — `useEffect` / `setTimeout` standard patterns; no new hooks), ADR-028 (no auth surface change — both fixes are pure client-side state; `isAuthenticated` consumed as before). NOT deployed — coordinated single deploy by main session after Wave-2-R7 (101, 102, 103) all land. R-32 → **RESOLVED**. R-33 → **RESOLVED**. See deploy memo "Task 101 — Pin persistence fix (Context tool + Workspace auto-open)". | F (Verification — remediation) | ✅ | 092, 099 | — | Wave-2-R7 (parallel-safe with 102, 103) | FULL | 2 |
| 102 | Workspace dropdown small fixes — template-filter consistency + "+/+" duplicate removal + dropdown ordering (Round 7 operator smoke 2026-05-22, Wave-2-R7 commit 2 of 3). Three small operator findings consolidated. **Fix 1 (template filter)**: `WorkspacePaneMenu` (Create launch) passed the 6-template `SPAARKEAI_TEMPLATE_FILTER` to the wizard; `ManageWorkspacesPane.launchEditWizard` (Edit launch, task 093) did NOT, so the wizard surfaced all 9 templates from its `LAYOUT_TEMPLATES` registry. Factored the constant out to a new shared file `src/solutions/SpaarkeAi/src/constants/workspaceTemplateFilter.ts` (with `SpaarkeAiTemplateId` type) and imported from BOTH `WorkspacePaneMenu.tsx` (Create) and `ManageWorkspacesPane.tsx` (Edit). `launchEditWizard`'s data-params string now appends `templateFilter=...` so `WorkspaceLayoutWizard.main.tsx` parses it into a typed `LayoutTemplateId[]` and `TemplateStep` filters accordingly. FR-25 preservation: the 3-column templates remain in the wizard's `LAYOUT_TEMPLATES` registry (`templateFilter` is a runtime membership SET, not a code deletion); standalone LegalWorkspace continues to launch with no filter → all 9 templates remain visible there. **Fix 2 ("+ +" duplicate)**: `WorkspacePaneMenu`'s `+ New Workspace` MenuItem rendered "+" twice — once via the `AddRegular` icon prop and once as a literal "+" prefix in the label text. Dropped the literal "+" from the label (now `"New Workspace"`). Ribbon JS / XML audit (operator hypothesis: "in the Ribbon JS the title to 'Add Workspace' includes a text '+' sign"): `Get-ChildItem -Recurse -Include *.js,*.ts,*.tsx,*.xml | Select-String "\+\s*Add Workspace|\+\s*New Workspace"` returned 8 matches — ALL in `src/solutions/{SpaarkeAi,LegalWorkspace}/src/**/*.{ts,tsx}` (component code + JSDoc + the fixed line) plus two task-poml files. NO ribbon XML / JS / command bar definitions surfaced. The "+ +" duplication was entirely caused by the SpaarkeAi menu entry — no ribbon-level source exists. Operator's hypothesis about the source was incorrect; the symptom observation was correct. **Fix 3 (dropdown ordering)**: operator wants "default first, then pinned in current order, then others — no numbers." `WorkspacePaneMenu` previously rendered `layouts.map(...)` in raw BFF order. Added a `React.useMemo` computing `orderedLayouts = [...(activeLayout? [activeLayout] : []), ...pinnedList.filter(p => p.layoutId !== activeLayout?.id).map(p => layouts.find(l => l.id === p.layoutId)).filter(Boolean), ...layouts.filter(l => l.id !== activeLayout?.id && !pinnedIdSet.has(l.id))]` from `(layouts, activeLayout, pinnedIds)`. JSX `layouts.map(...)` replaced with `orderedLayouts.map(...)`. Empty-state safety: `getPinnedWorkspaces()` returns `[]` if storage unavailable → section 2 contributes nothing; `activeLayout` is `null` on cold load → section 1 contributes nothing; list degrades to BFF order in either case. **Decision (`activeLayout` vs `WorkspaceLayoutDto.isDefault`)**: BFF DTO carries `isDefault: bool`, but using `activeLayout` is cleaner — it already encodes the WORKING selection cascade (first-default → first-system → first-layout) AND respects the user's runtime "switch" via `setActiveLayoutById`. Operator sees their current selection at the top if they've switched within the session. **Files modified**: `src/solutions/SpaarkeAi/src/constants/workspaceTemplateFilter.ts` (NEW); `src/solutions/SpaarkeAi/src/components/workspace/WorkspacePaneMenu.tsx` (import shared constant, drop literal "+", add `orderedLayouts` useMemo, swap `layouts.map` → `orderedLayouts.map`); `src/solutions/SpaarkeAi/src/components/workspace/ManageWorkspacesPane.tsx` (import shared constant, append `templateFilter=...` to wizard navigateTo data). **Build**: SpaarkeAi **899.62 KB gzip** (+0.23 KB vs task 100 baseline 899.39 — near-zero envelope as expected for one new small constant file + memo + edit-wizard filter forward); LegalWorkspace not rebuilt (no LegalWorkspace files touched — FR-25 / NFR-10 preserved). ADR-012 (constant placed SpaarkeAi-local, not `@spaarke/ui-components` — 6-template filter is a SpaarkeAi product decision per FR-14; LegalWorkspace must NOT consume this so its FR-25 9-template behavior is preserved), ADR-021 (no styling changes — text + memo + import), ADR-022 (React 19 — `useMemo` correctly scoped to `[layouts, activeLayout, pinnedIds]`), ADR-028 (no auth surface change). NOT deployed — coordinated single deploy by main session after Wave-2-R7 (101, 102, 103) all land. R-34 → **RESOLVED**. R-35 → **RESOLVED**. R-36 → **RESOLVED**. See deploy memo "Task 102 — Workspace dropdown small fixes". | F (Verification — remediation) | ✅ | 091, 098, 093 | — | Wave-2-R7 (parallel-safe with 101, 103) | STANDARD | 1 |
| 100 | Collapsed strip top-alignment + empty-pane width redistribution (Round 7 operator smoke 2026-05-22 — Wave-1-R7). Two CSS-driven fixes in `@spaarke/ui-components/ThreePaneLayout`. **Fix 1 (icon top-alignment)**: the collapsed strip's icon container (task 096 `styles.collapsedIcon`) had `flexGrow: 1`, so the icon container stretched to fill the entire vertical space of its column-flex parent and `alignItems/justifyContent: center` centered the icon in that stretched container — visual result: icon floated mid-strip. Removed `flexGrow: 1`; added `justifyContent: 'flex-start'` to all three `*PaneCollapsed` styles; bumped `paddingTop: spacingVerticalS → spacingVerticalM` (~8px → ~12px) to mirror the expanded PaneHeader's visual icon offset (PaneHeader: `paddingTop: spacingVerticalS` inside a 40px min-height row places its icon ~12px below the top edge; matching that rhythm means the icon doesn't visually shift when toggling collapse/expand). **Fix 2 (freed-width fill)**: when 2 of 3 panes are collapsed (or just the center is collapsed and both side panes are expanded), the remaining expanded pane(s) now flex-grow to fill the freed width instead of leaving an empty `colorNeutralBackground1` strip. Previously the layout used fixed `width: ${leftWidthPx}px` / `${rightWidthPx}px` on the side panes and `flex: 1` only on the center; when center was collapsed the side panes stayed fixed and the remainder fell off the right edge. New algorithm: `expandedCount = isLeftVisible + isCenterVisible + isRightVisible`; `leftIsSoleExpanded = isLeftVisible && expandedCount === 1`; `rightIsSoleExpanded = isRightVisible && expandedCount === 1`; `rightFillsForCollapsedCenter = isLeftVisible && !isCenterVisible && isRightVisible`. Left/right pane conditional `style`: `{ flex: '1 1 auto', width: 'auto' }` when sole-expanded (or right with collapsed-center) else `{ width: 'XXXpx' }`. Center pane is always `flex: 1` via existing `styles.centerPane` — no change needed. Splitters between adjacent panes hide when either neighbor is collapsed (task 094 logic, unchanged). All 8 collapse combinations covered: 3-expanded (center fills, unchanged), 1-collapsed-only (center or expanded side fills depending on which), 2-collapsed (sole expanded fills entire freed width), 3-collapsed degenerate (three 48px strips side-by-side — acceptable no-op). **What did NOT change**: SpaarkeAi pane components (`ConversationPane`/`WorkspacePane`/`ContextPaneController`/`ThreePaneShell`); `ThreePaneLayout.types.ts` (no new props); `useThreePaneLayout` hook (width persistence untouched — new flex behavior overrides only in sole-expanded / center-collapsed cases); LegalWorkspace (FR-25 preserved — doesn't import `ThreePaneLayout`). **Build**: shared lib TypeScript clean; SpaarkeAi **899.39 KB gzip** (+0.04 KB / +40 bytes vs task 093 baseline 899.35 — within near-zero envelope expected for a CSS-only change); LegalWorkspace **569.38 KB gzip** (byte-identical to task 096 baseline — LegalWorkspace doesn't import `ThreePaneLayout`, only `PaneHeader`, which was untouched). ADR-012 (shared lib placement — width-redistribution + alignment belong in the shared layout primitive, not duplicated per consumer), ADR-021 (Fluent v9 tokens only — `spacingVerticalM`/`spacingVerticalXS`; flex shorthands `'1 1 auto'` and CSS keyword `width: 'auto'` are CSS-spec values, exempt from token requirement same way `flex: 1` already was), ADR-022 (React 19 functional — no new hooks; derived booleans are plain consts, cheaper than `useMemo` for 3-boolean math) all complied with. Deployed to spaarkedev1 (`sprk_spaarkeai` resource `5206a442-3451-f111-bec7-7ced8d1dc988`). R-31 → **RESOLVED**. See deploy memo "Task 100 — Collapsed strip top-alignment + empty-pane width redistribution". | F (Verification — remediation) | ✅ | 096 | — | Wave-1-R7 (not parallel-safe — touches shared lib used by all panes) | FULL | 1 |
| 093 | Manage Workspaces side pane — view-manager pattern (Round 6 operator UX iteration 2026-05-22, Wave 3 / R-30 closure). The "Manage workspaces" dropdown MenuItem (stub since task 089; left as `console.log` by task 098 with a JSDoc breadcrumb pointing here) now opens a Fluent v9 `<OverlayDrawer position="end" size="medium">` (~440px) listing ALL the user's workspace layouts with per-row actions. **Per-row affordances**: (1) Pin star LEFT — `PinRegular`/`PinFilled` brand-color when pinned; calls `pinWorkspace`/`unpinWorkspace` from `services/pinnedWorkspaces.ts` — SAME localStorage key (`spaarke:workspace:pinned-list`) the dropdown writes (task 098) and the cold-load auto-open effect in `WorkspacePane.tsx` reads (task 092). Operator's "Default = Pin" semantics: pinning adds to the auto-open list. (2) Name — double-click rename (Path A inline `<Input>` with autoFocus, Enter commits, Escape cancels, blur commits) via new `renameWorkspaceLayout` helper → BFF `PUT /api/workspace/layouts/{id}` (full-overwrite with existing layoutTemplateId/sectionsJson/isDefault + new name). System layouts are no-op on double-click (cannot be renamed). (3) Secondary metadata — "System layout — Save As to edit" italic hint for `isSystem === true` rows. BFF DTO doesn't carry `modifiedOn` so we surface the system/user distinction instead (no DTO extension per CLAUDE.md §10 BFF Hygiene). (4) Edit `EditRegular` — launches `sprk_workspacelayoutwizard` via frame-walked `Xrm.Navigation.navigateTo`; system layouts → `mode=saveAs` (clone-then-edit, pre-populates template/sections/name); user layouts → `mode=edit` (in-place). Pattern lifted verbatim from `LegalWorkspace/src/components/Shell/WorkspaceGrid.tsx` lines ~720-760 and the pre-task-098 `handleEditWorkspace`. After wizard closes, `useWorkspaceLayouts.refetch()` re-syncs. (5) Delete `DeleteRegular` — opens inline Fluent v9 `<Dialog>` ("Delete workspace?", body "{layoutName} will be permanently removed. This cannot be undone.", primary "Delete" styled with `colorStatusDangerBackground3` / secondary "Cancel"); on confirm calls new `deleteWorkspaceLayout` helper → BFF `DELETE /api/workspace/layouts/{id}` (soft-delete via Dataverse deactivation), drops layout from pin list if pinned (so cold-load auto-open doesn't fail to resolve next session), closes dialog, refetches. System layouts: Delete button DISABLED with Tooltip "System layouts can't be deleted — use Edit then Save As to clone." **Files added**: `src/solutions/SpaarkeAi/src/components/workspace/ManageWorkspacesPane.tsx` (~470 lines including inline delete `<Dialog>` — header JSDoc cross-references operator feedback + R-30 + canonical wizard launch source); `src/solutions/SpaarkeAi/src/services/workspaceLayoutMutations.ts` (thin `authenticatedFetch`-based wrappers `renameWorkspaceLayout()` + `deleteWorkspaceLayout()`; error class extended with `.status` so callers can branch on 403 / 404; rename helper sends full PUT payload — BFF endpoint is not PATCH). **Files modified**: `src/solutions/SpaarkeAi/src/components/workspace/WorkspacePaneMenu.tsx` — `handleManageWorkspaces` no longer `console.log`s; flips new `isManageOpen` state; drawer mounted as sibling of `<Menu>` inside a React Fragment (Fluent v9 portals the drawer's DOM anyway); added `handleManageOpenChange`; dropped unused `isPinned` import. **BFF endpoints**: ALL FOUR pre-existed in `Sprk.Bff.Api/Api/Workspace/WorkspaceLayoutEndpoints.cs` (GET list, GET default, PUT update with 403 for system layouts, DELETE with 403 for system layouts). **ZERO new BFF endpoints added** — per CLAUDE.md §10 BFF Hygiene Binding Governance, this task's Placement Justification is vacuous (nothing was placed in BFF). **Build**: SpaarkeAi **899.35 KB gzip** (+5.12 KB / +0.57% vs Wave 2 follow-up baseline 894.23 — increase tracks new `<OverlayDrawer>` + `<Dialog>` + `<DrawerHeader>`/`Title`/`Body` + `<DialogSurface>`/`Body`/`Title`/`Content`/`Actions` + `EditRegular`/`DeleteRegular`/`DismissRegular` imports + rename `<Input>` + edit-wizard launch helper ~80 LOC + two mutation helpers + `buildBffApiUrl` calls; task 097 had removed the previous Drawer surface so this re-adds it). LegalWorkspace not rebuilt (no LegalWorkspace files touched — FR-25 / NFR-10 preserved). **ADR compliance**: ADR-012 (all three SpaarkeAi-local — depend on solution-local pin storage + useWorkspaceLayouts + useAiSession), ADR-021 (Fluent v9 tokens only — `colorNeutralStroke2`, `colorNeutralBackground2Hover`, `colorNeutralForeground1/3`, `colorBrandForeground1`, `colorStatusDangerBackground1/3/3Hover/3Pressed`, `colorStatusDangerForeground1`, `colorNeutralForegroundOnBrand`, `fontSizeBase200`, `borderRadiusMedium`, `spacingHorizontalS/M/L`, `spacingVerticalS/M/L/XXL`; destructive Delete button uses canonical `colorStatusDangerBackground3`), ADR-022 (React 19 functional — useState for isManageOpen/renamingId/renameDraft/errorMsg/deleteTarget/isDeleting/pinnedIds; useEffect re-syncs pinnedIds on drawer open; useCallback throughout), ADR-025 (`DismissRegular`/`PinRegular`/`PinFilled`/`EditRegular`/`DeleteRegular` all `@fluentui/react-icons` v9), ADR-028 (`authenticatedFetch` + `bffBaseUrl` from `useAiSession()`; no token snapshots; mutation helpers accept auth as typed argument). Deployed to spaarkedev1 (`sprk_spaarkeai` resource `5206a442-3451-f111-bec7-7ced8d1dc988`). R-30 → **RESOLVED**. See deploy memo "Task 093 — Manage Workspaces side pane". | F (Verification — remediation) | ✅ | 098 | — | Wave-3 (parallel-safe) | FULL | 3 |
| 097 | Assistant pane revisions — History trigger becomes a Fluent v9 dropdown matching Workspace + Context UX; collapsed Assistant strip wired to task 096's `leftCollapsedIcon` API (Round 6 — operator smoke 2026-05-22, Wave 2 commit 1 of 3). **Investigation finding**: the prior History affordance (task 022) was a Fluent v9 `<OverlayDrawer>` (NOT a Dataverse-table-driven MDA side pane as the operator hypothesised) backed by `GET /api/ai/chat/sessions?limit=50` via `authenticatedFetch`; the MDA-look feeling came from the combination of icon-only trigger + slide-in surface + no text label. Converting to a dropdown therefore required no data-source plumbing — only the trigger surface + the list surface change. **Path A chosen** (full UX unification — replace BOTH trigger AND surface with `<Menu>` + `<MenuPopover>`; the 50-item ceiling fits inside a scrollable popover with `max-height: 360px` + `overflow-y: auto`); Path B (keep OverlayDrawer surface, swap only trigger) rejected because it would leave Assistant with a surface mismatched against Workspace + Context. **Files modified**: (1) `src/solutions/SpaarkeAi/src/components/conversation/HistoryOverlay.tsx` — wholly rewritten as `HistoryMenu`: subtle `<Button>` with text "History" + `<ChevronDownRegular />` icon `iconPosition="after"` (mirrors WorkspacePaneMenu lines 492–505 + ContextPaneMenu lines 141–155 verbatim); `<MenuPopover>` with scrollable session list rendered as `<MenuItem>` rows (two-line layout: title semibold + relative timestamp meta); loading / empty / error states inline inside the popover (`statusRow` style); BFF fetch + telemetry constant `TELEMETRY_HISTORY_OVERLAY_LOAD_FAILURE` unchanged. Legacy `HistoryOverlay` named export retained as `export const HistoryOverlay = HistoryMenu` alias. `HistoryMenuProps` replaces `HistoryOverlayProps` (no more `open` / `onClose` — Menu manages its own state); the old type is preserved as a `@deprecated` alias. (2) `src/solutions/SpaarkeAi/src/components/conversation/ConversationPane.tsx` — removed `HistoryRegular` import (no longer used); renamed `HistoryOverlay` import to `HistoryMenu`; removed `historyOpen` state + open/close handlers; replaced the `<Tooltip><Button icon={<HistoryRegular />}>` wrapper in `PaneHeader.rightSlot` with `<HistoryMenu onSelectSession={setChatSessionId} bffBaseUrl={...} authenticatedFetch={...} />` directly; removed the separate `<HistoryOverlay>` sibling render block (Menu renders its popover via Fluent v9 portal — no sibling render needed). (3) `src/solutions/SpaarkeAi/src/components/shell/ThreePaneShell.tsx` — added `import { ChatRegular } from "@fluentui/react-icons"`; added `leftCollapsedIcon={<ChatRegular />}` prop on `<ThreePaneLayout>` (consumes task 096's new shared-lib API) so the collapsed Assistant strip renders the chat icon instead of rotated-text fallback. **What this does NOT change**: BFF endpoint contract (`GET /api/ai/chat/sessions?limit=50`) is identical — same URL, same auth, same response shape; telemetry constant name unchanged (rename to `TELEMETRY_HISTORY_LOAD_FAILURE` flagged as cosmetic follow-up); no LegalWorkspace files touched (LegalWorkspace doesn't render `ConversationPane` / `ThreePaneShell` — FR-25 / NFR-10 preserved). **Build**: SpaarkeAi **892.62 KB gzip** (−4.05 KB / −0.45% vs task 096 baseline 896.67 — net reduction from removing `<OverlayDrawer>` + `<DrawerHeader>` + `<DrawerBody>` surface code in favor of inline `<Menu>` reuse; Menu primitives already imported by WorkspacePaneMenu + ContextPaneMenu so zero-additional-cost); LegalWorkspace not rebuilt (no files touched). ADR-012 (HistoryMenu is solution-local, mirrors WorkspacePaneMenu + ContextPaneMenu placement), ADR-021 (Fluent v9 tokens only — no hex/rgba), ADR-022 (React 19 functional), ADR-028 (no auth surface change — `authenticatedFetch` still a prop, no token snapshots). NOT deployed in this task — Wave 2 commit 1 of 3; deploy is coordinated by main session after tasks 098 (Workspace) + 099 (Context) commits land. See deploy memo "Task 097 — Assistant pane: History as dropdown". | F (Verification — remediation) | ✅ | 096 | — | Wave-2 (parallel-safe with 098, 099) | FULL | 2 |
| 095 | Context pane tool selector + Semantic Search Criteria (Round 5 — operator UX iteration). New PaneHeader rightSlot dropdown (Fluent v9 Menu mirroring `WorkspacePaneMenu` pattern) exposes two Context tools: **Quick Start** (default — existing `GetStartedCardsWidget` 7-card grid) and **Semantic Search** (new `SemanticSearchCriteriaTool` — in-pane Domain `<Dropdown>` + AI query `<Textarea>` + optional from/to date `<Input type="date">` + primary Search button). Search button launches `sprk_semanticsearch` via `Xrm.Navigation.navigateTo` with `query` + `domain` + `dateFrom` + `dateTo` URL params (80% × 80% modal). **Files added**: `src/solutions/SpaarkeAi/src/hooks/useContextTool.ts` (typed localStorage-persisted tool selection — same try/catch posture as `usePaneCollapse.ts` from task 094); `src/solutions/SpaarkeAi/src/components/context/ContextPaneMenu.tsx` (Fluent v9 Menu dropdown — `MenuTrigger` Button + `MenuPopover` + `MenuList` + `MenuGroupHeader` + two MenuItems with active-checkmark marker); `src/solutions/SpaarkeAi/src/components/context/SemanticSearchCriteriaTool.tsx` (in-pane criteria editor + Xrm frame-walk modal launcher; criteria state persisted in `spaarke:context:semantic-search-criteria`). **Files modified**: `src/solutions/SpaarkeAi/src/components/context/ContextPaneController.tsx` — wires `useContextTool()` hook + composes `<ContextPaneMenu>` in PaneHeader rightSlot alongside existing stage label `<Text>` in a flex row; `renderContent()` updated so `selectedTool` is the source of truth (`semantic-search` → always renders the criteria tool; `quick-start` + no server-driven `activeWidget` → renders `<GetStartedCardsWidget/>` on any stage — uniform fix for the "pane goes blank after modal close" bug operator reported for the playbook-modal flow). Persisted selection in `localStorage["spaarke:context:selected-tool"]` is the same posture as task 094's `spaarke:panes:collapsed`. **Build**: SpaarkeAi 896.59 KB gzip (+1.55 KB vs task 094 baseline 895.04); LegalWorkspace 569.35 KB gzip — byte-identical to task 094 baseline (FR-25 / NFR-10 preserved); SemanticSearch Code Page untouched. ADR-012 (SpaarkeAi-local placement correct — `ContextToolId` union + storage namespace + SemanticSearch launch are solution-specific concerns), ADR-021 (Fluent v9 tokens only — no hex / rgba), ADR-022 (React 19 functional), ADR-025 (`@fluentui/react-icons` v9 — `ChevronDownRegular`, `AppsListRegular`, `SearchRegular`, `CheckmarkRegular`), ADR-028 (no auth surface change — criteria stay client-side until modal hand-off; `sprk_semanticsearch` has its own MSAL bootstrap). Deployed to spaarkedev1. | F (Verification — remediation) | ✅ | 094 | — | — (serial) | FULL | 3 |
| 094 | Per-pane collapse/expand on the SpaarkeAi three-pane shell (Round 5 — operator UX iteration). Operator request: click anywhere on a pane's header (but NOT on icon buttons inside the header's rightSlot) to toggle that pane's collapsed state; collapsed pane renders as a narrow vertical strip (~36px) with the pane's name rotated 90° (mirrors SmartToDo `KanbanBoard.tsx` column-header collapse pattern). All three panes (Assistant / Workspace / Context) can be collapsed simultaneously. State persists across browser sessions via `localStorage` key `spaarke:panes:collapsed` so a refresh restores the user's preference. **Files modified**: (1) `src/client/shared/Spaarke.UI.Components/src/components/PaneHeader/PaneHeader.tsx` — added optional `onCollapse?: () => void` + `expanded?: boolean` props; when wired, header becomes a `role="button"` with `tabIndex=0`, `aria-expanded`, Enter/Space keyboard handlers, and `cursor:pointer` hover treatment. The rightSlot wrapper applies `stopPropagation` on click + keydown so interactive children (Buttons, Dropdowns) don't bubble their events up to the header collapse handler. When `onCollapse` is undefined, ALL new code paths short-circuit — backwards compatible with the existing PaneHeader contract (LegalWorkspace standalone unchanged). (2) `src/client/shared/Spaarke.UI.Components/src/components/ThreePaneLayout/ThreePaneLayout.tsx` + `.types.ts` — added 6 optional props for externally-controlled collapse: `leftCollapsed` / `centerCollapsed` / `rightCollapsed` + `onToggleLeft` / `onToggleCenter` / `onToggleRight` + `centerPaneCollapseLabel`. The center pane is now collapsible (operator requested all three panes collapsible — base layout previously only supported left/right). Collapsed center renders as a 36px strip with rotated label; click / Enter / Space re-expands. Splitters hide when adjacent panes are collapsed. (3) `src/solutions/SpaarkeAi/src/hooks/usePaneCollapse.ts` (new, ~140 lines) — typed Set-based hook with `PaneId = 'assistant' \| 'workspace' \| 'context'` discriminated union; reads / writes `localStorage["spaarke:panes:collapsed"]` (try/catch-wrapped per task 092's posture); validates persisted IDs against `VALID_PANE_IDS` so a corrupted entry can't crash render. (4) `src/solutions/SpaarkeAi/src/components/shell/ThreePaneShell.tsx` — wires `usePaneCollapse` + new `PaneCollapseContext` (so each pane can read its own collapsed state + toggle without prop-drilling); passes all 6 collapse props to `ThreePaneLayout`; collapse labels updated to bare pane names ("Assistant", "Workspace", "Context"). (5) `src/solutions/SpaarkeAi/src/components/conversation/ConversationPane.tsx` — wires `onCollapse={handleHeaderCollapse}` + `expanded={isAssistantExpanded}` on its PaneHeader; History icon button onClick adds `e.stopPropagation()` as a defensive belt (PaneHeader's rightSlot container also stops propagation — belt-and-braces). (6) `src/solutions/SpaarkeAi/src/components/workspace/WorkspacePane.tsx` — wires Workspace pane PaneHeader (WorkspacePaneMenu in rightSlot is already protected by PaneHeader's rightSlot stopPropagation guard). (7) `src/solutions/SpaarkeAi/src/components/context/ContextPaneController.tsx` — wires Context pane PaneHeader (stage label in rightSlot is non-interactive Text, but the rightSlot guard still applies). **Reuse**: pattern lifted verbatim from `src/solutions/LegalWorkspace/src/components/SmartToDo/SmartToDo.tsx` (lines 337-352 — `collapsedColumns: ReadonlySet<string>` state) and `src/solutions/LegalWorkspace/src/components/shared/KanbanBoard.tsx` (lines 132-156 — `columnCollapsed` / `columnCollapsedHeader` / `columnCollapsedTitle` styles with `writingMode: 'vertical-rl'` + `transform: 'rotate(180deg)'`). Set-based state generalises to N panes (future-proof for a possible 4th pane). **Build**: SpaarkeAi 895.04 KB gzip (vs 894.21 task 092 baseline = +0.83 KB / +0.09%); shared lib rebuilds clean. LegalWorkspace 569.35 KB gzip (vs 569.31 task 092 baseline = +40 bytes from new PaneHeader conditional branch code that LegalWorkspace standalone never traverses at runtime — FR-25 preserved behaviorally; bundle delta is unused dead-conditional cost). ADR-021 (Fluent v9 tokens — uses `colorNeutralBackground2`, `colorNeutralBackground2Hover`, `colorStrokeFocus2`, `colorNeutralForeground3`; no hex / rgba), ADR-022 (React 19 functional), ADR-028 (no auth surface affected — localStorage is client-side state only). Deployed to spaarkedev1. | F (Verification — remediation) | ✅ | 010 | — | — (serial) | FULL | 4 |
| 103 | Context pane polish — drop dropdown type icons, responsive Quick Start grid, Semantic Search width cap (Round 7 operator smoke 2026-05-22, Wave-2-R7 commit 3 of 3). **ContextPaneMenu.tsx**: removed the type icon (left side) from each tool's MenuItem per operator request "remove the icons (only leave the pin)". Visual rhythm now reads `[pin] [tool name] [active-check]` — pin remains the only left-side icon, checkmark only appears on the currently-selected tool. Dropped unused `AppsListRegular` + `SearchRegular` imports from `@fluentui/react-icons`; collapsed `MenuItem.icon` to render `undefined` (Fluent v9 collapses the slot entirely) when not active. **GetStartedCardsWidget.tsx** (`@spaarke/ai-widgets`): grid switched from fixed `gridTemplateColumns: '1fr 1fr'` to `repeat(auto-fill, minmax(160px, 1fr))` so the 7 Quick Start cards arrange responsively. Narrow Context pane (~400px): 2 columns (preserves pre-task-103 behavior). Wide Context pane (when Assistant + Workspace are both collapsed via task 100): 3-5 columns auto-fill based on available width. 160px minimum chosen to match the visual size of the previous fixed 2-col layout at the default pane width — cards don't shrink below a usable touch target. Keyboard navigation updated: `moveFocus()` now computes `columnsPerRow` at runtime via DOM measurement (cards in row 1 share `getBoundingClientRect().top`) instead of hard-coding ±2 for up/down — arrow nav stays correct at any column count. **SemanticSearchCriteriaTool.tsx**: added `CRITERIA_MAX_WIDTH_PX = 480` cap with `marginLeft: 'auto'` + `marginRight: 'auto'` on a new inner `criteria` wrapper div so the form-style criteria stays centered at a comfortable form-panel width even when the Context pane is full-width. The outer `root` continues to fill the pane for background coverage; only the form content is capped. Narrow pane: criteria fill the pane naturally (max-width only constrains, doesn't shrink below natural width). Wide pane (>~520px including outer padding): criteria stay centered at 480px instead of stretching across the screen. TODO breadcrumb left in the file header for future Context tools (Search Saved, Filter Library, etc.) to follow the same width-cap discipline — operator caveat: "As other tools are added we'll need to address responsiveness." **Verification — LegalWorkspace impact**: confirmed via `Grep` that LegalWorkspace consumes neither `@spaarke/ai-widgets` nor `GetStartedCardsWidget` (LegalWorkspace has its OWN `GetStarted` widget in `src/solutions/LegalWorkspace/src/components/GetStarted/` — different code path). LegalWorkspace bundle byte-identical to task 100 baseline. **Not touched**: `ContextPaneController.tsx` (no wrapper grid change needed — Option B in task brief, the widget's own grid is responsive); `useContextTool.ts` (Task 101 sibling agent is modifying it — out of scope); `WorkspacePaneMenu.tsx` + `ManageWorkspacesPane.tsx` (Task 102 sibling agent — out of scope); `ThreePaneLayout.tsx` + `PaneHeader.tsx` (Wave 1 sealed); LegalWorkspace files (FR-25 preserved). **Build**: shared lib `Spaarke.AI.Widgets` TypeScript build has pre-existing cross-rootDir errors (LegalWorkspace types pulled in via `@spaarke/ui-components` → `workspaceConfig.tsx` transitive imports — predates this task; flagged for separate cleanup); production builds via Vite succeed: SpaarkeAi **899.81 KB gzip** (+0.42 KB / +420 bytes vs task 100 baseline 899.39 — bytes split across the responsive grid CSS rule + keyboard nav DOM measurement helper + criteria max-width wrapper + JSDoc comments; under 0.5 KB envelope expected for CSS+small-helper change); LegalWorkspace **569.38 KB gzip** (byte-identical to task 100 — LegalWorkspace doesn't consume `GetStartedCardsWidget`). ADR-012 (shared lib placement — responsive grid belongs in `GetStartedCardsWidget` because the widget should be responsive by default for ALL consumers, not duplicated per consumer), ADR-021 (Fluent v9 tokens only — `spacingHorizontalS/M`, `colorNeutralBackground1`, `colorNeutralStroke2`; the `'repeat(auto-fill, minmax(160px, 1fr))'` and `maxWidth: '480px'` are layout-dimension literals with no matching Fluent v9 token, same exemption as the existing `width: '20px'` / `height: '20px'` pin button + the task 096 `COLLAPSED_STRIP_PX = 48` constant), ADR-022 (React 19 functional — no new hooks; `getColumnsPerRow` is a plain function), ADR-028 (no auth surface change) all complied with. NOT deployed in this task — Wave-2-R7 commit 3 of 3; coordinated deploy by main session after the Wave 2 R7 trio (101 Pin persistence + 102 Workspace UX + 103 Context polish) lands. See deploy memo "Task 103 — Context pane polish". R-37 + R-38 + R-39 → **RESOLVED**. | F (Verification — remediation) | ✅ | 099, 100 | — | Wave-2-R7 (parallel-safe with 101, 102) | FULL | 1 |
| 104 | Manage Workspaces MDA-style revamp (Round 7 operator final block 2026-05-22, Wave-3-R7). Substantial rework of `ManageWorkspacesPane.tsx` (task 093) aligning the side pane with the model-driven app "manage view" UX. **Row UX**: pin icon hover-only (`opacity: 0` → `1` on row hover via Griffel `:hover .manage-pin-hover-only` selector + static class hook) when unpinned, persistent + brand-colored (`PinFilled` + `colorBrandForeground1`) when pinned; first row in the Pinned section carries a Fluent v9 `<Badge appearance="tint" color="brand" size="small">Default</Badge>` inline with the name (chosen over a left-side star icon — pin column already conveys "pinned" so a second left-side icon would visually compete); inline rename preserved from task 093 (double-click name → Input; system layouts no-op); row click (outside pin + ⋯ menu) dispatches `widget_load` via `useDispatchPaneEvent` (mirrors `WorkspacePaneMenu.handleLayoutSelect`) so the workspace opens as a new tab + closes the drawer. **Three-dot (⋯) per-row Fluent v9 `<Menu>`**: 1) Pin / Unpin (label toggles by state) — `PinFilled`/`PinRegular`; 2) Set as default — `StarRegular`, calls new `moveWorkspaceToTop`, disabled if `isDefault`; 3) Move up — `ArrowUpRegular`, swaps with index N-1 via `setPinnedWorkspacesOrder`, disabled if not pinned or `pinIdx <= 0`; 4) Move down — `ArrowDownRegular`, swaps with N+1, disabled if not pinned or at bottom; 5) Edit — `EditRegular`, launches `sprk_workspacelayoutwizard` via `launchEditWizard` with `templateFilter=SPAARKEAI_TEMPLATE_FILTER` (saveAs for system layouts, edit-in-place for user layouts); 6) Delete — `DeleteRegular`, opens existing confirmation `<Dialog>`, disabled for system layouts with `Tooltip` "System layouts can't be deleted — use Edit then Save As to clone." Disabled `MenuItem`s use `aria-disabled` + guarded `onClick` (no-op when disabled) so keyboard activation can't bypass the disabled state. **"Default = first in pinned list" semantics**: ZERO new storage keys. The pinned list at `localStorage["spaarke:workspace:pinned-list"]` IS ordered, and index 0 is the default. Task 101's auto-open effect already iterates `getPinnedWorkspaces()` in array order — putting the default at index 0 means it opens first on cold load, no code change needed in `WorkspacePane.tsx`. **Order display**: Pinned section header ("Pinned", `<Text size={200} weight="semibold">`) with rows in `getPinnedWorkspaces()` order (index 0 carries the Default badge); divider rule (`1px solid colorNeutralStroke2`); "All workspaces" header with unpinned rows in BFF order. If pinned list is empty → no headers, no divider — flat list. **Footer** (operator: "no Save or Close button"): right-justified `<div>` outside `DrawerBody` (so it doesn't scroll with the list) containing `Cancel` (secondary) + `Save` (primary, focus-default) buttons. Both close the drawer. All operations are committed INSTANTLY (pin / unpin / set-default / move / rename / delete) so neither button reverts or applies anything — operator-visible exit affordances. Tooltips document this: Cancel — "Close without applying further changes."; Save — "Close the manage panel." File header documents the future-proofing for a transactional mode. The `<DrawerHeader>` ✕ button remains as a third dismissal path (matches MDA). **New `pinnedWorkspaces.ts` exports**: `setPinnedWorkspacesOrder(list)` writes the supplied array verbatim with defensive de-dup on `layoutId` (keeps FIRST occurrence) + normalization (filters entries missing required string fields); `moveWorkspaceToTop(layoutId, layoutName?)` pins if not pinned + moves to index 0 (`layoutName` REQUIRED when promoting unpinned; reads existing entry's name otherwise; bails if both missing). Both follow the same try/catch posture as existing exports — silent failure on private-browsing / quota errors. Reorder choice — Option B (Up/Down in ⋯ menu only, NOT inline arrows) for visual consistency with the other actions; if operator iterates wanting inline arrows, the change is localized. **Files changed**: `src/solutions/SpaarkeAi/src/services/pinnedWorkspaces.ts` (extended); `src/solutions/SpaarkeAi/src/components/workspace/ManageWorkspacesPane.tsx` (body rewritten; OverlayDrawer wrapper + dialog + rename + BFF integration preserved). **NOT touched**: `WorkspacePane.tsx` (auto-open works as-is with ordered pin list — task 101 already fixed the macrotask deferral); `WorkspacePaneMenu.tsx` (task 102's `orderedLayouts` cascade unchanged — the dropdown's "default" via `activeLayout` and task 104's "default" via first-of-pinned-list typically align for new/cold state; small follow-up may be needed if operator iterates); `services/workspaceLayoutMutations.ts` (PUT/DELETE helpers unchanged — reused); LegalWorkspace (FR-25 / NFR-10 preserved). **Build**: SpaarkeAi **901.21 KB gzip** (+1.40 KB / +1,433 bytes vs task 103 baseline 899.81 — within +5-10 KB envelope expected by brief; bytes from new `Badge` import + 4 icon imports (`MoreHorizontalRegular`/`StarRegular`/`ArrowUpRegular`/`ArrowDownRegular`) + 2 new pinned-utility helpers + substantially expanded body JSX with 6-item menu per row + section headers + footer); LegalWorkspace not rebuilt (no LegalWorkspace files touched). Deployed to spaarkedev1 (`sprk_spaarkeai` resource `5206a442-3451-f111-bec7-7ced8d1dc988`, overwrite). **CLAUDE.md §10**: ZERO new BFF endpoints — pin/unpin/set-default/move are pure client-side localStorage; rename/delete reuse existing `WorkspaceLayoutEndpoints.cs` PUT/DELETE via task-093's `workspaceLayoutMutations.ts`. **ADR-012** (both modified files SpaarkeAi-local — correct, not reusable in LegalWorkspace as-is; FR-25 preserved), **ADR-021** (Fluent v9 tokens — `spacing*`, `color*`, `borderRadius*`, `fontSize*`; only literal-px values are intentional sizing constants `28px` icon button + `2px` name gap, consistent with pre-existing patterns), **ADR-022** (React 19 — standard `useState`/`useCallback`/`useMemo`/`useEffect`; no new hooks), **ADR-025** (`MoreHorizontalRegular`/`StarRegular`/`ArrowUpRegular`/`ArrowDownRegular` are `@fluentui/react-icons` v9 named exports), **ADR-028** (auth surface unchanged — BFF calls via `useAiSession().authenticatedFetch`; no token snapshots). R-40 + R-41 + R-42 + R-43 → **RESOLVED**. See deploy memo "Task 104 — Manage Workspaces MDA-style revamp". | F (Verification — remediation) | ✅ | 093, 098, 101, 102 | — | Wave-3-R7 (parallel-safe: false — single side-pane rework, no parallel companions) | FULL | 2 |
| 105 | Popup auto-launch investigation + removal (Round 8 operator final-block 2026-05-22, Wave-1-R8 commit 1 of 3). Operator reported two popups firing automatically on SpaarkeAi cold load: (a) a Daily Briefing modal, (b) a `webresourceName=sprk_spaarkeai` popup ("not sure what it is"). **Investigation**: searched the entire cold-load path — `main.tsx` (clean bootstrap), `App.tsx` / `ThreePaneShell.tsx` (auth probe + provider tree only), `WorkspacePane.tsx` (Home tab install + task 101's pinned-workspace auto-open), `WorkspaceLayoutWidget.tsx` (inline `<LegalWorkspaceApp embedded />`, no navigateTo), `WorkspaceHomeTab.tsx` (inline Daily Briefing section via `createDailyBriefingRegistration`, no popup), `ContextPaneController.tsx` (wizard launchers behind click handlers only), `launch-resolver.ts::openSpaarkeAi` (only called from ribbon scripts, never from SpaarkeAi runtime UI). **Root cause** — SINGLE bug responsible for both observations: `src/solutions/LegalWorkspace/src/hooks/useDailyDigestAutoPopup.ts` opens `sprk_dailyupdate` (title literal: "Daily Briefing") as a `target: 2` modal via `Xrm.Navigation.navigateTo` on first session mount, called unconditionally from `WorkspaceGrid.tsx:154` (`useDailyDigestAutoPopup({ webApi, userId });`). When SpaarkeAi cold-loads with any pinned workspace, task 101's auto-open effect dispatches `widget_load → WorkspaceLayoutWidget → LegalWorkspaceApp (embedded) → WorkspaceGrid → useDailyDigestAutoPopup` and the modal fires. **Operator's second observation is the SAME bug**: the URL `webresourceName=sprk_spaarkeai` is the URL of the page UNDER the modal (SpaarkeAi is the host page); the modal opens on top of SpaarkeAi, so the URL bar still reads SpaarkeAi. Codebase audit confirmed `sprk_spaarkeai` as a navigateTo target is referenced ONLY in `launch-resolver.ts::openSpaarkeAi`, called solely from the two ribbon scripts (never from SpaarkeAi runtime). There is no self-popup bug; both observations describe one symptom. **Fix**: set the hook's existing sessionStorage sentinel (`spaarke_dailyDigestShown`) synchronously in SpaarkeAi's `bootstrap()` BEFORE any React tree mounts, via a new `suppressLegalWorkspaceDailyDigestAutoPopup()` helper in `main.tsx`. The hook's own guard (`if (sessionStorage.getItem(SESSION_KEY)) return;`) then short-circuits before the modal launches. Flag value `"suppressed-by-spaarkeai"` is informational; the hook checks any truthy value. Helper sets the flag IFF the key is not already present (preserves operator-side "opted-out" or prior "shown" values). Wrapped in try/catch — silent degradation for private browsing / sandboxed iframe (the hook's own try/catch handles those same cases identically). **Why this scope is correct**: LegalWorkspace source files UNTOUCHED (FR-25 / NFR-10 preserved by construction); standalone LegalWorkspace browser tab has its OWN sessionStorage, unaffected; inline Daily Briefing section in WorkspaceHomeTab (task 086) is unaffected — it does NOT use `Xrm.Navigation.navigateTo`, operator still sees Daily Briefing content INLINE on the Home tab, only the auto-launched MODAL is suppressed. Alternative considered: modify `WorkspaceLayoutWidget.tsx` in the shared `@spaarke/ai-widgets` lib — rejected because suppression is a SpaarkeAi product decision, not a widget-level concern. `main.tsx` is the correct (single-solution-bootstrap) scope. **Files modified**: `src/solutions/SpaarkeAi/src/main.tsx` (~50 lines net — added `DAILY_DIGEST_SESSION_KEY` constant, `suppressLegalWorkspaceDailyDigestAutoPopup()` helper, and an early call inside `bootstrap()` before `resolveRuntimeConfig`; mostly extensive docblock comments documenting the cross-package suppression contract). **NOT touched**: `useDailyDigestAutoPopup.ts` + `WorkspaceGrid.tsx` (LegalWorkspace — FR-25 / NFR-10); `WorkspaceLayoutWidget.tsx` (shared lib — out of scope); ContextPaneController + ContextPaneMenu (Wave 1 task 106 sibling); WorkspaceTabManagerComponent (Wave 1 task 107 sibling). **Build**: SpaarkeAi **901.81 KB gzip** (+0.60 KB / +614 bytes vs task 104 baseline 901.21 — bytes dominated by the extensive cross-package suppression docblock; executable code is ~10 lines); LegalWorkspace not rebuilt. **CLAUDE.md §10**: zero BFF changes. **ADR-022** (React 19 — pure pre-React side effect inside `bootstrap()`; no new hooks), **ADR-028** (no auth surface change — sessionStorage writes are pure client-side, do not touch MSAL / tokens / BFF), **FR-25 / NFR-10** (LegalWorkspace files unchanged; suppression sits in SpaarkeAi-side bootstrap using the hook's own pre-existing public sessionStorage contract). NOT deployed in this task — Wave 1 R8 commit 1 of 3; coordinated deploy by main session after the Wave-1-R8 trio (105 + 106 + 107) lands. R-44 → **RESOLVED**. See deploy memo "Task 105 — Popup auto-launch investigation + removal". | F (Verification — remediation) | ✅ | 086, 087 | — | Wave-1-R8 (parallel-safe: true with 106, 107) | FULL | 1 |
| 106 | Context pane: Semantic Search left-align + drop active checkmark (Round 8 operator UX iteration 2026-05-22, Wave-1-R8 commit 2 of 3). Two surgical CSS-driven fixes; no logic changes. **SemanticSearchCriteriaTool.tsx**: criteria container's horizontal anchoring flipped from CENTERED (`marginLeft: 'auto'` + `marginRight: 'auto'`, R7 task 103) to LEFT-aligned (`marginLeft: 0` + `marginRight: 'auto'`). The `CRITERIA_MAX_WIDTH_PX = 480` cap is RETAINED so the form-style criteria still doesn't stretch across the full pane on the wide-layout case (task 100 expanded layout when Assistant + Workspace both collapsed); empty space now sits on the RIGHT only instead of being split between both sides. Operator (Round 8, 2026-05-22): "In the Context pane, the semantic search component should be left aligned (see screenshot)." JSDoc + inline comment updated to describe the new "left-aligned, max-width 480px" anchoring + operator rationale. Narrow pane (~400px) behavior unchanged — max-width only constrains, never shrinks below natural width. **ContextPaneMenu.tsx**: removed the active-tool checkmark icon from each MenuItem per operator request (Round 8, 2026-05-22): "'Active tool marker' I don't think this is really necessary — we know what's active because it's what's loaded and there can only be one. Can remove that icon." Changes: dropped `CheckmarkRegular` from the `@fluentui/react-icons` named imports (no other usages); removed the `activeMarker` Griffel style entry; removed the `isActive` const computation in the row renderer (also unused after removal); MenuItem `icon` prop now passes `undefined` unconditionally so Fluent v9 collapses the icon slot entirely. File header + inline catalog comment updated to document the change. After this change each MenuItem renders `[pin button] [tool name]` (pin remains per R7 directive "only leave the pin"). The `selectedTool` prop is kept on `ContextPaneMenuProps` for interface stability — parent `ContextPaneController` still passes it from `useContextTool` — with a JSDoc note explaining it's now a no-op consumer parameter. **NOT touched**: `useContextTool.ts` (Task 101 sealed it via the storage split); `ContextPaneController.tsx` (parent contract unchanged); `GetStartedCardsWidget.tsx` (sealed in task 103); `contextToolPin.ts` (task 099 sealed); Workspace files (Task 107 sibling); Popup investigation files (Task 105 sibling); LegalWorkspace (FR-25 / NFR-10 preserved). **Build**: SpaarkeAi **901.81 KB gzip** (+0.60 KB / +614 bytes vs task 104 baseline 901.21 — within ±1 KB envelope for a CSS-only + 1-icon-import-drop change; the dropped CheckmarkRegular import contributes a small minified saving offset by Rollup chunk-boundary shifts that produce small +/- swings); LegalWorkspace not rebuilt (no files touched). **CLAUDE.md §10**: ZERO BFF endpoints touched. Both changes are pure client-side presentational. No DI, no NuGet packages, no DLL changes. **ADR-012** (both modified files are SpaarkeAi-local — correct placement; criteria max-width discipline is solution-specific, Context tool dropdown is SpaarkeAi-local), **ADR-021** (Fluent v9 tokens only — no new tokens introduced; `marginLeft: 0` is a CSS-spec literal same exemption as the existing `flex: 1` / `width: 100%` patterns), **ADR-022** (React 19 functional — no hook changes), **ADR-025** (the dropped `CheckmarkRegular` was `@fluentui/react-icons` v9; no new icons introduced) all complied with. NOT deployed in this task — Wave-1-R8 commit 2 of 3; coordinated deploy by main session after tasks 105 (Popup) + 107 (Workspace scrollbar UX) commits land. R-45 + R-46 → **RESOLVED**. See deploy memo "Task 106 — Context pane: Semantic Search left-align + drop active checkmark". | F (Verification — remediation) | ✅ | 099, 103 | — | Wave-1-R8 (parallel-safe: true with 105, 107) | STANDARD | 1 |
| 107 | Workspace pane — hide vertical scrollbar + tab overflow with arrow buttons (Round 8 operator smoke 2026-05-22, Wave-1-R8 commit 3 of 3). Two surgical UX fixes in `src/solutions/SpaarkeAi/src/components/workspace/WorkspaceTabManagerComponent.tsx` (sole file touched). **Fix 1 (vertical scrollbar)**: applied `scrollbarWidth: 'none' + '::-webkit-scrollbar': { display: 'none' }` to the `content` style — the `<div className={styles.content}>` wrapper around `ActiveWidgetContent`. Wheel/trackpad/keyboard scroll still work; only the visible bar is gone. NOT applied to ConversationPane (Assistant chat keeps its visible scroll), NOT to ContextPane (sibling task 106 scope), NOT to LegalWorkspace internals (FR-25 / NFR-10 preserved). The WorkspacePane.tsx itself has no content-scroll wrapper — it delegates to WorkspaceTabManagerComponent below the PaneHeader — so this single class change covers both the pane and the tab-content area. **Fix 2 (tab overflow arrows)**: restructured `tabBar` from `overflow-x: auto` (which produced a visible horizontal scrollbar when tabs overflowed) to a flex layout `[arrowLeft] [tabScroll] [arrowRight]`. The new `tabScroll` inner container retains `overflow-x: auto + flexGrow: 1` but applies the same hidden-scrollbar treatment (no visible bar). Arrow buttons (Fluent v9 `<Button appearance="subtle" size="small">` with `ChevronLeft20Regular` / `ChevronRight20Regular`, new `arrowButton` style at 28×28px) handle overflow via `el.scrollBy({ left: ±200, behavior: 'smooth' })`. **Visibility logic**: `canScrollLeft = scrollLeft > 0`; `canScrollRight = scrollLeft + clientWidth < scrollWidth - 1` (1px subpixel tolerance). Three independent observers keep state in sync — (a) `scroll` listener on the scroll container (passive), (b) ResizeObserver on the scroll container (covers pane width changes from collapse/expand), (c) recompute effect keyed on `tabs` (covers add/close/rename/restore). Both arrows are ALWAYS rendered with `visibility: hidden + pointerEvents: none + tabIndex: -1 + aria-hidden: true` via a new `arrowHidden` class when unreachable, preventing tab-strip jitter as overflow state changes. **Active-tab scroll-into-view**: separate effect keyed on `[activeTabId, tabs]` queries the active tab via `data-testid="workspace-tab-${activeTabId}"` and calls `scrollIntoView({ inline: 'nearest', block: 'nearest', behavior: 'smooth' })`. `inline: 'nearest'` is a no-op for already-visible tabs; only clipped tabs scroll. Deferred via `requestAnimationFrame` so layout settles before measuring; RAF cancellation on cleanup prevents stale callbacks after unmount. **Accessibility**: `aria-label="Scroll tabs left"` / `"Scroll tabs right"`; hidden arrows get `aria-hidden=true + tabIndex=-1` so screen readers skip them. Existing tab close-button labels unchanged. **Edge cases handled**: single tab + zero tabs (both arrows hidden because `scrollWidth ≤ clientWidth`), all tabs fit (same), active tab clipped (scroll-into-view), pane collapse→expand (ResizeObserver re-fires), mid-overflow tab add (recompute effect re-runs on `tabs` change). **Build**: SpaarkeAi **901.78 KB gzip** (+0.57 KB / +584 bytes vs task 104 baseline 901.21 — within +1-2 KB envelope; bytes split across ResizeObserver wiring + two arrow Button renders + scroll-into-view effect + four new style entries + two icon imports); LegalWorkspace not rebuilt (no LegalWorkspace files touched — FR-25 / NFR-10 preserved). **CLAUDE.md §10**: ZERO new BFF endpoints — pure client-side UI restructure. No NuGet packages, no publish-size BFF impact. **ADR-021** (Fluent v9 tokens — `colorNeutralForeground1/3`, `colorNeutralBackground3Hover`; `28px` arrow size + `200px` scroll delta + `1px` tolerance are intentional UI affordance literals consistent with task 098's `16px` close button literal), **ADR-022** (React 19 — standard `useRef`/`useState`/`useCallback`/`useEffect`; `ResizeObserver` is a standard browser API used behind a `typeof ResizeObserver !== 'undefined'` guard; `requestAnimationFrame` is standard), **ADR-025** (`ChevronLeft20Regular`/`ChevronRight20Regular` are `@fluentui/react-icons` v9 named exports), **ADR-028** (auth surface unchanged). NOT deployed in this task — Wave-1-R8 commit 3 of 3; coordinated deploy by main session after tasks 105 (Popup) + 106 (Context polish) commits land. R-47 + R-48 → **RESOLVED**. See deploy memo "Task 107 — Workspace pane: hide vertical scrollbar + tab overflow with ◀ ▶ arrow buttons". | F (Verification — remediation) | ✅ | 098 | — | Wave-1-R8 (parallel-safe: true with 105, 106) | STANDARD | 1 |
| 108 | Build + seed system workspace layouts in Dataverse (Round 8 Wave 2a, 2026-05-22). Operator decision Option B: system workspaces — **Daily Briefing**, **Smart To Do List**, **My Work**, **Documents** — become real `sprk_workspacelayout` Dataverse records with `sprk_issystem=true`, NOT a hard-coded frontend list. Architectural goal: one workspace concept, one rendering pipeline (`widget_load → WorkspaceLayoutWidget → LegalWorkspaceApp embed → section factories`). **Reconnaissance**: live-queried `sprk_workspacelayout` schema — pre-existing columns: `sprk_workspacelayoutid` (uniqueidentifier), `sprk_name` (string), `sprk_layouttemplateid` (string), `sprk_sectionsjson` (memo), `sprk_isdefault` (boolean), `sprk_sortorder` (integer 0-20), `_ownerid_value` (lookup). **`sprk_issystem` did NOT exist** — was a code-constant DTO flag on `WorkspaceLayoutDto` only (system layouts lived as `SystemWorkspaceLayouts.cs` code constant, never in Dataverse). The brief's verification query `$filter=sprk_issystem eq true` requires the column to exist, so the script's schema half adds it via Dataverse metadata Web API (idempotent: skip if present). **Section factory IDs confirmed** via `src/solutions/LegalWorkspace/src/sectionRegistry.ts` aggregating `sections/*.registration.ts`: `daily-briefing` (Daily Briefing), `todo` (Smart To Do List → LegalWorkspace's "My To Do List"), `quick-summary` (My Work — Wave 3 task 109 extends with sprk_communication + sprk_invoices cards, layout JSON unchanged), `documents` (Documents — Wave 3 task 110 verifies + add/expand affordances). Note: spec brief guessed `dailyBriefing`/`smartToDo`/`activeDocuments` — actual IDs use kebab-case + the existing `todo` / `documents` names. **Layout template confirmed**: `single-column` (from shared lib `src/client/shared/Spaarke.UI.Components/src/components/WorkspaceShell/layoutTemplates.ts` — 4 stacked rows × 1 slot). The brief mentioned BFF code's `1-column` ID but live data shows `single-column` is canonical (real user records use `single-column`, `2-col-equal`, `3-row-mixed`). The BFF's `WorkspaceLayoutEndpoints.cs` `AvailableTemplates` list (`1-column`, `2-column`, `3-column`, `3-row-mixed`) diverges from the shared-lib canonical list — flagged as pre-existing issue, NOT fixed here. **`sectionsJson` shape confirmed** via Corporate Workspace code constant + `WorkspaceLayoutWizard/src/App.tsx::buildSectionsJson`: `{ schemaVersion: 1, rows: [{ id, columns, columnsSmall, sections: [] }], scope: 'my' }`. Single-column system layouts populate row-1's single slot with the target section ID; rows 2-4 hold empty-string slots (filtered out by `buildDynamicWorkspaceConfig`). **Path B chosen** (direct Dataverse Web API, NOT BFF `POST /api/workspace/layouts`) because: (a) `isSystem` is an admin-level field; the BFF's `CreateWorkspaceLayoutRequest` DTO has no `IsSystem` property — the field is intentionally not exposed to user callers; (b) seeding is a one-time admin task, not a runtime user operation; (c) Path B matches the existing pattern used by `Deploy-SpaarkeAi.ps1` for web-resource updates and `Create-ScopeCapabilityFields.ps1` for metadata changes. **Script architecture**: `scripts/Deploy-SystemWorkspaceLayouts.ps1` + sidecar `scripts/system-layouts.json` (4 layouts, externalized for readability). Steps: (1) load JSON sidecar; (2) acquire `az login` token; (3) schema half — `Test-AttributeExists` → if missing, `POST EntityDefinitions(LogicalName='sprk_workspacelayout')/Attributes` with `BooleanAttributeMetadata` (`sprk_IsSystem`, default=false, Yes/No options) — skippable via `-SkipSchema`; (4) data half — for each layout, `GET ?$filter=sprk_name eq '<name>' and sprk_issystem eq true` → skip if found (or PATCH if `-Force`) → `POST` if missing with `Prefer: return=representation` to capture GUID. Idempotent: re-running is a no-op without `-Force`. `-WhatIf` prints would-create/would-replace without writes. **Sortorder fix**: Dataverse `sprk_sortorder` schema range is 0-20 (not unbounded — discovered on first live run when initial -4/-3/-2/-1 values were rejected). System layouts use 0-3 instead; service-layer logic concatenates system layouts BEFORE user layouts (see `WorkspaceLayoutService.GetLayoutsAsync` lines 64-66) so collision with user sortOrder 0-3 doesn't matter for ordering. **Dev seed run** (2026-05-22, against `https://spaarkedev1.crm.dynamics.com`, owner `ralph.schroeder@spaarke.com` UserId `1d02f31c-1872-f011-b4cb-7c1e52671ad0`): [1/4] Daily Briefing → `6d41d537-fe55-f111-a824-3833c5ed9c11` (sort=0, default=true); [2/4] Smart To Do List → `a49f9e3a-fe55-f111-a824-7ced8dd899c3` (sort=1, default=false); [3/4] My Work → `06c9203e-fe55-f111-a824-3833c5ed9a2b` (sort=2, default=false); [4/4] Documents → `07c9203e-fe55-f111-a824-3833c5ed9a2b` (sort=3, default=false). Verification: `GET sprk_workspacelayouts?$filter=sprk_issystem eq true` returns 4 records, all with correct sectionsJson + layoutTemplateId. Idempotency verified: second run reports "skipping (use -Force to replace)" for all 4 with zero writes. `-Force -WhatIf` verified to produce "would-replace" entries with matching GUIDs. **NO frontend changes**: Wave 2b (future task) will (a) remove hard-coded `WorkspaceHomeTab` and (b) wire auto-install of default system layout via `useWorkspaceLayouts` querying `isSystem=true && isDefault=true`. Operator-verifiable smoke after this task ships (before Wave 2b): the 4 new system layouts appear in the SpaarkeAi Workspaces dropdown (visible because `GET /api/workspace/layouts` returns them — but Daily Briefing's per-owner-id filter in `GetDefaultLayoutAsync` means only the script-runner currently sees Daily Briefing as default; other users still fall through to Corporate Workspace code constant). Operator clicks each from dropdown → confirms each section renders correctly when loaded as a single-section workspace inside SpaarkeAi (this is the embed-render smoke that Wave 3 tasks 109+110 will refine). **CLAUDE.md §10** (BFF Hygiene): ZERO new BFF endpoints added. Direct Web API metadata + data writes; same pattern as Deploy-SpaarkeAi.ps1 web-resource updates. Placement Justification: schema column + seed data both belong in Dataverse (not in code), so direct Web API is the correct mechanism. **Operator-verifiable smoke**: Power Apps maker portal → tables → `sprk_workspacelayout` → records — see 4 new "System Workspace Layout" records with the `Is System` field = Yes; or `Advanced Find` with filter `Is System = Yes` returns 4 records. **Pre-existing follow-ups flagged (NOT fixed here)**: (1) BFF's `AvailableTemplates` list in `WorkspaceLayoutEndpoints.cs:588-636` uses template IDs `1-column`/`2-column`/`3-column`/`3-row-mixed` that diverge from the shared-lib canonical list `single-column`/`2-col-equal`/`3-col-equal`/`3-row-mixed`/etc.; the BFF list is unused (clients fetch templates from the shared lib). (2) `WorkspaceLayoutService.GetDefaultLayoutAsync` filters defaults by per-user `ownerid` — for multi-tenant system layouts to be discoverable as defaults across users, Wave 2b will need to either change this query to also accept `sprk_issystem=true` or have the frontend explicitly fetch via `?$filter=sprk_issystem eq true`. (3) BFF's `WorkspaceLayoutDto` has no `IsSystem` setter on `CreateWorkspaceLayoutRequest` / `UpdateWorkspaceLayoutRequest` — system records cannot currently be modified via BFF (correct security default; documented here for Wave 2b awareness). (4) Pre-existing dirty worktree files `deploy/api-publish/*.dll` + `src/client/shared/Spaarke.AI.Outputs/src/**/*.{js,d.ts,*.map}` NOT staged per task brief. **Files created**: `scripts/Deploy-SystemWorkspaceLayouts.ps1` (~280 lines), `scripts/system-layouts.json` (4 layout definitions). **Schema change**: `sprk_workspacelayout.sprk_issystem` Boolean column added to dev Dataverse (default=false, Yes/No options). R-49 → **RESOLVED**. See deploy memo "Task 108 — Build + seed system workspace layouts (Dataverse, dev)". | F (Verification — remediation) | ✅ | 086, 087, 098, 093 | — | Wave-2-R8 (parallel-safe: FALSE — sequential before Wave 2b) | FULL | 1 |
| 109 | BFF system-layout merge + frontend Home removal + auto-install default (Round 8 Wave 2b, 2026-05-22). Realizes Option B's architectural unity: all workspaces (hard-coded `SystemWorkspaceLayouts.cs` Corporate Workspace, the 4 Wave 2a Dataverse-seeded `sprk_issystem=true` records, and user-created layouts) flow through ONE BFF list endpoint and ONE frontend pipeline (`widget_load → WorkspaceLayoutWidget → LegalWorkspaceApp(embedded) → section factories`). **BFF changes (CLAUDE.md §10 Placement Justification: zero new endpoints; pure behavior extension of existing `GET /api/workspace/layouts`, `GET /api/workspace/layouts/default`, and `PUT/DELETE /api/workspace/layouts/{id}`. Same DTO, same auth boundary, same domain. Belongs in `WorkspaceLayoutService` — the existing service class)**: (1) `WorkspaceLayoutDto.IsSystem` XML doc tightened to document SERVER-CONTROLLED semantics — the request DTOs don't include the property, so clients can't override it; the service populates from data source. (2) `WorkspaceLayoutService.GetLayoutsAsync` now merges THREE sources: hard-coded `SystemWorkspaceLayouts.All` (Corporate Workspace) → Dataverse system records via new `QueryDataverseSystemLayoutsAsync` (no ownerid filter; sorted by `sprk_sortorder`) → user-owned records via updated `QueryUserLayoutsAsync` (now adds `sprk_issystem != true` to criteria to avoid double-counting). System + user fetches run in parallel via `Task.WhenAll`. `SelectColumns` extended to include `sprk_issystem`; `MapToDto` reads it instead of hard-coding `false`. (3) `WorkspaceLayoutService.GetDefaultLayoutAsync` four-step cascade (return type now `Task<WorkspaceLayoutDto?>`): per-user default (existing `sprk_isdefault=true` + ownerid filter + `sprk_issystem != true`) → Dataverse system default (`sprk_issystem=true` + `sprk_isdefault=true`, cross-user, `TopCount=1`, sortOrder ascending — this surfaces Wave 2a's Daily Briefing as the global default) → hard-coded system flagged as global default (forward-compat — Corporate Workspace today has `IsDefault=false` so this returns nothing) → null. Each step has its own try/catch for graceful degradation. (4) `WorkspaceLayoutService.GetLayoutByIdAsync` — if retrieved record has `sprk_issystem=true`, bypass the ownerid check (system records are global). (5) `WorkspaceLayoutService.UpdateLayoutAsync` / `DeleteLayoutAsync` — after the existence check, also reject if `existing.IsSystem` is true with "System workspaces cannot be modified/deleted." error message → endpoint handler returns 403 Forbidden. Defense-in-depth complement to client-side disable affordances. (6) `WorkspaceLayoutEndpoints.GetDefaultLayout` handler returns `TypedResults.Ok<WorkspaceLayoutDto?>(null)` (200 with null body) when the service returns null — frontend distinguishes "no default" from a 5xx fetch failure. **Frontend changes**: (1) `WorkspaceHomeTab.tsx` DELETED — grep confirmed no consumers other than the previous import in `WorkspacePane.tsx`; other references are in comments only. (2) `WorkspacePane.tsx` — removed `import { WorkspaceHomeTab } from "./WorkspaceHomeTab"`; removed the mount effect that called `manager.ensureHomeTab("Home", null, WorkspaceHomeTab)`; added new auto-install effect that waits for `isAuthenticated`, reads `activeLayout` from `useWorkspaceLayouts({ bffBaseUrl, authenticatedFetch, isAuthenticated })` (the SpaarkeAi hook that mirrors LegalWorkspace's cascade — pinned-id → BFF default → first-default/first-system/first-layout), guards against re-runs via `autoInstalledDefaultRef`, skips if the layout is already open (NFR-09 tab restore brought it back), skips if the layout is in `getPinnedWorkspaces()` (the existing task-101 pin auto-open handles it), otherwise dispatches `widget_load` with `widgetType: 'workspace'`, `widgetData: { layoutId, layoutName }`, `displayName: layoutName` — the same shape `WorkspacePaneMenu.handleLayoutSelect` uses. Uses `setTimeout(0)` macrotask deferral (identical pattern to task 101's pin auto-open) so the dispatch lands AFTER `usePaneEvent`'s subscription effect runs. (3) Empty `tabs.length === 0` render path: the first-paint Spinner placeholder is now also the steady-state empty-state UI for users with no default + no pinned tabs. Operator UX rationale: no fake "Home" tab; an empty pane is the honest signal. **Coordination with task 101 pin auto-open**: both effects use `setTimeout(0)`; dispatch order in the macrotask queue is default-install first, pins second. The default-install effect SKIPS dispatch if the default is pinned (pin effect handles it). If not pinned, both dispatch independently; FIFO eviction via `MAX_WORKSPACE_TABS = 8` still applies. **Task 105 popup suppression verified**: `main.tsx` sets `sessionStorage.setItem("spaarke_dailyDigestShown", ...)` BEFORE any React mounts (unchanged). When Daily Briefing renders as the cold-load tab via embedded LegalWorkspaceApp, LegalWorkspace's `useDailyDigestAutoPopup` reads the sentinel and self-suppresses. No popup. **Build**: SpaarkeAi **900.29 KB gzip** (−1.52 KB vs task 107 baseline 901.81 — small DECREASE from removing the hard-coded WorkspaceHomeTab + its inline DailyBriefing registration setup; the section is now reached only via the lazy `'workspace'` widget path that LegalWorkspaceApp already lazy-imports). BFF builds cleanly (0 errors; pre-existing warnings only). **Deploys**: BFF via `Deploy-BffApi.ps1` to `spe-api-dev-67e2xz` (package 75.2 MB, healthz passed, SHA-256 file verification matched all 6 critical files); SpaarkeAi via `Deploy-SpaarkeAi.ps1` (resource ID `5206a442-3451-f111-bec7-7ced8d1dc988`, customizations published). **ADR compliance**: ADR-021 (no styling changes; existing `firstPaintPlaceholder` style preserved), ADR-022 (functional components; `useEffect` + `useRef` patterns identical to existing effects), ADR-028 (`useAiSession()` for `authenticatedFetch` + `bffBaseUrl` + `isAuthenticated`; the new effect uses the existing `useWorkspaceLayouts` hook which auto-defers until auth is ready; no token snapshots), FR-25 / NFR-10 (zero LegalWorkspace files modified). **NOT touched**: `WorkspaceTabManager.ts` (Home tab primitive preserved for future use), `WorkspaceTabManager.test.ts` (tests still pass — they cover the primitive). **NOT staged** (pre-existing dirty): `deploy/api-publish/*.dll`, `src/client/shared/Spaarke.AI.Outputs/src/**/*.{js,d.ts,*.map}`. **Pre-existing follow-ups flagged**: (1) BFF `AvailableTemplates` divergence from shared-lib canonical list still unfixed (unused by clients; future BFF cleanup). (2) Long-term Wave-4 candidate: migrate Corporate Workspace from `SystemWorkspaceLayouts.cs` code constant to a 5th Dataverse system record so ALL system layouts live in one source. R-50 → **RESOLVED**. See deploy memo "Task 109 — BFF system-layout merge + frontend Home removal + auto-install default". | F (Verification — Option B realization) | ✅ | 087, 101, 104, 108 | — | Wave-2-R8 (parallel-safe: FALSE — sequential after Wave 2a) | FULL | 1 |
| 110 | Extend LegalWorkspace QuickSummary section: add `sprk_communication` + `sprk_invoice` cards (Round 8 Wave 3a, 2026-05-22, commit 1 of 2). Operator request: the "My Work" system workspace seeded by Wave 2a task 108 (Dataverse `sprk_workspacelayout` with `sprk_issystem=true` and `sectionsJson` referencing the `quick-summary` section factory) must show 6 cards in a 2x3 grid — existing 4 (My Matters, My Projects, Assign Work, Open Tasks) plus 2 new (Communications, Invoices). Click on each card opens the entity list as a modal dialog. **Reconnaissance**: existing card pattern is fully data-driven via `QUICK_SUMMARY_CARDS: IQuickSummaryCardConfig[]` in `src/solutions/LegalWorkspace/src/components/QuickSummary/quickSummaryConfig.ts`; `QuickSummaryRow.tsx` maps the array to `<QuickSummaryMetricCard>` instances and feeds counts via the generic `useQuickSummaryCounts` hook (iterates the config array — no hook changes needed when adding cards). Click handler is `navigateToEntityList(entityName, viewId)` → `Xrm.Navigation.navigateTo({ pageType: 'entitylist', entityName, viewId }, { target: 2, width/height 80% })` — matches the operator's "click opens the entity modal view" requirement verbatim. **Entity logical names confirmed via Dataverse MCP `list_tables` + `read_query`**: `sprk_communication` (primaryKey `sprk_communicationid`, default view "Active Communications" = savedqueryid `2bf1c5a5-0eca-4f37-92df-2e3c386dee98`), `sprk_invoice` (primaryKey `sprk_invoiceid`, default view "Active Invoices" = `2220a3bc-330e-4405-b441-3605df961878` isdefault=true). Both have standard `statecode` (0=Active) and writeable `ownerid` lookup, so existing `buildOwnerFilter(ctx)` ownership pattern applies verbatim. **Data path: `Xrm.WebApi` direct** (no new BFF endpoints). Zero new BFF placement debt → CLAUDE.md §10 governance satisfied without a Placement Justification section. **Card config additions**: positions 5 + 6 in `QUICK_SUMMARY_CARDS` — Communications uses `MailRegular` (already imported by FilterBar + fileIconMap), Invoices uses `ReceiptRegular` (already imported by NotificationFilters + FilterBar + NotificationItem) — no new icon code splits. Both use `badgeType: "new"` (green badge for `createdon ≥ 7 days ago + active + owner`), matching My Matters / My Projects pattern. **Grid layout change** in `QuickSummaryRow.tsx`: flex-wrap row → CSS grid `gridTemplateColumns: repeat(2, minmax(0, 1fr))` so the 6 cards arrange as 2x3; `defaultHeight` in `quickSummary.registration.ts` bumped from `180px` → `440px` to fit 3 rows of 120px cards + gaps + section padding. **FR-25 evolution note**: standalone Corporate Workspace code page (`sprk_corporateworkspace`) now ships with 6 cards instead of 4 — this is the intended architectural-unity payoff (Round 8 Option B unified architecture), NOT a regression. Per task brief: FR-25's byte-identical clause no longer applies to Wave 3 LegalWorkspace changes; reduced to a behavioral guarantee that no existing Quick Summary card REGRESSES (verified by inspection — the 4 Round-6 cards retain identical config: entity, view, filter, badge logic). **Bundle deltas**: LegalWorkspace 569.59 KB gzip (+0.21 KB vs task 100 baseline 569.38 — within noise for 2 new card entries reusing already-imported icons); SpaarkeAi 900.44 KB gzip (+0.15 KB vs task 109 baseline 900.29 — LegalWorkspaceApp is transpiled into SpaarkeAi's bundle per task 087 Vite config, so this growth is expected). **Embed safety**: works in BOTH standalone Corporate Workspace code page AND SpaarkeAi-embedded "My Work" tab because both surface `Xrm.WebApi` + `Xrm.Navigation` through the existing frame-walk pattern in `WorkspaceLayoutWidget` (Round 4 Fix 4 / task 087). **Files touched**: `src/solutions/LegalWorkspace/src/components/QuickSummary/quickSummaryConfig.ts` (+2 imports, +2 card entries), `src/solutions/LegalWorkspace/src/components/QuickSummary/QuickSummaryRow.tsx` (flex-row → 2-column CSS grid + JSDoc update), `src/solutions/LegalWorkspace/src/sections/quickSummary.registration.ts` (defaultHeight 180px → 440px). **NOT touched**: Documents section (Task 111 scope), SpaarkeAi (LegalWorkspaceApp embed already wires QuickSummary), BFF API (Xrm.WebApi direct), `ThreePaneLayout`/`PaneHeader`/`WorkspacePaneMenu`/`ManageWorkspacesPane` (sealed in earlier tasks), `Spaarke.UI.Components` (no shared-lib changes). **NOT staged** (pre-existing dirty): `deploy/api-publish/*.dll`, `src/client/shared/Spaarke.AI.Outputs/src/**/*.{js,d.ts,*.map}`; also stale `.js`/`.d.ts` siblings of `.ts` files throughout `src/solutions/LegalWorkspace/src/` (~150+ files from a historical `tsc` invocation) — locally removed only the QuickSummary + quickSummary.registration siblings (untracked anyway, so no git impact) to unblock the Vite build whose default resolve order prefers `.js`. **Pre-existing follow-ups flagged**: (1) Stale `.js`/`.d.ts` cruft throughout LegalWorkspace `src/` — future hygiene task should `.gitignore` `src/**/*.{js,d.ts,*.js.map,*.d.ts.map}`, sweep all stale siblings worktree-wide, and configure Vite `resolve.extensions` to prefer `.ts`/`.tsx` first. (2) `QuickSummaryDashboardDialog` remains a "Coming Soon" stub; out of scope for task 110. (3) No custom filtered views (e.g. "My Communications" / "Communications I sent") for the 2 new cards beyond the default system view; matches existing cards. **ADR compliance**: ADR-021 (Fluent v9 tokens — `MailRegular`/`ReceiptRegular` from `@fluentui/react-icons`; no hex literals introduced; `grid-template-columns: repeat(2, minmax(0, 1fr))` is CSS-spec literal same exemption as the existing `width: 100%` patterns), ADR-022 (React 19 functional component — no new hooks), ADR-028 (Xrm-based auth via `webApi` frame-walk; no BFF calls; no token snapshots). **NOT deployed in this task** — Wave-3-R8 commit 1 of 2; coordinated deploy by main session after task 111 (Documents section verification) commits land. R-51 → **RESOLVED**. See deploy memo "Task 110 — QuickSummary extension (sprk_communication + sprk_invoices cards; My Work 2x3 grid)". | F (Verification — Option B Wave 3 enhancement) | ✅ | 108 | — | Wave-3-R8 (parallel-safe: true with 111) | FULL | 1 |
| 114 | Hoist EventsPage components to `@spaarke/events-components` shared library (Round 9 Calendar widget Q&A 2026-05-22, Option A — architectural unity). Operator decision: the new Calendar workspace widget (task 115, dispatched next) and the standalone `sprk_eventspage` code page will consume the SAME Events + Tasks components from a new shared lib, mirroring the Round 8 Option B layout decision. **Reconnaissance**: EventsPage source surface = 9 components (CalendarSection, CalendarDrawer, GridSection, AssignedToFilter, RecordTypeFilter, StatusFilter, ColumnFilterHeader, ColumnHeaderMenu, ViewSelectorDropdown), 1 context (EventsPageContext + 5 selector hooks), 1 service (FetchXmlService — Xrm.WebApi-based view + FetchXML exec), placeholder hooks barrel, providers/ThemeProvider (thin wrapper over `@spaarke/ui-components`), config/eventConfig (drill-through + entity-prefix view discovery + side-pane web-resource names). **Classification**: hoisted = the 9 components + EventsPageContext + FetchXmlService (all UI-agnostic / data-rendering / Xrm-only auth); stayed = App.tsx (page-specific composition: calendar in `Xrm.App.sidePanes`, grid in main area, drill-through URL parsing, mutual-exclusivity logic), main.tsx (standalone entry), providers/ThemeProvider (page-tuned zero-arg-callback adapter — its primitives already live in `@spaarke/ui-components`), config/* (EventsPage-specific web-resource names + view discovery). **New package**: `src/client/shared/Spaarke.Events.Components/` — `@spaarke/events-components` v0.1.0, `private: true`, `"type": "module"`, source-as-entrypoint via `main`/`types`/`exports` map. `tsconfig.json`: `strict: true`, `noEmit: true` (post-task-112 hygiene), `moduleResolution: bundler`, `types: ["node"]`. `package.json` peer-deps React 16-19 + Fluent v9; dev-deps for `tsc --noEmit` (typescript + @types/{react,react-dom,node} + Fluent + React). README documents consumers + ADR-012/021/022/028 constraints. **Folder-per-component layout**: each component has its own folder + `index.ts` re-export, sub-package barrels under `components/`, `context/`, `services/`, `hooks/`, `types/`. Top-level `src/index.ts` barrel uses `export * from`. **Moves done via `git mv`** so file blame/history follows. **Internal-import rewrites in moved files**: `context/EventsPageContext.tsx` → `"../components/CalendarSection/CalendarSection"`; `components/GridSection/GridSection.tsx` → `"../CalendarSection/CalendarSection"` + `"../ColumnHeaderMenu/ColumnHeaderMenu"` + `"../../services"`; CalendarDrawer's same-folder import unchanged. **Surgical type-bridges** (each annotated with `// Task 114 note:`): 4 pre-existing type errors that EventsPage's Vite/esbuild build never enforced (no `tsc` step) were bridged with minimal `as unknown as <T>` casts so the new shared lib's `tsc --noEmit` passes with `strict: true` — Combobox `onInput` 2-arg vs 1-arg drift in AssignedToFilter:299 + RecordTypeFilter:275; `CalendarDrawerProps.eventDates: string[]` vs `IEventDateInfo[]` drift in CalendarDrawer:120; `IEventRecord` missing index signature in GridSection:503/509/535. Runtime semantics unchanged; flagged for follow-up. **EventsPage refactor**: ONE import block in `App.tsx` switched from `./context` + `./components` to `@spaarke/events-components`. Layout composition (calendar-in-sidepane + grid-main + drill-through + sidepane mutual exclusivity) **UNCHANGED**. `package.json` adds `"@spaarke/events-components": "file:../../client/shared/Spaarke.Events.Components"` dep. `tsconfig.json` adds `paths` entries for both new lib AND `@spaarke/ui-components` (the latter was missing). `vite.config.ts` adopts the canonical "shared-lib source alias" pattern (matching LegalWorkspace/SpaarkeAi): `resolveSharedLibDeps()` plugin, `react.include` covers events-components source, `resolve.alias` maps `@spaarke/events-components`, `resolve.extensions` adds `.ts`-first ordering (matches task-112 hardening), `resolve.dedupe` lists React/Fluent. **`@spaarke/ui-components` deliberately NOT source-aliased** in EventsPage's new vite config — it remains on its prebuilt `dist/` resolution path to preserve baseline bundle size attribution. **Preparation for task 115** (alias-only wiring; consumer code follows in task 115): SpaarkeAi + LegalWorkspace vite.config.ts get the events-components source path added to `sharedLibPaths` + react.include + resolve.alias; tsconfig.json paths entries added; package.json dependency added. Task 115 can immediately register a Calendar section factory in LegalWorkspace's `sectionRegistry.ts` without further plumbing. **Cleanup**: deleted 4 orphan barrel files (`components/index.ts`, `context/index.ts`, `services/index.ts`, `hooks/index.ts`) via `git rm`; empty parent dirs auto-pruned. **Build verification (all 4 packages clean)**: `@spaarke/events-components` tsc --noEmit → 0 errors (with `strict: true`); EventsPage Vite → 0 errors, 3,278 modules, **329.92 KB gzip** (baseline 415.97 KB → -86.05 KB / -20.7%, explained: baseline lacked `resolve.dedupe` + `resolve.extensions` so React/Fluent were duplicated in bundle; new config matches LegalWorkspace/SpaarkeAi hygiene — pure tree-shaking improvement, NO behavior change, sampled symbols confirmed); SpaarkeAi Vite → 0 errors (3 pre-existing AppInsights `/*#__PURE__*/` warnings unchanged), 3,409 modules, **900.83 KB gzip** (**0 delta** vs task 112 baseline — alias prep dormant until task 115 imports); LegalWorkspace Vite → 0 errors, 3,216 modules, **569.98 KB gzip** (**0 delta** vs task 112 baseline). **No tests existed** in `src/solutions/EventsPage/src/__tests__/` — pure build verification. **NOT deployed** (pure refactor — task 115 will deploy). **Files touched**: `src/client/shared/Spaarke.Events.Components/**` (NEW package: package.json, tsconfig.json, README.md, src/index.ts, src/components/{CalendarSection,GridSection,AssignedToFilter,RecordTypeFilter,StatusFilter,ColumnFilterHeader,ColumnHeaderMenu,ViewSelectorDropdown}/{<Component>.tsx,index.ts}, src/components/index.ts, src/context/{EventsPageContext.tsx,index.ts}, src/services/{FetchXmlService.ts,index.ts}, src/hooks/index.ts, src/types/index.ts); `src/solutions/EventsPage/{App.tsx,package.json,vite.config.ts,tsconfig.json}` (App.tsx import block + workspace dep + alias + paths); `src/solutions/SpaarkeAi/{package.json,vite.config.ts,tsconfig.json}` (alias prep); `src/solutions/LegalWorkspace/{package.json,vite.config.ts,tsconfig.json}` (alias prep); deploy memo + plan.md + TASK-INDEX.md docs. **NOT touched**: BFF API (zero new endpoints — CLAUDE.md §10 N/A); `src/solutions/EventsPage/{main.tsx, providers/, config/}` (page-specific, stayed); SmartToDo / DailyBriefing / QuickSummary / Documents sections (out of scope); any other shared lib. **NOT staged** (pre-existing dirty): `deploy/api-publish/*.dll`, `src/client/shared/Spaarke.AI.Outputs/src/**/*.{js,d.ts,*.map}` (task 112 archived these); task-113's untracked `docs/architecture/SPAARKEAI-WORKSPACE-ARCHITECTURE.md` + `projects/.../notes/r8-plan.md` (parallel agent owns). **Pre-existing follow-ups flagged** (NOT fixed): (1) the 4 type-drift items bridged with `// Task 114 note:` casts — a future Events-quality task should fix at the source (add `IEventRecord` index signature; align `CalendarDrawerProps.eventDates` to `IEventDateInfo[]`; update Combobox handler signatures for Fluent v9 `onInput` API); (2) `@spaarke/ui-components` source-alias not wired in EventsPage (would tree-shake another ~90 KB if enabled — separate optimization decision outside hoist scope); (3) no unit tests in EventsPage — Calendar widget (task 115) should consider co-located tests. **ADR compliance**: ADR-012 (shared lib consumed by 2+ surfaces), ADR-021 (Fluent v9 tokens — no hex literals, unchanged from source), ADR-022 (React 19 — peer-deps allow 16-19), ADR-028 (services use `Xrm.WebApi` exclusively — no `authenticatedFetch`, no BFF calls). **CLAUDE.md §10 BFF Hygiene**: ZERO new endpoints, ZERO new services, ZERO NuGet changes. R-55 → **ADDED + RESOLVED**. See deploy memo "Task 114 — Hoist EventsPage components to @spaarke/events-components". | F (Refactor — architectural unity prep for task 115 Calendar widget) | ✅ | 108, 110 | — | R9-Calendar (parallel-safe: false; coordinates with task 115) | FULL | 2 |
| 113 | Workspace architecture documentation + honest componentization audit (2026-05-22). Operator request: document the SpaarkeAi workspace pipeline (cold-load → widget render data flow, component model, auth path, BFF surface) AND deliver an honest assessment of whether the current state is componentized + reusable for maintenance and extendability. Phase F doc-only task (read-and-write documentation only — no `src/**` changes, no deploy). Created 4 new docs under `docs/`: (1) `architecture/SPAARKEAI-WORKSPACE-ARCHITECTURE.md` — end-to-end pipeline reference (cold-load → embedded LegalWorkspaceApp render; PaneEventBus channels; ShellStageManager 4-stage lifecycle; BFF surface for workspace endpoints; Dataverse `sprk_workspacelayout` schema + sprk_issystem flag; the 5 system layouts shipped today — Corporate Workspace hard-coded + Daily Briefing + Smart To Do List + My Work + Documents; storage contract for 10 localStorage/sessionStorage keys). (2) `architecture/SPAARKEAI-COMPONENT-MODEL.md` — inventory of `@spaarke/ui-components` (41 component folders), `@spaarke/ai-widgets` (16 workspace-widget registrations + 2 registries + PaneEventBus + AiSessionProvider), `@spaarke/auth` (Spaarke Auth v2 surface with INV-1..INV-8), `@spaarke/legal-workspace` (LegalWorkspaceApp embedded contract). Lists every SpaarkeAi-local and LegalWorkspace-local component with reuse-candidate annotations. Includes PaneEventBus contract section (4 channels × event types × dispatch patterns; subscription-race fix documented). (3) `architecture/SPAARKEAI-COMPONENTIZATION-AUDIT.md` — honest assessment per operator's reuse question. Catalogs **6 specific coupling gaps**: (a) duplicate `useWorkspaceLayouts` hooks in SpaarkeAi + LegalWorkspace with different fallback strategies — drift risk; (b) section factories LegalWorkspace-internal, tightly coupled to `DataverseService` + `FeedTodoSyncContext` + `useQuickSummaryCounts` — only Daily Briefing has been hoisted; (c) `WorkspaceLayoutWidget` hard-wired to `LegalWorkspaceApp` — no `WorkspaceRenderer` interface; (d) Xrm.WebApi vs BFF decision criteria undocumented — Wave 3a's QuickSummary chose Xrm but no rule exists; (e) embedded-mode contract informal (boolean prop with implicit pre-mount setup of `setLegalWorkspaceRuntimeConfig` + `sessionStorage["spaarke_dailyDigestShown"]` + theme ownership — not formalized); (f) dual runtime-config singletons in `main.tsx`. Prioritized remediation backlog: items 1 (Xrm.WebApi vs BFF criteria, 2h doc-only) and 2 (embedded-mode contract, 3h doc-only) are immediate wins; item 3 (consolidate dual hooks, 8h code) is the highest-ROI code refactor; sections 4-7 are deferred until new non-MDA hosts are on the roadmap. Also documents 10 things that ARE unambiguously clean (PaneEventBus, both registries, Spaarke Auth v2, SectionFactoryContext, `GetDefaultLayoutAsync` cascade, the unified `workspace` widget-type, 4-stage shell lifecycle, NFR-09 persistence, MAX_WORKSPACE_TABS FIFO, Vite single-file build). (4) `guides/BUILD-A-NEW-WORKSPACE-WIDGET.md` — step-by-step tutorial with decision tree (Pattern A = section factory + Dataverse layout; B = Code Page dispatcher; C = AI output). Worked example: complete Calendar widget walk-through (5 new files + 2 edits + 2 commands; ZERO SpaarkeAi changes; ZERO BFF changes; ZERO new packages). Pointer updates: root `CLAUDE.md` §16 (added 4 new rows linking to the new docs) + `projects/spaarke-ai-platform-unification-r3/CLAUDE.md` (new "Workspace architecture" section pointing at the new docs). NO src/* changes. NO deploy. NO BFF additions (CLAUDE.md §10 N/A). R-54 → **ADDED + RESOLVED**. See deploy memo "Task 113 — Workspace architecture documentation". | F (Documentation) | ✅ | 108, 109, 110, 111 | — | Wave-R3-doc (parallel-safe: true with 112) | FULL | 4 |
| 112 | Sweep stale tsc-emit .js/.d.ts/.map cruft from src/solutions/LegalWorkspace/src/ + src/client/shared/Spaarke.AI.Outputs/src/ + harden against recurrence (Round 9 cleanup, 2026-05-22). Operator request after Wave 3a (task 110) discovered ~344 untracked stale `.js`/`.d.ts`/`*.map` files actively breaking edits because Vite's default `resolve.extensions` prefers `.js` over `.ts`. **Origin theory**: tsc-emit output that landed inside `src/` instead of `dist/` — most likely from out-of-tree `tsc` invocation (e.g. `npx tsc` from a parent dir without `-p` flag) or an earlier tsconfig revision where `outDir` was unset. Spaarke.AI.Outputs/package.json has `"build": "tsc"`; LegalWorkspace tsconfig uses `noEmit:true` today (so cruft predates current config). Sampled `.d.ts.map` files have `"sources":["ActivityFeed.tsx"]` with no path prefix → emit happened in-place. **Safety verified before deletion**: zero tracked cruft files (`git ls-files` returned NONE for `*.js`/`*.d.ts`/`*.map` under both directories); zero importers reference cruft via explicit `.js` extension (`grep -rE 'LegalWorkspace.*\.js[\"'\'']' src/**/*.{ts,tsx,json}` → 0 matches; same for Spaarke.AI.Outputs); no .vscode/tasks.json automation; no per-project .vscode/ overrides. **Sweep totals**: defensive 2-pass script that verifies a `.ts`/`.tsx` sibling exists for every cruft candidate before deletion — 344 files deleted under LegalWorkspace/src (86 each: `.js` + `.d.ts` + `.js.map` + `.d.ts.map`), 136 files deleted under Spaarke.AI.Outputs/src (34 each); 480 total; ZERO preserved (every cruft file had a sibling, confirming 100% are compilation output). Dry-run executed first (reported identical 480/0); real run matched. Files were untracked so no `git rm` needed. **`.gitignore` hardening**: added 8 rules under `src/solutions/*/src/**` + `src/client/shared/*/src/**` patterns excluding `*.js`/`*.d.ts`/`*.js.map`/`*.d.ts.map`. Pre-rule `git ls-files` returned 2 legitimately tracked files: `src/solutions/SpaarkeAi/src/ribbon/xrm-globals.d.ts` (hand-written Xrm globals declaration) + `src/client/shared/Spaarke.UI.Components/src/__mocks__/diff.js` (Jest mock) — both got explicit `!` negation rules; verified with `git check-ignore -v` → both NOT ignored (negation works); confirmed future cruft path WOULD be ignored. **Vite `resolve.extensions` hardening** (defense-in-depth so even if cruft re-appears Vite never prefers `.js` over `.ts`): added `extensions: [".ts",".tsx",".mts",".cts",".js",".mjs",".cjs",".jsx",".json"]` to `resolve` block in 3 vite configs that import source from outside their own src/: `src/solutions/LegalWorkspace/vite.config.ts`, `src/solutions/SpaarkeAi/vite.config.ts`, `src/solutions/WorkspaceLayoutWizard/vite.config.ts`. Other 18 vite configs left unmodified (no cross-package source aliases → no shadowing risk). **Build verification**: all 4 affected packages built clean post-changes: `@spaarke/ui-components` tsc 0 errors; LegalWorkspace 569.98 KB gzip (matches task 111 baseline); SpaarkeAi 900.83 KB gzip (matches task 111 baseline; same 3 pre-existing AppInsights `/*#__PURE__*/` warnings unchanged); WorkspaceLayoutWizard 427.84 KB gzip. **Files touched**: `.gitignore` (+19 lines incl. comments and negation rules), 3 vite.config.ts files (+3 lines each — `extensions` array). **Files DELETED** (untracked sweep): 480 stale tsc-emit files. **NOT touched**: any `.ts`/`.tsx` source file in either directory; tsconfigs (already correctly set: LegalWorkspace `noEmit:true`, Spaarke.AI.Outputs `outDir:./dist`); other vite configs; BFF API; `deploy/api-publish/*.dll` (pre-existing dirty). **ADR compliance**: ADR-022 (React 19) only-touchpoint — no auth surface change, no behavior change. CLAUDE.md §10 BFF hygiene: zero BFF additions. **NOT deployed** — pure hygiene task. **Pre-existing follow-ups flagged** (NOT fixed): (1) Spaarke.AI.Outputs `clean` script uses POSIX `rm -rf dist` (Windows-incompatible) — future hygiene could cross-platform-ify; (2) LegalWorkspace pre-existing TS errors (task 111 already flagged: WorkspaceGrid unused locals, SmartToDo missing TodoItem, LegalWorkspaceApp IWebApi divergence, DocumentCard.tsx:181/327, sectionRegistry missing @types/node) — Vite/esbuild passes, TS-server-level only; (3) Other shared packages (`Spaarke.UI.Components`, `Spaarke.AI.Widgets`, `Spaarke.AI.Context`, `Spaarke.Auth`) had ZERO untracked cruft on inspection — clean today; the new `.gitignore` rule covers them defensively going forward. R-53 → **ADDED + RESOLVED**. See deploy memo "Task 112 — Sweep stale .js/.d.ts/.map cruft + harden against recurrence". | F (Verification — remediation) | ✅ | 110 | — | R9-cleanup (parallel-safe: true) | STANDARD | 0.5 |
| 111 | LegalWorkspace Documents section polish: 2x10 grid, no scrollbar, +/expand affordances + Smart To Do List embed verification (Round 8 Wave 3b, 2026-05-22, commit 2 of 2). Operator request: the "Documents" system workspace seeded by Wave 2a task 108 (Dataverse `sprk_workspacelayout` with `sprk_issystem=true` and `sectionsJson` referencing the `documents` section factory) must render as 2 wide x 10 height grid (= 20-card cap), no scrollbar, include `+` to add document, include expand-to-open-sprk_document-modal affordance per card. Earlier in R8: "create a new 'system workspace' widget called Smart To Do List; this has the Smart To Do list code page loaded; this can also be added and pinned, but not deleted" — verify embed renders cleanly. **Reconnaissance**: Documents section factory at `src/solutions/LegalWorkspace/src/sections/documents.registration.ts` registers `documents` (kebab-case) — toolbar already has `+` (AddRegular → `onOpenWizard('sprk_documentuploadwizard')`) and Open documents list (OpenRegular → `onOpenDocumentsDialog(viewId)`). DocumentsTab at `src/solutions/LegalWorkspace/src/components/RecordCards/DocumentsTab.tsx` uses `useDocumentsTabList` → `DataverseService` → BFF `/api/dataverse/.../sprk_documents` (broad filter, top=50) → vertical stacked `RecordCardList` with IntersectionObserver windowing. DocumentCard at `src/solutions/LegalWorkspace/src/components/RecordCards/DocumentCard.tsx` uses `RecordCardShell` with tools Preview / AI Summary / Open file / Find Similar; per-card expand was IMPLICIT via double-click + Enter → `navigateToEntity → Xrm.Navigation.openForm` (full-frame, not modal). NO explicit visible expand button. **Changes**: (1) Added `gridMode?: { columns: number; maxRows: number }` prop to `DocumentsTab.tsx`. When provided, branches to new CSS-grid rendering path: `display: grid` + dynamic `gridTemplateColumns: repeat(N, minmax(0, 1fr))` (inline style) + `gap: tokens.spacingHorizontalM` + `flex: 1 1 0` + `overflow: hidden` + defensive `scrollbarWidth: 'none'` + `::-webkit-scrollbar { display: 'none' }` (mirrors task 107's hidden-scrollbar treatment). Effective cap = columns × maxRows = 2 × 10 = 20; docs past index 20 not rendered (no overflow → no scrollbar trigger). DEFAULT mode (no `gridMode`) unchanged — standalone Corporate Workspace keeps vertical `RecordCardList`. (2) Added explicit per-card Expand tool button in `DocumentCard.tsx` tools row (rightmost). Uses `<Tooltip content="Expand">` + `<Button icon={<OpenRegular />}>`; handler calls existing `openRecordDialog("sprk_document", doc.sprk_documentid)` utility from `navigation.ts` which invokes `Xrm.Navigation.navigateTo({pageType:'entityrecord', entityName:'sprk_document', entityId}, {target:2, width:80%, height:80%})` — modal dialog (target:2) per operator spec. Double-click + Enter handlers retained (still open full-frame). Expand button appears in BOTH contexts (standalone + embedded) because `DocumentCard.tsx` is shared — intentional UX improvement, no regression. (3) `documents.registration.ts` factory wired: `maxVisible` 6 → 20; new `gridMode: { columns: 2, maxRows: 10 }` passed through `DocumentsSectionContent` → `DocumentsTab`; new `onShowMore` reuses existing `context.onOpenDocumentsDialog?.(selectedViewIdRef.current)` so "View all (N more)..." link below the grid opens full sprk_document view in modal when totalCount > 20. JSDoc updated. **`+` add affordance** ALREADY EXISTED (verified, not added) — section-level toolbar `<Button icon={<AddRegular />}>` at `documents.registration.ts` lines 287-294 pre-task. **Smart To Do List embed verification (static analysis PASS, runtime smoke deferred to operator)**: confirmed `todo` registration in `sectionRegistry.ts` (import line 18, export line 27); `SmartToDo` honors `embedded` + `disableSidePane` + `webApi` + `userId` + `scope` + `businessUnitId` + `onCountChange` + `onRefetchReady` props (all passed by Wave 2a `todo.registration.ts`); `embeddedRoot` style (SmartToDo.tsx:134) used when `embedded={true}` (line 669); `KanbanHeader` hidden when `disableSidePane=true` (line 674); "Open full view" button gated by `disableSidePane` (line 770); Wave 2a `scripts/system-layouts.json` includes Smart To Do List with `sectionId: "todo"` + `single-column` + sort=1; Wave 2b task 109's auto-install cascade covers runtime path. SmartToDo is SOURCE of task 094's `usePaneCollapse` pattern → well-trodden. NO code changes to SmartToDo per scope discipline. **FR-25 evolution**: standalone Corporate Workspace Documents section uses DEFAULT vertical-list (no `gridMode`); per-card Expand button visible in both contexts (shared `DocumentCard` improvement); embedded "Documents" tab gets new 2×10 grid + 20-card cap + "View all..." overflow; behavioral guarantee preserved (all 5 tools + section toolbar + view picker remain). **Bundle deltas**: LegalWorkspace **569.59 KB gzip** (+0.21 KB vs task 100 baseline 569.38; combined with task 110's parallel +0.21 KB — final post-merge measurement by main session); SpaarkeAi **900.44 KB gzip** (+0.15 KB vs task 109 baseline 900.29 — LegalWorkspaceApp transpiled into SpaarkeAi bundle per task 087 Vite config). **Embed safety**: works in BOTH standalone Corporate Workspace code page AND SpaarkeAi-embedded "Documents" tab because both surface `Xrm.WebApi` + `Xrm.Navigation` through existing frame-walk pattern in `WorkspaceLayoutWidget` (task 087). **Files touched**: `src/solutions/LegalWorkspace/src/components/RecordCards/DocumentsTab.tsx` (added grid-mode rendering branch + styles + new prop; default behavior unchanged), `src/solutions/LegalWorkspace/src/components/RecordCards/DocumentCard.tsx` (+OpenRegular import + handleExpand callback + Expand tool button), `src/solutions/LegalWorkspace/src/sections/documents.registration.ts` (wired gridMode + onShowMore + maxVisible 6 → 20 + JSDoc). **NOT touched**: QuickSummary section (task 110 scope), SmartToDo internals (verification only), SpaarkeAi (LegalWorkspaceApp embed already wires documents), BFF API (no new endpoints), `ThreePaneLayout`/`PaneHeader`/`WorkspacePaneMenu`/`ManageWorkspacesPane` (sealed in earlier tasks), `Spaarke.UI.Components` (no shared-lib changes). **NOT staged** (pre-existing dirty): `deploy/api-publish/*.dll`, `src/client/shared/Spaarke.AI.Outputs/src/**/*.{js,d.ts,*.map}`. **Pre-existing follow-ups flagged**: LegalWorkspace `tsc --noEmit` reports several pre-existing TypeScript errors unrelated to this task (`WorkspaceGrid.tsx` unused locals, `SmartToDo/index.ts` missing `./TodoItem`, `LegalWorkspaceApp.tsx` IWebApi/IThemeWebApi divergence, `sectionRegistry.ts` missing `@types/node`, `DocumentCard.tsx:181` pre-existing `doc.sprk_name` issue in handleOpenFile, `DocumentCard.tsx:327` onClick type mismatch on RecordCardShell prop); Vite build (esbuild) is permissive and passes; future TypeScript cleanup task should address. **ADR compliance**: ADR-021 (Fluent v9 tokens — `tokens.spacingHorizontalM`, `tokens.colorNeutralForeground3`, `tokens.colorPaletteRedForeground3`; no hex literals; `display: grid` + `repeat(N, minmax(0, 1fr))` are CSS-spec literals same exemption as `flex: 1` / `width: 100%`), ADR-022 (React 19 — `useCallback`/`useEffect` standard hooks; no React 16 fallbacks), ADR-025 (`OpenRegular` from `@fluentui/react-icons` v9 family — same package as rest of file), ADR-028 (no new BFF calls; existing `DataverseService` + `useDocumentsTabList` path routes through `authenticatedFetch`). **CLAUDE.md §10 BFF Hygiene**: ZERO new endpoints, ZERO new services, ZERO NuGet changes. **NOT deployed in this task** — Wave-3-R8 commit 2 of 2; coordinated deploy by main session after tasks 110 + 111 commits land. R-52 → **RESOLVED**. See deploy memo "Task 111 — Documents section polish (2x10, no scrollbar, +/expand) + Smart To Do embed verification". | F (Verification — Option B Wave 3 enhancement) | ✅ | 108 | — | Wave-3-R8 (parallel-safe: true with 110) | FULL | 1 |
| 115 | Calendar workspace widget (5th Round-8 system layout) — operator brief (Round 9, 2026-05-22): a new "Calendar" SpaarkeAi system workspace surfaces all events + tasks the user has access to, reusing the `@spaarke/events-components` shared lib hoisted in task 114. Mockup: top date-range filter (Filter by Date Field dropdown + From/To pickers) → 3-month calendar strip → full EventsPage toolbar (New / Delete / Complete / Close / Cancel / On Hold / Archive / Refresh / Calendar) → Active Events view selector → grid of events + tasks. Round 9 Q&A decisions: data scope = all Events + Tasks the user has access to (matches standalone EventsPage); event detail opens as MODAL via `Xrm.Navigation.navigateTo({pageType:'entityrecord', entityName, entityId},{target:2, 80%×80%})` (NOT `Xrm.App.sidePanes`) mirroring task 111 Documents Expand; toolbar = full EventsPage parity. **Implementation**: created `CalendarWorkspaceWidget` in `src/client/shared/Spaarke.Events.Components/src/widgets/CalendarWorkspaceWidget/` (NOT in LegalWorkspace — per the componentization audit in task 113, placing the new container in the shared lib avoids regressing into LW-internal coupling). The widget composes: (a) date-range filter row INLINE (Fluent v9 Dropdown + Input type="date" × 2 wiring through `EventsPageContext.setCalendarFilter` with `dateFields` arg — not a new shared component), (b) single `<CalendarSection>` (which already renders `current month + next 2 ahead` internally per CalendarSection v1.0.0 lines 477-484 `monthsToShow` memo — the brief's "Option 1: render 3 instances" plan was based on an assumed single-month component shape; the actual shape needs no wrapping), (c) full EventsPage toolbar via `<Toolbar size="small">` with all 9 buttons — bulk-action handlers (delete via `Xrm.WebApi.deleteRecord`, complete/close/cancel/on-hold/archive via `bulkStatusUpdate` + `bulkArchive` helpers, `confirmDialog` via `Xrm.Navigation.openConfirmDialog`) partially duplicated from `EventsPage/App.tsx` lines 685-984 rather than extracted into the shared lib (flagged for future `useEventsBulkActions` hook), (d) `<ViewSelectorDropdown>` with `useViewSelection()` defaulted to Active Events (plus a one-shot prime effect for optional `initialViewId` prop), (e) `<GridSection>` auto-binding via `EventsPageContext.filters` with `key={refreshTrigger}` for refresh. Wrapped in `<EventsPageProvider onOpenEvent={handleOpenEvent}>` where `handleOpenEvent` calls `Xrm.Navigation.navigateTo({pageType:'entityrecord', entityName:'sprk_event', entityId},{target:2, width/height:{value:80,unit:'%'}, position:1})` — modal at 80%×80%, NOT side pane. The standalone EventsPage Calendar-toolbar button opens/closes `Xrm.App.sidePanes` Date Filter; in the embedded widget the calendar strip is permanently visible above the grid, so the button is mapped to "clear active date filter" — defensible mapping (the button still has meaning) documented in source. Hidden scrollbar pattern: `scrollbar-width: none` + WebKit override at root (matches task 107 Workspace pane treatment). **Section factory shim** at `src/solutions/LegalWorkspace/src/sections/calendar.registration.ts` mirrors `dailyBriefing.registration.ts` from task 069 — thin `ContentSectionConfig` with `renderContent: () => React.createElement(CalendarWorkspaceWidget)`, no factory-context props forwarded (widget is self-contained, uses `Xrm.WebApi` directly via shared services hoisted in task 114). Registered in `sectionRegistry.ts` (7 sections total: getStarted + quickSummary + latestUpdates + todo + documents + dailyBriefing + **calendar**); added to `sections/index.ts` barrel. **Dataverse seed**: appended 5th entry to `scripts/system-layouts.json` (`name: "Calendar", sectionId: "calendar", layoutTemplateId: "single-column", sortOrder: 4, isDefault: false`). `Deploy-SystemWorkspaceLayouts.ps1 -WhatIf` confirmed 4 existing skip + Calendar would-create; live run: 4 existing skipped + Calendar created (record GUID `fcaf3776-1556-f111-a824-7ced8dd899c3`); verification query reports 5 system layouts. **Build verification** all 3 packages clean: `@spaarke/events-components` tsc --noEmit → 0 errors; LegalWorkspace Vite → 0 errors, 3,243 modules, **587.31 KB gzip** (vs 569.98 task 114 baseline → +17.33 KB); SpaarkeAi Vite → 0 errors, 3,436 modules, **916.60 KB gzip** (vs 900.83 task 114 baseline → +15.77 KB; 3 pre-existing AppInsights `/*#__PURE__*/` warnings unchanged). EventsPage NOT rebuilt (its source unchanged — task 114's hoist already migrated it; standalone EventsPage continues to work identically). **Deploy**: `Deploy-SpaarkeAi.ps1` → updated existing web resource `sprk_spaarkeai` (resource ID **`5206a442-3451-f111-bec7-7ced8d1dc988`**, 3349 KB), customizations.PublishAllXml succeeded. Standalone EventsPage (`sprk_eventspage`) + standalone LegalWorkspace (`sprk_corporateworkspace`) NOT redeployed in this task (operator focus is SpaarkeAi). **Files created**: `src/client/shared/Spaarke.Events.Components/src/widgets/CalendarWorkspaceWidget/{CalendarWorkspaceWidget.tsx, index.ts}`, `src/client/shared/Spaarke.Events.Components/src/widgets/index.ts`, `src/solutions/LegalWorkspace/src/sections/calendar.registration.ts`. **Files modified**: `src/client/shared/Spaarke.Events.Components/src/index.ts` (export `./widgets`), `src/client/shared/Spaarke.Events.Components/package.json` (`./widgets` exports entry), `src/solutions/LegalWorkspace/src/sectionRegistry.ts` + `src/solutions/LegalWorkspace/src/sections/index.ts` (register calendarRegistration), `scripts/system-layouts.json` (5th entry), deploy memo + plan.md (R-56) + TASK-INDEX.md. **NOT touched**: standalone EventsPage `App.tsx` (its calendar-in-sidepane + grid-main composition stays — both surfaces consume `@spaarke/events-components` from the same source), BFF API (CLAUDE.md §10 ZERO new endpoints), SpaarkeAi source beyond the vite alias prep from task 114 (already in place), LegalWorkspace components beyond the section registry, `CalendarSection` itself (composition over modification). **NOT staged** (pre-existing dirty): `deploy/api-publish/*.dll`. **Pre-existing follow-ups flagged (NOT fixed)**: (1) external CalendarSection ◀ ▶ "Navigate Months" arrows from the operator mockup are NOT present — CalendarSection's `viewDate` is internal state, adding external arrows requires lifting `viewDate` to a prop on the shared component (breaks composition-over-modification) or duplicating logic in the widget; deferred to a follow-up round; (2) bulk-action handler duplication between standalone EventsPage and the new widget — future `useEventsBulkActions(selectedIds)` hook extraction into `@spaarke/events-components` if drift becomes a maintenance burden; (3) task 114 type-bridges still in place (`IEventRecord` index signature, Combobox `onInput` drift, `CalendarDrawerProps.eventDates` shape); (4) standalone EventsPage + standalone LegalWorkspace code pages NOT redeployed — if those surfaces should pick up `@spaarke/events-components` changes, queue separate deploys. **Audit observation**: the Calendar widget being in the shared lib (`@spaarke/events-components`) rather than LegalWorkspace successfully demonstrates the proper componentization pattern — the LegalWorkspace `calendar.registration.ts` shim is thin (61 lines including JSDoc), imports `CalendarWorkspaceWidget` from `@spaarke/events-components`, and forwards no factory-context props because the widget is self-contained (uses `Xrm.WebApi` directly via the shared services hoisted in task 114). NO coupling issues encountered that required falling back to LW-local. The audit's prediction (task 113 §2 "section factories LegalWorkspace-internal") is broken for this section: `calendar.registration.ts` is in LegalWorkspace's `sections/` folder ONLY for the SECTION_REGISTRY wiring contract — the widget itself lives in the shared lib. This pattern should be the model for future widgets per the BUILD-A-NEW-WORKSPACE-WIDGET.md tutorial. **ADR compliance**: ADR-012 (shared lib reuse — widget lives in `@spaarke/events-components`, registered via thin LW shim), ADR-021 (Fluent v9 tokens — zero hex / rgba literals introduced; all styles via `tokens.*` and `shorthands.*`), ADR-022 (React 19 — standard hooks, no fallbacks), ADR-028 (auth via `Xrm.WebApi` exclusively — all bulk-action handlers use `getXrm().WebApi.*`, no `authenticatedFetch`, no BFF calls). **CLAUDE.md §10 BFF Hygiene**: ZERO new endpoints, ZERO new services, ZERO new DI registrations, ZERO new NuGet packages, ZERO publish-size delta on BFF. R-56 → **ADDED + RESOLVED**. See deploy memo "Task 115 — Calendar workspace widget (5th Round-8 system layout)". | F (Calendar widget + section factory + Dataverse seed + SpaarkeAi deploy) | ✅ | 114 | — | R9-Calendar widget (parallel-safe: false; coordinates with task 114) | FULL | 1 |
| 116 | Calendar widget polish (Round 10 operator smoke feedback, 2026-05-22) — 5 refinements to the Calendar workspace widget shipped in task 115: (1) horizontal calendar strip (was vertical stacking); (2) responsive month count via ResizeObserver on the strip container — 1/2/3/4/5 months at viewport breakpoints 380/720/1060/1400 (calibrated for SpaarkeAi's default 25/50/25 split per task 117 ~600px Workspace pane = 2 months; 5-month cap to avoid over-rendering on ultra-wide); (3) external ◀ ▶ arrow navigation flanking the strip — 1-month step per click, Shift+click jumps by visible window, **unbounded** (any past/future month/year reachable, removing the prior "current + 2 ahead" cap); (4) hidden internal scrollbar — arrows ARE the navigation, strip container `overflow: hidden`; (5) collapsible strip — right-side chevron toggle; when collapsed the calendar strip unmounts and the grid below absorbs the freed vertical space via existing `flex: 1 1 auto`, while date filter / toolbar / view selector / grid all remain visible. Collapsed preference persists in `localStorage["spaarke:calendar:collapsed"]` via try/catch-wrapped accessors (mirrors `pinnedWorkspaces.ts` task-092 pattern). Also reduced default strip height — `calendarRow` has `flex-shrink: 0; min-height: 280px` (one-month grid baseline) so the strip is bounded rather than dominating the workspace. **Shared-component extension** (backwards-compatible): added 3 OPTIONAL props to `CalendarSection` — `viewDate?: Date` (when provided, CONTROLLED mode — parent owns the month anchor; internal `viewDate` state is bypassed), `monthsToShow?: number` (defaults to 3 — prior internal behavior), `layout?: 'vertical' \| 'horizontal'` (defaults to 'vertical' — prior CSS). EventsPage standalone (`CalendarDrawer` → `CalendarSection`) passes NONE of the new props, so renders byte-identically (confirmed: EventsPage 330.24 KB gzip vs 329.92 task 115 baseline → +0.32 KB near-zero delta consistent with adding 3 optional prop slots to the typed surface). New horizontal-layout CSS uses `display: flex; flex-direction: row; gap: 16px` with each month as `flex: 1 1 0; min-width: 240px`. **CalendarWorkspaceWidget rewrite**: owns `viewDate` state initialized to `startOfMonth(new Date())`; computes responsive `monthsToShow` via `ResizeObserver` on `stripRef` using `computeMonthsForWidth()` (initial 2 to match default 25/50/25, ResizeObserver overrides on first measurement); external Tooltip-wrapped ◀ ▶ `Button` icons (`ChevronLeft/Right20Regular`) with `aria-label` accessibility; `onPrevMonth`/`onNextMonth` advance via `addMonths(prev, ±step)` where step = `e.shiftKey ? monthsToShow : 1`; collapse toggle (`ChevronUp/Down20Regular`) on a right-side bordered slot; `calendarCollapsed` initialized from localStorage via `readCollapsedPref()`; `toggleCollapsed` calls `writeCollapsedPref()`. **New util module** at `src/client/shared/Spaarke.Events.Components/src/utils/dateMath.ts` exports `addMonths` + `startOfMonth` (rejected adding `date-fns` to peer-deps — keeps events-components zero-dep beyond React + Fluent v9, preserving bundle-size attribution baseline). Added `./utils` exports entry to package.json + top-level barrel re-export. **Build verification** all 4 packages clean: `@spaarke/events-components` tsc 0 errors; EventsPage Vite 0 errors, 3,283 modules, **330.24 KB gzip** (vs 329.92 task 115 baseline → +0.32 KB near-zero — confirms standalone EventsPage backwards-compat); LegalWorkspace Vite 0 errors, 3,245 modules, **588.32 KB gzip** (vs 587.31 task 115 baseline → +1.01 KB — ResizeObserver + arrow nav + collapse state + dateMath util); SpaarkeAi Vite 0 errors, 3,438 modules, **917.48 KB gzip** (vs 916.60 task 115 baseline → +0.88 KB; 3 pre-existing AppInsights `/*#__PURE__*/` warnings unchanged). **Deploy**: `Deploy-SpaarkeAi.ps1` → updated existing `sprk_spaarkeai` (resource ID **`5206a442-3451-f111-bec7-7ced8d1dc988`**, 3353 KB), customizations.PublishAllXml succeeded. Standalone EventsPage + standalone LegalWorkspace code pages NOT redeployed in this task — operator focus is SpaarkeAi. **Files modified**: `src/client/shared/Spaarke.Events.Components/src/components/CalendarSection/CalendarSection.tsx` (3 new optional props + horizontal layout styles + controlled-mode logic + monthsToShowCount memo), `src/client/shared/Spaarke.Events.Components/src/widgets/CalendarWorkspaceWidget/CalendarWorkspaceWidget.tsx` (rewrite — responsive horizontal strip + arrow nav + collapse + height fix; preserved all task-115 toolbar handlers + modal-open `Xrm.Navigation.navigateTo`), `src/client/shared/Spaarke.Events.Components/src/index.ts` (export `./utils`), `src/client/shared/Spaarke.Events.Components/package.json` (`./utils` exports entry). **Files created**: `src/client/shared/Spaarke.Events.Components/src/utils/dateMath.ts`, `src/client/shared/Spaarke.Events.Components/src/utils/index.ts`. **NOT touched**: standalone EventsPage `App.tsx` (FR-25-equivalent backwards-compat preserved via default props), `CalendarDrawer.tsx`, `calendar.registration.ts` (thin shim — no changes), ThreePaneLayout / PaneHeader (task 117 owns), BFF (CLAUDE.md §10 ZERO new endpoints). **NOT staged** (pre-existing dirty + task 117 work): `deploy/api-publish/*.dll`, ThreePaneLayout files (task 117 owns those). **Pre-existing follow-ups flagged (NOT fixed)**: (a) bulk-action handler duplication between standalone EventsPage and the widget — carried from task 115, future `useEventsBulkActions` hook extraction; (b) task 114 type-bridges still in place (`IEventRecord` index signature, Combobox `onInput` drift, `CalendarDrawerProps.eventDates` shape); (c) `CalendarDrawer`'s `eventDates: string[]` vs `IEventDateInfo[]` API drift unaffected by this task — bridged with existing cast at the call site; (d) standalone EventsPage + standalone LegalWorkspace code pages NOT redeployed — operator focus is SpaarkeAi. **ADR compliance**: ADR-012 (shared-lib reuse — CalendarSection backwards-compat preserved via optional props with prior-behavior defaults; EventsPage byte-identical; horizontal layout + responsive count are shared-lib-resident, reusable by any future consumer), ADR-021 (Fluent v9 tokens only — zero hex/rgba literals; new styles use `tokens.*` + `shorthands.*`), ADR-022 (React 19 — `useState` lazy initializers, `useRef` for ResizeObserver target, `useCallback` for arrow handlers, `useEffect` cleanup for ResizeObserver disconnect), ADR-028 (auth via `Xrm.WebApi` — unchanged from task 115; ZERO BFF calls). **CLAUDE.md §10 BFF Hygiene**: ZERO new endpoints, ZERO new services, ZERO new DI registrations, ZERO new NuGet packages, ZERO publish-size delta on BFF (task is frontend-only). R-57 → **ADDED + RESOLVED**. See deploy memo "Task 116 — Calendar widget polish (horizontal layout, responsive count, ◀ ▶ navigation, collapse, height)". | F (Calendar widget polish — shared lib API extension + widget rewrite + SpaarkeAi deploy) | ✅ | 115 | — | Wave-R10 (parallel-safe: true; runs alongside task 117 — disjoint files) | FULL | 2 |
| 117 | SpaarkeAi default pane widths 25/50/25 at first cold load (Round 10, 2026-05-22). Operator: "For the sprk ai initial load the default pane width distribution should be 25% / 50% / 25%." Q&A clarification: percentage of viewport at first load; user resizes persist (Recommended). On a fresh session with no saved widths, compute 25/50/25 from the current viewport; once the user drags a splitter, the absolute pixel width persists in sessionStorage and wins on subsequent mounts. **Problem statement**: pre-task ThreePaneLayout used fixed pixel defaults (340 left + 400 right + flex-1 center). On a 2000px viewport this distributes ~17/50/33, NOT the desired 25/50/25. **API shape** (Option A — backwards-compatible additive): added two optional props to `ThreePaneLayoutProps`: `defaultLeftWidthFrac?: number` and `defaultRightWidthFrac?: number` (e.g. 0.25 for 25%). When undefined, legacy `defaultLeftWidthPx` / `defaultRightWidthPx` behavior is preserved verbatim. **Precedence implementation**: introduced `resolveInitialWidth(key, defaultPx, min, frac)` helper that replaces the previous `loadPersistedWidth` in `useThreePaneLayout.ts`. Three-tier resolution: (1) sessionStorage stored pixel width — user-dragged value persists, always wins; (2) frac × window.innerWidth — applied only on cold mount when no stored value, when frac is defined and finite > 0, and when `typeof window !== 'undefined'`; (3) defaultLeftWidthPx — legacy pixel fallback for SSR / non-browser. Computed value clamped to at least `min` so narrow viewports respect the floor. Wired into both `useState` lazy initializers (left + right) — pure-function, fully compatible with React 19 strict-mode double-invocation. **window.innerWidth chosen** as the viewport reference because the layout's own bounding rect is not yet measurable at first mount (containerRef populates post-mount). SpaarkeAi shell fills the viewport, so this is an accurate proxy. **Type definitions**: matched props added to `UseThreePaneLayoutOptions` in `useThreePaneLayout.ts`. JSDoc on both new props documents the precedence chain. **SpaarkeAi wire-up**: `ThreePaneShell.tsx` now passes `defaultLeftWidthFrac={0.25}` + `defaultRightWidthFrac={0.25}` alongside the existing `defaultLeftWidthPx={340}` + `defaultRightWidthPx={400}` (which now serve as the legacy fallback layer in the precedence chain). **LegalWorkspace differentiation (FR-25)**: confirmed via grep that LegalWorkspace does NOT consume `ThreePaneLayout` at all — it uses `WorkspaceGrid`-based section rendering. FR-25 trivially preserved; no LegalWorkspace files touched. **Min-width floor analysis**: existing `minLeftWidthPx={240}` / `minRightWidthPx={240}` / `minCenterWidthPx={320}` still apply. Viewport 800px → 25% = 200px → clamped to 240 + 240 = 480 + center 320 = exactly fits. Viewport 600px → all three clamped to floors → 240+240+320=800 > 600 → would not fit; existing layout clamp logic handles overflow (flagged but no change in this task; SpaarkeAi typical viewports are >> 800px). **Build verification all 3 packages clean**: `@spaarke/ui-components` tsc → 0 errors; SpaarkeAi Vite → 0 errors, 3,436 modules, **916.69 KB gzip** (vs 916.60 task 115 baseline → +0.09 KB trivial — matches the ~100-300 byte expected delta for 2 optional props + 3-branch helper); LegalWorkspace Vite → 0 errors, 3,243 modules, **587.34 KB gzip** (vs 587.31 task 115 baseline → +0.03 KB; module-hash noise — LegalWorkspace does NOT consume ThreePaneLayout so semantically byte-identical). **Smoke walkthrough**: viewport 1600px fresh session → Left ~400px (25%), Center ~800px (50%), Right ~400px (25%); user drags left to 320px → reload → Left=320px (sessionStorage wins); `sessionStorage.clear()` + reload → returns to 25/50/25. **Deploy**: `Deploy-SpaarkeAi.ps1` → updated existing `sprk_spaarkeai` (resource ID **`5206a442-3451-f111-bec7-7ced8d1dc988`**, 3350 KB), customizations.PublishAllXml succeeded. **Files modified**: `src/client/shared/Spaarke.UI.Components/src/components/ThreePaneLayout/ThreePaneLayout.types.ts` (added defaultLeftWidthFrac + defaultRightWidthFrac to ThreePaneLayoutProps with JSDoc-documented precedence), `src/client/shared/Spaarke.UI.Components/src/components/ThreePaneLayout/useThreePaneLayout.ts` (added matching options to UseThreePaneLayoutOptions; replaced `loadPersistedWidth` with `resolveInitialWidth`; wired into useState lazy initializers), `src/client/shared/Spaarke.UI.Components/src/components/ThreePaneLayout/ThreePaneLayout.tsx` (forwarded the two new props from component into hook), `src/solutions/SpaarkeAi/src/components/shell/ThreePaneShell.tsx` (passed defaultLeftWidthFrac=0.25 + defaultRightWidthFrac=0.25 with multi-line comment explaining precedence + Task 117 attribution). **NOT touched**: LegalWorkspace (does not consume ThreePaneLayout — FR-25 preserved), Calendar widget files (task 116 owns), BFF API (CLAUDE.md §10 ZERO new endpoints/services/DI/packages), any styles (no Fluent v9 token changes), `PanelSplitter` / `useTwoPanelLayout`. **NOT staged** (pre-existing dirty): `deploy/api-publish/*.dll`. **Pre-existing follow-ups flagged (NOT fixed)**: (1) ESLint config drift — `Spaarke.UI.Components` has `eslint.config.(js|mjs|cjs)` migration pending from ESLint v9; TypeScript build is the authoritative check and passes clean. (2) Narrow-viewport edge case (<800px) where minimum floors sum exceeds viewport — existing layout overflow handling unchanged in this task; deferred follow-up if observed in practice. **ADR compliance**: ADR-012 (shared lib — `ThreePaneLayout` stays context-agnostic; no PCF-specific dependencies; new props are pure React), ADR-021 (Fluent v9 — no styles touched, no hex/rgba literals introduced), ADR-022 (React 19 — `useState` lazy initializer preserved; pure-function `resolveInitialWidth`; fully compatible with strict-mode double-invocation). **CLAUDE.md §10 BFF Hygiene**: ZERO new endpoints, ZERO new services, ZERO new DI registrations, ZERO new NuGet packages, ZERO publish-size delta on BFF (task is frontend-only). R-58 → **ADDED + RESOLVED**. See deploy memo "Task 117 — Default pane widths 25/50/25 percentage at first cold load". | F (ThreePaneLayout API extension + SpaarkeAi wire-up + deploy) | ✅ | 094, 100 | — | Wave-R10 (parallel-safe: true; runs alongside task 116 Calendar widget polish — disjoint files) | FULL | 1 |
| 118 | Calendar widget Round 11 polish — six operator UX refinements on the SpaarkeAi Calendar workspace widget (2026-05-22). **Fix 1 (collapse chevron moves to filter row)**: removed the chevron from the right edge of the calendar strip and added it to the right edge of the filter row (Filter by Date Field + From + To + flex-spacer + chevron). When collapsed, the filter row remains fully interactive and continues to drive grid filtering — only the strip itself hides. Operator-critical: "allows the filters to still show when the calendars are collapsed and still filter the Event list." Right-justify works at any viewport width via the flex-spacer pattern; no media queries needed. **Fix 2 (different collapse icon)**: `ChevronUp20Regular` / `ChevronDown20Regular` → `CaretUp24Regular` / `CaretDown24Regular`. Caret triangles visually distinct from the month-navigation chevrons (◀ ▶) and clearly convey vertical collapse. Both confirmed in installed `@fluentui/react-icons` version. **Fix 3 (calendar header + separator removed)**: in `CalendarSection.tsx`, the `<div styles.header>` block (Calendar24Regular icon + "Calendar" text + bottom border) is now suppressed when `layout='horizontal'`. The selectionInfo banner + footer (Clear + version) are also suppressed in horizontal mode — the widget owns those affordances on its own filter / toolbar rows. Operator: "duplicative — everyone knows they're calendars." `layout='vertical'` (EventsPage CalendarDrawer) unchanged. **Fix 4 (month grid spacing)**: `CalendarSection.calendarContentHorizontal` column gap bumped from `shorthands.gap("16px")` → `columnGap: tokens.spacingHorizontalXL` (~20px). Visual breathing room between adjacent months without sacrificing visible month count. Vertical layout unaffected. **Fix 5 (event-day highlight + click-to-filter)** — biggest item: investigated existing behavior — CalendarSection already tracked `hasEvents` per day and rendered a 4×4px brand-bg dot at the bottom of the cell, but it was easy to miss at SpaarkeAi's embedded calendar-cell density. Added `dayWithEvents` Griffel style applied to in-month event days: `colorBrandBackground2` + `colorBrandForeground1` + `fontWeightSemibold` + `colorBrandBackground2Hover`. Stacking priority: selected > in-range > has-events > today > other-month base. Other-month days NOT tinted (would compete visually). Added two new optional CalendarSection props (both backwards-compat): `selectedDate?: Date | null` + `onSelectDate?: (date: Date | null) => void`. When `onSelectDate` provided, parent owns the selection: day clicks emit through `onSelectDate` (Shift+click still hits `onFilterChange` for range selection). Widget wires up: `selectedDate: Date | null` widget state + `onDaySelect` callback that (1) updates local state for visual highlight, (2) dispatches `EventsPageContext.setCalendarFilter({type: 'range', start: iso, end: iso, dateFields: [dateField]})` (single-day range — re-uses existing range plumbing in GridSection), (3) on null toggle-off, clears via `{type: 'clear'}`. Added filter-divergence `useEffect` watching `filters.calendarFilter` — if active filter no longer matches local `selectedDate` (because user changed From/To, or another component dispatched), `selectedDate` is reset to `null` so the day highlight cannot lie about what's actually filtering. Day-click takes precedence over From/To (operator priority). **Fix 6 (grid open-in-full icon)**: in the view-selector row, added flex-spacer + `<Button icon={<Open24Regular />} />` with tooltip "Open Events list". Click → `Xrm.Navigation.navigateTo({pageType: 'entitylist', entityName: 'sprk_event'}, {target: 2, width: 80%, height: 80%, position: 1})`. Same `Open24Regular` icon family + 80% sizing used by task 111 Documents per-row Expand and FilePreview "Open Record" — consistent SpaarkeAi modal-launch UX. **Files modified**: `src/client/shared/Spaarke.Events.Components/src/widgets/CalendarWorkspaceWidget/CalendarWorkspaceWidget.tsx` (Fixes 1, 2, 6 + Fix 5 widget-side wiring), `src/client/shared/Spaarke.Events.Components/src/components/CalendarSection/CalendarSection.tsx` (Fix 3 horizontal-mode header/footer suppression + Fix 4 month gap + Fix 5 day-cell `dayWithEvents` styling + new optional controlled props; VERSION 1.0.0 → 1.1.0). **NOT modified**: `src/solutions/EventsPage/src/App.tsx` (standalone — CalendarSection backwards-compat preserved by optional-prop defaults; bundle delta -0.01 KB confirms byte-parity within rounding), `src/solutions/CalendarSidePane/` (its own separate CalendarSection copy — unaffected; pre-existing follow-up to reconcile), BFF (zero new endpoints — `Xrm.Navigation.navigateTo` is the integration seam; CLAUDE.md §10 ZERO new endpoints / services / DI / NuGet / publish-size delta). **Bundle verification**: `@spaarke/events-components` tsc clean; EventsPage Vite → 0 errors, 330.23 KB gzip (-0.01 KB vs task 116 baseline of 330.24 = parity); LegalWorkspace Vite → 0 errors, 588.72 KB gzip (+0.40 KB vs 588.32 baseline); SpaarkeAi Vite → 0 errors, 917.85 KB gzip (+0.37 KB vs 917.48 baseline). All four packages well within expected ~2-5 KB delta per package. **Deploy**: `Deploy-SpaarkeAi.ps1` → updated existing `sprk_spaarkeai` (resource ID **`5206a442-3451-f111-bec7-7ced8d1dc988`**, 3355 KB), customizations.PublishAllXml succeeded. **NOT staged** (pre-existing dirty): `deploy/api-publish/*.dll`. **Pre-existing follow-ups flagged (NOT fixed)**: (1) `CalendarSidePane` web resource maintains its own separate CalendarSection copy divergent from shared library (carried from task 114/115/116). (2) Bulk-action handler duplication between standalone EventsPage and widget pending extraction to `useEventsBulkActions` hook (carried from task 115). (3) `CalendarDrawer.eventDates: string[]` vs `IEventDateInfo[]` API drift (carried from task 116). (4) Standalone EventsPage + LegalWorkspace code pages NOT redeployed (operator focus is SpaarkeAi). **ADR compliance**: ADR-012 (CalendarSection backwards-compat preserved via optional props; EventsPage byte-identical within rounding; horizontal-mode header/footer suppression + event-day tint live in shared lib, reusable by future consumers), ADR-021 (Fluent v9 — `tokens.spacingHorizontalXL`, `colorBrandBackground2`, `colorBrandBackground2Hover`, `colorBrandForeground1`, `fontWeightSemibold`; zero hex / rgba literals), ADR-022 (React 19 — `useState`, `useEffect`, `useCallback` hooks; no React-16 fallbacks), ADR-028 (`Xrm.WebApi` + `Xrm.Navigation` only — zero BFF calls in widget). **CLAUDE.md §10 BFF Hygiene**: ZERO new endpoints / services / DI / NuGet / publish-size delta on BFF (task is frontend-only). R-59 → **ADDED + RESOLVED**. See deploy memo "Task 118 — Calendar widget Round 11 polish". | F (Calendar widget Round 11 polish — shared lib API extension + widget restructure + SpaarkeAi deploy) | ✅ | 115, 116 | — | Wave-R11 (parallel-safe: true — sole task this round) | FULL | 2 |
| 119 | All-panes-collapsed empty state with smiley + Open button forcing 25/50/25 reset (Round 12, 2026-05-22). Operator: "if all three panes are collapsed, show a small smiley face in the center of the view pane; and then below is a button for 'Open' which will open the sprk ai to the 25% 50% 25%." Q&A clarifications: smiley icon = `EmojiSmileSlight24Regular`; Open behavior = force 25/50/25 always (re-apply frac defaults regardless of prior sessionStorage saved widths). **Problem statement**: post-task 100 ThreePaneLayout's "all 3 panes collapsed" state renders as three 48px strips side-by-side with a barren `colorNeutralBackground1` empty area to the right and no obvious affordance to recover the working layout. Operator request: replace the empty area with a friendly empty-state and a single-click reset path. **Files modified**: (1) `src/client/shared/Spaarke.UI.Components/src/components/ThreePaneLayout/useThreePaneLayout.ts` — added new `resetToFracDefaults()` method that re-computes left/right widths as `defaultLeftWidthFrac` / `defaultRightWidthFrac` × `window.innerWidth` (clamped to per-pane minimums) AND persists the new pixel values via `persistWidth` so they OVERWRITE any user-dragged values in sessionStorage (operator: "force 25/50/25 always"). Falls back to legacy pixel defaults when frac is not provided. Does NOT touch visibility — that's left to the existing onToggle* callbacks the consumer already wires. (2) `src/client/shared/Spaarke.UI.Components/src/components/ThreePaneLayout/ThreePaneLayout.types.ts` — added `resetToFracDefaults: () => void` to `UseThreePaneLayoutResult` with JSDoc describing semantics + caller contract. (3) `src/client/shared/Spaarke.UI.Components/src/components/ThreePaneLayout/ThreePaneLayout.tsx` — imported `Button`, `Text`, `EmojiSmileSlight24Regular`; added three new makeStyles entries: `allCollapsedOverlay` (flex: 1, vertical-centered column, neutralBackground2, height 100%), `allCollapsedSmiley` (fontSize 64px, colorNeutralForeground3), `allCollapsedText` (fontSizeBase400, colorNeutralForeground3); consumed `resetToFracDefaults` from the hook; computed `allCollapsed = !isLeftVisible && !isCenterVisible && !isRightVisible`; built `handleOpenFromAllCollapsed` memo (3-step sequence: resetToFracDefaults() → guarded onToggleLeft/Center/Right calls); rendered the empty-state overlay AFTER the right-pane JSX (sibling to the 3 collapsed strips inside the existing top-level flex row) — `flex: 1 1 auto` lets it absorb remaining viewport width after the 3 × 48px = 144px strip column; primary Button is `autoFocus` for keyboard-Enter recovery; outer `<div role="region" aria-label="All panes are collapsed">` for screen-reader landmark. **Implementation strategy chosen: Option B (self-contained inside ThreePaneLayout)** — the layout already owns the width state via the hook + receives the toggle callbacks externally; needed only to extend the hook with one new method, no new props on `ThreePaneLayoutProps`. No SpaarkeAi-side changes needed; ThreePaneShell.tsx already wires the collapsed states + toggle callbacks (since task 094). **sessionStorage keys verified**: `${storageKey}-left-width-px` / `${storageKey}-right-width-px` (dash + suffix) — `resetToFracDefaults` writes new pixel values to BOTH so the next mount picks them up via the existing three-tier `resolveInitialWidth` precedence (stored value wins, but stored value is now the new 25/50/25 pixel equivalent). **LegalWorkspace differentiation (FR-25)**: confirmed task 117 already established LegalWorkspace does NOT consume ThreePaneLayout — bundle delta confirms (+0.07 KB ≈ noise from module-hash regeneration). **Build verification all 3 packages clean**: `@spaarke/ui-components` tsc → 0 errors; SpaarkeAi Vite → 0 errors, 3,438 modules, **918.28 KB gzip** (vs 917.85 task 118 baseline → +0.43 KB trivial — new icon import + Button text + 3 makeStyles entries + 1 hook method + 1 useCallback); LegalWorkspace Vite → 0 errors, 3,245 modules, **588.79 KB gzip** (vs 588.72 baseline → +0.07 KB; module-hash noise — semantically byte-identical since LW doesn't consume ThreePaneLayout). **Smoke walkthrough**: viewport 1600px fresh session → 25/50/25 (task 117); collapse Assistant → 1 strip + Workspace (flex-grown) + Context; collapse Workspace → 2 strips + Context wide; collapse Context → all 3 strips + smiley empty state appears in the remaining ~viewport-144px width with "Welcome back" text + primary Open button autoFocused; press Enter (or click Open) → all 3 uncollapse + widths reset to 25/50/25 of current viewport even if widths had been previously dragged to other sizes. **Deploy**: `Deploy-SpaarkeAi.ps1` → updated existing `sprk_spaarkeai` (resource ID **`5206a442-3451-f111-bec7-7ced8d1dc988`**, 3356 KB), customizations.PublishAllXml succeeded. **NOT touched**: any SpaarkeAi file, any LegalWorkspace file, Calendar widget files (sealed tasks 116/118), BFF API (CLAUDE.md §10 ZERO new endpoints/services/DI/packages), `PanelSplitter` / `useTwoPanelLayout`. **NOT staged** (pre-existing dirty): `deploy/api-publish/*.dll`. **Pre-existing follow-ups flagged (NOT fixed)**: (1) ESLint config drift — `Spaarke.UI.Components` ESLint v9 migration still pending (carried). (2) `resetToFracDefaults` discards user-dragged widths intentionally per operator "force 25/50/25 always"; if future operator changes the policy to "preserve user widths on Open", flip to a non-persisting variant. **ADR compliance**: ADR-012 (ThreePaneLayout stays context-agnostic; new logic + UI added to shared lib; no PCF-specific dependencies; SpaarkeAi pays nothing for opt-in), ADR-021 (Fluent v9 — `tokens.colorNeutralBackground2`, `tokens.colorNeutralForeground3`, `tokens.spacingVerticalL`, `tokens.spacingVerticalXXL`, `tokens.spacingHorizontalXL`, `tokens.fontSizeBase400`; zero hex / rgba literals introduced; smiley icon 64px is a deliberate visual sizing literal like task 096's 48px strip width — no Fluent v9 token matches), ADR-022 (React 19 — `useCallback` memo for handler; pure-function hook method; fully compatible with strict-mode). **CLAUDE.md §10 BFF Hygiene**: ZERO new endpoints, services, DI, NuGet, publish-size delta on BFF (task is frontend-only). R-60 → **ADDED + RESOLVED**. See deploy memo "Task 119 — All-panes-collapsed empty state with smiley + Open". | F (ThreePaneLayout empty-state UX + hook reset method + SpaarkeAi deploy) | ✅ | 094, 100, 117 | — | Wave-R12 (parallel-safe: true — sole task this round) | STANDARD | 1 |
| 124 | Calendar widget Round 13 follow-up #3 (2026-05-22). Three operator findings after task 122 deployed. **Fix 1 — "we have way too many 'x' all over the UI"**: replaced the `Dismiss24Regular` icon Clear button on the filter row with a small Fluent `<Button appearance="subtle" size="small">Clear</Button>`. Same handler (`onCalendarToolbarClick`); same conditional rendering (only when `fromDate \|\| toDate` is non-empty). **Fix 2 (operator: "I don't see the date highlighted")** — root cause: test Task had `sprk_duedate = null` and `createdon = "Apr 2, 2026"`. Per task 121's fallback chain, effective date is Apr 2. But the calendar strip's default `viewDate` was the current month (May 2026), showing May/Jun/Jul/Aug — Apr 2 outside the visible window. Highlight code correct; problem was discoverability. Fix: added auto-anchor in `handleRecordsLoaded`. When records load AND none of their effective dates fall in the visible window AND the records-set signature differs from `lastAutoAnchorSignatureRef.current`, set `viewDate` to the month containing the EARLIEST event date. Sorted-keys signature dedupes so re-renders with same records don't override the user's subsequent navigation. **Fix 3 (operator: "add a little spacing between the bottom of the calendars section and the top of the grid")**: added `marginTop: tokens.spacingVerticalL` (~16px) to `toolbarRow` style. **Files modified**: `src/client/shared/Spaarke.Events.Components/src/widgets/CalendarWorkspaceWidget/CalendarWorkspaceWidget.tsx` only. **Bundle**: SpaarkeAi 918.75 KB gzip (vs 918.60 task 122 baseline → +0.15 KB). **Deploy**: `sprk_spaarkeai` resource `5206a442-3451-f111-bec7-7ced8d1dc988`, 3358 KB, customizations.PublishAllXml succeeded. ADR-021 / 022 / 028 complied. R-70 + R-71 + R-72 added + resolved. | F (Calendar Round 13 follow-up #3 — text Clear button + calendar→grid spacing + auto-anchor to earliest-event month) | ✅ | 121, 122 | — | Wave-R13 (parallel-safe: true) | STANDARD | 1 |
| 123 | Refresh architecture documentation through Round 13 (2026-05-22). The 4 architecture docs from task 113 (commit `92dece20`, Round 9) froze at the post-R9 state; Rounds 10–13 added substantial new architecture surfaces + new components + new operator-facing behaviors. Operator (2026-05-22): "we need to ensure the documents are up to date." Refreshed (1) `docs/architecture/SPAARKEAI-WORKSPACE-ARCHITECTURE.md` — updated header + storage contract (new key `spaarke:calendar:collapsed` from task 116), added §7.1 pane-width precedence (task 117 — stored pixel > frac × innerWidth > legacy pixel), added §7.2 all-panes-collapsed empty state (task 119), expanded "5 system layouts" → "6 system layouts" with Calendar row (task 115) + note on Calendar's distinct "shared-lib widget + thin LW shim" architecture, appended document changelog. (2) `docs/architecture/SPAARKEAI-COMPONENT-MODEL.md` — extended ThreePaneLayout entry to mention task 117 + 119 props + added §2.1.1 with the API additions (`defaultLeftWidthFrac`, `defaultRightWidthFrac`, `resetToFracDefaults`), inserted new §6 `@spaarke/events-components` (task 114) with full component + widget + services inventory + consumers + CalendarSection controlled-mode props (`viewDate`, `monthsToShow`, `layout`, `selectedDate`, `onSelectDate` from tasks 116/118), renumbered subsequent sections (old §6 SpaarkeAi → §7, old §7 LegalWorkspace → §8, old §8 PaneEventBus → §9, old §9 Cross-references → §10). (3) `docs/architecture/SPAARKEAI-COMPONENTIZATION-AUDIT.md` — added §2A documenting Calendar (task 115) as the proven canonical "shared-lib widget + thin LW shim" pattern (62-line shim + ~1100-LOC widget in shared lib); revised §2's "all section factories are LW-internal" prediction (still true for 5 originals, but Calendar shows the path forward); §4 reinforced with task 114 evidence that Events services use Xrm.WebApi exclusively; §7.7 added "Add a NEW shared-lib widget (Calendar-style)" extensibility scenario with effort breakdown; §8 remediation backlog item 5 (hoist 5 sections) recategorized as DEFER (Calendar reduces urgency); §9 expanded with `@spaarke/events-components` (item 11) + Calendar shim (item 12) as unambiguously clean. (4) `docs/guides/BUILD-A-NEW-WORKSPACE-WIDGET.md` — decision tree expanded with **Pattern D: SHARED-LIB WIDGET + THIN LW SHIM** as the recommended default for new widgets; §5 worked example rewritten from forward projection to the actual shipped Calendar implementation (tasks 114 + 115 + polish history 116/118/120/121/122 + commit SHAs); §7 common pitfalls extended with 5 new entries (timezone-asymmetric date keys per task 120's CAUSE D; filter-state conflicts with passive indicators per task 122; field-priority chain for date derivation per task 121; localStorage key drift per task 116; Pattern A vs Pattern D placement justification). Last-reviewed dates bumped to 2026-05-22 across all 4 docs. (5) Pointer touch-ups in root `CLAUDE.md` §16 (4 rows refreshed to mention 6 layouts, the new shared lib, Calendar §2A, Pattern D) and `projects/spaarke-ai-platform-unification-r3/CLAUDE.md` workspace-architecture section header "post-Round-8 reference set" → "current — refreshed through Round 13" with summary of R10–R13 deltas. **Files modified**: 4 architecture/guide docs + 2 CLAUDE.md pointers + TASK-INDEX + plan.md + deploy memo. **Files NOT modified**: any `src/` file, any BFF, `deploy/api-publish/*.dll`. No code changes. No deploy. R-69 added + resolved. | F (Documentation refresh) | ✅ | 113, 114, 115, 116, 117, 118, 119, 120, 121, 122 | — | Standalone (parallel-safe: true — doc-only) | STANDARD | 2 |
| 122 | Calendar widget Round 13 follow-up #2 — three operator findings on the deployed Calendar widget after task 121 landed. **Fix 1 (event-day highlight invisible inside an active From/To range)**: operator showed a screenshot where Task 2 (Spaarke), due May 25 2026, was visible in the grid but May 25 was NOT highlighted blue on the calendar even though the From/To filter was set to 05/20–05/26. Root cause traced to `CalendarSection.tsx` line 646: `showEventsTint = hasEvents && !isOtherMonth && dateState !== "selected" && dateState !== "in-range"`. The `dateState !== "in-range"` exclusion suppressed the brand-tint background whenever a day fell inside an active From/To range, so the user's range selection visually clobbered the event-day indicator. Fix: removed the `in-range` exclusion. The event-day highlight now wins over the in-range visualization (the `selected` state still wins — explicit click should highlight more strongly than the passive indicator). **Fix 2 (operator wants "blue background, white font")**: task 118's `dayWithEvents` style used `colorBrandBackground2` (soft tint) + `colorBrandForeground1` (dark brand text). Operator's exact wording — "blue background, white font" — wanted the solid `colorBrandBackground` + `colorNeutralForegroundOnBrand` (white). Style updated; `:hover` variant updated to `colorBrandBackgroundHover`. **Fix 3 (no way to clear the From/To dates)**: added a small `<Button appearance="subtle" icon={<Dismiss24Regular/>}>` Clear affordance on the filter row (between the To input and the flex spacer). Conditionally rendered only when `fromDate \|\| toDate` is non-empty — no dead affordance in the default state. Reuses the existing `onCalendarToolbarClick` handler (clears both inputs + calendarFilter + selectedDate). New `dateRangeClearSlot` Griffel style (mirrors `collapseToggleSlot`'s alignment). Tooltip: "Clear date filter"; aria-label matches. **Fix 4 (months visually run into each other in horizontal strip)**: operator follow-up message: "visually the calendars run into each other." Added a thin `borderLeft: 1px solid colorNeutralStroke2` to every horizontal month container EXCEPT the first (`:first-of-type` override removes the border on the leftmost). Combined with the existing `columnGap: tokens.spacingHorizontalXL` (~20px) this creates a subtle light vertical divider centered in each inter-month gap. **Files modified**: `src/client/shared/Spaarke.Events.Components/src/components/CalendarSection/CalendarSection.tsx` (showEventsTint condition + dayWithEvents style + monthContainerHorizontal borderLeft), `src/client/shared/Spaarke.Events.Components/src/widgets/CalendarWorkspaceWidget/CalendarWorkspaceWidget.tsx` (Clear button JSX + dateRangeClearSlot style). **Bundle**: SpaarkeAi 918.60 KB gzip (vs 918.48 task 121 baseline → +0.12 KB trivial); LegalWorkspace + EventsPage carry the CalendarSection changes too but were not rebuilt this round (operator focus is SpaarkeAi). **Deploy**: `Deploy-SpaarkeAi.ps1` → `sprk_spaarkeai` resource `5206a442-3451-f111-bec7-7ced8d1dc988`, 3357 KB, customizations.PublishAllXml succeeded. **ADR compliance**: ADR-021 (Fluent v9 tokens only — `colorBrandBackground`, `colorNeutralForegroundOnBrand`, `colorBrandBackgroundHover`, `colorNeutralStroke2`, `spacingHorizontalL` — zero hex / rgba added), ADR-022 (React 19 functional), ADR-028 (no auth surface change). **CLAUDE.md §10**: zero BFF endpoints / services / DI / NuGet / publish-size delta. R-65 + R-66 + R-67 + R-68 added + resolved. | F (Calendar widget Round 13 follow-up #2 — event-day highlight inside active range + solid brand background + Clear filter button + inter-month divider) | ✅ | 120, 121 | — | Wave-R13 (parallel-safe: true) | STANDARD | 1 |
| 121 | Calendar event-date fallback per operator (Round 13 follow-up, 2026-05-22) — corrected the date-field priority chain in `CalendarWorkspaceWidget.handleRecordsLoaded`. Was: `sprk_duedate → sprk_startdate → createdon` (task 120 default). Now: `sprk_duedate → createdon` (sprk_startdate intentionally skipped per operator: "the date highlight should be based on due date or if no due date then created date"). One-line code change + JSDoc comment update documenting the operator rationale (events without a due date anchor to when they were created, not when they were scheduled to begin — start-date fallback could mislead the user about deadline visibility). **Files modified**: `src/client/shared/Spaarke.Events.Components/src/widgets/CalendarWorkspaceWidget/CalendarWorkspaceWidget.tsx` (handleRecordsLoaded — removed the sprk_startdate fallback branch; JSDoc rewritten). **Bundle**: SpaarkeAi 918.48 KB gzip (vs 918.49 task 120 baseline = -0.01 KB net; removing the fallback shrinks by a hair). EventsPage / LegalWorkspace untouched. **Deploy**: `Deploy-SpaarkeAi.ps1` → `sprk_spaarkeai` resource `5206a442-3451-f111-bec7-7ced8d1dc988` (3357 KB), customizations.PublishAllXml succeeded. ADR-022 (React 19 functional) complied. Zero BFF / Xrm / auth surface changes. R-64 added + resolved. | F (Calendar event-date fallback policy correction) | ✅ | 120 | — | Wave-R13 (parallel-safe: true) | STANDARD | 1 |
| 120 | Calendar widget Round 13 polish — event-day highlight bug fix + grid spacing + Open icon row move (2026-05-22). Three operator findings from Round 13. **Fix 1 (event-day highlight bug fix)**: investigated why task 118's `dayWithEvents` brand-blue tint wasn't appearing on calendar cells matching events visible in the grid. **Root cause**: CAUSE A — `EventsPageContext.eventDates` was NEVER populated by the Calendar workspace widget. Context defaulted to `[]`; `setEventDates` was only ever called by `CalendarSidePane/App.tsx` (a separate web resource, not the widget). Result: `CalendarSection.eventDateMap` was always empty → `hasEvents` always false → `showEventsTint` always false → no tint rendered. Bonus latent bug surfaced (CAUSE D — timezone): `CalendarSection.toIsoDateString` used `date.toISOString().split('T')[0]` which converts to UTC, causing off-by-one day bugs for users in positive UTC offsets. Fixed pre-emptively. **Implementation**: (a) `GridSection.tsx` — new optional `onRecordsLoaded?: (records: IEventRecord[]) => void` callback fired alongside `onDataLoaded` at all three fetch paths (mock, FetchXML, OData fallback) + error case. Existing consumers unaffected by the additive prop. (b) `CalendarWorkspaceWidget.tsx` — new `handleRecordsLoaded` useCallback derives per-date counts from loaded records using LOCAL date components (`getFullYear/Month/Date`, never `toISOString()`) keyed as `YYYY-MM-DD`, then dispatches `setEventDates(next)`. Date field priority: `sprk_duedate` → `sprk_startdate` → `createdon` (matches the date-range filter dropdown's default). Wired `onRecordsLoaded={handleRecordsLoaded}` on `<GridSection>`. (c) `CalendarSection.tsx` `toIsoDateString` rewritten to use local components — symmetric with widget-side derivation so eventDateMap membership checks succeed regardless of timezone. Standalone EventsPage CalendarDrawer benefits automatically (it was also affected by the latent timezone bug). **Fix 2 (grid-to-calendar spacing)**: added `marginTop: tokens.spacingVerticalL` (~16px) to `gridContainer` style. Operator: "add a little padding/margin space between the grid and the calendars." Token chosen rather than `XL` (~20px) per operator "a little." **Fix 3 (Open icon to toolbar row)**: restructured `toolbarRow` from a single `<Toolbar>` element to a flex container: `<Toolbar>{CRUD buttons}</Toolbar>` + `<div toolbarRowSpacer/>` + `<Button icon={<Open24Regular/>}/>`. Open button right-justified via flex-spacer. View-selector row simplified to ONLY contain `<ViewSelectorDropdown>` (the `viewSelectorSpacer` style was removed entirely — no longer referenced). Click target unchanged: `Xrm.Navigation.navigateTo({pageType: 'entitylist', entityName: 'sprk_event'}, {target: 2, width: 80%, height: 80%})` (same as task 118). **Files modified**: `src/client/shared/Spaarke.Events.Components/src/widgets/CalendarWorkspaceWidget/CalendarWorkspaceWidget.tsx` (Fix 1 widget-side derivation + Fix 2 marginTop + Fix 3 toolbar restructure + viewSelectorSpacer removal), `src/client/shared/Spaarke.Events.Components/src/components/GridSection/GridSection.tsx` (Fix 1 — new `onRecordsLoaded` prop wired at 3 fetch paths + error case + effect dep array), `src/client/shared/Spaarke.Events.Components/src/components/CalendarSection/CalendarSection.tsx` (Fix 1 — `toIsoDateString` rewritten to use LOCAL date components; benefits standalone EventsPage CalendarDrawer too). **NOT modified**: `src/solutions/EventsPage/src/App.tsx` (standalone — CalendarDrawer pre-existing follow-up about `eventDates: string[]` vs `IEventDateInfo[]` API drift carried, but the latent timezone bug is now fixed via the shared CalendarSection), `src/solutions/CalendarSidePane/` (its own separate CalendarSection copy — pre-existing follow-up), BFF (zero new endpoints — CLAUDE.md §10 ZERO new endpoints / services / DI / NuGet / publish-size delta). **Bundle verification all 4 packages clean**: `@spaarke/events-components` tsc → 0 errors; EventsPage Vite → 0 errors, **330.33 KB gzip** (vs task 119 baseline 330.23 → +0.10 KB; CalendarSection toIsoDateString rewrite + GridSection new prop); LegalWorkspace Vite → 0 errors, **589.09 KB gzip** (vs 588.79 baseline → +0.30 KB; includes widget changes via shared lib); SpaarkeAi Vite → 0 errors, 3,438 modules, **918.49 KB gzip** (vs 918.28 baseline → +0.21 KB trivial — handleRecordsLoaded memo + marginTop style + toolbar restructure JSX). **Verification trace (Fix 1)**: Feb 3 sprk_event exists → FetchXML returns it → GridSection.fetchEvents loads → onRecordsLoaded({records}) fires → widget.handleRecordsLoaded derives `key="2026-02-03"` → setEventDates([{date:"2026-02-03",count:1}]) → context.eventDates updated → re-render with new prop → CalendarSection eventDateMap.has("2026-02-03") = true → showEventsTint = true → `dayWithEvents` class applied → `colorBrandBackground2` blue tint visible. **Deploy**: `Deploy-SpaarkeAi.ps1` → updated existing `sprk_spaarkeai` (resource ID **`5206a442-3451-f111-bec7-7ced8d1dc988`**, 3357 KB), customizations.PublishAllXml succeeded. **NOT staged** (pre-existing dirty): `deploy/api-publish/*.dll`. **Pre-existing follow-ups flagged (NOT fixed)**: (1) `CalendarSidePane` separate CalendarSection copy divergent from shared library (carried from 114/115/116/118/119). (2) Bulk-action handler duplication between standalone EventsPage and Calendar widget pending `useEventsBulkActions` hook extraction (carried). (3) `CalendarDrawer.eventDates: string[]` vs `IEventDateInfo[]` API drift (carried). (4) ESLint config drift — `Spaarke.UI.Components` ESLint v9 migration still pending (carried). (5) Standalone EventsPage + LegalWorkspace code pages NOT redeployed (operator focus is SpaarkeAi only). **ADR compliance**: ADR-012 (shared lib — `IEventRecord` already exported; new `onRecordsLoaded` callback additive on GridSection; CalendarSection backwards-compat preserved; EventsPage benefits automatically from timezone fix in shared `toIsoDateString`), ADR-021 (Fluent v9 — `tokens.spacingVerticalL` only token change; zero hex / rgba literals introduced; existing `colorBrandBackground2` / `colorBrandForeground1` tint kept), ADR-022 (React 19 — `useCallback` memo; pure-function derivation; fully compatible with strict-mode double-invocation), ADR-028 (`Xrm.WebApi` + `Xrm.Navigation` only — zero BFF calls in widget). **CLAUDE.md §10 BFF Hygiene**: ZERO new endpoints, services, DI, NuGet, publish-size delta on BFF (task is frontend-only). R-61 + R-62 + R-63 → **ADDED + RESOLVED**. See deploy memo "Task 120 — Calendar widget Round 13 polish". | F (Calendar widget Round 13 polish — event-day highlight bug fix + grid spacing + Open icon row move + SpaarkeAi deploy) | ✅ | 115, 118 | — | Wave-R13 (parallel-safe: true — sole task this round) | FULL | 1 |
| 070 | Deploy SpaarkeAi via `Deploy-SpaarkeAi.ps1` | G (Deploy) | ✅ | 060-063, **065** | 071-074 | — | FULL | 1 |
| 071 | UI smoke — Assistant pane (FR-02..FR-09) | G (Smoke) | 🔄 | 070 | 090 | G | STANDARD | 2 |
| 072 | UI smoke — Workspace pane (FR-10..FR-16) | G (Smoke) | 🔄 | 070 | 090 | G | STANDARD | 2 |
| 073 | UI smoke — Context pane (FR-17..FR-22) | G (Smoke) | 🔄 | 070 | 090 | G | STANDARD | 2 |
| 074 | NFR verification (Lighthouse + History overlay timings) | G (Smoke) | 🔄 | 070 | 090 | G | STANDARD | 2 |
| 090 | Project wrap-up (code-review + adr-check + repo-cleanup + lessons-learned) | H (Wrap-up) | 🔲 | 070-074 (+050, 051 if Phase E) | — | — | STANDARD | 3 |

**Total estimated effort**: ~75 hours (excludes Phase E if skipped). With Phase E: ~80 hours.

---

## Parallel Execution Plan (Waves)

Tasks in the same wave can run simultaneously once prerequisites are met. Each task agent invokes `task-execute` independently. **Hard cap: 6 concurrent agents per wave.**

### Wave 0 — Spike (serial, blocking)

| Wave | Tasks | Prerequisite | Files Touched | Safe to Parallelize |
|------|-------|--------------|---------------|---------------------|
| 0 | 001 | none | `notes/spikes/*` only | n/a (single task) |

### Wave 1 — Foundations (4-way parallel)

| Wave | Tasks | Prerequisite | Files Touched | Safe to Parallelize |
|------|-------|--------------|---------------|---------------------|
| 1 | 010, 011, 012, 013 | 001 ✅ | Separate modules (Spaarke.UI.Components, WorkspaceTabManager, ActionCard lift, telemetry helpers) | ✅ Yes |

### Wave 2 — Independent Phase B/C/D tasks (≤6 parallel)

After Wave 1 completes, dispatch first-tier independent tasks from B/C/D in parallel (up to 6):

| Wave | Tasks | Prerequisite | Files Touched | Safe to Parallelize |
|------|-------|--------------|---------------|---------------------|
| 2a | 020, 021, 023, 024 | 010-013 ✅ | WelcomePanel, ConversationPane, SprkChat (023 reserves SprkChat lock), `useChatFileAttachment` hook (net-new) | ✅ Yes (23 + 24 don't overlap; 25 waits) |
| 2b | 030, 033, 034, 040, 041, 043 | 010-013 ✅ | WorkspacePane (030), LayoutWizard, DailyBriefing (net-new), ContextPaneController (040), GetStartedCardsWidget (net-new), wizard wrappers (net-new) | ✅ Yes (no file overlaps; max 6 concurrent) |
| 2c | 044, 045 | 010-013 ✅ | Analysis Builder intents, AssignWork launcher (both net-new) | ✅ Yes |

> Note: Waves 2a + 2b + 2c may need to be split further if you want strict 6-agent cap respected per wave. Sequence option: dispatch 2a (4 agents), then 2b (6 agents), then 2c (2 agents) on success.

### Wave 3 — Phase B/C/D dependent tasks (serial within phase, parallel across phases)

| Wave | Tasks | Prerequisite | Files Touched | Safe to Parallelize |
|------|-------|--------------|---------------|---------------------|
| 3a | 022 | 021 ✅ | ConversationPane.tsx + new HistoryOverlay.tsx | serial w/ 021 |
| 3b | 025 | 023, 024 ✅ | SprkChat.tsx (serial w/ 023) | serial w/ 023 |
| 3c | 031, 032 | 030 ✅ | WorkspacePane.tsx (serial w/ 030) | serial w/ 030 |
| 3d | 035 | 013, 034 ✅ | DailyBriefingSection.tsx (extends 034) | serial w/ 034 |
| 3e | 042 | 040, 041 ✅ | ContextPaneController.tsx + ContextWidgetRegistry.ts | serial w/ 040 |

Across phases (3a + 3b + 3c + 3d + 3e), these can run in parallel since they touch different files (still respecting 6-agent cap).

### Wave 4 — Phase B final + Phase E (conditional)

| Wave | Tasks | Prerequisite | Notes |
|------|-------|--------------|-------|
| 4a | 026 | 001, 024, 025 ✅ | Frontend attachment-payload wiring — proceeds whether Phase E is in or out |
| 4b | 050 (if needed) | 001 (verdict: extension-needed) | Backend payload extension |
| 4c | 051 (if needed) | 050 ✅ | BFF unit tests |

If spike says NO extension needed: mark 050 + 051 as ⏭️ skipped; proceed directly to Wave 5.

### Wave 5 — Verification (4-way parallel, read-only audits)

| Wave | Tasks | Prerequisite | Files Touched | Safe to Parallelize |
|------|-------|--------------|---------------|---------------------|
| 5 | 060, 061, 062, 063 | All Phases B/C/D (+E if executed) ✅ | Audit reports only (notes/*) | ✅ Yes |

### Wave 6 — Deploy (serial)

| Wave | Tasks | Prerequisite | Notes |
|------|-------|--------------|-------|
| 6 | 070 | 060-063 ✅ | Production deploy via `Deploy-SpaarkeAi.ps1` |

### Wave 7 — Smoke (4-way parallel, independent tests)

| Wave | Tasks | Prerequisite | Files Touched | Safe to Parallelize |
|------|-------|--------------|---------------|---------------------|
| 7 | 071, 072, 073, 074 | 070 ✅ | Smoke reports only (notes/*) | ✅ Yes |

### Wave 8 — Wrap-up (serial, final)

| Wave | Tasks | Prerequisite | Notes |
|------|-------|--------------|-------|
| 8 | 090 | 071-074 ✅ (+ 050, 051 if Phase E) | Quality gates + repo-cleanup + lessons-learned |

---

## How to Execute Parallel Waves

1. Confirm all prerequisites are ✅ in this index.
2. Invoke `task-execute` skill via the Skill tool with **multiple tool calls in ONE message** (one per task in the wave).
3. Each agent runs task-execute for its task with full context loading.
4. After all agents in the wave complete, run build verification (per [`CLAUDE.md` build-verification-between-waves](../../CLAUDE.md)):
   - If any `.cs` modified → `dotnet build src/server/api/Sprk.Bff.Api/`
   - If any `.ts`/`.tsx` modified → `npm run build` in the relevant package
5. Update this index (🔲 → ✅ for each completed task) before proceeding to next wave.
6. If any task fails → mark 🔄, do NOT skip wave verification, decide retry vs report.

**Respect**:
- 6-agent cap per wave (hard limit — API overload guard).
- Sub-agent permission boundary: tasks with `<parallel-safe>false</parallel-safe>` due to `.claude/` writes MUST run in main session sequentially (none here, but rule still applies).

---

## Critical Path

`001 → (010 ∨ 011 ∨ 012 ∨ 013) → (Phase B/C/D longest chain: 030 → 032 → ...) → 060-063 (verify) → 070 (deploy) → 071-074 (smoke) → 090 (wrap-up)`

Longest chain (Phase C): `001 → 010 → 030 → 032 → 060-063 → 070 → 071-074 → 090` ≈ 11 tasks deep. With parallel execution of other phases, total wall-clock is bounded by this path.

---

## High-Risk Items

| Risk ID | Task | Risk | Mitigation |
|---|---|---|---|
| R-1 | 001 → 026, 050, 051 | Spike result determines whether Phase E exists | Phase E tasks are gated; 026 has conditional behavior path |
| R-3 | 012 → 041 | ActionCard lift may surface coupling issues | Decision documented in `notes/drafts/012-actioncard-decision.md`; alternate path is direct import from LegalWorkspace |
| R-4 | 034 | Daily Briefing endpoint may lack per-user caching | `useDailyBriefing` adds frontend TTL cache (~5 min per ADR-014) |
| R-5 | 063 → potentially follow-up | NFR-09 persistence may need `SessionPersistenceService` extension | Verification surfaces gap; fix task opened mid-Phase F if needed |
| R-6 | 024, 061 | PDF.js + mammoth bundle size may exceed budget | Lazy-load both; verify in task 061 |

---

## File Modification Map (for parallel-safety analysis)

| File | Touched by tasks |
|---|---|
| `src/solutions/SpaarkeAi/src/components/WelcomePanel.tsx` | 020 |
| `src/solutions/SpaarkeAi/src/components/conversation/ConversationPane.tsx` | 021, 022 (serial) |
| `src/solutions/SpaarkeAi/src/components/conversation/HistoryOverlay.tsx` (new) | 022 |
| `src/client/shared/Spaarke.UI.Components/src/components/SprkChat/SprkChat.tsx` | 023, 025, 026 (serial) |
| `src/client/shared/Spaarke.UI.Components/src/components/SprkChat/hooks/useChatFileAttachment.ts` (new) | 024 |
| `src/solutions/SpaarkeAi/src/components/workspace/WorkspacePane.tsx` | 030, 031, 032 (serial) |
| `src/solutions/SpaarkeAi/src/components/workspace/WorkspaceTabManager.ts` | 011 |
| `src/solutions/SpaarkeAi/src/components/workspace/WorkspacePaneMenu.tsx` (new) | 032 |
| `src/solutions/SpaarkeAi/src/components/workspace/WorkspaceLandingWidget.tsx` (delete) | 031 |
| `src/solutions/WorkspaceLayoutWizard/src/App.tsx` or `steps/TemplateStep.tsx` | 033 |
| `src/solutions/LegalWorkspace/src/sections/dailyBriefing/*` (new) | 034, 035 (serial) |
| `src/solutions/LegalWorkspace/src/sectionRegistry.ts` | 034 |
| `src/solutions/SpaarkeAi/src/components/context/ContextPaneController.tsx` | 040, 042 (serial) |
| `src/client/shared/Spaarke.AI.Widgets/src/widgets/context/GetStartedCardsWidget.tsx` (new) | 041 |
| `src/client/shared/Spaarke.AI.Widgets/src/registry/ContextWidgetRegistry.ts` | 042 |
| `src/client/shared/Spaarke.AI.Widgets/src/widgets/workspace/*` (new wizard wrappers) | 043, 044, 045 |
| `src/client/shared/Spaarke.UI.Components/src/components/PaneHeader/*` (new) | 010 |
| `src/client/shared/Spaarke.UI.Components/src/components/ActionCard/*` (new or verified) | 012 |
| `src/solutions/SpaarkeAi/src/telemetry/errorTelemetry.ts` (new) | 013 |
| `src/server/api/Sprk.Bff.Api/Api/Ai/ChatEndpoints.cs` | 050 (conditional) |
| Test files for above | 011, 010, 013, 024, 033, 050 → 051 |

**Conflict-free parallelization rule**: any two tasks listed against the same file MUST be serialized. This index encodes that via `parallel-safe: false` in the relevant POML files.

---

*This index is the single source of truth for task status. Update 🔲 → ✅ on each task completion. The `task-execute` skill updates this file automatically as part of its protocol.*
