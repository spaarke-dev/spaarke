<?xml version="1.0" encoding="UTF-8"?>
<task id="034" project="record-header-and-notepad-r1">
  <metadata>
    <title>Build MemoList component (FR-16)</title>
    <phase>3: Notepad</phase>
    <status>not-started</status>
    <estimated-hours>2</estimated-hours>
    <dependencies>033</dependencies>
    <blocks>037</blocks>
    <tags>react, typescript, fluent-ui, frontend</tags>
    <rigor-hint>FULL</rigor-hint>
    <rigor-reason>New React component + unit tests</rigor-reason>
    <parallel-group>E</parallel-group>
    <parallel-safe>true</parallel-safe>
  </metadata>

  <prompt>Build src/solutions/Notepad/src/components/MemoList.tsx per FR-16 — a dropdown showing prior memos (most-recent first) with derived-title preview + createdon date. Clicking an item switches the editor to that memo.</prompt>

  <role>Spaarke platform developer. Fluent UI v9 expert.</role>

  <goal>MemoList.tsx + tests; renders memo list with derived titles; click invokes callback with memo id.</goal>

  <context>
    <background>Design §3.6 — the list icon dropdown. Uses deriveTitle utility from 031 and memo data from 033.</background>
    <relevant-files>
      <file>src/solutions/Notepad/src/components/MemoList.tsx</file>
      <file>src/solutions/Notepad/src/components/__tests__/MemoList.test.tsx</file>
    </relevant-files>
  </context>

  <constraints>
    <constraint source="ADR-021">Fluent v9 semantic tokens; zero hex/rgb.</constraint>
    <constraint source="spec">FR-16: dropdown of prior memos with derived-title preview; click switches editor.</constraint>
  </constraints>

  <knowledge>
    <files>
      <file>projects/record-header-and-notepad-r1/spec.md</file>
      <file>projects/record-header-and-notepad-r1/CLAUDE.md</file>
      <file>.claude/adr/ADR-021-fluent-ui-design-system.md</file>
      <file>.claude/patterns/ui/fluent-v9-component-authoring.md</file>
    </files>
  </knowledge>

  <steps>
    <step order="1">Declare IMemoListProps { memos: Memo[]; currentMemoId: string | null; onSelect: (id: string) =&gt; void; }</step>
    <step order="2">Implement as a Fluent v9 Menu triggered by a list icon button. Each MenuItem shows deriveTitle(memo.sprk_body) + createdon date (Intl.DateTimeFormat locale).</step>
    <step order="3">Highlight the currentMemoId row (colorNeutralBackground3 or similar).</step>
    <step order="4">Add unit tests: renders one MenuItem per memo; onSelect fires with correct id; empty list renders "No memos yet" state.</step>
    <step order="5">Run tests.</step>
    <step order="6">Update TASK-INDEX.md: mark 034 ✅.</step>
    <step order="7">Document deviations in notes/.</step>
  </steps>

  <tools><tool name="npm">Build + test</tool></tools>

  <outputs>
    <output type="code">src/solutions/Notepad/src/components/MemoList.tsx</output>
    <output type="test">src/solutions/Notepad/src/components/__tests__/MemoList.test.tsx</output>
  </outputs>

  <acceptance-criteria>
    <criterion testable="true">Given 3 memos, when list opens, then 3 MenuItems visible with derived titles.</criterion>
    <criterion testable="true">Given click on a MenuItem, onSelect fires with that memo's id.</criterion>
    <criterion testable="true">Given empty memos list, MenuItem "No memos yet" renders.</criterion>
  </acceptance-criteria>

  <ui-tests>
    <test name="Dark Mode Compliance (ADR-021)">
      <steps><step>Render MemoList in webLightTheme then webDarkTheme</step></steps>
      <expected>All Menu colors adapt via semantic tokens</expected>
    </test>
  </ui-tests>

  <notes>Do NOT sort inside this component — the memos prop is already sorted by 033.</notes>

  <execution>
    <skill>.claude/skills/task-execute/SKILL.md</skill>
    <protocol>Load knowledge files first. Follow task-execute skill.</protocol>
  </execution>
</task>
