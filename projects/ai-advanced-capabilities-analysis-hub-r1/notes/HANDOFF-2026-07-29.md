# CONTEXT HANDOFF — ai-advanced-capabilities-analysis-hub-r1 (2026-07-29)

> Full status + next steps for resuming after compaction. Authoritative snapshot.
> Branch: `work/ai-advanced-capabilities-analysis-hub-r1` · PR **#694** · synced to master (0 behind at handoff).
> Owner: ralph.schroeder. Execution mode: autonomous via `task-execute` subagents, orchestrator commits per wave.

---

## 1. Where we are in one paragraph

All **code** for the Analysis platform is built (24/28 tasks, Phases 0–6) and **committed + pushed** to PR #694. The
**BFF, SpaarkeAi code page, ribbon scripts, and Matter/Project subgrids are DEPLOYED to `spaarkedev1`/Azure dev and
verified.** THEN the owner found a real **exposure gap**: the Analysis Hub was built as a *widget* but never wired as a
*surfaced workspace* — so it does NOT appear in "Manage Workspaces" or the workspace builder. Fixing that front door is
the top remaining task. A few smaller finish items + a sibling-project coordination thread remain. No hard escalations
occurred all project.

---

## 2. Tasks complete (24/28) — committed

Phase 0: **001** e2e green baseline. Phase 1: **010** schema preflight, **011** client `sprkAnalysis.ts` type, **012**
regarding resolver, **013** file SPE hop. Phase 2 (session): **020** session↔Analysis FK, **021** `POST /api/ai/analysis/fork`,
**022** durable archive (`sprk_isarchived`), **023** two-tier + `POST /api/ai/analysis/promote`, **024** add-file prompt,
**025** persistence hardening (stale→Cosmos, tab-anchor, edit-restore — recovered from a mid-run interruption + finalized).
Phase 3 (hub): **030** `AnalysisHubWidget` (3 cards + grid), **031** reopen-from-grid (in-place session switch).
Phase 4 (wizard): **040** `CreateAnalysisWizardWidget` (3-step), **041** `activeWorkType` plumbing. Phase 5 (entry):
**050** entry-matrix keystone (2a–2d host routing, opus), **051** Matter/Project **Analyses subgrid** (live+published),
**052** `openSpaarkeAi` params + `AnalysisRecordLaunch.ts` ribbon handlers, **053** ADR-039 grep-enforce.
Phase 6 (retirement, ordered): **060** repoint 4 server C# deep-links, **061** repoint client launch points, **062**
retire legacy `/continue`+`/resume`+`sprk_chathistory` read + dead entity (escalation-guarded, hand-off to 064), **063**
delete AnalysisWorkspace tree + web resources, **064** drop chathistory read + write cleanup + generalize NDA hook.

Remaining tasks: **070** BFF deploy (✅ done, see §3), **071** client/ribbon/form deploy (🔄 partial), **072** e2e/UAT,
**090** wrap-up + `/test-diet`.

## 3. Deploy state (spaarkedev1 / Azure dev)

- ✅ **BFF** deployed + verified: `fork`/`promote`/`by-analysis` = 401 (registered); `continue`/`resume` = 404 (retired);
  analysis GET = 401. Hash-verify 4/4, health OK, publish 48.11 MB.
- ✅ **SpaarkeAi code page** `sprk_spaarkeai` updated + published (hub/wizard/entry bundle confirmed present).
- ✅ **Ribbon scripts** deployed (`sprk_spaarkeai_analysisrecordlaunch` created + published).
- ✅ **Matter/Project Analyses subgrids** live + published (task 051; FormXml checked in).
- ✅ Env WR deletion: **3 of 4 deleted** (canvasapp `sprk_analysisworkspace_8bc0b`, `bundle.js`, `css`).

## 4. 🔴 TOP REMAINING WORK — the Analysis "front door" (exposure gap the owner caught)

**Root cause**: task 030 registered `analysis-hub` in `WorkspaceWidgetRegistry` (`register-workspace-widgets.ts`) — makes
it *loadable* via `widget_load`/deep-link. But the **LegalWorkspace section registry** (`src/solutions/LegalWorkspace/src/sections/*.registration.ts`)
is what drives BOTH the "Manage Workspaces" launcher AND the workspace-builder component picker AND `system-layouts.json`.
There is **no `analysisHub.registration.ts`** and **no `system-layouts.json` "Analysis" entry**. So the hub has NO
user-facing way to open in the deployed workspace (ribbon buttons also not imported yet).

**Owner intent (2026-07-29)**: a **system workspace named "Analysis"** (NOT "Analysis Hub") that loads the hub tab
(top cards → create wizard; bottom grid → open analysis). Owner is **not concerned** about adding Analysis as a
component in custom workspace-layout builder (unlikely use) — but the system-workspace still likely routes via the
section registry, so do the minimal registration needed for the system workspace to render.

**Fix (mirror Email r5 "Pattern D dual-use" exactly)**:
1. Create `src/solutions/LegalWorkspace/src/sections/analysisHub.registration.ts` (mirror `email.registration.ts`),
   sectionId `analysis` (or `analysis-hub`), rendering the `analysis-hub` direct widget. Add to `sections/index.ts`.
2. Add an entry to `scripts/system-layouts.json`: `{ "name": "Analysis", "sectionId": "<same>", "layoutTemplateId":
   "single-column", "sortOrder": <n>, "isDefault": false, "description": "..." }`.
3. Seed the `sprk_workspacelayout` row (the system-layouts seed script) so "Analysis" appears in Manage Workspaces.
4. Rebuild (`npm run build` in SpaarkeAi) + redeploy (`Deploy-SpaarkeAi.ps1`).
Reference: `email.registration.ts`, the "Email" entry in `system-layouts.json` (single-column, sectionId=email),
`notes/task-030-deviations.md`, `SPAARKEAI-DASHBOARD-AND-WIDGET-MODEL.md`.

**OPEN DECISION for owner (tab model — clarified 2026-07-29)**: Owner's intent is grid-row → **open the analysis as a
NEW workspace tab titled with the analysis `sprk_name`** (e.g. "NDA Review — ACME"). BUT what task 031 actually built is
an **IN-PLACE session switch** (no new tab; switches the already-open three-pane's Assistant session + opens the linked
file in a `document-viewer` tab titled by FILENAME). So the reopen path needs a **behavior change**: spawn a new workspace
tab (title = `sprk_analysis.sprk_name`) hosting the three-pane for that analysis, instead of the in-place session switch.
Same applies to the 2b/2d record path (currently a code-page modal). Current tab titles in code: hub tab = layout name
("Analysis"); card click = "Create Agreement Review Analysis" (`handleAgreementReviewClick` displayName); reopen = in-place
+ filename-titled doc-viewer (`handleRowOpen`, AnalysisHubWidget.tsx ~L408). **Decide: per-analysis tab named `sprk_name`
(recommended, matches owner model) vs keep in-place.** This is net-new work on the reopen/entry path.

## 5. Other remaining finish items (queue behind §4)

- **Ribbon buttons** (Matter/Project): 052 built `AnalysisRecordLaunch.openNewAnalysisFromRecord`/`openExistingAnalysis`
  + deployed the JS; the **button XML import** to `MatterRibbons`/`ProjectRibbons` (live core-entity ribbons — corruption
  risk) is NOT done. **Owner design correction**: the button should be **"Create Analysis"** opening the **card selector**
  (all cards, regarding pre-set) — NOT a hardcoded "New Agreement Analysis". This means a small tweak to
  `AnalysisRecordLaunch.ts` (drop/clean the confusing `worktype` param that only flips "new mode") + rebuild + redeploy
  ribbon scripts, THEN import buttons via `/ribbon-edit` (export MatterRibbons/ProjectRibbons → add button XML mirroring
  the existing `createproject`/`createevent` form buttons → import). SHOW the XML to owner before the live import.
- **4th web-resource delete BLOCKED**: `sprk_analysisworkspace` (HTML, GUID `ba85d6be-6413-f111-8343-7c1e520aa4df`) can't
  be deleted — referenced by the **sprk_analysis "Analysis main form"** (systemform `d408a721-77d7-f011-8406-7c1e525abd8b`,
  component type 60). Must strip the web-resource reference (embedded control or OnLoad library) from that form, then delete
  the web resource + publish. (Verify via `RetrieveDependenciesForDelete(ObjectId=...,ComponentType=61)`.)
- **Tech-debt sweep** (owner asked — "no confusion in the code"): (1) sprk_analysis form ref above; (2) `GET /api/ai/analysis/{id}`
  still returns an empty `ChatHistory` field (vestigial after 064 — remove the field); (3) **DF-04** `AnalysisContextBuilder.BuildContinuationPrompt*`
  dead after `/continue` removal — sweep; (4) DF-01 history tier badge (blocked on pre-existing `StoredSession.EntityRefs`
  write gap), DF-02 localStorage eviction cap, DF-03 keystroke re-render (all minor); (5) `sprk_analysis_commands.js`
  hand-duplicated `openSpaarkeAi` wire format (061 flagged for future sync). See `notes/deferred-followups.md`.
- **072 e2e / UAT**: walk 2a–2d, hub card→wizard→run, fork, reopen, retirement grep-clean.
- **090 wrap-up**: `/test-diet`, lessons-learned, README→Complete, TASK-INDEX all ✅.

## 6. Dataverse the OWNER must create/seed (answers "what do I need to create in Dataverse")

Already created earlier (owner): `sprk_worktype` (Choice 100000000/1/2), `sprk_regardingmatter/project/document`,
`sprk_description`, `sprk_aichatsummary.sprk_analysis` FK. Still needed:

| # | Artifact | Who | For | Status |
|---|---|---|---|---|
| 1 | **4 saved queries (views) on `sprk_analysis`**: "All Analyses" (no filter), "Agreement Review Analyses" (`sprk_worktype eq 100000000`), "Legal Research Analyses" (100000001), "Patent Application Analyses" (100000002) | owner (maker) or I can script | Hub grid + view-by-type dropdown | 🔲 needed |
| 2 | **1 `sprk_gridconfiguration` row** (`sprk_name`="Analysis Hub", `sprk_entitylogicalname`="sprk_analysis", `sprk_configjson` → savedQueryId of "All Analyses"). Then replace placeholder GUID `00000000-...` in `AnalysisHubWidget.tsx` | I can seed via Web API | Hub grid data | 🔲 needed (recipe: `notes/hub-grid-config-deployment.md`) |
| 3 | **1 `sprk_workspacelayout` seed** for the "Analysis" system workspace | I seed (after §4 registration) | Front door | 🔲 needed |
| 4 | **`sprk_analysis.sprk_subdomain`** (Choice) — agreements-r1 coordination A2 | owner (schema, HUMAN GATE) | agreement sub-domain persistence | 🔲 OPTIONAL (only if doing subDomain thread now) |

## 7. Sibling coordination — agreements-r1 (memo: `notes/COORDINATION-with-analysis-hub-r1.md`)

agreements-r1 = the review *machine*; this project = the *platform*. Status of their asks:
- **A3 activeWorkType** ✅ done. **A4 non-wizard binding** ✅ — `POST /api/ai/analysis/promote` is their classifier entry
  point (live). **A5 `sprk_analysisoutput`** ✅ stable/KEEP. **A6 KEEP surfaces** ✅ (retirement done+deployed).
- **A1 wizard sub-domain picker** ❌ not built (fast-follow; needs their C1 registry + col #4). **A2 `sprk_subdomain`** ❌
  owner-gated. **A3 subDomain envelope param** ❌ small add to `openSpaarkeAi`/`main.tsx` (mirror worktype).
- ⚠️ FLAG to them: transcript now comes from Cosmos via `GET /api/ai/chat/sessions/by-analysis/{id}`, NOT GET's empty
  `ChatHistory` field; `/export` no longer bundles chathistory.
- My assessment written in chat; consider persisting a response section into the coordination memo.

## 8. Key facts / invariants (don't relearn)

- Session GUIDs minted server-side (`ChatSessionManager.cs:108`). Cosmos = transcript store-of-record (ADR-040 Path A);
  Dataverse = anchor + `sprk_analysisoutput`. `Services/Ai/` sole-owned by `spaarke-ai-architecture-redesign-r2` (that
  worktree appears merged now) — consumed `PublicContracts`, no fork; all `/conflict-check` were clean.
- Two registries: `WorkspaceWidgetRegistry` (widget loadable) vs LegalWorkspace `sections/*.registration.ts` (surfaced
  in launcher+builder+system-layouts). Built the first, not the second — that's the §4 gap.
- Deploy scripts: `Deploy-BffApi.ps1` (pwsh), `Deploy-SpaarkeAi.ps1` (build first via `npm run build` in SpaarkeAi →
  `dist/spaarkeai.html`), `Deploy-SpaarkeAiRibbon.ps1` (dist-ribbon/*.js). Ribbon solutions `MatterRibbons`/`ProjectRibbons` exist.
- MCP dataverse reconnected; `delete_record` needs `hasUserApproved` per call. `read_query` for lookups.

## 9. Recommended next-step order

1. **§4 front door**: `analysisHub.registration.ts` + `system-layouts.json` "Analysis" entry + `sprk_workspacelayout`
   seed + Dataverse #1/#2 (views + gridconfig) → rebuild + redeploy → hub is reachable + shows real records. (Confirm name
   "Analysis" + grid-row modal-vs-in-place with owner.)
2. Fix `AnalysisRecordLaunch` worktype seam → redeploy ribbon scripts → `/ribbon-edit` import "Create Analysis" buttons
   (show XML first).
3. sprk_analysis form-reference cleanup → delete the 4th web resource → publish.
4. Tech-debt sweep (§5) + optional agreements-r1 subDomain wiring (needs owner col #4).
5. 072 e2e/UAT → 090 wrap-up + `/test-diet`.

## 10. Latest commits (branch tip context)

Deploy/merge state pushed to PR #694; last project commits were 064 (`74b725be8`→ merged) through the master-sync merge
`02760957b`. Tracking edits for 070✅/071🔄 are uncommitted local edits at handoff (commit them on resume).
