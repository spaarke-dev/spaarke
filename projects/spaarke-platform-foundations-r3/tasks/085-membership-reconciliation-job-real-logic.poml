<task id="R3-085" project="spaarke-platform-foundations-r3">
  <metadata>
    <title>`MembershipReconciliationJob` — real logic (nightly source-vs-junction recon)</title>
    <status>completed</status>
    <completion-date>2026-06-22</completion-date>
    <completion-notes>Wave 22 (2026-06-22): authored MembershipReconciliationJob (IScheduledJob) at src/server/api/Sprk.Bff.Api/Services/Ai/Membership/MembershipReconciliationJob.cs + MembershipReconciliationOptions (entity-list + cron + page-size knobs). Algorithm: discover identity-Lookup fields via IMembershipFieldDiscoveryService → scan parents paginated with NotNull-OR filter → dispatch Updated events per populated lookup (handler's idempotent Update-or-Create self-heals missing rows) → scan junction for orphans (entity-logical-name + sourceField filter; in-memory set-difference vs expected) → dispatch Removed events. Lifetime: Singleton + IServiceScopeFactory.CreateScope() per ExecuteAsync (mirrors PlaybookSchedulerJob task 023). REUSES task 084's IMembershipJunctionUpdater for the write path — no duplicated upsert logic. Independence from topic-deploy (task 071): TRUE — the job writes the junction directly via the handler; does NOT publish to the topic. Safe to ship enabled-by-default. Cron: 0 2 * * * (02:00 UTC daily, configurable). Bootstrap hosted service in MembershipModule seeds the BackgroundJobDefinition row + registers the handler with ScheduledJobRegistry before ScheduledJobHost's first tick (mirrors SchedulingModule.SchedulingBootstrapHostedService pattern). 27 unit tests pass; full BFF sweep 7557 pass / 0 fail. Publish 46.23 MB (+0.01 vs 46.22 task-084 baseline). No new HIGH CVE. Per inventory §3A / §3D / §3E, this is the load-bearing path for the 8 Q4 sprk_assigned* fields + sprk_task + sprk_opportunity (all maker-portal-only mutations).</completion-notes>
    <phase>P8</phase>
    <parallel-group>S (with 084)</parallel-group>
    <parallel-safe>true</parallel-safe>
    <rigor>FULL</rigor>
    <estimated-effort>4-6h</estimated-effort>
    <tags>bff-api,membership,scheduling,phase2</tags>
    <relevant-files>
      <file>src/server/api/Sprk.Bff.Api/Services/Ai/Membership/MembershipReconciliationJob.cs (new)</file>
      <file>tests/unit/Sprk.Bff.Api.Tests/Services/Ai/Membership/MembershipReconciliationJobTests.cs</file>
    </relevant-files>
    <dependencies>013, 070, 084</dependencies>
  </metadata>
  <prompt>Implement IScheduledJob — nightly recon that scans source-of-truth lookups across all configured entities and reconciles vs `sprk_userentityassociation` rows. Upserts missing, deletes orphans. Reports ProcessedItems per run. Catches drift from non-BFF mutations (maker portal direct edits).</prompt>
  <goal>Real recon logic — not no-op. Registered in Spaarke.Scheduling. Visible/triggerable via /api/admin/jobs. Detects intentionally-introduced drift in integration test.</goal>
  <constraints>
    <constraint source="spec FR-2P2.7">Real logic (NOT no-op — Phase 2 in-scope per owner)</constraint>
    <constraint source="NFR-08">correlationId per run</constraint>
    <constraint source="bff-extensions.md §A">BFF pre-merge checklist</constraint>
  </constraints>
  <knowledge>
    <topic>scheduled-recon-job</topic>
    <files>
      <file>.claude/patterns/dataverse/entity-operations.md</file>
      <file>src/server/shared/Spaarke.Scheduling/IScheduledJob.cs</file>
      <file>projects/spaarke-platform-foundations-r3/spec.md</file>
    </files>
  </knowledge>
  <steps>
    <step order="0">Context check + FR-2P2.7</step>
    <step order="1">Implement MembershipReconciliationJob : IScheduledJob</step>
    <step order="2">Reconcile per entity: source lookups + identity normalization → expected junction rows → diff against actual → upsert/delete</step>
    <step order="3">Report ProcessedItems = rowsTouched</step>
    <step order="4">Seed sprk_backgroundjob row with cron `0 2 * * *` (nightly 2AM UTC)</step>
    <step order="5">Unit tests: drift detection happy path + orphan removal + missing-row upsert</step>
    <step order="6">Integration test: introduce drift in junction table → run job → verify reconciled</step>
  </steps>
  <acceptance-criteria>
    <criterion testable="true">AC-1P2.6 — recon detects drift + corrects it</criterion>
    <criterion testable="true">ProcessedItems reported correctly</criterion>
    <criterion testable="true">Visible in GET /api/admin/jobs as "membership-reconciliation"</criterion>
    <criterion testable="true">Triggerable via POST /trigger</criterion>
  </acceptance-criteria>
</task>
