# Success Criteria Verification — spaarke-modal-system (task 100, 2026-08-02)

Spec §"Success Criteria" 1–10, each with concrete evidence. Overall: **8 fully met · 1 met-with-documented-deferral (SC-6) · 3 carry a manual-visual arm** consolidated into the one-time P7 visual review (SC-2, part of SC-3/SC-9). Commit range: `origin/master..7c8b4108b` + wrap-up.

| # | Criterion | Verdict | Evidence |
|---|---|---|---|
| 1 | Shell + 6 presets ship, compile React 18+19 | ✅ | `SprkModal/` family in `@spaarke/ui-components` (P0); PCF proofs: `SemanticSearchControl` `build:prod` green ×3 waves, `CommunicationConversationPanel` `build:prod` green (P5, real React 16); Code Pages: SpaarkeAi full build green ×4 waves, DocumentUploadWizard/SpeAdminApp/external-spa (React 18) green (P6) |
| 2 | Sizes fit + landscape aspect at 1280/1440/2560 | ✅ automated arm / 👁 visual arm | `sizes.test.ts` (10) verifies the scale math incl. md/lg caps; visual check at 3 widths → **one-time visual review** (`notes/oob-navigateto-inventory.md` flags + this row) |
| 3 | `--sprk-ui-scale` 1.0–1.5 scales whole modal, no clipping | ✅ automated arm / 👁 demo arm | `scaledTheme.test.ts` (7) + `uiScale.test.ts` (21) + P0.5 shell wiring (`useUiScale`); scaled-theme demo → visual review |
| 4 | ~13 dialogs carry standard window controls | ✅ (1 documented exception) | P1 wave: 13 wired/inherited/verified (`wave-p1-completion.md` table); P2–P6 re-bases formalized the interim wiring into the shell. Exception: legacy `SendEmailDialog` (in-file v1.1.59 no-X decision) — retirement-bound via #713; recorded in `defer-issues.md` Decision-pending |
| 5 | 3 hand-rolled overlays retired | ✅ | `ActionConfirmationDialog.tsx` DELETED (P2); `ConversationModal.tsx` grep: 0 `createPortal`/`position:fixed` (P5); `sprk_DocumentOperations.js` both copies grep: 0 `window.top.document` (P7). Wrap-up re-grep 2026-08-02: the only `position:` hits in SprkChat are 4 in-flow menu/popover styles, none `role="dialog"` overlays |
| 6 | `@deprecated FilePreviewDialog` + legacy `SendEmailDialog` removed | ✅ / ⏸ | `FilePreview/FilePreviewDialog.tsx`: file ABSENT (P4; sole consumer migrated; grep zero imports). Legacy `SendEmailDialog`: **deferred** — two live consumers (`pcf-safe` export [zero actual PCF imports], `DailyBriefingApp` legacy `onSend` engine flows) → **Issue #713 / DEF-002**, per §6-honest escalation rather than a breaking delete |
| 7 | All `navigateTo` via the two hubs at the OOB scale; `navigation.ts` retired | ✅ | `oobModalSizes.ts` + both hubs consume it; 89-site inventory (`notes/oob-navigateto-inventory.md`): 45 repointed, 8 page-navs intentionally-left w/ reasons, 10 outliers flagged for the visual review (preserved, not silently resized); both `navigation.ts` files ABSENT (wrap-up Test-Path: False/False), 26 callers repointed |
| 8 | Net reusable modal-component count DECREASES | ✅ | **Envelope-owner inventory** — BEFORE: 16 bespoke envelope implementations (~13 dialogs each owning Dialog chrome + 3 hand-rolled overlays). AFTER: **1 shell + 6 thin presets serve 12+ surfaces**; remaining own-envelope: 3 (WizardShell by owner design §11-G; EmailComposer wrapper + legacy SendEmailDialog deferred #713). Files DELETED: `ActionConfirmationDialog.tsx`, `FilePreviewDialog.tsx`, `navigation.ts` ×2, `showChoiceDialog` (function), + the `toolbarLaunchDefaults` duplicate-constants module and a 4-way `resolveXrmNavigation` dedup (P7). Net: 16 envelope owners → 3 (−13) |
| 9 | WCAG 2.1 AA per modal | ✅ automated arm / 👁 manual pass | `a11y.test.tsx` (focus/ESC/aria incl. real keyDown light-vs-alert) + P2 consolidation added the missing `aria-labelledby` + Fluent Dialog focus-trap/aria-modal now free on all conversions (042's a11y-parity evidence); manual keyboard pass → visual review |
| 10 | Standards doc + ADR + pattern published + cross-linked | ✅ | `docs/standards/MODAL-DESIGN-SYSTEM.md` + `.claude/adr/ADR-050` + `.claude/patterns/ui/modal-shell.md` + `MODAL-DECISION-CRITERIA.md` component-layer section + root CLAUDE.md §17 row + CHANGELOG (P0 tasks 010–013, commits `d9453713d`/`fc83496b2`) |

**Deviation summary for the owner**: SC-6's legacy-SendEmailDialog leg and SC-4's same-dialog exception ride Issue #713 (the follow-on retirement). The consolidated **one-time visual review** covers: SC-2 widths, SC-3 scale demo, SC-9 keyboard pass, the 10 flagged OOB outlier sizes, 091's three behavior deltas, 070/060's dark/transform visual passes, and 092's manual ribbon script.

---

## Task-100 Quality-Gate Results (appended 2026-08-02)

**adr-check (branch aggregate)**: 9/9 ADRs+NFRs compliant, ZERO Critical. 3 warnings, all resolved at the gate: (1) ADR-050 text-vs-code gap on BrowseModal/RecordNavigationModalShell -> Path B amendment applied to ADR-050 itself (seam-based composition now correctly described); (2) two 091 behavior deltas promoted through the two-write rule -> DEF-006 / Issue #717; (3) pre-existing inline token-color styles in both CloseProjectDialog copies -> moved to makeStyles classes.

**code-review (branch aggregate, 4 independent passes)**: ZERO Critical across 213 files / ~150 tests. All recommended fixes applied at the gate: FindSimilarDialog uiScale threading (the one re-based dialog that missed it); SendEmailDialog maxHeight re-sourced to SIZE_SPEC.md.heightMax; task-051-completion.md corrected (main-session cap edit now attributed); PinnedMemoryEditDialog stale header comment fixed + its test upgraded to assert busy disabled+spinner (the "stale dist" caveat never applied to jest); ComposeConflictDialog stale aria-labelledby claim corrected; WizardShell pre-existing 1px literal fixed; DEF-004/#715 wording corrected (wrapper duplication, NOT a shell fork - shell import is canonical). Post-fix verification: UI.Components build clean + scoped jest 340/341 (sole failure = the documented pre-existing characterize baseline); AI.Widgets memory 20/20; LW tsc 0 errors on touched files.

**Accepted/deferred review items (owner visibility)**:
- MUST BEFORE DEPLOY: the checked-in CommunicationConversationPanel Solution bundle.js predates the P5 source conversion (still contains ArrowMaximize bytes) - rebuild + repack via the PCF deployment flow before any deploy of that control. (Deploy is outside this project's scope; flagged here and in current-task.md.)
- Accepted as documented: jest.retryTimes(2) in actionConfirmationIntegration only (root-caused + logged first attempts; sibling SSE suites have not exhibited the flake - extend the MessageChannel polyfill to them only if they ever do); WizardModal preset currently has zero production consumers BY DESIGN (it is the prepared landing zone for the deferred deeper WizardShell re-base, owner 11-G light-first); 5 ribbon/webresource size literals carry citation comments (no module system; drift-watch via oobModalSizes comment convention); FindSimilarResultsStep open-file cascade has no dedicated tests (follow-up-worthy, Minor); 6 out-of-scope files reference the retired ActionConfirmationDialog name in comments only (other surfaces' doc drift); entitylist->record size mapping + the 10 flagged OOB outliers ride the one-time visual review.
