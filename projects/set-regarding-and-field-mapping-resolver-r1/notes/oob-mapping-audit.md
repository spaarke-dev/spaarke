# OOB Dataverse Mapping Audit

> **Date**: 2026-07-02 · **Environment**: spaarkedev1 (https://spaarkedev1.crm.dynamics.com)
> **Purpose**: Enumerate overlaps between active `sprk_fieldmappingprofile` records and OOB Dataverse `EntityMap` / `AttributeMap` records on the same source→target relationships.
> **Delivered by**: task SRFR-070 (FR-B6-01) in project `set-regarding-and-field-mapping-resolver-r1`.
> **Policy cross-reference**: spec `Appendix A §A.6` — OOB-mapping mutual-exclusivity anti-pattern.

**REPORT-ONLY** — no OOB `EntityMap` or `AttributeMap` records were modified. Administrator review required for any flagged collisions.

---

## Summary

| Metric | Value |
|---|---|
| Active profiles reviewed | 2 |
| Unique (source, target) pairs audited | 2 |
| OOB attribute-mappings discovered | 4 |
| Collisions (OOB field also in profile rule-set) | 0 |

### Profiles reviewed

| Profile name | Source entity | Target entity | Rule count |
|---|---|---|---|
| Matter to Event | `sprk_matter` | `sprk_event` | 0 |
| Project to Event | `sprk_project` | `sprk_event` | 1 |

---

## Overlap table

| Source entity | Target entity | OOB relationship | OOB source-field | OOB target-field | Collision | Profile |
|---|---|---|---|---|---|---|
| `sprk_matter` | `sprk_event` | `sprk_event_RegardingMatter_n1` | `sprk_matterid` | `sprk_regardingmatter` | N | Matter to Event |
| `sprk_matter` | `sprk_event` | `sprk_event_RegardingMatter_n1` | `sprk_matternumber` | `sprk_regardingmattername` | N | Matter to Event |
| `sprk_project` | `sprk_event` | `sprk_event_RegardingProject_n1` | `sprk_projectid` | `sprk_regardingproject` | N | Project to Event |
| `sprk_project` | `sprk_event` | `sprk_event_RegardingProject_n1` | `sprk_projectnumber` | `sprk_regardingprojectname` | N | Project to Event |

---

## Recommendation

**4 OOB attribute-mapping(s) present but no field collisions detected.** These OOB mappings currently target different fields than the active profile rules.

**Action**: Review each OOB mapping to confirm its intent is compatible with the Field Mapping subsystem. Any new profile rules on these fields will create a collision — re-run this audit after adding rules.

---

## Policy reference

Per `spec.md` Appendix A §A.6 (OOB Dataverse mapping mutual-exclusivity, anti-pattern):

> A source→target entity pair with an active `sprk_fieldmappingprofile` MUST NOT ALSO have overlapping OOB attribute-mappings on any 1:N relationship between them. Overlap causes ambiguity, diagnosis pain, and drift.

This audit is the enforcement surface. It is report-only by design — administrator judgment is required for each remediation.

---

*Generated by `projects/set-regarding-and-field-mapping-resolver-r1/scripts/audit-oob-mappings.ps1`. Idempotent — re-run anytime to refresh.*

