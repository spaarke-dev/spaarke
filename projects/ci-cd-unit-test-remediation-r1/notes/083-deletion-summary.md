# Task CICD-083 — Mechanical deletion of 268 scaffolding-class methods

**Date**: 2026-06-26
**Task**: CICD-083 (Phase 2.5)
**Author**: Claude Code (autonomous execution per project autonomous-mode directive)
**Source**: 082b judgment classification (`test-inventory-judgment.csv`)
**ADR**: ADR-038 §7 (scaffolding-class test deletion, binding ≥6 months from 2026-06-26)
**Spec**: FR-B07 (FULL rigor for test-modifying tasks) + FR-B06 (path-check safety)

---

## 1. Outcome

| Metric | Value |
|---|---:|
| Total DELETE-scaffolding rows classified by 082b | 268 |
| Total methods deleted by 083 | 268 |
| Whole-file deletions | 2 files (56 methods) |
| Method-level edits | 87 files (212 methods) |
| Build status (`dotnet build tests/unit/Sprk.Bff.Api.Tests/`) | ✅ PASS (0 errors, 1 pre-existing warning) |
| Baseline test attribute count | 6,685 |
| Surviving test attribute count | 6,417 |
| Net delta | -268 (-4.01%) |
| Misses (method not found / decl ambiguous) | 0 |
| Spot-check 1: `OpenAiClientTests` | 6/6 pass (was 10, 4 deleted) |
| Spot-check 2: `RecordSearchServiceTests` | 27/27 pass (was 33, 6 deleted) |
| Path-protection check (FR-B06) | 0 rows under `tests/integration/{auth,regression,data-mutation,tenant,contract}/**` |

## 2. Whole-file deletions (`git rm`)

Both files had 100% of their methods (29 / 27) classified DELETE-scaffolding by 082b — total file-level taint per ADR-038 §7 B1.

| File | Methods | Rationale (per 082b) |
|---|---:|---|
| `tests/unit/Sprk.Bff.Api.Tests/Services/Ai/Nodes/CreateNotificationNodeExecutorTests.cs` | 29 | B1 — every method is `Mock<HttpMessageHandler>` / DI-registration / pass-through wrapper |
| `tests/unit/Sprk.Bff.Api.Tests/Services/Ai/PlaybookServiceTests.cs` | 27 | B1 — every method is all-mocks Setup→Verify characterization or coverage-filler |

Subtotal: **56 methods**.

## 3. Method-level edits (top 20 files by deletion count)

Mixed files where 082b judgment identified specific scaffolding methods while preserving KEEP-behavioral methods. Generated by `c:\tmp\083-delete-methods.ps1` which:

- Located each `public ... <methodName>(` declaration via regex
- Walked backward to capture preceding `[Fact]`/`[Theory]`/`[InlineData]`/`[Trait]`/`[Fact(Skip=)]`/etc. attributes and immediately-adjacent xml-doc comments
- Walked forward through a character-by-character brace-depth scan (with string/char/comment awareness) to find the matching closing `}` or `;` for expression-bodied methods
- Removed the contiguous block + collapsed runs of 3+ blank lines

| Methods | File | Dominant pattern (per 082b) |
|---:|---|---|
| 14 | `Api/Reporting/ReportingEndpointsTests.cs` | B8 reflection-based `MethodExists_WithExpectedSignature` |
| 8 | `Api/SpeAdmin/RecycleBinTests.cs` | JE-2 reflection structural (`*_Method`, `*_ReturnsBoolTask`) |
| 8 | `Api/Reporting/ReportingEmbedServiceTests.cs` | JE-2 + B10 (`Constructor_DoesNotThrow_WithValidOptions`) |
| 7 | `Filters/AnalysisAuthorizationFilterTests.cs` | B10 coverage-fillers |
| 6 | `Api/SpeAdmin/ContainerTypeEndpointsTests.cs` | B8 + JE-2 mixed |
| 6 | `Api/Reporting/ReportingProfileManagerTests.cs` | JE-2 (`Constructor_AcceptsNullOptionalParameters` etc.) |
| 6 | `Services/Communication/InboundPipelineTests.cs` | B7 all-mocks + `Verify.Once()` characterization |
| 6 | `Filters/AiAuthorizationFilterTests.cs` | B10 coverage-fillers |
| 6 | `Integration/SpeAdmin/Phase3IntegrationTests.cs` | mixed B7 / JE-2 / B10 |
| 6 | `Services/Ai/RecordSearch/RecordSearchServiceTests.cs` | B10 |
| 6 | `Services/Ai/Chat/InvokePlaybookDescriptionTests.cs` | B10 |
| 4 | `Services/Ai/Membership/Events/MembershipEventPublisherTests.cs` | B10 |
| 4 | `Services/Ai/Chat/ToolHandlerToAIFunctionAdapterTests.cs` | JE-2 reflection |
| 4 | `Api/SpeAdmin/UpdateContainerTypeSettingsTests.cs` | B8 |
| 4 | `Services/Ai/Chat/Tools/DataverseQueryToolsTests.cs` | JE-2 reflection |
| 4 | `Services/Ai/Chat/Tools/WebSearchToolsTests.cs` | JE-2 reflection |
| 4 | `Services/Ai/OpenAiClientTests.cs` | B10 (`Constructor_With{X}_ThrowsException` lifted to UriFormatException — pure language tests) |
| 3 | `Integration/DuplicateDetectionTests.cs` | mixed |
| 3 | `Api/SpeAdmin/ContainerTypePermissionTests.cs` | JE-2 reflection |
| 3 | `Api/Workspace/WorkspaceFileEndpointsTests.cs` | B10 |

Long-tail: 67 files with 1-3 deletions each. Full row-level audit trail in `test-inventory-judgment.csv` (filter `judgment_class == 'DELETE-scaffolding'`).

Subtotal: **212 methods across 87 files**.

## 4. Build verification

```
dotnet build tests/unit/Sprk.Bff.Api.Tests/Sprk.Bff.Api.Tests.csproj
```

- Build succeeded
- 0 errors
- 1 pre-existing warning (`MemoryCompositionServiceTests.cs:215` CS8604 nullable-ref) — predates this task; not introduced by deletion

Spot-checked classes (`OpenAiClientTests`, `RecordSearchServiceTests`) compile + all surviving tests pass.

## 5. Skipped rows / judgment-error follow-ups

**None.** All 212 method-level rows resolved to a clean `public ... <methodName>(` declaration via regex; all 212 method bodies resolved cleanly via the brace-depth scanner. The script reported 0 misses on dry-run and 0 misses on execution.

No method deleted required revert. No helper-method confusion (the script only removes method blocks bounded by their own `{...}`; it does not touch class-level helpers, fields, or `Setup()` methods).

## 6. Confidence

**HIGH** for the corpus as a whole:

- Whole-file deletes (B1 file-level taint): 082b confidence rating was 100% HIGH; mechanical `git rm` is unambiguous
- Reflection-based JE-2 / B8 deletes (`MethodExists_*`, `*_ReturnsBoolTask`, etc.): these test the compiler, not behavior — unambiguous DELETE
- `Constructor_DoesNotThrow_WithValidOptions` / `Constructor_WorksWithNullLogger` (JE-2 + B10 variants): B4 + B10 pattern is canonical scaffolding
- `_DoesNotThrow` / `_Works` named tests without scenario+expected (B13 extension): violate naming convention + assert no behavior

**MEDIUM** notes (none triggered revert, but worth recording for PR review):

- B7 all-mocks-Verify-Once methods in `InboundPipelineTests.cs` (568 LOC deleted across 6 methods, ~95 LOC/method avg): these had legitimate-looking Arrange sections with detailed Graph SDK mock setup. The 082b classifier correctly identified them as B7 because (a) every collaborator is mocked, (b) the assertions are `Verify(...Times.Once)` interaction shapes rather than output behavior. The remaining InboundPipeline tests preserve the genuine behavior coverage; the deleted methods were characterization tests by Feathers' definition.
- B10 coverage-fillers in `Filters/A*AuthorizationFilterTests.cs` (7 + 6 deletions): these were `Should().NotBeNull()` / `Should().NotThrow()` shape methods sitting alongside legitimate `ProblemHttpResult` status-code assertions. The deletions removed only the no-assertion variants; the status-code methods are preserved as KEEP-behavioral.

## 7. Commit

Single atomic commit covering all 89 changed files (2 deletes + 87 method-edits). Per project autonomous-mode directive, **NOT pushed** — main session will open the PR after parallel ADR-007/009 fixes land.

```
test(b-phase2.5)(083): mechanical deletion of 268 scaffolding-class methods per ADR-038 §7 + CICD-082b judgment
```

## 8. Next tasks

- **CICD-084** — KEEP-path relocation of 254 HTTP-contract methods (different scope, NOT in this PR)
- **CICD-085** — AMBIGUOUS-row review queue (66 methods, engineer judgment)
- **CICD-085** also covers full `dotnet test` run validation (deferred from this task per task description)

---

*Generated by deterministic deletion script (`c:\tmp\083-delete-methods.ps1`). Re-running on the post-commit state would be a no-op since all targeted methods are gone. The script is preserved for audit reproducibility but not committed (transient tooling per ADR-038 §7 convention).*
