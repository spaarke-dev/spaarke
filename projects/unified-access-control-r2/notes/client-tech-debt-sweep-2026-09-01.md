# Client Tech-Debt Sweep — Dead Code, Forks, Shims, Mis-wired Paths, Stale Docs

> **Filed**: 2026-09-01 · **Project**: `unified-access-control-r2` · **Scope**: CLIENT codebase (`src/client/**`, `src/solutions/**`); server touched only where a client↔server contract is at stake.
> **Method**: symbol-grep + path-fragment-grep + `React.lazy`/dynamic-import/string-reference tracing, verified from the repo ROOT `c:\code_files\spaarke-wt-unified-access-control-r2`. **Excluded everywhere**: `**/dist/**`, `**/node_modules/**`, `**/bin/**`, `**/obj/**`, `**/out/**`, `**/.vite/**`, and PCF `**/Solution/**` compiled-bundle folders — compiled copies are artifacts, not implementations. PCF controls legally import via `@spaarke/ui-components/dist/...` deep paths; that consumption channel was checked **separately from source** (see §1.7) so dist-exclusion cannot produce false "dead" claims.
> **Status**: ANALYSIS ONLY. Nothing was deleted, moved, or fixed. Confidence discipline: **VERIFIED** = I checked the evidence stated; **LIKELY** = strong evidence, one link unproven; **UNSURE** = stated as such with what would settle it. "I could not find a caller" is always distinguished from "there is no caller".
>
> **Coordination**: the four LegalWorkspace wizard chains (`CreateProject`/`CreateMatter`/`CreateEvent`/`FindSimilar`) and `SummarizeFilesDialog.tsx` are being handled by the main session RIGHT NOW (deletion was already in progress during this sweep — `components/CreateEvent/` and `components/FindSimilar/` were gone by the time the docs sub-sweep ran). Those items appear below only where this sweep adds NEW information (transitive dependents, a fifth chain, twin-drift data). The full wizard-pair story is in [`create-wizard-duplication-analysis.md`](create-wizard-duplication-analysis.md) and is not repeated.

---

## 1. DEAD CODE — zero live importers

Proof standard per row: symbol-name grep + path-fragment grep + `React.lazy`/`import(`/string-reference grep across `src/` from repo root, plus a check of every barrel that re-exports the item (a barrel re-export is NOT a consumer — the barrel entry's own importers were traced).

| # | Path | What it is | Evidence of zero use | Deployed-artifact risk | Confidence | Disposition |
|---|---|---|---|---|---|---|
| 1.1 | `src/client/shared/Spaarke.UI.Components/src/components/FindSimilar/**` (FindSimilarDialog.tsx, FindSimilarResultsStep.tsx ~600 lines, findSimilarService.ts, findSimilarTypes.ts, index.ts) | The **shared-lib Find Similar wizard family** (distinct from the live `FindSimilarViewer` iframe viewer, renamed in #714) | NOT exported by the main components barrel (`components/index.ts` — only `./FindSimilarViewer` is) and NOT in `pcf-safe.ts` exports. Repo-wide grep for `FindSimilarDialog`, `runFindSimilar`, `findSimilarTypes`, `IFindSimilarServiceConfig`, `FindSimilarResultsStep`: only importer outside the folder was the LegalWorkspace adapter `src/solutions/LegalWorkspace/src/components/FindSimilar/**` — itself a zero-importer dead chain that the main session is deleting (and had already deleted mid-sweep). `src/solutions/FindSimilarCodePage/src/App.tsx` was READ in full: it is a standalone implementation importing only `OOB_MODAL_SIZES` from the shared lib | None — never exported to `pcf-safe`, so no PCF dist import can exist; nothing compiles it into any bundle once the LW adapter is gone | **VERIFIED** (in-repo) | **delete** — must go in the SAME sweep as the LW chains, or it becomes the next 6-month orphan (the exact pattern being remediated) |
| 1.2 | `src/solutions/LegalWorkspace/src/components/Playbook/**` (PlaybookCardGrid, ScopeConfigurator, ScopeList, DocumentUploadStep, FollowUpActionsStep, analysisService.ts, playbookService.ts, types.ts, index.ts) | A **FIFTH dead LegalWorkspace chain** — NOT in the main session's four-chain deletion list | Symbol grep (`PlaybookCardGrid|playbookService|analysisService|ScopeConfigurator|ScopeList|DocumentUploadStep|FollowUpActionsStep`) across ALL of `src/solutions/LegalWorkspace/src`: hits only inside the folder + its own `index.ts`. Path-form grep (`from '../Playbook`, `./components/Playbook`): zero importers. The `@spaarke/legal-workspace` package boundary re-exports ONLY the LW public barrel (`Spaarke.LegalWorkspace/src/index.ts:88` → LW `src/index.ts`, which curates App/renderer/section-registry — no Playbook), so no outside consumer can reach it either. The folder's own files import from the SHARED `@spaarke/ui-components/components/Playbook` (`analysisService.ts:15`), i.e. it is a stale adapter layer over the live shared Playbook | None — unimported code is not in any built bundle | **VERIFIED** | **delete** — flag to the main session to widen its sweep |
| 1.3 | `src/client/shared/Spaarke.UI.Components/src/services/SprkChatBridge.ts` (+ `services/__tests__/SprkChatBridge.test.ts`) | `@deprecated` BroadcastChannel chat bridge ("Use AnalysisAiContext streaming callbacks instead") | Repo-wide grep: only importers are its own unit test and the also-deprecated `__test-harness__/StreamingWriteHarness.tsx`. Yet it is STILL exported on the public services barrel (`services/index.ts:153` + its 12 types at `:222-235`) — a dead API kept discoverable | None (not in pcf-safe; no dist deep-import found) | **VERIFIED** | **delete** service + test + harness + barrel lines (test qualifies for the ADR-038 test-diet SCAFFOLDING class) |
| 1.4 | `src/client/shared/Spaarke.UI.Components/src/__test-harness__/StreamingWriteHarness.tsx` | `@deprecated` manual E2E harness, "Superseded by streaming-e2e.test.ts (task R2-071). Retained for manual debugging only" | Sole consumer of SprkChatBridge; nothing imports the harness | None | **VERIFIED** | delete with 1.3, or leave-with-reason if manual-debug value is still claimed — but it is the only thing keeping 1.3 alive |
| 1.5 | `src/client/pcf/VisualHost/control/configurations/matterMainCards.ts` + `matterReportCardTrends.ts` | `@deprecated v1.2.33` static chart configs ("replaced by Dataverse chart definition records") | Grep for `configurations/`, `matterMainCards`, `matterReportCardTrends` across `src/client/pcf/VisualHost/control`: zero importers (re-verified repo-wide; only a comment in `Spaarke.Visuals/src/types/index.ts:131` mentions the filename) | None — unimported code is tree-shaken/not bundled | **VERIFIED** | **delete** |
| 1.6 | `src/client/shared/Spaarke.Visuals/src/components/GradeMetricCard.tsx` | `@deprecated v1.2.33 — Use MetricCardMatrix` | Repo-wide symbol grep: only consumers were the dead configs in 1.5 + its own barrel export (`Spaarke.Visuals/src/components/index.ts:26`) + comments | None found in repo; **INFERRED none deployed** (its only import chain is dead, so no live bundle contains it) | **VERIFIED** | **delete** + barrel line |
| 1.7 | `src/client/shared/Spaarke.UI.Components/src/hooks/`: `useDatasetMode.ts`, `useHeadlessMode.ts`, `useVirtualization.ts`, `useEntityTypeConfig.ts`, `useDirtyFields.ts`, `useOptimisticSave.ts`, `useWriteMode.ts` (+ their `__tests__`) | The **DatasetGrid-era hook family**, orphaned when the legacy grid views were retired in Phase F (task 051) | Repo-wide symbol grep (incl. `resolveAutoMode`, `WRITE_MODE_COMMANDS`): only own files, own tests, `hooks/index.ts` barrel, an odd re-export from `types/index.ts:40-41`, and a stale comment in CalendarWorkspaceWidget. The PCF dist-import channel was checked separately: every `from '@spaarke/ui-components/dist/...'` in `src/client/pcf/**` source was enumerated — the only hooks consumed are `useRecordFieldValues`, `useRecordHeaderToolbarActions`, `useRecordHeaderFields`, `toolbarLaunchDefaults`, `useAiSummary` (pcf-safe). Siblings that ARE live and must be kept: `useKeyboardShortcuts` (PageChrome/CommandBar.tsx:39), `useAiPrefill` (4 wizard steps + BFF NFR-07 binding), `useForceSimulation` (DocumentRelationshipViewer code page DocumentGraph.tsx:26 — and its ESM `d3-force` transitively taxes 5 solutions with jest stubs) | None | **VERIFIED** per hook | **delete** the 7 + tests (test-diet class); also remove `Spaarke.UI.Components/jest.config.js:11-12` coverage entries and the `types/index.ts:40-41` hook re-exports |
| 1.8 | `src/client/code-pages/DocumentRelationshipViewer/src/components/RelationshipNetwork.tsx` (760 lines) + `RelationshipTimeline.tsx` (688 lines) | Two full SVG visualizations whose imports are **commented out** in `App.tsx:47-48` | Grep across the code page: no other importer; the live graph is `DocumentGraph.tsx` | None (not compiled into the page bundle while unimported) | **VERIFIED** | **delete** (~1,450 lines), or consciously reinstate — a commented-out import is not a preservation strategy |
| 1.9 | `src/client/shared/Spaarke.UI.Components/src/components/WizardRegistry/PlaceholderWizard.tsx` | "Not yet available" bridge wizard from task 011 | Zero importers since tasks 030/040 replaced its registry entries; retention is explicitly documented in `wizardRegistry.ts:109-119` ("left in place for any future entry") | None | **VERIFIED** | **leave-with-reason** (documented) — or delete; it is 50 lines |
| 1.10 | Client feature-flag surface: `getFeatureFlag()` + the `featureFlags` map in `src/solutions/LegalWorkspace/src/config/publicConfig.ts:80` AND `src/solutions/SpaarkeAi/src/config/publicConfig.ts:100` | Flag plumbing (fetch → cache → getter), duplicated in two solutions | Repo-wide grep for `getFeatureFlag(` call sites: ZERO consumers — only the two definitions and their own doc comments. The surrounding `publicConfig` fetch is live (bffUrl/msalClientId/tenantId), only the flag surface is unused. Server side (`ConfigEndpoints.cs`) faithfully serves a map nobody reads | The map is served over the wire — harmless | **VERIFIED** | **document** (or delete the getter until a first flag exists); see also fork item 2.6 |
| 1.11 | `src/client/pcf/tsconfig.json:22` — `"UniversalDatasetGrid/**"` exclude entry | tsconfig exclusion for a folder that no longer exists (control deleted) | Glob: `src/client/pcf/UniversalDatasetGrid` absent (verified from repo root; 20 real controls enumerated) | n/a | **VERIFIED** | **fix** (remove the line) |

**Additional dead/zombie findings from the outer-areas census** (code-pages, external-spa, office-addins, webresources, other shared libs): pending sub-sweep completion at filing time — see §6 addendum at the end of this file; the addendum is part of this inventory.

---

## 2. DUPLICATE / FORKED COMPONENTS

| # | Pair | Which is live | In sync? / drift direction | Confidence | Disposition |
|---|---|---|---|---|---|
| 2.1 | LW `CreateProject`/`CreateMatter`/`CreateEvent`/`FindSimilar` vs shared-lib wizard chains | Shared is live; LW dead | Covered exhaustively in [`create-wizard-duplication-analysis.md`](create-wizard-duplication-analysis.md); deletion in flight. NOT re-analyzed here | — | in progress (main session) |
| 2.2 | `src/solutions/LegalWorkspace/src/components/CreateProject/CloseProjectDialog.tsx` + `closureService.ts` ⇄ `src/client/shared/Spaarke.UI.Components/src/components/CreateProjectWizard/CloseProjectDialog.tsx` + `closureService.ts` | **LW copy is the mounted one** (`Shell/WorkspaceGrid.tsx:55-56` `React.lazy` + render at `:1034`). The **shared twin has ZERO external importers**: grep for `CloseProjectDialog|closureService|closeSecureProject` across `src/client` (excl. the folder itself) and across `src/solutions` (excl. LW) → no hits; it is only exported from the `CreateProjectWizard` barrel (`index.ts:34-49`) that nothing consumes for these symbols | LW copy self-documents "**kept in lockstep with the shared-lib copy**" (`CloseProjectDialog.tsx:44`) — a MANUAL SYNC arrangement. That comment is **debt in its own right**: one forgotten edit = silent divergence in secure-project **closure** semantics (access revocation, container disposition — squarely this project's subject matter). Line-level drift not diffed (shell too slow in this environment); the two files are within a few lines of each other by structure (:282 vs :284 component decl) | **VERIFIED** (liveness + zero-importer twin + lockstep comment); drift extent **UNSURE** — settle with `git diff --no-index` of the two pairs on a responsive machine | **consolidate**: keep exactly one copy (shared, given LW's library future), delete the twin, delete the lockstep comment. ⚠️ See §4.6 first — the "live" copy's own trigger has no in-repo caller |
| 2.3 | `src/solutions/SmartTodo/src/services/{DataverseService,queryHelpers}.ts` (+ `hooks/useUserPreferences.ts`, `types/entities.ts`) ⇄ `src/solutions/LegalWorkspace/src/services/{DataverseService,queryHelpers}.ts` (+ same siblings) | **BOTH live** (LW via the SpaarkeAi dashboard through `@spaarke/legal-workspace`; SmartTodo as its own code page) | **Forked and drifting in both directions.** Each file's header claims to be "the **single** data access layer" — both can't be. Concrete drift: LW imports `DOCUMENT_TAB_SELECT_FIELDS` + `buildOwnerFilter` from its queryHelpers (SmartTodo's doesn't); SmartTodo imports `ITodoRegardingFilter` and carries newer notes (DEF-11 Part 2 2026-07-04; "task 021's Filter-pane predicate import — REMOVED smart-todo-r5 UAT 2026-08-17") absent from LW's copy. Same `sprk_todo` statuscode map today (Open:1/Completed:2/Dismissed:659490002) — agreement by luck, not mechanism. NO lockstep comment on either side; neither references the other | **VERIFIED** existence + both-live + directional drift markers; full behavioural diff **LIKELY** larger (line offsets diverge ~13 lines by mid-file) | **consolidate** — this is the same disease as the wizard fork but with BOTH copies alive, i.e. worse: query-semantics drift shows two surfaces different rows for the same entity with no error anywhere. Candidate home: `@spaarke/smart-todo-components` (which already exists to share this domain) |
| 2.4 | `components/EmailStep/SendEmailStep.tsx` (legacy generic) ⇄ `components/EmailComposer/wrappers/SendEmailStep.tsx` (canonical) — both in Spaarke.UI.Components | **BOTH live.** EmailComposer's barrel claims it "Replaces the 6 ad-hoc client send-email implementations", but the legacy step is still consumed by `DocumentEmailWizard.tsx:43` and by `WizardFollowOns/steps/SendEmailFollowOnStep.tsx:29` — which is itself mounted by SIX hosts (CreateRecordWizard:516, WorkAssignmentWizardDialog:383, CreateReportCardWizard:351, CreateInvoiceWizard:352, SummarizeFilesDialog:386, ai-widgets CreateAnalysisWizardWidget:559) | Two components named `SendEmailStep`, BOTH exporting an interface named `ISendEmailStepProps` with **different shapes** (legacy: controlled to/subject/body + onSearchUsers; canonical: composer-handle forwarding). Only the canonical one is on the main barrel; the legacy survives via relative imports — an import-shape trap for the next maintainer | **VERIFIED** | **consolidate**: migrate SendEmailFollowOnStep + DocumentEmailWizard onto the EmailComposer wrapper, then delete `components/EmailStep/` (incl. its private LookupField copy, 2.5) |
| 2.5 | `LookupField` ×4 inside the same shared lib: `components/LookupField/LookupField.tsx` (canonical, barrel-exported) · `components/EmailStep/LookupField.tsx` (**independent full re-implementation** with its OWN `ILookupItem` interface duplicating `types/LookupTypes.ts`) · `components/RecordHeader/fields/LookupField.tsx` (different purpose — form field renderer; name collision only) · `components/CreateMatterWizard/LookupField.tsx` (thin documented wrapper adding AiFieldTag — legitimate) | Canonical + RecordHeader + wrapper live; EmailStep copy live only through the legacy EmailStep (2.4) | EmailStep copy predates the canonical extraction and never got retired; duplicate `ILookupItem` definitions can drift against `types/LookupTypes.ts` (which pcf-safe + MatterHeader PCF consume) | **VERIFIED** | **consolidate** with 2.4 (deleting EmailStep kills the copy); RecordHeader name collision: **document** (rename would churn PCFs) |
| 2.6 | `src/solutions/LegalWorkspace/src/config/publicConfig.ts` ⇄ `src/solutions/SpaarkeAi/src/config/publicConfig.ts` | Both live | Near-identical twins (same fetch/cache/ETag/getter design, cosmetic divergence already visible: line offsets, log prefixes). Glob confirms exactly these two copies exist | **VERIFIED** | **consolidate** into a shared-lib helper; until then they are silent-divergence candidates |
| 2.7 | `DataGrid/filterChips/chipDiscovery.ts:255` — `humanizeLogicalName` "Duplicated from `configResolution.ts#humanizeLogicalName`" | Both live | Self-documented intra-package duplication | **VERIFIED** | fix (import instead) — trivial |
| 2.8 | CalendarWorkspaceWidget dedicated grid-config record vs shared "Event Default" record (`CalendarWorkspaceWidget.tsx:134-145`) | Both live **by design** | This is a DATA twin (two `sprk_gridconfiguration` rows), intentionally split so workspace view-curation can't affect drill-through; fully documented at the constant | **VERIFIED** | **leave-with-reason** (documented); the accepted residual risk is behavioural drift between the two records' command/chip config — a Dataverse-data concern, not code |
| 2.9 | Cross-language manual-sync mirrors with client copies (each self-flagged "keep in sync", i.e. one forgotten edit from divergence): `src/client/webresources/js/sprk_todo_score_onchange.js` = LITERAL MIRROR of `todoScoreMappings.ts` (scoring semantics!) · `src/solutions/DocumentUploadWizard/sprk_subgrid_commands.js:511` literal ↔ module constant · `src/dataverse/forms/sprk_matter/communicationsGridOnLoad.{ts,js}` ↔ server `RegardingFieldMap.cs` (AND the checked-in `.js` is a compiled copy of the `.ts` — a second sync axis) · `src/solutions/CreateTodoWizard/src/main.tsx:22` ↔ `src/solutions/LegalWorkspace/src/sections/todo.registration.ts:79` (BroadcastChannel names `sprk_todo:lifecycle`/`sprk_todo:created` — if these drift, created To Dos silently stop appearing in the workspace list) · `Spaarke.Notifications/README.md:128` wire-contract types "kept in sync by hand" · `Spaarke.SmartTodo.Components/components/Header/Header.tsx:130` "Kept in sync manually" | — | Every one is a defect-in-waiting, not a mitigation. The BroadcastChannel pair and the score-mapping mirror have USER-VISIBLE silent-failure modes | **VERIFIED** (comments + both sides located) | **document** as a registry (this table) + prioritize extracting the BroadcastChannel constants to `@spaarke/smart-todo-components` and generating the webresource mirror from the TS source |

---

## 3. SHIMS, ADAPTERS, HANGING CHADS

| # | Item | Detail | Confidence | Disposition |
|---|---|---|---|---|
| 3.1 | LW `components/Wizard/**` (5 files) + LW `components/FindSimilar/findSimilarService.ts` + LW `components/CreateMatter/WizardStepper.tsx` | Pure re-export shims over `@spaarke/ui-components` left behind by the 2026-03-19 refactor so the (dead) chains kept compiling. Consumers: ONLY the dead chains. The moment the chains are deleted these shims are zero-importer | **VERIFIED** | **delete** with the chains (tell main session to include them) |
| 3.2 | Cross-package DEEP RELATIVE imports into shared-lib SOURCE: LW dialogs `../../../../../client/shared/Spaarke.UI.Components/src/components/Wizard/wizardShellTypes` (EventWizardDialog.tsx:17, ProjectWizardDialog.tsx:27, WizardDialog.tsx:24 — all in dead chains, dying) · `Spaarke.Events.Components/.../CalendarWorkspaceWidget.tsx:127-129` imports `../../../../Spaarke.UI.Components/src/...` (LIVE, but rationale documented per task 033b: avoids pulling PCF-framework types into the package's tsc) | The Events.Components case is a deliberate, documented boundary bypass; the LW cases are refactor debris | **VERIFIED** | LW ones: delete with chains. Events.Components: **leave-with-reason**; revisit if the lib ever gets a pcf-free sub-barrel |
| 3.3 | `pcf-safe.ts:8` header usage example: `import { FindSimilarDialog } from '@spaarke/ui-components/src/pcf-safe'` | `pcf-safe.ts` does NOT export FindSimilarDialog (and never could post-#714 rename) — the file's own instruction misleads the next PCF author | **VERIFIED** | **fix** comment |
| 3.4 | `components/index.ts:205-217` SendEmailDialog note | Self-contradictory vintage stack: says the legacy `components/SendEmailDialog` "is DELETED" (#713) AND "the legacy component file is preserved… until task 060 (W6) migrates FilePreviewDialog off the old `onSend` API". The folder does not exist (verified). Also stale: FilePreviewDialog itself was deleted (the migration completed) | **VERIFIED** | **fix** comment (collapse to the deleted-state truth) |
| 3.5 | `components/index.ts:69-72` LinearRunProgress note | Says the component is "paired with the `useLinearRunProgress` hook" — the hook was deleted in task 045 (`hooks/index.ts:13-16` records it; only its TYPES remain) | **VERIFIED** | **fix** comment |
| 3.6 | `wizardRegistry.ts:180` resolution-order comment | Still documents `sprk_kpiassessment` → `kpi-assessment`, contradicting the RENAME NOTE 40 lines above and the actual `ENTITY_TO_WIZARD_KEY` map (`report-card`) | **VERIFIED** | **fix** comment |
| 3.7 | `types/index.ts:40-41` exporting `useDatasetMode`/`useVirtualization` (hooks) from the TYPES barrel | Layering chad from the DatasetGrid era; both hooks are dead (1.7) | **VERIFIED** | delete with 1.7 |
| 3.8 | TODO/HACK with real consequences (curated; trivial ones ignored): (a) `SpeAdminApp ContainersPage.tsx:1029-1070` — see §4.1, the TODO marks a fake operation; (b) `SpaarkeAi useCommandRouting.ts:185-187` `tenantId: ""` "TODO(task 084)" — reference-resolver caching disabled (documented degraded mode; resolution still works); (c) `SpaarkeAi AssistantToolMenu.tsx:91,145` "TODO(042)" — a visible menu item wired to a placeholder until the My Assistant questionnaire lands (inert affordance risk; its test suite pins the placeholder, so it is at least deliberate); (d) `WorkspaceLayoutWizard App.tsx:263,733` + `SpaarkeAi pinnedWorkspaces.ts:17,36` "TODO(BFF migration)" — user prefs/pins are localStorage-only: silently NOT synced across devices/browsers (user-visible under-delivery, documented); (e) `chipDiscovery.ts:114` TODO(R2) metadata-derived lookup targets — grid filter chips limited to hardcoded targets; (f) `EventCommands sprk_event_ribbon_commands.js:348` OOB confirm dialog TODO | (b)-(f) each degrade gracefully and say so; none hides a wrong result the way (a) does | **VERIFIED** (each read in context) | (a) → §4.1 fix; (b)-(f) **leave-with-reason** / backlog |
| 3.9 | Deprecated-but-retained prop/interface members nothing reads (cat: "optional props never passed" / no-op props): `ConversationView.types.ts:100` "Mark as read" prop is a **no-op since R3 UAT 2026-07-23** · `CommunicationsWorkspaceWidget.tsx:139` prop "No longer used by this widget's body. Retained ONLY so…" · `DigestHeader.tsx:92` subtitle prop no longer rendered · `dailyBriefing.registration.ts:132-150` FOUR deprecated-and-ignored registration options · `EmailProcessingDashboard.tsx:154` auth prop "Kept for API compatibility" · `SemanticSearchControl types/props.ts:30-32` two toolbar props "Kept optional for backward compat" · `DataGridConfiguration.ts:230-232` two schema fields "ignored by defaultRecordOpen" · `useAiSummary.ts:147,569` deprecated callback + removed background-analysis surface · `external-spa/config.ts:119` deprecated alias · `ContainerTypeSettingsForm.tsx:62` deprecated alias · Compose `AgreementReviewSummaryPanel.tsx:455,678,686` deprecated risk-banner prop + two aliases · `HistoryOverlay.tsx:263,1477` renamed-alias pair | All are DOCUMENTED tombstones (good hygiene individually), but collectively they are the lib's accreting "prop necropolis" — each optional prop a future caller can pass and silently get nothing | **VERIFIED** (each tag read) | **document** now; schedule a breaking-change prop-diet at the next major version of each package |
| 3.10 | `Spaarke.UI.Components/jest.config.js:11-12` | Coverage-ignore entries for `useVirtualization.ts`/`useKeyboardShortcuts.ts` — half points at soon-dead code (1.7), and ignoring a LIVE hook's coverage (useKeyboardShortcuts) is its own small smell | **VERIFIED** | fix with 1.7 |
| 3.11 | `wizardLaunchers.ts:231` (shared lib) | Launcher exported with **no production caller** (finding inherited from `create-wizard-duplication-analysis.md` §B.1 row 1, re-confirmed there) | VERIFIED (by that analysis) | document / fold into wizard-consolidation follow-up |
| 3.12 | `src/client/pcf/CLAUDE.md` build guidance | Module doc says "Build for production: `npm run build -- --mode production`" and demonstrates `pac pcf push`, while ROOT CLAUDE.md §12 mandates `npm run build:prod` (AP-1 failure mode explicitly documents that `npm run build` is the wrong command for PCF prod) and the deployment section itself says NOT to use `pac pcf push` for releases. An agent loading only the module file gets the banned command | **VERIFIED** (both files read; root file auto-loads, module file quoted above) | **fix** (cannot be done by sub-agents if under `.claude/`; this one is under `src/`, so it IS editable — but per sweep rules, reporting only) |

---

## 4. MISDIRECTED / MIS-WIRED PATHS — runs, but reaches the wrong destination or silently under-delivers

| # | Item | What actually happens | What the user sees | Confidence | Disposition |
|---|---|---|---|---|---|
| 4.1 | **SpeAdmin container "Delete" is a stage prop** — `src/solutions/SpeAdminApp/src/components/containers/ContainersPage.tsx:1036-1070` | `handleDelete` shows `window.confirm("Delete N containers? They will be moved to the Recycle Bin and can be restored within 93 days.")`, then — per its own comments (:1049-1055) — makes **NO server call at all**: it optimistically removes rows from local state and prints "N containers deleted (moved to Recycle Bin)." The containers, and every client document in them, remain fully live; they reappear on next load/refresh | An admin believes SharePoint Embedded containers (document stores) were destroyed with a 93-day recovery window. **Nothing was deleted.** For retention/DSAR/offboarding actions this is a false compliance signal; the admin has no error, no warning | **VERIFIED** (code + comments) | **fix**: either disable the button until `DELETE /api/spe/containers/{id}` exists, or wire it. Route-agreement sub-sweep is confirming the server side; `SpeAdminClientRouteAgreementTests` should then pin it |
| 4.2 | **Follow-on To Dos silently skip the Field Mapping Framework** — `WizardFollowOns/steps/AddTodoFollowOnStep.tsx:125-136` | `createTodoRegardingChild(dataService, …)` constructs `new TodoService(dataService)` with NO `authenticatedFetch`/`bffBaseUrl`. `todoService.ts:79-91` documents the consequence: "when omitted, the engine call is skipped (graceful no-op — same as 'no profile configured')" — so no field-mapping warning is even recorded. The narrow function signature makes the degradation STRUCTURAL: hosts that demonstrably possess both deps (e.g. `src/solutions/CreateInvoiceWizard/src/main.tsx:28-29` passes them into the wizard) cannot thread them through. Live degraded callers: `CreateInvoiceWizard.tsx:550`, `CreateReportCardWizard.tsx:542`, `Spaarke.AI.Widgets/CreateAnalysisWizardWidget.tsx:916`. Worse: that widget's OWN comment (`:36`) claims `createTodoRegardingChild` is "**already Field-Mapping-driven internally**" — the code proves the opposite | A To Do created via the "Add To Do" follow-on card is real but silently lacks every admin-configured mapping profile (inherited assigned resources, defaults, copies) that the SAME To Do created through the Create To Do wizard gets (`TodoWizardDialog.tsx:244` passes all three deps). No warning anywhere; data quality quietly forks by entry path — the exact `SummarizeFilesDialog` shape | **VERIFIED** end-to-end | **fix**: add optional `authenticatedFetch`/`bffBaseUrl` params to `createTodoRegardingChild` and thread from all three hosts; correct the false comment |
| 4.3 | **Inconsistent degrade wiring for follow-on WorkAssignmentService** — `CreateProjectWizard.tsx:605`, `CreateInvoiceWizard.tsx:529`, `CreateReportCardWizard.tsx:521` | These hosts construct `new WorkAssignmentService(dataService, authFetch ?? fetch.bind(window), bffBaseUrl ?? '')` — when authFetch is absent this doesn't SKIP the BFF field-mapping call (the service's documented no-op path); it ATTEMPTS it **unauthenticated against base URL `''`** (relative to the host origin). Contrast `CreateMatterWizard.tsx:551` which passes the real deps with no fallback. Three degrade behaviours now coexist in one wizard family: skip (4.2), fake-fetch (this row), full wiring | Today every known host passes real deps, so the fallback is dormant; the moment a new host omits them the follow-on path emits failing unauthenticated requests (401/404 surfaced only as a mapping warning at best) instead of the designed graceful skip | **VERIFIED** (wiring); consequence **LIKELY** (dormant) | **fix**: drop the `?? fetch.bind(window)` fallbacks — pass `undefined` through and let the service's documented skip path run |
| 4.4 | **`sprk_registrationribbon.js` is environment-pinned to DEV** — `src/client/webresources/js/sprk_registrationribbon.js:26-45` | `SPRK_REG_BFF_API_URL = "https://spe-api-dev-67e2xz.azurewebsites.net"` (with the TODO admitting it), plus hardcoded dev MSAL clientId/bffAppId/tenantId and `redirectUri: "https://spaarkedev1.crm.dynamics.com"`. Approve/Reject Demo Access — an ACCESS-GRANTING action — will call the dev BFF (or fail MSAL) from any other environment this webresource is deployed to | In a non-dev org, approving a registration either errors or, worse, provisions against the wrong environment. Whether a prod deployment exists is **INFERRED-unknowable from the repo** | **VERIFIED** (code) / **INFERRED** (deployment exposure) | **fix**: environment-variable-driven config like the other webresources; audit deployed copies |
| 4.5 | Assistant reference-resolver runs tenant-less — `SpaarkeAi/useCommandRouting.ts:185-188` | `tenantId: ""` documented: "empty string turns OFF the resolver's caching (degraded) but resolution works" | Slower reference resolution; correctness preserved. Included here because it is the same "host wires fewer collaborators" shape at low severity | **VERIFIED** | leave-with-reason / task-084 backlog |
| 4.6 | **The live CloseProjectDialog may be unreachable** — `LW Shell/WorkspaceGrid.tsx:434-455, 1030-1039` | The ONLY open-trigger for the mounted (LW) CloseProjectDialog is the window global `window.__SPAARKE_OPEN_CLOSE_PROJECT__` (registered at `:450`; the JSDoc at `:447`/`:1030` says "Usage: window.__SPAARKE_OPEN_CLOSE_PROJECT__(projectId, …)"). Repo-wide grep (all of `src/` incl. webresources, solutions, dataverse forms — AND `infrastructure/**`, which holds the ribbon deploy copies; also case-insensitive `closeproject`/`close_project` sweeps of both): **ZERO callers** of that global. I could not find a caller — which is not the same as "there is no caller": an org-side Ribbon-Workbench customization never round-tripped into the repo, or a console-driven admin flow, could still invoke it | If no out-of-repo caller exists, the entire Close-Secure-Project UX (grant revocation, container disposition — uac-r2 task 016's cascade subject) is dead UI: mounted, tested by humans never, launchable by nothing | **VERIFIED** zero in-repo callers; overall **UNSURE** — settle by (a) searching deployed ribbon/webresource customizations in the target org for `__SPAARKE_OPEN_CLOSE_PROJECT__`, (b) asking the owner where Close Project is supposed to be launched from | escalate to owner alongside 2.2 consolidation |
| 4.7 | Contextual note (already in-flight, not re-reported): `SummarizeFilesDialog` constructs `new ProjectService(dataService)` without authFetch/bffBaseUrl so its create-project follow-on renders a Secure toggle that never provisions — the template for 4.2/4.3. Main session is fixing it | — | — | (owner) | in progress |

---

## 5. STALE DOCUMENTATION that would mislead an implementer

All rows below were VERIFIED doc-against-code by the dedicated docs sub-sweep (its ground-truth section is preserved here). The two already-known items (`WORKSPACE-ENTITY-CREATION-GUIDE.md:353-356`, `DATA-ACCESS-DECISION-CRITERIA.md:139-145`) are excluded per instructions. `.claude/**` rows CANNOT be fixed by sub-agents (write boundary) — the main session must apply them.

**Ground truth**: `src/client/pcf/` = exactly 20 controls; NO `UniversalDatasetGrid/`, `AssociationResolver/`, `UniversalQuickCreate/`, `AIMetadataExtractor/`, `SpeFileViewer/`. `components/DatasetGrid/` = `ViewSelector.tsx` only. No `components/SendEmailDialog/`, no `components/FindSimilarDialog/`, no simple `FilePreviewDialog`. `StatusBadge` does not exist anywhere in the shared lib.

| # | Doc:line | Stale claim | Truth | Severity |
|---|---|---|---|---|
| 5.1 | `.claude/constraints/pcf.md:70` | Binding "✅ MUST implement/extend `src/client/pcf/UniversalDatasetGrid/`" | Path does not exist; framework is `Spaarke.UI.Components/src/components/DataGrid/` | **would-mislead-agent (binding MUST → dead path)** |
| 5.2 | `.claude/constraints/pcf.md:180,188-190` | Tree lists `UniversalDatasetGrid/`; shared-lib tree shows `components/{layout,data,feedback}/` subfolders | Neither exists; `components/` is flat | would-mislead-agent |
| 5.3 | `.claude/patterns/pcf/control-initialization.md:11` · `error-handling.md:11-12` · `theme-management.md:11-12` · `fluent-v9-modern-theming.md:14` · `fluent-v9-canvas-vs-mda-disabled.md:12` · `.claude/patterns/ui/fluent-v9-theming.md:12` | SIX pointer files whose canonical exemplar is a `UniversalDatasetGrid` path | None exist. Surviving equivalents: `VisualHost/control/providers/ThemeProvider.ts`, etc. Pointer files ARE their pointer — these are 100% payload-dead | **would-mislead-agent** (pattern layer is loaded as ground truth per root CLAUDE.md §2) |
| 5.4 | `.claude/patterns/pcf/pcf-build-scaffold.md:213` | References `AssociationResolver/handlers/FieldMappingHandler.ts:440` | AssociationResolver PCF retired/deleted (SRFR-045); `src/client/pcf/CLAUDE.md:16` itself says "Do not reference" | would-mislead-agent |
| 5.5 | `.claude/adr/ADR-012-shared-components.md:151,178,209,213,216` | PCF-safe table lists `FindSimilarDialog`; deep-import recipe `from "@spaarke/ui-components/dist/components/FindSimilarDialog"`; catalog rows `DatasetGrid (Grid/Card/List/Virtualized)`, `SendEmailDialog` | The deep-import path cannot resolve (viewer = `dist/components/FindSimilarViewer` post-#714); grid views retired; SendEmailDialog moved under `EmailComposer/wrappers/` with a different API | **would-mislead-agent (copy-pasteable import that fails the build)** |
| 5.6 | `docs/architecture/SPAARKE-REPOSITORY-ARCHITECTURE.md:90-94` | PCF catalog = AIMetadataExtractor, SpeFileViewer, ThemeEnforcer, UniversalDatasetGrid, UniversalQuickCreate | **4 of 5 do not exist**; real tree has 20 controls | would-mislead-agent (first-stop orientation doc) |
| 5.7 | `docs/architecture/shared-ui-components-architecture.md:25,30` (header claims `Status: New`) | Dataset/Grid category lists 6 retired view components; Dialogs list names 3 moved/deleted dialogs | See ground truth | would-mislead-agent; needs a supersession banner like its sibling |
| 5.8 | `docs/standards/MODAL-DECISION-CRITERIA.md:85,46` | Family-2 example links `components/FindSimilarDialog/`; lists `FilePreviewDialog (deprecated)` as pickable | Folder doesn't exist (#714 rename); FilePreviewDialog deleted — `RichFilePreviewDialog` is "the sole preview/browse surface" | would-mislead-agent (standards decision table) |
| 5.9 | `docs/architecture/SPAARKEAI-COMPONENT-MODEL.md:73,75` | `SendEmailDialog`/`FindSimilarDialog` → `components/<Name>/`; DatasetGrid "Used by PCF + Code Pages" | Wrong folders; DatasetGrid = ViewSelector only | would-mislead-agent |
| 5.10 | `docs/architecture/SPAARKEAI-DASHBOARD-AND-WIDGET-MODEL.md:252,267` | LW source tree "Retained… otherwise untouched"; "Everything in `src/solutions/LegalWorkspace/src/` … is library code consumed by the engine" | The Create*/FindSimilar/Playbook folders are consumed by NOTHING (and are being deleted). The blanket claim is the kind of sentence that hid the original 6-month orphan | **would-mislead-agent** |
| 5.11 | `docs/architecture/sdap-pcf-patterns.md:25,26` | `UniversalQuickCreate` + `UniversalDatasetGrid` presented as live catalog rows (while row 41 correctly strikes AssociationResolver) | Neither exists | would-mislead-agent |
| 5.12 | `docs/architecture/membership-resolution-pattern.md:24,546` | Cites AssociationResolver PCF as "stable" | Retired/deleted | would-mislead-agent |
| 5.13 | `src/client/shared/CLAUDE.md` (auto-loaded module doc) | ENTIRE running example is `StatusBadge` (structure tree, JSDoc, barrel snippet, tests) + cites `hooks/usePagination.ts` | `StatusBadge` does not exist in the lib; no `usePagination` — and the same file's own ADR-051 section bans the pager pattern the example implies | would-mislead-agent (auto-loads every shared-lib session) |
| 5.14 | `src/client/pcf/CLAUDE.md:10,59` + build section | Lists UniversalDatasetGrid as a live control; `import { DataGrid, StatusBadge … }` as the CORRECT example; prod build via `npm run build -- --mode production` and `pac pcf push` | Control gone; StatusBadge fictional; root CLAUDE.md §12 + AP-1 mandate `npm run build:prod` and solution-import deployment | would-mislead-agent |
| 5.15 | `docs/architecture/universal-dataset-grid-architecture.md:22` | "**Verified against code**: components/DatasetGrid/ contains UniversalDatasetGrid.tsx, GridView.tsx…" (banner at :3-11 does mark it superseded) | None of those files exist; the "Verified against code" phrasing is the hazard if read past the banner | cosmetic→hazard |
| 5.16 | `docs/architecture/client-resources-inventory.md:87` | UniversalDatasetGrid row left "Confirm whether deployed but unbound" | Source deleted; row reads unresolved (legitimately a deployed-artifact register, so: update wording, don't delete row) | cosmetic |
| 5.17 | Code-comment strays with the same effect (outside `docs/`): `pcf-safe.ts:8` (3.3), `components/index.ts:205-217` (3.4), `:69-72` (3.5), `wizardRegistry.ts:180` (3.6), `CalendarWorkspaceWidget.tsx:124` (names retired `UniversalDatasetGrid`/dead `useDatasetMode` as live package-root surface) | — | — | fix opportunistically |

Clean bill (checked, no action): every other `.claude/patterns/ui/*.md` pointer target exists; `docs/standards/MODAL-DESIGN-SYSTEM.md`, `CHAT-ATTACHMENT-POLICY.md`, `ODATA-NAMING-CONVENTION.md`, `UI-SCROLLBARS.md`, `UI-DESIGN-STANDARDS.md` all resolve; `LEGALWORKSPACE-RETIREMENT.md`'s one client-path citation exists; `SPAARKE-DATAGRID-FRAMEWORK-ARCHITECTURE.md` and `client-resources-inventory.md:14,80` correctly mark retirements.

---

## 6. ADDENDUM A — outer client areas (code-pages · external-spa · office-addins · webresources · other shared libs)

### 6A.1 Dead auth-shim family — delete now (all VERIFIED zero-importer; source-only, no deployment risk)

Every code page and PCF migrated to `@spaarke/auth` via a local `authInit.ts`, but the superseded files were left behind. Live-path evidence was traced for each (the page/control's real entry imports `authInit`, never these):

| Path | Note |
|---|---|
| `src/client/code-pages/PlaybookBuilder/src/services/authService.ts` + `src/config/msalConfig.ts` | "scheduled for removal"; msalConfig's sole importer is authService (transitive zombie). No hardcoded IDs |
| `src/client/code-pages/DocumentRelationshipViewer/src/services/auth/{MsalAuthProvider,msalConfig}.ts` + `src/types/auth.ts` | msalConfig carries **hardcoded dev CLIENT_ID/TENANT_ID/REDIRECT_URI** (`spaarkedev1`) — deleting removes a pinned dev tenant from the tree |
| `src/client/pcf/EmailProcessingMonitor/control/AuthService.ts` | zero importers; Host uses `./authInit` |
| `src/client/pcf/SemanticSearchControl/.../services/auth/{msalConfig,index}.ts` | already gutted to no-export tombstones |
| `src/client/pcf/DocumentRelationshipViewer/.../services/auth/msalConfig.ts` + `types/auth.ts` | msalConfig body is `export {};` whose own header requests deletion; types/auth not even barrel-exported |

Also dead in code-pages (beyond §1.8): `code-pages/DocumentRelationshipViewer/src/components/NodeActionBar.tsx` (180 lines; `App.tsx:56` comment "NodeActionBar removed — node click now opens FilePreviewDialog"). The PCF twin's NodeActionBar is live+tested — see 6A.5. **VERIFIED · delete** (~1.6k lines total with §1.8).

### 6A.2 Mis-wired (additions to §4)

| Item | Detail | Confidence | Disposition |
|---|---|---|---|
| **LegalWorkspace daily-briefing options silently discarded** | `Spaarke.DailyBriefing.Components/src/widgets/dailyBriefing.registration.ts:130-156` accepts `authenticatedFetch`/`tenantId`/`onRateLimitError`/`loadNotificationContext` marked `@deprecated R2.1 — Ignored`, and **discards them**. But the live caller `src/solutions/LegalWorkspace/src/sections/dailyBriefing/dailyBriefing.registration.ts:85-86` still passes `onRateLimitError: routeRateLimitTelemetry` + `loadNotificationContext`, threaded up through `sectionRegistry.ts:108` and LW `index.ts:102`. LW believes it routes rate-limit telemetry to App Insights and injects a notification loader; **both are dropped on the floor**. (`SpaarkeAi/main.tsx:252-253` correctly documents the ignore — the two hosts disagree) | **VERIFIED** | **fix**: honour `onRateLimitError` or delete the 4-prop surface AND the LW plumbing feeding it — a functional bug disguised as a deprecation |
| office-addins `IHostAdapter.ts:193` — **FALSE `@deprecated` on a live type** | `IHostContext` is tagged deprecated but is heavily live (`shared/taskpane/App.tsx:4,117,146`, both host adapters, SaveView tests) and has NO replacement. The reverse trap: an annotation inviting deletion of working code | **VERIFIED** | **fix** (remove the tag) |
| office-addins dead branch | `IContentData`/`HostFeature` + `getContentToSave()`/`supportsFeature()` implemented in BOTH adapters (`WordHostAdapter.ts:261,282`, `OutlookHostAdapter.ts:331,353`) but declared in no interface and called by nothing | **VERIFIED** | delete branch |

### 6A.3 Webresources — env pinning + two-copy drift + binding census

16 `.js` webresources; 4 are referenced by in-repo ribbon XML (`sprk_playbook_commands`, `sprk_aichatcontextmap_ribbon`, `sprk_ThemeMenu`, `sprk_wizard_commands`). The other 12 have NO in-repo XML reference — **all INFERRED on liveness** because form OnLoad/OnChange handlers and Ribbon-Workbench customizations live only in the deployed solution: most are corroborated live by docs/deploy scripts; the standout exception is **`sprk_openSprkChatPane.js` — explicitly slated for removal** by `ai-analysis-workspace-sprkchat-integration` (spec.md:23, design.md:346, task 015) and self-referenced only → **LIKELY dead, highest-confidence webresource deletion candidate**. `sprk_registrationribbon.js` is referenced only by a markdown ribbon-definition under a DIFFERENT name (`sprk_/js/registrationribbon.js`) — reconcile.

Environment config (extends §4.4):

| File | Hardcoded | Runtime detection | Verdict |
|---|---|---|---|
| `sprk_registrationribbon.js` | dev BFF URL (L27) + clientId/bffAppId/tenantId (L35-37) + redirectUri pinned to `spaarkedev1` (L44) | PARTIAL — hostname switch (L80-86) picks the URL, but **IDs + redirect are unconditionally dev** | **VERIFIED · fix** |
| `sprk_aichatcontextmap_ribbon.js` | same ID set (L23-25,32) + URLs (L58-70) | PARTIAL — same shape. `production-environment-setup-r2` task 044 was written to fix exactly this but targeted a path that no longer holds the file — **the remediation landed nowhere** | **VERIFIED · fix** |
| `sprk_updaterelated_commands.js:18` | `apiBaseUrl: "https://spe-api-dev-67e2xz.azurewebsites.net"` | **NONE** — flat const, no switch, no env-var | **VERIFIED · fix (worst offender)** |
| `sprk_analysis_commands.js:388`, `sprk_fieldmapping_push.js:142` | dev URL as labelled FALLBACK after `sprk_BffApiBaseUrl` env-var lookup | YES | acceptable · document |

**Two-copy deploy contract**: `sprk_DocumentOperations.js` ⇄ `infrastructure/dataverse/ribbon/DocumentRibbons/WebResources/` copy — hash-identical (contract held). **`sprk_emailactions.js` ⇄ its `EmailRibbons` copy — DIFFER (732 vs 730 lines)** under the same byte-consistency contract (`.claude/patterns/webresource/custom-dialogs-in-dataverse.md:26`). The manual-sync failure mode of §2.9, **already realized**: next deploy ships whichever copy the script grabs. **VERIFIED · fix before next email-ribbon deploy** (reconcile + declare one authoritative).

### 6A.4 Other shared libs

- No wholesale-dead package: all 11 `@spaarke/*` client libs have real importers (1,521 occurrences / 605 files).
- `Spaarke.DailyBriefing.Components/hooks/index.ts:32-42` — `useBriefingNarration`/`useBriefingNotifications`/`useBriefingActions` barrel exports are **test-only** (own tests are the sole importers; production path is `useBriefingRender` since the R7 Wave-12 cutover). The barrel comment's claimed non-test consumers do not exist. **VERIFIED** · delete re-exports (+ test-diet the tests).
- `Spaarke.SmartTodo.Components/src/types/entities.ts` mirror vs LW `types/entities.ts`: 21 fields, one drift — `createdon`/`modifiedon` REQUIRED in LW, OPTIONAL in the package (package strictly wider; benign today, direction that matters flows fine). **VERIFIED** · document at the mirror header. Consolidation is *correctly* forbidden (ADR-012 — the package must not reach into solutions), which is why the mirror exists.
- Calendar config twin (extends §2.8): shared "Event Default" `e15c2b93-…` (EventsPage `App.tsx:41`) vs Calendar-widget copy `5294c28a-…` (`CalendarWorkspaceWidget.tsx:145`). Both are Dataverse record GUIDs — record-body drift is **unverifiable from the repo** (INFERRED); the "identical except source/availableViews" invariant has no test and no repo representation. Document the pairing in `client-resources-inventory.md`.
- `Spaarke.SdapClient/SDAP-CLIENT-V2-*.md` (3 package-root docs) — code samples import `MsalAuthProvider`, which no longer exists live anywhere. **VERIFIED stale** · fix/delete (also a §5 row in spirit).
- `CommunicationsWorkspaceWidget.tsx:139` deprecated `configId` prop — intentional compile-shim, regression-locked by its own test, but carries a stale grid-config GUID through a dead prop. Leave-with-reason; schedule removal with the registration call site.

### 6A.5 DocumentRelationshipViewer PCF ⇄ code page (fork, additions to §2)

Near-complete fork: same-named `hooks/useForceLayout.ts`, `hooks/useVisualizationApi.ts`, `services/VisualizationApiService.ts`, `types/{api,graph}.ts` on both sides. **Code page is canonical** (`sprk_documentrelationshipviewer` maps to the code page; `client-resources-inventory.md:197` "Code Page is canonical; PCF may be retired"; `.claude/constraints/pcf.md:181` labels the PCF "legacy — migrate"). Drift runs both ways: code page grew Network/Timeline/FilePreview/CsvExport then abandoned three of them (§1.8/6A.1); PCF kept NodeActionBar alive+tested while the code page's copy died — **test coverage now sits on the retired surface**. PCF retirement is **LIKELY-but-INFERRED**: "no form binding" rests on repo-side XML absence, and deployed `customizations.xml` could still bind `Spaarke.Controls.DocumentRelationshipViewer` (`spaarke-components-inventory.json:339,10497` shows it shipped inside SpaarkeMaster/SpaarkeCore). **Disposition: consolidate, GATED on an environment query for the control binding first.**

---

## 7. ADDENDUM B — client↔server route agreement

Method: 480 raw `/api/…` literals in client/solutions (188 distinct shapes after dropping tests/mocks/Dataverse Web API) matched against 412 server `Map*` registrations with `MapGroup` prefixes resolved (incl. caller-supplied groups). Every "no matching route" claim below was re-verified with a dedicated literal grep of the distinguishing segment across `src/server/api/Sprk.Bff.Api/**/*.cs`.

### 7.1 Live client call sites targeting deleted or never-existing routes (all route-absence claims VERIFIED)

| # | Client | URL → verb | Server truth |
|---|---|---|---|
| R1 | `Spaarke.UI.Components/SprkChat/SprkChat.tsx:1767` (live `readSseStream` at :1784) | `POST /api/ai/chat/sessions/{id}/plan/approve` | **Deliberately deleted** with the Phase-2F dispatcher stack (`ChatEndpoints.cs:347-350`); live replacement is `/gates/{gateId}/resolve` (:365). Caveat: whether a plan-approval UI still renders to REACH this call was not traced — route absence is VERIFIED, user-visible 404 is LIKELY |
| R2 | `SpaarkeAi/usePlaybookOptions.ts:95` | `POST /api/ai/playbook-dispatch/execute` | Removed ("dead click endpoint", `SprkChatAgentFactory.cs:1213`); client self-flags at :14 |
| R3 | `Reporting/src/services/reportingApi.ts:348` | `GET /api/reporting/privilege` | `/api/reporting` group has no `/privilege` |
| R4 | `pcf/EmailProcessingMonitor/EmailProcessingDashboard.tsx:183` | `GET /api/admin/email-processing/stats` | No `email-processing` anywhere server-side |
| R5 | `Spaarke.UI.Components/utils/adapters/xrmUploadServiceAdapter.ts:106,163` | `POST /api/documents/upload` | No `/upload` under any `/api/documents` group (only `/api/compose/upload` exists) |
| R6 | same adapter `:177` | `GET /api/containers/{entity}/{id}` | Route's home `Api/UploadEndpoints.cs` **deleted by uac-r2 task 073** |
| R7-R9 | `Spaarke.SdapClient/SdapApiClient.ts:168` + `operations/{Delete,Download}Operation.ts:16` | `GET/DELETE /api/obo/drives/{d}/items/{i}` (+`/content`) | **Deleted 2026-08-26 by uac-r2 task 071** (`OBOEndpoints.cs:378-380`) |
| R10-R11 | `external-spa/api/web-api-client.ts:433,592` | `GET/POST /api/v1/external/projects/{id}/events` | External group serves no `/events` |
| R12 | `external-spa/api/web-api-client.ts:605` | `PATCH /api/v1/external/events/{id}` | Near-miss ×2: `PATCH /external/todos/{id}` exists; separate `/api/v1/events/{id}` group is PUT-only, no `external` segment |
| R13-R15 | `external-spa/components/DocumentLibrary.tsx:274,584,408` | `GET /external/documents/{id}/versions` · `GET /external/documents/{id}/download` · `POST /external/documents/upload` | No `/documents/*` under the external group — live shape is `/projects/{id}/documents/{docId}/content`. **Shipped external-user document features 404** |
| R16 | `external-spa/hooks/usePlaybookExecution.ts:123` | `POST /api/v1/external/ai/playbook` | No `/ai/*` under external group |
| R17 | `Spaarke.UI.Components/utils/adapters/bffDataServiceAdapter.ts:100` (consumer: external-spa `PlaybookLibraryPage.tsx`) | `GET /api/dataverse/{entity}` (+`/{id}`) | `/api/dataverse` group has no bare entity-collection route — **LIKELY** (which IDataService methods that page exercises was not traced) |

**Pattern worth naming**: R6-R9 are THIS project's own server-route retirements (tasks 071/073) leaving client callers dangling — the same "removed the imports, not the twin" failure shape, but across the wire. Client-side cleanup tasks should accompany every route retirement.

### 7.2 Confirmations for §4 items

- **Container delete (4.1)**: server confirmed to have **no `MapDelete` on `/api/spe/containers/{id}`** (repo-wide MapDelete grep) — the fake-delete UI cannot be wired without a new endpoint.
- **closureService twins (2.2)**: both copies call the SAME live route `POST /api/v1/external-access/close-project` (registered `ProjectClosureEndpoint.cs:48` + `ExternalAccessEndpoints.cs:142`) — no route drift between the twins. Two side-findings: (a) the shared twin's JSDoc example passes a base URL ending in `/api`, which with its `${bffBaseUrl}/api/v1/…` template would yield `/api/api/v1/…` — wrong doc, host-dependent hazard; (b) the endpoint appears to be mapped TWICE for the same template — if both extension methods run at startup that is an `AmbiguousMatchException` at request time — **UNSURE**, 2-minute composition-root check recommended.
- **Registration ribbon (4.4)**: approve/reject paths+verbs+GUID constraint all AGREE with `RegistrationEndpoints.cs` — 4.4 is an environment-pinning defect only, not a 404.

### 7.3 Dormant shapes (deliberately inert — do not re-report)

`POST /api/workspace/invoices/pre-fill` (behind `PREFILL_ENABLED=false`; known server gap) · `GET/PUT /api/me/preferences` (localStorage TODO) · `/api/workspace/notifications` (mock-backed) · `POST /api/compose/action/{consumerType}` (documented-deleted; real path is sessions dispatch) · three OBO upload-session shapes inside task-076 tombstone comments.

### 7.4 Instrument gap

`SpeAdminClientRouteAgreementTests` lives at `tests/Spaarke.ArchTests/` (not `tests/unit/Sprk.Bff.Api.Tests` as commonly cited) and pins ONLY `/spe` URLs in `speApiClient.ts` — every mismatch above sits outside its blast radius. Follow-up: generalize the instrument (a sibling rule per client package) so R1-R17 become build failures, not archaeology.

---

## PRIORITISED TOP-10 (risk to correctness or security, not tidiness)

1. **SpeAdmin container Delete is fake (4.1)** — an admin gets written confirmation that document containers were destroyed when nothing happened; a retention/offboarding/compliance action silently no-ops.
2. **`createTodoRegardingChild` drops the Field-Mapping engine (4.2)** — follow-on To Dos from three live surfaces silently lack admin-configured inheritance, and a shared-lib comment asserts the opposite of the code; data quality forks by entry path with zero warnings.
3. **Close-Secure-Project trigger has no in-repo caller (4.6 + 2.2)** — if no deployed artifact calls `__SPAARKE_OPEN_CLOSE_PROJECT__`, secure-project closure (the access-revocation cascade — this project's subject) is unlaunchable UI; and its logic exists as a manual-lockstep twin either copy of which can drift.
4. **Seventeen live client call sites target deleted or never-existing BFF routes (7.1)** — shipped external-user document download/versions/upload and events flows 404 (`DocumentLibrary.tsx`, `web-api-client.ts`), SprkChat's plan-approve streams into a deleted endpoint, Reporting privilege-check and EmailProcessingMonitor stats never had a server; four of them (R6-R9) were orphaned by THIS project's own route retirements (tasks 071/073) — the wizard incident's shape, across the wire.
5. **Env-pinned access-granting webresources (4.4, 6A.3)** — `sprk_registrationribbon.js` + `sprk_aichatcontextmap_ribbon.js` hardcode dev tenant/client/BFF IDs (and the r2 remediation task for the latter landed on a path that no longer holds the file); `sprk_updaterelated_commands.js` hardcodes the dev BFF URL with no escape hatch — approve/reject and update flows that hit the wrong environment anywhere but dev (routes themselves verified to exist, 7.2).
6. **Binding agent docs command dead paths (5.1-5.5)** — `.claude/constraints/pcf.md:70`'s MUST, six `.claude/patterns` pointer files, and ADR-012's copy-pasteable `dist/components/FindSimilarDialog` import all aim at components that don't exist; agents load these as ground truth, which is how the next six-month orphan (or a fresh parallel grid) gets built.
7. **LegalWorkspace silently loses daily-briefing telemetry + loader (6A.2)** — LW passes `onRateLimitError`/`loadNotificationContext` into a factory that discards them as "deprecated — ignored"; rate-limit telemetry the workspace believes it routes never arrives, with zero warnings.
8. **LegalWorkspace ⇄ SmartTodo service fork, both alive (2.3)** — two drifting "single data access layers" for the same entities; the failure mode is two surfaces silently showing different rows — one filter-drift away from an access-adjacent wrong answer.
9. **`sprk_emailactions.js` two-copy drift, already realized (6A.3)** — the byte-consistency deploy contract between `src/client/webresources` and the `EmailRibbons` deploy copy is broken (732 vs 730 lines); the next ribbon deploy ships whichever copy the script grabs. The rest of the manual-sync registry (2.9) is the same defect on a timer.
10. **Deletion-sweep completeness (1.1, 1.2, 3.1, 6A.1, 6A.5)** — the in-flight chain deletion must also take: the shared-lib `FindSimilar` wizard family, the LW `Playbook` fifth chain, the LW `Wizard` re-export shims, the 12 zombie auth shims, and (gated on a deployed-binding check) the legacy DocumentRelationshipViewer PCF — or the original incident repeats with the survivors.

*(Bubbling under: 4.3's `?? fetch.bind(window)` degrade-wiring trap; the false `@deprecated` on office-addins' live `IHostContext` (6A.2) — an annotation inviting deletion of working code; and the possible duplicate `close-project` route registration (7.2b). Dead code that is merely dead — 1.3-1.9, 6A.1 — deliberately ranks below everything that produces a wrong live outcome; it holds #10 only because leaving it repeats the incident that triggered this sweep.)*
