# Draft punch rows — auth-v4 §10 addendum (Δ1-Δ5 + §10.5 traps + §10 DELIVERED consumption)

> **Date**: 2026-08-25
> **Deliverable**: 3 of 4 (auth-v4 integration response) — copy-paste-ready rows for `task-202-punch-list.md`
> **Source**: auth-v4 canonical `PROVISIONING-CHANGE-REQUEST.md` §10 addendum (Δ1-Δ5 + §10.2 live contract + §10.3 RBAC + §10.4 dual app-user + §10.5 traps) + §10 DELIVERED (`-FicOnly` contract) + 2026-08-25 CORRECTION block
> **Companions**: [`auth-v4-integration-remediation-plan.md`](auth-v4-integration-remediation-plan.md) §4/§7 · [`decisions/adr-028-a4-integration-conflict-resolution.md`](decisions/adr-028-a4-integration-conflict-resolution.md) · [`auth-v4-integration-open-questions.md`](auth-v4-integration-open-questions.md) (Q2, Q5, Q6, Q9)
> **Schema**: matches `task-202-punch-list.md` Class-A row shape exactly — `| # | lesson-id | title | landing-spot | effort-h | deps | last-known-status | source |` — grouped by blocks-E2E as in the punch list.
> **Numbering**: the punch list already extends to **A34** (A28-A34 exist; B22-refined proposed). New rows start at **A35**. Do NOT number A28+.

## Executive summary

10 draft rows (A35-A44), 27h total. 8 block task 186 E2E (A35-A42); 2 are quality gates (A43, A44). Dedup against the existing punch list found: Δ4 **provisionally covered** (verify, don't rebuild), Δ5 **partial** (dedupe against in-flight agents), Δ1/Δ2 RBAC **NOT covered** by A20/A13 (different principal — BFF runtime UAMI, not L2 provisioning UAMI), Δ3 a **total gap** (0 of 8 settings present). Critical path to 186: A35 → (A38 ∥ A42) → A39.

---

## A-blocks-E2E YES (8 rows) — must land BEFORE task 186 live-fire

| # | lesson-id | title | landing-spot | effort-h | deps | last-known-status | source |
|---|---|---|---|---|---|---|---|
| A35 | auth-v4-master-merge-sync | Merge `origin/master` into r1 worktree (verified 2026-08-25: 281 behind / 273 ahead; HEAD `45e14556a`). Brings `Register-EntraAppRegistrations.ps1` with `-FicOnly` (verified: 16 occurrences on master; `-SkipClientSecret` also real — 4 occurrences; both coexist) + `Assert-SpaarkeFicTenancy` guard + ADR-028 **A4 + E-3 CLOSED** + corrected `auth.md:108` + migrated `DataverseServiceClientImpl`/`DataverseWebApiService` (verified: master has MI branch + secret-free ordered credential; THIS branch still raw ClientSecret at `:41-65`) + corrected `oauth-obo-patterns.md` / BFF `CLAUDE.md`. Until merged, adr-check runs against a pre-A4 ADR. Resolution rules per remediation plan §6 (script = union merge: master's FIC estate verbatim + re-apply task-010 reconcile layer; `.claude/**` auth files = take theirs). Use `worktree-net10-migrate` guard. | worktree/git | 3 | none | not-applied — commits `dee3df03c`/`39b2bda38` NOT ancestors of HEAD (git-verified) | PROVISIONING-CHANGE-REQUEST §10 DELIVERED; branch-drift verification 2026-08-25 |
| A36 | d1-bff-uami-sb-data-roles | Grant **BFF runtime UAMI** (`sharedBffUami` Model 1; per-customer `mi-bff` Model 2) `Azure Service Bus Data Sender` + `Data Receiver` on the BFF SB namespace. NOT covered by A20 (grants the **L2 provisioning UAMI** key-extraction roles on shared source services — different principal, different purpose) or A13 (L2 worker's own queue). Model 2: role assignments repeat per-customer stamp — bake into the reusable RBAC module, not a one-off. | bicep (`model1-shared.bicep` + `customer.bicep` RBAC modules, pattern per A20/A21) | 2 | none | not-applied | §10.1 Δ1 + §10.3 (live contract 2026-08-25) |
| A37 | d2-bff-uami-search-roles | Grant BFF runtime UAMI `Search Index Data Contributor` + `Search Service Contributor` on AI Search (both models; per-customer under Model 2 — same module note as A36). A20's Search Service Contributor is L2-UAMI-scoped — different principal. | bicep (same modules as A36) | 1.5 | none | not-applied | §10.1 Δ2 + §10.3 |
| A38 | d1-d2-kv-manifest-secret-free-omit | H4/H4-shared credential-type seam (FR-39): on secret-free envs, **OMIT — never sentinel** (§9.1; sentinel → opaque `AADSTS7000215`) the `BFF-API-ClientSecret`, `ServiceBus-ConnectionString`, `AiSearch--AdminKey` manifest entries. Verified: `StaticKvSecretManifest.cs:74` still upserts `AiSearch--AdminKey` (FromBicepOutput). Positive migration marker = KV tag or provisioning-state field, never the credential slot. **Partially closes A30's sentinel contract — the H4 half only; the H7/task-142 half is in scope here explicitly or must get its own row** (A30's source names BOTH H4 and H7 per AUTH-V4-RESPONSE). Acceptance MUST state Model 1 (shared KV) vs Model 2 (per-customer `kv-{customerId}-{secretsVer}` × N vaults) behavior explicitly, incl. how the secret-free marker is applied consistently across N per-customer vaults. Executor: grep-locate the `ServiceBus-ConnectionString` manifest entry's source site first (only `AiSearch--AdminKey` grep-confirmed in `StaticKvSecretManifest`). | code (`Handlers/KvSecretsPopulation/*` manifests + tests) | 4 | A35 | **ESCALATED 2026-08-25 (task 205a first pass — `partial-omit-set-discovered`; site inventory + re-scope below) → RE-SCOPED A38a/A38b/A38c/A44.5, owner-approved → ALL FOUR APPLIED 2026-08-25/26 in working tree (see per-row execution records below); A38a+A38b await main-session atomic bundle-commit; ONE open item: `site-inventory-drifted` fired by A38a second pass (`Setup-OfficeServiceBus.ps1:172` 6th SB-conn upsert site — legacy dev-bootstrap class; owner disposition pending before bundle-commit)** | §9.1 + §10.1 Δ1/Δ2; A30; FR-39; §6.5 resolution record |
| A39 | d3-h4b-secret-free-settings | Extend H4b `per_env_settings` manifest (verified: `H4bBulkAppSettingsHandler.cs` + `FilePerEnvSettingsManifest.cs` exist; 0 of 8 keys present) with the §10.2 live contract: `Graph__Credentials__Order__0=ManagedIdentityFederated` (ONLY entry), `Graph__Credentials__RequireSecretFreeIdentity=true` (fail-fast), `ManagedIdentity__ClientId` (FromHandlerOutput — UAMI clientId), `ServiceBus__FullyQualifiedNamespace` + `Membership__EventPublisher__ServiceBusNamespace` + `Membership__JunctionUpdater__ServiceBusNamespace` (`<ns>.servicebus.windows.net`, NOT conn string), `AiSearch__ManagedIdentity__Enabled=true`, `AiSafety__ContentSafety__ManagedIdentity__Enabled=true`. **ORDERING GUARD (BINDING per §10.2)**: identity + FIC + RBAC provisioned BEFORE these settings — "provision the identity first, then the setting — never the reverse". Hence deps include **A42** (the row that actually creates/verifies the FIC): setting `RequireSecretFreeIdentity=true` before the FIC exists boot-loops a fresh stamp (exit-134 class). Also: no load-bearing key may be `required=false` (H4b `:286` silently skips missing optional entries). | code (`H4bBulkAppSettingsHandler` manifest + tests) | 4 | A36, A37, A38, **A42** | not-applied (0 of 8 verified) | §10.2 live `spaarke-bff-dev` contract; §10.1 Δ3 |
| A40 | d4-kvrefidentity-verify-and-assert | Δ4 **provisionally covered — content unverified**: `ArmAppServiceIdentityPatcher.cs` exists (dir-listing only; body never read by any reviewer). Step 1: CODE-READ the patcher + H4 handler bodies to confirm dual-slot `keyVaultReferenceIdentity` PATCH actually implemented per FR-37/T1/T5. Step 2: task-186 E2E assertion `site.keyVaultReferenceIdentity == UAMI resource ID` post-H4 + exit-134 symptom in runbook §12. Step 3 (**slot-persistence criterion, per §10.2**): the property is a SITE PROPERTY not copied by `az webapp deployment slot create --configuration-source` — add an acceptance criterion that any later slot create/swap path (H9 deploy, rollback ceremony) re-asserts it; live counter-example exists at `projects/dotnet-10-upgrade-r1/notes/deploy-net10-slot-phase1.ps1:40`. A24 is unrelated (healthCheckPath). A25 (KV Secrets User on sharedBffUami) already applied. | code-read + E2E assert + runbook | 2 | none | provisionally-covered — verify content, then assert | §10.1 Δ4 + §10 CORRECTION; FR-37, T1/T5 |
| A41 | d5-h10-uami-dual-app-user | H10/T2: TWO Dataverse app-user rows per environment (§10.4) — (1) app-reg row (OBO; token appid stays the app-reg under MI-FIC), (2) **UAMI row: applicationid = UAMI clientId, `azureactivedirectoryobjectid == UAMI principalId — NOT clientId`** (the silent-fail trap; app-only DV 401s otherwise; auth-v4's "single most-missed item"). Design intent present (design.md:153 "BFF app-reg + UAMI"); execution deferred. **Dedupe against in-flight `Wave-3E-053-H10-AppUser` / `ds8-uami-dv-appuser` output FIRST** — may collapse to probe-only. Extend task-177 T2 probe with the principalId byte-equality assertion (⚠️ CONFIRM: probe suspected count-only from test messages "OK (count=1)"/"COUNT=0" — read the verifier before extending; unconfirmed). Model 1: registers the SHARED app-reg SP + shared UAMI once per DV env — zero per-customer app-reg objects (§5.1 Reading 1). Fix design.md:153 figure drift (says "10/14 GUIDs null"; spec says 11-of-14 +1 = 15; `GraphAppRoles.cs` is source of truth). **SCOPE EXTENSION per Q8 owner disposition 2026-08-25 (+30 min effort)**: also fix `design.md:61` D3 wording drift — "Managed Identity" listed as per-customer dedicated for Model 1 is stale (predates v3.5 auth-v4 MI-FIC coordination which established the single shared `sprk-{env}-shared-bff-uami`). Draft edit: replace "Managed Identity" in D3's Model 1 "per-customer dedicated" list with an explicit shared-UAMI clause referencing task-029 slot-swap fix + I6 code-level isolation. **Also add a Model 1 row to the Naming Standards table (`design.md:~658`)** for `sprk-{env}-shared-bff-uami` alongside the existing Model 2 `mi-spaarke-{customerId}-{env}` row — prevents future drift by making the Model 1 shared-UAMI pattern explicit in the naming registry. | code (H10) + T2 probe + runbook §12 + design.md D3 + Naming Standards | 3.5 (was 3; +0.5h per Q8) | A35; 053/ds8 output review | **applied 2026-08-26 (task 205d, Sonnet @ high, FULL rigor)** — dedupe found task 053 landed the two-row structure but neither dedupe source (053 or ds8) closed the §10.4 objectid trap; NO scope-collapse (H10 handler code changed). Full execution record below (§ "A41 execution record"). | §10.1 Δ5 + §10.4; `mi-proof-dataverse-side.md`; **Q8 owner disposition 2026-08-25** |
| A42 | g3-ficonly-consume-reconcile | Task 130 already landed C# FIC creation (`GraphAppRegistrationProvisioner.cs` — verified: per-profile issuer at `:547-557`, `customer-owned-model2` → customer tenant, else Spaarke tenant; reading-(a)-consistent) = the §9.3-anticipated fallback path. **Reconcile per FR-C4 — don't leave duplicate estates diverging**: either (a) H3 delegates to `.\Register-EntraAppRegistrations.ps1 -FicOnly -FederatedCredentialAppId <id> -UamiResourceId <ARM resource ID — NEVER a name; 5 UAMIs exist, `spaarke-bff-identity` is a decoy> -TenantId <tenant>` as **subprocess (NEVER dot-source)**, or (b) keep both under ONE written contract with parity tests: idempotency by (issuer,subject,audience) triple; propagation retry on **AADSTS70025 exact-match** (measured — NOT 70021; substring matchers also catch 700211/700213 config faults); exit codes 0/1/2 (2 = structurally-correct-unverifiable = NORMAL off-Azure → REQUIRE recorded post-App-Service verification, never terminal success). **Port the cross-tenant tenancy refusal** (master script's `Assert-SpaarkeFicTenancy` — verified present on master, verified ABSENT from the C# provisioner) into `CreateFic`. E2E closes auth-v4 §11 invariants 1+2 (wrong-subject → AADSTS700213 detected; fresh-FIC flap ~8 failures/~130s retried) — first real exercise. Model 1: zero FIC calls (I6 tests assert ProvisionCallCount==0). ⚠️ CONFIRM: "greenfield stamp sub `cd95fcec` is Spaarke-hosted (same-tenant, §9.2-independent)" is an UNVERIFIED ASSUMPTION — verify tenancy shape before dispatching 186 (open question Q9). Escalation trigger: STOP if a customer-owned-tenant Model 2 run is attempted while §9.2 (Q2) is unanswered. | code (`H3EntraAppRegHandler` script-invocation seam / contract-parity) | 4 | A35; §9.2 escalation-gate (customer-owned M2 only) | **applied 2026-08-25 (task 205b, Fable @ xhigh FULL rigor)** — path **(b) contract-parity** per owner Q5 disposition + spec.md:279 Option-D no-shell-out MUST NOT (POML step-3's "(a) recommended" superseded; rationale: contract §1). Contract: `notes/decisions/205b-a42-fic-parity-contract.md`. `Assert-SpaarkeFicTenancy` PORTED → `GraphAppRegistrationProvisioner.AssertFicTenancy` + `CrossTenantFicRefusedException` + rejection code `appreg-cross-tenant-fic-refused`, first-check in the FIC path (SF-5 closed; unconditional refusal = PS parity; inert under reading (a)). Triple-keyed idempotency (`FindEquivalentByTriple`, any-name, exactly-one-audience) replaces name-first check incl. re-GET verify (SF-7). `FicExchangeOutcomeClassifier` ports 70025/70021 exact-numeric-match retry + 700211/700213/7000215 fail-fast + authorization-layer acceptance + 5s-doubling-cap-30/600s-budget policy for exchange-capable hosts (SF-6). Exit codes typed as `FicVerificationState` (0/1/2); every L2 creation = exit-2 equivalent → `InterStepState.FicPendingPostAppServiceVerification=true` recorded by H3, discharged by H13/T4 (SF-8 — never terminal success). **26 new tests** (A42a-g); task-130 I6 tests UNMODIFIED + green (23/23, `ProvisionCallCount==0` preserved + stronger A42e guard); full suite 1581 pass / 0 fail. §11 invariants 1+2 first-exercised at unit level (700213 wrong-subject named; 8-failure/~130s-order flap retried within budget on virtual clock); LIVE exercise still owed to task 186 E2E. Zero escalation triggers fired (disposition: contract §7); no customer-owned-M2 dispatch anywhere in fixtures. No BFF touch — NFR-01 N/A. design.md §6.2 key-list refresh (`ficPendingPostAppServiceVerification`) rides S3 cascade. | §10 DELIVERED + §9.3 + §11; FR-C4; task-130 landed notes |

## A-blocks-E2E MAYBE / NO (2 rows) — before Phase-F sign-off, not before 186

| # | lesson-id | title | landing-spot | effort-h | deps | last-known-status | source |
|---|---|---|---|---|---|---|---|
| A43 | trap2-deploy-allindexes-gate | Gate `Deploy-AllIndexes.ps1`'s silent fallback ("KV secret AiSearch--AdminKey not found — falling back to live admin key"): when `AiSearch__ManagedIdentity__Enabled=true` or env carries the secret-free marker, **FAIL LOUD or use AAD-token path — never re-mint the admin key** (re-mint quietly undoes the Δ2 migration while the run reports green). | scripts/ai-search | 2 | A35 | **applied 2026-08-26 (task 205e, Sonnet @ high, FULL rigor)** — three-branch gate landed at `Deploy-AllIndexes.ps1`'s admin-key resolution path (was `:340-366` pre-edit; new `Resolve-AiSearchAuthContext` function replaces that block) via a NEW `function Resolve-AiSearchAuthContext`, dot-sourcing the SAME shared marker detector A38c already established (`scripts/common/Assert-SpaarkeSecretFreeGate.ps1` → `Test-SpaarkeSecretFreeMarker`) rather than a 4th independent copy (§11 extend). Branch 1 (secret-free marker present + `AiSearch--AdminKey` missing) → `Write-Error` naming the marker + linking `.claude/constraints/auth.md` + punch row A38a, `exit 10`, `az search admin-key show` never reached. Branch 2 (`AiSearch__ManagedIdentity__Enabled=true`, same query shape as the pre-existing `-CutoverBffSettings` gate) → AAD Bearer token via `Get-AzAccessToken -ResourceUrl "https://search.azure.com/"` (Az.Accounts 5.3.0 confirmed installed in this environment; wrapped in try/catch + SecureString-safe `.Token` unwrap — Az.Accounts 3.0+ returns SecureString by default) with fallback to `az account get-access-token --resource https://search.azure.com/` when Az.Accounts is unavailable; `$restHeaders` becomes `Authorization: Bearer <token>`; admin-key path never touched. Branch 3 (neither signal) → pre-existing KV-then-live-admin-key fallback byte-for-byte unchanged. **A38 tag scheme: FINALIZED, not a placeholder** — consumes A38a's landed `spaarke-secret-free-identity=true` KV tag convention directly (no `TODO(A38)`). **Deviation from the punch-row draft's illustrative "exit 6"**: exit 6 already carries two distinct pre-existing meanings in this script (verify-only + post-deploy invariant-violation counts); reusing it for a credential-security refusal would collide three meanings onto one code — used `exit 10` (branch 1) / `exit 11` (branch 2 token-acquisition failure) instead, both previously unused, documented inline. Model 1 (shared vault) vs Model 2 (N per-customer `kv-{customerId}-{secretsVer}` vaults) fleet-consistency documented in the gate's inline comment block (checks exactly the one vault it's given; does not iterate a fleet — fleet enumeration is A38a's proposed `SecretFreeMarkerConsistencyDetector` follow-up, not this script). Existing `-CutoverBffSettings` gate (line range shifted by the insertion, content grep+diff-confirmed byte-identical) untouched. New tests: `tests/scripts/Deploy-AllIndexes.Tests.ps1` (13 unit + 4 integration-style idempotency cases, 17/17 passing) — MI-enabled branch (Get-AzAccessToken path + az-CLI fallback + never-calls-admin-key-show), secret-free-marker branch (exit 10 + message content + 3x-repeat idempotency), pre-migration fallback branch (unchanged api-key header + KV-secret-present path + no-KeyVaultName legacy shape + 3x-repeat byte-identical integration check), plus 6 static regression guards (CutoverBffSettings untouched, gate-comment content, no TODO placeholder, AST parse-clean). No existing test surface pre-dated this row for this script; `dotnet`/`.cs` N/A (pure PowerShell). Script re-verified via `pwsh -NoProfile -File ... -DryRun` (exit 0, unchanged plan output) after every edit. Self-run code-review + adr-check (Sub-Agent Write Boundary N/A — no `.claude/**` touched): 2 findings self-identified and fixed before landing (uncaught `Get-AzAccessToken` exception under `$ErrorActionPreference='Stop'`; SecureString `.Token` unwrap for Az.Accounts 3.0+) — 0 open Critical/Warning; ADR-028 A4 compliant (no `.WithClientSecret(...)`/`ClientSecretCredential` introduced; branch 2 is identity-based AAD Bearer, consistent with A4's secret-free intent). Files: `scripts/ai-search/Deploy-AllIndexes.ps1` (modified), `tests/scripts/Deploy-AllIndexes.Tests.ps1` (new). No `git commit` — main session commits after review per binding sequencing. | §10.5 trap 2 |
| A44 | trap1-template-guard-and-doc-align | (a) Consumer guard: H4b/H9 deploy paths must never apply `appsettings.template.json`'s `ServiceBus-ConnectionString` KV ref on secret-free envs (template fix itself is auth-v4 obligation **051-E**, deferred to 2026-11-23 — ours is only the guard). (b) Doc sweep: replace r1's stale 280-line `PROVISIONING-CHANGE-REQUEST.md` mirror with the 626-line canonical + header naming the canonical path; delete `PROVISIONING-CHANGE-REQUEST copy.md`; apply spec.md:259/:275 edits per the §6.5 resolution record; fix stale rotation-cadence line in `provisioning.md:~112`; post-merge grep-confirm `oauth-obo-patterns.md` + BFF `CLAUDE.md` corrections arrived (verified present on master 2026-08-25). | code guard + docs | 2.5 | A35; §6.5 owner sign-off (for spec edits) | **applied 2026-08-26 (task 205f, Sonnet @ high, FULL rigor, MAIN-SESSION-ONLY per Sub-Agent Write Boundary §3)** — `last_known_status_after_execution`: **(a) DROPPED — scope-drift escalation, owner-disposed Path C**. Live-code verification 2026-08-26 (`H4bBulkAppSettingsHandler.cs` + `H9BffDeployHandler.cs` grep-read in full) confirmed **neither handler iterates `appsettings.template.json`**: H4b iterates `IPerEnvSettingsManifest` (the `per_env_settings` yaml manifest, not the template); H9 does Kudu zip-deploy of a CI artifact + slot swap + /health probe + rollback (never reads the template file). The ONLY C# consumer of `appsettings.template.json` is `NamingConformanceChecker.cs` (name-conformance R1/R2/R3 scan only — doesn't write KV refs). The actual §10.5 trap 1 mitigation lives at (i) **auth-v4 obligation 051-E** (template fix — deferred 2026-11-23; not r1's scope), and (ii) **205c A39** landing `ServiceBus__FullyQualifiedNamespace` runtime setting (in-flight background at 205f dispatch time; verified LANDED at wave commit). Sub-scope (a) authoring speculative guard code in H4b/H9 that guards a non-existent code path was rejected as anti-§11 (cost-of-doing-nothing cannot be concretely stated for a code path that doesn't exist). Owner escalation surfaced via `AskUserQuestion` before ANY edit; disposition: Path C (drop). **(b) §6.5 EDIT package APPLIED VERBATIM per decision doc 2026-08-25** (`notes/decisions/adr-028-a4-integration-conflict-resolution.md` APPROVED owner sign-off; Q7 owner narrowing to prong 3 folded into EDIT 1 replacement text): **EDIT 1** `.claude/constraints/provisioning.md` lines 27-36 REPLACED with rewritten `## KV credential lifecycle` section (4 prongs; Q7-narrowed prong 3 explicitly names `spaarkedev1` + bars new Model 2 stamps under prong 3 until A36-A42 land per Q6). **EDIT 2** root `CLAUDE.md` §17 `/provision-environment` row never-delete fragment REPLACED (retained the `customer-provisioning-orchestration-r1 tasks 075 + 076 (2026-08-18)` provenance tail). **EDIT 3 + 4** `spec.md:259` (MUST NOT delete line — rewritten to KV credential-lifecycle rule reference) + `spec.md:275` (tail clause — rewritten to time-boxed retention rationale with corrected `#3b/task-022 landed` reference). All 4 EDITs verified byte-identical BEFORE-text at Step 1 grep-verify (no drift since decision doc 2026-08-25). **Companion sweep** applied to **6 of 7** sites (Site F `.claude/skills/azure-deploy/SKILL.md:100` was ALREADY cured — the row already carries "🔴 **DELETED 2026-08-24**" + secret-free-contract language; verified in place, no re-edit): (1) `.claude/skills/provision-environment/SKILL.md:63` + `:1068` — replaced with pointer to constraint §KV credential lifecycle + explicit correction of the retired "still consumed by OBO" rationale (falsified by BFF `CLAUDE.md` 2026-08-20 + E-3 closed 2026-08-24); (2) `.claude/patterns/provisioning/manifest-driven-secret-catalog.md:27` — 4-prong summary + pointer; (3) `.claude/patterns/provisioning/manifest-driven-secret-catalog.md:48` — anti-pattern list updated (temporary-delete now cites prong 2; NEW anti-pattern added for sentinel-seeding on secret-free envs per §9.1); (4) `docs/guides/SPAARKE-CUSTOMER-DEPLOYMENT-GUIDE.md:391` — replaced rationale + pointer; (5) `scripts/canonical-secret-catalog/manifest.yaml` — Dataverse-ClientSecret exception_note (rollback-window hold; sunset 2026-11-23; auth-v4 owns retirement) + BFF-API-ClientSecret exception_note (DELETED 2026-08-24; entry retained to BLOCK re-creation; no sentinel per §9.1; soft-deleted DO-NOT-PURGE before 2026-11-22); (6) `design.md:805` + `:861` + `:1233` — annotate-only per decision doc (superseded 2026-08-25 references; original text struck-through with `~~...~~` markdown; NO history rewrite). **Post-merge cured verify**: `docs/standards/oauth-obo-patterns.md` (grep zero matches for "OBO requires secret" — cured on master merge A35) + `src/server/api/Sprk.Bff.Api/CLAUDE.md` (lines :112/:121/:126/:139/:231 all carry A4 corrections + `Never add a new .WithClientSecret(...)` guard + `ClientSecretCredential is not a local-dev fallback for OBO any more either` — cured); NO local edits required. **(c) DOC SWEEP applied**: (i) **Mirror replaced 2-step atomic** per canonical-copy rule — `notes/PROVISIONING-CHANGE-REQUEST.md` was 280 lines, now 642 (633-line canonical + 9-line header naming canonical path + not-edit-in-place discipline); `notes/PROVISIONING-CHANGE-REQUEST copy.md` deleted via `git rm -f` AFTER mirror replacement (2-step order preserved to avoid orphan reference). (ii) **Rotation-cadence line fixed** at `.claude/constraints/provisioning.md:149` (post-EDIT 1 line-shift from :112) — annotated as "retired for the BFF identity" (secret-free per A4 + E-3 closed; no client secret to rotate) + "retained for E-1" (per-customer SpeAdmin container-type secrets). (iii) **SF-1 UAMI decoy trap fixed** at **3 sites** (POML said 2; discovered 3 via `Grep`): `scripts/Grant-GraphAppRoles.ps1:52` (UamiPrincipalId) + `scripts/provisioning/Grant-ControlPlaneIdentity.ps1:103` (UamiClientId) + `:111` (UamiPrincipalId) — each now shows ARM resource ID lookup as PREFERRED with an explicit warning that the dev subscription has 5 UAMIs and `spaarke-bff-identity` is NOT the BFF's UAMI (SF-1 decoy per remediation plan §5); name-based path retained for backwards-compat with an ⚠️ warning tag. **Generator verification**: `Invoke-CatalogGenerator.ps1` regenerated (exception_note edits changed the projection of `appsettings.tokens.generated.md` — expected + captured); re-run with `-Verify` returned `VERIFY: OK - generated/ is in sync with manifest.yaml` (exit 0). `Configure-AppServiceSettings.generated.ps1` diff-verified byte-identical to 205c wave's regeneration (exception_note is docs-only; no ps1 script-body change). **Build**: `dotnet build src/server/api/Sprk.Bff.Api/ -c Release` — **0 warnings / 0 errors**. **Test suite**: NOT re-run (205c wave already verified `dotnet test Sprk.Provisioning.ControlPlane.Tests` = **1668 passed / 0 failed / 1 skipped** co-green with all 4 wave changes; my 205f edits are docs + text metadata + generator-regenerated artifacts — zero C# behavioral changes). **BFF publish-size**: NOT re-measured (my 205f edits touch zero BFF-referenced code; the wave's absolute is 45.07 MB compressed incl. PDBs = +0.11 MB delta vs 44.96 MB baseline per 205c measurement — well under NFR-01 +5 MB justification threshold + 60 MB HARD STOP). **Escalation triggers fired**: **sub-scope (a) scope-drift** — surfaced via §6.5 escalation format before ANY edit, owner-disposed Path C (drop). NO other POML-named triggers fired (EDIT 3/4 verbatim BEFORE-texts matched no drift; post-merge cured docs verified present; A35 landed with A4 amendment; §6.5 decision doc not amended post-2026-08-25; canonical source reachable). Zero constraints violated. Executor did NOT commit — main session commits 205c/d/e/f wave atomically after review. | §10.5 trap 1 + §10.6; 051-E; §6.5 resolution record |

**Totals**: 10 rows, **28.5h** (was 28h; A41 +0.5h per Q8 owner disposition — D3 wording fix + Naming Standards row). Blocks-186: A35-A42 (8 rows, 24h). Quality-gate-only: A43, A44 (4.5h).

**S1∥ mini-wave DISPATCHED 2026-08-25**: A36 + A37 + A40 running as 3 parallel background agents (per Q10 owner disposition — 3-4 active projects manageable manually; no broadcast). A36 landed clean 2026-08-25: new `infrastructure/bicep/modules/bff-runtime-rbac.bicep` (~118 lines) with SB Data Sender + Data Receiver grants; wired into `stacks/model1-shared.bicep` (`sharedBffUami.outputs.principalId` + `sharedServiceBusName`) + `customer.bicep` (per-customer `uami.outputs.principalId` + `serviceBus.outputs.serviceBusName`); all three files compile 0 errors; §11 justification in module header. A37 + A40 pending completion notifications.

---

## Dedup notes (verified against punch list + repo, not assumed)

| Δ / item | Covered by existing row? | Evidence |
|---|---|---|
| Δ1 SB roles (BFF UAMI) | **NO** — A20 grants the **L2 provisioning UAMI** roles on Model 1 shared source services (key extraction); A13 is the L2 worker's own queue. Different principal → new row A36 | punch rows A13/A20 definitions (`task-202-punch-list.md:199,206`) |
| Δ2 Search roles (BFF UAMI) | **NO** — same different-principal reasoning → A37 | punch row A20 |
| Δ3 8 credential-selection settings | **NO** — 0 of 8 present (grep-verified; only older `Graph__ManagedIdentity__*` appear, as comments/test-fixture keys) → A39 | `src/server/` grep 2026-08-25 |
| Δ4 keyVaultReferenceIdentity | **PROVISIONALLY** — `ArmAppServiceIdentityPatcher.cs` exists + A25 applied; handler BODY never read → A40 verify-then-assert (downgraded from "already covered") | KvSecretsPopulation dir listing; A25 (`:211`) |
| Δ5 UAMI Dataverse app-user | **PARTIAL** — design.md:153 intent; in-flight agents `Wave-3E-053-H10-AppUser`/`ds8-uami-dv-appuser` → A41 dedupe-first | design.md:153; active-agent roster |
| H4/H7 sentinel contract | **A30 (partial)** — A38 closes the H4 half; H7/task-142 half explicitly retained in A38's scope note | A30 (`:221`), AUTH-V4-RESPONSE sentinel item |
| KV clobber protection | **A23 applied** (skip-if-absent + never-declared) — A38 modifies the SAME manifest area; executor must not regress A23's guard | A23 (`:25,:209`) |
| never-delete rewrite | **NEW** — §6.5 resolution record (decisions/); spec edits ride A44(b) | decisions doc |

## Sequencing note — which task do these belong in?

**Split (recommended — see remediation plan §4/§7)**, NOT a 203c extension:
- **203b-2 mini-wave (dispatch now, parallel-safe)**: A36, A37, A40 (5.5h) — r1-owned bicep/verify with exact values from §10.2/§10.3; **no master-merge dependency**; same shape as applied A20/A21/A25. Sonnet-5 @ high.
- **Task 205 (new sub-phase, "auth-v4 runtime-contract integration")**: A35 (prescriptive Step 0) → A38 ∥ A42 → A39, with A41/A43/A44 parallel after A35. A38 + A42 at Fable/Opus FULL rigor (credential logic, cross-worktree consumption; auth-tagged → code-review + adr-check unconditional); rest Sonnet-5 @ high.
- **Why not 203c**: 203c's queue (A02/A03/A04/A15/A16) is L2-identity/skill flavored, needs no merge, and burying FULL-rigor credential work in a continuation bucket loses its quality gate. **Why not route to auth-v4**: §10 is addressed TO provisioning; every landing spot is r1 estate; auth-v4 retained exactly one obligation (051-E) and is COMPLETE (merged via PRs #814/#816/#817/#818; sole unmerged tip `ef61a5f5a`).
- **Critical path to 186**: A35(3h) → {A38(4h) ∥ A42(4h)} → A39(4h) = **11h serial** with A36/A37/A40/A41 in parallel lanes.

## Owner-review checklist (confirm BEFORE integrating into the master punch list)

- [ ] Numbering: confirm no A35+ rows were added by a parallel session since 2026-08-25 (punch list ended at A34 when drafted).
- [ ] Q2 (§9.2) answered or A42's escalation-gate accepted as-is (customer-owned M2 blocked until answered).
- [ ] Q9: sub `cd95fcec` tenancy shape verified (Spaarke-hosted assumed, unconfirmed) — determines whether 186 itself hits §9.2.
- [ ] §6.5 resolution record signed (decisions/adr-028-a4-integration-conflict-resolution.md) — gates A44(b) spec edits + A38's omit-contract framing.
- [x] A41 dedupe done against `Wave-3E-053-H10-AppUser` / `ds8-uami-dv-appuser` landed output (2026-08-26, task 205d) — did NOT shrink to probe-only: task 053 landed the two-row structure but the §10.4 objectid trap itself was uncovered by both dedupe sources; H10 handler code changed.
- [ ] A39's dependency on A42 accepted (ordering guard) — do NOT dispatch A39 in a parallel lane ahead of A42.
- [ ] Effort deltas: A35 3h assumes clean conflict classes per remediation plan §6 table; the `Register-EntraAppRegistrations.ps1` union merge (532→982 ours / 532→1582 master, both verified via `wc -l`) is the long pole — escalate to +2h if function-level interleaving required.
- [ ] Model-tier assignments (Fable/Opus for A38/A42) accepted per §8.5 cost posture.

---

## A38 execution record (2026-08-25, task 205a — Fable @ xhigh, FULL rigor) — ESCALATED, no code applied

`last_known_status_after_execution`: **ESCALATED — `partial-omit-set-discovered` trigger fired at POML Step 2 (grep-verify). Zero code/test edits applied (incomplete omit is worse than no change per row A38 executor obligation). A23 clobber guard grep-confirmed intact (untouched).**

### Grep-confirmed LIVE upsert-site inventory (the trigger evidence)

The remediation plan §5 item 3 carried `customer.bicep` / `Rotate-Secrets.ps1` / `listKeys()` as ⚠️ analysis-only cites ("A38/A43/A44 executors grep-confirm before editing"). Task 205a's grep-verify CONFIRMS them as live, and finds two more. Sites with NO assigned punch row are the escalation:

1. **`infrastructure/bicep/customer.bicep:705` + `:713`** — `kvSecretValues` map UNCONDITIONALLY contains `AiSearch--AdminKey: aiSearch.outputs.searchServiceAdminKey` and `ServiceBus-ConnectionString: serviceBus.outputs.serviceBusConnectionString`, feeding `kv-secrets.generated.bicep` whose `if (contains(secretValues, ...))` skip-if-absent condition therefore ALWAYS fires → both secrets are WRITTEN to the per-customer KV on EVERY Model 2 `customer.bicep` deploy (i.e. inside the provisioning DAG itself, at H2a — BEFORE H4 even runs). customer.bicep:676-680's own comment: this module call "is the actual value-writer H4 depends on". A manifest-only A38 omit would report "omitted/migrated" in the H4 run record while the SAME RUN's H2a step had already seeded both secrets — the exact "falsely reports migrated" failure the trigger names.
2. **`scripts/Rotate-Secrets.ps1:590` (platform vault) + `:663` (per-customer vaults)** — rotation resurrects `ServiceBus-ConnectionString` with no secret-free gate. (`BFF-API-ClientSecret` rotation is already a retired no-op at `:604-619` ✅.)
3. **`scripts/Seed-ProductionKeyVault.ps1:123` (`ServiceBus-ConnectionString`) + `:178` (`AiSearch--AdminKey`)** — operator seeding writes, no secret-free gate. (`BFF-API-ClientSecret` seeding already REMOVED 2026-08-24 per auth-v4 task 033 ✅.)
4. **`scripts/Provision-Customer.ps1:533`** — legacy 13-step orchestrator still writes `ServiceBus-ConnectionString`.
5. (`infrastructure/byok/main.bicep:539` declares `AiSearch--AdminKey` on the BYOK variant — inventory note, lower priority.)

In-scope-elsewhere sites (NOT part of the escalation): `GraphAppRegistrationProvisioner.cs:97` H3 `BFF-API-ClientSecret` write = **A42** (running in parallel); `Deploy-AllIndexes.ps1` fallback = **A43**; `appsettings.template.json` SB KV-ref = **A44(a)**/051-E; `Seed-CustomerKeyVault.generated.ps1` = correctly SKIPs/REFUSEs ✅; `kv-secrets.generated.bicep:26` never-declares the never-delete pair (A23 ✅).

Manifest-source facts for the re-scope: the LIVE `IKvSecretManifest` is **`FileKvSecretManifest`** (Worker/Program.cs:449) over embedded `scripts/canonical-secret-catalog/manifest.yaml` — `BFF-API-ClientSecret` (from-existing-kv, :118) flows via per-tenant H4; `ServiceBus-ConnectionString` (:255) + `AiSearch--AdminKey` (:433) are `from-shared-service` → **H4-shared** (extract + write + rotate-on-drift, SHARED vault). `StaticKvSecretManifest` (rows :69/:74) is interim/revert-only. NOTE: `FileKvSecretManifest.cs:151` BINDING invariant refuses to serve entries unless `BFF-API-ClientSecret` is present with `never_delete: true` — the omit design must filter SERVED entries, not delete the yaml row. `H4KvSecretsPopulationHandler` already carries the FR-39 run-parameter omit seam (`ficOmitSecretNames` → `KvSecretWriteRequest.OmitCanonicalNames` → `KvSecretWriteAction.Omitted`, task 126) — the manifest half of A38 should extend THAT seam, not invent a parallel one.

### Proposed complete omit-set re-scope (for owner approval)

- **A38a (= original A38 landing spot, 4h)**: manifest/handler omit branch + positive migration marker (KV tag `spaarke-secret-free-identity` + `sprk_dataverseenvironment` state field) + Model 2 N-vault fleet-consistency detector + tests, extending the existing FR-39 `ficOmitSecretNames`/`OmitCanonicalNames` seam + `KvSecretsPopulationOptions`. Dispatch ONLY together with A38b (same wave) — A38a alone re-opens the false-migrated trap.
- **A38b (NEW, ~2h, Bicep)**: gate `customer.bicep`'s `kvSecretValues` entries for `AiSearch--AdminKey` + `ServiceBus-ConnectionString` behind a `requireSecretFreeIdentity` bool param (default false; H2a passes it from the same run state that drives A38a), so the generated module's skip-if-absent guard becomes effective on secret-free stamps. Regenerate `customer.json`.
- **A38c (NEW, ~1.5h, scripts)**: marker pre-check gate (read KV tag; refuse/skip + loud message) in `Rotate-Secrets.ps1` (SB rotation branches), `Seed-ProductionKeyVault.ps1` (SB + admin-key writes), `Provision-Customer.ps1:533` (or deprecation banner — superseded by L2). Same FAIL-LOUD shape as A43.
- **A44.5 (NEW — the H7/task-142 half of A30's sentinel contract; could not be closed inside A38's 4h/`KvSecretsPopulation/**` scope)**: H7 (`H7DataverseEnvVarValuesHandler`) + H6 CONSUME `BFF-API-ClientSecret` as their own client-credentials auth; task 142 wired `EnvVarValues__ClientSecret` → KV-ref UNCONDITIONALLY in `modules/controlplane-worker-app-service.bicep` and `EnvVarValuesOptions.Validate()` fail-fasts the whole L2 Worker boot when it is empty. On a secret-free env (prong 1: never-create/seed/restore `BFF-API-ClientSecret`) the Worker therefore boot-loops — or invites a sentinel value into the slot, the exact §9.1 pathology. Scope: FR-39-style credential-selection seam for H7/H6's own Dataverse auth (MI-FIC ordered credential w/ client-secret fallback, mirroring master's `DataverseServiceClientImpl` migration), + make the Bicep KV-ref conditional, + relax `Validate()` under the secret-free mode. Substantially different surface (L2 Worker auth + Bicep), own POML + reviewer attention. Est 4h. Deps: A35 (landed), A42.

| # | lesson-id | title | landing-spot | effort-h | deps | last-known-status | source |
|---|---|---|---|---|---|---|---|
| A38c | d1-d2-trap-operator-script-gates | Operator-script marker pre-check gates closing the operator-invoked half of A38's omit-set (A38a = manifest/handler; A38b = Bicep `customer.bicep`; A38c = THIS ROW — the three operator entry points): `Rotate-Secrets.ps1` SB rotation branches (platform `:591` + per-customer `:677`), `Seed-ProductionKeyVault.ps1` SB `:135` + AiSearch-admin-key `:195` seeding writes, `Provision-Customer.ps1:563` legacy-orchestrator SB write + optional deprecation banner. | scripts (`Rotate-Secrets.ps1`, `Seed-ProductionKeyVault.ps1`, `Provision-Customer.ps1`; new shared `scripts/common/Assert-SpaarkeSecretFreeGate.ps1`) | 1.5 | A35 | **applied 2026-08-26 (task 205h, Sonnet @ high, FULL rigor)** — gates landed at all 5 grep-verified call sites via one shared `Assert-SpaarkeSecretFreeGateNotTripped` helper (single source of truth, avoids 5-copy drift). FAIL-LOUD shape mirrors A43 (`Deploy-AllIndexes.ps1:610-670`): `Write-Error` naming the marker + linking the A38a omit contract (`.claude/constraints/auth.md` + punch row A38a) + `exit 7` (distinct from A43's 6/8/9 range, documented inline). Marker convention: KV tag `spaarke-secret-free-identity` — landed initially per the `a38a-marker-convention-undefined` escalation trigger's sanctioned interim (`# TODO(A38a)` placeholder, presence-only check), then **RECONCILED same-session** when A38a landed concurrently mid-execution: A38a's `ISecretFreeMarkerApplier.VaultTagName`/`ArmSecretFreeMarkerApplier` confirmed the exact key `spaarke-secret-free-identity` AND value `"true"` this row already used as its working key — tightened the check from presence-only to exact `-ieq 'true'` match, re-verified 26/26 tests green. The `sprk_dataverseenvironment.sprk_credentialmode` secondary signal remains an optional `-CredentialMode` caller-supplied pass-through (these 3 operator scripts have no Dataverse Web API client) — flagged `# TODO(A38a-followup)` for a future task to wire a live read; documented as a Warning (Path C, deferred) in this row's code-review pass, not a silent gap. No-regress confirmed (grep + test): `BFF-API-ClientSecret` no-op branch (`Rotate-Secrets.ps1:614-629`) untouched; `BFF-API-ClientSecret` remains absent from `Seed-ProductionKeyVault.ps1` writes; Redis rotation branches untouched; `Dataverse-ClientSecret` never appears in any write path across the 3 scripts (§6.5 preserved). `Provision-Customer.ps1` gained an optional deprecation banner (§5.2 L2 `/provision-environment` supersession), suppressible via `$env:SPAARKE_SUPPRESS_LEGACY_ORCHESTRATOR_BANNER`. Test coverage: no PowerShell test surface existed anywhere in the repo before this row (only Pester 3.4.0 installed, not a repo dependency) — created `tests/scripts/Auth-V4-Operator-Script-Gates.Tests.ps1` (Pester 3.4-compatible syntax so it actually runs), 26/26 passing: 7 marker-detection unit tests (incl. Model-2 N-vault + idempotency), 5 FAIL-LOUD/backwards-compat tests via child-process exit-code assertions (exit-calling code can't be tested in-process), 14 source-level regression guards (no-regress on BFF-API-ClientSecret/Redis/Dataverse-ClientSecret + gate-site presence). All 3 scripts + shared gate + test file parse cleanly (`pwsh` AST parse, 0 errors). | §10.5 trap class; A43 precedent; A38a marker contract (in-flight at 205h execution time) |
| A44.5 | a30-h7-secretfree-worker-auth | H7/task-142 half of A30 sentinel contract: L2 Worker's own `BFF-API-ClientSecret` dependency (`EnvVarValues__ClientSecret` KV-ref, unconditional per task 142; `EnvVarValuesOptions.Validate` boot fail-fast) made secret-free-compatible — credential-selection seam for H7/H6 Dataverse auth + conditional Bicep KV-ref + no sentinel ever written to the slot (§9.1) | code (`Handlers/EnvVarValues/**`, `Handlers/SolutionImport/**` shared identity, `modules/controlplane-worker-app-service.bicep`) | 4 | A35, A42 | **applied 2026-08-25 (task 205i, Fable @ xhigh, FULL rigor)** — `last_known_status_after_execution`: (a) **Files modified**: `Handlers/EnvVarValues/{H7DataverseEnvVarValuesHandler,EnvVarValuesOptions,IEnvVarValuesWriter,DataverseWebApiEnvVarValuesWriter}.cs` + `Handlers/SolutionImport/{H6SolutionImportHandler,SolutionImportOptions,ISolutionImporter,ISolutionVerifier,DataverseWebApiSolutionImporter,DataverseWebApiSolutionVerifier}.cs` + `Worker/Program.cs` (factory singleton + importer wiring) + `modules/controlplane-worker-app-service.bicep` + `platform-controlplane.bicep` (param thread-through). (b) **New files** (§11 justification in `CredentialKind.cs` header, one per component family): `Handlers/Credentials/{CredentialKind,WorkerCredentialSelectionOptions,WorkerDataverseCredentialFactory}.cs` — Azure.Identity-idiom mirror of master `DataverseServiceClientImpl` + BFF `OrderedCredentialClientProvider`/`CredentialSelectionOptions` (A35-brought): `{Section}:Credentials:Order` config contract (`ManagedIdentityFederated`\|`ClientSecret`; `KeyVaultCertificate` deliberately absent — unbuilt L2 cert estate, §9.2 "dropped, not deferred"), MI-FIC = `ClientAssertionCredential` over UAMI token for `api://AzureADTokenExchange` (≡ MSAL `ManagedIdentityClientAssertion`), `RequireSecretFreeIdentity` order-assertion (rule-6 mirror, no Dev exemption), fail-closed exhausted-chain throw whose message forbids the sentinel workaround. Documented narrowings: no probe/negative-cache/fall-through-from-MIF machinery (per-run collaborator on an always-IMDS host; MI failure = writer `AuthFailure` → §4C Resumable; strictly stronger than BFF — no silent secret downgrade possible); empty `Order` ⇒ legacy `[ClientSecret]` (backwards-compat constraint) instead of master's empty-fails-loud. A42 landed NO shared factory (H3 FIC-creation surface only) → per `a42-shared-helper-diverges`: scoped factory authored; **follow-on unification row proposed** (CustomerRunGuard `DataverseRegistryConcurrencyStore.cs:298` still ClientSecretCredential — its Bicep KV-ref IS gated here to avoid the partial-omit trap, code seam deferred; on secret-free envs `customerRunGuardEnabled` MUST stay false until it lands). (c) **Tests**: 26 new cases — 10 factory (`Handlers/Credentials/WorkerDataverseCredentialFactoryTests.cs`: secret-free MI-FIC selection, legacy fallback, transitional order, fail-closed+anti-sentinel message, invalid-chain, rule-6, **Model 1 shared-UAMI one-chain-per-env (2 tenants → same pin)** + **Model 2 per-stamp-UAMI byte-inequality pin (SF-2)**, lookup precedence, system-assigned) + 10 H7 (handler MIF-proceeds w/ null secret + explicit-secret-first still fails + 8 `Validate()` boundary cases incl. accepts-empty-under-MIF (goal c) + all fail-fast cases) + 1 H6 (MIF-proceeds) + 5 boot (`Dispatch/WorkerSecretFreeBootTests.cs` — REAL Worker composition root via `WebApplicationFactory`, secret-free fixture = exact Bicep secret-free app-setting shape → options validate + H6/H7 keyed graphs resolve; legacy-missing-secret fixture still fail-fasts; honest scope: no live MI-FIC exchange from test host per SF-4 — real proof lands post-App-Service in H13/T4/186). Preserved: all pre-existing task-142 (H7 file + `WorkerTestFactory` fixture), task-141/204a (H6 + importer/verifier files — internal `Func` seam signatures unchanged), task-130/A42 (`H3EntraAppRegHandlerTests` untouched). Suite outcome: **FULL in-repo suite GREEN — 1646 passed / 0 failed / 1 skipped (env-guarded smoke) of 1647** (`dotnet test Sprk.Provisioning.ControlPlane.Tests`, run AFTER sibling 205a/A38a's concurrent test updates landed; includes A38a's new tests). Interim: while A38a's KvSecretsPopulation test-file lag transiently broke the shared test project (4 CS7036 in THEIR files, disjoint surface), 205i pre-verified via an ISOLATED scratchpad csproj (`AssemblyName=Sprk.Provisioning.ControlPlane.Tests` for InternalsVisibleTo, real Core/Api/Worker ProjectReferences) over the 4 A44.5-touched test files + H3/A42 preservation files: 152/154 (2 fails = pre-existing repo-path-walking source tests, environmental only — both green in the final in-repo run). One preserved-test interaction found + resolved during the full run: the verifier's `Token acquisition failed:` diagnostic prefix is asserted by task-141's `VerifyAsync_TokenAcquisitionFails_ReturnsMissingAllWithAuthDiagnostic` — 205i restored the exact prefix across H6 importer/verifier + H7 writer (credential-SELECTION failures remain distinct via the factory's own "No credential could be selected …" inner message); zero preserved tests modified. (d) **Bicep**: `requireSecretFreeIdentity bool = false` param (name grep-matches A38b's `customer.bicep:87` — verified aligned live); appSettings = `concat(base, flag ? secretFreeCredentialAppSettings : legacyClientSecretAppSettings)`; ALL THREE `BFF-API-ClientSecret` KV-refs gated together (partial gating would re-open A38's false-migrated trap); `platform-controlplane.bicep` threads the param; **`platform-controlplane.json` regenerated** (bicep 0.46.1, grep-confirmed conditional shape); `customer.json` NOT regenerated by this row (worker module not referenced by customer.bicep — its concurrent diff is A38b's). (e) **`Validate()` boundary**: empty `ClientSecret` accepted IFF effective chain primary = MI-FIC (§9.1 empty-is-the-signal); legacy/unconfigured chain preserves task-142 boot fail-fast byte-for-byte; unknown kind / duplicate / RSFI×secret-listed / RSFI×no-order all fail-fast; H6 `Validate()` gains chain-SHAPE checks only (missing secret stays runtime-Resumable `MissingClientSecret` per task-204a's §4C classification — lifecycle difference preserved). (f) **Deviations**: none from row spec; grep criterion 2 note — `AuthType=ClientSecret` literal never existed in L2 (the equivalent `ClientSecretCredential` construction is now confined to the factory fallback branch). Quality gates: code-review + adr-check self-pass executed (2 findings fixed: obsolete `ManagedIdentityCredential(string)` ctor → `ManagedIdentityId`; ADR-038-banned bare DI-registration test removed); ADR-028 A4 compliant via the approved §6.5 record's prong-3 pluggability carve-out. | A30 (H7 half); AUTH-V4-RESPONSE sentinel item; §9.1; task-142 wiring |

### A23 / no-regress confirmation

`skip-if-absent` (`Invoke-CatalogGenerator.ps1:1088`, `kv-secrets.generated.bicep:35`) + `never-declared` (`kv-secrets.generated.bicep:26`, `Invoke-CatalogGenerator.ps1:1079`) both present; no files under `KvSecretsPopulation/**`, `scripts/**`, or `infrastructure/**` were modified by task 205a. Full site table: task-205a executor scratchpad (`a38-site-inventory.md`, transcript-attached).

## A38a execution record (2026-08-25, task 205a second pass — Fable @ xhigh, FULL rigor) — APPLIED, paired sibling of A38b

`last_known_status_after_execution`: **APPLIED — manifest half of the A38 re-scope landed in working tree; DID NOT `git commit` per BINDING sequencing constraint (main session commits the A38a + A38b bundle atomically). One escalation fired + surfaced (see below); zero constraints violated.**

- **(a) Source sites edited (line anchors post-edit)**: `FileKvSecretManifest.cs` — new `SecretFreeIdentityOmitTargets` public static set (`:96-101` region), ctor gains `IOptions<KvSecretsPopulationOptions>` (DI-auto-resolved; Worker registration line unchanged), served-entry filter applied INSIDE `ParseYaml` after the entry-emission loop + BEFORE the determinism sort, logging actually-removed names; new `ParseYamlForTest` internal seam so the invariant's refusal branches are unit-testable against synthetic yaml. `H4KvSecretsPopulationHandler.cs` — three A38a targets unioned into the EXISTING task-126 `omitCanonicalNames` HashSet (post-edit `:390`; parse still keyed by `FicOmitSecretNamesParameterKey` at `:179` — anchor UNCHANGED) when `RequireSecretFreeIdentity && !SecretFreeIdentityRollback`; new step (10.5) applies the positive marker via `ISecretFreeMarkerApplier` before MarkComplete (failure ⇒ Resumable `kvsecrets-secret-free-marker-apply-failed`); success log gains `omitted-fr39`/`secretFree` fields. `H4SharedKvSecretsPopulationHandler.cs` — h4-shared-omit-path-missing trigger remedied by ADDING the mirror seam: same `ficOmitSecretNames` parameter key const + omit-set build + union; per-entry loop checks omit FIRST (no parse/extract/read/write for omitted entries; new `SharedEntryAction.Omitted`); marker step (8.5) on the shared vault; summary log gains omitted count. `KvSecretsPopulationOptions.cs` — `RequireSecretFreeIdentity` (default false) + `SecretFreeIdentityRollback` (Q3 Path A, default false). Both rejection-code files gain `SecretFreeMarkerApplyFailed`. `Worker/Program.cs` — unconditional `ISecretFreeMarkerApplier` singleton (ADR-032: option gates behavior, never the DI graph). New files (§11 justifications in headers): `ISecretFreeMarkerApplier.cs` (seam + `SecretFreeMarker` constants: tag `spaarke-secret-free-identity`=`true`, mode `secret-free` — 205h/A38c grep-reconciled against these same-session), `ArmSecretFreeMarkerApplier.cs` (tag half check-then-apply via ArmClient `GenericResource` — NO new package; registry half via registry-client extension), `SecretFreeMarkerConsistencyDetector.cs` (§5.3 detector — pure, Warning + Mixed failure record, never throws). Registry extension (CLAUDE.md §11 extend-over-duplicate): `IDataverseEnvironmentRegistryClient` gains `UpdateCredentialModeAsync` + `RegistryCredentialModeUpdate` (default-interface-method body FAIL-LOUDs — chosen so the 6 pre-A38a fakes in H05/H13/updater/seam test files compile UNCHANGED mid-parallel-wave); real client implements the PATCH (`sprk_credentialmode` as JSON STRING — single-line-text column, unlike `sprk_setupstatus`'s option-set integer; missing-column 400 diagnostic names the schema prerequisite); Null client overrides with ADR-032 P2 quiet-Success + WARN.
- **(b) StaticKvSecretManifest.cs CONFIRMED UNCHANGED** — `git diff HEAD` empty (revert-only interim preserved). H4's omit-union is deliberate defense-in-depth for exactly that emergency DI-revert (a reverted static manifest serves the targets unfiltered; the writer still marks them `Omitted`).
- **(c) Marker mechanism landed** — BOTH halves, per §9.1 (marker outside the credential slots): (1) KV tag `spaarke-secret-free-identity=true`, check-then-apply idempotent (`IsVaultTagAlreadyApplied` pure-tested; 2nd invocation = read-only no-op); (2) `sprk_dataverseenvironment.sprk_credentialmode=secret-free` via registry client — VALUE-idempotent PATCH (re-run yields same state). Documented micro-deviation from the constraint's "check-then-apply" parenthetical for the registry half: pre-checking would require `$select=sprk_credentialmode` on `LookupByTenantIdAsync`, which HTTP-400s wherever the column doesn't exist yet — breaking H0.5's LIVE read path; value-idempotent PATCH meets the constraint's stated goal. NOT applied under Q3 rollback (rolled-back env is not secret-free). Marker failure = Resumable FAIL-LOUD (unmarked secret-free vault IS the §5.3 gap). **SCHEMA PREREQUISITE (coord item)**: `sprk_credentialmode` single-line-text column must be created on the admin env's `sprk_dataverseenvironment` BEFORE any environment sets `RequireSecretFreeIdentity=true` — grep-verified absent from all schema today; missing column fail-louds with a diagnostic naming it.
- **(d) Model 1 vs Model 2 verified** — Model 1 (shared vault): H4-shared tags `sharedKeyVaultName` once per env run (RG assumed = `sourceResourceGroupName` per model1-shared stack topology; wrong RG = loud ARM-404 Resumable, never silent — optional `sharedKeyVaultResourceGroupName` param proposed as follow-up). Model 2 (N per-customer vaults): the marker rides the EXISTING per-customer DISPATCH fan-out — each customer's H4 run tags its own vault; **there is no N-vault loop inside any single handler run by design** (documented deviation from the POML's mental model, which placed the iteration inside H4-shared; manifest.yaml routes SB-conn/:255 + admin-key/:433 as from-shared-service → the SHARED vault only; per-customer Model 2 vaults receive those two secrets ONLY from H2a/customer.bicep — which is exactly A38b's gate, re-proving the bundle constraint). N=3 fan-out uniformity unit-tested (A38a9: 3 customers/vaults → marker exactly once per vault, uniform).
- **(e) A23 clobber guard CONFIRMED intact post-edit** — `skip-if-absent` (`Invoke-CatalogGenerator.ps1:1088`, `kv-secrets.generated.bicep:35`) + `never-declared` (`Invoke-CatalogGenerator.ps1:1079`, `kv-secrets.generated.bicep:26`) re-grepped after all edits; zero A23 test-name churn (no A23 surface touched by A38a).
- **(f) :151 BINDING invariant PRESERVED** — check block intact + still refuses to serve ANY entries unless both never-delete rows present with `never_delete=true`; runs against the RAW yaml document; the A38a filter is provably DOWNSTREAM (synthetic-yaml tests pin the ordering: missing-row → Failure even with filter active; rows-present + secret-free → Success with 3 targets absent from SERVED list). Line anchor moved `:151` → `:219` purely from header/constant/ctor additions above it — behavior byte-equivalent. manifest.yaml: empty diff (all three rows + classifications unchanged).
- **(g) FR-39 seam EXTENDED, not parallel-invented** — `FicOmitSecretNamesParameterKey` still `:179`; union feeds the SAME `KvSecretWriteRequest.OmitCanonicalNames` → `KvSecretWriteAction.Omitted` path (writer untouched); H4-shared's new mirror uses the SAME parameter key string so one coordinated run parameter drives both handlers. Zero new parameter keys; the only new option surface is `RequireSecretFreeIdentity`/`SecretFreeIdentityRollback` as sanctioned by the POML. Operator `ficOmitSecretNames` path regression-tested (union coexists).
- **(h) Test coverage** — 25 new/extended cases, all green: FileKvSecretManifestTests +9 (filter on/off/rollback vs real embedded manifest with exact −3 count; Dataverse-ClientSecret in all 3 branches; omit-target-set shape; invariant-fires-with-filter-active on synthetic yaml ×2; filter-downstream-of-invariant ordering), H4 tests +10 (seam union; default-empty; operator-param union incl. count=5; rollback no-omit + no-marker; marker applied w/ vault+tenant; marker skipped when off; marker failure → Resumable + code; Level-3 no-op skips marker; Applied(alreadyPresent) still Success; Model-2 N=3 fan-out uniformity), H4-shared tests +6 (SB-conn + admin-key neither extracted nor written under secret-free while other 4 shared entries still write — proves handler-level omit holds even under a non-filtering manifest; rollback restores 6; operator param honored; marker on shared vault; marker failure → `h4shared-` code; default no-regression), new `SecretFreeMarkerTests.cs` (detector mixed 2+1 → Warning + Mixed record; uniform all/none/empty; tag-idempotency pure fn ×3; marker-constants contract pin consumed by A38c gates; credential-mode patch-body wire shape ×2). **Full in-repo suite: `dotnet build` Core + Worker clean (0 warn/0 err); `dotnet test` 1646 passed / 0 failed / 1 pre-existing env-guarded skip — this run INCLUDES 205i/A44.5's 26 new tests + resolves the CS7036 test-lag their record flagged (the wave-bundle re-run the main session was asked to perform is hereby done and green).** Quality gates run (code-review + adr-check, unconditional): 0 Critical, 0 ADR violations; 1 finding fixed in-pass (filter log now names actually-removed entries); 2 Warnings accepted + documented (registry-half blind-idempotent PATCH rationale above; H4-shared marker RG assumption).
- **(i) Commit-bundle status: READY** — working tree carries A38a (this record) + A38b (205g, applied) + A38c (205h, applied) + A44.5 (205i, applied); executor ran NO `git commit` (HEAD unchanged at `cc6ecb6e4`). **Main session: commit A38a + A38b atomically (A38c/A44.5 may ride the same bundle — all four verified co-green in the 1646-test run).**
- **ESCALATION RESOLVED 2026-08-26 (owner disposition + main-session fold-in)**: `site-inventory-drifted` — Step-2 sweep found a 6th `ServiceBus-ConnectionString` upsert site beyond the peer's declared-complete 5-site inventory: **`scripts/Setup-OfficeServiceBus.ps1:172`** (`az keyvault secret set`, NO secret-free gate). Assessment: legacy dev-bootstrap one-off (defaults `spaarke-servicebus-dev` / legacy vault `spaarke-spekvcert` / RG `SharePointEmbedded` / `Environment=Development` tags) — NOT in the provisioning DAG, non-canonical vault by default; exactly the A38c operator-script class. **Owner diagnosis via live `az` queries (2026-08-26)** confirmed script is 80% dead: SB namespace `spaarke-servicebus-dev` LIVE and Actively-Used by current dev BFF via MI (`ServiceBus__FullyQualifiedNamespace`, no `ServiceBus__ConnectionString` app setting anywhere per auth-v4 task 051); 3 queues (`office-upload-finalization`/`office-profile`/`office-indexing`) LIVE + hardcoded `const string QueueName` in `src/server/api/Sprk.Bff.Api/Workers/Office/*.cs` (`UploadFinalizationWorker`/`ProfileSummaryWorker`/`IndexingWorkerHostedService`); Step-4 KV secret `ServiceBus-ConnectionString` in `spaarke-spekvcert` DELETED (`az keyvault secret show` returns `SecretNotFound` — auth-v4 task 033 removal confirmed); Step-5 App Service `spe-api-dev-67e2xz` in `spe-infrastructure-westus2` DELETED (`az webapp show` returns `ResourceNotFound`). **Owner chose disposition (2)**: retain script + add A38c-style gate + deprecation banner (path (a) equivalent). Main session applied 2026-08-26: (i) added ~50-line deprecation banner to file header documenting the diagnosis + naming canonical replacements (Bicep `service-bus.bicep`, auth-v4 task 051 MI migration, `ServiceBusOptions.cs:19-25`); (ii) dot-sourced `scripts/common/Assert-SpaarkeSecretFreeGate.ps1` (peer 205h's shared helper — single source of truth for marker convention `spaarke-secret-free-identity=true`); (iii) inserted `Assert-SpaarkeSecretFreeGateNotTripped -SecretName "ServiceBus-ConnectionString" -KeyVaultName $KeyVaultName` immediately before Step 4's `az keyvault secret set`. Script parses cleanly (`pwsh` AST, 0 errors). Follow-up-row (4) `Setup-OfficeServiceBus.ps1 disposition` from A38a's proposed follow-ups list is now CLOSED.
- **Follow-up rows proposed (for task-202-punch-list integration)**: (1) wire `SecretFreeMarkerConsistencyDetector` into the fleet-observation surface that actually enumerates N vaults (T8-probe / H13-aggregation family, task-186 acceptance) — the detector class + tests landed; only the runtime fleet enumeration is deferred (per the `fleet-consistency-detector-cannot-land-in-budget` trigger's file-don't-silently-defer rule); (2) `sprk_credentialmode` column creation on admin env (schema prerequisite, item (c)); (3) optional `sharedKeyVaultResourceGroupName` H4-shared parameter (item (d)); (4) `Setup-OfficeServiceBus.ps1` disposition (escalation above); (5) A38c's `-CredentialMode` pass-through live-read wiring (205h's own TODO(A38a-followup) — now unblocked since `UpdateCredentialModeAsync`/`sprk_credentialmode` contract exists).

## A38b execution record (2026-08-25, task 205g — Sonnet @ high, FULL rigor) — APPLIED, paired sibling of A38a

`last_known_status_after_execution`: **APPLIED — `infrastructure/bicep/customer.bicep`'s `kvSecretValues` map (`:704-727` post-edit) gates `AiSearch--AdminKey` + `ServiceBus-ConnectionString` behind a new `requireSecretFreeIdentity` bool param (default `false`; backwards compat preserved bit-identical to pre-A38b via `union(kvSecretValuesBase, kvSecretValuesGated)` where `kvSecretValuesGated` is `{}` when `true`, never a sentinel). `customer.json` regenerated via `az bicep build` (0 errors, 0 new warnings vs pre-change baseline — same 7 pre-existing warnings, shifted line numbers only). H2a wiring: **verified-absent, ADDED per Step 7** (trivial 3-file mirror of the `signalrEnabled` template — `H2aBicepInfraDeployHandler.cs` const `RequireSecretFreeIdentityParameterKey` + `TryGetNonEmpty(...)` extraction + `BicepDeployRequest` field; `IBicepDeployRunner.cs` record gains `bool RequireSecretFreeIdentity = false` (default keeps existing callers compiling unchanged); `ArmDeploymentRunner.cs`'s `BuildParametersPayload` — the actual ARM-wire site — adds `["requireSecretFreeIdentity"] = new { value = request.RequireSecretFreeIdentity }`, shared by `ArmWhatIfDriftDetector` automatically). A23 clobber guard (`contains(secretValues, ...)` + `never-declared`) grep-confirmed intact — `kv-secrets.generated.bicep` untouched (zero diff). `stacks/model1-shared.bicep` untouched (zero diff). All 8 non-gated `kvSecretValues` entries grep-confirmed unchanged. Tests: 2 new theories + 1 new fact added to `H2aBicepInfraDeployHandlerTests.cs` mirroring the T14/T15 `signalrEnabled` pattern (`RequireSecretFreeIdentityFlag_FlowsToRunnerRequest` + `RequireSecretFreeIdentityFlag_AbsentParameter_DefaultsFalse`); full suite 1584/1585 passed (1 pre-existing unrelated skip), `dotnet build` clean. Executor did NOT commit — MUST land in the same commit bundle as 205a A38a per binding sequencing constraint; without this gate, A38a's manifest-omit alone re-opens the false-migrated trap (H2a would still unconditionally re-seed both secrets on every Model 2 deploy).

## A41 execution record (2026-08-26, task 205d — Sonnet @ high, FULL rigor) — APPLIED, no scope-collapse

`last_known_status_after_execution`: **APPLIED — dual-row structure was already landed by task 053 (Wave-3E-053-H10-AppUser); the §10.4 objectid trap itself was NOT closed by either dedupe source, so H10 handler + T2 probe code edits were required. Scope did NOT collapse to probe-only (the −2h reduction does NOT apply) — H10 handler code changed. Zero escalation triggers fired. Executor did NOT commit.**

- **(a) Dedupe verdict (Step 2, BINDING)**: `Wave-3E-053-H10-AppUser` = landed task 053 (`TASK-INDEX.md` ✅, "Implement H10 Dataverse App User + Graph app-role parity"). Its `H10DataverseAppUserGraphParityHandler.cs` DID already create BOTH systemuser rows (BFF app-reg via step 9, UAMI via step 10) — the "two rows" half of Δ5 was genuinely landed. `ds8-uami-dv-appuser` = the design-study doc `notes/design-study-ds8-uami-dv-appuser-maturity.md` (read in full) — it answers a DIFFERENT question (whether L2's OWN identity should authenticate to the ADMIN Dataverse env via UAMI-as-app-user, Path X vs Path Y) and is SILENT on the §10.4 objectid trap; its own worked Web API idiom (§4 point 2) shows only `{"applicationid": "<uamiClientId>", "businessunitid@odata.bind": "..."}` — no `azureactivedirectoryobjectid` at all, i.e. it does not cover the trap either. **Neither dedupe source closed the §10.4 trap** — `DataverseWebApiAppUserCreator.CreateSystemUserAsync` (pre-edit) POSTed only `applicationid`, relying entirely on Dataverse's own AAD auto-resolution for `azureactivedirectoryobjectid` on BOTH rows, including the UAMI row. **Scope did NOT collapse to probe-only** — the −2h reduction from the punch row's effort annotation does not apply; H10 handler code required a real edit.
- **(b) T2 probe read (Step 3, BINDING §5 SF-9 caveat)**: read `DataverseAppUserPairT2Probe.cs` in full BEFORE editing. Confirmed count-only, NOT already byte-equality — the exact test-message fragments `"OK (count=1)"` / `"COUNT={n}"` quoted in the punch row and remediation plan §5 SF-9 are verbatim in `BuildPairMismatchDiagnostic`. No pre-existing byte-equality assertion existed anywhere in the T2 path (handler's own inline check is also count-only, by design — it is the CREATOR's belief, not the independent H13 re-verification).
- **(c)/(e)/(f) H10 handler dual-row apply**: `DataverseAppUserCreationRequest` gained an optional trailing `AzureActiveDirectoryObjectId` field (default `null`, source-compatible with all pre-existing single-positional-arg call sites). `DataverseWebApiAppUserCreator.CreateSystemUserAsync` now writes `azureactivedirectoryobjectid` in the SAME POST as `applicationid` whenever the caller supplies it (GUID-shape-validated, same defense-in-depth as the existing `ApplicationId` check) — never a follow-up PATCH. `H10DataverseAppUserGraphParityHandler`'s UAMI creation call now passes `AzureActiveDirectoryObjectId: uamiObjectId` (`InterStepState.MiObjectId`, the UAMI's real principalId) — NEVER `uamiClientId`. The BFF app-reg row deliberately does NOT set this field (Dataverse's applicationid-only auto-resolution is reliable for a standard app registration; only the Managed Identity row needed the explicit set). Model 1 / Model 2 code-path assertions added as file-header doc comments (H10 is deliberately tenancy-model-agnostic — the shape is established upstream by H3/H2a and carried in InterStepState; a `TenancyModel` branch inside H10 itself would be a wrong pivot, duplicating a decision already encoded) citing §5.1 Reading 1 (Model 1) and `GraphAppRegistrationProvisioner.cs:547-557` (Model 2, task 130).
- **(d) T2 probe byte-equality extension**: `IDataverseAppUserVerifier.Verified` gained an optional trailing `AzureActiveDirectoryObjectId` field (source-compatible with all 18 pre-existing single-arg construction call sites across 2 test files + 1 production file — grep-verified before the edit). `DataverseWebApiAppUserVerifier` now selects + returns `azureactivedirectoryobjectid`. `TrapVerificationRequest` (the shared 6-sibling-probe record) gained an optional trailing `UamiObjectId` field — unlike T3's own file-header precedent ("modifying that shared record is a coordinating change" — T3 needed a NEW Graph lookup to resolve its equivalent value), this value is ALREADY resolved at H13's call site (`InterStepState.MiObjectId`, the same field every other handler reads) so threading it through as an ADDITIVE optional default-empty field is zero-cost for the other 5 sibling probes (all construct `TrapVerificationRequest` via named arguments; grep-confirmed before the edit). `H13E2EAcceptanceGateHandler` now supplies `UamiObjectId: uamiObjectId`. `DataverseAppUserPairT2Probe` now byte-compares the UAMI half's observed `azureactivedirectoryobjectid` against the expected principalId when supplied — PASS distinguishes `"byte-equality verified"` from the backward-compatible `"count only — DEGRADED"` case (when `UamiObjectId` is empty) via distinct log messages; a mismatch (including the clientId-in-objectid trap and a null-observed-value case) returns `Failed` with a diagnostic naming §10.4 + the observed/expected GUIDs + (when the observed value literally equals the UAMI clientId) an explicit "single most-missed item" trap-recognition hint, plus a pointer to the runbook §12.7 recovery recipe.
- **(g)/(h)/(i) design.md edits**: `:61` D3 — replaced "Managed Identity" in Model 1's per-customer-dedicated list with the explicit shared-UAMI clause (`sprk-{env}-shared-bff-uami`, single per environment, task-029 slot-swap fix + I6 code-level isolation per FR-40 + task 130); verified against `infrastructure/bicep/stacks/model1-shared.bicep:116-117` (`sharedBffUamiName` param = `sprk-${environment}-shared-bff-uami`) BEFORE editing per Q8's own recommendation — wording-drift reading (a) confirmed, no further post-2026-08-25 drift found. `:153` H10 row — removed the stale "10/14 GUIDs null" text; cites `GraphAppRoles.cs` as source of truth and states its ACTUAL current state (verified by reading the file's own remarks): all 15 role entries have populated `AppRoleId` GUIDs (14 by task 005 2026-08-17 + 1 by task 144 2026-08-20) — i.e. the figure has moved past the punch row's drafted "11 null + 3 filled" text (203c A16 territory is not in play; the catalog is fully populated already). Added per-model identity-source clauses. `:~658` Naming Standards table — added a Model 1 row for `sprk-{env}-shared-bff-uami` (verified present as row alongside, and RELABELED the existing row "UAMI — Model 2 (dedicated)" for table clarity) — the pre-existing Model 2 `mi-spaarke-{customerId}-{env}` row was confirmed present before editing (Q8(b)'s STOP-if-absent condition did not fire).
- **(j) Runbook**: added `docs/guides/SPAARKE-CUSTOMER-DEPLOYMENT-GUIDE.md` §12.7 — dual-row contract, trap-recognition symptom (401 despite passing count-check), Web-API-PATCH recovery recipe, explicit non-reintroduction note for the retired never-delete rationale (superseded 2026-08-24 by ADR-028 E-3 closure) per the constraint's guard.
- **(k) Tests**: `H10DataverseAppUserGraphParityHandlerTests.AC1_HappyPath...` extended to assert the UAMI creation request's `AzureActiveDirectoryObjectId == UamiObjectId` (never `UamiClientId`) and the BFF row's is `null`. `DataverseAppUserPairT2ProbeTests` gained 4 new cases (T12): byte-equality match → Passed + "byte-equality verified" log; byte-equality mismatch with the observed value EQUAL to the UAMI clientId (the literal trap shape) → Failed naming "§10.4" + "single most-missed item"; observed-null → Failed; `UamiObjectId` not supplied → Passed (DEGRADED) + distinguishing log. New `CapturingLogger` (plain `ILogger<T>` recorder, ADR-038-compliant, parity with `H10SeamsSmokeTests.cs`'s existing pattern) added for the two log-message-distinguishing assertions.
- **(l) Build/test verification**: `dotnet build src/server/services/Sprk.Provisioning.ControlPlane.Tests/...` — 0 errors/0 warnings. Targeted run (T2 probe + H10 handler + H13 handler tests) — 79/79 passed. Full `Sprk.Provisioning.ControlPlane.Tests` suite — 1658 passed / 10 failed / 1 skipped of 1669; the 10 failures are ALL in `FilePerEnvSettingsManifestTests`/`H4bBulkAppSettingsHandlerTests` — files modified by a DIFFERENT concurrent in-flight task (205c / A39, `git status` confirms those files are dirty from that task, not this one) — unrelated to A41, not fixed here (out of scope; would be main-session-coordination territory if still failing when 205c lands). `dotnet build src/server/api/Sprk.Bff.Api/` — 0 errors/0 warnings (NFR-01 publish-size verification correctly NOT required — A41 touches zero BFF-consumed shared code; determination re-confirmed).
- **(m) Effort**: actual ≈3h against the 3.5h estimate (no scope-collapse discount applied, since the objectid-trap code edit was the substantive part of the estimate all along — the punch row's "may collapse to probe-only" framing anticipated dedupe covering the WHOLE Δ5 contract including the trap; it only covered the row-existence half). No commit made — main session handles commit after review.
