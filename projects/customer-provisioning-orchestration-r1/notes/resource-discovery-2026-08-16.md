# Resource Discovery — customer-provisioning-orchestration-r1 (2026-08-16)

> **Generated by** `/project-pipeline` Step 2 via Explore agent (subagent_type=Explore, search-breadth=very thorough)
> **Written by** main session (Explore agent lacks Write tool)
> **Purpose**: Comprehensive enumeration of ADRs, patterns, constraints, canonical implementations, and gaps that the 60–90 task POMLs reference. Verified against actual repo state 2026-08-16 (post-master-merge commit `be3f12bb6`).

---

## 1. ADRs (with 1-line why-relevant)

**Two ADR directories exist and BOTH are canonical for different ADRs**:
- `docs/adr/` — 44 files; contains 001–023, 026–048 (missing 024, 025, 028, 049, 050)
- `.claude/adr/` — 47 files; contains 001–050 EXCEPT 035, 038 (missing) — includes 028

**Tasks MUST reference the correct path** — spec §5.5 and §14A ADR quotes use `docs/adr/` for ADR-038 but this repo also cross-references `.claude/adr/ADR-028-spaarke-auth-architecture.md`. Suggest tasks preferentially point to `docs/adr/` where the file exists there and fall back to `.claude/adr/` for ADR-024, 025, 028, 049, 050.

- **ADR-004**: `docs/adr/ADR-004-async-job-contract.md` — L1 handler contract; MUST implement `IJobHandler`; one message → one handler → one outcome; 3-level idempotency (Service Bus MessageId dedup + Redis `IdempotencyService` + Dataverse alt-key upsert). Directly governs H0–H14 handler shape.
- **ADR-010**: `docs/adr/ADR-010-di-minimalism.md` — BFF DI ceiling; provisioning handlers register in L2 control-plane, NOT BFF. Bounds H0.5 endpoint additions to `Onboarding/Endpoints/**` folder only.
- **ADR-013**: `docs/adr/ADR-013-ai-architecture.md` — H0.5 endpoint MUST NOT inject `IActionResolver`/`IActionRunner` directly; use `Services/Ai/PublicContracts/` facade if AI capability needed at consent-callback.
- **ADR-014**: `docs/adr/ADR-014-ai-caching-and-reuse-policy.md` — `spaarke-session-files` index MUST carry canonical `tenantId + sessionId` dual-filter invariant. FR-29 (I2) strengthens this to `tenantId` on ALL indexes.
- **ADR-017**: `docs/adr/ADR-017-async-job-status-and-persistence.md` — per-handler job status is one concern; multi-phase ProvisioningRun in Cosmos DB is a different concern (§5.3). Both stores kept separate.
- **ADR-020**: `docs/adr/ADR-020-versioning-strategy-apis-jobs-client-packages.md` — pins model deployment versions (`gpt-4o` 2024-08-06, `gpt-4o-mini` 2024-07-18, `text-embedding-3-large` v1) per §7.4.
- **ADR-027**: `docs/adr/ADR-027-subscription-isolation-and-dataverse-solution-management.md` — subscription-per-customer isolation + Model 2 default; Model 1 shared-tier is a documented exception (Path A per §3A A1).
- **ADR-028**: `.claude/adr/ADR-028-spaarke-auth-architecture.md` — 21 MUSTs bind H4 KV secrets population, UAMI RBAC assignments, `keyVaultReferenceIdentity` PATCH. E-2 fallback: `AzureOpenAI__ApiKey` KV ref when MI auth on `kind=AIServices` unavailable.
- **ADR-032**: `docs/adr/ADR-032-bff-nullobject-kill-switch.md` — optional SignalR (`Notifications:SignalRSpine:Enabled`) feature-gated via ADR-032 P1/P2/P3 Null-Object pattern; any conditional L2 service same.
- **ADR-034**: `docs/adr/ADR-034-user-record-membership.md` — optional per-customer SignalR resource aligns with realtime pattern.
- **ADR-036**: `docs/adr/ADR-036-background-job-infrastructure.md` — background-job infrastructure (Service Bus + `IJobHandler` + Redis idempotency); L2 reuses this stack.
- **ADR-038**: `docs/adr/ADR-038-testing-strategy.md` — integration-heavy pyramid; test-modifying tasks trigger unconditional `code-review + adr-check` per root CLAUDE.md §8; 5 new ArchTests (I1–I5) sequence into this pattern.
- **ADR-039**: `docs/adr/ADR-039-grounded-execution-closed-catalogs.md` — H12a AI seed chain ends at `playbook consumers` (single AI routing surface); `spaarke-playbook-embeddings` index retired.
- **ADR-044**: `.claude/adr/ADR-044-dataverse-guid-canonicalization.md` — Registry key patterns for new columns (`sprk_currentrunid`, `sprk_tenantid`, `sprk_bffversion`, `sprk_solutionversion`).

## 2. Constraints (`.claude/constraints/**`)

**15 files under `.claude/constraints/`**. Load these based on task type:

- `.claude/constraints/bff-extensions.md` — **BINDING** governance for any BFF PR (H0.5 endpoint, `DemoExpirationService` refactor, `GraphAppRoles.cs` completion). References ADR-001/007/008/010/013/029. §§F.1–F.3 = asymmetric-registration Tier 1.5 anti-pattern + Fixture-Config-FIRST inspection + Empirical-Reproduction-FIRST protocol.
- `.claude/constraints/azure-deployment.md` — Azure App Service config + deployment safety; publish baseline 44.96 MB / 60 MB ceiling (already reflects net10 rebaseline). Load on H4/H9/H2a tasks.
- `.claude/constraints/testing.md` — .NET unit + integration testing; ADR-038 operational standard; load on all 5 new ArchTests (I1–I5) + test-modifying tasks.
- `.claude/constraints/jobs.md` — background jobs / async processing (ADR-004 + ADR-017). Load on ALL L1 handler tasks.
- `.claude/constraints/auth.md` — ADR-003/004/008/009 auth binding; load on H3/H4/H10 + H0.5 endpoint tasks.
- `.claude/constraints/api.md` — BFF API endpoints (ADR-001/004/008/010/019). Load on H0.5 consent-callback + all L2 API endpoint tasks.
- `.claude/constraints/config.md` — ADR-018 + ADR-020; load on H12b + upgrade-model tasks.
- `.claude/constraints/data.md` — ADR-005/007/009; load on H2a Cosmos/Storage tasks.
- `.claude/constraints/ai.md` — ADR-013/014/015/016; load on H2b + H12a + `PublicContracts/` usage.
- `.claude/constraints/plugins.md` — not r1 scope.
- `.claude/constraints/pcf.md` / `.claude/constraints/webresource.md` — Phase A code-page audit only.
- `.claude/constraints/theme-consistency.md` / `.claude/constraints/react-versioning.md` — not r1 scope.
- `.claude/constraints/INDEX.md` — index / loading strategy table.

## 3. Patterns (`.claude/patterns/**`)

**Total 53 pointer files across 9 subdirectories**. Enumerated relevance to r1 (only ones r1 tasks should load):

**api/ (8 patterns)** — load on L1 handlers + L2 control-plane:
- `.claude/patterns/api/provisioning-pipeline.md` — Canonical exemplar via `DemoProvisioningService.cs` 9-step
- `.claude/patterns/api/background-workers.md` — Queue-driven `IJobHandler`; DocumentProcessingJobHandler exemplar
- `.claude/patterns/api/scheduled-jobs.md` — `IScheduledJob` (ADR-036) for L2 state-reconciler
- `.claude/patterns/api/endpoint-definition.md` — H0.5 + all L2 REST endpoints
- `.claude/patterns/api/endpoint-filters.md` — ADR-008 filter for L2 auth
- `.claude/patterns/api/error-handling.md` — ADR-019 ProblemDetails; ALL L2 endpoints
- `.claude/patterns/api/resilience.md` — external Graph / Dataverse / OpenAI calls
- `.claude/patterns/api/service-registration.md` — ADR-010 feature-module DI; L2 scaffold
- `.claude/patterns/api/send-email-integration.md` — H14 email webhook

**auth/ (13 patterns)** — load on H3/H4/H0.5/H10 + Graph tasks:
- `.claude/patterns/auth/spaarke-sso-binding.md` — Canonical `@spaarke/auth` for H0.5
- `.claude/patterns/auth/graph-sdk-v5.md` — ⚠ **v5-titled but net10 baseline is v6.5**; cross-check `dotnet-10-upgrade-r1/notes/graph6-kiota2-break-assessment.md` for `ODataError` migration
- `.claude/patterns/auth/graph-endpoints-catalog.md` — H10 + H14
- `.claude/patterns/auth/managed-identity-resource-rbac.md` — Phase C UAMI migration
- `.claude/patterns/auth/msal-client.md`, `oauth-scopes.md`, `obo-flow.md`, `token-caching.md` — H3 + operator toolchain
- `.claude/patterns/auth/service-principal.md` — H3
- `.claude/patterns/auth/dataverse-obo.md`, `xrm-webapi-vs-bff-auth.md` — H10
- `.claude/patterns/auth/spe-writer-identity-matching.md` — H8 + T6 fix
- `.claude/patterns/auth/graph-webhooks.md` — H14
- `.claude/patterns/auth/spaarke-auth-initialization.md`, `uac-access-control.md`, `bff-url-normalization.md` — L2 API auth setup

**testing/ (4 patterns)** — 5 new ArchTests + integration tests:
- `.claude/patterns/testing/god-class-ratchet.md` — NFR-07 (no NEW `src/server/**/*.cs` > 2,000 LOC); verified 2026-08-15 (ArchTests 38/0)
- `.claude/patterns/testing/integration-tests.md` — H13 acceptance
- `.claude/patterns/testing/unit-test-structure.md`, `mocking-patterns.md` — 5 new ArchTests

**dataverse/ (5 patterns)** — H5/H6/H7/H10/H12a-c + registry extension

**caching/ (3 patterns)** — H4 KV + L2 state cache + Graph token cache

**ai/ (7 patterns)** — H2b indexing + `PublicContracts/` facade + H12a

**pcf/**, **ui/**, **webresource/** — not r1 primary scope.

## 4. Skills (`.claude/skills/**`) — 75 skills total

r1 will invoke these:

- `task-execute` — MANDATORY per-task orchestrator (root CLAUDE.md §4)
- `code-review` — Step 9.5 quality gate on FULL-rigor
- `adr-check` — Step 9.5 ADR-compliance
- `adr-aware` — Auto-loads ADRs
- `context-handoff` — Every 3 steps or context > 70%
- `conflict-check` — **BEFORE EVERY BFF PR** (19 active BFF worktrees per r3 handoff §7)
- `bff-deploy` — H9 (272 LOC canonical BFF deployment)
- `azure-deploy` — H2a/H4 (401 LOC Bicep + KV secrets)
- `deploy-new-release` — **REFERENCE MODEL** for new `/provision-environment` L3 skill (Phase D per §4.3a.4)
- `dataverse-deploy` — H6 (managed solution import)
- `dataverse-create-schema` — FR-26 (12 new columns; reconciled 2026-08-17 by task 023 per §9)
- `dataverse-mcp-usage` — §4.3a.5 fallback matrix
- `worktree-net10-migrate`, `worktree-sync`, `worktree-setup` — Cross-project coordination
- `merge-to-master`, `push-to-github`, `pull-from-github` — Standard git ops
- `project-continue`, `project-pipeline`, `project-setup`, `task-create` — Pipeline
- `project-defer-issue-tracking` — Deferrals uncovered during r1
- `test-diet` — Project close per ADR-038 build-vs-maintain reconciliation
- `doc-drift-audit` — Gap 4 doc consolidation
- `repo-cleanup` — Project close hygiene
- `script-aware` — Before writing new PS
- `spaarke-conventions` — Naming/structure/technology patterns

**Referenced but does NOT yet exist**: `.claude/skills/provision-environment/SKILL.md` — Phase D deliverable (task 075).

## 5. Canonical `IJobHandler` implementations (pattern exemplars for L1 handlers)

**`src/server/api/Sprk.Bff.Api/Services/Jobs/Handlers/` (8 handlers)**:
- **`DocumentProcessingJobHandler.cs`** — **STRONGEST exemplar (rating A)** — 99 LOC, canonical structure: DI injection → `JobType` prop → `ProcessAsync` with `try/catch/finally`, `Stopwatch` timing, payload parsing helper, `JobOutcome.Success(job.JobId, job.JobType, stopwatch.Elapsed)`. Read in one pass. Points cleanly at ADR-004 contract.
- **`RagIndexingJobHandler.cs`** — **Second exemplar (A-)** — 401 LOC; realistic complexity: multi-service DI, idempotency-key derivation, error partitioning, telemetry pattern. Template for larger handlers (H2a, H6, H9, H12a).
- Also: `AttachmentClassificationJobHandler.cs`, `IncomingCommunicationJobHandler.cs`, `IncomingMessagingJobHandler.cs`, `InvoiceExtractionJobHandler.cs`, `InvoiceIndexingJobHandler.cs`, `SpendSnapshotGenerationJobHandler.cs`

**`src/server/api/Sprk.Bff.Api/Services/Ai/Jobs/` (4 IJobHandlers + 1 BackgroundService)**:
- **`AppOnlyDocumentAnalysisJobHandler.cs`** — 254 LOC; exemplar for **confidential-client (app-only) pattern** → directly relevant to **H8 T6 fix** + H10 app-only Graph calls
- `BulkRagIndexingJobHandler.cs`, `EmailAnalysisJobHandler.cs`, `ProfileSummaryJobHandler.cs`
- `EmbeddingMigrationService.cs` — BackgroundService pattern (relevant to L2 state-reconciler `BackgroundService`)

**Contract source**: `src/server/api/Sprk.Bff.Api/Services/Jobs/IJobHandler.cs` (22 LOC — minimal interface).

**Supporting infrastructure to REUSE**:
- `Services/Jobs/JobContract.cs` — Job envelope schema
- `Services/Jobs/JobOutcome.cs` — Uniform outcome record
- `Services/Jobs/IdempotencyService.cs` + `IIdempotencyService.cs` — Redis 3-level idempotency
- `Services/Jobs/JobSubmissionService.cs` — Enqueue mechanism
- `Services/Jobs/ServiceBusJobProcessor.cs` — Router
- `Services/Jobs/DeadLetterQueueService.cs` — DLQ pattern
- `Services/Jobs/BatchJobStatusStore.cs` — Status aggregation

**Registration provisioning exemplar for H11**:
- `Services/Registration/DemoProvisioningService.cs` — **315 LOC 9-step orchestration** → direct pattern for H11. Sequential steps with `completedSteps` list + `DemoProvisioningException` carrying partial-completion diagnostics + non-fatal optional step (Step 8 SPE with skip-on-fail).

## 6. Bicep module inventory (relevant to H2a)

- **Modules**: **25 `.bicep`** + 1 `.json` lifecycle policy = 26 items (confirms spec §11.2 v3.2 correction)
- **Stacks**: **4 `.bicep`** (`ai-foundry-stack.bicep`, `model1-customer.bicep`, `model1-shared.bicep`, `model2-full.bicep`) + 3 `.bicepparam`

### 25 modules with disposition

| Module | Purpose | r1 disposition |
|---|---|---|
| `acs-communication.bicep` | ACS + Event Grid + BFF webhook | REUSE |
| `ai-foundry-hub.bicep` | AI Foundry Hub + Project | REUSE |
| `ai-search.bicep` | Azure AI Search | REUSE (extend H2a) |
| `alerts.bicep` | Alert rules | REUSE |
| **`app-service.bicep`** | App Service for BFF | **EXTEND Phase C UAMI refactor** — bind UAMI to BOTH slots |
| `app-service-config.bicep` | Site config | REUSE |
| `app-service-plan.bicep` | ASP | REUSE |
| `app-service-slot.bicep` | Slot | REUSE |
| `autoscale.bicep` | Autoscale | REUSE |
| `content-safety.bicep` | AI Content Safety | REUSE |
| **`cosmos-db.bicep`** | Cosmos DB serverless | **EXTEND H2a** (add to customer.bicep — v3 BFF prereq) |
| `dashboard.bicep` | Azure Dashboard | REUSE |
| `deployment-slot.bicep` | Staging slot | REUSE |
| `doc-intelligence.bicep` | Document Intelligence | REUSE (per-customer H2a) |
| **`key-vault.bicep`** | Hardened KV | **EXTEND Phase G** — vault-name as PARAM per FR-35 |
| `membership-topic.bicep` | Service Bus topic | REUSE |
| `monitoring.bicep` | App Insights + Log Analytics | REUSE (per-customer H2a) |
| `openai.bicep` | Azure OpenAI | REUSE (per-customer M2; shared M1) |
| `private-endpoints.bicep` | Private endpoints | REUSE |
| **`redis.bicep`** | Redis Cache | **REUSE PER-ENV** (NOT per-customer — Q-E FR-12) |
| `role-assignment-keyvault.bicep` | KV RBAC | REUSE (Phase C UAMI RBAC) |
| `service-bus.bicep` | Service Bus | REUSE |
| `storage-account.bicep` | Storage Account | REUSE |
| `temp-blob-lifecycle.bicep` | 24h TTL lifecycle | REUSE |
| `vnet.bicep` | VNet with 3 subnets | REUSE |

### NEW vs EXTEND vs REUSE

**NEW (Phase C)**:
- `infrastructure/bicep/modules/uami.bicep` — **DOES NOT EXIST** (confirmed) — Phase C FR-37
- `infrastructure/bicep/platform-controlplane.bicep` — **DOES NOT EXIST** (confirmed) — L2 orchestrator infra

**EXTEND**:
- `customer.bicep` — Add Cosmos + optional SignalR + UAMI param; remove Redis
- `app-service.bicep` — Consume UAMI on BOTH slots (Phase C)
- `platform.bicep` — Rebuild to control-plane-only (D12)
- **`model1-shared.bicep` — EXISTS (verified)** — already first-class per §3A A1 ⚠ **spec.md scope item 20 + plan.md Phase C task 032 marked as NEW but it's ACTUALLY existing**; task 032 should be REVIEW / VERIFY / EXTEND, not NEW
- `key-vault.bicep` — Vault name as PARAM (Phase G FR-35)

## 7. Existing Provisioning scripts (basis for handler ports)

All spec §11.1 disposition-table scripts verified present with LOC:

| Script | Path | LOC | Notes |
|---|---|---|---|
| `Provision-Customer.ps1` | `scripts/` | **1,610** | Basis for H2a (add 6 new module invocations per FR-04) |
| `Register-EntraAppRegistrations.ps1` | `scripts/` | **539** | Basis for H3. ✅ v3.3 tenant-hardcode fix VERIFIED (commit `1834b77bc`) — `-TenantId` Mandatory (line 70); `a221a95e` GUID only in 3 comment/example lines |
| `Create-NewContainerType.ps1` | `scripts/` | **256** | Basis for H8; needs T6 confidential-client fix |
| `New-BusinessUnitContainer.ps1` | `scripts/` | **192** | Basis for H8 |
| `Deploy-BffApi.ps1` | `scripts/` | **597** | Called by H9 |
| `Deploy-Release.ps1` | `scripts/` | **641** | Called by H9; Phase 4 needs `customerId`-driven hardening (Gap 2) |
| `Deploy-DataverseSolutions.ps1` | `scripts/` | **597** | Called by H6; ships **8 solutions** per `$SolutionImportOrder`; extend to Package Deployer |
| `Validate-DeployedEnvironment.ps1` | `scripts/` | **447** | Called by H13; extend to E2E acceptance gate (Gap 4) — verify all §4B T1–T6 traps + naming-conformance exit 0 |
| **`Deploy-AllIndexes.ps1`** | **`scripts/ai-search/`** | **637** | REUSE by H2b (7 canonical indexes). Path correction v3.2 verified |
| **`naming-conformance-check.ps1`** | `scripts/` | **200** | ✅ **VERIFIED PRESENT** (r3 task 063 landed 2026-08-14 commit `cec19d374`) |
| `Deploy-RedisCache.ps1` | `scripts/` | present | REUSE per-env (Q-E FR-12) |
| `Create-DataverseEnvironmentSchema.ps1` | `scripts/` | **197** | v2 baseline creates `sprk_dataverseenvironment` with **16 columns** |

Related seed suite present under `scripts/seed-data/`: `Deploy-Actions.ps1`, `Deploy-Knowledge.ps1`, `Deploy-OutputTypes.ps1`, `Deploy-Playbooks.ps1`, `Deploy-Skills.ps1`, `Deploy-TypeLookups.ps1`, plus `Verify-*.ps1` and JSON manifests.

## 8. AI Search index catalog audit

**`scripts/ai-search/Deploy-AllIndexes.ps1` (637 LOC)**. Catalog at lines 197–263 confirms exactly **7 canonical indexes** matching spec §8.2:

| # | Catalog Key | Name | Vector Fields | Required Filterable |
|---|---|---|---|---|
| 1 | `files-index` | `spaarke-files-index` | `contentVector3072`, `documentVector3072` | `tenantId`, `privilege_group_ids` |
| 2 | `discovery-index` | `spaarke-discovery-index` | `contentVector3072`, `documentVector3072` | `tenantId`, `privilege_group_ids` |
| 3 | `records-index` | `spaarke-records-index` | `contentVector` | `tenantId`, `recordType`, `dataverseRecordId`, `dataverseEntityName`, `privilege_group_ids` |
| 4 | `rag-references` | `spaarke-rag-references` | `contentVector3072` | `tenantId`, `documentType`, `knowledgeSourceId` (SemanticReferencesField `documentType`; ForbiddenFieldNames `domain`) |
| 5 | `insights-index` | `spaarke-insights-index` | `contentVector` | `tenantId`, `artifactType` |
| 6 | `session-files` | `spaarke-session-files` | `contentVector3072`, `documentVector3072` | `tenantId`, `sessionId` (ADR-014 canonical invariant) |
| 7 | `invoices-index` | `spaarke-invoices-index` | `contentVector` | `tenantId`, `invoiceId`, `matterId`, `projectId` |

**JSON schemas verified present** at `infrastructure/ai-search/` for all 7 + `_archive/` for retired `spaarke-knowledge-index`. Catalog matches spec.md §8.2 exactly. No discrepancies.

## 9. Dataverse `sprk_dataverseenvironment` schema baseline

**`scripts/Create-DataverseEnvironmentSchema.ps1` (197 LOC)**. Confirms v2 baseline **16 columns**:

1. `sprk_name` (primary, 200)
2. `sprk_mdaappid` (String, 100)
3. `sprk_envaccountdomain` (String, 200)
4. `sprk_businessunitname` (String, 200)
5. `sprk_teamname` (String, 200)
6. `sprk_specontainerid` (String, 500)
7. `sprk_securitygroupid` (String, 100)
8. `sprk_dataverseurl` (String URL, 500, Required)
9. `sprk_description` (Memo, 2000)
10. `sprk_licenseconfigjson` (Memo, 4000)
11. `sprk_adminemails` (Memo, 1000)
12. `sprk_environmenttype` (Picklist)
13. `sprk_setupstatus` (Picklist)
14. `sprk_isactive` (Boolean, default True)
15. `sprk_isdefault` (Boolean, default False)
16. `sprk_defaultdurationdays` (Integer, 1–365)

**12 NEW columns per FR-26 that need to be added** — ⚠ **Spec §26 says "11 new columns" but enumerates 12 items**:
1. `sprk_azuresubscriptionid` — NEW
2. `sprk_resourcegroupname` — NEW
3. `sprk_appservicename` — NEW
4. `sprk_keyvaultname` — NEW
5. `sprk_containertypeid` — NEW (v2 has `sprk_specontainerid` which is different — container ID vs container-type ID)
6. `sprk_provisionedon` — NEW
7. `sprk_currentrunid` — NEW (v3.3 concurrency invariant I5)
8. `sprk_tenancymodel` (Choice Model1Shared/Model2Dedicated) — NEW
9. `sprk_tenantid` — NEW (v3.3 D18)
10. `sprk_bffversion` — NEW (v3.3 §14A)
11. `sprk_solutionversion` — NEW (v3.3 §14A)
12. `sprk_ClientCacheBustToken` — NEW (v3.3 upgrade cache-bust)

Total 16 + 12 = **28 columns**. Task 023 must reconcile the count discrepancy in spec (11 vs 12).

**BFF code references `sprk_dataverseenvironment` in 11 files** (source-only, no `.csproj` impact): `DataverseEnvironmentService.cs`, `DataverseEnvironmentRecord.cs`, `RegistrationDataverseService.cs`, `RegistrationEndpoints.cs`, `RegistrationModule.cs`, `DemoProvisioningOptions.cs`, `AgentTokenOptions.cs`, `AgentTokenService.cs`, `SpeAdminGraphService.cs`, `RecordSyncJob.cs`, `sprk_registrationribbon.js`.

## 10. Docs to consolidate (Gap 4)

**~11,316 LOC of deployment documentation to consolidate** into ONE authoritative deploy guide:

| File | LOC |
|---|---|
| `AI-DEPLOYMENT-GUIDE.md` | 1,257 |
| `CUSTOMER-DEPLOYMENT-GUIDE.md` | 1,136 |
| `DEPLOYMENT-VERIFICATION-GUIDE.md` | 423 |
| `ENVIRONMENT-DEPLOYMENT-GUIDE.md` | 824 (13 known issues per spec §11.5) |
| `PRODUCTION-DEPLOYMENT-GUIDE.md` | 1,679 |
| `SPAARKE-DEPLOYMENT-GUIDE.md` | **2,038** (LARGEST) |
| `COMMUNICATION-DEPLOYMENT-GUIDE.md` | 1,085 |
| `M365-COPILOT-DEPLOYMENT-GUIDE.md` | 504 |
| `PCF-DEPLOYMENT-GUIDE.md` | 773 |
| `CUSTOMER-ONBOARDING-RUNBOOK.md` | 599 (9 sections per spec §11.5) |
| `auth-deployment-setup.md` | 840 (10-section runbook; 21 MUSTs) |
| `CUSTOMER-QUICK-START-CHECKLIST.md` | 158 |
| Plus: `office-addins-deployment-checklist.md`, `ai-guide-playbook-deploy-recipe.md`, `DECLARATIVE-AGENT-BUILD-AND-DEPLOY-GUIDE.md`, `DATAVERSE-AUTHENTICATION-GUIDE.md` |

**Recent updates (2026-07+)**: Only `da6b989ad docs(dotnet-10-upgrade-r1): sweep normative .NET 8 -> .NET 10 across canonical docs` (baseline sweep). No other DEPLOY-guide-specific commits in past 6+ weeks.

Consolidation target: one canonical `docs/deployment/customer-provisioning-guide.md` with others reduced to explicit stubs.

## 11. Coordination signals from other worktrees

### `projects/code-quality-and-assurance-r3/notes/r3-handoff.md` (57 LOC)

**Handoff is FROM `dotnet-10-upgrade-r1` INTO r3** (2026-08-14), NOT r3 → r1. Establishes net10 baseline. Key signals for r1:

- **.NET 10 LTS baseline authoritative** — master + dev on net10, suite green, zero-CVE. Publish baseline **44.96 MB incl PDBs** (was 49.63 net8)
- **Graph v6 / Kiota 2.0 DONE**: `Microsoft.Graph 6.5.0`. **API impact**: Kiota throws `ODataError` (not `ServiceException`); `ResponseStatusCode` is int; `ResponseHeaders` is dict-shaped. r1 H10 Graph SDK calls MUST catch `ODataError` per NFR-09
- **DO NOT re-pin framework-provided packages** (`System.Text.Json`, `System.Formats.Asn1`, `System.Text.RegularExpressions`). KEEP shared-lib pins: `System.Security.Cryptography.Xml` + `Pkcs` at `10.0.11`
- **`DiGraphValidationTests` is MAINTAIN/KEEP** — do NOT weaken; do NOT disable `ValidateOnBuild`/`ValidateScopes`
- **Only `spaarke-dev` is live** — demo/prod decommissioned

**r3 task landing status confirmed via git history**:
- ✅ **061** ValidateOnStart on 24 Tier-1 IOptions — commit `5a6ad556e`
- ✅ **062** `GraphAppRoles.cs` constant — commit `917322e37`
- ✅ **060** S2S drop — commit `515e461f2`
- ✅ **017** KV federation assessment — commit `2ef4ff39b`
- ✅ **063 + 042** naming standard + gate — commit `cec19d374`

**Related r3 handoff files** (also present):
- `projects/code-quality-and-assurance-r3/notes/task-061-config-validation-classification.md` (115 LOC) — Tier 1/2/3 classification for FR-05/NFR-05
- `projects/code-quality-and-assurance-r3/notes/task-063-naming-standard-r1-handoff.md` (44 LOC) — current→canonical rename map + LIVE-state pre-check obligation

### ⚠ Missing coordination artifact

**`projects/ci-cd-unit-test-remediation-r1/notes/task-042-063-ci-gate-wiring-deferral.md` DOES NOT EXIST**. r3 task-063 handoff (line 17) references it, spec.md line 82 + 192 references it — but it was never authored. r1 spec + plan tasks referencing it must **either reconstitute the artifact OR coordinate via alternate doc**.

**Substitute coordination signals available**:
- `projects/ci-cd-unit-test-remediation-r1/CLAUDE.md` — ci-cd-r1 autonomous mode, `.github/workflows/**` ownership, Phase 1 dispatch strategy, "MUST keep `sdap-ci.yml` running in parallel through Phase 2; retire only post-cutover after 14 days stability"
- `projects/ci-cd-unit-test-remediation-r1/tasks/042-build-ci-tier2-advisory-yml.poml` — task 042 itself
- `projects/ci-cd-unit-test-remediation-r1/notes/branch-protection-baseline-decision.md`
- `projects/ci-cd-unit-test-remediation-r1/notes/path-reorganization-design.md`

**Phase H coordination expectations**: MUST NOT edit `.github/workflows/**` in isolation — coordinated PR with ci-cd-r1. Naming-conformance + tenant-isolation ArchTest CI wiring is the coordinated deliverable.

## 12. Gaps + Risks (verified)

| Question | Status | Evidence |
|---|---|---|
| `Sprk.Provisioning.ControlPlane` project scaffold | ❌ **NO (expected)** | `src/server/services/` does NOT exist — only `src/server/api/` and `src/server/shared/`. Task 036 NEW. |
| `uami.bicep` module | ❌ **NO (expected)** | Not in `infrastructure/bicep/modules/`. Task 028 NEW per FR-37. |
| **`model1-shared.bicep` stack** | ✅ **YES — ALREADY EXISTS** | Present at `infrastructure/bicep/stacks/model1-shared.bicep`. ⚠ **spec.md scope 20 + plan.md task 032 says NEW — reconcile as REVIEW/EXTEND** |
| `.claude/skills/provision-environment/SKILL.md` | ❌ **NO (expected)** | Directory doesn't exist. Phase D NEW deliverable (task 075). |
| `scripts/naming-conformance-check.ps1` | ✅ **YES** | 200 LOC. r3 task 063 landed commit `cec19d374`. |
| `Infrastructure/Auth/GraphAppRoles.cs` | ✅ **YES / 11 nulls (NOT 10)** | Present 159 LOC. **14 total roles, 3 populated + 11 null.** Spec §Prerequisites + §FR-06 + SC #16b say "10 of 14 null" but actual = 11 nulls. Reconcile in Phase A. |
| `Register-EntraAppRegistrations.ps1:63` tenant hardcode removed | ✅ **YES** | Commit `1834b77bc`. Line 70 `-TenantId` Mandatory; comments at lines 27/31/64 document removal. `a221a95e` only in 3 comment/example lines. |

## Additional risks/gaps uncovered

1. **ADR path drift**: `docs/adr/` (44) and `.claude/adr/` (47) don't overlap perfectly. Tasks reference wrong path → executor confusion. Phase A should standardize.
2. **Missing coordination artifact**: `task-042-063-ci-gate-wiring-deferral.md` never authored. Phase H's coord PR must reconstitute OR coord via alternate doc.
3. **Column count discrepancy**: spec §26 FR-26 says "11 new columns" but enumerates 12 items. Task 023 must reconcile.
4. **Graph SDK version drift**: `.claude/patterns/auth/graph-sdk-v5.md` (v5-titled) but net10 is v6.5. Phase A task should refresh title/content.
5. **`GraphAppRoles.cs` GUID count**: spec says "10 of 14 null" but actual = **11 of 14 null**. Task 005 escalation-gate condition needs correction.
6. **`model1-shared.bicep` scope drift**: spec.md scope item 20 + plan.md task 032 marked as NEW work; file already exists. Task 032 should verify/extend, not create.

---

**Report prepared 2026-08-16. All 12 sections complete. Search-breadth: very thorough. 60–90 task POMLs should have full knowledge-reference material.**

**MAIN-SESSION FOLLOW-UPS** (to be tracked in task-execute during Phase A):
- FR-26: reconcile 11-vs-12 new column count in spec + plan + design
- FR-06 / SC #16b: reconcile "10 of 14 null AppRoleId" → "11 of 14 null" in spec + plan + design
- Scope item 20 / task 032: verify `model1-shared.bicep` existing state; adjust to review/extend rather than NEW
- Phase H: reconstitute `task-042-063-ci-gate-wiring-deferral.md` OR coordinate via CLAUDE.md alternate docs
- Phase A: refresh `.claude/patterns/auth/graph-sdk-v5.md` title/content for v6.5
- All tasks: use correct ADR path per ADR (docs/adr/ vs .claude/adr/) — see Section 1
