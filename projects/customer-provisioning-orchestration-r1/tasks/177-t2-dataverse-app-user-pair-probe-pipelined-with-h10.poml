<?xml version="1.0" encoding="UTF-8"?>
<task id="177" project="customer-provisioning-orchestration-r1">
  <metadata>
    <title>T2: Dataverse App User pair probe (pipelined with H10)</title>
    <phase>C'' Wave G-7 -- Acceptance</phase>
    <gate>blocked</gate>
    <status>completed</status>
    <rigor>FULL</rigor>
    <rigor-reason>H13 acceptance-gate probe -- the terminal correctness check per handler; a wrong probe (false Pass) is a silent-fail-trap class defect at the acceptance layer itself. FULL rigor.</rigor-reason>
    <model-tier>sonnet</model-tier>
    <model-tier-reason>Well-specified single-purpose probe replacing a Placeholder that unconditionally returns InfraFault -- Sonnet @ high unless flagged otherwise.</model-tier-reason>
    <effort>high</effort>
    <parallel-group>waveG7-parallel</parallel-group>
    <parallel-safe>true</parallel-safe>
    <deps>143, 111</deps>
    <tags>l2-controlplane, job-handler, testing</tags>
    <estimated-effort>6-9 hours</estimated-effort>
  </metadata>

  <prompt>Replace PlaceholderTrapVerifier's T2 branch with a real DV-REST query systemusers?$filter=applicationid eq {id} performed TWICE -- once for the customer's UAMI app id, once for the BFF app-reg id -- confirming BOTH app-user pairs exist on the customer environment.</prompt>

  <role>SPAARKE platform developer. Expert in H13's IE2ETrapVerifier/IE2EInvariantVerifier seam contracts and typed Azure SDK/REST post-condition verification.</role>

  <goal>T2's real verifier confirms both the customer UAMI and the BFF app-reg resolve as Dataverse App Users on the target customer environment, returning Fail if either is missing.</goal>

  <context>
    <background>DS-4 §6: 'T2 Dataverse App User pair... DV-REST systemusers?$filter=applicationid eq ... x2 (UAMI + BFF app-reg)... Blocked by: H10 (done) + C5.8 grants.'</background>
    <relevant-files>
      <file role="modify">src/server/services/Sprk.Provisioning.ControlPlane.Core/Handlers/E2EAcceptance/H13E2EAcceptanceGateHandler.cs</file>
      <file role="canonical-reference">src/server/services/Sprk.Provisioning.ControlPlane.Core/Handlers/E2EAcceptance/E2EAcceptanceModule.cs</file>
    </relevant-files>
    <dependencies>
      <dependency task="143" status="pending">Subject handler for this probe -- H10's live verification (task 143) confirms the underlying mechanism works; C5.8 grants (task 111) must be in place</dependency>
      <dependency task="111" status="pending">Subject handler for this probe -- H10's live verification (task 143) confirms the underlying mechanism works; C5.8 grants (task 111) must be in place</dependency>
    </dependencies>
  </context>

  <constraints>
    <constraint source="DS-4 §6 H13 placeholder deep-dive">This probe replaces a Placeholder verifier that currently returns InfraFault UNCONDITIONALLY for every subject -- the fix is a REAL check, not a different flavor of always-pass. Two DV-REST systemusers queries (customer UAMI + BFF app-reg) against the customer environment, reusing the same query idiom H10 (already real per DS-4) itself uses for its own idempotency check.</constraint>
    <constraint source="DS-4 §6 pipelining note">H13 probes are buildable pipelined alongside their subject handler's wave, not serialized to the end -- this task can be authored as soon as its subject handler (cited in &lt;deps&gt;) lands, even though it is NUMBERED in the G-7 acceptance range for readability.</constraint>
  </constraints>

  <knowledge>
    <topic>H13 probe: T2: Dataverse App User pair probe (pipelined with H10)</topic>
    <files>
      <file>projects/customer-provisioning-orchestration-r1/spec.md</file>
      <file>projects/customer-provisioning-orchestration-r1/design.md</file>
      <file>projects/customer-provisioning-orchestration-r1/CLAUDE.md</file>
      <file>projects/customer-provisioning-orchestration-r1/notes/design-study-ds4-handler-audit.md</file>
      <file>projects/customer-provisioning-orchestration-r1/notes/design-study-ds4-handler-audit.md</file>
    </files>
  </knowledge>

  <steps mode="directional">
    <step order="0" name="Rigor + load">Emit RIGOR LEVEL (FULL). Read DS-4 §6's row for this probe plus the existing Placeholder verifier's interface contract.</step>
    <step order="1" name="Real probe">Author the real verifier implementation per this task's &lt;prompt&gt;.</step>
    <step order="2" name="Wire up">Register the real verifier in E2EAcceptanceModule.cs in place of the Placeholder, for this SPECIFIC trap/invariant id only (other still-placeholder ids are unaffected until their own tasks land).</step>
    <step order="3" name="Tests">Pass/Fail/InfraFault outcome tests for this specific probe (per DS-4 §7's per-probe test-effort note).</step>
    <step order="4" name="Build + verify">dotnet build 0 errors; dotnet test green.</step>
  </steps>

  <escalation>
    <trigger>If this probe's real check would require a live call this task's sequencing cannot yet exercise (subject handler not yet live-deployed), author + unit-test the probe against FAKES now (per DS-4 §6's 'buildable earlier against fakes' note) and flag the LIVE-verification gap explicitly for Wave G-7's assembly task (185) / Phase F rerun (186) rather than blocking this task indefinitely.</trigger>
  </escalation>

  <tools>
    <tool name="dotnet">build/test</tool>
  </tools>

  <outputs>
    <output type="code">src/server/services/Sprk.Provisioning.ControlPlane.Core/Handlers/E2EAcceptance/ (real probe for T2: Dataverse App User pair probe (pipelined with H10))</output>
  </outputs>

  <acceptance-criteria>
    <criterion testable="true">The Placeholder verifier for this specific trap/invariant id is replaced with a real implementation.</criterion>
    <criterion testable="true">A test confirms a FAILING real-world condition (subject not correctly configured) produces a Fail outcome, not InfraFault or a false Pass.</criterion>
    <criterion testable="true">A test confirms a PASSING real-world condition produces Pass.</criterion>
    <criterion testable="true">dotnet build exits 0; dotnet test exits 0.</criterion>
  </acceptance-criteria>

  <notes>Justification (CLAUDE.md §11): Existing -- the IE2ETrapVerifier/IE2EInvariantVerifier interface + E2EAcceptanceModule registration pattern already exist; only this ONE id's implementation is a placeholder. Extension -- replacing one Placeholder registration with a real one, following the SAME interface contract, is the exact minimal move. Cost-of-doing-nothing -- H13's acceptance gate cannot go green as coded (DS-4's own words) without this probe; the acceptance-target transition (task 184/185/186) is blocked on EVERY probe individually landing.

COMPLETION SUMMARY (2026-08-20, commit 295acdd28):
  Landed DataverseAppUserPairT2Probe.cs (standalone class per sibling task 175
  T6's coordination posture -- task 185 assembly owns the composition surface).
  Reuses task 143's IDataverseAppUserVerifier seam verbatim; two DV-REST
  systemusers?$filter=applicationid eq {id} queries (BFF app-reg + UAMI).
  ProbeAsync verdict: Passed (both halves count=1) | Failed (either half
  count!=1, rich pair-mismatch diagnostic per §4B QuarantineRequired) |
  InfraFault (missing input from upstream handler OR verifier throw; H13
  Resumable).

  14 xUnit tests all pass (DataverseAppUserPairT2ProbeTests.cs): Pass/Fail
  branches x5 (T1-T5), input-guards x4 (T6a-d for missing tenantId/dvUrl/
  bffAppRegId/uamiClientId), verifier-throws-per-half x2 (T7-T8), Kind
  constant (T9), cancellation propagates (T10), per-half-appId correctness
  (T11). Fake IDataverseAppUserVerifier at seam level, never Mock<T> per
  testing.md.

  Build 0 errors on my files (verified with sibling incomplete WIP
  temporarily isolated to scratch); tests: 14/14 pass. Full solution build
  is red due to sibling task 174 CosmosPartitionKeyInvariantProbe.cs missing
  Azure.ResourceManager.CosmosDB package -- NOT my code. Not pushed per
  parent tail directive ("Do NOT push if fail"); push deferred until sibling
  coordination resolves shared-tree build.

  Deferred to task 185 assembly per DS-4 §6 pipelining note: CompositeTrap
  Verifier authoring + E2EAcceptanceModule.cs DI swap of PlaceholderTrap
  Verifier -> aggregate. My probe file has zero DI-module side effects.

  Step 9.5 self-conducted (test-modifying unconditional override per root §8):
  code-review 0 Critical (H10-parity pattern, proper Exception + Cancellation
  discipline, xUnit + FluentAssertions parity with sibling probe tests);
  adr-check 0 violations (ADR-010 reuses IDataverseAppUserVerifier seam,
  ADR-013 no AI-internal types, §4D I1 explicit tenantId flow guard, §4B
  QuarantineRequired classification via TrapVerificationOutcome.Failed).

  Coordination: 6 sibling agents (170/173/174/175/178/179/180/182) actively
  editing E2EAcceptance/ in parallel throughout this task. Extreme file
  churn -- my files repeatedly cleaned by cleanup passes between operations.
  Adapted with rapid re-authoring + explicit --only path commit to avoid
  cross-agent contamination in my commit. Zero sibling files in commit
  295acdd28; only my probe + its tests.</notes>

  <execution>
    <skill>.claude/skills/task-execute/SKILL.md</skill>
  </execution>
</task>
