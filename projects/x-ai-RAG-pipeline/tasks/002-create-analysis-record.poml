<?xml version="1.0" encoding="UTF-8"?>
<task id="002" project="ai-RAG-pipeline">
  <metadata>
    <title>Create Analysis record before running playbook tools</title>
    <phase>0: Analysis Workflow Alignment</phase>
    <status>completed</status>
    <estimated-hours>3</estimated-hours>
    <dependencies>001</dependencies>
    <blocks>003</blocks>
    <tags>bff-api, ai, dataverse, services</tags>
    <rigor-hint>FULL</rigor-hint>
    <rigor-reason>Task modifies core service logic, creates Dataverse records</rigor-reason>
  </metadata>

  <prompt>
    Modify AppOnlyAnalysisService.AnalyzeDocumentAsync to create a sprk_analysis record in Dataverse
    BEFORE running the playbook tools. This mirrors what AnalysisOrchestrationService does for OBO
    analysis requests, ensuring background analyses are visible in the Analysis Workspace.
  </prompt>

  <role>
    SPAARKE platform developer. Expert in Dataverse integration and the AI analysis workflow.
    Follow ADR-013 for AI architecture patterns.
  </role>

  <goal>
    AppOnlyAnalysisService creates a sprk_analysis record before executing playbook tools.
    The Analysis record ID is available for linking AnalysisOutput records in task 003.
  </goal>

  <context>
    <background>
      The OBO flow in AnalysisOrchestrationService creates an Analysis record that serves as
      a container for analysis outputs. The app-only flow currently skips this, making background
      analyses invisible. We need to replicate the Analysis creation pattern.
    </background>
    <relevant-files>
      <file>src/server/api/Sprk.Bff.Api/Services/Ai/AppOnlyAnalysisService.cs</file>
      <file>src/server/api/Sprk.Bff.Api/Services/Ai/AnalysisOrchestrationService.cs</file>
      <file>src/server/api/Sprk.Bff.Api/Services/Dataverse/IDataverseService.cs</file>
    </relevant-files>
  </context>

  <constraints>
    <constraint source="ADR-013">Follow existing AI architecture patterns</constraint>
    <constraint source="ADR-015">Do not log document content, only identifiers</constraint>
    <constraint source="project">Analysis record must be created before playbook execution</constraint>
  </constraints>

  <knowledge>
    <files>
      <file>.claude/adr/ADR-013-ai-architecture.md</file>
      <file>.claude/adr/ADR-015-ai-data-governance.md</file>
      <file>.claude/constraints/ai.md</file>
    </files>
    <patterns>
      <pattern name="Analysis Creation" location="src/server/api/Sprk.Bff.Api/Services/Ai/AnalysisOrchestrationService.cs">
        See CreateAnalysisAsync call pattern in ExecuteAnalysisAsync method
      </pattern>
    </patterns>
  </knowledge>

  <steps>
    <step order="1">Read AnalysisOrchestrationService.cs to understand Analysis creation pattern</step>
    <step order="2">Identify the CreateAnalysisAsync method signature in IDataverseService</step>
    <step order="3">In AppOnlyAnalysisService.AnalyzeDocumentAsync, add call to CreateAnalysisAsync before playbook execution</step>
    <step order="4">Store the returned Analysis ID for use in AnalysisOutput creation (task 003)</step>
    <step order="5">Add appropriate logging (ID only, not content per ADR-015)</step>
    <step order="6">Handle errors gracefully - if Analysis creation fails, log warning but continue with analysis</step>
    <step order="7">Run dotnet build to verify compilation</step>
    <step order="8">Update TASK-INDEX.md: change task 002 status to ✅ completed</step>
  </steps>

  <tools>
    <tool name="dotnet">Build and test .NET projects</tool>
    <tool name="terminal">Run shell commands</tool>
  </tools>

  <outputs>
    <output type="code">src/server/api/Sprk.Bff.Api/Services/Ai/AppOnlyAnalysisService.cs</output>
  </outputs>

  <acceptance-criteria>
    <criterion testable="true">AnalyzeDocumentAsync calls CreateAnalysisAsync before playbook execution</criterion>
    <criterion testable="true">Analysis ID is stored and available for AnalysisOutput linking</criterion>
    <criterion testable="true">Error handling does not block analysis if Dataverse write fails</criterion>
    <criterion testable="true">dotnet build succeeds without errors</criterion>
  </acceptance-criteria>

  <notes>
    The Analysis record creation should be "best effort" - if it fails, log a warning but
    proceed with the analysis. This ensures existing functionality isn't broken by Dataverse issues.
  </notes>

  <completion-notes>
    <completed-date>2026-01-15</completed-date>
    <summary>
      Added Analysis record creation before playbook execution in both AnalyzeDocumentAsync and
      AnalyzeEmailAsync methods. The dataverseAnalysisId is passed to ExecutePlaybookAnalysisAsync
      and ExecuteEmailPlaybookAnalysisAsync for linking AnalysisOutput records in task 003.
      Used best-effort pattern with try-catch and LogWarning to not block analysis on Dataverse failures.
    </summary>
    <files-modified>
      <file>src/server/api/Sprk.Bff.Api/Services/Ai/AppOnlyAnalysisService.cs</file>
    </files-modified>
  </completion-notes>

  <execution>
    <skill>.claude/skills/task-execute/SKILL.md</skill>
    <protocol>
      Before starting this task, load all files listed in knowledge/files.
      Follow the task-execute skill for mandatory pre-execution checklist.
    </protocol>
  </execution>
</task>
