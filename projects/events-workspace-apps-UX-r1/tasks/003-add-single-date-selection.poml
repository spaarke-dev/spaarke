<?xml version="1.0" encoding="UTF-8"?>
<task id="003" project="events-workspace-apps-UX-r1">
  <metadata>
    <title>Add Single Date Selection</title>
    <phase>1</phase>
    <tags>pcf, calendar, interaction</tags>
    <estimate>2 hours</estimate>
    <dependencies>002</dependencies>
    <parallel-group>A</parallel-group>
    <status>completed</status>
    <completed-date>2026-02-04</completed-date>
  </metadata>

  <notes>
    Task 003 completed. Keyboard navigation (arrow keys) added to CalendarMonth and CalendarStack.

    Files modified:
    - CalendarMonth.tsx: Added focusedDate prop, onFocusDateChange callback, arrow key handlers,
      focus visual indicator style (dayCellFocused), roving tabindex for keyboard navigation
    - CalendarStack.tsx: Added focusedDate state management, handleFocusDateChange to coordinate
      cross-month navigation when arrows move beyond displayed month boundaries

    Verified:
    - Click selects date with blue background (colorBrandBackground)
    - Click same date deselects (toggle behavior in EventCalendarFilterRoot)
    - Arrow keys navigate between dates (Up/Down = +/-7 days, Left/Right = +/-1 day)
    - Enter/Space selects focused date
    - Visual focus ring (colorStrokeFocus2) for keyboard navigation
    - Selection callbacks fire on change

    Build result: 795 KB bundle (well under 5MB limit)
  </notes>

  <prompt>
    <goal>Implement single date click selection with visual feedback and selection state.</goal>

    <context>
      When user clicks a date, it should be highlighted and the filter output should update.
      The selected date will be used to filter the Event grid to show only events on that date.
    </context>

    <constraints>
      - Single click selects date
      - Click same date again deselects (clears filter)
      - Visual highlight using Fluent tokens (colorBrandBackground)
      - Keyboard accessible (Enter/Space to select)
    </constraints>
  </prompt>

  <knowledge>
    <files>
      <file path=".claude/adr/ADR-021-fluent-design-system.md" reason="Accessibility requirements" />
    </files>
  </knowledge>

  <steps>
    <step order="1">Add selectedDate state to Calendar component</step>
    <step order="2">Implement onClick handler for date cells</step>
    <step order="3">Add visual highlight styles for selected state</step>
    <step order="4">Add keyboard navigation (arrow keys, Enter to select)</step>
    <step order="5">Emit selection change via callback prop</step>
  </steps>

  <outputs>
    <artifact type="file">src/client/pcf/EventCalendarFilter/control/components/Calendar.tsx (updated)</artifact>
  </outputs>

  <acceptance-criteria>
    <criterion>Click selects date with visual highlight</criterion>
    <criterion>Click selected date deselects</criterion>
    <criterion>Keyboard accessible</criterion>
    <criterion>Selection callback fires on change</criterion>
  </acceptance-criteria>
</task>
