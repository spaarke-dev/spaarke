<?xml version="1.0" encoding="UTF-8"?>
<task id="033" project="visualization-module">
  <metadata>
    <title>Integrate dataset grid with platform filtering</title>
    <phase>4: Drill-Through Workspace</phase>
    <status>not-started</status>
    <estimated-hours>3</estimated-hours>
    <dependencies>032</dependencies>
    <blocks>034</blocks>
    <tags>pcf, dataverse, frontend</tags>
  </metadata>

  <prompt>
    Create a DatasetGrid component that renders records from the platform-provided
    dataset (context.parameters.dataset). The grid automatically updates when
    filters are applied via the FilterStateContext (which uses dataset.filtering API).
  </prompt>

  <role>
    PCF developer with Dataset PCF and Fluent UI v9 expertise.
  </role>

  <goal>
    A DatasetGrid component that:
    1. Renders records from context.parameters.dataset
    2. Uses Fluent UI v9 DataGrid component
    3. Automatically reflects filter changes (platform handles re-fetch)
    4. Supports selection for drill-through actions
  </goal>

  <context>
    <background>
      Since DrillThroughWorkspace is now a Dataset PCF (refactored in Task 032),
      the grid receives records via the platform dataset binding. When filters
      are applied via FilterStateContext (which calls dataset.filtering.setFilter),
      the platform automatically re-fetches data and triggers updateView().
      We just need to render the dataset.records - no manual WebAPI calls needed.
    </background>
    <architecture-decision>
      Reuse the DatasetGrid pattern from UniversalDatasetGrid but adapt for
      the DrillThroughWorkspace context. The grid is simpler because:
      - No CRUD operations (read-only drill-through view)
      - No MSAL auth (view is already bound in Dataverse context)
      - Platform handles filtering via dataset.filtering API
    </architecture-decision>
  </context>

  <constraints>
    <constraint source="ADR-011">MUST use Dataset PCF pattern - render from context.parameters.dataset</constraint>
    <constraint source="ADR-021">MUST use Fluent UI v9 DataGrid component</constraint>
    <constraint source="project">MUST NOT make manual WebAPI calls for grid data</constraint>
    <constraint source="project">Platform handles security trimming automatically</constraint>
  </constraints>

  <knowledge>
    <files>
      <file>.claude/adr/ADR-011-dataset-pcf.md</file>
      <file>.claude/adr/ADR-021-fluent-design-system.md</file>
      <file>src/client/pcf/UniversalDatasetGrid/control/components/DatasetGrid.tsx</file>
      <file>src/client/pcf/UniversalDatasetGrid/control/index.ts</file>
    </files>
  </knowledge>

  <steps>
    <step order="1">Create DatasetGrid component adapted from UniversalDatasetGrid pattern</step>
    <step order="2">Accept dataset prop from context.parameters.dataset</step>
    <step order="3">Map dataset.records and dataset.columns to Fluent DataGrid format</step>
    <step order="4">Render with Fluent UI v9 DataGrid component</step>
    <step order="5">Add loading state when dataset.loading is true</step>
    <step order="6">Handle empty results state with friendly message</step>
    <step order="7">Add selection support for potential future actions</step>
    <step order="8">Integrate into TwoPanelLayout right panel</step>
    <step order="9">Test that grid updates when filter applied via chart interaction</step>
    <step order="10">Update TASK-INDEX.md: change this task's status to completed</step>
  </steps>

  <outputs>
    <output type="code">src/client/pcf/DrillThroughWorkspace/control/components/DatasetGrid.tsx</output>
    <output type="code">src/client/pcf/DrillThroughWorkspace/control/components/DrillThroughWorkspaceApp.tsx (updated)</output>
  </outputs>

  <acceptance-criteria>
    <criterion testable="true">Grid renders records from context.parameters.dataset</criterion>
    <criterion testable="true">Grid columns match dataset.columns from bound view</criterion>
    <criterion testable="true">Grid updates automatically when filter applied (via updateView)</criterion>
    <criterion testable="true">Loading indicator shows when dataset.loading is true</criterion>
    <criterion testable="true">Empty state shown when dataset has no records</criterion>
  </acceptance-criteria>

  <execution>
    <skill>.claude/skills/task-execute/SKILL.md</skill>
  </execution>
</task>
