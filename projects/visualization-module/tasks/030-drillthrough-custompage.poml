<?xml version="1.0" encoding="UTF-8"?>
<task id="030" project="visualization-module">
  <metadata>
    <title>Create drill-through Custom Page</title>
    <phase>4: Drill-Through Workspace</phase>
    <status>not-started</status>
    <estimated-hours>4</estimated-hours>
    <dependencies>023</dependencies>
    <blocks>031</blocks>
    <tags>pcf, react, frontend, dataverse</tags>
  </metadata>

  <prompt>
    Create a Power Apps Custom Page that serves as the drill-through workspace.
    This page opens as an expanded modal and contains the chart + dataset side-by-side.
  </prompt>

  <role>
    Power Platform developer with Custom Page and React expertise.
  </role>

  <goal>
    A Custom Page that opens from Visual Host PCF and displays the interactive
    drill-through workspace with chart and filtered dataset grid.
  </goal>

  <context>
    <background>
      The drill-through workspace pattern shows chart (1/3) + dataset (2/3) side-by-side.
      Clicking chart elements filters the dataset grid in real-time.
    </background>
    <relevant-files>
      <file>scripts/Deploy-CustomPage.ps1</file>
    </relevant-files>
  </context>

  <constraints>
    <constraint source="project">MUST open as expanded modal from Visual Host</constraint>
    <constraint source="project">MUST pass chart definition ID as parameter</constraint>
    <constraint source="ADR-021">MUST use Fluent v9 styling</constraint>
  </constraints>

  <knowledge>
    <files>
      <file>.claude/patterns/pcf/dialog-patterns.md</file>
      <file>src/client/pcf/CLAUDE.md</file>
    </files>
  </knowledge>

  <steps>
    <step order="1">Create Custom Page project structure</step>
    <step order="2">Configure page to receive chartDefinitionId parameter</step>
    <step order="3">Create DrillThroughWorkspace PCF control for the page</step>
    <step order="4">Wire up Visual Host expand button to open Custom Page</step>
    <step order="5">Test page opens correctly with parameter</step>
    <step order="6">Add close button and escape key handler</step>
    <step order="7">Update TASK-INDEX.md: change this task's status to completed</step>
  </steps>

  <outputs>
    <output type="code">src/client/pcf/DrillThroughWorkspace/</output>
    <output type="solution">Custom Page definition</output>
  </outputs>

  <acceptance-criteria>
    <criterion testable="true">Custom Page opens as modal from Visual Host</criterion>
    <criterion testable="true">Page receives chartDefinitionId parameter</criterion>
    <criterion testable="true">Page closes with button or escape key</criterion>
  </acceptance-criteria>

  <execution>
    <skill>.claude/skills/task-execute/SKILL.md</skill>
  </execution>
</task>
