<?xml version="1.0" encoding="UTF-8"?>
<task id="011" project="visualization-module">
  <metadata>
    <title>Implement BarChart component</title>
    <phase>2: Core Chart Components</phase>
    <status>not-started</status>
    <estimated-hours>4</estimated-hours>
    <dependencies>004</dependencies>
    <blocks>017, 020</blocks>
    <tags>pcf, react, fluent-ui, frontend</tags>
  </metadata>

  <prompt>
    Build the BarChart component using @fluentui/react-charting. Support both
    vertical and horizontal bar charts with drill-through on bar click.
  </prompt>

  <role>
    React developer with @fluentui/react-charting expertise.
  </role>

  <goal>
    A reusable BarChart component that renders vertical or horizontal bar charts
    using Fluent v9 charting library, with click handlers for drill-through.
  </goal>

  <context>
    <background>
      Bar charts show categorical data like "Matters by Status" or "Invoices by Month".
      Users can click individual bars to drill into the underlying records.
    </background>
    <relevant-files>
      <file>src/client/pcf/VisualHost/control/components/</file>
    </relevant-files>
  </context>

  <constraints>
    <constraint source="spec">MUST use @fluentui/react-charting (VerticalBarChart, HorizontalBarChart)</constraint>
    <constraint source="ADR-021">MUST NOT use Chart.js, Recharts, VisX, or D3</constraint>
    <constraint source="ADR-021">MUST use Fluent design tokens for colors</constraint>
    <constraint source="project">MUST emit DrillInteraction on bar click</constraint>
  </constraints>

  <knowledge>
    <files>
      <file>.claude/adr/ADR-021-fluent-design-system.md</file>
      <file>.claude/patterns/pcf/theme-management.md</file>
      <file>src/client/pcf/CLAUDE.md</file>
    </files>
    <patterns>
      <pattern name="Fluent Charting" location="https://github.com/microsoft/fluentui/tree/master/packages/charts/react-charting">
        Reference Fluent charting library documentation and examples
      </pattern>
    </patterns>
  </knowledge>

  <steps>
    <step order="1">Install @fluentui/react-charting if not already installed</step>
    <step order="2">Create BarChart.tsx component file</step>
    <step order="3">Define props interface (data, orientation, onBarClick)</step>
    <step order="4">Implement using VerticalBarChart or HorizontalBarChart based on props</step>
    <step order="5">Configure color palette from Fluent tokens</step>
    <step order="6">Implement onBarClick that emits DrillInteraction</step>
    <step order="7">Add responsive sizing with useContainerSize hook</step>
    <step order="8">Create Storybook stories (vertical, horizontal, with click)</step>
    <step order="9">Verify dark mode appearance</step>
    <step order="10">Update TASK-INDEX.md: change this task's status to completed</step>
  </steps>

  <tools>
    <tool name="npm">Build and test</tool>
  </tools>

  <outputs>
    <output type="code">src/client/pcf/VisualHost/control/components/BarChart.tsx</output>
    <output type="code">src/client/pcf/VisualHost/stories/BarChart.stories.tsx</output>
  </outputs>

  <acceptance-criteria>
    <criterion testable="true">Component renders vertical bar chart correctly</criterion>
    <criterion testable="true">Component renders horizontal bar chart correctly</criterion>
    <criterion testable="true">Bar click emits DrillInteraction with category value</criterion>
    <criterion testable="true">Uses @fluentui/react-charting (no other charting library)</criterion>
    <criterion testable="true">Component looks correct in dark mode</criterion>
  </acceptance-criteria>

  <execution>
    <skill>.claude/skills/task-execute/SKILL.md</skill>
  </execution>
</task>
