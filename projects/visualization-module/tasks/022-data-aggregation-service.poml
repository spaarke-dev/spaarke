<?xml version="1.0" encoding="UTF-8"?>
<task id="022" project="visualization-module">
  <metadata>
    <title>Implement data aggregation service</title>
    <phase>3: Visual Host PCF Control</phase>
    <status>not-started</status>
    <estimated-hours>4</estimated-hours>
    <dependencies>021</dependencies>
    <blocks>030</blocks>
    <tags>pcf, dataverse, frontend</tags>
  </metadata>

  <prompt>
    Create a service that fetches data from Dataverse views and performs
    client-side aggregation (count, sum, avg, min, max) for chart visualization.
  </prompt>

  <role>
    PCF developer with data processing expertise.
  </role>

  <goal>
    A DataAggregationService that queries Dataverse views and returns
    aggregated data ready for chart rendering.
  </goal>

  <context>
    <background>
      Charts need aggregated data (e.g., count by status, sum by category).
      This service fetches the base view data and performs aggregation.
    </background>
  </context>

  <constraints>
    <constraint source="project">MUST query using sprk_baseviewid FetchXML</constraint>
    <constraint source="project">MUST support all aggregation types (count, sum, avg, min, max)</constraint>
    <constraint source="project">MUST respect Dataverse security context automatically</constraint>
  </constraints>

  <knowledge>
    <files>
      <file>.claude/patterns/pcf/dataverse-queries.md</file>
      <file>src/client/pcf/CLAUDE.md</file>
    </files>
  </knowledge>

  <steps>
    <step order="1">Create DataAggregationService.ts</step>
    <step order="2">Implement fetchViewData(viewId, entityName) using WebAPI</step>
    <step order="3">Implement aggregateData(records, aggregationType, aggregationField, groupByField)</step>
    <step order="4">Return data in chart-friendly format (labels, values)</step>
    <step order="5">Handle pagination for large datasets</step>
    <step order="6">Add caching with configurable TTL</step>
    <step order="7">Create unit tests</step>
    <step order="8">Update TASK-INDEX.md: change this task's status to completed</step>
  </steps>

  <outputs>
    <output type="code">src/client/pcf/VisualHost/control/services/DataAggregationService.ts</output>
    <output type="test">src/client/pcf/VisualHost/control/services/__tests__/DataAggregationService.test.ts</output>
  </outputs>

  <acceptance-criteria>
    <criterion testable="true">Service fetches data from Dataverse view</criterion>
    <criterion testable="true">Aggregation works for all 5 types</criterion>
    <criterion testable="true">Group by field produces correct categories</criterion>
    <criterion testable="true">Handles empty datasets gracefully</criterion>
  </acceptance-criteria>

  <execution>
    <skill>.claude/skills/task-execute/SKILL.md</skill>
  </execution>
</task>
