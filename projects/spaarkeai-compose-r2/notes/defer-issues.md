# Deferrals + Issues — spaarkeai-compose-r2

> Track deferred work + newly-discovered issues in BOTH this file (source of truth) AND GitHub Issues (visibility), kept in sync via `/project-defer-issue-tracking` (alias `/defer`). Every entry MUST name a concrete failure mode (CLAUDE.md §11). `push-to-github` blocks push on entries lacking a GitHub URL.

## Deferrals filed during execution

| ID | Title | GitHub Issue | Root cause | Recommended action | Cost-of-doing-nothing |
|---|---|---|---|---|---|
| DEF-01 | `ComposeEditor` `docxBytes` effect resets `dirty=false` after a transient mount | [#601](https://github.com/spaarke-dev/spaarke/issues/601) | Pre-existing latent mechanism (shared with merged FR-03/task-012 upload path); surfaced in task 010 review. `ComposeEditor.tsx`'s `docxBytes` effect unconditionally calls `onDirtyChange(false)` after mammoth conversion, including for transient (Browse/Upload) mounts. Tests mock `ComposeEditor` so the reset never fires in test. | Patch `ComposeEditor` to skip the dirty-reset when the mount is transient (e.g. `documentRef.speDriveItemId` empty); verify live in a deployed env. | A Browse/Upload transient draft can report `isDirty=false` in production → first Save does not trigger create-on-save (FR-05/task 013) → the draft silently fails to persist. |
| DEF-02 | `searchResolvedDriveId` has no reset path on `loadFailed`→retry-Search within one mount | _(filed at next push)_ | Task 011 review (non-blocking robustness note, not an ADR violation). The new `searchResolvedDriveId` override (FR-02 Search load) is reset on Browse/Upload transient mounts but not on a load-failure→retry-Search sequence in the same mount instance. | ✅ **FIXED 2026-07-11 (UAT round-2 wrap-up).** `handleSearchRequested` now `setSearchResolvedDriveId(null)` up front on every new pick (after the dismiss check, before resolution) so a resolution that fails (retrieveRecord throws / half-provisioned record) leaves no stale override; a successful resolve re-sets the correct drive. Defensive: the in-place re-Search path is not currently reachable via the UI (the error state exposes no Search CTA), so no new behavioral test was contrived — the existing `ComposeWorkspace.search.test.tsx` (incl. the failed-resolution → error case) still passes 116/116. | (was) a stale drive-id could target the wrong drive if a failed Search load is retried in-place. |
| DEF-03 | `Compose:Webhook:SigningKey` + `ClientState` must be provisioned before FR-26 use | [#602](https://github.com/spaarke-dev/spaarke/issues/602) | Task 053 shipped the SPE webhook receiver with fail-closed HMAC + clientState verification (mirrors CommunicationEndpoints); the two config values were out of the task's write boundary. | ✅ **RESOLVED 2026-07-10.** DEV provisioned as direct App Service settings on `spaarke-bff-dev` (`Compose__Webhook__SigningKey` / `__ClientState` / `__NotificationUrl=.../api/compose/webhooks/spe-doc-changed`; crypto-strong generated, values in App Service config only — NOT the repo; mirrors the Communication webhook pattern since the dev KV `sprkspaarkedev-aif-kv` lives in `spe-infrastructure-westus2`). PROD KV-ref placeholders added to `appsettings.template.json` (`Compose:Webhook:*` → secrets `compose-webhook-signingkey`/`compose-webhook-clientstate`). Webhook filter can now validate instead of fail-closed-rejecting. | (was) webhook filter fails closed → return-from-Word change detection never fires (poll fallback worked). |
| — | (014 split, not a defect) Host the parent-association PROMPT UI in the Tier-2c gate dialog | _(follow-on task, per 014 POML split)_ | **UPDATED by task 100 (2026-07-10)**: the `associate(documentId)`-from-save-completion WIRE is now LANDED — `ComposeWorkspace.onCreateOnSaveComplete` fires `useCreateOnSaveAssociation.associate(newSprkDocumentId)` on the transient-create Save (provided via `ComposeLaunchContext` from `ThreePaneShell`), and `documentAssociationWrite` writes the `@odata.bind` with `cleanGuid`. What REMAINS: rendering `CreateOnSaveAssociationPrompt` in the real Tier-2c gate dialog so the user can CHOOSE a parent (the selection surface). Until then `associate()` runs but with no selection (graceful no-op — a standalone Document is valid, Save is never blocked on a parent). | Host `CreateOnSaveAssociationPrompt` in the Tier-2c gate dialog + wire its selection into `useCreateOnSaveAssociation.setAssociation`. | The persist mechanism + cleanGuid bind are proven; the user cannot yet PICK a parent during compose create-on-save (no selection surface hosted). |
| DEF-08 | Assistant "draft a letter" → drafted content never reaches the Compose editor; the open-tab flow opens a **blank** Compose tab and the "paste into Compose" is **hallucinated** (false UI claim) | _(filed at next push)_ | **UAT round-2 (2026-07-10, owner; two screenshots).** Uploaded doc → "summarize" ✅ → "based on summary, write a letter" → letter returned as **inline Assistant prose** (no Compose routing). Then "open this as a document" → `send_workspace_artifact` **did open a Compose tab but EMPTY** (Browse/Search entry-state) — the open-tab flow carries **no content payload**. Then "yes paste it" → agent **hallucinated the paste**: re-dumped the letter into the *Assistant* pane, claimed "The Compose workspace tab is now open. Here is the draft…" while the tab stayed blank. THREE distinct sub-gaps: **(8a)** no capability generates a WorkProduct draft + seeds a Compose tab WITH it; **(8b)** `send_workspace_artifact`→Compose opens the empty shell, no content-seed path; **(8c)** no tool injects text into the Compose editor from the Assistant → "paste" is LLM role-play → **R2-D false-UI-claim** (the exact class FR-34/task 071 D-F3 honest-ack targets; 071 as scoped acks tab-OPEN, not content-render). Side effect: each "open" spawns a new blank Compose tab (no reuse/idempotency). | **(A)** catalog Action ("draft / compose response", WorkProduct disposition) → destination = Compose tab **seeded with the drafted content** (extend the open-Compose-tab flow to carry a content payload, not just open the shell); agent selects on produce-a-document intent — NOT hardcoded trigger words. **(B)** safety-net "Open in Compose" affordance on assistant-authored prose that injects the text into a (reused) Compose tab. **(C)** honest-ack the content RENDER (extend D-F3/071 from tab-open to content-render) so the agent cannot claim "pasted" when it didn't; refuse the hallucinated narration. **Ownership**: catalog row + Compose destination/seed UI + affordance = compose-r2; agent tool-selection + `send_workspace_artifact` content-payload contract + ack semantics = shared with core (ADR-039 tool projection). *(scoping investigation running to pin exact seams + split.)* | The **primary end-to-end story** — chat with the Assistant, then draft the response into the editor — is broken AND actively misleading: the agent asserts it pasted the draft into Compose while Compose is blank. Worse than a dead-end: a truthfulness defect (claims work it did not do). |

| DEF-09 | Toolbar **Draft Alternative** renders its proposed text as an **Assistant message** with **no way to apply it** — the inline redline never materializes in the Compose document (design regression) | _(filed at next push)_ | **UAT round-2 (2026-07-10, owner; screenshot).** In the Compose editor, selected a clause → bubble toolbar → **Draft alternative** → the Assistant pane showed "Drafted an alternative clause. Rationale:… Proposed text: [box]" but the **document was NOT redlined** and there is **no apply/accept affordance**. This VIOLATES the 2026-07-08 design decision (project CLAUDE.md "Decisions Made"): *"Inline redline (Word-style) is the output-staging surface; confirmation in the Assistant; undo/replace via ledger supersession"* — the redline is supposed to appear **in the document** (Flow-5: `compose-draft-alternative` Compose-disposition output → `usePendingRedline`/`materializeComposeDraft` → inline track-change in `ComposeEditor`), with only a **confirmation** in the Assistant. Instead the proposed text lands as Assistant prose with no injection path. Same "unit-green, E2E-inert" class as the 115 wire-loss — the Flow-5 apply/materialize leg is not firing through the deployed dispatch→ledger→editor wire. **CONFIRMED ROOT CAUSE (2026-07-10): session-scope mismatch.** The toolbar dispatch (via `ConversationPane.dispatchComposeAction`, `getSessionId()→chatSessionId`) writes the compose-disposition ledger entry into the **Assistant chat session**, but `ComposeWorkspace.materializeComposeDraftFromLedger` reads `compose-outputs` from its **own document-keyed session** (`state.sessionId`, minted by compose Load keyed by DocumentId+MatterId per tasks 062/102). Two different session ids → materialize finds nothing → `if(!target) return` → no redline, no accept/reject affordance (the affordance EXISTS in `ComposeEditor.tsx` `compose-redline-controls` but `redline.pending` never populates). Wire/115 is fine; `OutputRouter` writes the ledger correctly; the break is purely which session the write vs read target. Secondary: `dispatchComposeAction` also unconditionally renders the FULL draft-alt prose in the Assistant (should be confirmation-only). **Decision (memory model, §6.5, core-seam-adjacent):** should a Compose AI edit belong to the DOCUMENT session (travels with the doc, resumes on reopen — preserves 062/102; recommended) or the CHAT session (unify — simplest but couples doc edit-history to the chat)? | Root-cause why the Compose-disposition draft output is rendered as an Assistant message instead of materializing as an inline redline in the editor; restore Flow-5 (open-editor redline + accept/reject) so the alternative applies to the actual document text. Add a **wire-body** test (dispatch → Compose ledger entry → client materialize → editor redline present) per the 115 process lesson — NOT a stored-ledger-only test. | THE core inline-editing promise of Compose is broken: the AI drafts an alternative but the user cannot get it into the document. Compose degrades to "chat that suggests text you retype by hand." |

## Cross-project dependencies (owned elsewhere, tracked here — NOT a compose-r2 deferral)

| ID | Title | GitHub Issue | Owner | Status | Cost-of-doing-nothing |
|---|---|---|---|---|---|
| XPD-01 | FR-30 memory-governance: dispatched-action / gated `memory.write` capture path + untrusted-origin gate | [#629](https://github.com/spaarke-dev/spaarke/issues/629) | **redesign-r2 (core)** | Handed off 2026-07-10 ([HANDOFF-to-core-fr30-memory-governance-2026-07-10.md](HANDOFF-to-core-fr30-memory-governance-2026-07-10.md)). This is FR-30's **correct end-state** (design.md §8) — core's shipped `memory.write` (057) is chat-only + the poisoning gate was deferred; only core can build the capture facade + gate (ADR-013 / charter §3.4 forbid a Compose fork). Compose task 063 ready to build on delivery. NO interim substitute (061/062 are their own FRs, not FR-30). | AI-derived insights are not durable, recallable, governed memory → FR-30 unmet (no cross-session insight recall; and if built un-gated, memory poisoning). |

## Resolved (fixed, not deferred)

- **DEF-04 / #604** (anchored-annotation HTTP endpoint + client rehydrate) — **FIXED by task 102 (2026-07-10)**. `ComposeEndpoints.cs` now (a) binds `[FromQuery] sessionId + matterId` on Load and routes to `ComposeService.LoadAsync`'s resume branch (gap 4.1), (b) projects `anchoredAnnotations`/`definedTermsTracking`/`actionHistory` onto `LoadComposeDocumentResponse` (gaps 4.2/4.4), and (c) adds `GET/POST /api/compose/sessions/{sessionId}/annotations` → `Get/SaveComposeAnnotationsAsync` (gap 4.3). Client `ComposeWorkspace.tsx` forwards `matterId`, parses the 3 collections, and persists annotations on mutation. Proven E2E by `tests/integration/contract/Api/Ai/ComposeMemoryResumeEndpointContractTests.cs` (WebApplicationFactory: save-annotation → reopen(Load) resumes the SAME session + restores all three collections).
- **DEF-05 / #605** (anchored-annotation Cosmos warm tier) — **FIXED by task 102 (2026-07-10)**. `StoredSession.cs` gained `AnchoredAnnotations` + `DefinedTermsTracking` and `ChatSessionManager` maps them both directions (persist + restore), so annotations survive past the Redis TTL. Action history is a projection over the already-persisted `Outputs` ledger (survives automatically).

## Open architectural escalations (owner/architect decision pending — NOT DEFs)

### Task 013 (FR-05 create-on-save) — BLOCKED on three coupled forks (2026-07-09)
Task 013 as scoped assumes a **server-side** BU→SPE-container primitive that does not exist. Grounding (Step 0) surfaced three coupled forks requiring owner/architect resolution before coding (POML pre-authorized this escalation; spec marks Unresolved Q#2):
- **Fork A — BU→container**: the "same mechanism as matter/project creation" is **client-side** (`resolveBusinessUnitContainerId` reads `businessunit.sprk_containerid`, passes the id into the BFF). BFF has no BU→container resolver by design (multi-container INV-7 + project CLAUDE.md keep it in the wizards). Recommended: client resolves container id and passes it into the Save/promote request (this IS the existing convention — §6.5 path C, pivot-to-comply). Reject a new BFF resolver.
- **Fork B — transient contract**: `SaveAsync` requires an existing `DocumentSpeId`+`DriveId`; a transient Browse/Upload draft (task 010/012 output) has no SPE item. Save must create the drive-item in the resolved container when `DocumentSpeId` is absent. Client half couples to task 010/012.
- **Fork C — profile analysis**: only seam is `IAppOnlyAnalysisService` (`Services/Ai/`, not `PublicContracts/`) — injecting into `Services/Compose` trips the ADR-013 NetArchTest facade rule, and it is app-only/MI (likely 403s reading the OBO-written Compose file). Recommended: new thin `Services/Ai/PublicContracts/IDocumentProfileAi` facade wrapping the stream overload, run under OBO. **New `Services/Ai/PublicContracts/` surface in core's territory → route to core (redesign-r2), not compose-minted.**

Already clean/buildable once A/B/C settle: indexing (`IPostUploadIndexingEnqueuer`, OBO), record step (idempotent), per-step result shape (`JobAwareCompletionState` already names Compose's 4 steps `container → record → profile-analysis → indexing`).

**Awaiting owner decision**: (1) re-scope 013 to A+B backbone (compose-owned) + file C to core, or (2) hold all of 013 until core weighs in on C.

## How to file NEW deferrals

Invoke `/project-defer-issue-tracking` (or `/defer`). Every entry MUST name a concrete failure mode per CLAUDE.md §11. NEVER add an entry only here without filing the corresponding GitHub Issue.
