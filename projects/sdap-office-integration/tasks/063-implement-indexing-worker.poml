<?xml version="1.0" encoding="UTF-8"?>
<task id="063" project="sdap-office-integration">
  <metadata>
    <title>Implement indexing worker</title>
    <phase>5: Background Workers</phase>
    <status>not-started</status>
    <estimated-hours>4</estimated-hours>
    <dependencies>060</dependencies>
    <blocks>065</blocks>
    <tags>bff-api, worker, job, ai</tags>
    <rigor-hint>FULL</rigor-hint>
    <rigor-reason>Worker implementation with RAG index integration</rigor-reason>
  </metadata>

  <prompt>
    Implement the indexing worker that adds documents to the RAG search index
    for AI-powered retrieval. Integrate with Azure AI Search for vector embeddings
    and indexing.
  </prompt>

  <role>
    .NET developer specializing in search indexing. Expert in Azure AI Search
    and RAG patterns.
  </role>

  <goal>
    A worker that indexes documents in Azure AI Search for RAG retrieval with
    proper chunking and embedding generation.
  </goal>

  <context>
    <background>
      Documents can be added to the RAG index for AI-powered search and retrieval.
      The indexing worker chunks the document, generates embeddings, and updates
      the Azure AI Search index.
    </background>
    <relevant-files>
      <file>src/server/api/Sprk.Bff.Api/Workers/Office/IndexingWorker.cs (to create)</file>
      <file>docs/guides/SPAARKE-AI-ARCHITECTURE.md</file>
    </relevant-files>
  </context>

  <constraints>
    <constraint source="ADR-004">MUST update job status at each stage</constraint>
    <constraint source="spec">MUST only run if user enabled index option</constraint>
    <constraint source="spec">MUST use existing RAG infrastructure</constraint>
  </constraints>

  <knowledge>
    <files>
      <file>.claude/constraints/ai.md</file>
      <file>.claude/patterns/api/background-workers.md</file>
      <file>docs/guides/SPAARKE-AI-ARCHITECTURE.md</file>
      <file>projects/sdap-office-integration/spec.md</file>
    </files>
  </knowledge>

  <steps>
    <step order="1">Create IndexingWorker implementing IOfficeJobHandler</step>
    <step order="2">Check if index option was enabled in job payload</step>
    <step order="3">Skip and mark complete if not enabled</step>
    <step order="4">Retrieve document content from SPE</step>
    <step order="5">Chunk document content for embedding</step>
    <step order="6">Generate embeddings via Azure OpenAI</step>
    <step order="7">Index chunks in Azure AI Search</step>
    <step order="8">Update Document with index reference</step>
    <step order="9">Update job status to "indexed"</step>
    <step order="10">Handle indexing errors gracefully</step>
    <step order="11">Write unit tests</step>
    <step order="12">Update TASK-INDEX.md: change this task's status to ✅ completed</step>
  </steps>

  <tools>
    <tool name="dotnet">Build and test</tool>
    <tool name="terminal">Run commands</tool>
  </tools>

  <outputs>
    <output type="code">src/server/api/Sprk.Bff.Api/Workers/Office/IndexingWorker.cs</output>
    <output type="test">tests/Sprk.Bff.Api.Tests/Workers/Office/IndexingWorkerTests.cs</output>
  </outputs>

  <acceptance-criteria>
    <criterion testable="true">Worker skips if index option not enabled</criterion>
    <criterion testable="true">Document is chunked appropriately</criterion>
    <criterion testable="true">Embeddings are generated</criterion>
    <criterion testable="true">Document is indexed in Azure AI Search</criterion>
    <criterion testable="true">Job status is updated to "indexed"</criterion>
  </acceptance-criteria>

  <notes>
    Reuse existing RAG infrastructure for chunking and embedding. The indexing
    worker should be optional - if it fails, the document remains accessible
    but won't be found via AI search.
  </notes>

  <execution>
    <skill>.claude/skills/task-execute/SKILL.md</skill>
    <protocol>
      Before starting this task, load all files listed in knowledge/files.
      Follow the task-execute skill for mandatory pre-execution checklist.
    </protocol>
  </execution>
</task>
