{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://spaarke.com/schemas/analysisaction/compose-make-concise.schema.json",
  "title": "Compose — Make More Concise — Output Schema (compose-make-concise)",
  "description": "Output schema for sprk_analysisaction sprk_actioncode='compose-make-concise' (ai-advanced-capabilities-nda-r1, Contextual AI Tool Library phase 3). MIRRORS VERBATIM the outputSchema baked into infra/dataverse/actions/compose-make-concise.action.json (the systemPrompt/schema author surface). The STRUCTURED-EDIT PAYLOAD that becomes the opaque SessionOutput.Payload written under the `compose` disposition before render (ADR-040). Azure OpenAI Structured Outputs compatible: additionalProperties:false, object-level required array, no anyOf/oneOf/discriminator; match_mode is a closed set via the `enum` keyword. Shares the shape with compose-draft-alternative (both emit a clause redline). spaarkeai-compose-r8 task 052: target_text and match_mode are RETIRED — target_para_id is the targeting field and no placement path searches document text (ADR-049 I-7). spaarkeai-compose-r8 task 052: target_text and match_mode are RETIRED — target_para_id is the targeting field and no placement path searches document text (ADR-049 I-7). spaarkeai-compose-r8 task 052: target_text and match_mode are RETIRED — target_para_id is the targeting field and no placement path searches document text (ADR-049 I-7).",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "target_para_id",
    "new_text",
    "rationale",
    "sources"
  ],
  "$comment-required": "Object-level required array (ALL four fields required). Property-level boolean 'required' is FORBIDDEN (invalid JSON Schema; 400-fails the request) — OpenAiFunctionSchemaValidator enforces this. spaarkeai-compose-r8 task 052 RETIRED target_text and match_mode from this Action: text matching is no longer a placement channel for an AI edit (ADR-049 I-7), so the model is no longer asked for either. target_para_id is the targeting field. It stays NULLABLE via the type union [\"string\",\"null\"] — required as a KEY (Structured Outputs demands it) but legitimately null when the caller supplied no anchor, in which case the edit is REFUSED rather than text-searched.",
  "properties": {
    "target_para_id": {
      "type": [
        "string",
        "null"
      ],
      "description": "The document's own stable Word paragraph identifier (w14:paraId) for the clause this edit targets — copied VERBATIM from the targetParaId input when one was supplied, otherwise null. This is the ONLY placement channel (spaarkeai-compose-r8 task 052, ADR-049 I-7): the editor resolves the paragraph by this identifier and NEVER searches the document text for the edit's wording. A null identifier means the edit cannot be placed and is refused — there is no prose fallback. Never invent, alter, abbreviate, or reformat this value.",
      "maxLength": 64
    },
    "new_text": {
      "type": "string",
      "description": "The concise replacement clause language — self-contained, drafting-ready contract prose (no placeholders, no commentary, no markup), meaning-preserving. The FULL revised text of the paragraph target_para_id names; the editor diffs it against that paragraph's current text and marks only what changed, inserted as a pending track-change.",
      "maxLength": 16000
    },
    "rationale": {
      "type": "string",
      "description": "Concise note on WHAT was condensed plus confirmation legal meaning is unchanged (≤1 short paragraph, ≤1500 chars).",
      "maxLength": 1500
    },
    "sources": {
      "type": "array",
      "description": "Grounding for the edit. Empty array when the retrieval layer supplied no grounding — never fabricated. Usually empty for concision.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "type",
          "id",
          "snippet"
        ],
        "properties": {
          "type": {
            "type": "string",
            "description": "The source kind (e.g. 'playbook', 'precedent', 'clause').",
            "maxLength": 80
          },
          "id": {
            "type": "string",
            "description": "The retrieval-layer identifier for the source — never fabricated."
          },
          "snippet": {
            "type": "string",
            "description": "A SHORT supporting excerpt from the source.",
            "maxLength": 1000
          }
        }
      }
    }
  },
  "$comment-mirror": "Task 052: RE-PROJECTED from the seed's outputSchema (these three mirrors had drifted — they never received task 051's target_para_id). The DEPLOYED source is infra/dataverse/actions/compose-make-concise.action.json (Deploy-AnalysisAction.ps1 reads that file, not this one) — this mirror exists for CI schema validation."
}
